---
phase: quick-260623-9dn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/delete-poll/index.ts
  - src/components/admin/SuggestionKebabMenu.tsx
  - src/components/admin/DeleteSuggestionDialog.tsx
  - src/__tests__/admin/suggestion-crud-edge.test.ts
  - .planning/PROJECT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "An admin can delete a suggestion that has responses (votes) without receiving a 409"
    - "An admin can delete a suggestion at any lifecycle stage: before votes, active with votes, closed"
    - "Deleting a suggestion removes its votes and cached vote_counts with no orphan rows and no FK-violation error"
    - "The admin delete control is enabled regardless of response count"
    - "The confirm dialog warns that all responses will be permanently removed"
    - "Individual-vote deletion is NOT introduced — only whole-suggestion deletion"
    - "Admin gate, UUID validation, poll_deleted audit log, and 404-on-missing all still work"
    - "PROJECT.md Key Decisions documents the D-18 reversal and its VOTE-03 relationship"
  artifacts:
    - path: "supabase/functions/delete-poll/index.ts"
      provides: "Always-allowed admin hard delete (votes EXISTS 409 guard removed)"
    - path: "src/components/admin/SuggestionKebabMenu.tsx"
      provides: "Delete menu item enabled at any stage"
    - path: "src/components/admin/DeleteSuggestionDialog.tsx"
      provides: "Destructive confirm copy warning responses are removed"
  key_links:
    - from: "supabase/functions/delete-poll/index.ts"
      to: "supabase/migrations/00000000000000_schema.sql"
      via: "polls DELETE relies on ON DELETE CASCADE FKs on votes/vote_counts/choices"
      pattern: "from\\(\\s*['\"]polls['\"]\\s*\\)\\s*\\.\\s*delete"
---

<objective>
Reverse the D-18 zero-votes delete lock so admins can delete a suggestion at ANY lifecycle stage — before any votes, while voting is active, or after close. Whole-suggestion deletion only; individual-vote deletion is explicitly out of scope.

Purpose: Tim now requires maximum admin flexibility to remove suggestions outright, accepting the integrity trade-off that responses are destroyed. This is a deliberate, documented reversal — not a silent change.

Output: votes-guard-free Edge Function, enabled admin delete control with destructive confirm copy, updated tests, and a PROJECT.md decision record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@supabase/functions/delete-poll/index.ts
@src/components/admin/SuggestionKebabMenu.tsx
@src/components/admin/DeleteSuggestionDialog.tsx
@src/__tests__/admin/suggestion-crud-edge.test.ts

# FK cascade state determined during planning — NO MIGRATION NEEDED.
# supabase/migrations/00000000000000_schema.sql already defines:
#   votes.poll_id        UUID NOT NULL REFERENCES public.polls(id) ON DELETE CASCADE  (line ~85)
#   vote_counts.poll_id  UUID NOT NULL REFERENCES public.polls(id) ON DELETE CASCADE  (line ~99/115)
#   choices.poll_id      UUID NOT NULL REFERENCES public.polls(id) ON DELETE CASCADE
# Deleting a poll already cascades to votes + vote_counts + choices. No orphans, no FK error.
@supabase/migrations/00000000000000_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the votes EXISTS 409 guard from delete-poll and update its header comment</name>
  <files>supabase/functions/delete-poll/index.ts</files>
  <action>
Delete the votes pre-check block that returns 409 — the section that selects `id` from the `votes` table filtered by `poll_id`, the `voteCheckError` branch, and the `if (voteRow)` block returning the "Cannot delete: responses already received" 409 response. Removing this block makes deletion always-allowed for an authenticated admin.

Leave fully intact, in order: the OPTIONS/method handling, the Authorization-header check, the `getUser` auth check, the `requireAdmin` admin gate, the JSON body parse, the `poll_id` presence check, the UUID-format regex validation, the best-effort pre-DELETE snapshot select on `polls`, the `polls` DELETE with `.single()` and its PGRST116 → 404 branch, the `writeAudit` call recording the `poll_deleted` action, and the success/catch responses.

Rewrite the file's header comment (the lines that currently describe the D-18 server-side delete lock and the EXISTS-on-votes 409 rejection) to describe the new behavior: an authenticated admin may hard-delete a suggestion at any lifecycle stage; the DELETE cascades to votes, vote_counts, and choices via ON DELETE CASCADE FKs. Keep it WHY-only and do NOT cite any plan, phase, round, or decision ID in the source comment (project convention forbids rot tags in src/). Do not reference "D-18" in the comment.

After editing, the `votes` table must no longer be referenced anywhere in this file.
  </action>
  <verify>
    <automated>grep -c "from('votes')" supabase/functions/delete-poll/index.ts | grep -qx 0 &amp;&amp; grep -c "responses already received" supabase/functions/delete-poll/index.ts | grep -qx 0 &amp;&amp; grep -qE "requireAdmin\(" supabase/functions/delete-poll/index.ts &amp;&amp; grep -qE "poll_deleted" supabase/functions/delete-poll/index.ts &amp;&amp; grep -qE "PGRST116" supabase/functions/delete-poll/index.ts &amp;&amp; echo PASS</automated>
  </verify>
  <done>The file has zero references to the votes table and zero "responses already received" 409 text; requireAdmin, poll_deleted audit, UUID regex, and PGRST116 404 all remain; the header comment describes always-allowed deletion with cascade and contains no plan/phase/decision IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Enable the admin delete control at any stage and strengthen the confirm copy</name>
  <files>src/components/admin/SuggestionKebabMenu.tsx, src/components/admin/DeleteSuggestionDialog.tsx</files>
  <action>
In SuggestionKebabMenu.tsx, remove the vote-based gating of the Delete menu item only. Specifically: drop `deleteDisabled` (currently `const deleteDisabled = hasVotes`) and its uses on the Delete `DropdownMenuItem` — remove the `disabled={deleteDisabled}` prop, remove the `aria-describedby={deleteDisabled ? ...}` expression on that item, and remove the conditional reason `<span>` (the "Cannot delete after responses received." block with id `kebab-delete-reason-${pollId}`). The Delete item's `onClick` must unconditionally call `setDeleteOpen(true)` (drop the `!deleteDisabled &&` guard). Keep the Edit item's `editDisabled`/`hasVotes` lock and its reason text UNCHANGED — the edit lock is unrelated and stays. `hasVotes` may remain if still used by Edit; if `hasVotes` becomes unused after this change, remove it too (the build fails on unused locals via noUnusedLocals).

In DeleteSuggestionDialog.tsx, replace the `DialogDescription` copy so it no longer claims "Suggestions with responses cannot be deleted." Make it explicitly warn that deletion is destructive of real response data at any stage: state that this permanently removes the suggestion, all of its choices, AND all responses already received, and that this cannot be undone. Keep the dialog structure, the `useDeletePoll` call, the title, and the Cancel / "Delete permanently" buttons unchanged.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit 2>/dev/null; grep -c "deleteDisabled" src/components/admin/SuggestionKebabMenu.tsx | grep -qx 0 &amp;&amp; grep -c "Cannot delete after responses received" src/components/admin/SuggestionKebabMenu.tsx | grep -qx 0 &amp;&amp; grep -c "cannot be deleted" src/components/admin/DeleteSuggestionDialog.tsx | grep -qx 0 &amp;&amp; grep -qiE "response" src/components/admin/DeleteSuggestionDialog.tsx &amp;&amp; echo PASS</automated>
  </verify>
  <done>SuggestionKebabMenu.tsx has no `deleteDisabled` and no delete-reason text; the Delete item is always clickable; Edit's lock is untouched; DeleteSuggestionDialog.tsx copy warns that all responses are permanently removed and no longer says deletion is blocked when responses exist; `tsc -b` is clean (no unused-local errors).</done>
</task>

<task type="auto">
  <name>Task 3: Update the delete-poll test block and record the D-18 reversal in PROJECT.md</name>
  <files>src/__tests__/admin/suggestion-crud-edge.test.ts, .planning/PROJECT.md</files>
  <action>
In suggestion-crud-edge.test.ts, rewrite the `describe('delete-poll server-side delete lock', ...)` block (around lines 148-180) so it asserts the NEW always-allowed behavior instead of the removed lock. Rename the describe to reflect always-allowed deletion (e.g. "delete-poll always-allowed admin delete"). Keep the assertions that the function exists/non-empty, imports and calls `requireAdmin`, and deletes from the `polls` table. REMOVE the three guard-specific tests: "contains EXISTS guard on votes table", "returns 409 on guard hit", and "vote guard appears BEFORE the delete call". ADD a test asserting the source no longer references the votes table — `expect(src).not.toMatch(/from\(\s*['"]votes['"]\s*\)/)` — and a test asserting no "responses already received" 409 text remains — `expect(src).not.toMatch(/responses already received/)`. Do not touch the other describe blocks in this file (the update-poll edit-lock block still legitimately asserts 409 on its own guard at lines ~64-65 — leave it).

In .planning/PROJECT.md, append a new row to the `## Key Decisions` table documenting the reversal. Decision column: that admins may now delete a suggestion at ANY lifecycle stage (D-18 reversal). Rationale column: Tim requires maximum flexibility and explicitly owns the integrity trade-off — deleting a suggestion permanently removes its responses; note this relaxes the prior zero-votes delete lock while VOTE-03 (no per-vote UPDATE/DELETE via RLS) still holds, so only whole-suggestion deletion is possible, not individual-vote tampering. Outcome column: a dated note for 2026-06-23 that the EF votes-guard was removed and FK ON DELETE CASCADE (already present in schema.sql) cleans up votes + vote_counts. Cite "D-18" and "VOTE-03" literally in the row text so the relationship is explicit. Keep the existing VOTE-03 line in the requirements list (around line 151) unchanged — it still accurately describes RLS on the votes table.
  </action>
  <verify>
    <automated>npm run test -- src/__tests__/admin/suggestion-crud-edge.test.ts --run 2>&amp;1 | grep -qiE "pass|✓" &amp;&amp; grep -qE "D-18" .planning/PROJECT.md &amp;&amp; grep -qE "VOTE-03" .planning/PROJECT.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>The delete-poll test block asserts always-allowed deletion (no votes-guard / 409-on-votes / guard-ordering tests, plus new negative assertions for the votes table and 409 text); the vitest file passes; PROJECT.md Key Decisions has a new row citing both D-18 and VOTE-03 documenting the reversal and integrity trade-off.</done>
</task>

</tasks>

<verification>
- delete-poll EF: no `votes` table reference, no 409 "responses already received"; requireAdmin + UUID regex + poll_deleted audit + PGRST116 404 intact.
- No database migration added (FK cascade already present in schema.sql — verified during planning).
- Admin UI: Delete control enabled at any stage; confirm copy warns responses are permanently removed.
- Tests: `npm run test -- src/__tests__/admin/suggestion-crud-edge.test.ts --run` passes with rewritten delete block.
- `npx tsc -b` clean (no unused-local fallout from removing deleteDisabled/hasVotes).
- PROJECT.md Key Decisions row records the D-18 reversal and VOTE-03 relationship.
- No individual-vote deletion path introduced anywhere.
</verification>

<success_criteria>
An admin can delete a suggestion with existing responses and receive success (not 409); the delete cascades cleanly to votes and vote_counts with no orphans; the admin UI exposes delete at any stage with destructive confirm copy; whole-suggestion deletion only; the reversal is documented in PROJECT.md.
</success_criteria>

<output>
Create `.planning/quick/260623-9dn-admin-delete-any-stage/260623-9dn-SUMMARY.md` when done.
</output>
