---
phase: 19-db-migration-a11y-restore
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
  - 19-02
files_modified: []
autonomous: false
requirements:
  - DBHY-05

must_haves:
  truths:
    - "supabase db push applies Migration 15 to the LOCAL Supabase stack — this is the Phase 19 closure bar; production deploy (--linked push) is a milestone-ship step, not a Phase 19 gate (CONTEXT.md: no users, ships in normal order)"
    - "Zero new Supabase advisor WARNs after the migration — the 0011_function_search_path_mutable WARN does not reappear (lint run against the same target as the push: local stack)"
    - "npm run test:integration is GREEN — profile-trigger-gate.test.ts passes all five+ assertions (3 protected-column rejections, 1 allowed-column sanity, the ordered RPC proof, and the GUC-leak guard)"
    - "submit-vote smoke round-trip remains PASS — no regression from the DB change"
    - "Local-vs-linked command consistency: the push target and the lint target are the SAME — both local (default) for Phase 19 closure; if --linked is used it is used for BOTH push and lint, never mixed"
  artifacts:
    - path: "supabase/migrations/00000000000015_trusted_profile_update_guc.sql"
      provides: "Applied to local DB (apply confirmed)"
      contains: "pg_catalog.set_config('app.trusted_profile_update'"
  key_links:
    - from: "Migration 15 SQL"
      to: "local Supabase Postgres instance"
      via: "supabase db push"
      pattern: "supabase db push"
    - from: "profile-trigger-gate.test.ts"
      to: "public.profiles trigger post-migration"
      via: "npm run test:integration"
      pattern: "profile-trigger-gate"
---

<objective>
Apply Migration 15 to the LOCAL Supabase stack via `supabase db push`, then verify:
1. Zero new Supabase advisor WARNs (the `0011_function_search_path_mutable` WARN must not reappear) — lint run against the SAME target as the push (local)
2. Integration tests GREEN — `profile-trigger-gate.test.ts` proves both gate directions work against the live database (3 protected-column rejections, 1 allowed-column sanity, the ordered RPC proof, the GUC-leak guard)
3. `submit-vote` E2E smoke round-trip remains PASS — no regression from the migration

This is the [BLOCKING] push plan. The migration file was created in Plan 01 (Wave 1). Before running `supabase db push`, the local Supabase stack must be running and environment keys must be exported.

**SC2 / production-deploy scope (review clarification):** ROADMAP SC2 references "deploys to production." Phase 19 CLOSURE proves the migration applies cleanly to the LOCAL stack, lints clean, and passes integration + smoke. The actual production (`--linked`) push is a normal milestone-ship step in the standard deploy order — per CONTEXT.md there are no live users, so no expedited/out-of-band deploy is needed. Phase verification must therefore have a SEPARATE row for "local migration applied" (the Phase 19 gate) vs "prod migration applied" (deferred to milestone ship) and must NOT mark SC2's prod-deploy half satisfied from a local push alone.

Purpose: Prove Migration 15 is applied locally, the security gate works in both directions, and no advisor regressions were introduced.
Output: Locally applied migration + green integration test run + smoke confirmation.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-db-migration-a11y-restore/19-CONTEXT.md
@.planning/phases/19-db-migration-a11y-restore/19-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Start local Supabase stack and export environment keys</name>

  <what-built>Migration 15 SQL file created in Plan 01. Integration test created in Plan 01. This checkpoint ensures the local stack is live and keys are accessible before the push.</what-built>

  <how-to-verify>
  1. Confirm local Supabase stack is running:
     supabase status
     Expected: Shows Studio URL, API URL, DB URL, anon key, service_role key

  2. If NOT running, start it:
     supabase start

  3. Export the stack keys to your shell session:
     export VITE_SUPABASE_URL=$(supabase status --output json | python3 -c "import sys,json; print(json.load(sys.stdin)['API URL'])")
     export VITE_SUPABASE_ANON_KEY=$(supabase status --output json | python3 -c "import sys,json; print(json.load(sys.stdin)['anon key'])")
     export SUPABASE_SERVICE_ROLE_KEY=$(supabase status --output json | python3 -c "import sys,json; print(json.load(sys.stdin)['service_role key'])")

     (Or export them manually from supabase status output — see MEMORY note: "export Supabase stack keys before npm run test:integration")

  4. Verify keys are set:
     echo $VITE_SUPABASE_URL (should start with http://127.0.0.1)
     echo $VITE_SUPABASE_ANON_KEY (should be a long JWT)
  </how-to-verify>

  <resume-signal>Type "stack ready" once supabase status shows running and keys are exported</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push Migration 15 to local stack</name>
  <files></files>

  <read_first>
    supabase/migrations/00000000000015_trusted_profile_update_guc.sql
  </read_first>

  <action>
  Apply Migration 15 to the LOCAL Supabase stack.

  Preferred local-apply command (operates on the running local stack, no project link required):
  supabase migration up
  (If your workflow uses `supabase db push` against the local stack instead, that is acceptable — but the lint in the next step MUST target the SAME stack you pushed to. Do NOT push local and lint --linked, or vice versa: the push target and lint target must match. Production --linked push is NOT part of Phase 19 closure — see objective SC2 note.)

  If the command prompts for confirmation, confirm (type y / yes).

  After the apply completes successfully, run the advisor lint against the SAME (local) target to confirm zero new WARNs:
  supabase db lint --level warning

  (No --linked: lint the LOCAL stack, matching the local apply above. The automated verify below uses the identical local command.)

  Expected output: No WARNs for 0011_function_search_path_mutable (the SET search_path = '' on both functions in Migration 15 prevents this). If any new WARNs appear, investigate before proceeding.
  </action>

  <verify>
    <automated>supabase db lint --level warning 2>&1 | grep -i "WARN\|ERROR" | grep -v "^#" || echo "LINT CLEAN"</automated>
  </verify>

  <acceptance_criteria>
    - supabase migration up (or local supabase db push) exits with code 0 (success)
    - supabase db lint --level warning (LOCAL target — same as the apply) shows no new WARNs — specifically, 0011_function_search_path_mutable does not appear for profile_self_update_allowed or update_profile_after_auth
    - Migration 15 appears in the applied migrations list (supabase migration list)
    - Push target and lint target are the SAME (both local) — no local-push/linked-lint mismatch
  </acceptance_criteria>

  <done>Migration 15 applied to the LOCAL Supabase Postgres instance. Advisor lint (same local target) clean. Production --linked deploy is explicitly deferred to milestone ship.</done>
</task>

<task type="auto">
  <name>Task 3: Run integration tests — verify DBHY-05 gate in both directions</name>
  <files></files>

  <read_first>
    e2e/integration/profile-trigger-gate.test.ts
    vitest.config.integration.ts
  </read_first>

  <action>
  Run the full integration test suite:
  npm run test:integration

  This runs all tests in e2e/integration/ including the new profile-trigger-gate.test.ts.

  Expected results:
  - profile-trigger-gate.test.ts: 5+ PASS — rejection of mfa_verified direct UPDATE, rejection of is_admin direct UPDATE, rejection of guild_member direct UPDATE, allowed-column (discord_username) success, the ordered RPC success path (flip→call→read-back), and the post-RPC GUC-leak guard
  - vote-counts-rls.test.ts: 12 PASS / 0 FAIL (sibling suite — unchanged, but verify not broken by afterAll/beforeEach cleanup in profile-trigger-gate)
  - All other integration tests: PASS

  If profile-trigger-gate.test.ts fails:
  - If any protected-column rejection case (mfa_verified / is_admin / guild_member) returns error=null (direct UPDATE was NOT rejected): the migration apply did not take — check supabase migration list and re-run the local apply
  - If the allowed-column case (discord_username) returns error!=null: the gate is over-broad — the GUC-absent branch is rejecting non-protected columns. Re-read the trigger body; only is_admin/mfa_verified/guild_member should be guarded
  - If the ordered RPC case fails the read-back (post-RPC mfa_verified/guild_member still false): the GUC bypass is broken — check that set_config uses pg_catalog.set_config(..., true) and that the gate is pg_catalog.current_setting(..., true) IS DISTINCT FROM 'on'
  - If the GUC-leak guard case (f) returns error=null: the transaction-local flag leaked across requests — confirm is_local=true (third arg) on set_config
  - If error message does not match the regex: the RAISE EXCEPTION message in the trigger may differ — read the deployed function body via supabase db dump or SELECT prosrc FROM pg_proc WHERE proname = 'profile_self_update_allowed'

  If vote-counts-rls.test.ts fails: check that profile-trigger-gate.test.ts afterAll ran and restored memberUser.mfa_verified=true, guild_member=true
  </action>

  <verify>
    <automated>npm run test:integration 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - npm run test:integration exits with code 0
    - Output shows profile-trigger-gate.test.ts with 5+ passing tests (the three protected-column rejections, the allowed-column sanity, the ordered RPC proof, and the GUC-leak guard)
    - Output shows vote-counts-rls.test.ts with 12 passing tests (0 failures, 0 skips)
    - No integration tests fail or are skipped
  </acceptance_criteria>

  <done>DBHY-05 regression is proven: direct client UPDATE to is_admin/mfa_verified/guild_member is rejected, allowed columns still pass, and the RPC path is proven to actually commit protected columns (ordered flip→call→read-back) with no GUC leak. The migration is functionally correct against the live local stack.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: E2E smoke verification — submit-vote round-trip</name>

  <what-built>Migration 15 applied and integration tests GREEN. This checkpoint verifies the migration does not regress the live user-facing submit-vote flow.</what-built>

  <how-to-verify>
  Run the Playwright smoke suite:
  npx playwright test --config e2e/playwright.config.ts --grep @smoke

  Expected: All @smoke tests PASS. The submit-vote smoke round-trip (which exercises the authenticated profile + vote flow) should remain PASS. Migration 15 only changes the profile update trigger behavior — it does not touch the vote submission path — but this is the required phase gate per ROADMAP.md Success Criterion 2.

  If any @smoke test fails, investigate whether the failure is related to:
  - The profile trigger change (unlikely — submit-vote does not update profiles)
  - An unrelated environmental issue (e.g., local stack state, timing)

  Record the smoke run outcome below.
  </how-to-verify>

  <resume-signal>Type "smoke PASS" if all @smoke tests pass, or describe the failure with the test name and error message</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase db push → local Postgres | Migration SQL is applied by the Postgres superuser; function bodies replace existing SECURITY DEFINER definitions |
| integration test harness → local stack | Vitest + supabase-js connects to local Postgres via the anon/authed/serviceRole keys exported from supabase status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Tampering | supabase migration apply order | accept | Supabase migrations are applied in sequential filename order. Migration 15 filename 00000000000015_* follows 00000000000014_* — correct order guaranteed. No out-of-order risk. |
| T-19-09 | Elevation of Privilege | Profile trigger gate — post-apply correctness | mitigate | Task 3 (npm run test:integration) exercises both gate directions against the live post-migration DB. The ordered RPC case (flip→call→read-back) proves the trusted path actually commits protected columns rather than passing as a no-op; the three protected-column rejection cases prove the gate fires; the GUC-leak guard proves the transaction-local flag does not persist. This is the regression proof required by ROADMAP.md Success Criterion 1. |
| T-19-10 | Denial of Service | supabase db lint WARN regression | mitigate | Task 2 runs supabase db lint against the SAME (local) target as the apply — no local-apply/linked-lint mismatch (review fix). Zero new WARNs required per ROADMAP.md Success Criterion 2. The SET search_path = '' on both functions in Migration 15 prevents 0011_function_search_path_mutable from reappearing. |
| T-19-11 | Repudiation | SC2 prod-deploy closure tracking | accept | Phase 19 proves LOCAL apply + lint + integration + smoke. The production (--linked) deploy is a normal milestone-ship step (CONTEXT.md: no live users, ships in standard order). The verification checklist below has separate "local migration applied" (Phase 19 gate) and "prod migration applied" (deferred) rows so closure does not silently claim the prod half of SC2 from a local apply. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan. |
</threat_model>

<verification>
Phase 19 gate (all must be TRUE before /gsd:verify-work):

1. LOCAL migration applied: supabase migration list shows 00000000000015_trusted_profile_update_guc.sql as applied to the local stack
2. supabase db lint --level warning (LOCAL target, same as the apply) — zero new WARNs (especially no 0011_function_search_path_mutable for the two updated functions)
3. npm run test:integration — 5+ PASS in profile-trigger-gate.test.ts (3 protected-column rejections + allowed-column sanity + ordered RPC proof + GUC-leak guard) + 12 PASS in vote-counts-rls.test.ts
4. npm run test — unit suite GREEN (UIDN-06 heading assertions pass, full unit suite clean)
5. @smoke Playwright tests PASS — submit-vote round-trip unaffected
6. Migration 15 SQL review-hardening present: null-safe IS DISTINCT FROM 'on' gate, pg_catalog-qualified built-ins, explicit REVOKE/GRANT EXECUTE on update_profile_after_auth, and the trust-boundary COMMENT documenting the caller-supplied-arg residual (T-19-07)
7. DBHY-05 success criteria:
   - SC1: protected-column branch proven reachable and correct via the regression test (ordered RPC proof + three rejections)
   - SC2 (local half — Phase 19 gate): zero new advisor WARNs + submit-vote smoke PASS against the local stack
   - SC2 (prod half — DEFERRED): production --linked deploy is a milestone-ship step (CONTEXT.md: no users, normal order). Do NOT mark this row satisfied from a local apply.
8. UIDN-06 success criterion satisfied: <h2> headings restored, accessibility assertion passes

Closure-tracking rows (keep separate — review fix):
| Item | Phase 19 status |
|------|-----------------|
| Local migration applied | REQUIRED for Phase 19 closure |
| Prod (--linked) migration applied | DEFERRED to milestone ship — not a Phase 19 gate |
</verification>

<success_criteria>
- Migration 15 applied to the LOCAL Supabase stack (prod --linked deploy deferred to milestone ship)
- Zero new Supabase advisor WARNs post-migration (lint target = apply target = local)
- npm run test:integration GREEN — 5+/5+ DBHY-05 gate assertions PASS (incl. ordered RPC proof + GUC-leak guard)
- npm run test GREEN — UIDN-06 heading unit tests PASS
- @smoke E2E suite PASS — submit-vote round-trip unaffected
- Phase 19 ROADMAP success criteria 1, 2 (local half), 3 satisfied; SC2 prod-deploy half explicitly tracked as deferred
</success_criteria>

<output>
Create .planning/phases/19-db-migration-a11y-restore/19-03-SUMMARY.md when done
</output>
