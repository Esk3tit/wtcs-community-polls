---
phase: 21-dependency-hygiene
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified: []
autonomous: false
requirements: [DEP-01]
user_setup: []

must_haves:
  truths:
    - "PR #44 has been rebased onto current main via @dependabot rebase — the stale commit 0ddb157 is no longer the branch tip (D-01: rebase + re-run CI before judging the stale failure, keep the full 18-package group)"
    - "The post-rebase head SHA is confirmed different from the pre-rebase head SHA — headRefOid changed before any CI judgment was made, proving the rebase force-push actually landed (D-01: do not judge on the stale run)"
    - "CI has re-run on the rebased branch and all three jobs (lint-and-unit, test-integration, e2e) are green, and that green statusCheckRollup is bound to the NEW post-rebase head SHA — not the stale run 26865651805 nor any pre-rebase commit (D-01)"
    - "PR #44 work only begins after PR #34 is merged (D-02 sequencing: this plan is Wave 2, depends_on the #34 plan)"
    - "Local npm run build succeeds on the rebased branch, including node scripts/verify-sourcemap-names.mjs passing (D-03 validation bar: CI + local build/smoke)"
    - "Local app smoke confirms the app loads and the auth/suggestion list flows appear intact (D-03)"
    - "All configured review bots (CodeRabbit, gemini-code-assist, greptile) have reviewed PR #44, and each bot's review was submitted against the current post-rebase head SHA — reviews on the stale pre-rebase commit do NOT satisfy the merge gate (D-03 validation bar binds review freshness to the final head)"
    - "Immediately before merge, headRefOid is re-read and matches the SHA that was validated and bot-reviewed — no new commit landed between operator approval and merge (D-03)"
    - "Operator has explicitly approved the merge"
    - "PR #44 is merged to main — gh pr view 44 --json mergedAt is non-null"
    - "gh pr list --author app/dependabot --state open returns empty (no v1 carry-forward PRs remain)"
  artifacts:
    - path: "package.json"
      provides: "18-package minor+patch group bumped by dependabot"
      contains: "@tanstack/react-router"
    - path: "package-lock.json"
      provides: "Lockfile updated for all 18 bumped packages"
  key_links:
    - from: "dependabot/npm_and_yarn/minor-and-patch-6029210c9a branch"
      to: "main HEAD (post Phases 19/20)"
      via: "@dependabot rebase comment"
      pattern: "@dependabot rebase"
    - from: "vite.config.ts rolldownOptions.output.keepNames: true"
      to: "scripts/verify-sourcemap-names.mjs"
      via: "CI lint-and-unit job / local npm run build"
      pattern: "keepNames"
---

<objective>
Rebase, validate, and merge dependabot PR #44 — the 18-package minor+patch group — closing DEP-01.

Purpose: The PR's CI is currently red on a STALE run (commit 0ddb157, 82 commits behind main, from 2026-06-03 — before Phases 19 and 20 merged). The failure is definitively NOT caused by the 18 bumped packages: it is the verify-sourcemap-names.mjs check failing because the stale branch lacks the Phase 16 vite.config.ts keepNames:true change. Rebasing onto current main is expected to clear the failure entirely. Per D-01, we do NOT judge the merge on the stale CI run — we rebase first, then evaluate. This plan depends on Plan 01 (PR #34 merged first, per D-02 sequencing).

Output: PR #44 merged to main; DEP-01 closed; zero open dependabot PRs remain in the v1 carry-forward list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-dependency-hygiene/21-CONTEXT.md
@.planning/phases/21-dependency-hygiene/21-RESEARCH.md
@.planning/phases/21-dependency-hygiene/21-VALIDATION.md
@.planning/phases/21-dependency-hygiene/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger dependabot rebase and wait for CI on the rebased branch</name>
  <files></files>
  <read_first>
    - Run `gh pr view 44` to confirm current PR state, base commit, branch name (dependabot/npm_and_yarn/minor-and-patch-6029210c9a), and that it is still open
    - Run `gh pr view 44 --json author,headRefName,headRepositoryOwner,baseRefName,headRefOid,mergeStateStatus` to capture the pre-rebase head SHA and confirm the PR author is `app/dependabot` and the head repo owner is the expected fork before any rebase/checkout
    - Run `gh pr checks 44` to observe the current (stale) CI state before rebasing — note the stale run timestamp (2026-06-03) as evidence this run predates Phases 19/20
    - Read `.planning/phases/21-dependency-hygiene/21-RESEARCH.md` sections "Root Cause of Stale CI Failure" and "Rebase + Re-Run Mechanics" for the full diagnosis and rebase mechanics
    - Read `.planning/phases/21-dependency-hygiene/21-RESEARCH.md` section "Root-Cause Hypotheses If #44 CI Is STILL Red After Rebase" for the D-04 fallback diagnosis protocol
  </read_first>
  <action>
    Step 1 — Confirm Plan 01 completed: Verify `gh pr view 34 --json mergedAt` is non-null (PR #34 merged). If not, stop — Plan 01 must complete first per D-02 sequencing.

    Step 1b — Capture the pre-rebase head SHA: Record `OLD_SHA=$(gh pr view 44 --json headRefOid -q .headRefOid)`. This is the stale commit (expected to be 0ddb157 or its current equivalent). Every CI/freshness judgment below is bound to a head SHA that must differ from OLD_SHA — this prevents `gh pr checks --watch` from latching onto the pre-rebase run.

    Step 2 — Trigger dependabot rebase: Run `gh pr comment 44 --body "@dependabot rebase"`. Dependabot will react with a thumbs-up and force-push a rebased branch within a few minutes (up to 10 minutes if dependabot is busy). Do NOT run `gh run rerun 26865651805` — that re-runs against the same stale commit and will still fail.

    Step 3 — Wait for the head SHA to change (rebase landed), THEN watch CI bound to the new SHA:
      Step 3a — Poll `gh pr view 44 --json headRefOid -q .headRefOid` every ~30s until it returns a value different from OLD_SHA. Record this as `NEW_SHA`. Do NOT evaluate any CI result until headRefOid != OLD_SHA — a green `gh pr checks 44` reading before the head changes is the STALE run and must be ignored. If headRefOid has not changed after 15 minutes, treat the rebase as not-triggered and use the Manual fallback for rebase below.
      Step 3b — Only after NEW_SHA is confirmed, watch CI for that commit: run `gh pr checks 44 --watch` (the visible head is now NEW_SHA) and/or poll `gh run list --branch dependabot/npm_and_yarn/minor-and-patch-6029210c9a --limit 3`. Then confirm the result deterministically against the exact commit SHA (not the PR's "current head", which can race): run `gh api repos/Esk3tit/wtcs-community-polls/commits/$NEW_SHA/check-runs --jq '.check_runs[] | {name: .name, status: .status, conclusion: .conclusion}'`. This returns the check-runs bound to NEW_SHA itself — verify lint-and-unit, test-integration, and e2e each show `status: completed` / `conclusion: success`. This SHA-pinned API query is the authoritative green signal; `gh pr view --json statusCheckRollup` is a convenience cross-check only. Confirm none of the results trace to OLD_SHA or the stale run 26865651805.

    Step 4 — Evaluate CI result (bound to NEW_SHA):

      CASE A — All three jobs (lint-and-unit, test-integration, e2e) are green AND the green statusCheckRollup is bound to NEW_SHA (not OLD_SHA, not run 26865651805):
        Proceed to Step 4b (post-rebase drift check) then Step 5. This is the expected outcome per RESEARCH.md.

      CASE B — CI is STILL red after rebase (on NEW_SHA):
        Diagnose the root cause using the ordered hypotheses from RESEARCH.md:

        Hypothesis 1 (HIGH PROBABILITY — residual sourcemap names): Check out the rebased branch locally (`git fetch origin && git checkout dependabot/npm_and_yarn/minor-and-patch-6029210c9a && npm ci`). Run `npm run build 2>&1` and `node scripts/verify-sourcemap-names.mjs`. If verify-sourcemap-names.mjs still fails with different names missing, update the allowlist in `scripts/verify-sourcemap-names.mjs` only for names that were genuinely renamed in the new package version (e.g., if a @tanstack/react-router 1.170.x export changed its function name). Push the fix to the dependabot branch (`git push --force-with-lease origin dependabot/npm_and_yarn/minor-and-patch-6029210c9a`) and wait for CI to re-run.

        Hypothesis 2 (LOW — tsc type drift from @types/react 19.2.16): Run `npm ci && tsc -b --noEmit 2>&1 | head -40`. If TypeScript errors appear, apply a targeted type annotation fix on-branch — do not pin @types/react. Push and wait for CI.

        Hypothesis 3 (LOW — eslint rule change): Run `npm run lint 2>&1 | head -40`. Fix any lint violation on-branch (fix the code, not suppress with eslint-disable unless genuinely wrong rule). Push and wait for CI.

        Hypothesis 4 (VERY LOW — unit test failure from supabase-js 2.107.0): Run `npm test -- --run 2>&1 | grep -E "FAIL|failed"`. If auth-related tests fail, update test mocks to match the new client behavior (the public API is unchanged — only internal mutex changed).

        D-04 isolate-and-flag path: If a specific package in the #44 group genuinely cannot be made green on-branch (a real breaking change that is not quickly fixable), isolate just that package: edit package.json on the dependabot branch to revert only that one package's version, run npm install to update the lockfile, push, and wait for CI. If CI then goes green, proceed to merge the reduced group. FLAG the isolated package to the operator at the merge checkpoint — report: the package name, the from/to versions, why it was isolated, and what would be needed to re-integrate it. Never silently drop a package and never abandon the whole group.

    Step 4b — Post-rebase package/lockfile drift check (bind the diff to NEW_SHA): Dependabot's rebase can advance package/lockfile versions beyond the originally-reviewed diff. Check out the rebased head locally and diff against main: `git fetch origin && git checkout dependabot/npm_and_yarn/minor-and-patch-6029210c9a && git diff origin/main...HEAD -- package.json package-lock.json`. Use this local `git diff` as the authoritative check — the `gh pr diff 44 -- <paths>` pathspec form is unreliable across gh versions and can silently return nothing. Confirm the bumped set is still exactly the intended 18-package minor+patch group (the versions listed in the Task 3 what-built block). Record any version drift (a package now at a higher patch than originally reviewed, or a package added/removed) and FLAG it to the operator at the Task 3 merge checkpoint. Do not silently accept drift.

    Step 5 — Confirm @dependabot rebase succeeded: After CI goes green on NEW_SHA, run `gh pr view 44` and confirm the branch is no longer based on commit 0ddb157 (the base should now be current main HEAD or close to it) and that headRefOid still equals NEW_SHA. Run `gh pr checks 44` to snapshot the green state for NEW_SHA.

    Manual fallback for rebase (Option B from RESEARCH.md): If `@dependabot rebase` does not change headRefOid within 15 minutes or reports a conflict, perform a manual rebase. First re-verify the PR author is `app/dependabot` and the branch is `dependabot/npm_and_yarn/minor-and-patch-6029210c9a` (re-run the `gh pr view 44 --json author,headRefName,headRepositoryOwner` from read_first). Then: `git fetch origin dependabot/npm_and_yarn/minor-and-patch-6029210c9a`, `git checkout dependabot/npm_and_yarn/minor-and-patch-6029210c9a`, `git rebase origin/main`. If there are conflicts (expected only in package-lock.json), resolve mechanically by re-running `npm install` (never hand-edit package-lock.json beyond npm's output), then `git add package-lock.json`, `git rebase --continue`. Before pushing, run `git diff origin/main...HEAD -- package.json package-lock.json` and confirm the diff is exactly the intended 18-package set plus the mechanical lockfile regeneration — no executor-authored code changes. Push with `git push --force-with-lease origin dependabot/npm_and_yarn/minor-and-patch-6029210c9a`. GitHub Actions CI will auto-trigger on the force-push; capture the resulting commit as NEW_SHA and bind CI evaluation to it as in Step 3b. If CI does NOT auto-trigger after the force-push, fall back to an empty commit (`git commit --allow-empty -m "ci: re-trigger"` then push) or close/reopen the PR to force a fresh run.
  </action>
  <verify>
    <automated>gh pr checks 44</automated>
  </verify>
  <acceptance_criteria>
    - `OLD_SHA` (pre-rebase headRefOid) was captured before the `@dependabot rebase` comment, and `gh pr view 44 --json headRefOid` now returns a `NEW_SHA` that differs from `OLD_SHA` (the rebase force-push landed; the head actually changed)
    - The green CI result is bound to NEW_SHA via the SHA-pinned API query `gh api repos/Esk3tit/wtcs-community-polls/commits/$NEW_SHA/check-runs`: lint-and-unit, test-integration, and e2e each show conclusion: success for the NEW_SHA commit — NOT the stale run 26865651805 and NOT OLD_SHA
    - The rebased branch is no longer based on commit 0ddb157 (the stale pre-Phase-16 commit)
    - Post-rebase drift check ran: `git diff origin/main...HEAD -- package.json package-lock.json` (on the checked-out rebased head) confirms exactly the intended 18-package minor+patch set; any version drift beyond the originally-reviewed diff is recorded for the operator checkpoint
    - If any D-04 fix-forward was applied on-branch, the specific fix is documented (what was changed, why) in the task output for the operator to review at the merge checkpoint
    - If D-04 isolate-and-flag was invoked, the isolated package name and reason are recorded for the operator checkpoint
  </acceptance_criteria>
  <done>CI is green on the rebased PR #44 branch and bound to the new post-rebase head SHA (or D-04 fix-forward applied and CI green on the new head). Ready for local validation in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Local validation of PR #44 (D-03 bar)</name>
  <files></files>
  <read_first>
    - Read `.planning/phases/21-dependency-hygiene/21-RESEARCH.md` section "Merge Protocol (D-03 Validation Bar)" for the PR #44 checklist
    - Run `gh pr view 44 --json headRefOid,reviews` to capture the current post-rebase head SHA (NEW_SHA from Task 1) AND the bot review list with each review's commit and submission time — this is the basis for the post-rebase bot re-review check below
    - Confirm Task 1 completed with CI green bound to NEW_SHA — if CI is not green on the new head SHA, do not proceed here
  </read_first>
  <action>
    Step 1 — Check out the rebased branch and install: `git fetch origin && git checkout dependabot/npm_and_yarn/minor-and-patch-6029210c9a && npm ci`. This installs all 18 bumped packages.

    Step 2 — Local build smoke (D-03): Run `npm run build`. Must complete with exit code 0. The build output should be in `dist/`.

    Step 3 — Sourcemap verification (D-03): Run `node scripts/verify-sourcemap-names.mjs`. Must pass (exit 0, no "FAIL" lines). This verifies the 5 function names (ConsentProvider, ConsentBanner, AuthProvider, RootLayout, AppErrorFallback) are present in the dist bundle — confirming that keepNames:true is effective post-rebase.

    Step 4 — Local app smoke (D-03): Run `npm run preview` (serves the built dist/) or `npm run dev`. Confirm the dev/preview server starts without crash. The operator will do a browser-level visual check at the merge checkpoint; the executor confirms the server starts and emits no startup errors. After confirming startup, stop the server.

    Step 5 — Return to main: `git checkout main`.

    Step 6 — Confirm bot reviews are against the POST-REBASE head SHA (not stale): Re-read NEW_SHA via `gh pr view 44 --json headRefOid -q .headRefOid`. Then extract each review's bound commit deterministically with the REST reviews endpoint, which exposes `commit_id` per review (the `gh pr view --json reviews` shape does NOT reliably include the commit OID): `gh api repos/Esk3tit/wtcs-community-polls/pulls/44/reviews --jq '.[] | {user: .user.login, state: .state, commit_id: .commit_id, submitted_at: .submitted_at}'`. For each required bot (CodeRabbit, gemini-code-assist, greptile), confirm it has a review whose `commit_id` equals NEW_SHA (i.e., submitted against the rebased head, not the stale pre-rebase commit). A bot review on the pre-rebase commit does NOT count — the force-push invalidated it. If a required bot has only a stale-commit review (or none), re-request the review (e.g., comment to re-trigger, or wait for the bot to auto-review the new head) and re-check until all three bots have a review bound to NEW_SHA. If `commit_id` is null for a given bot, fall back to confirming the review's `submitted_at` is after the rebase force-push timestamp recorded in Task 1. If a bot flags the supabase-js 2.107.0 navigator.locks mutex removal as requiring additional auth testing, note this in the operator checkpoint summary so the operator can verify the Discord sign-in flow during their smoke check.

    Step 7 — Confirm zero remaining dependabot PRs: Run `gh pr list --author app/dependabot --state open`. After PR #34 is already merged, PR #44 is the only remaining v1 carry-forward item. This check previews the post-merge state.

    Note on supabase CLI devDep vs CI pin: The package.json bumps `supabase` devDep from 2.102.0 to 2.104.0. CI's test-integration and e2e jobs use `supabase/setup-cli@v2` pinned to `version: 2.102.0` — these are independent. The devDep bump does not affect the CI CLI version. No CI config change is needed.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-sourcemap-names.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 on the checked-out dependabot branch
    - `node scripts/verify-sourcemap-names.mjs` exits 0 (no missing names reported)
    - Dev/preview server starts without startup errors
    - `gh api repos/Esk3tit/wtcs-community-polls/pulls/44/reviews --jq '.[] | {user: .user.login, commit_id, submitted_at}'` confirms CodeRabbit, gemini-code-assist, and greptile each have a review bound to the post-rebase NEW_SHA (review commit_id == NEW_SHA, or submitted_at after the rebase force-push) — stale pre-rebase reviews do NOT satisfy this criterion
    - If any bot raised concerns about supabase-js 2.107.0, those concerns are noted for the operator checkpoint
  </acceptance_criteria>
  <done>Local validation passes (build + sourcemap check + app starts). All three bots have reviewed the post-rebase head SHA. Ready for operator approval and merge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator approval and merge PR #44 (DEP-01)</name>
  <read_first>
    - Review `gh pr view 44` output (title, diff summary, reviews, current CI state)
    - Review `gh pr checks 44` output to confirm CI state at time of this checkpoint
    - If D-04 isolate-and-flag was triggered in Task 1, present the flagged package and its reason here
  </read_first>
  <what-built>
    Tasks 1 and 2 confirmed: PR #44 rebased onto current main (post Phases 19/20); CI green on all three jobs (lint-and-unit, test-integration, e2e) on the rebased branch; npm run build succeeds locally; node scripts/verify-sourcemap-names.mjs passes (confirms the stale CI failure was entirely due to the pre-Phase-16 base commit, not the 18 bumped packages); local dev/preview server starts; all three review bots have reviewed.

    PR #44 bumps 18 packages (minor+patch only): @tanstack/react-router 1.169.2→1.170.11, @supabase/supabase-js 2.105.4→2.107.0, react/react-dom 19.2.6→19.2.7, @types/react 19.2.14→19.2.16, @sentry/react 10.53.1→10.56.0, posthog-js 1.373.4→1.379.0, lucide-react 1.14.0→1.17.0, and related tooling bumps. All are minor/patch releases with no identified breaking changes.

    If D-04 was triggered: [Report isolated package name, from/to versions, and reason here. State that the remaining packages were validated and are being merged without the isolated package.]
  </what-built>
  <how-to-verify>
    1. Review bot feedback and the PR diff on PR #44 at: https://github.com/Esk3tit/wtcs-community-polls/pull/44
    2. Confirm CI checks are green (GitHub PR page or `gh pr checks 44`)
    3. Optionally: open http://localhost:5173 (npm run dev on the branch) and verify:
       - App loads without console errors
       - Discord sign-in button appears and the OAuth redirect works (supabase-js 2.107.0 auth flow)
       - Suggestion list renders
    4. If D-04 was triggered: review the isolated package flagged by Task 1 and decide if deferral is acceptable
    5. If satisfied, type "approved" to proceed with merge
    6. If any issue is found, describe it and the executor will investigate before merging
  </how-to-verify>
  <action>
    After operator types "approved": first re-read `MERGE_SHA=$(gh pr view 44 --json headRefOid -q .headRefOid)` and confirm it still equals the NEW_SHA that was CI-validated (Task 1) and bot-reviewed against (Task 2). If MERGE_SHA differs (dependabot or a security bump pushed a new commit between approval and merge), STOP — do not merge; return to Task 1 to re-validate and re-review the new head SHA, then re-seek operator approval.

    If MERGE_SHA matches, merge bound to that SHA. First check gh's capability: `gh --version`. If gh >= 2.30.0 (supports `--match-head-commit`), run `gh pr merge 44 --merge --match-head-commit "$MERGE_SHA"` — GitHub rejects the merge if the head moved. If gh < 2.30.0 (no `--match-head-commit`), do NOT fall back to a bare `gh pr merge` (it has no atomic head guard); instead use the REST merge endpoint with an explicit expected SHA, which carries the same guard semantics: `gh api -X PUT repos/Esk3tit/wtcs-community-polls/pulls/44/merge -f merge_method=merge -f sha="$MERGE_SHA"` (GitHub returns 409 Conflict and refuses the merge if the head is no longer `$MERGE_SHA`). After merge completes:
    - Run `gh pr view 44 --json mergedAt` to confirm the merge timestamp is non-null
    - Run `git checkout main && git pull origin main` to bring local main up to date
    - Run `gh pr list --author app/dependabot --state open` to confirm the v1 carry-forward list is now empty (Success Criterion 3 satisfied)
    - Report the final state: both PR #34 and PR #44 merged, zero open dependabot PRs, DEP-01 and DEP-02 closed
  </action>
  <resume-signal>Type "approved" to proceed with merge, or describe any issue found during review. If D-04 was triggered, confirm you accept the isolated package deferral or ask to re-investigate.</resume-signal>
  <acceptance_criteria>
    - Operator has explicitly typed "approved" (PR merge gate satisfied)
    - Immediately before merge, headRefOid was re-read and matched the NEW_SHA validated in Task 1 and bot-reviewed in Task 2 (no commit landed between approval and merge); if it differed, the merge was aborted and re-validation occurred
    - The merge is bound to the validated SHA: `gh pr merge 44 --merge --match-head-commit "$MERGE_SHA"` (gh >= 2.30.0) or the REST fallback `gh api -X PUT repos/Esk3tit/wtcs-community-polls/pulls/44/merge -f merge_method=merge -f sha="$MERGE_SHA"` (gh < 2.30.0) exits 0 — a bare unguarded `gh pr merge` is NOT used
    - `gh pr view 44 --json mergedAt` returns a non-null timestamp
    - `git pull origin main` succeeds — local main reflects the merged PR #44
    - `gh pr list --author app/dependabot --state open` returns empty (no v1 carry-forward PRs remain)
  </acceptance_criteria>
  <done>PR #44 is merged. DEP-01 is closed. All 18 packages bumped to latest minor/patch on main. Zero open dependabot PRs remain for the v1 carry-forward list. Phase 21 Dependency Hygiene is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json / lockfile | 18 packages resolved from the npm registry via dependabot's automated bump |
| dependabot branch → main | PR diff authored by GitHub's dependabot bot; merge introduces updated lockfile state for all 18 packages |
| GitHub Actions CI → build output | CI runs lint/typecheck/unit/e2e against the bumped lockfile; acts as behavioral tripwire |
| scripts/verify-sourcemap-names.mjs → dist/ | Post-build script verifies function names present in the bundle; confirms keepNames integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Tampering | npm registry — any of the 18 bumped packages containing malicious code | mitigate | All 18 packages are existing project dependencies with long npm registry history, bumped by official GitHub dependabot (not manual edits). RESEARCH.md Package Legitimacy Audit cleared all packages. CI (lint-and-unit + test-integration + e2e) runs a full behavioral test suite against the bumped deps — acts as a tripwire for any injected malicious behavior. |
| T-21-06 | Tampering | Transitive dependency injection via package-lock.json — a new transitive dep introduced by a bumped package contains a supply-chain compromise | mitigate | The PR diff (package-lock.json) is reviewed by CodeRabbit/gemini/greptile before operator approval. CI running `npm ci` against the locked manifest + full test suite provides behavioral coverage. Operator reviews PR diff before explicitly approving. |
| T-21-07 | Tampering | Merging a PR not actually authored by dependabot — e.g., a social-engineering attack substituting a malicious PR | mitigate | Verify PR author is `app/dependabot` via `gh pr view 44 --json author` before triggering rebase. Operator reviews the PR at GitHub before approving merge — PR #44 is the known existing dependabot PR. |
| T-21-08 | Information Disclosure | supabase-js 2.107.0 navigator.locks removal — new session management could inadvertently expose session tokens differently | accept | The change is internal to the SDK (commit guard replaces navigator.locks mutex). The public auth API (onAuthStateChange, signInWithOAuth) is unchanged. RESEARCH.md changelog analysis confirms no auth token exposure risk. The local app smoke + D-03 validation includes verifying the auth flow appears intact. Risk is LOW; accepted. |
| T-21-09 | Denial of Service | A bumped package introduces a performance regression (e.g., @tanstack/react-router 1.170.x) that degrades the app | accept | All bumps are minor/patch. The 403 unit tests passed in the stale CI run even with these bumps. Local build smoke + app smoke verify the app remains functional. Perf regression in a patch bump is extremely low probability; accepted. |
| T-21-10 | Tampering | Force-push to dependabot branch (Option B manual rebase) replacing the dependabot-authored diff with executor-authored content | mitigate | Option B (manual rebase) is a fallback used ONLY if @dependabot rebase fails. If used: the rebase is purely mechanical (git rebase origin/main, npm install for lockfile, no code changes), the pre-push diff is inspected (`git diff origin/main...HEAD`) to confirm no executor-authored code, and the PR diff is reviewed by bots and the operator before merge. Force-push uses --force-with-lease (not --force) to prevent overwriting concurrent pushes. |
| T-21-11 | Tampering | Stale-head approval — a green CI run or a bot review against the pre-rebase commit (or a new commit pushed between approval and merge) satisfies the merge gate, allowing an unreviewed/unvalidated dependency diff to merge | mitigate | All CI judgment is bound to NEW_SHA (headRefOid polled until it differs from OLD_SHA before any CI is evaluated; statusCheckRollup cross-checked against NEW_SHA). Each required bot review must be bound to NEW_SHA (review commit_id == NEW_SHA, else re-requested). headRefOid is re-read immediately before merge and the merge uses `--match-head-commit <NEW_SHA>` (or aborts on drift). This closes the dependabot-force-push race so no stale-commit CI or review can satisfy the gate. |
| T-21-SC | Tampering | npm/pip/cargo installs introduce compromised package | mitigate | All packages cleared in RESEARCH.md Package Legitimacy Audit. Dependabot-sourced PR only; no manual version edits. Operator merge gate is blocking (autonomous: false). CodeRabbit/gemini/greptile review lockfile diff before merge. |
</threat_model>

<verification>
Run these checks after all tasks complete:
1. `gh pr view 44 --json mergedAt` — must return non-null timestamp
2. `gh pr view 34 --json mergedAt` — must return non-null timestamp (from Plan 01)
3. `gh pr list --author app/dependabot --state open` — must return empty (zero v1 carry-forward PRs)
4. `git log --oneline -5` on main — should show both merge commits (#34 and #44)
5. Head-SHA freshness audit (from the task record): the merged commit SHA equals the NEW_SHA that (a) differed from the pre-rebase OLD_SHA, (b) carried the green statusCheckRollup, and (c) all three review bots reviewed — confirming no stale-commit CI run or stale bot review satisfied the gate
</verification>

<success_criteria>
- PR #44 is merged to main (gh pr view 44 --json mergedAt is non-null)
- DEP-01 is closed: 18-package minor+patch group on main (supersedes #40 per DEP-01 "equivalent up-to-date bump" clause)
- gh pr list --author app/dependabot --state open returns empty — zero v1 carry-forward dependabot PRs remain (ROADMAP Success Criterion 3)
- Operator explicitly approved the merge (PR merge gate satisfied per project convention)
- Both ROADMAP Success Criteria 1 and 3 are satisfied (SC2 satisfied by Plan 01)
</success_criteria>

<output>
Create `.planning/phases/21-dependency-hygiene/21-02-SUMMARY.md` when done
</output>
