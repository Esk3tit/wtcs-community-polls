---
phase: 13-uidn-02-mobile-audit-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/closure/audit-screenshots.mjs
autonomous: true
requirements:
  - UIDN-02

must_haves:
  truths:
    - "D-01: Strengthens PR #24 hydration-wait fix with deterministic sentinel — audit-screenshots.mjs exits 0 with 'sha256 uniqueness check passed (42 PNGs, 6 allowed home↔admin collisions, 0 unexpected collisions)' in stdout"
    - "D-02: Zero src/ edits — sentinel uses existing aria-label; all changes confined to .planning/closure/audit-screenshots.mjs (Phase 9 zero-src/-edits closure invariant honored)"
    - "D-03: Sentinel selector is exactly [aria-label='Toggle color theme'] (Navbar.tsx:76 theme-toggle Button, unconditionally rendered)"
    - "D-04: Soft 10s timeout with .catch preserved on both passes — generous for prod first-byte latency on cold Netlify hit"
    - "D-05: sha256 dupe-check runs BEFORE MANIFEST write and calls process.exit(1) on any UNEXPECTED collision; per-D-19 whitelist allows the home↔admin per-width pair as expected Phase 9 D-06 evidence"
    - "D-06: Sentinel waitFor AND sha256 dupe-check applied uniformly to BOTH Pass-A and Pass-B (line 80 + line 161 swap sites)"
    - "D-07: Pass-B extended to 4 sub-routes — admin /admin/suggestions/* + member /topics + /archive (reuses signInWithPassword + addInitScript infra)"
    - "D-08: Pass-B uses TWO Playwright contexts — adminUser (22222222-…) for /admin/*, memberUser (11111111-…) for /topics + /archive; constants inline-mirrored from e2e/fixtures/test-users.ts per D-23 (harness is .mjs, fixture is .ts — no runtime import)"
    - "D-09: Pass-A drops unauth /topics + /archive (AuthGuard renders LandingPage in place — byte-identical to / unauth); /admin unauth STAYS (Phase 9 D-06 locked evidence); Pass-A captures exactly 3 unauth routes: /, /auth/error, /admin (18 PNGs total)"
    - "D-10: Plain naming bp-{w}-topics.png / bp-{w}-archive.png — no auth- prefix (unambiguous after unauth drop)"
    - "D-19: sha256 dupe-check whitelists the per-width home↔admin pair (6 expected collision groups) and fails the harness only on UNEXPECTED collisions (loading-shell signature)"
    - "D-21: All harness path references inside this plan are repo-relative (.planning/..., src/..., e2e/...) — no /Users/... absolute paths"
    - "audit-screenshots.mjs exits 0 with 'All DOM assertions matched.' in stdout (Phase 9 F6 warning-only philosophy preserved on per-PNG probes)"
    - "Pass-B captures exactly 4 auth routes across 2 contexts: 2 admin routes (adminUser) + /topics + /archive (memberUser) = 24 PNGs total"
  artifacts:
    - path: ".planning/closure/audit-screenshots.mjs"
      provides: "Fixed screenshot harness with sentinel, 2nd context, dupe-check"
      contains: "Toggle color theme"
    - path: ".planning/closure/artifacts/screenshots/"
      provides: "42 PNGs from the clean rerun"
      min_count: 42
    - path: ".planning/closure/artifacts/MANIFEST.json"
      provides: "sha256-pinned manifest of the 42 PNGs"
      contains: "sha256"
  key_links:
    - from: ".planning/closure/audit-screenshots.mjs"
      to: "src/components/layout/Navbar.tsx:76"
      via: "page.locator('[aria-label=\"Toggle color theme\"]').waitFor({timeout: 10000}).catch(() => {})"
      pattern: "Toggle color theme"
    - from: ".planning/closure/audit-screenshots.mjs"
      to: "e2e/fixtures/test-users.ts"
      via: "fixtureUsers.memberUser.id literal INLINE-MIRRORED (not imported — .mjs cannot runtime-import .ts) with source comment per D-23"
      pattern: "11111111-1111-1111-1111-111111111111"
    - from: "Pass-B member context"
      to: "http://localhost:4173/topics + /archive"
      via: "signInWithPassword(memberUser) + addInitScript + goto"
      pattern: "playwright-user-member@test.local"
---

<objective>
Fix the `audit-screenshots.mjs` harness to eliminate the Phase 9 Plan 02 hydration-wait defect, then run it to produce a clean 42-PNG screenshot corpus.

Purpose: The v1.1 harness produced 24 byte-identical loading-shell PNGs because the PR #24 `body.filter({hasText})` waitFor fired before React hydration on the Netlify CDN cold-start. This plan replaces that fragile wait with a deterministic sentinel (`[aria-label="Toggle color theme"]` — the Navbar theme-toggle, unconditionally rendered on every route), adds a second Playwright context for memberUser to capture authenticated `/topics` and `/archive`, reduces Pass-A from 5 to 3 unauth routes (dropping by-design redirect dupes), and adds a sha256 uniqueness gate that hard-fails before the MANIFEST write if any PNGs are byte-identical.

Output: Modified `audit-screenshots.mjs`; clean 42-PNG corpus in `.planning/closure/artifacts/screenshots/`; updated `MANIFEST.json`.
</objective>

<execution_context>
@.planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md
@.planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md
@.planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md

<interfaces>
<!-- Key identifiers the executor needs. Extracted from codebase — no exploration needed. -->

From src/components/layout/Navbar.tsx:76 (sentinel target — D-03):
  aria-label="Toggle color theme"
  Location: inside <div className="flex items-center gap-2"> at line 72, BEFORE the {user ? ... : ...} ternary at line 98.
  Confirmed unconditionally rendered on every route.

From e2e/fixtures/test-users.ts:21-30 (fixture constants — D-08):
  fixtureUsers.memberUser.id = '11111111-1111-1111-1111-111111111111'
  fixtureUsers.memberUser.email = 'playwright-user-member@test.local'
  fixtureUsers.adminUser.id = '22222222-2222-2222-2222-222222222222'
  fixtureUsers.adminUser.email = 'playwright-user-admin@test.local'
  FIXTURE_PASSWORD = 'playwright-fixture-only-do-not-use-in-prod'

From .planning/closure/audit-screenshots.mjs (current harness — lines that change):
  Line 80 (Pass-A sentinel swap): await page.locator('body').filter({ hasText: route.mustSee }).first().waitFor({ timeout: 5000 }).catch(() => {})
  Line 161 (Pass-B sentinel swap): await page.locator('body').filter({ hasText: route.mustSee }).first().waitFor({ timeout: 5000 }).catch(() => {})
  Lines 28-40 (UNAUTH_ROUTES): 5-entry array; /topics and /archive entries to be removed per D-09
  Lines 41-46 (AUTH_ROUTES): rename to ADMIN_ROUTES; member routes declared separately
  Line 183: const total = BREAKPOINTS.length * (UNAUTH_ROUTES.length + AUTH_ROUTES.length) — must be updated to use ADMIN_ROUTES + MEMBER_ROUTES
  Lines 179: await context.close() — admin context closes here; member context block follows before browser.close()
  Lines 195-228: sha256 dupe-check must land BEFORE this existing MANIFEST block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply harness fixes — sentinel swap, route reduction, member context, sha256 dupe-check with whitelist</name>
  <read_first>
    - .planning/closure/audit-screenshots.mjs (the file being modified — read all 229 lines)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md (D-01..D-26 locked decisions; D-19/D-21/D-23 are the cross-AI review closure decisions that change the dupe-check rule, path semantics, and fixture-mirroring pattern)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md (exact patterns: Pattern 1 sentinel, Pattern 2 member context, Pattern 3 dupe-check, Pattern 4 route reduction, Pattern 5 total log)
    - src/components/layout/Navbar.tsx (verify aria-label="Toggle color theme" at line 76 before writing the selector)
    - src/components/auth/AuthGuard.tsx (verify unauth /topics + /archive render <LandingPage /> in place at line 18 — NO navigation; this grounds D-09 rationale wording)
    - src/components/auth/AdminGuard.tsx (verify unauth /admin returns <Navigate to="/" /> at line 18; combined with src/routes/index.tsx:22 LandingPage render at /, this produces the home↔admin sha256 collision that D-19 whitelists)
    - e2e/fixtures/test-users.ts (verify memberUser.id = '11111111-...' and FIXTURE_PASSWORD literal values — these are mirrored inline into the harness per D-23, not imported)
  </read_first>
  <files>.planning/closure/audit-screenshots.mjs</files>
  <action>
    Apply four distinct edits to .planning/closure/audit-screenshots.mjs. Each edit corresponds to a locked decision; reference the decision IDs inline in any new code comments (per Phase 9 precedent — plan-ID comments are acceptable in .planning/closure/ scripts).

    EDIT 1 — Update file header comment (lines 5-9): Update the "Two passes" comment block to reflect the new matrix: Pass A = 3 unauth routes × 6 widths = 18 PNGs; Pass B = 4 auth sub-routes (2 admin + 2 member) × 6 widths = 24 PNGs; Total = 42 PNGs. Reference Phase 13 D-07/D-08/D-09.

    EDIT 2 — Reduce UNAUTH_ROUTES from 5 entries to 3 entries (D-09): Remove the /topics and /archive entries from the UNAUTH_ROUTES array (lines 36-37 in the current file). The remaining 3 entries are: /, /auth/error, /admin. Add a comment explaining WHY these two routes were dropped (corrected wording — AuthGuard does NOT navigate; it renders LandingPage in place):
      "AuthGuard renders <LandingPage /> in place for unauth visitors at /topics + /archive (src/components/auth/AuthGuard.tsx:18 — NO navigation), producing PNGs byte-identical to / unauth at every width. Dropped per D-09 to keep Pass-A focused on routes whose unauth shell is meaningful. The /admin entry STAYS — AdminGuard navigates unauth /admin to / (src/components/auth/AdminGuard.tsx:18), which IS the locked Phase 9 D-06 evidence of public-unauthenticated /admin UX. The home↔admin per-width sha256 collision is expected and handled by the D-19 dupe-check whitelist."

    EDIT 3 — Rename AUTH_ROUTES to ADMIN_ROUTES and declare MEMBER_ROUTES (D-08/D-10): Rename the const AUTH_ROUTES at line 41 to ADMIN_ROUTES (keeping the 2 existing entries unchanged). Immediately after, declare a new const MEMBER_ROUTES with 2 entries:
      - { path: '/topics', expectedPath: '/topics', name: 'topics', mustSee: /topics|polls|suggestions/i }
      - { path: '/archive', expectedPath: '/archive', name: 'archive', mustSee: /archive|past|closed/i }
    Note: expectedPath for both is the authenticated path (NOT '/') — auth-honored routes land at their actual path (per Pitfall 5 in RESEARCH.md). Add comment: "Phase 13 D-10: plain naming, no auth- prefix; expectedPath is auth-honored path (not '/')."

    EDIT 4 — Swap fragile sentinel for deterministic sentinel at line 80 (Pass-A) (D-01/D-02/D-03/D-04): Replace:
      await page.locator('body').filter({ hasText: route.mustSee }).first().waitFor({ timeout: 5000 }).catch(() => {})
    with:
      await page.locator('[aria-label="Toggle color theme"]').waitFor({ timeout: 10000 }).catch(() => {})
    Update the preceding comment to explain: "Phase 13 D-03/D-04: replaced fragile body-text filter with stable post-hydration marker. Navbar.tsx:76 renders this button unconditionally (outside the auth ternary). Soft 10s timeout; .catch preserved — sha256 dupe-check is the hard backstop."

    EDIT 5 — Update Pass-B admin context to use sentinel at line 161 (D-06): Replace the same body.filter waitFor at line 161 with the identical sentinel line. Update preceding comment similarly, referencing D-06 (uniform application to both passes).

    EDIT 6 — Update AUTH_ROUTES reference in the Pass-B loop to ADMIN_ROUTES: In the `for (const route of AUTH_ROUTES)` loop (around line 153), change AUTH_ROUTES to ADMIN_ROUTES to match the renamed constant.

    EDIT 7 — Add member context block after admin context.close() (D-07/D-08/D-23): After `await context.close()` at line 179 (end of admin context block), add a new scoped block `{ ... }` that:
      - Declares MEMBER_FIXTURE with id '11111111-1111-1111-1111-111111111111' and email 'playwright-user-member@test.local' INLINE-MIRRORED from e2e/fixtures/test-users.ts (D-23: harness is .mjs and the fixture file is .ts — Node's ESM loader cannot runtime-import TypeScript across that boundary). Match the existing ADMIN_FIXTURE pattern at lines 53-56 exactly, including a source-comment of the form `// D-23: mirrored from e2e/fixtures/test-users.ts:21-30 — single source of truth lives there; update both if fixture changes.`
      - Calls supabase.auth.signInWithPassword with MEMBER_FIXTURE.email and FIXTURE_PASSWORD (reusing the same supabase client instance already in scope from the outer Pass-B block)
      - Hard-throws if memberError or !memberData.session (same pattern as line 130-131)
      - Builds memberPayload from memberData.session fields (same shape as payload at lines 134-141)
      - Creates memberContext via browser.newContext()
      - Creates memberPage via memberContext.newPage()
      - Calls memberPage.addInitScript with [STORAGE_KEY, JSON.stringify(memberPayload)] BEFORE any goto (LO-03 sequencing — Pitfall 1 in RESEARCH.md)
      - Outer loop: for (const width of BREAKPOINTS) with memberPage.setViewportSize({width, height: 800}) BEFORE goto (Pitfall 4)
      - Inner loop: for (const route of MEMBER_ROUTES) with memberPage.goto(url, {waitUntil: 'networkidle'}), then the sentinel waitFor line, then memberPage.screenshot(), then the F6 DOM+path assertion warning block (same shape as admin context, prefix '[auth:member]')
      - Closes with await memberContext.close()
      Add comment block above: "Pass B (member context) — /topics + /archive with memberUser session. Phase 13 D-08: separate context captures real voter UI (no Admin link in Navbar). signInWithPassword + addInitScript before goto (LO-03)."

    EDIT 8 — Update total count log (line 183) (RESEARCH Pattern 5): Change:
      const total = BREAKPOINTS.length * (UNAUTH_ROUTES.length + AUTH_ROUTES.length)
    to:
      const total = BREAKPOINTS.length * (UNAUTH_ROUTES.length + ADMIN_ROUTES.length + MEMBER_ROUTES.length)
    This will compute 6 * (3 + 2 + 2) = 42 — correct.

    EDIT 9 — Add sha256 dupe-check block WITH WHITELIST before the MANIFEST block (D-05/D-06/D-19) (RESEARCH Pattern 3 + cross-AI review correction): After the F6 warning summary block (around line 193) and BEFORE the MANIFEST upsert block (starting at line 195), insert a new scoped block `{ ... }` that:
      - Imports readdir from 'node:fs/promises' (can shadow the outer import using a local name like readdirForDupe to avoid collision with the MANIFEST block's readdir)
      - Imports path from 'node:path'
      - Reads all .png files from ARTIFACTS_DIR, sorted
      - For each file: readFile + createHash('sha256').update(buf).digest('hex') — reusing the existing imported createHash and readFile
      - Groups sha256 hashes into a Map<string, string[]> (key = hash, value = array of basenames)
      - Builds the D-19 whitelist: for each width W in BREAKPOINTS, the expected-collision pair is the JS Set `new Set([`bp-${W}-home.png`, `bp-${W}-admin.png`])`. Total 6 expected pairs (one per breakpoint).
      - For each collision group with length > 1:
        - Convert the group's basenames to a Set<string>
        - If the Set has size === 2 AND its members match exactly one of the 6 width-pair whitelist sets: classify as EXPECTED (home↔admin per Phase 9 D-06 + D-19) and increment expectedCount.
        - Otherwise: classify as UNEXPECTED (loading-shell signature) and push to unexpectedGroups[].
      - If unexpectedGroups.length > 0: console.error with "=== sha256 DUPE FAILURE: ${unexpectedGroups.length} unexpected collision group(s) ===" and list each unexpected group's basenames on its own line, then console.error "Hydration sentinel did not prevent loading-shell captures. Fix the harness before re-running. (Note: home↔admin per-width pairs are whitelisted per D-19 / Phase 9 D-06 — those do NOT trigger this failure.)", then process.exit(1)
      - On success (no unexpected collisions): console.log `sha256 uniqueness check passed (${pngCount} PNGs, ${expectedCount} allowed home↔admin collision pairs per D-19, 0 unexpected collisions)` — expected expectedCount is 6 when all widths captured both home and admin successfully.
    Add comment: "D-05/D-19 — sha256 uniqueness gate with per-width home↔admin whitelist. Phase 9 defect signature: 4-of-30 unauth-prod PNGs at the same sha256 (different routes hashing to the loading-shell). Whitelist exception: /admin unauth navigates to / via AdminGuard (src/components/auth/AdminGuard.tsx:18) which renders LandingPage — the same content as / unauth (src/routes/index.tsx:22). That collision IS the Phase 9 D-06 locked evidence of public-unauthenticated /admin UX, not a loading-shell defect. Per-width pair whitelist (bp-{W}-home.png ↔ bp-{W}-admin.png) lets the legitimate collision pass while any other collision (signaling true loading-shell capture) still hard-fails. Exit non-zero BEFORE MANIFEST write so a known-bad run does not record a clean manifest."
    Note on MANIFEST ordering: implement exit BEFORE the MANIFEST upsert per CONTEXT.md canonical design. The dupe-check block calls process.exit(1) directly on unexpected collisions; the MANIFEST upsert never runs on that path.

    Anti-patterns to avoid (per RESEARCH.md):
    - Do not use page.waitForSelector() — use page.locator().waitFor()
    - Do not modify any file under src/
    - Do not run the Lighthouse audit from this harness — Lighthouse stays in audit-mobile.sh
    - Do not reuse the same Playwright context for both admin and member
    - Do not place the dupe-check inside the MANIFEST block — it must precede it
  </action>
  <verify>
    <automated>node --check .planning/closure/audit-screenshots.mjs && echo "syntax OK"</automated>
    Confirm the file passes Node.js syntax check. Then confirm sentinel lines exist:
    <automated>grep -c "Toggle color theme" .planning/closure/audit-screenshots.mjs</automated>
    Expected: 3 (one per pass: Pass-A, Pass-B admin loop, Pass-B member loop).
    <automated>grep -c "sha256 DUPE FAILURE" .planning/closure/audit-screenshots.mjs</automated>
    Expected: 1 (the dupe-check error message).
    <automated>grep -c "D-19\|home↔admin\|whitelist" .planning/closure/audit-screenshots.mjs</automated>
    Expected: at least 1 (the whitelist comment / variable name present).
    <automated>grep -c "MEMBER_ROUTES" .planning/closure/audit-screenshots.mjs</automated>
    Expected: at least 2 (declaration + usage in loop + total count — likely 3).
    <automated>grep "playwright-user-member" .planning/closure/audit-screenshots.mjs</automated>
    Expected: line containing MEMBER_FIXTURE email.
    <automated>grep -c "topics\|archive" .planning/closure/audit-screenshots.mjs</automated>
    Expected: at least 4 (MEMBER_ROUTES entries + comment references).
    Confirm UNAUTH_ROUTES has 3 entries (not 5). Use a structural check robust to inline comments + multi-line array entries:
    <automated>node -e "const s=require('fs').readFileSync('.planning/closure/audit-screenshots.mjs','utf8');const m=s.match(/const UNAUTH_ROUTES\s*=\s*\[([\s\S]*?)\][^\]]*?const /);if(!m){console.error('NO MATCH for UNAUTH_ROUTES block');process.exit(2)}const body=m[1];const n=(body.match(/\{\s*path:/g)||[]).length;console.log(n);process.exit(n===3?0:1)"</automated>
    Expected stdout: "3"; exit code 0. The regex grabs everything between `const UNAUTH_ROUTES = [` and the next `const ` declaration (which would be `const AUTH_ROUTES` / `const ADMIN_ROUTES`), then counts `{ path:` openings inside. Replaces the brittle `grep -A 10 ... | grep -c "path:"` check flagged in REVIEWS.md Codex LOW — robust against inline comments and multi-line entries.
  </verify>
  <acceptance_criteria>
    - audit-screenshots.mjs passes `node --check` with no syntax errors
    - `grep -c 'Toggle color theme' .planning/closure/audit-screenshots.mjs` returns 3 (Pass-A + Pass-B admin + Pass-B member)
    - `grep -c "body.*filter.*hasText" .planning/closure/audit-screenshots.mjs` returns 0 (old fragile sentinel fully removed from both loop sites)
    - `grep -c 'MEMBER_ROUTES' .planning/closure/audit-screenshots.mjs` returns at least 2 (declaration + loop reference)
    - `grep 'playwright-user-member' .planning/closure/audit-screenshots.mjs` is non-empty (member fixture email present)
    - `grep 'sha256 DUPE FAILURE' .planning/closure/audit-screenshots.mjs` is non-empty (dupe-check error message present)
    - `grep -c 'process.exit(1)' .planning/closure/audit-screenshots.mjs` returns 1 (dupe-check exit, before MANIFEST upsert)
    - `grep -E "D-19|home↔admin|whitelist|expectedCollisionPair" .planning/closure/audit-screenshots.mjs` returns at least one match (D-19 whitelist marker present in either comment, variable name, or success-log message)
    - The UNAUTH_ROUTES array literal contains exactly 3 entries (verified by the AST-style Node check in verify, not by line-grep)
    - The dupe-check classification logic includes both a per-width whitelist construction (referencing BREAKPOINTS and the `bp-${W}-home.png` / `bp-${W}-admin.png` filename pair) and an "unexpected" classification path that calls process.exit(1)
    - No files under src/ are modified
    - ADMIN_ROUTES appears in the file (renamed from AUTH_ROUTES)
    - Total log line computes BREAKPOINTS.length * (UNAUTH_ROUTES.length + ADMIN_ROUTES.length + MEMBER_ROUTES.length)
    - Member fixture constants are inline-mirrored from e2e/fixtures/test-users.ts with a D-23 source comment (NOT imported — harness is .mjs, fixture is .ts)
  </acceptance_criteria>
  <done>audit-screenshots.mjs is syntax-valid; both sentinel swap sites confirmed; member context block present; sha256 dupe-check WITH D-19 home↔admin whitelist present before MANIFEST upsert; UNAUTH_ROUTES has 3 entries verified via AST-style node check (not brittle grep); ADMIN_ROUTES named; total count uses both route arrays; MEMBER_FIXTURE inline-mirrored per D-23; zero src/ edits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator pre-flight + harness execution</name>
  <what-built>
    Task 1 fixed audit-screenshots.mjs. This checkpoint asks the operator to start the required pre-requisites and run the fixed harness so we know it exits 0 and produces 42 unique PNGs before proceeding to the Lighthouse rerun in Plan 13-02.
  </what-built>
  <how-to-verify>
    Pre-flight checklist (operator runs in a separate terminal or sequence):
    1. In one terminal: `supabase start` — wait for "API URL: http://localhost:54321" in output.
    2. From the project root: `npm run build && npm run preview` — wait for "Local: http://localhost:4173/" in output.
    3. Export the anon key. The exact `supabase status` line shape varies across CLI versions; try the awk approach first, then fall back to regex if the column count differs:
       Primary: `export VITE_SUPABASE_ANON_KEY=$(supabase status | awk -F': *' '/anon key:/{print $2; exit}' | tr -d ' ')`
       Fallback: `export VITE_SUPABASE_ANON_KEY=$(supabase status | grep -E "anon key" | sed -E 's/.*anon key:[[:space:]]*([A-Za-z0-9._-]+).*/\1/')`
       Verify with: `echo "${VITE_SUPABASE_ANON_KEY:0:8}..."` — should print 8 chars then "...", not just "...".
    4. Run the harness from the repo root: `node .planning/closure/audit-screenshots.mjs`

    Expected stdout (must all appear):
    - Lines starting with [unauth] for each of 3 routes × 6 widths = 18 lines
    - Lines starting with [auth] for each of 2 admin routes × 6 widths = 12 lines
    - Lines starting with [auth:member] for each of 2 member routes × 6 widths = 12 lines
    - "Wrote 42 screenshots to .planning/closure/artifacts/screenshots/"
    - "All DOM assertions matched." (zero F6 warnings — critical; if warnings appear, note them)
    - "sha256 uniqueness check passed (42 PNGs, 6 allowed home↔admin collision pairs per D-19, 0 unexpected collisions)" (the 6 reflects one expected pair per width — bp-{W}-home.png ↔ bp-{W}-admin.png — per Phase 9 D-06 unauth /admin → / navigation)
    - "Updated .planning/closure/artifacts/MANIFEST.json with N total entries" where N is 42 screenshot entries + any prior lighthouse entries

    Verify the PNG count:
    `ls .planning/closure/artifacts/screenshots/*.png | wc -l` → 42

    Pre-flight artifact cleanup (Gemini suggestion — REVIEWS.md): the harness performs `rm -rf .planning/closure/artifacts/screenshots/` near line 61, but if a previous crashed run left a directory in an unexpected state, manually verify it is empty before re-running:
    `ls .planning/closure/artifacts/screenshots/ 2>/dev/null | wc -l` → should be 0 BEFORE the run, 42 AFTER.

    Spot-check one authenticated screenshot:
    Open `.planning/closure/artifacts/screenshots/bp-390-topics.png` (or bp-375-topics.png). Confirm it shows the /topics page with the Navbar visible (not a loading shell or login redirect to /). The member username or "Sign in" absence should be visible — authenticated UI, not the unauthenticated landing page.

    Spot-check that the home↔admin whitelist is working (not masking a real defect):
    Compute sha256 for one width pair: `shasum -a 256 .planning/closure/artifacts/screenshots/bp-390-home.png .planning/closure/artifacts/screenshots/bp-390-admin.png` — the two hashes SHOULD be identical (proves the whitelist correctly let the legitimate Phase 9 D-06 collision pass). If they DIFFER, the whitelist did not apply but no failure was raised either — investigate before accepting.

    If the harness exits non-zero:
    - Exit code 1 with "sha256 DUPE FAILURE: N unexpected collision group(s)" → sentinel did not wait long enough; verify the Navbar theme-toggle is rendering on prod (prod site must be live); check that `npm run preview` is running; the printed unexpected-collision filenames identify which routes captured loading shells
    - "Local preview server not reachable" → run `npm run build && npm run preview` first
    - "VITE_SUPABASE_ANON_KEY" error → export the anon key per step 3
    - "signInWithPassword returned no session" → run `supabase start` and confirm fixture seed is applied
  </how-to-verify>
  <resume-signal>
    Type "harness-ok: N warnings" where N is the number of DOM-assertion warnings (0 is ideal, but if warnings appear during the /admin unauth route or /auth/error route, document them for the evidence file). If the harness exited non-zero, type "harness-fail: [error message]" and the executor will diagnose.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → harness | The operator runs audit-screenshots.mjs; no user-facing surface involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-01 | Tampering | audit-screenshots.mjs harness output | accept | This is a planning closure script, not a user-facing service. The sha256 dupe-check ensures the PNG corpus is authentic (no byte-identical loading shells). No user data at risk. |

No security-relevant changes in scope for this phase. This plan modifies `.planning/closure/audit-screenshots.mjs` and produces binary screenshots in `.planning/closure/artifacts/screenshots/` (gitignored). No authentication flows, no user-facing surfaces, no API endpoints, no database schema changes.

ASVS V2 (Authentication): N/A — no auth flow changes.
ASVS V3 (Session): N/A — no session management changes.
ASVS V4 (Access Control): N/A — no authorization changes.
ASVS V5 (Input Validation): Existing behavior only — harness validates its own environment (Pitfall 5 probe at line 104, ANON_KEY guard at line 114). These are unchanged by Phase 13 edits.
</threat_model>

<verification>
After Task 2 checkpoint approves:
- `ls .planning/closure/artifacts/screenshots/*.png | wc -l` → 42
- `node --check .planning/closure/audit-screenshots.mjs` → exits 0
- `grep -c 'Toggle color theme' .planning/closure/audit-screenshots.mjs` → 3
- Operator recorded "sha256 uniqueness check passed (42 PNGs, 6 allowed home↔admin collision pairs per D-19, 0 unexpected collisions)" in Task 2 resume-signal (the 6 is the exact whitelist count when all widths captured both home and admin)
- No src/ files modified: `git diff --name-only` shows only `.planning/closure/audit-screenshots.mjs`
</verification>

<success_criteria>
- audit-screenshots.mjs is syntax-valid and runs to exit 0 with clean stdout
- 42 PNGs captured: 3 unauth-prod × 6 widths + 2 admin-auth-local × 6 widths + 2 member-auth-local × 6 widths = 18 + 12 + 12
- sha256 dupe-check confirms "0 unexpected collisions" (the 6 home↔admin per-width pairs are EXPECTED per D-19 and Phase 9 D-06 — they are NOT loading-shell defects)
- Both sentinel swap sites (line 80 Pass-A, Pass-B admin loop) replaced with [aria-label="Toggle color theme"] 10s soft waitFor per D-03/D-04
- New member context (D-08) captures /topics + /archive under memberUser session; constants inline-mirrored from e2e/fixtures/test-users.ts per D-23 (not imported)
- UNAUTH_ROUTES reduced from 5 to 3 entries (dropped /topics + /archive per D-09; /admin retained per Phase 9 D-06 + D-19 whitelist)
- Zero src/ edits (Phase 9 zero-src/-edits closure invariant honored per D-02)
- MANIFEST.json updated with all 42 PNG entries
- All plan-internal path references are repo-relative (.planning/..., src/..., e2e/...) per D-21 — no /Users/khaiphan/... absolute paths
</success_criteria>

<output>
After completion, create `.planning/phases/13-uidn-02-mobile-audit-closure/13-01-SUMMARY.md` documenting (D-24: this file is NOT in the Plan 13-02 atomic commit — operator may ship it separately):
- Which lines were changed in audit-screenshots.mjs (sentinel sites, route reduction, member context, dupe-check with D-19 whitelist)
- Harness exit code and stdout summary from Task 2 checkpoint (harness-ok: N warnings)
- PNG count confirmed (target: 42)
- Whether any F6 DOM-assertion warnings appeared (and which routes)
- D-19 dupe-check log line as recorded: "sha256 uniqueness check passed (42 PNGs, X allowed home↔admin collision pairs per D-19, 0 unexpected collisions)" — the X count should equal 6 (one per BREAKPOINT width) when all widths captured both home and admin successfully
- MANIFEST.json entry count
</output>
