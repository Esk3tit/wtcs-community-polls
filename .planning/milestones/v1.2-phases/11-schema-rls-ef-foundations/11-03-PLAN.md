---
phase: 11-schema-rls-ef-foundations
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - supabase/functions/close-expired-polls/index.ts
  - supabase/functions/close-poll/index.ts
  - supabase/functions/create-category/index.ts
  - supabase/functions/delete-category/index.ts
  - supabase/functions/delete-poll/index.ts
  - supabase/functions/demote-admin/index.ts
  - supabase/functions/pin-poll/index.ts
  - supabase/functions/promote-admin/index.ts
  - supabase/functions/rename-category/index.ts
  - supabase/functions/set-resolution/index.ts
  - supabase/functions/update-poll/index.ts
autonomous: true
requirements:
  - VIS-02
tags:
  - edge-function
  - audit
  - retrofit
  - supabase

must_haves:
  truths:
    - "All 11 existing mutation admin EFs in scope here import `writeAudit` from `_shared/audit.ts` (create-poll is retrofitted separately in Plan 11-03b)"
    - "Each EF emits exactly one `writeAudit(...)` call per state-changing path, placed AFTER the mutation success check and BEFORE the success `return json(...)` (RESEARCH Pattern 5)"
    - "No EF wraps its `writeAudit` call in try/catch (Pitfall 3 — emitter swallows; caller must not silence)"
    - "Existing response shapes are unchanged (all 11 retrofitted EFs continue to return their pre-existing JSON shape; only `toggle-results-visibility` from Plan 02 returns the full poll row)"
    - "`close-expired-polls` (cron) writes one audit row per closed poll with `actor_id: null` (D-03) and `action: 'poll_auto_closed'`"
    - "Each EF's `action` string follows snake_case verb-phrase per the locked PATTERNS.md table; `before`/`after` JSONB is compact (mutated fields only — D-07)"
    - "D-05: The audit retrofit covers ALL 12 mutation admin EFs plus the new toggle EF (13 total emitters): `close-expired-polls` (cron, `actor_id=NULL`, `action='poll_auto_closed'`), `close-poll`, `create-category`, `create-poll`, `delete-category`, `delete-poll`, `demote-admin`, `pin-poll`, `promote-admin`, `rename-category`, `set-resolution`, `update-poll`, and the new `toggle-results-visibility`. This plan covers 11 of the 12 retrofits (create-poll is in Plan 11-03b; the new toggle EF is in Plan 02), giving the 13-emitter floor for v1.2."
    - "D-06: Audit retrofit EXCLUDES the two read-only admin EFs `search-admin-targets` and `get-upload-url` — they do not change state, so no audit row is written and these EFs are not modified by this plan"
    - "REVIEW-FIX-C2-H2: `promote-admin` has TWO mutation branches per the live EF (`supabase/functions/promote-admin/index.ts`, body lines verified in cycle-2 review): Branch 1 (`target_user_id` present) UPDATEs `profiles.is_admin = true` for a single existing profile and returns `{success: true, mode: 'existing'}`. Branch 2 (`target_discord_id` present) INSERTs into `admin_discord_ids` (with ON CONFLICT 23505 idempotent ignore) AND retroactively UPDATEs `profiles.is_admin = true` for every existing profile whose `discord_id` matches the snowflake (zero, one, or many rows), returning `{success: true, mode: 'preauth', promotedExistingProfiles: N}`. The audit retrofit emits: for Branch 1, ONE `admin_promoted` row. For Branch 2, ONE `admin_preauthorized` row keyed on `target_id = target_discord_id` (target_type = 'admin_discord_ids') PLUS one `admin_promoted` row per profile flipped by the retroactive UPDATE (zero rows if `promotedExistingProfiles` is 0; N rows if `promotedExistingProfiles` is N). Addresses cycle-2 Codex HIGH #2."
    - "REVIEW-FIX-C3-H1: Branch 2's `admin_preauthorized` row writes `target_id = target_discord_id` (a Discord snowflake — 17–19 digit decimal string per `admin_discord_ids.discord_id TEXT PRIMARY KEY` at `supabase/migrations/00000000000000_schema.sql:16`). This is the ONLY emitter in the 13-emitter v1.2 audit surface whose `target_id` is not a UUID. Plan 01's D-02 schema declares `audit_log.target_id text` (not `uuid`) precisely to admit this snowflake alongside the UUIDs used by every other emitter — without that column-type fix, the Postgres INSERT would fail with `invalid input syntax for type uuid` and `writeAudit`'s fail-open contract (Plan 02 Pitfall 3) would silently drop the audit row while the EF returned HTTP 200. The two-plan contract is locked: Plan 01 declares text; Plan 03 Branch 2 writes the snowflake without casting. Cross-AI cycle 3 (Gemini + Codex) converged on this finding."
  artifacts:
    - path: "supabase/functions/close-expired-polls/index.ts"
      provides: "Audit retrofit: per-closed-poll row with actor_id=null"
      contains: "writeAudit"
    - path: "supabase/functions/close-poll/index.ts"
      provides: "Audit retrofit: poll_closed"
      contains: "writeAudit"
    - path: "supabase/functions/create-category/index.ts"
      provides: "Audit retrofit: category_created"
      contains: "writeAudit"
    - path: "supabase/functions/delete-category/index.ts"
      provides: "Audit retrofit: category_deleted"
      contains: "writeAudit"
    - path: "supabase/functions/delete-poll/index.ts"
      provides: "Audit retrofit: poll_deleted"
      contains: "writeAudit"
    - path: "supabase/functions/demote-admin/index.ts"
      provides: "Audit retrofit: admin_demoted"
      contains: "writeAudit"
    - path: "supabase/functions/pin-poll/index.ts"
      provides: "Audit retrofit: poll_pinned / poll_unpinned"
      contains: "writeAudit"
    - path: "supabase/functions/promote-admin/index.ts"
      provides: "Audit retrofit: admin_promoted"
      contains: "writeAudit"
    - path: "supabase/functions/rename-category/index.ts"
      provides: "Audit retrofit: category_renamed"
      contains: "writeAudit"
    - path: "supabase/functions/set-resolution/index.ts"
      provides: "Audit retrofit: resolution_set"
      contains: "writeAudit"
    - path: "supabase/functions/update-poll/index.ts"
      provides: "Audit retrofit: poll_updated"
      contains: "writeAudit"
  key_links:
    - from: "11 retrofitted EFs (this plan)"
      to: "supabase/functions/_shared/audit.ts (Plan 02)"
      via: "import { writeAudit } from '../_shared/audit.ts'"
      pattern: "import \\{ writeAudit \\} from '\\.\\./_shared/audit\\.ts'"
---

<objective>
Audit retrofit across 11 existing mutation admin EFs (excluding `create-poll`, which is planned separately in Plan 11-03b because its `results_hidden` extension carries an additional contract surface). Each of the 11 EFs gets one new import line and one new `writeAudit(...)` call per state-changing path. The new `toggle-results-visibility` is already audit-emitting from Plan 02; this plan, together with Plan 11-03b, brings the pre-existing surface to parity, giving v1.2 a 13-emitter audit floor.

Purpose: VIS-02 requires every change to write a new `audit_log` row. Phase 11 D-05 expands this to the full mutation surface (12 existing EFs + the new toggle). Without this retrofit, `audit_log` would only capture results-visibility toggles — a gap that defeats the forensics purpose of the table. `create-poll` is split off into Plan 11-03b because the D-08/D-09/D-10 body extension + the post-RPC conditional UPDATE pattern are a non-trivial second deliverable that warrants its own plan boundary.

Output: 11 EF files modified. Each gets one new import line and one new `writeAudit(...)` call per state-changing path. Zero changes to response shapes. Zero changes to existing validation, error handling, CORS, or admin-gate code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md
@.planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md

<!-- Plan 02 shared helper — this plan's primary dependency -->
@supabase/functions/_shared/audit.ts
@supabase/functions/_shared/admin-auth.ts

<!-- Each EF file is its own analog — executor reads each before modifying -->
@supabase/functions/close-expired-polls/index.ts
@supabase/functions/close-poll/index.ts
@supabase/functions/create-category/index.ts
@supabase/functions/delete-category/index.ts
@supabase/functions/delete-poll/index.ts
@supabase/functions/demote-admin/index.ts
@supabase/functions/pin-poll/index.ts
@supabase/functions/promote-admin/index.ts
@supabase/functions/rename-category/index.ts
@supabase/functions/set-resolution/index.ts
@supabase/functions/update-poll/index.ts

<interfaces>
<!-- Locked per-EF action string + before/after shape (from PATTERNS.md "Audit retrofit" table) -->
<!-- create-poll is excluded from this table — see Plan 11-03b -->

| EF | action | target_type | actor_id | before | after |
|----|--------|-------------|----------|--------|-------|
| close-expired-polls | poll_auto_closed | poll | null (D-03) | {status:'active'} | {status:'closed'} (one row per closed poll) |
| close-poll | poll_closed | poll | user.id | {status:'active'} | {status:'closed', resolution} |
| create-category | category_created | category | user.id | null | {name} |
| delete-category | category_deleted | category | user.id | {name} | null |
| delete-poll | poll_deleted | poll | user.id | {title, status} | null |
| demote-admin | admin_demoted | profile | user.id | {is_admin:true} | {is_admin:false} |
| pin-poll | poll_pinned / poll_unpinned | poll | user.id | {is_pinned:!isPinned} | {is_pinned:isPinned} |
| promote-admin (Branch 1: target_user_id, existing profile) | admin_promoted | profile | user.id | {is_admin:false} | {is_admin:true} (one row, target_id = target_user_id) |
| promote-admin (Branch 2: target_discord_id, pre-auth INSERT) | admin_preauthorized | admin_discord_ids | user.id | null | {discord_id:target_discord_id} (one row, target_id = target_discord_id) |
| promote-admin (Branch 2: retroactive profile flip) | admin_promoted | profile | user.id | {is_admin:false} | {is_admin:true} (one row PER profile whose discord_id matches; can be 0..N rows) |
| rename-category | category_renamed | category | user.id | {name:old} | {name:new} |
| set-resolution | resolution_set | poll | user.id | {resolution:prior} | {resolution:new} |
| update-poll | poll_updated | poll | user.id | null (or prior values if RPC returns them) | <fields the RPC mutated, D-07> |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 03-01: Audit retrofit batch A (5 EFs) — close-expired-polls, close-poll, create-category, delete-category, delete-poll</name>
  <files>supabase/functions/close-expired-polls/index.ts, supabase/functions/close-poll/index.ts, supabase/functions/create-category/index.ts, supabase/functions/delete-category/index.ts, supabase/functions/delete-poll/index.ts</files>
  <read_first>
    - supabase/functions/_shared/audit.ts (Plan 02 output — `writeAudit` signature + `AuditEntry` shape)
    - supabase/functions/close-expired-polls/index.ts (full file — lines 53-58 `.select('id')` return is the closed-poll array to iterate; D-03 actor_id=null is the cron exception)
    - supabase/functions/close-poll/index.ts (full file — locate the UPDATE success check + the existing `return json({success:true}, 200, corsHeaders)`; audit slots between them)
    - supabase/functions/create-category/index.ts (full file — locate the INSERT success path; audit after error-check, before success return)
    - supabase/functions/delete-category/index.ts (full file — DELETE success path; before/after pattern is {name} → null)
    - supabase/functions/delete-poll/index.ts (full file — DELETE success path; capture `title` and `status` for `before` from the EF's existing pre-DELETE SELECT if present, otherwise add a minimal SELECT)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "Audit retrofit of 12 existing EFs" (canonical insertion-point example using pin-poll; per-EF table verbatim values)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pattern 5: close-expired-polls cron audit (special case)" (per-row loop pattern)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-03 (cron actor_id=null), D-05 (13 emitters), D-07 (before/after JSONB shape per-EF)
  </read_first>
  <action>
    For each of the 5 EFs: (1) Add `import { writeAudit } from '../_shared/audit.ts'` to the existing import block, placed immediately after the `admin-auth` import line. (2) Locate the mutation success site — the line AFTER the `if (error)` early-return for the SELECT/INSERT/UPDATE/DELETE that performs the EF's primary mutation, and BEFORE the success `return json(...)`. (3) Insert exactly one `await writeAudit(supabaseAdmin, { ... })` call with the locked action / target_type / actor_id / before / after per the table in `<interfaces>`. Use `user.id` for actor_id except where the table says `null` (close-expired-polls only). Do NOT wrap in try/catch (Pitfall 3).

    Per-EF specifics:

    close-expired-polls (D-03 + RESEARCH Pattern 5): the EF closes multiple polls in one cron invocation. The existing code does a bulk UPDATE and selects back closed IDs (the `.select('id')` array at lines 53-58 area). After the bulk UPDATE error check and BEFORE the success return, iterate `data` with `for (const row of data) { await writeAudit(supabaseAdmin, { actor_id: null, action: 'poll_auto_closed', target_type: 'poll', target_id: row.id, before: { status: 'active' }, after: { status: 'closed' } }); }`. Keep sequential `await` — do not `Promise.all`. The cron is not latency-bound and serial writes match the existing EF style.

    close-poll: After UPDATE success, emit one audit row with `action: 'poll_closed'`, `before: { status: 'active' }`, `after: { status: 'closed', resolution }`. Pull `resolution` from the EF body (the existing variable). If the EF already does a SELECT-before-UPDATE that captures prior status, use that — otherwise the literal `'active'` is acceptable (the gate check ensures the poll was active).

    create-category: After INSERT success, emit one audit row with `action: 'category_created'`, `target_id: <new category id from the INSERT return>`, `before: null`, `after: { name }`. Capture the new id from `.select('id').single()` if the EF doesn't already.

    delete-category: Capture `name` from a SELECT-before-DELETE (reuse the EF's existing fetch if present; if not, add a minimal `.from('categories').select('name').eq('id', category_id).single()` immediately before the DELETE). `action: 'category_deleted'`, `before: { name: <captured> }`, `after: null`. If adding the SELECT introduces a meaningful behavior change beyond audit (e.g., changes the 404 path), fall back to `before: { id: <category_id> }` and add an SQL/code comment WHY the name is omitted.

    delete-poll: Same SELECT-before-DELETE pattern — capture `title` and `status`. `action: 'poll_deleted'`, `before: { title, status }`, `after: null`. If the EF already does a SELECT for ownership/auth or 404 detection, reuse that result rather than adding a second round-trip.

    Do NOT change any response shapes. Do NOT change CORS, admin-gate, validation, or error-handling code. Do NOT touch comments unrelated to the new audit emission. Do NOT add review-round / Plan / Round / PR tags to any new comments (per CLAUDE.md project comment policy — WHY-only).
  </action>
  <acceptance_criteria>
    - All 5 EF files import `writeAudit` from `_shared/audit.ts`: running `for f in close-expired-polls close-poll create-category delete-category delete-poll; do grep -q "import.*writeAudit.*_shared/audit" supabase/functions/$f/index.ts || echo "MISSING: $f"; done` returns no MISSING lines
    - Each EF has ≥1 `writeAudit(` call: `for f in close-expired-polls close-poll create-category delete-category delete-poll; do test $(grep -c "writeAudit(" supabase/functions/$f/index.ts) -ge 1 || echo "FAIL: $f"; done` returns no FAIL
    - `close-expired-polls` writes inside a loop over closed polls: `grep -B 3 "writeAudit" supabase/functions/close-expired-polls/index.ts | grep -E "(for \\(const|for\\s*\\(\\s*const)"` returns ≥1 match
    - `close-expired-polls` uses `actor_id: null` (D-03): `grep -A 5 "writeAudit" supabase/functions/close-expired-polls/index.ts | grep -E "actor_id:\\s*null"` returns ≥1 match
    - `close-expired-polls` uses `action: 'poll_auto_closed'`
    - `close-poll` uses `action: 'poll_closed'`
    - `create-category` uses `action: 'category_created'`
    - `delete-category` uses `action: 'category_deleted'`
    - `delete-poll` uses `action: 'poll_deleted'`
    - No file wraps `writeAudit` in try/catch (Pitfall 3): for each EF, the line immediately preceding `writeAudit(` is NOT `try {` — verify by `grep -B 1 "writeAudit(" supabase/functions/{close-expired-polls,close-poll,create-category,delete-category,delete-poll}/index.ts | grep -E "^\\s*try\\s*\\{$"` returns nothing
    - No file gains review-round / phase-ID archaeology: `grep -iE "(Round [0-9]|Plan [0-9]+-[0-9]+|PR #[0-9]+|review-round)" supabase/functions/{close-expired-polls,close-poll,create-category,delete-category,delete-poll}/index.ts` returns nothing
    - `npm run lint` exits 0 on all 5 modified files (no unused imports introduced)
    - Existing unit suite passes: `npm run test` exits 0 (no EF logic regression)
  </acceptance_criteria>
  <verify>
    <automated>for f in close-expired-polls close-poll create-category delete-category delete-poll; do grep -q "import.*writeAudit.*_shared/audit" supabase/functions/$f/index.ts && grep -q "writeAudit(" supabase/functions/$f/index.ts || { echo "FAIL $f"; exit 1; }; done && grep -qE "action:\\s*'poll_auto_closed'" supabase/functions/close-expired-polls/index.ts && grep -A 5 "writeAudit" supabase/functions/close-expired-polls/index.ts | grep -qE "actor_id:\\s*null" && grep -qE "action:\\s*'poll_closed'" supabase/functions/close-poll/index.ts && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>
    All 5 EFs in batch A emit audit rows on state change with the locked per-EF action strings; close-expired-polls uses cron `actor_id=null` + per-poll loop; no try/catch wraps any audit call; no response-shape changes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 03-02: Audit retrofit batch B (6 EFs) — demote-admin, pin-poll, promote-admin, rename-category, set-resolution, update-poll</name>
  <files>supabase/functions/demote-admin/index.ts, supabase/functions/pin-poll/index.ts, supabase/functions/promote-admin/index.ts, supabase/functions/rename-category/index.ts, supabase/functions/set-resolution/index.ts, supabase/functions/update-poll/index.ts</files>
  <read_first>
    - supabase/functions/_shared/audit.ts (Plan 02 output)
    - supabase/functions/demote-admin/index.ts (full file — UPDATE on profiles success path)
    - supabase/functions/pin-poll/index.ts (full, 83 lines — canonical retrofit example shown verbatim in PATTERNS.md "Audit retrofit"; `isPinned` body field branches the action string)
    - supabase/functions/promote-admin/index.ts (full file — TWO mutation branches: `target_user_id` profile UPDATE vs. `target_discord_id` admin_discord_ids INSERT + retroactive multi-profile UPDATE; the retrofit emits audit rows in BOTH branches per REVIEW-FIX-C2-H2)
    - supabase/functions/rename-category/index.ts (full file — old name capture for `before`)
    - supabase/functions/set-resolution/index.ts (full file — prior resolution capture)
    - supabase/functions/update-poll/index.ts (full file — RPC-based update; `after` captures only the fields the RPC mutated per D-07)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "Audit retrofit of 12 existing EFs" (per-EF table verbatim values; pin-poll insertion-point example verbatim)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-05 (13 emitters), D-07 (before/after JSONB shape per-EF)
  </read_first>
  <action>
    Same retrofit pattern as Task 03-01 for all 6 EFs: one import, one `writeAudit` call per state-changing path, no try/catch wrap, no response-shape change, no archaeology comments.

    Per-EF specifics:

    demote-admin: After profile UPDATE success, emit `action: 'admin_demoted'`, `target_type: 'profile'`, `target_id: <target user id from body>`, `before: { is_admin: true }`, `after: { is_admin: false }`.

    pin-poll: `action: isPinned ? 'poll_pinned' : 'poll_unpinned'` (ternary on the existing `isPinned` boolean from the request body), `target_type: 'poll'`, `before: { is_pinned: !isPinned }`, `after: { is_pinned: isPinned }`. This is the canonical example shown verbatim in PATTERNS.md and is the simplest retrofit.

    promote-admin (REVIEW-FIX-C2-H2 — two-branch retrofit per the live EF source):

    The live EF (`supabase/functions/promote-admin/index.ts`) has two mutation branches selected by which of `target_user_id` / `target_discord_id` is present in the request body (the EF rejects the request earlier if neither or both are set). The retrofit emits per-branch audit rows AFTER each branch's mutation success check and BEFORE its `return json(...)` success response:

    Branch 1 — `target_user_id` (existing profile UPDATE): after the existing `.update({ is_admin: true }).eq('id', target_user_id).select('id').single()` success path (the live EF block returning `{ success: true, mode: 'existing' }`), emit exactly ONE `writeAudit` call with `actor_id: user.id`, `action: 'admin_promoted'`, `target_type: 'profile'`, `target_id: target_user_id`, `before: { is_admin: false }`, `after: { is_admin: true }`.

    Branch 2 — `target_discord_id` (pre-auth + retroactive flip): after the existing `admin_discord_ids` INSERT success check (the block that ignores PostgreSQL 23505 idempotency errors), emit ONE `writeAudit` call with `actor_id: user.id`, `action: 'admin_preauthorized'`, `target_type: 'admin_discord_ids'`, `target_id: target_discord_id`, `before: null`, `after: { discord_id: target_discord_id }`. Then, after the retroactive `.update({ is_admin: true }).eq('discord_id', target_discord_id).select('id')` success check (which returns the `promotedProfiles` array — zero, one, or many rows), iterate that array and emit ONE `writeAudit` call PER returned profile with `actor_id: user.id`, `action: 'admin_promoted'`, `target_type: 'profile'`, `target_id: <profile.id>`, `before: { is_admin: false }`, `after: { is_admin: true }`. Use sequential `await` inside the loop — do NOT `Promise.all` (matches the close-expired-polls cron loop discipline). If `promotedProfiles` is empty (no matching profiles yet — the pre-auth user hasn't signed in), emit no `admin_promoted` rows in this branch — only the single `admin_preauthorized` row.

    The 23505-ignored idempotent case (`admin_discord_ids` row already existed) is a state-change no-op: when `insertError?.code === '23505'`, do NOT emit the `admin_preauthorized` row — the row already exists from a prior call (matches D-11 toggle no-op-no-audit precedent). Implementation: only call `writeAudit` for `admin_preauthorized` when `insertError` is `null`. The retroactive flip still runs and still audits any rows it flips.

    Do NOT change the EF's response shape (`{success, mode}` for both branches). Do NOT change validation, snowflake regex, or admin-gate code.

    rename-category: capture the prior name from the existing SELECT-before-UPDATE (reuse the EF's existing fetch if present; the EF likely already SELECTs for name-uniqueness validation). `action: 'category_renamed'`, `before: { name: <old> }`, `after: { name: <new from body> }`.

    set-resolution: capture prior `resolution` from a SELECT (the EF likely already fetches the poll to validate the state transition). `action: 'resolution_set'`, `before: { resolution: <prior> }`, `after: { resolution: <new from body> }`.

    update-poll: RPC-based — the EF calls an RPC (e.g., `update_poll_with_choices` or similar) for editing. `before` may be `null` if the RPC doesn't return prior values (per D-07 — compact, grep-able; do NOT add a separate pre-read SELECT just for audit). `action: 'poll_updated'`, `target_type: 'poll'`, `after: { ... only the fields the RPC mutated ... }` — list whatever fields are present in the request body (`title`, `description`, `image_url`, `category_id`, etc.) NOT the full row.

    Do NOT modify any of the 6 EFs' response shapes. Do NOT introduce new dependencies. Do NOT wrap any `writeAudit` call in try/catch. Do NOT add review-round / Plan / Round / PR archaeology in comments.
  </action>
  <acceptance_criteria>
    - All 6 EF files import `writeAudit`: `for f in demote-admin pin-poll promote-admin rename-category set-resolution update-poll; do grep -q "import.*writeAudit.*_shared/audit" supabase/functions/$f/index.ts || echo "MISSING: $f"; done` returns no MISSING
    - Each EF has ≥1 `writeAudit(` call
    - `demote-admin` uses `action: 'admin_demoted'` AND `target_type: 'profile'`: both grep checks return ≥1
    - `pin-poll` branches action string on `isPinned`: `grep -E "action:.*isPinned\\s*\\?\\s*'poll_pinned'\\s*:\\s*'poll_unpinned'" supabase/functions/pin-poll/index.ts` returns ≥1 match
    - REVIEW-FIX-C2-H2: `promote-admin` emits BOTH `admin_promoted` AND `admin_preauthorized` actions: `grep -cE "action:\\s*'admin_promoted'" supabase/functions/promote-admin/index.ts` returns ≥2 (one inside Branch 1 single-profile path; one inside Branch 2 retroactive loop) AND `grep -cE "action:\\s*'admin_preauthorized'" supabase/functions/promote-admin/index.ts` returns ≥1
    - REVIEW-FIX-C2-H2: `promote-admin` Branch 2 iterates `promotedProfiles` and audits per-row: `grep -B 3 "writeAudit" supabase/functions/promote-admin/index.ts | grep -E "(for \\(const|for\\s*\\(\\s*const).*(promotedProfiles|profile of)"` returns ≥1 match
    - REVIEW-FIX-C2-H2: `promote-admin` `admin_preauthorized` row uses `target_type: 'admin_discord_ids'`: `grep -A 5 "admin_preauthorized" supabase/functions/promote-admin/index.ts | grep -E "target_type:\\s*'admin_discord_ids'"` returns ≥1 match
    - REVIEW-FIX-C2-H2: `promote-admin` does NOT emit the `admin_preauthorized` row in the 23505 idempotent no-op path: `grep -B 5 "admin_preauthorized" supabase/functions/promote-admin/index.ts | grep -E "(insertError\\s*===\\s*null|!insertError|23505)"` returns ≥1 match (guard present)
    - `promote-admin` writeAudit calls use `target_type: 'profile'` for at least the Branch 1 single-profile path AND the Branch 2 retroactive flip path: `grep -cE "target_type:\\s*'profile'" supabase/functions/promote-admin/index.ts` returns ≥2
    - `rename-category` uses `action: 'category_renamed'` AND captures old name in `before`: `grep -E "before:\\s*\\{\\s*name:" supabase/functions/rename-category/index.ts` returns ≥1
    - `set-resolution` uses `action: 'resolution_set'`
    - `update-poll` uses `action: 'poll_updated'`
    - No file wraps `writeAudit` in try/catch: `for f in demote-admin pin-poll promote-admin rename-category set-resolution update-poll; do grep -B 1 "writeAudit(" supabase/functions/$f/index.ts | grep -E "^\\s*try\\s*\\{$"; done` returns nothing
    - No file gains review-round / phase-ID archaeology
    - `npm run lint` exits 0 on all 6 modified files
    - Existing unit suite passes: `npm run test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>for f in demote-admin pin-poll promote-admin rename-category set-resolution update-poll; do grep -q "import.*writeAudit.*_shared/audit" supabase/functions/$f/index.ts && grep -q "writeAudit(" supabase/functions/$f/index.ts || { echo "FAIL $f"; exit 1; }; done && grep -qE "action:\\s*'admin_demoted'" supabase/functions/demote-admin/index.ts && grep -qE "action:.*isPinned\\s*\\?\\s*'poll_pinned'\\s*:\\s*'poll_unpinned'" supabase/functions/pin-poll/index.ts && grep -qE "action:\\s*'admin_promoted'" supabase/functions/promote-admin/index.ts && grep -qE "action:\\s*'poll_updated'" supabase/functions/update-poll/index.ts && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>
    All 6 EFs in batch B emit audit rows on state change. `pin-poll` branches action on `isPinned`. `demote-admin` and `promote-admin` use `target_type: 'profile'`. No try/catch wraps any audit call. No response-shape changes. Lint + unit suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin JWT → each EF | Existing `requireAdmin` gate unchanged; retrofit must NOT introduce any code that runs before the gate |
| EF mutation → audit_log | Audit write must come AFTER the mutation success check; an unwritable audit must not be observable to the caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-AUDIT-DRIFT | Repudiation | 11 retrofitted EFs | mitigate (audit integrity) | Per-EF locked action string + before/after shape (PATTERNS.md table) ensures consistent grep-ability across the 13 emitters (11 here + create-poll in Plan 11-03b + toggle-results-visibility in Plan 02). Acceptance criteria grep each action string literal per EF. No EF wraps `writeAudit` in try/catch (Pitfall 3); helper handles its own error path. |
| T-AUDIT-FAIL-OPEN | Repudiation | All 11 EFs | accept | Per Pitfall 3, audit failure does not fail the user-facing mutation. Same disposition as Plan 02. The 13 emitters collectively cover every admin state change in v1.2; sporadic gaps log to Supabase Function Logs. |
| T-CRON-ACTOR-NULL | Repudiation | close-expired-polls cron audit | mitigate | D-03 explicitly admits `actor_id: null` for system actions. `audit_log.actor_id` is NULLABLE in migration 10 schema. Acceptance criteria asserts the `null` literal in the cron's audit call. |
| T-RESPONSE-SHAPE-DRIFT | Tampering | 11 retrofitted EFs | mitigate | Acceptance criteria assert no `json(...)` shape changes via grep + git-diff review. Only `toggle-results-visibility` (Plan 02) returns the full poll row; the 11 retrofitted EFs preserve their pre-existing `{ success: true }` (or equivalent) shape. |
</threat_model>

<verification>
- `npm run lint` passes with zero new warnings (no unused `writeAudit` imports)
- `npm run test` (unit suite) passes — no EF logic regression
- Per-EF git diff is small: import line + audit-call block. Total diff per EF should be 10-20 lines.
- Plan 04 (Wave 3) provides runtime verification via TEST-12 (audit row presence/absence on toggle-results-visibility) — but does NOT directly test the 11 retrofitted EFs. The retrofit's correctness is verified at deploy time (Plan 05) by smoke-invoking each EF and confirming an `audit_log` row appears.
- create-poll retrofit is verified separately in Plan 11-03b.
</verification>

<success_criteria>
- VIS-02 partially covered for this plan ("every change writes a new row to audit_log" — 11 of 13 emitters live; create-poll covered in Plan 11-03b; toggle-results-visibility covered in Plan 02)
- D-05 partially covered (11 of 12 mutation admin EFs retrofitted; create-poll covered in Plan 11-03b)
- No response-shape drift across the 11 retrofitted EFs (Phase 12 admin UI continues to work against the pre-existing shapes)
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-rls-ef-foundations/11-03-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Declare `requirements-completed: [VIS-02 (partial — combined with Plan 02 and Plan 11-03b)]`.
</output>
