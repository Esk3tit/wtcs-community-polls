---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 04
type: execute
wave: 2
depends_on: ["12-00"]
files_modified:
  - src/hooks/useVoteCounts.ts
  - src/components/suggestions/SuggestionCard.tsx
  - src/components/suggestions/SuggestionList.tsx
autonomous: true
requirements:
  - VIS-08
tags:
  - vis-08
  - voter-ui
  - shadcn
  - alert
  - polling

must_haves:
  truths:
    - "VIS-08 D-09: `SuggestionCard` renders a shadcn `<Alert>` block with `<EyeOff>` lucide icon and the locked title `'Results temporarily hidden by admin'` when BOTH `userChoiceId` is set (voter has voted) AND `resultsHidden === true`. The Alert sits in the same vertical slot as `<ResultBars>` (i.e., replaces it in that branch of the existing ternary at line 125 of SuggestionCard.tsx)"
    - "VIS-08 D-10: A voter's-choice line `<p className=\"text-sm text-muted-foreground\">Your response: <span className=\"text-foreground font-medium\">{label}</span></p>` renders ABOVE the Alert. The `{label}` is resolved from `userChoiceId` against `suggestion.choices` (the choices array already on `SuggestionWithChoices`). If the lookup fails (defensive — should not happen post-Plan-00), render `'Your response: (unknown)'` and surface a Sentry breadcrumb"
    - "VIS-08 D-12: The Alert title copy is uniform `'Results temporarily hidden by admin'` for both live and archived polls — no adaptive copy for closed polls"
    - "VIS-08 D-11: `useVoteCounts` is extended to also fetch `results_hidden` from `polls_effective` on the SAME 8s polling cadence (existing `usePolling`-driven). New return shape: `{ voteCounts, resultsHidden: Map<string, boolean>, refetchVoteCounts }`. The Map keys are poll IDs that the voter has voted on (matches the existing `votedPollIds` input filter); values are the boolean `results_hidden` flag from `polls_effective`. SELECTs happen against `polls_effective` (NOT `polls`) to preserve the Phase 11 VIS-09 invariant (zero direct `from('polls')` reads in `src/`)"
    - "VIS-08 D-11: The voter UI auto-updates within ~8s of an admin flip — when the admin hides via Plan 03 Switch, the voter's next `useVoteCounts` poll cycle pulls the new `results_hidden = true`, the SuggestionCard branch flips to render the Alert, and the voter sees the hidden-state UI without a page reload. The reverse direction works identically (hidden → visible)"
    - "VIS-08 call-site wiring (cross-AI review HIGH-fix): `src/components/suggestions/SuggestionList.tsx` is the SOLE consumer of `useVoteCounts` and the SOLE renderer of `<SuggestionCard />` in `src/`. The route files `src/routes/topics.tsx` and `src/routes/archive.tsx` render `<SuggestionList />` and need NO modification. The `resultsHidden` Map is destructured in SuggestionList and passed to each card as `resultsHidden={resultsHidden.get(suggestion.id) ?? false}`."
    - "VIS-08 test mocks (cross-AI review MEDIUM): if `src/__tests__/components/suggestions/SuggestionList.test.tsx` (or analog) mocks `useVoteCounts`, the mock factory MUST return `{ voteCounts, resultsHidden, refetchVoteCounts }` — otherwise the destructure yields `undefined.get(...)` at render time. Discover via `grep -rn 'vi.mock.*useVoteCounts' src/__tests__/`."
    - "VIS-08 RLS safety (Phase 11 VIS-04): `vote_counts` SELECT is RLS-gated so that even if the React branch is bypassed (e.g., admin flips results_hidden between two of the voter's poll cycles), the underlying `vote_counts` query returns 0 rows for `results_hidden = true` polls. The UI defense is layered on top of the existing DB defense — no leak path"
    - "VIS-08 archive view inherits: the same `SuggestionCard` is used by the archive view per Phase 2 architecture (no archive-specific component); the Alert renders identically on archived polls (D-12 uniform copy)"
    - "VIS-08 testability: `data-testid={`results-hidden-alert-${pollId}`}` is on the Alert root element so Plan 06 TEST-13 has a locked locator that does NOT depend on the copy string"
    - "useVoteCounts extension is type-safe via Plan 00's regenerated `database.types.ts` — `Tables<'polls_effective'>` carries `results_hidden: boolean` and `results_hidden_changed_at: string | null`"
    - "Existing test `src/__tests__/admin/polls-effective-invariant.test.ts` continues to pass — the new SELECT targets `polls_effective` (the canonical projection), not `polls` directly"
    - "`npm run lint`, `tsc -b`, and `npm run test` all pass"
  artifacts:
    - path: "src/hooks/useVoteCounts.ts"
      provides: "Extended hook returning voteCounts AND resultsHidden Map, polled on the same 8s cadence"
      exports: ["useVoteCounts"]
    - path: "src/components/suggestions/SuggestionCard.tsx"
      provides: "Three-way branch in the results slot: hidden-alert when (voted AND hidden), ResultBars when (voted AND not hidden), ChoiceButtons when (not voted)"
      contains: "Results temporarily hidden by admin"
  key_links:
    - from: "src/hooks/useVoteCounts.ts"
      to: "polls_effective view"
      via: "supabase.from('polls_effective').select('id, results_hidden').in('id', votedPollIds)"
      pattern: "polls_effective"
    - from: "src/components/suggestions/SuggestionCard.tsx"
      to: "src/hooks/useVoteCounts.ts (resultsHidden map)"
      via: "prop or wrapper consumer (existing useVoteCounts caller passes the map down)"
      pattern: "resultsHidden"
---

<objective>
Implement VIS-08: the voter-side hidden-state experience. Extend `useVoteCounts` to also fetch `results_hidden` from `polls_effective` at the same 8s polling cadence (D-11). Branch `SuggestionCard.tsx` so a voter who has voted on a `results_hidden = true` suggestion sees an `<Alert>` block ("Results temporarily hidden by admin") with their own choice surfaced above (D-09 + D-10). Live polling auto-updates within ~8s of an admin flip.

Purpose: Closes the voter-facing half of v1.2 admin visibility. The Phase 11 RLS policy (VIS-04) already blocks `vote_counts` reads when `results_hidden = true` — but without the UI branch, voters who have already voted would see an empty count bar, not a clear "results hidden" explanation. D-09/D-10 + the live-polling D-11 give voters a graceful, informative state that survives an admin flip mid-session.

Output: 1 extended hook (return shape grows with `resultsHidden` Map + the new polling SELECT against `polls_effective`); 1 edited component (three-way branch replaces the two-way ternary in the results slot, voter's-choice line above the Alert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Source-of-truth files (executor MUST read before editing) -->
@src/hooks/useVoteCounts.ts
@src/hooks/usePolling.ts
@src/components/suggestions/SuggestionCard.tsx
@src/components/suggestions/ResultBars.tsx
@src/components/suggestions/ChoiceButtons.tsx
@src/components/ui/alert.tsx
@src/lib/types/suggestions.ts

<!-- Existing useVoteCounts caller: src/components/suggestions/SuggestionList.tsx -->
<!-- Routes (src/routes/topics.tsx, src/routes/archive.tsx) render <SuggestionList />; SuggestionList is the sole consumer of useVoteCounts and the sole renderer of <SuggestionCard />. The route files do NOT need editing — all wiring happens in SuggestionList.tsx (verified via grep against src/). -->

<!-- Phase 11 RLS defense (read-only — DO NOT MODIFY) -->
@supabase/migrations/00000000000010_results_hidden_audit.sql

<interfaces>
<!-- Current useVoteCounts return: -->
<!--   { voteCounts: Map<pollId, Map<choiceId, count>>, refetchVoteCounts: () => Promise<void> } -->

<!-- Extended return: -->
<!--   {
<!--     voteCounts: Map<pollId, Map<choiceId, count>>,
<!--     resultsHidden: Map<pollId, boolean>,
<!--     refetchVoteCounts: () => Promise<void>
<!--   } -->

<!-- New SELECT inside fetchCounts (alongside the existing vote_counts SELECT): -->
<!--   const { data: hiddenRows } = await supabase
<!--     .from('polls_effective')
<!--     .select('id, results_hidden')
<!--     .in('id', ids)
<!--   // build Map<id, results_hidden> -->

<!-- SuggestionCard branch shape (replaces the existing two-way ternary at line 125): -->
<!--
{userChoiceId && resultsHidden
  ? <HiddenAlert userChoice={...} pollId={suggestion.id} />
  : userChoiceId
    ? <ResultBars ... />
    : <ChoiceButtons ... />}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-04-01: Extend useVoteCounts to fetch results_hidden from polls_effective on the same polling cadence</name>
  <files>src/hooks/useVoteCounts.ts</files>
  <read_first>
    - src/hooks/useVoteCounts.ts (entire file — 65 lines; the extension adds one new SELECT inside `fetchCounts` and grows the return shape; existing polling + ref stabilization pattern is preserved)
    - src/hooks/usePolling.ts (the polling driver — confirm it polls a single callback at the configured interval; no per-source separation needed)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-11 (locked: extend `useVoteCounts` to also re-fetch `results_hidden` from `polls_effective` at the same 8s polling cadence; one extra small SELECT per poll cycle; free-tier safe at 20-30 concurrent users)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "3. VIS-08 Hidden-state Alert" "Live updates (D-11)" (within ~8s; reverse direction works the same; no page reload)
    - supabase/migrations/00000000000010_results_hidden_audit.sql (confirms `polls_effective` view projects `results_hidden`)
    - src/__tests__/admin/polls-effective-invariant.test.ts (the invariant: zero new direct `from('polls')` reads in `src/`. The new SELECT MUST target `polls_effective`, not `polls`.)
    - src/lib/types/database.types.ts (post-Plan-00 — confirm `polls_effective.Row` carries `results_hidden: boolean`)
  </read_first>
  <action>
    Edit `src/hooks/useVoteCounts.ts`. The current hook holds one piece of state (`voteCounts`) and fetches one source (`vote_counts`). The extension adds a second piece of state and a second SELECT, batched into the same `fetchCounts` so both data sources update on the same 8s tick.

    Step 1 — add a second state hook adjacent to `voteCounts`:
    `const [resultsHidden, setResultsHidden] = useState<Map<string, boolean>>(new Map())`

    Step 2 — extend `fetchCounts`. After the existing `supabase.from('vote_counts').select(...).in('poll_id', ids)` query, add a parallel SELECT against `polls_effective`:

    `const { data: hiddenRows, error: hiddenErr } = await supabase.from('polls_effective').select('id, results_hidden').in('id', ids)`

    Run the two queries in parallel (`Promise.all`) to keep the round-trip cost flat at 1 RTT instead of 2. The existing query is awaited; restructure to:

    ```
    const [vcResult, hiddenResult] = await Promise.all([
      supabase.from('vote_counts').select('poll_id, choice_id, count').in('poll_id', ids),
      supabase.from('polls_effective').select('id, results_hidden').in('id', ids),
    ])
    ```

    Then destructure each result independently. Preserve the existing error-handling shape: if the `vote_counts` query errors, the hook keeps the previous `voteCounts` (existing behavior). Apply the same pattern to the new hidden query — on error, keep the previous `resultsHidden` map (do NOT reset to empty; a transient network blip should not flip the UI to "results visible" if the prior state said hidden). Log via `console.error('Failed to fetch results_hidden:', hiddenErr)` consistent with the existing `console.error('Failed to fetch vote counts:', error)` line.

    Step 3 — build the `resultsHidden` Map from `hiddenRows`:
    ```
    if (hiddenRows) {
      const hMap = new Map<string, boolean>()
      for (const row of hiddenRows) {
        hMap.set(row.id, Boolean(row.results_hidden))
      }
      setResultsHidden(hMap)
    }
    ```
    The `Boolean(...)` coerce is defensive against the type-regen edge case where the column might come through as `boolean | null` from `polls_effective` (the underlying `polls.results_hidden` is `NOT NULL` per migration 10 — but the view's type-level nullability may differ depending on how `gen types` infers view nullability). Per project convention, default to `false` (visible) on any null reading.

    Step 4 — extend the return shape:
    `return { voteCounts, resultsHidden, refetchVoteCounts: fetchCounts }`

    Step 5 — empty-input handling. The existing `if (ids.length === 0) { setVoteCounts(new Map()); return }` early-out should also clear `resultsHidden`: add `setResultsHidden(new Map())` alongside `setVoteCounts(new Map())`. This handles the case where a voter has not yet voted on anything — the hidden map is empty (consistent with voteCounts being empty).

    Step 6 — verify the existing callers. Run `grep -rn 'useVoteCounts' src/ --include='*.tsx' --include='*.ts' | grep -v __tests__ | grep -v useVoteCounts.ts`. The sole production caller is `src/components/suggestions/SuggestionList.tsx` (verified at plan time). It currently destructures `{ voteCounts }` from the hook. Adding `resultsHidden` to the return is purely additive — the existing destructure continues to work because it simply ignores the new property. Task 12-04-02 updates SuggestionList to destructure `resultsHidden` and forward it to each `<SuggestionCard />`.

    Comment policy: the new SELECT block deserves ONE WHY-only comment per project source-comment policy. Suggested: `// VIS-08 D-11: results_hidden polled on the same 8s cadence so voter UI auto-updates within ~8s of an admin flip. Targets polls_effective (not polls direct) to preserve the Phase 11 VIS-09 invariant.` No phase/plan/round IDs in source per CLAUDE.md feedback.
  </action>
  <verify>
    <automated>grep -q 'resultsHidden' src/hooks/useVoteCounts.ts && grep -q "from('polls_effective')" src/hooks/useVoteCounts.ts && grep -q 'Promise.all' src/hooks/useVoteCounts.ts && npm run lint && npx tsc -b && npm run test -- --run src/__tests__/admin/polls-effective-invariant
  </verify>
  <done>
    `useVoteCounts` returns `{ voteCounts, resultsHidden, refetchVoteCounts }`; both SELECTs are batched in `Promise.all`; the new SELECT targets `polls_effective` (not `polls`); the `polls-effective-invariant.test.ts` continues to pass; lint + tsc green.
  </done>
</task>

<task type="auto">
  <name>Task 12-04-02: Branch SuggestionCard for the hidden-state Alert + voter's-choice line + wire resultsHidden through SuggestionList</name>
  <files>src/components/suggestions/SuggestionCard.tsx, src/components/suggestions/SuggestionList.tsx</files>
  <read_first>
    - src/components/suggestions/SuggestionCard.tsx (entire file — 176 lines; the change is scoped to the results slot around line 125 — the existing `userChoiceId ? <ResultBars> : <ChoiceButtons>` ternary becomes a three-way branch)
    - src/components/suggestions/SuggestionList.tsx (entire file — the SOLE call-site for `<SuggestionCard />` and the SOLE consumer of `useVoteCounts` per `grep -rn 'useVoteCounts\|<SuggestionCard' src/`; this task must update its `useVoteCounts` destructure to include `resultsHidden` and pass it to every rendered SuggestionCard)
    - src/__tests__/components/suggestions/SuggestionList.test.tsx (or equivalent — discover via `ls src/__tests__/components/suggestions/`; if a test exists that mocks `useVoteCounts`, the mock factory must be updated to return `{ voteCounts, resultsHidden, refetchVoteCounts }` — cross-AI review MEDIUM: mocks without `resultsHidden` will type-fail or render `undefined`)
    - src/components/ui/alert.tsx (existing primitive — confirms `Alert`, `AlertTitle` (and optionally `AlertDescription`) export shape; usage precedent in SuggestionForm error state)
    - src/components/suggestions/ResultBars.tsx (confirm the existing render block dimensions so the Alert sits in the equivalent vertical slot without layout regression)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-09 + D-10 + D-12 (locked Alert primitive, locked voter's-choice line, uniform copy live + archived)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "3. VIS-08 Hidden-state Alert" (JSX shape, `space-y-3` wrapper around voter's-choice + Alert, locked AlertTitle copy)
    - src/lib/types/suggestions.ts (confirm `SuggestionWithChoices.choices` is `Array<{ id, label, ... }>` so the voter's-choice label lookup works)
    - Routes that render `<SuggestionList />` — `src/routes/topics.tsx`, `src/routes/archive.tsx`. These do NOT need editing (cross-AI review HIGH-fix: SuggestionList is the abstraction layer between routes and SuggestionCard). The routes pass only `status` and `focusId` props.
  </read_first>
  <action>
    **Two-part edit, both in SuggestionCard.tsx:**

    **Part A — add the new prop.**

    The current `Props` shape (see lines 17-27 of the file) accepts `userChoiceId: string | undefined`, `voteCounts: Map<string, Map<string, number>>` (or similar), `totalResponses`, `onVote`, etc. Add a new prop:

    `resultsHidden: boolean`

    Place it adjacent to `userChoiceId` since they are paired conceptually (both gate the results slot). Default to `false` at the call site if not provided (defensive — existing callers that haven't been updated yet won't compile-fail because we'll update the call sites in this same task).

    **Part B — three-way branch in the results slot.**

    The current JSX at lines 124-144 reads roughly:
    ```
    <div className="mt-4">
      {userChoiceId ? (
        <ResultBars choices={...} voteCounts={...} userChoiceId={...} totalResponses={...} />
      ) : (
        <ChoiceButtons choices={...} pollId={...} pollStatus={...} hasVoted={false} onVote={...} submittingPollId={...} submittingChoiceId={...} totalResponses={...} />
      )}
    </div>
    ```

    Replace with a three-way branch:
    ```
    <div className="mt-4">
      {userChoiceId && resultsHidden ? (
        <div className="space-y-3" data-testid={`results-hidden-alert-${suggestion.id}`}>
          <p className="text-sm text-muted-foreground">
            Your response:{' '}
            <span className="text-foreground font-medium">
              {suggestion.choices.find((c) => c.id === userChoiceId)?.label ?? '(unknown)'}
            </span>
          </p>
          <Alert>
            <EyeOff className="h-4 w-4" aria-hidden="true" />
            <AlertTitle>Results temporarily hidden by admin</AlertTitle>
          </Alert>
        </div>
      ) : userChoiceId ? (
        <ResultBars choices={...preserve existing props...} />
      ) : (
        <ChoiceButtons {...preserve existing props...} />
      )}
    </div>
    ```

    Add imports at the top: `Alert`, `AlertTitle` from `@/components/ui/alert` (the file already imports `EyeOff` if it doesn't, add it from `lucide-react`; check existing lucide imports first to keep imports tidy).

    The `data-testid` is on the OUTER wrapper div (the `space-y-3` block) so Plan 06 TEST-13 can target the entire hidden-state block (testid contains pollId for unambiguous targeting in lists). The Alert itself does not need a separate testid; the wrapper's testid covers the test surface.

    The choice label lookup uses `suggestion.choices.find((c) => c.id === userChoiceId)?.label` — `suggestion.choices` is `SuggestionWithChoices.choices` already on the props (Phase 2 architecture; verify by reading the type), so no new prop is needed for the lookup. The `?? '(unknown)'` fallback is defensive — in practice the lookup always finds the row because `userChoiceId` is set only after a successful vote against one of the suggestion's choices.

    **Part C — wire `resultsHidden` through `SuggestionList.tsx` (the SOLE call-site for `<SuggestionCard />`).**

    Cross-AI review HIGH-fix: route files (`src/routes/topics.tsx`, `src/routes/archive.tsx`) render `<SuggestionList />`, not `<SuggestionCard />`. SuggestionList is the abstraction that consumes `useVoteCounts` and renders the cards. The wiring lives entirely in SuggestionList — routes need no edits.

    Open `src/components/suggestions/SuggestionList.tsx`. The current destructure (line ~39) reads:
    ```
    const { voteCounts } = useVoteCounts(votedPollIds, enablePolling)
    ```
    Update to:
    ```
    const { voteCounts, resultsHidden } = useVoteCounts(votedPollIds, enablePolling)
    ```
    (`refetchVoteCounts` is not destructured today and stays as-is unless a future caller needs it.)

    Then at the SuggestionCard render (line ~145), add the new prop:
    ```
    <SuggestionCard
      ...existing props...
      resultsHidden={resultsHidden.get(suggestion.id) ?? false}
    />
    ```
    The `?? false` default mirrors the DB DEFAULT (visible) — an absent map entry behaves as "visible". This is the SAFE direction: defaulting to `true` (hidden) could blank the UI for unvoted polls; defaulting to `false` keeps the existing visible behavior and the Phase 11 RLS still gates count-bar data at the DB layer.

    Add a WHY-only comment near the new prop wiring: `// VIS-08: results_hidden polled at the useVoteCounts cadence; map-miss defaults to visible (RLS enforces no-leak independently).` Single comment; no plan/phase IDs.

    **Test mock updates (cross-AI review MEDIUM-fix).** If a test file mocks `useVoteCounts` (search via `grep -rn "vi.mock.*useVoteCounts\|jest.mock.*useVoteCounts" src/__tests__/`), update the mock factory to return `{ voteCounts: new Map(), resultsHidden: new Map(), refetchVoteCounts: vi.fn() }` so the destructure does not yield `undefined.get(...)` at runtime. Likely candidate: `src/__tests__/components/suggestions/SuggestionList.test.tsx` (or wherever the existing mock lives). If no such mock exists, this step is a no-op — verify by searching.

    Add a WHY-only comment near the new branch in SuggestionCard.tsx: `// VIS-08: hidden state precedes the visible branch; admin policy + voter-status both required for the placeholder. RLS additionally enforces zero count leakage at the DB layer.` Single comment; no plan/phase IDs.
  </action>
  <verify>
    <automated>grep -q 'Results temporarily hidden by admin' src/components/suggestions/SuggestionCard.tsx && grep -q "data-testid={\`results-hidden-alert-" src/components/suggestions/SuggestionCard.tsx && grep -q 'Your response:' src/components/suggestions/SuggestionCard.tsx && grep -q 'EyeOff' src/components/suggestions/SuggestionCard.tsx && grep -q 'resultsHidden' src/components/suggestions/SuggestionList.tsx && grep -q 'resultsHidden\.get(' src/components/suggestions/SuggestionList.tsx && npm run lint && npx tsc -b && npm run test
  </verify>
  <done>
    SuggestionCard renders the three-way branch correctly; `resultsHidden` prop added; voter's-choice line + Alert + EyeOff icon render when (voted AND hidden); `SuggestionList.tsx` destructures `resultsHidden` from the extended `useVoteCounts` and forwards it as a prop to every rendered SuggestionCard (the routes themselves are unchanged — they render `<SuggestionList />`); test mocks for `useVoteCounts` (if any) are updated to include `resultsHidden`; lint + tsc + tests pass; archive view inherits identically (also routed through `<SuggestionList status="closed" />`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → polls_effective view (RLS-gated) | The new SELECT is anon-key client → public view; the view exposes only the row columns Phase 11 VIS-09 chose to project, gated by `security_invoker = on` which inherits the caller's RLS perspective. |
| browser → vote_counts view (RLS-gated) | Pre-existing — Phase 11 VIS-04 enforces voted-AND-not-hidden gating at the DB level. The Phase 12 UI branch is a usability layer above this defense. |
| local React state ↔ live admin flips | Voter's UI may show stale "visible" for up to ~8s after admin flips to hidden. The DB defense (VIS-04 RLS) closes this gap by returning 0 rows from `vote_counts` immediately — the only thing stale is the *labelling*, not the data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | Information Disclosure | Voter sees count bars during the ~8s stale window | mitigate | Phase 11 VIS-04 RLS policy returns 0 rows from `vote_counts` IMMEDIATELY upon `results_hidden = true` — independent of the UI branch. Worst-case stale window: voter sees an EMPTY ResultBars rendering (0 / N) until the next 8s tick swaps the branch to the Alert. No actual count leakage. |
| T-12-04-02 | Information Disclosure | Voter's-choice line "Your response: {label}" | accept | The voter's choice is the voter's own data, not leaked aggregate. Visible only to the voter who cast that vote — userChoiceId is sourced from their own vote row. |
| T-12-04-03 | Spoofing | Tampered `userChoiceId` prop via React DevTools | accept | A tampered prop could display a different choice label, but: (a) only on the local browser, (b) the data shown is from the suggestion's own choice list (also a public read), (c) does not affect any persisted state or other users. |
| T-12-04-04 | Tampering | Voter mutates `resultsHidden` prop to bypass the branch | accept | Same as T-12-04-01 — even if the React branch is bypassed, the underlying `vote_counts` SELECT returns 0 rows for hidden polls per VIS-04 RLS. The branch IS the usability layer, not the security layer. |
| T-12-04-05 | Repudiation | No audit on voter-side renders | accept | Voter UI reads do not require audit per v1.2 scope. Admin write-side audit is covered by Phase 11 D-04 (silent audit_log in v1.2). |
| T-12-04-06 | Denial of Service | Doubled SELECT cost (vote_counts + polls_effective per poll cycle) | mitigate | Batched via `Promise.all` — 1 RTT instead of 2. Per CONTEXT estimate: "free-tier safe at 20-30 concurrent users". v1.2 closure should validate against Supabase's free-tier 500 MB DB + 2M EF invocation cap (the new SELECT is against the view, not an EF — closer to the connection-pool concern than the EF quota). |
| T-12-04-07 | Elevation of Privilege | Non-voter bypasses the branch to read results | accept | The three-way branch's `userChoiceId &&` outer gate is purely cosmetic — non-voters never reach the ResultBars OR the Alert; they see `ChoiceButtons`. The same VIS-04 RLS that gates voted-voters also gates non-voters (the policy requires voted=true). |

</threat_model>

<verification>
- `useVoteCounts` returns `{ voteCounts, resultsHidden, refetchVoteCounts }`
- The new SELECT targets `polls_effective` (NOT `polls`)
- Both SELECTs are batched in `Promise.all`
- The hidden-state branch in SuggestionCard renders only when `userChoiceId && resultsHidden` are both truthy
- The voter's-choice line renders above the Alert with the resolved choice label
- AlertTitle copy is the verbatim locked string "Results temporarily hidden by admin"
- `data-testid={`results-hidden-alert-${pollId}`}` is on the wrapper div
- `src/components/suggestions/SuggestionList.tsx` destructures `resultsHidden` from `useVoteCounts` and forwards `resultsHidden={resultsHidden.get(suggestion.id) ?? false}` to each `<SuggestionCard />`
- Route files (`src/routes/topics.tsx`, `src/routes/archive.tsx`) are NOT modified — they continue to render `<SuggestionList />` only
- Any existing test mocks for `useVoteCounts` are updated to return `{ voteCounts, resultsHidden, refetchVoteCounts }` (or the suite reports `undefined.get` failures pre-fix)
- `polls-effective-invariant.test.ts` continues to pass
- `npm run lint`, `tsc -b`, `npm run test` all pass
- Manual smoke: in dev, two browsers (admin + voter), admin flips Switch in Plan 03 UI; within ~8s the voter's card transitions from ResultBars to the Alert; reverse flip transitions back
</verification>

<success_criteria>
- VIS-08 fully wired — voters see informative hidden-state UI with their own choice preserved
- D-11 live polling propagates admin flips within ~8s without page reload
- D-12 uniform copy across live AND archive views (single SuggestionCard component)
- Phase 11 RLS (VIS-04) remains the security layer; UI is the usability layer
- TEST-13 (Plan 06) can target the Alert wrapper via `[data-testid="results-hidden-alert-{pollId}"]`
- Zero direct `from('polls')` reads introduced in `src/`; invariant test passes
- Call-site wiring lives in `SuggestionList.tsx`, not in route files (cross-AI review HIGH closed)
- Test mocks for `useVoteCounts` (if any) updated to return `resultsHidden` (cross-AI review MEDIUM closed)
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-04-SUMMARY.md`
</output>
