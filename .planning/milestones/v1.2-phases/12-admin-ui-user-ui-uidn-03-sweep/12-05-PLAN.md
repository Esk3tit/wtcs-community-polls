---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 05
type: execute
wave: 2
depends_on: ["12-00"]
files_modified:
  - src/components/suggestions/form/DropZone.tsx
  - src/components/suggestions/form/ImageInput.tsx
autonomous: true
requirements:
  - UIDN-03
tags:
  - uidn-03
  - dropzone
  - shadcn
  - a11y

must_haves:
  truths:
    - "UIDN-03 D-13: New component `src/components/suggestions/form/DropZone.tsx` exports `DropZone`. The outer `<div>` carries `role=\"region\"`, `aria-label=\"Image upload\"`, the drag handlers (`onDragEnter/Over/Leave/Drop`), and the dashed-border + state styling — NOT a button (no `tabIndex`, no `role=\"button\"`, no `aria-label` as a button). The inner `<Button variant=\"outline\" size=\"sm\">Browse files</Button>` is the keyboard-activatable Browse trigger"
    - "UIDN-03 D-13: The outer region's state styling: idle = `bg-muted/30` + dashed border; drag-over = `bg-muted/60 ring-2 ring-ring` (matches shadcn focus-ring convention); uploading = `bg-muted/30` (no ring; Button hidden, replaced by inline Loader2 + 'Uploading…' text); error = `border-destructive bg-destructive/5` (carried from Phase 4 §9). All four states reproduce the existing visual contract"
    - "UIDN-03 D-13: Idle-state content renders `<ImagePlus className=\"h-8 w-8 text-muted-foreground\" />` + `<p className=\"text-sm font-medium\">Drop an image here</p>` + `<p className=\"text-xs text-muted-foreground\">JPG, PNG, or WebP · max 2 MB</p>` + the inner Browse Button. The headline is shortened from the old combined wording (\"Drop an image here, or click to browse\") because the Browse button now visually carries the click-to-browse affordance"
    - "UIDN-03 D-13: ImageInput.tsx removes the existing `<button>` element on lines 107-126 (the dual-role drag+click target) and renders `<DropZone>` in its place. The hidden `<input type=\"file\">` stays in ImageInput.tsx (preserves the existing fileRef-driven file picker); DropZone calls a `onBrowseClick` callback prop to trigger `fileRef.current?.click()`"
    - "UIDN-03 D-13: The DropZone component accepts props `{ disabled?: boolean; uploading: boolean; dragOver: boolean; dropError: string | null; onDrop: (file: File) => void; onBrowseClick: () => void; onDragStateChange: (over: boolean) => void }` — minimal, single-purpose. State (`uploading`, `dragOver`, `dropError`) is OWNED by ImageInput and PASSED IN; DropZone is purely presentational + event-forwarding"
    - "UIDN-03 D-13 ARIA contract: The outer region has `role=\"region\"` + `aria-label=\"Image upload\"`. The inner Button has `aria-label=\"Browse files\"`. The drag-over visual is decorative; screen readers do not announce it. The error message renders inside an `<Alert>` block below the region (NOT inside the region itself) — preserves the existing `aria-live=\"polite\" role=\"status\"` wrapper from ImageInput.tsx lines 162-170"
    - "UIDN-03 D-13 keyboard: Tab focuses the Browse Button; Enter/Space activates it; the drag handlers are pointer-only — keyboard users use the Button (the intentional fix for the dual-role footnote [c])"
    - "UIDN-03 D-13: Existing image-upload behavior preserved — Drop a file → validates size + MIME → triggers `handleFile` (the existing path); Click Browse → opens hidden file input → onChange triggers the same `validateAndAccept` validator → `handleFile`; both paths arrive at the same `uploadImage(file)` call"
    - "Both the existing image-upload tests (if any) and the existing E2E specs that interact with the image input continue to pass; the inner Button's text \"Browse files\" replaces the old combined headline phrase \"or click to browse\""
    - "D-19: UIDN-03-FOLLOWUP-LIST-CARDS stays deferred to v1.3. The hand-rolled list containers in `AdminsList`, `CategoriesList`, and `PromoteAdminDialog` (the `divide-y border rounded-md` pattern) are NOT folded into Phase 12. Plan 05 closes the locked UIDN-03 four-site sweep ONLY (SearchBar + SuggestionForm ×2 + ImageInput DropZone). Phase 12 stays tight to the audit's confirmed FAIL cells; the FOLLOWUP-LIST-CARDS items remain on the v1.3+ backlog"
    - "`npm run lint`, `tsc -b`, `npm run test` all pass"
  artifacts:
    - path: "src/components/suggestions/form/DropZone.tsx"
      provides: "Decoupled drag-region + Browse-button DropZone component (D-13)"
      exports: ["DropZone"]
    - path: "src/components/suggestions/form/ImageInput.tsx"
      provides: "ImageInput consumes DropZone; native dual-role <button> removed (UIDN-03 footnote [c] closed)"
      contains: "DropZone"
  key_links:
    - from: "src/components/suggestions/form/ImageInput.tsx"
      to: "src/components/suggestions/form/DropZone.tsx"
      via: "import + render"
      pattern: "from './DropZone'"
    - from: "src/components/suggestions/form/DropZone.tsx"
      to: "@/components/ui/button"
      via: "import Button"
      pattern: "from '@/components/ui/button'"
---

<objective>
Resolve UIDN-03 footnote [c] (D-13): extract a new `<DropZone>` component that separates the drag-region (outer `<div role="region">`) from the Browse trigger (inner shadcn `<Button variant="outline" size="sm">`). The current `ImageInput.tsx:108` renders a single `<button>` that acts as BOTH the drag target AND the click target — the dual-role accessibility anti-pattern UIDN-03 audit footnote [c] called out. Extraction cleanly separates concerns: drag handlers + visual state live on the region; keyboard entry lives on the inner Button.

Purpose: This is the largest of the four UIDN-03 sweep sites. Closes UIDN-03-FAIL footnote [c] for `ImageInput.tsx:108`. Together with Plan 01 (D-15 SearchBar) and Plan 02 (D-14 SuggestionForm back-links), this closes all four sites of the locked UIDN-03 sweep.

Output: 1 new file (`DropZone.tsx`); 1 edited file (`ImageInput.tsx`); the visible UI is functionally equivalent to today's drop zone — same idle/drag-over/uploading/error states, same upload semantics — but the underlying DOM structure now has proper role separation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Source-of-truth files (executor MUST read before editing) -->
@src/components/suggestions/form/ImageInput.tsx
@src/components/ui/button.tsx
@src/components/ui/alert.tsx
@src/lib/imageConstraints.ts

<!-- UIDN-03 closure audit baseline -->
@.planning/closure/UIDN-03-shadcn-audit.md

<interfaces>
<!-- Current ImageInput.tsx state ownership (preserved post-refactor): -->
<!--
const [uploading, setUploading] = useState(false)
const [dragOver, setDragOver] = useState(false)
const [dropError, setDropError] = useState<string | null>(null)
const fileRef = useRef<HTMLInputElement>(null)
-->

<!-- DropZone props contract (NEW): -->
<!--
type DropZoneProps = {
  disabled?: boolean
  uploading: boolean
  dragOver: boolean
  dropError: string | null   // shown above/below by ImageInput, not by DropZone
  onDrop: (file: File) => void
  onBrowseClick: () => void
  onDragStateChange: (over: boolean) => void
}

export function DropZone({ disabled, uploading, dragOver, onDrop, onBrowseClick, onDragStateChange }: DropZoneProps) {
  return (
    <div
      role="region"
      aria-label="Image upload"
      onDragEnter={(e) => { e.preventDefault(); e.stopPropagation(); if (disabled || uploading) return; onDragStateChange(true) }}
      onDragOver={(e) => { e.preventDefault(); e.stopPropagation(); if (disabled || uploading) return; onDragStateChange(true) }}
      onDragLeave={(e) => { e.preventDefault(); e.stopPropagation(); onDragStateChange(false) }}
      onDrop={(e) => {
        e.preventDefault(); e.stopPropagation()
        onDragStateChange(false)
        if (disabled || uploading) return
        const f = e.dataTransfer?.files?.[0]
        if (f) onDrop(f)
      }}
      className={cn(
        'flex flex-col items-center justify-center gap-2 rounded-md border-2 border-dashed bg-muted/30 px-4 py-8 text-sm transition-colors min-h-[160px]',
        dragOver && 'bg-muted/60 ring-2 ring-ring',
        disabled && 'opacity-60 cursor-not-allowed',
      )}
    >
      {uploading ? (
        <>
          <Loader2 className="h-6 w-6 animate-spin text-muted-foreground mb-2" aria-hidden="true" />
          <p className="text-sm text-muted-foreground">Uploading…</p>
        </>
      ) : (
        <>
          <ImagePlus className="h-8 w-8 text-muted-foreground mb-2" aria-hidden="true" />
          <p className="text-sm font-medium">Drop an image here</p>
          <p className="text-xs text-muted-foreground">JPG, PNG, or WebP · max 2 MB</p>
          <Button
            type="button"
            variant="outline"
            size="sm"
            disabled={disabled}
            onClick={onBrowseClick}
            aria-label="Browse files"
            className="mt-2"
          >
            Browse files
          </Button>
        </>
      )}
    </div>
  )
}
-->

<!-- ImageInput.tsx consumer change (around lines 107-126 in the current file): -->
<!-- The existing dual-role <button> block is replaced with:
<DropZone
  disabled={disabled}
  uploading={uploading}
  dragOver={dragOver}
  dropError={dropError}
  onDragStateChange={setDragOver}
  onDrop={validateAndAccept}
  onBrowseClick={openPicker}
/>
The error <Alert> wrapper sits BELOW the DropZone (same as today). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-05-01: Create DropZone.tsx component</name>
  <files>src/components/suggestions/form/DropZone.tsx</files>
  <read_first>
    - src/components/suggestions/form/ImageInput.tsx (entire file — 189 lines; the existing render block at lines 105-160 is the source-of-truth for the visual + behavioral contract DropZone must reproduce; copy the state-class strings, the icon sizing, and the validateAndAccept call shape)
    - src/components/ui/button.tsx (confirms `Button` exports + variants — `variant="outline"` and `size="sm"` both exist)
    - src/lib/utils.ts (confirms `cn` helper signature)
    - src/lib/imageConstraints.ts (NOT needed in DropZone — constraints are enforced by ImageInput's validator before `onDrop` is called)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-13 (locked: outer div = drag handlers + dashed border; inner shadcn Button = keyboard entry; idle/drag-over/uploading state classes; resolves UIDN-03 footnote [c])
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "4c. ImageInput.tsx:108 — DropZone trigger" (verbatim JSX + classes + ARIA contract)
    - .planning/closure/UIDN-03-shadcn-audit.md (footnote [c] — original drift rationale)
  </read_first>
  <action>
    Create `src/components/suggestions/form/DropZone.tsx` per the `<interfaces>` block above. Critical correctness:

    1. **Outer `<div>` is NOT a button.** No `tabIndex`, no `role="button"`, no `aria-label` shaped like a button. Only `role="region"` + `aria-label="Image upload"`. This is the entire point of the refactor — the region is the drag target, not the click target.

    2. **Inner `<Button>` is the keyboard entry.** Tab order lands on the Button. Enter/Space activates `onBrowseClick` (which is wired by the parent to `openPicker` → `fileRef.current?.click()`).

    3. **Drag handlers MUST call `e.preventDefault()` AND `e.stopPropagation()`** on all four events to prevent browser-default behaviors (open the file directly in the tab, etc.). The existing ImageInput code does this verbatim — copy the pattern.

    4. **State classes:** idle/drag-over use the Phase 4 §9 inherited classes (`bg-muted/30` idle; `bg-muted/60 ring-2 ring-ring` drag-over); `min-h-[160px]` ensures the region is tall enough to be a comfortable drop target. `disabled` (form lock or `uploading`) adds `opacity-60 cursor-not-allowed` AND short-circuits the drag handlers so a locked form does not visually flash drag-over.

    5. **No `dropError` rendering inside DropZone.** The error message is rendered by ImageInput in the existing `<Alert>` block below the region — keeping the error UI inside the parent's `aria-live="polite" role="status"` wrapper preserves the existing screen-reader announcement behavior without DropZone needing to know about the live-region pattern.

    6. **Sizing:** the Browse button uses `size="sm"` which is shadcn's `h-9 px-3` by default. `Button variant="outline" size="sm"` matches the Phase 4 form-secondary button height (UI-SPEC.md typography row 8). Do NOT override size unless visual fit demands.

    7. **Imports:** `import { Button } from '@/components/ui/button'`, `import { cn } from '@/lib/utils'`, `import { ImagePlus, Loader2 } from 'lucide-react'`. No barrel imports.

    8. **Named export only:** `export function DropZone(...) {...}`. No default export, per project convention.

    Single WHY comment at the top: `// UIDN-03 D-13: separates drag-region (outer div with role="region") from the keyboard-Browse entry (inner shadcn Button). Closes the dual-role accessibility footnote [c] in the v1.1 audit.` Single line, no archaeology.
  </action>
  <verify>
    <automated>test -f src/components/suggestions/form/DropZone.tsx && grep -q 'export function DropZone' src/components/suggestions/form/DropZone.tsx && grep -q 'role="region"' src/components/suggestions/form/DropZone.tsx && grep -q 'aria-label="Image upload"' src/components/suggestions/form/DropZone.tsx && grep -q 'aria-label="Browse files"' src/components/suggestions/form/DropZone.tsx && ! grep -E '^\s*<button[^>]+(tabIndex|role="button")' src/components/suggestions/form/DropZone.tsx && grep -q 'variant="outline"' src/components/suggestions/form/DropZone.tsx && npm run lint && npx tsc -b</automated>
  </verify>
  <done>
    DropZone.tsx exists, exports the named component with the props contract, renders `role="region"` outer div + `aria-label="Browse files"` inner Button, includes all four drag-handler implementations with proper preventDefault/stopPropagation, supports all three visual states (idle, drag-over, uploading); lint + tsc green.
  </done>
</task>

<task type="auto">
  <name>Task 12-05-02: ImageInput.tsx — replace native dual-role button with DropZone consumer</name>
  <files>src/components/suggestions/form/ImageInput.tsx</files>
  <read_first>
    - src/components/suggestions/form/ImageInput.tsx (entire file — 189 lines; the change scope is the file/tab-content block around lines 100-160 that wraps the hidden file input and the dual-role button)
    - src/components/suggestions/form/DropZone.tsx (Task 12-05-01 output — confirms the prop contract)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "4c" (locked: hidden file input stays inside ImageInput; DropZone is purely presentational; error Alert remains in ImageInput's `aria-live` wrapper)
    - .planning/closure/UIDN-03-shadcn-audit.md (final closure — once Plan 01 + 02 + 05 all land, this audit baseline should flip from "deferred" to "complete"; updating the audit file is OUT OF SCOPE for Plan 05, lands in Plan 06 closure step OR follow-up)
  </read_first>
  <action>
    Edit `src/components/suggestions/form/ImageInput.tsx`. The state ownership and validation logic stay in ImageInput — only the dual-role `<button>` render block is replaced by a `<DropZone>` consumption.

    Step 1 — add the import. After the existing local imports (e.g., `import { uploadImage } from '@/hooks/useUploadImage'`), add: `import { DropZone } from './DropZone'`.

    Step 2 — identify the openPicker helper. The existing onClick handler in the dual-role button (line ~107) is `openPicker` — this is a local function that calls `fileRef.current?.click()`. Confirm by reading the file. If `openPicker` is defined inline rather than as a hoisted function, hoist it: `const openPicker = () => { if (disabled || uploading) return; fileRef.current?.click() }`. This becomes the `onBrowseClick` prop on DropZone.

    Step 3 — replace the dual-role button block. The current JSX inside the `<TabsContent value="upload">` block contains, roughly:
    - The hidden `<input ref={fileRef} type="file" ... />` (preserved as-is)
    - A `<div role="region" aria-label="Image upload" className="space-y-2">` wrapper
    - Inside it: a `<button type="button" onClick={openPicker} onDragEnter={...} onDragOver={...} onDragLeave={...} onDrop={...} className={cn(...)}>` containing idle/uploading content (ImagePlus + headline + helper OR Loader2 + Uploading…)
    - A separate `<div aria-live="polite" role="status" className="min-h-0">` containing the conditional `<Alert>` for `dropError`

    Replace the inner `<button>` (including all its drag handlers and content) with a single `<DropZone>` render:
    ```
    <DropZone
      disabled={disabled}
      uploading={uploading}
      dragOver={dragOver}
      dropError={dropError}
      onDrop={validateAndAccept}
      onBrowseClick={openPicker}
      onDragStateChange={setDragOver}
    />
    ```
    The outer wrapper `<div role="region" aria-label="Image upload">` becomes redundant — DropZone's outer div IS the region. Remove the outer wrapper, OR — if the wrapper is referenced by any test selector — keep an unstyled `<div>` wrapper without role/aria-label. Prefer removal; surface in the SUMMARY if tests need updates.

    Step 4 — keep the `<input ref={fileRef} type="file" ... />` and the dropError `<Alert>` block in ImageInput.tsx. They stay outside DropZone. The hidden file input stays sibling to DropZone; the Alert block stays below it (still inside the `aria-live="polite" role="status"` wrapper).

    Step 5 — verify the validator. The existing `validateAndAccept` function checks size + MIME + sets `dropError` and calls `handleFile(file)` on pass. DropZone calls `onDrop(file)` which IS `validateAndAccept`. No changes to validator behavior; the path is verified equivalent by inspection.

    Step 6 — verify the URL tab path is unchanged. ImageInput has TWO tabs (Upload + URL); only the Upload tab's dropzone is refactored. The URL tab JSX is untouched.

    Step 7 — verify the preview state is unchanged. When `value` is truthy (poll has an uploaded URL), ImageInput renders a thumbnail + clear button — NOT the DropZone. This branch is preserved.

    After the edit, the file's line count is expected to DECREASE slightly (because the drag-handler boilerplate moves to DropZone). The behavior is functionally identical.

    Update the inline WHY comment that previously read `// LR-07: dropzone. Whole region is a button so keyboard Enter/Space opens the file picker; drag-over adds a ring via tokens.` Replace with: `// UIDN-03 D-13: dropzone region is a region, not a button; inner Browse Button (in DropZone) is the keyboard entry point.` This corrects the misleading comment from the v1.0 LR-07 implementation.
  </action>
  <verify>
    <automated>! grep -E '^\s*<button[^>]+(onDragEnter|onDragOver|onDragLeave|onDrop)' src/components/suggestions/form/ImageInput.tsx && grep -q "<DropZone" src/components/suggestions/form/ImageInput.tsx && grep -q "from './DropZone'" src/components/suggestions/form/ImageInput.tsx && npm run lint && npx tsc -b && npm run test
  </verify>
  <done>
    ImageInput.tsx no longer has a `<button>` element carrying drag handlers; it consumes `<DropZone>` from the local module; the hidden file input + the error `<Alert>` block remain in place; lint + tsc + tests pass; manual smoke: drag-and-drop, click-Browse, file picker open, file upload all work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file drop → React state | Validated by ImageInput's existing `validateAndAccept` (size + MIME). DropZone is a pure pass-through; no new validation surface. |
| user click Browse → hidden file input | Standard HTML file picker; constraints enforced by `accept` attribute + same validator. |
| no new network or auth boundaries | Plan 05 is a pure presentational refactor; same upload path (`uploadImage`), same Supabase Storage upload, same constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-01 | Tampering | File MIME spoofing on drop | accept | Existing `validateAndAccept` in ImageInput enforces `MAX_IMAGE_BYTES` + MIME via `ALLOWED_IMAGE_MIME`. Phase 12 does not change this validator. Server-side `get-upload-url` EF re-validates before issuing the signed URL. |
| T-12-05-02 | Denial of Service | Large file drop | accept | `MAX_IMAGE_BYTES` (2 MB) check rejects before upload begins. Same as v1.0 behavior. |
| T-12-05-03 | Information Disclosure | Hidden file input is focusable (`sr-only` not used in current code) | mitigate | Existing pattern uses `className="hidden"` which is non-focusable per CSS display: none. DropZone refactor preserves this — keyboard tab order lands on the inner Browse Button, not the hidden input. |
| T-12-05-04 | Spoofing | Browser dev tools alters `role="region"` to "button" | accept | Same risk class as any front-end-only ARIA — the actual upload validation runs server-side via the get-upload-url EF, which does not trust client labels. |

</threat_model>

<verification>
- `src/components/suggestions/form/DropZone.tsx` exists with the documented exports + ARIA contract
- ImageInput.tsx no longer renders a `<button>` carrying drag handlers
- ImageInput.tsx imports `DropZone` from `./DropZone`
- The hidden file input + `dropError` Alert wrapper remain in ImageInput.tsx
- The Tabs structure (Upload + URL) is unchanged
- `npm run lint`, `tsc -b`, `npm run test` all pass
- Manual smoke: drag-drop a valid image → uploads + preview replaces dropzone; drag-drop oversize → error Alert appears below; click Browse → file picker opens; tab order: Browse Button is reachable, hidden file input is NOT in tab order
</verification>

<success_criteria>
- UIDN-03 D-13 closed — fourth and final native-button drift site resolved (Plan 01 closed D-15 SearchBar; Plan 02 closed D-14 SuggestionForm; Plan 05 closes D-13 ImageInput)
- ARIA correctness: drag region is `role="region"`, Browse trigger is `<button>` via shadcn Button — dual-role footnote [c] closed
- Zero behavioral regression: same drop validation, same click validation, same uploadImage path, same error messaging
- Together with Plans 01, 02: all 4 UIDN-03 sites swept; Phase 12 ROADMAP SC5 (ESLint + tsc pass after replacements) achievable
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-05-SUMMARY.md`
</output>
