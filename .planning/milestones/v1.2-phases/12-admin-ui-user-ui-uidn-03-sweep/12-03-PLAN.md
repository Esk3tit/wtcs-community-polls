---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 03
type: execute
wave: 2
depends_on: ["12-00"]
files_modified:
  - src/hooks/useToggleResultsVisibility.ts
  - src/components/admin/AdminSuggestionRow.tsx
  - src/components/admin/AdminSuggestionsTab.tsx
autonomous: true
requirements:
  - VIS-07
tags:
  - vis-07
  - admin
  - shadcn
  - switch
  - sonner

must_haves:
  truths:
    - "New hook `src/hooks/useToggleResultsVisibility.ts` exports `useToggleResultsVisibility` returning `{ toggleResultsVisibility, submitting }`. Mirrors `usePinPoll` shape exactly: optimistic call site sets local state, hook fires `supabase.functions.invoke('toggle-results-visibility', { body: { poll_id, hidden } })`, surfaces sonner toast on success (\"Results hidden for: {title}\" / \"Results visible for: {title}\" — D-03), reverts + error toast on failure"
    - "VIS-07 D-05: `AdminSuggestionRow` renders the Switch + label cluster between the response-count text and the existing `<SuggestionKebabMenu />` (not inside the kebab). The right side becomes `<div className=\"flex items-center gap-2 shrink-0\">` holding the new VisibilitySwitch component then the existing kebab"
    - "VIS-07 D-06: Switch checked state mirrors `!resultsHidden` (ON = visible, OFF = hidden). Action label below the breakpoint reads `'Hide results'` when currently visible and `'Show results'` when currently hidden"
    - "VIS-07 D-07: At sub-`sm` breakpoint, the action label is hidden (`hidden sm:inline`) and a lucide `Eye` (visible) or `EyeOff` (hidden) icon renders in its place (`sm:hidden inline-flex`)"
    - "VIS-07 D-02: While the EF call is in-flight, the Switch is `disabled` AND a `Loader2` spinner with `animate-spin` renders adjacent — re-enabled on response. Rapid double-click during in-flight is a no-op (Switch disabled + an inflightRef guard in the hook prevents the second invoke)"
    - "VIS-07 D-08: Switch renders identically on closed-with-null-resolution rows (amber left border, \"Needs resolution\" pill) — no layout shift; the two concerns are orthogonal"
    - "VIS-07 D-01 + D-03: On click, AdminSuggestionsTab flips the local `results_hidden` in `items` optimistically, fires the EF in the background, on success keeps the local state (server matches), on failure reverts + the hook's error toast surfaces. The success toast copy is symmetric per D-03; the error toast copy is `'Could not update visibility. Try again.'` plus the existing Phase 4 403/404 mappings for unusual error paths"
    - "AdminSuggestionsTab passes a new prop `onToggleResultsVisibility(pollId, nextHidden)` to AdminSuggestionRow following the existing `onTogglePin` pattern (line 204 of AdminSuggestionsTab.tsx). The handler in AdminSuggestionsTab follows the same optimistic + reconcile shape as `handleTogglePin` (line 93 of AdminSuggestionsTab.tsx)"
    - "VIS-07 ARIA: The Switch has `aria-label={'Results currently ' + (resultsHidden ? 'hidden' : 'visible')}` (CURRENT state, not next state, per UI-SPEC.md Accessibility Contract). `aria-busy=\"true\"` is set during in-flight"
    - "VIS-07 ARIA: A wrapping `<label className=\"inline-flex items-center gap-2 min-h-[44px] cursor-pointer select-none\">` element wraps the Switch + label cluster — gives keyboard + click ergonomics + WCAG 2.5.8 ≥ 44px touch target"
    - "AdminSuggestion type in AdminSuggestionRow.tsx gains `results_hidden: boolean` to match the `polls_effective` projection (Phase 11 VIS-09 + Plan 00 type regen). `AdminSuggestionsTab` already SELECTs * from `polls_effective` so the field arrives automatically — just narrow the type"
    - "D-04: Audit-log surface is silent in v1.2 — no tooltip on the Switch, no toast suffix mentioning audit. No `Tooltip` primitive is vendored. The `audit_log` table (Phase 11) remains a backend forensics surface; the admin audit-log UI is explicitly deferred to v1.3+. The Switch cluster MUST NOT render any visual hint that an audit row was written"
    - "`data-testid={`visibility-switch-${suggestion.id}`}` is on the Switch primitive — Plan 06 TEST-13 targets this locator"
    - "`npm run lint`, `tsc -b`, and `npm run test` all pass; `src/__tests__/admin/polls-effective-invariant.test.ts` continues to pass (zero new `from('polls')` reads in `src/`)"
  artifacts:
    - path: "src/hooks/useToggleResultsVisibility.ts"
      provides: "Admin mutation hook for toggle-results-visibility EF (optimistic + toast + revert)"
      exports: ["useToggleResultsVisibility"]
    - path: "src/components/admin/AdminSuggestionRow.tsx"
      provides: "Inline Switch + label cluster + Loader2 + Eye/EyeOff between response-count and kebab"
      contains: "results-hidden"
    - path: "src/components/admin/AdminSuggestionsTab.tsx"
      provides: "Optimistic + reconcile handler for results_hidden flip; passes onToggleResultsVisibility to each row"
      contains: "useToggleResultsVisibility"
  key_links:
    - from: "src/components/admin/AdminSuggestionRow.tsx"
      to: "src/components/admin/AdminSuggestionsTab.tsx"
      via: "onToggleResultsVisibility callback prop"
      pattern: "onToggleResultsVisibility"
    - from: "src/components/admin/AdminSuggestionsTab.tsx"
      to: "src/hooks/useToggleResultsVisibility.ts"
      via: "import + handler invocation"
      pattern: "useToggleResultsVisibility"
    - from: "src/hooks/useToggleResultsVisibility.ts"
      to: "supabase/functions/toggle-results-visibility/index.ts"
      via: "supabase.functions.invoke('toggle-results-visibility', { body })"
      pattern: "toggle-results-visibility"
---

<objective>
Implement VIS-07: admin-side inline Switch on `AdminSuggestionRow` that flips `results_hidden` via the Phase 11 `toggle-results-visibility` EF, with optimistic flip + sonner toast + revert-on-error matching the existing `usePinPoll` precedent (D-01).

Purpose: VIS-07 is the always-visible admin control across the entire admin suggestion list (live + archived per ROADMAP SC2). Reads the current state via the existing `polls_effective` SELECT (Phase 11 VIS-09 projection plus Plan 00 type regen) and writes via the Phase 11 EF. Co-lands the new `useToggleResultsVisibility` hook (the consumed mutation entry) plus the AdminSuggestionsTab wiring (optimistic + reconcile state shape, identical to the existing pin-flip pattern at line 93 of AdminSuggestionsTab.tsx).

Output: 1 new hook file, 2 edited components. Three files total; AdminSuggestionRow surface gains a state-mirroring Switch + adaptive label/icon + in-flight Loader2; AdminSuggestionsTab gains an optimistic-flip handler and passes the new prop down.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Source-of-truth files (executor MUST read before editing) -->
@src/components/admin/AdminSuggestionRow.tsx
@src/components/admin/AdminSuggestionsTab.tsx
@src/hooks/usePinPoll.ts
@src/hooks/useUpdatePoll.ts
@src/components/ui/switch.tsx
@src/lib/fn-error.ts
@src/lib/supabase.ts

<!-- Phase 11 EF contract (read-only — Phase 12 does NOT modify the EF) -->
@supabase/functions/toggle-results-visibility/index.ts
@.planning/phases/11-schema-rls-ef-foundations/11-02-SUMMARY.md

<interfaces>
<!-- New hook contract (matches usePinPoll shape verbatim): -->
<!--
export function useToggleResultsVisibility() {
  const [submitting, setSubmitting] = useState(false)
  const inflightRef = useRef(false)

  const toggleResultsVisibility = useCallback(
    async (input: { poll_id: string; hidden: boolean; title: string }) => {
      if (inflightRef.current) return { ok: false as const }
      inflightRef.current = true
      setSubmitting(true)
      try {
        const { error } = await supabase.functions.invoke('toggle-results-visibility', {
          body: { poll_id: input.poll_id, hidden: input.hidden }
        })
        if (error) {
          toast.error(await extractFunctionErrorMessage(error, 'Could not update visibility. Try again.'))
          return { ok: false as const }
        }
        toast.success(input.hidden ? `Results hidden for: ${input.title}` : `Results visible for: ${input.title}`)
        return { ok: true as const }
      } catch {
        toast.error('Could not update visibility. Try again.')
        return { ok: false as const }
      } finally {
        inflightRef.current = false
        setSubmitting(false)
      }
    },
    [],
  )

  return { toggleResultsVisibility, submitting }
}
-->

<!-- VIS-07 EF contract (Phase 11 shipped — DO NOT MODIFY): -->
<!--   POST body: { poll_id: string (UUID), hidden: boolean } -->
<!--   200 OK: { poll: <full row> } on both state-change AND no-op paths (Phase 11 D-11/D-12 idempotency contract) -->
<!--   403 if non-admin; 400 if validation; 404 if poll missing -->

<!-- AdminSuggestion type addition (AdminSuggestionRow.tsx): -->
<!--   export type AdminSuggestion = { ..., results_hidden: boolean } -->
<!-- AdminSuggestionsTab SELECT already pulls `polls_effective.*` so the column flows through; Plan 00 type regen makes it type-safe. -->

<!-- VisibilitySwitch render shape (extracted as a local sub-component inside AdminSuggestionRow.tsx
     OR inline JSX — planner's discretion; inline is fine given the row is only 90 lines): -->
<!--
<label className="inline-flex items-center gap-2 min-h-[44px] cursor-pointer select-none">
  <Switch
    checked={!resultsHidden}
    onCheckedChange={(v) => onToggleResultsVisibility(suggestion.id, !v)}
    disabled={isPending}
    aria-busy={isPending}
    aria-label={`Results currently ${resultsHidden ? 'hidden' : 'visible'}`}
    data-testid={`visibility-switch-${suggestion.id}`}
  />
  <span className="hidden sm:inline text-sm font-medium">
    {resultsHidden ? 'Show results' : 'Hide results'}
  </span>
  <span className="sm:hidden inline-flex">
    {resultsHidden
      ? <EyeOff className="h-4 w-4 text-muted-foreground" aria-hidden="true" />
      : <Eye className="h-4 w-4 text-foreground" aria-hidden="true" />}
  </span>
  {isPending && <Loader2 className="h-4 w-4 animate-spin text-muted-foreground" aria-hidden="true" />}
</label>
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-03-01: Create useToggleResultsVisibility hook</name>
  <files>src/hooks/useToggleResultsVisibility.ts</files>
  <read_first>
    - src/hooks/usePinPoll.ts (canonical precedent — entire file; mirror the signature, state shape, inflightRef guard, try/catch/finally pattern, toast wiring, error-message extraction)
    - src/lib/fn-error.ts (the `extractFunctionErrorMessage` helper used in usePinPoll's error path — confirms the signature `(error, fallback) => Promise<string>` and that it surfaces structured `{ error: { code, message } }` from EFs)
    - src/lib/supabase.ts (confirm the `supabase` singleton export and the `supabase.functions.invoke` signature)
    - supabase/functions/toggle-results-visibility/index.ts (Phase 11 — confirms the request body shape `{ poll_id, hidden }`, the 200 response shape `{ poll: <row> }`, and the 403/400/404 error paths)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-01 + D-02 + D-03 (optimistic flip; in-flight disable; symmetric toast copy)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "Toast Copy" (locked: `'Results hidden for: {title}'` / `'Results visible for: {title}'` on success; `'Could not update visibility. Try again.'` on failure)
    - .planning/phases/11-schema-rls-ef-foundations/11-02-SUMMARY.md (Phase 11 EF deployment summary — confirms the EF is live in production)
  </read_first>
  <action>
    Create a new file `src/hooks/useToggleResultsVisibility.ts` matching the shape and conventions of `src/hooks/usePinPoll.ts`. The new hook:

    1. Imports React (`useState`, `useCallback`, `useRef`), the `supabase` client from `@/lib/supabase`, `toast` from `sonner`, and `extractFunctionErrorMessage` from `@/lib/fn-error`.
    2. Exports a named function `useToggleResultsVisibility()` (no default export per project convention).
    3. Holds `submitting` state and an `inflightRef` to suppress overlapping invokes.
    4. Returns `{ toggleResultsVisibility, submitting }`.

    The `toggleResultsVisibility` callback accepts `{ poll_id: string; hidden: boolean; title: string }`. The `title` is used only for the success-toast interpolation — it does not flow to the EF body. The EF body is `{ poll_id, hidden }` (Phase 11 EF accepts UUID + strict boolean; rejects other types with 400).

    Success path: on the 200 response from `supabase.functions.invoke`, emit `toast.success` with the locked copy:
      - if `hidden === true`: `Results hidden for: ${title}`
      - if `hidden === false`: `Results visible for: ${title}`
    These are the verbatim locked strings from D-03 + UI-SPEC.md.

    Error path: on a non-null `error` from the invoke (network failure, 403, 404, 5xx), call `extractFunctionErrorMessage(error, 'Could not update visibility. Try again.')` to surface the most specific message the EF returned (e.g., the 403 case may return "You do not have permission to perform this action." per the Phase 4 fn-error convention; the 404 case returns "Suggestion not found. It may have been deleted." per Phase 4 tone — both already encoded in the fn-error helper's mapping table). Emit `toast.error(message)`.

    Return shape: `{ ok: true as const }` on success, `{ ok: false as const }` on error. The caller (AdminSuggestionsTab) uses this to decide whether to revert the optimistic flip.

    The `inflightRef` (initialized via `useRef(false)`) is the rapid-click guard per D-02: if a second click arrives while a previous invoke is still pending, the callback returns immediately without firing a second EF call. The Switch's `disabled` prop in the row also enforces this at the UI layer (double-defense).

    No new types are needed — the input shape is inlined. The hook is parameter-less (matches `usePinPoll()`).

    Do NOT add a comment archaeology block. WHY-only inline comments per project policy: a single WHY comment explaining "inflightRef + Switch disabled in caller — D-02 rapid-click guard to prevent double audit rows" is appropriate.
  </action>
  <verify>
    <automated>test -f src/hooks/useToggleResultsVisibility.ts && grep -q 'export function useToggleResultsVisibility' src/hooks/useToggleResultsVisibility.ts && grep -q "supabase.functions.invoke.'toggle-results-visibility'" src/hooks/useToggleResultsVisibility.ts && grep -q "Results hidden for" src/hooks/useToggleResultsVisibility.ts && grep -q "Results visible for" src/hooks/useToggleResultsVisibility.ts && grep -q "Could not update visibility" src/hooks/useToggleResultsVisibility.ts && npm run lint && npx tsc -b</automated>
  </verify>
  <done>
    Hook file exists, exports the named function with the documented signature, calls the EF with the right body shape, surfaces the locked toast copy on success/failure, uses inflightRef + supabase singleton + fn-error helper exactly like `usePinPoll`. Lint + tsc green.
  </done>
</task>

<task type="auto">
  <name>Task 12-03-02: Add Switch cluster to AdminSuggestionRow + wire AdminSuggestionsTab optimistic handler</name>
  <files>src/components/admin/AdminSuggestionRow.tsx, src/components/admin/AdminSuggestionsTab.tsx</files>
  <read_first>
    - src/components/admin/AdminSuggestionRow.tsx (entire file — 90 lines; the Switch cluster slots into the right side between the response-count text and the existing kebab menu)
    - src/components/admin/AdminSuggestionsTab.tsx (entire file — read in full; mirror the `handleTogglePin` optimistic + reconcile shape at lines 93-114 for the new `handleToggleResultsVisibility`)
    - src/hooks/usePinPoll.ts (reference for the precedent the new hook follows — confirms the return shape used by `handleTogglePin`)
    - src/components/ui/switch.tsx (Wave 1 Plan 00 vendored primitive — confirm `Switch` exports `onCheckedChange` and accepts standard ARIA + `disabled` props)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "2. VIS-07 Inline Switch on AdminSuggestionRow" (verbatim layout: right cluster = `flex items-center gap-2 shrink-0`, Switch wrapper = `<label>` with `min-h-[44px]`, mobile-only icon swap, in-flight Loader2)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-05/D-06/D-07/D-02 (locked placement, labels, breakpoint, in-flight state)
  </read_first>
  <action>
    **Edit A — AdminSuggestionRow.tsx:**

    Step 1 — extend the `AdminSuggestion` type (currently lines 6-16 of the file). Add `results_hidden: boolean` to the type definition. Place it adjacent to `is_pinned` since both are admin-state booleans.

    Step 2 — extend the `Props` interface (currently lines 18-23). Add two new props:
    - `onToggleResultsVisibility: (pollId: string, nextHidden: boolean) => void` — follows the `onTogglePin` callback shape verbatim.
    - `isPendingVisibility?: boolean` — optional, tracks whether THIS row's Switch is currently pending. Provided by AdminSuggestionsTab so multiple-rows-in-flight is supportable.

    Step 3 — add imports at the top. The current imports are: `Pin` from lucide-react, `cn` from `@/lib/utils`, `SuggestionKebabMenu`, `Resolution` type. Add:
    - `Eye`, `EyeOff`, `Loader2` from `lucide-react`.
    - `Switch` from `@/components/ui/switch` (Wave 1 Plan 00 vendor).

    Step 4 — render the Switch cluster. The existing render JSX has two main children inside the wrapper `<div>`: a left-side `<div className="flex-1 min-w-0">` (badges + title + response-count) and the right-side `<SuggestionKebabMenu pollId={s.id} ...>`. Wrap the right side in a new flex container:

    `<div className="flex items-center gap-2 shrink-0">{visibilitySwitchJSX}<SuggestionKebabMenu .../></div>`

    The `visibilitySwitchJSX` block (inline, no extra component file — the row is only 90 lines and the cluster is ~15 lines):

    `<label className="inline-flex items-center gap-2 min-h-[44px] cursor-pointer select-none">`
    `  <Switch`
    `    checked={!s.results_hidden}`
    `    onCheckedChange={(v) => onToggleResultsVisibility(s.id, !v)}`
    `    disabled={isPendingVisibility}`
    `    aria-busy={isPendingVisibility}`
    `    aria-label={s.results_hidden ? 'Results currently hidden' : 'Results currently visible'}`
    `    data-testid={\`visibility-switch-${s.id}\`}`
    `  />`
    `  <span className="hidden sm:inline text-sm font-medium">{s.results_hidden ? 'Show results' : 'Hide results'}</span>`
    `  <span className="sm:hidden inline-flex">{s.results_hidden ? <EyeOff className="h-4 w-4 text-muted-foreground" aria-hidden="true" /> : <Eye className="h-4 w-4 text-foreground" aria-hidden="true" />}</span>`
    `  {isPendingVisibility && <Loader2 className="h-4 w-4 animate-spin text-muted-foreground" aria-hidden="true" />}`
    `</label>`

    Key correctness notes:
    - Switch `checked={!s.results_hidden}` — ON = visible (per D-06; reads "Switch is on when results are visible"). Coercion: the EF stores `results_hidden` as a boolean column NOT NULL DEFAULT false; the type regen from Plan 00 ensures `s.results_hidden` is `boolean`, not `boolean | null`.
    - `onCheckedChange={(v) => onToggleResultsVisibility(s.id, !v)}` — the Switch's `v` is the NEXT state of "visible". Hidden = NOT visible, so the EF receives `hidden: !v`. The shadcn Switch primitive's `onCheckedChange` may pass `boolean | "indeterminate"`; coerce via `v === true ? false : true` if the indeterminate case can ever fire (it cannot for a controlled Switch with `checked` set, but a defensive coerce avoids accidental bugs).
    - The wrapping `<label>` is intentional — it makes the action label + icon click-through to the Switch (WCAG 2.5.5 + 2.5.8 ≥ 44px touch target).
    - `aria-label` reflects CURRENT state, not next state, per UI-SPEC.md "Accessibility Contract" (Radix convention).
    - The coexistence with `needsResolution` (D-08): the existing `<div className={cn('flex items-start justify-between gap-3 p-4 min-h-[72px] bg-card hover:bg-muted/30 transition-colors', needsResolution && 'border-l-2 border-amber-500')}>` wrapper stays unchanged — the Switch sits in the new right cluster and renders identically on closed-with-null-resolution rows.

    **Edit B — AdminSuggestionsTab.tsx:**

    Step 1 — add the hook import. Existing imports include `usePinPoll`. Add: `import { useToggleResultsVisibility } from '@/hooks/useToggleResultsVisibility'`.

    Step 2 — destructure the hook in the component body (where `const { pinPoll } = usePinPoll()` is declared, around line 38). Add: `const { toggleResultsVisibility } = useToggleResultsVisibility()`. Also add a per-row pending tracker: `const [pendingVisibility, setPendingVisibility] = useState<Set<string>>(new Set())` — a Set of poll IDs currently in flight, so multiple rows can be in-flight independently.

    Step 3 — add `handleToggleResultsVisibility` callback mirroring `handleTogglePin` (lines 93-114):

    ```
    const handleToggleResultsVisibility = useCallback(
      async (pollId: string, nextHidden: boolean) => {
        const prev = items
        const optimistic = items.map((it) =>
          it.id === pollId ? { ...it, results_hidden: nextHidden } : it,
        )
        setItems(optimistic)
        setPendingVisibility((s) => new Set(s).add(pollId))
        const target = prev.find((it) => it.id === pollId)
        const title = target?.title ?? 'this suggestion'
        const res = await toggleResultsVisibility({ poll_id: pollId, hidden: nextHidden, title })
        setPendingVisibility((s) => { const n = new Set(s); n.delete(pollId); return n })
        if (!res.ok) {
          setItems(prev)
          return
        }
        void fetchAll()
      },
      [items, toggleResultsVisibility, fetchAll],
    )
    ```

    Note: unlike pin (which re-sorts post-mutation because `is_pinned` affects ordering), `results_hidden` does NOT affect list ordering — so the optimistic update does not need to re-sort. The post-mutation `fetchAll()` provides authoritative reconciliation.

    Step 4 — pass the new props to `AdminSuggestionRow`. The current usage on line 204 reads `<AdminSuggestionRow ... onTogglePin={(pid, next) => void handleTogglePin(pid, next)} />`. Add two new props matching the row's new prop interface:

    `onToggleResultsVisibility={(pid, next) => void handleToggleResultsVisibility(pid, next)}`
    `isPendingVisibility={pendingVisibility.has(suggestion.id)}`

    Reference `suggestion.id` from the iteration variable (the existing list iteration already maps over `items`).

    Step 5 — verify the SELECT pulls `results_hidden`. The current SELECT is `supabase.from('polls_effective').select('*')` which post-Plan-00 brings in `results_hidden` automatically via the Phase 11 VIS-09 view projection. No SELECT change needed.

    Cast updates: the line `const rows = ((data ?? []) as unknown) as AdminSuggestion[]` already does an unchecked cast; since the new `AdminSuggestion` type now contains `results_hidden`, the cast continues to compile but is stricter at compile time. If you'd prefer to remove the `unknown` indirection now that types align post-Plan-00, you can replace it with `(data ?? []) as AdminSuggestion[]` — but this is a cleanup beyond Phase 12 scope, leave it for a follow-up unless it actively errors.
  </action>
  <verify>
    <automated>grep -q 'results_hidden: boolean' src/components/admin/AdminSuggestionRow.tsx && grep -q 'onToggleResultsVisibility' src/components/admin/AdminSuggestionRow.tsx && grep -q 'data-testid={`visibility-switch-' src/components/admin/AdminSuggestionRow.tsx && grep -q 'useToggleResultsVisibility' src/components/admin/AdminSuggestionsTab.tsx && grep -q 'handleToggleResultsVisibility' src/components/admin/AdminSuggestionsTab.tsx && grep -q 'pendingVisibility' src/components/admin/AdminSuggestionsTab.tsx && npm run lint && npx tsc -b && npm run test -- --run src/__tests__/admin/polls-effective-invariant
  </verify>
  <done>
    AdminSuggestionRow imports Switch + Eye/EyeOff/Loader2, renders the Switch cluster between response-count and kebab, has the locked aria-label/testid/disabled wiring; AdminSuggestionsTab destructures the new hook, holds a per-poll-id pending Set, runs the optimistic+revert+reconcile handler, passes the callback + isPending down to each row; lint + tsc + invariant test all pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → toggle-results-visibility EF | Phase 11 EF gate: anon-key + admin role JWT required; rejects 403 on non-admin (verified by Phase 11 TEST-12). Phase 12 does not modify the EF. |
| EF → polls table conditional UPDATE | Phase 11 D-11/D-12 — race-safe, idempotent: no-op on same-state, writes audit + timestamp only on actual state change. |
| optimistic UI flip ↔ canonical server state | Discrepancy window: local state flips before EF confirms. On EF error, local state reverts (revert path); on success, `fetchAll()` reconciles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Spoofing | Admin-role check at the EF | mitigate | Phase 11 EF gates via `requireAdmin` helper; non-admin invocations return 403 (TEST-12 Plan 11-04 verified). Phase 12 hook does NOT bypass — every click flows through the same EF gate. |
| T-12-03-02 | Tampering | Optimistic state mutation via DevTools | accept | Local React state can be mutated client-side, but the canonical truth is the server. The next `fetchAll()` (~poll cycle OR on next admin action) reconciles. Worst case: a few seconds of UI lying to the local admin only — never affects voters who read via the Phase 11 RLS-gated `polls_effective`. |
| T-12-03-03 | Repudiation | Toggle is not audited | mitigate | Phase 11 EF writes one `audit_log` row per state-changing toggle (D-11/D-12 — no audit row on no-op). The in-flight Switch disable + inflightRef guard prevent rapid double-click from writing two audit rows for a single user intent. |
| T-12-03-04 | Information Disclosure | Toast surfaces poll title | accept | The title is already visible in the admin UI; the toast does not leak any state the admin cannot see. |
| T-12-03-05 | Denial of Service | Admin spam-clicks the Switch | mitigate | inflightRef + Switch `disabled` during in-flight blocks overlapping invokes (D-02). The EF's no-op idempotency path returns 200 instantly without writing audit, so even an overlapping click is cheap. |
| T-12-03-06 | Elevation of Privilege | A non-admin user reaches the AdminSuggestionsTab page | accept | Existing AuthContext + admin route guard (Phase 5) gates the admin shell entirely. The Switch is only visible inside this gated surface; even if it were exposed accidentally, the EF 403 path still blocks the mutation. |

</threat_model>

<verification>
- `src/hooks/useToggleResultsVisibility.ts` exists with the documented exports + behavior
- `AdminSuggestion` type contains `results_hidden: boolean`
- AdminSuggestionRow renders Switch + Loader2 (when pending) + Eye/EyeOff (sub-`sm`) + adaptive label (≥ `sm`)
- AdminSuggestionsTab destructures the new hook, holds a per-poll-id pending Set, exposes `handleToggleResultsVisibility` with optimistic + revert + reconcile
- `data-testid={`visibility-switch-${pollId}`}` is on each Switch
- `aria-label` reflects CURRENT state (not next state)
- `aria-busy` toggles with in-flight state
- `min-h-[44px]` is on the wrapping `<label>`
- All sub-`sm` icon swaps render correctly (Eye when visible, EyeOff when hidden)
- Switch coexists with `needsResolution` amber-flag styling — no layout shift on closed-with-null-resolution rows
- `npm run lint`, `tsc -b`, `npm run test` all pass
- Manual smoke: click Switch on a row in dev → success toast appears with poll title → row state reflects the new visibility on the next `fetchAll` poll cycle → click again → opposite toast → row reverts
</verification>

<success_criteria>
- VIS-07 fully wired — admins can flip `results_hidden` from the always-visible inline Switch in the admin list
- Optimistic flip + sonner toast + revert-on-error matches the `usePinPoll` precedent (D-01)
- D-02 in-flight discipline prevents double audit rows
- D-03 symmetric toast copy is the user-visible signal
- D-07 mobile-friendly icon swap keeps state visible on sub-`sm` viewports
- D-08 coexistence with the resolution flag — zero layout regression
- TEST-13 (Plan 06) can target the Switch via `[data-testid="visibility-switch-{pollId}"]`
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-03-SUMMARY.md`
</output>
