---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 02
type: execute
wave: 2
depends_on: ["12-00"]
files_modified:
  - src/components/suggestions/form/SuggestionForm.tsx
  - src/hooks/useCreatePoll.ts
  - src/lib/validation/suggestion-form.ts
autonomous: true
requirements:
  - VIS-06
  - UIDN-03
tags:
  - vis-06
  - uidn-03
  - suggestion-form
  - shadcn

must_haves:
  truths:
    - "VIS-06: `SuggestionForm.tsx` renders a shadcn `<Checkbox id=\"results-hidden\">` row below `CategoryPicker` with Label text \"Hide results from voters\" and helper text \"Voters with responses will see a placeholder instead of counts. Toggle anytime from the admin list.\" (REQUIREMENTS-verbatim label per D-17; helper copy verbatim per CONTEXT)"
    - "VIS-06: Checkbox defaults to unchecked (`results_hidden = false`) on a fresh `mode=\"create\"` form — backwards-compat with VIS-01 default"
    - "VIS-06 edit-mode behavior (create-only scope per cross-AI review): the `update-poll` Edge Function does NOT accept or persist `results_hidden`. To prevent a silent no-op where an admin edits the checkbox in edit mode and sees no actual change, the checkbox is rendered ONLY when `mode === 'create'`. In `mode === 'edit'`, a read-only status row replaces it: a disabled visual indicator showing the current `results_hidden` state (icon + label `'Results currently hidden'` / `'Results currently visible'`) plus helper text `'Toggle from the admin list to change visibility.'` that points admins to the Plan 03 Switch (which routes through `toggle-results-visibility`)"
    - "VIS-06: `SuggestionFormInput` (type in `src/lib/validation/suggestion-form.ts`) gains an optional `results_hidden?: boolean` field. The validator `validateSuggestionForm` MUST be updated to include `results_hidden: input.results_hidden === true` in its sanitized return object — leaving the validator unchanged drops the field on the explicit return (cross-AI review HIGH: validator returns an explicit sanitized object, omitting `results_hidden` would silently strip it from the create payload). `useCreatePoll` forwards it to the `create-poll` EF body (Phase 11 D-08: the EF already accepts the field)"
    - "VIS-06: Submitting `mode=\"create\"` with the checkbox checked produces a poll row where `results_hidden = true` (verified via direct DB SELECT or Plan 06 E2E); submitting unchecked produces `results_hidden = false`. The toast copy stays \"Suggestion created\" — no separate variant for the checked path per UI-SPEC.md"
    - "VIS-06: Unit-test coverage for the validator: add a test that `validateSuggestionForm` preserves `results_hidden: true` when input contains it; add a test that omitting the field yields `results_hidden: false` on the sanitized return (or the field is absent and the EF default takes over — either is valid as long as the create-poll body shape matches Phase 11 D-08). Tests live in `src/__tests__/lib/validation/suggestion-form.test.ts` (or the existing analog if differently named — discover via `ls src/__tests__/lib/validation/`)"
    - "UIDN-03 D-14: Both `<button type=\"button\" onClick={() => navigate({ to: '/admin' })}>` instances at `SuggestionForm.tsx:140` (loadError branch) and `:163` (main render path) are replaced with TanStack `<Link to=\"/admin\">`. The inline-flex span + `ChevronLeft` icon + \"Back to admin\" text + className visual styling are preserved verbatim — visual zero-delta"
    - "UIDN-03 D-14: Both `<Link>` sites are direct TanStack Router `<Link>` components imported from `@tanstack/react-router` — middle-click and cmd-click open in a new tab (regression-blocker that the prior `<button>` could not provide)"
    - "The `useNavigate` import is removed from `SuggestionForm.tsx` if it is no longer referenced after the two back-link sites are replaced (the file has no other navigation imperatives — verify by grep before removing)"
    - "VIS-06 + UIDN-03: `npm run lint`, `tsc -b`, and `npm run test` all pass"
    - "The `data-testid=\"visibility-checkbox\"` attribute is added to the Checkbox so Plan 06 TEST-13 can target it deterministically (UI-SPEC.md Test Surfaces section)"
  artifacts:
    - path: "src/components/suggestions/form/SuggestionForm.tsx"
      provides: "VIS-06 checkbox row below CategoryPicker + UIDN-03 D-14 back-link replacements"
      contains: "results-hidden"
    - path: "src/hooks/useCreatePoll.ts"
      provides: "useCreatePoll passes results_hidden through to the create-poll EF body"
      contains: "results_hidden"
    - path: "src/lib/validation/suggestion-form.ts"
      provides: "SuggestionFormInput.results_hidden optional boolean field"
      contains: "results_hidden"
  key_links:
    - from: "src/components/suggestions/form/SuggestionForm.tsx"
      to: "src/hooks/useCreatePoll.ts"
      via: "createPoll(input) — input now carries optional results_hidden"
      pattern: "results_hidden"
    - from: "src/components/suggestions/form/SuggestionForm.tsx"
      to: "@tanstack/react-router Link"
      via: "import"
      pattern: "import.*Link.*from '@tanstack/react-router'"
    - from: "src/hooks/useCreatePoll.ts"
      to: "supabase/functions/create-poll/index.ts"
      via: "supabase.functions.invoke body"
      pattern: "results_hidden"
---

<objective>
Implement VIS-06 (admin-side "Hide results from voters" checkbox at creation) and the two UIDN-03 D-14 back-link replacements in `SuggestionForm.tsx`. Co-landing in the same file because file ownership prevents parallel Wave-2 plans from touching SuggestionForm.tsx simultaneously.

Purpose: VIS-06 is the create-time half of admin visibility control — the new poll inherits the checkbox state. UIDN-03 D-14 replaces two imperative `<button onClick={() => navigate(...)}>` sites with declarative TanStack `<Link>` to restore browser-native middle-click / cmd-click semantics (regression-blocker per closure footnote [c]). Both changes touch the same file; bundling them keeps the diff coherent.

Output: One enlarged `SuggestionForm.tsx` (new Visibility section + two `<Link>` replacements), one extended `useCreatePoll.ts` (passes `results_hidden`), one extended `SuggestionFormInput` type. Three files, all in `src/`. No new files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Source-of-truth files (executor MUST read before editing) -->
@src/components/suggestions/form/SuggestionForm.tsx
@src/hooks/useCreatePoll.ts
@src/lib/validation/suggestion-form.ts
@src/components/ui/checkbox.tsx
@src/components/ui/label.tsx

<!-- Edge Function contract (Phase 11 D-08 already accepts results_hidden) -->
@supabase/functions/create-poll/index.ts

<!-- UIDN-03 closure audit baseline -->
@.planning/closure/UIDN-03-shadcn-audit.md

<interfaces>
<!-- Current SuggestionFormInput (src/lib/validation/suggestion-form.ts): -->
<!--
export type SuggestionFormInput = {
  title: string
  description: string
  choices: string[]
  category_id: string | null
  image_url: string | null
  closes_at: string // ISO8601
}
-->
<!-- Add: results_hidden?: boolean — optional, defaults to false at the EF and DB column level. -->

<!-- Phase 11 create-poll EF accepts the field (lines 126-136 of supabase/functions/create-poll/index.ts): -->
<!--   const results_hidden = body.results_hidden === undefined ? null
<!--     : typeof body.results_hidden === 'boolean' ? body.results_hidden : null -->
<!--   if (results_hidden === null && body.results_hidden !== undefined) return 400 -->
<!-- So passing `undefined` (omitted) yields the DEFAULT false; passing `true` opts in. -->

<!-- TanStack Link import (analogous to other declarative nav in the codebase): -->
<!--   import { Link } from '@tanstack/react-router' -->
<!--   <Link to="/admin" className="..."><ChevronLeft />Back to admin</Link> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-02-01: Extend SuggestionFormInput + useCreatePoll for results_hidden pass-through</name>
  <files>src/lib/validation/suggestion-form.ts, src/hooks/useCreatePoll.ts</files>
  <read_first>
    - src/lib/validation/suggestion-form.ts (entire file — confirms the `SuggestionFormInput` type shape and the `validateSuggestionForm` function; the new field must slot in without breaking any existing validator branch)
    - src/hooks/useCreatePoll.ts (entire file — confirms the `createPoll` signature passes the input directly to `supabase.functions.invoke('create-poll', { body: input })`; no per-field marshalling)
    - supabase/functions/create-poll/index.ts lines 40-200 (Phase 11 D-08 — confirms the EF already accepts optional `results_hidden?: boolean`; passing `undefined` defaults to false at the DB level, passing `true` triggers the post-RPC update path; passing `false` explicitly is also valid and yields the same row state as omitting it)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § "Integration Points" (`useCreatePoll` extension passes the VIS-06 checkbox value through; Phase 11's create-poll EF (D-08) already accepts the field)
    - .planning/phases/11-schema-rls-ef-foundations/11-03b-PLAN.md (Phase 11 reference — confirms the live EF contract for `results_hidden` at create time)
  </read_first>
  <action>
    Step 1 — extend the input type. In `src/lib/validation/suggestion-form.ts`, add an optional field to `SuggestionFormInput`:

    `results_hidden?: boolean`

    Place it after `closes_at`. The trailing `?` makes it optional so existing call sites (smoke tests, fixtures, the existing edit-mode update path) do not need to change.

    Step 2 — UPDATE `validateSuggestionForm` to preserve the field on the sanitized return (cross-AI review HIGH fix). The current validator builds an explicit return object `{ title, description, choices, category_id, image_url, closes_at }` and would silently drop `results_hidden` even if the caller passes it. Add a single coercion line ABOVE the `return { ok: true, value: ... }` block: `const resultsHidden = input.results_hidden === true` (strict-true coercion handles `undefined`, `false`, and the indeterminate `'indeterminate'` edge case from shadcn Checkbox by collapsing them all to `false`). Then include `results_hidden: resultsHidden` in the sanitized value object alongside `closes_at`. The field is NOT length/format-checked — a boolean has no validation surface — but it MUST appear on the return for the EF body to carry it through. The EF revalidates `body.results_hidden` is `boolean | undefined` (Phase 11 D-08 lines 126-135); the validator's job is to ensure the field is not stripped at the React layer.

    Step 3 — pass-through in `useCreatePoll`. The current hook calls `supabase.functions.invoke<{ id: string }>('create-poll', { body: input })`. After Step 2, `input.results_hidden` is part of the validated `SuggestionFormInput` and flows through with zero code change in `useCreatePoll.ts`. **No edit to `useCreatePoll.ts` is required.** However, to make the contract visible to future readers, add a single comment line above the `supabase.functions.invoke` call: `// VIS-06: results_hidden flows through SuggestionFormInput; create-poll EF accepts the field.` (WHY-only per project source-comment policy — no plan/phase IDs.)

    Step 4 — `useUpdatePoll` does NOT need extending. Per cross-AI review HIGH: the `update-poll` Edge Function does not accept `results_hidden`. Phase 12 deliberately scopes VIS-06 to create-only; visibility changes in edit mode route through the Plan 03 admin-list Switch (which calls `toggle-results-visibility`). Do NOT add `results_hidden` to the `useUpdatePoll` input or shape — that would be a silent no-op since the EF ignores the field. The hook is unchanged in this plan.

    Step 5 — add validator unit tests. Find or create `src/__tests__/lib/validation/suggestion-form.test.ts` (or the existing analog — discover via `ls src/__tests__/lib/validation/`). Add two test cases: (a) input with `results_hidden: true` → assert `result.ok && result.value.results_hidden === true`; (b) input with `results_hidden` omitted → assert `result.ok && result.value.results_hidden === false` (the strict-true coercion's default branch). If the file does not exist yet, scaffold it following the Vitest pattern used elsewhere in `src/__tests__/lib/`. These tests pin the validator contract so a future refactor cannot silently re-drop the field.
  </action>
  <verify>
    <automated>grep -q 'results_hidden\??\s*:\s*boolean' src/lib/validation/suggestion-form.ts && grep -q 'results_hidden:\s*\(resultsHidden\|input\.results_hidden\)' src/lib/validation/suggestion-form.ts && npx tsc -b && npm run test -- --run src/__tests__/lib/validation</automated>
  </verify>
  <done>
    `SuggestionFormInput` type contains optional `results_hidden?: boolean`; `validateSuggestionForm` returns `results_hidden` on its sanitized value (strict-true coercion); `useCreatePoll` is unchanged but its EF body now carries the field through `input`; `useUpdatePoll` is NOT extended (out of scope per cross-AI review — update-poll EF does not accept the field); `tsc -b` passes; new validator unit tests for `results_hidden` pass; no existing validation test regresses.
  </done>
</task>

<task type="auto">
  <name>Task 12-02-02: Add VIS-06 Checkbox row to SuggestionForm + two UIDN-03 D-14 back-link replacements</name>
  <files>src/components/suggestions/form/SuggestionForm.tsx</files>
  <read_first>
    - src/components/suggestions/form/SuggestionForm.tsx (entire file — 238 lines; this task adds one new vertical section below CategoryPicker AND replaces two `<button onClick={() => navigate({ to: '/admin' })}>` sites at lines 140-145 and 163-168)
    - src/components/ui/checkbox.tsx (Wave 1 Plan 00 vendored primitive — confirms `Checkbox` exports `onCheckedChange` and accepts `id`, `checked`, `disabled`)
    - src/components/ui/label.tsx (existing primitive — confirms `htmlFor` association pattern)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "1. VIS-06 Visibility Checkbox" (locked layout, verbatim label + helper copy, min-height ≥ 44px touch target on the row wrapper)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "4b. SuggestionForm.tsx:140 + :163" (TanStack Link replacement; preserve inline-flex span + ChevronLeft + "Back to admin" verbatim)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-17 (Checkbox placement below CategoryPicker; verbatim label and helper)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-14 (TanStack Link replacement; search-param disposition is Claude's discretion)
    - src/components/suggestions/form/CategoryPicker.tsx (read the file's render block to find where it ends in the parent form; the new Visibility section must slot immediately after it)
    - src/components/suggestions/form/TimerPicker.tsx (read the file's render block; same purpose — confirm where Visibility sits relative to TimerPicker)
  </read_first>
  <action>
    Two edits, applied in any order to the same file. Both should be in one commit.

    **Edit A — UIDN-03 D-14: replace two back-link `<button>` sites with TanStack `<Link>`.**

    Add to imports at the top: `import { Link } from '@tanstack/react-router'`. Keep the existing `useNavigate` import for now; remove it ONLY if the file no longer uses `navigate(...)` after both replacements (run `grep -n 'navigate(' src/components/suggestions/form/SuggestionForm.tsx` after the edits to confirm). The current file has two distinct `<button>` blocks — at line 140 (the `loadError` branch) and at line 163 (main render block); both render the same pattern: a `<button type="button" onClick={() => navigate({ to: '/admin' })} className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground mb-4"><ChevronLeft className="h-4 w-4 mr-1" /> Back to admin</button>`.

    Replace each with: `<Link to="/admin" className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground mb-4"><ChevronLeft className="h-4 w-4 mr-1" /> Back to admin</Link>`. The className stays verbatim — visual zero-delta. The hover behavior is the same because TanStack `<Link>` renders an `<a>` element which inherits the same Tailwind classes. The `type="button"` attribute is dropped (Link is an anchor, not a button — no form-submit risk since these buttons had no `type="submit"` and were not inside the form's onSubmit boundary in any case).

    Search-param disposition (D-14, Claude's discretion): the current `navigate({ to: '/admin' })` does not pass `search={{ tab: 'suggestions' }}`. The admin index route reads `tab` from URL search params and defaults to the suggestions tab when absent — verify by reading the route definition at `src/routes/admin/index.tsx` and the `AdminSuggestionsTab` `useSearch` call (it falls through to a default filter when `tab` is missing). **Decision for this plan: do NOT add `search={{ tab: 'suggestions' }}`** — the existing behavior routes correctly to the suggestions tab by default; adding the search param would be a behavior change beyond the UIDN-03 "zero-delta" mandate. If admins later report missing-context behavior (e.g., the active tab is not preserved on re-entry), it lands as a follow-up per CONTEXT.md `<deferred>`.

    After both edits, run `grep -n 'navigate(' src/components/suggestions/form/SuggestionForm.tsx`. If zero matches, remove the `useNavigate` import. If matches remain (e.g., a third `navigate(` call deep in the file that performs a non-back-link navigation), keep the import.

    **Edit B — VIS-06: add the Visibility Checkbox row below CategoryPicker (CREATE-ONLY) + read-only status row in edit mode.**

    Add to imports at the top: `import { Checkbox } from '@/components/ui/checkbox'`. The `Eye` / `EyeOff` icons are needed for the edit-mode read-only row — add them from `lucide-react` if not already imported (Plan 03 also imports these; verify existing lucide imports first). Confirm `Label` is already imported (it is — line 7 of the current file).

    The Visibility section sits between the `<CategoryPicker ... />` render and the `<TimerPicker ... />` render — confirm exact placement by reading the existing form body; the locked placement per D-17 is "below CategoryPicker, own vertical row". Match the existing `space-y-6` between-sections rhythm.

    Add a new state hook near the existing `useState` declarations at the top of the component: `const [resultsHidden, setResultsHidden] = useState(false)`. Initialize from the loaded row in edit mode — `loadPoll()` already fetches the poll row; add `setResultsHidden(Boolean(row.results_hidden))` to the success branch of that function alongside the other `set*` calls (e.g., `setTitle(row.title)`, `setDescription(row.description ?? '')`).

    **Cross-AI review HIGH fix — create-only scope.** The `update-poll` Edge Function does NOT accept or persist `results_hidden`. Rendering an editable checkbox in `mode === 'edit'` would let admins toggle it, see no DB change, and conclude the feature is broken. Branch the JSX so an EDITABLE Checkbox renders only in create mode; in edit mode, render a READ-ONLY status row that points admins to the admin-list Switch (Plan 03), which routes through `toggle-results-visibility` and DOES persist.

    Wrap the whole Visibility section in `<div className="space-y-2">` and split on `mode`:

    **Create-mode branch (`mode === 'create'`):**

    (a) row `<div className="flex items-start gap-2 min-h-[44px]">` holding `<Checkbox id="results-hidden" data-testid="visibility-checkbox" checked={resultsHidden} onCheckedChange={(v) => setResultsHidden(v === true)} />` and `<Label htmlFor="results-hidden" className="text-sm font-medium cursor-pointer">Hide results from voters</Label>` (REQUIREMENTS-verbatim label).

    (b) below the row, `<p className="text-xs text-muted-foreground">Voters with responses will see a placeholder instead of counts. Toggle anytime from the admin list.</p>` (CONTEXT-verbatim helper).

    No `disabled={locked}` is needed here because `locked = mode === 'edit' && voteCount > 0` — `locked` is always `false` in create mode by construction.

    **Edit-mode branch (`mode === 'edit'`):**

    A read-only status row:
    ```
    <div className="flex items-start gap-2 min-h-[44px]" data-testid="visibility-status">
      {resultsHidden
        ? <EyeOff className="h-4 w-4 text-muted-foreground mt-0.5" aria-hidden="true" />
        : <Eye className="h-4 w-4 text-foreground mt-0.5" aria-hidden="true" />}
      <span className="text-sm font-medium">{resultsHidden ? 'Results currently hidden' : 'Results currently visible'}</span>
    </div>
    <p className="text-xs text-muted-foreground">Toggle from the admin list to change visibility.</p>
    ```

    The edit-mode row carries `data-testid="visibility-status"` (not `visibility-checkbox`) so test code can distinguish modes. Plan 06 TEST-13 targets `visibility-checkbox` which exists ONLY in create mode — TEST-13 already plans to exercise the create path (a fresh poll), so the testid is reachable.

    **On submit, include `results_hidden` ONLY in the create path.** Find the existing submit handler (search for `validateSuggestionForm(` in the file). The handler currently builds an input object then calls either `createPoll(input)` or `updatePoll(input)` based on `mode`. Add `results_hidden: resultsHidden` to the input object UNCONDITIONALLY — the validator's strict-true coercion (Task 12-02-01 Step 2) collapses any false/undefined to `false`. The validator returns the field; create-poll EF persists it; update-poll EF ignores it (verified by reading `supabase/functions/update-poll/index.ts` — no `results_hidden` reference). The unconditional inclusion keeps the input shape consistent across modes without conditional logic at the call site; the EF behavior diverges naturally.

    The shadcn `Checkbox` returns `boolean | "indeterminate"` from `onCheckedChange`; coerce to boolean via `v === true` to keep `resultsHidden` strictly boolean (avoids the indeterminate state which is not used in this form).

    The `data-testid="visibility-checkbox"` attribute is locked by UI-SPEC.md "Test Surfaces" — Plan 06 TEST-13 targets this exact testid in the create-mode branch.
  </action>
  <verify>
    <automated>grep -q 'from "@tanstack/react-router".*Link\|Link.*from "@tanstack/react-router"\|Link.*from .@tanstack/react-router.' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'Hide results from voters' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'data-testid="visibility-checkbox"' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'data-testid="visibility-status"' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'Toggle from the admin list to change visibility' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'results-hidden' src/components/suggestions/form/SuggestionForm.tsx && ! grep -E '^\s*<button[^>]*navigate\({\s*to:\s*.\/admin' src/components/suggestions/form/SuggestionForm.tsx && npm run lint && npx tsc -b && npm run test -- --run src/__tests__/admin/polls-effective-invariant</automated>
  </verify>
  <done>
    SuggestionForm.tsx imports `Link` from `@tanstack/react-router`, `Checkbox` from `@/components/ui/checkbox`, and `Eye`/`EyeOff` from `lucide-react`; both `<button onClick={() => navigate({ to: '/admin' })}>` sites are replaced with `<Link to="/admin">`; the create-mode branch renders the editable Checkbox with the exact label, helper text, and `data-testid="visibility-checkbox"`; the edit-mode branch renders a read-only status row with icon + status text + `data-testid="visibility-status"` + helper text "Toggle from the admin list to change visibility."; both modes pre-populate `resultsHidden` from the loaded row in edit mode; the form submit passes `results_hidden: resultsHidden` into the input unconditionally (update-poll EF ignores the field — create-only persistence per cross-AI review HIGH); lint + tsc pass; polls-effective-invariant test still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → React form state | The Checkbox value lives in component state; no new trust boundary introduced beyond existing form fields. |
| admin browser → create-poll EF | Phase 11 already validates `results_hidden` as `boolean \| undefined` in the EF (line 126-135 of `create-poll/index.ts`); rejects other types with 400. Phase 12 passes the field through without intermediate transformation. |
| browser → TanStack Link → URL | Replacing `<button onClick={() => navigate(...)}>` with `<Link to="/admin">` actually NARROWS the trust surface — the Link uses TanStack's compile-time-typed `to` prop, which prevents arbitrary URL injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | `results_hidden` form input via browser DevTools | accept | The Phase 11 create-poll EF validates `body.results_hidden` is `boolean \| undefined` (line 126-135); the worst a tampered request achieves is the same outcome as a legitimate `true`/`false` submission. RLS on `polls` writes is enforced via the EF's admin-gate (Phase 11). |
| T-12-02-02 | Spoofing | Admin role check for results_hidden at create | mitigate | Already mitigated by the create-poll EF's existing `requireAdmin` gate (Phase 11 D-08). Phase 12 does not bypass — it only sets one new field on the same request flow. |
| T-12-02-03 | Information Disclosure | Helper text "Voters with responses will see a placeholder instead of counts" | accept | This is admin-facing copy describing voter-side UI; no PII or system internals exposed. |
| T-12-02-04 | Repudiation | The create-time `results_hidden=true` path | accept | Phase 11 D-08 already writes a second audit row `action='results_hidden_set_at_create'` when the field is `true` at create time. Phase 12 leverages that existing audit trail; no new repudiation surface. |
| T-12-02-05 | Tampering | TanStack `<Link to="/admin">` replacing imperative `navigate` | mitigate | The compile-time-typed `to` prop on TanStack `<Link>` actually narrows the attack surface compared to `navigate({ to: '/admin' })` — there is no dynamic path concatenation. |
| T-12-02-06 | Denial of Service | New form field rendering cost | accept | One Radix Checkbox + Label adds ~1 KB gzip; visual rendering is constant-time. |

</threat_model>

<verification>
- SuggestionForm.tsx imports `Link` from `@tanstack/react-router`, `Checkbox` from `@/components/ui/checkbox`, and `Eye`/`EyeOff` from `lucide-react`
- The two `<button>`s containing `navigate({ to: '/admin' })` are replaced with `<Link to="/admin">`
- `mode === 'create'`: the editable Checkbox renders below CategoryPicker with the REQUIREMENTS-verbatim label "Hide results from voters", the locked helper copy, and `data-testid="visibility-checkbox"`
- `mode === 'edit'`: a read-only status row renders in the same slot with `Eye`/`EyeOff` + "Results currently hidden" / "Results currently visible" + helper text "Toggle from the admin list to change visibility." + `data-testid="visibility-status"`
- `SuggestionFormInput.results_hidden?: boolean` exists
- `validateSuggestionForm` includes `results_hidden` on the sanitized return value (strict-true coercion)
- Validator unit tests cover both the checked and omitted/false branches
- The form submit handler passes `results_hidden` into the input unconditionally; the create-poll EF persists it (Phase 11 D-08); the update-poll EF ignores it (verified by reading `supabase/functions/update-poll/index.ts`)
- Edit-mode pre-populates the read-only row from the loaded row's `results_hidden`
- `useUpdatePoll` is NOT modified
- `npm run lint`, `tsc -b`, `npm run test` all pass
- Manual smoke: create a poll with the checkbox checked in dev → confirm a DB SELECT shows `polls.results_hidden = true`; open the same poll's edit form → confirm the read-only status row reflects the state and the editable Checkbox is absent
</verification>

<success_criteria>
- VIS-06: Admin can opt in to hidden-from-creation via the checkbox in create mode; default unchecked preserves v1.0/v1.1 backwards compat
- VIS-06: Edit mode shows a clear, non-editable status indicator + pointer to the admin-list Switch (avoids the silent no-op against `update-poll` EF that the cross-AI review caught)
- VIS-06: Validator preserves `results_hidden` on the sanitized return — no silent strip
- UIDN-03: 2 of 4 sites closed (SuggestionForm.tsx:140 + :163 — Plan 01 closed :22 SearchBar; Plan 05 will close ImageInput.tsx:108)
- Zero behavioral regression in existing form submission paths (Yes/No preset, choice editor, image input, timer picker, category picker)
- TEST-13 (Plan 06) can target the create-mode Checkbox via `[data-testid="visibility-checkbox"]`
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-02-SUMMARY.md`
</output>
