---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/suggestions/SearchBar.tsx
autonomous: true
requirements:
  - UIDN-03
tags:
  - uidn-03
  - shadcn
  - search

must_haves:
  truths:
    - "`src/components/suggestions/SearchBar.tsx:22` no longer contains a native `<button>` element — it renders a shadcn `<Button variant=\"ghost\" size=\"icon\">` instead (D-15)"
    - "The `X` lucide icon stays at `h-4 w-4` (or shadcn `Button size=\"icon\"` default) and the click region preserves `type=\"button\"`, `onClick={() => onChange('')}`, and `aria-label=\"Clear search\"` — zero behavioral regression"
    - "The clear-X stays positioned absolutely on the right edge of the `relative w-full` Input wrapper (existing layout preserved); the show/hide rule `value && (...)` is unchanged"
    - "Hover behavior inherits shadcn `Button variant=\"ghost\"` tokens (`hover:bg-accent hover:text-accent-foreground`); the visual hover is equivalent to or better than the prior `hover:text-foreground transition-colors` (visual zero-delta or improvement, not regression)"
    - "`npm run lint` passes — `Button` import added from `@/components/ui/button`; `X` and `Input` imports preserved"
    - "`tsc -b` passes — the Button component types are consumed correctly"
    - "Existing E2E specs that target the SearchBar (filter-search.spec.ts and any that interact with the clear-X) continue to pass — the `aria-label=\"Clear search\"` selector is preserved verbatim"
  artifacts:
    - path: "src/components/suggestions/SearchBar.tsx"
      provides: "SearchBar with shadcn Button clear-X (UIDN-03 D-15)"
      contains: "variant=\"ghost\""
  key_links:
    - from: "src/components/suggestions/SearchBar.tsx"
      to: "@/components/ui/button"
      via: "import"
      pattern: "from '@/components/ui/button'"
---

<objective>
Replace the native `<button>` clear-X at `src/components/suggestions/SearchBar.tsx:22` with shadcn `<Button variant="ghost" size="icon">` per UIDN-03 audit footnote [b] (closure D-15). Single-file, single-element edit. Visual zero-delta, behavior zero-delta — only the underlying primitive changes.

Purpose: One of the four UIDN-03 native-button drift sites Phase 12 sweeps. Wave 1 lands this in parallel with Plan 00 (type regen + primitives + REQUIREMENTS edit) because `SearchBar.tsx` has zero file overlap with any other Phase 12 plan. Closes one quarter of the locked UIDN-03 four-site sweep.

Output: Single file diff. The `<button>` element on line 22 becomes a `<Button>` import-and-render. All other lines in SearchBar.tsx unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Source-of-truth files -->
@src/components/suggestions/SearchBar.tsx
@src/components/ui/button.tsx

<!-- UIDN-03 closure audit baseline -->
@.planning/closure/UIDN-03-shadcn-audit.md

<interfaces>
<!-- Current SearchBar render (verbatim — must be preserved except for the button element): -->
<!--
<div className="relative w-full">
  <Search className="absolute left-3 top-1/2 -translate-y-1/2 size-4 text-muted-foreground pointer-events-none" />
  <Input ... className="h-10 pl-9 pr-9 bg-card shadow-sm dark:shadow-none" />
  {value && (
    <button
      type="button"
      onClick={() => onChange('')}
      aria-label="Clear search"
      className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground transition-colors"
    >
      <X className="size-4" />
    </button>
  )}
</div>
-->

<!-- shadcn Button signature (existing primitive): -->
<!--   <Button variant="ghost" size="icon" type="button" onClick={...} aria-label="...">{children}</Button> -->
<!--   size="icon" defaults to h-9 w-9; the surrounding Input is h-10. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-01-01: SearchBar clear-X native button → shadcn Button (UIDN-03 D-15)</name>
  <files>src/components/suggestions/SearchBar.tsx</files>
  <read_first>
    - src/components/suggestions/SearchBar.tsx (entire file — 32 lines; only the `<button>` on lines 21-29 changes)
    - src/components/ui/button.tsx (confirms the `variant="ghost"` and `size="icon"` variants exist and their default sizing — `size="icon"` is `h-9 w-9` per the shadcn new-york preset)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "4a. SearchBar.tsx:22 — clear-X button (D-15)" (sizing guidance: shadcn default `h-9 w-9` is the leading candidate against the `h-10` Input; planner may tune to `size-8` only if visual fit demands)
    - .planning/closure/UIDN-03-shadcn-audit.md (footnote [b] — original drift rationale)
    - src/components/admin/AdminSuggestionRow.tsx (existing usage of shadcn Button with variant="ghost" — sample of how the project imports + renders Button today, for stylistic consistency)
  </read_first>
  <action>
    Open `src/components/suggestions/SearchBar.tsx`. The file is 32 lines; the change is scoped to the `<button>` block on lines 21-29.

    Step 1 — add the Button import. The current imports are `import { Search, X } from 'lucide-react'` (line 1) and `import { Input } from '@/components/ui/input'` (line 2). Add a third line: `import { Button } from '@/components/ui/button'`. Match the existing alphabetical-by-source convention (`lucide-react` before `@/components/*`); if no convention is enforced, keep it after `Input` for readability.

    Step 2 — replace the native button element. The current JSX (lines 20-29):

    `{value && (<button type="button" onClick={() => onChange('')} aria-label="Clear search" className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground transition-colors"><X className="size-4" /></button>)}`

    Becomes:

    `{value && (<Button type="button" variant="ghost" size="icon" onClick={() => onChange('')} aria-label="Clear search" className="absolute right-3 top-1/2 -translate-y-1/2 size-8"><X className="size-4" /></Button>)}`

    Three changes inside the element: (a) tag `button` → `Button`; (b) add `variant="ghost"` + `size="icon"` props; (c) the className is condensed to `"absolute right-3 top-1/2 -translate-y-1/2 size-8"`. The `size-8` override matches UI-SPEC.md's sizing guidance — the default shadcn `size="icon"` is `h-9 w-9` which is too tall for visual alignment with the `h-10` Input's content area at the same vertical center; `size-8` (32×32px) is the leading candidate per UI-SPEC.md "4a" sizing line. If the resulting visual is misaligned in development preview, the alternative is to drop `size-8` and accept the default `h-9 w-9`; that decision is Claude's discretion at execution time but the default-first attempt is `size-8`.

    Drop the prior `text-muted-foreground hover:text-foreground transition-colors` classes — the shadcn Button `variant="ghost"` already applies `hover:bg-accent hover:text-accent-foreground` plus its own transition tokens. The visible idle color stays neutral (Button ghost variant inherits the default foreground); the hover behavior shifts from "icon color change only" to "subtle background fill + icon color" which is the more standard shadcn pattern and matches every other ghost-icon button in the app.

    Step 3 — verify the `X` icon size stays `h-4 w-4` (currently `size-4`, equivalent). Do not change the icon size.

    Do not change anything outside the button element. Do not change `Search`, `Input`, the wrapper `<div>`, or the conditional `{value && (...)}`. Do not edit any other file.
  </action>
  <verify>
    <automated>! grep -E '^\s*<button' src/components/suggestions/SearchBar.tsx && grep -q 'variant="ghost"' src/components/suggestions/SearchBar.tsx && grep -q 'size="icon"' src/components/suggestions/SearchBar.tsx && grep -q 'aria-label="Clear search"' src/components/suggestions/SearchBar.tsx && npm run lint && npx tsc -b</automated>
  </verify>
  <done>
    No `<button>` element remains in SearchBar.tsx; the file imports `Button` from `@/components/ui/button` and renders `<Button variant="ghost" size="icon" ...>` with the `X` icon inside; `type="button"`, `onClick`, and `aria-label="Clear search"` are preserved verbatim; ESLint and `tsc -b` pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → React state | The Input value flows to the `onChange` prop unchanged. No new boundary introduced. The clear-X is a local UI control that resets the value to `''`. |
| no new trust boundaries | Plan 01 is a pure presentational refactor — same element role (button), same behavior (clear the search), same accessibility surface (aria-label "Clear search"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | shadcn Button primitive consumed by SearchBar | accept | Existing primitive already vendored at `src/components/ui/button.tsx` and consumed elsewhere in the app; no new code introduced into the dependency graph. |
| T-12-01-02 | Information Disclosure | Search query value | accept | Pre-existing concern — the search value flows through the parent component; no new exposure surface created by changing the clear-X primitive. |
| T-12-01-03 | Denial of Service | Bundle size delta from shadcn Button reuse | accept | `Button` is already imported elsewhere in the SearchBar's parent tree — adding one more import site is tree-shaken to zero net delta. |

</threat_model>

<verification>
- `grep '<button' src/components/suggestions/SearchBar.tsx` returns no native `<button>` element
- `grep 'variant="ghost"'` succeeds; `grep 'size="icon"'` succeeds
- `aria-label="Clear search"` preserved verbatim (test fixture continuity)
- `npm run lint` exits 0
- `tsc -b` exits 0
- Manual visual check at dev preview: the clear-X aligns vertically with the search icon on the left of the Input (`top-1/2 -translate-y-1/2` centers both)
</verification>

<success_criteria>
- One of four UIDN-03 sites closed (SearchBar.tsx:22)
- Zero behavioral regression — clear-X clears the search input on click; show/hide rule on `value` unchanged
- Zero accessibility regression — `aria-label="Clear search"` preserved
- Three sites remain for Plan 02 (SuggestionForm.tsx D-14, two sites) and Plan 05 (ImageInput.tsx D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-01-SUMMARY.md`
</output>
