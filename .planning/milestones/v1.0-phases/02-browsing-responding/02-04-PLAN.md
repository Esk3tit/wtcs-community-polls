---
phase: 02-browsing-responding
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/topics.tsx
  - src/routes/archive.tsx
autonomous: true
gap_closure: true
requirements:
  - VOTE-01
  - VOTE-02
  - VOTE-03
  - RSLT-01
  - RSLT-02
  - RSLT-03
  - RSLT-04
  - RSLT-05
  - CATG-02
  - CATG-03
  - CATG-04
  - INFR-04
  - TEST-03
must_haves:
  truths:
    - "User sees a browsable list of active suggestions on the /topics page"
    - "User can access closed suggestions with results on the /archive page"
  artifacts:
    - path: "src/routes/topics.tsx"
      provides: "Topics page rendering SuggestionList with status=active"
      contains: "SuggestionList"
    - path: "src/routes/archive.tsx"
      provides: "Archive page rendering SuggestionList with status=closed"
      contains: "SuggestionList"
  key_links:
    - from: "src/routes/topics.tsx"
      to: "src/components/suggestions/SuggestionList"
      via: "direct import and render"
      pattern: "import.*SuggestionList.*from.*suggestions/SuggestionList"
    - from: "src/routes/archive.tsx"
      to: "src/components/suggestions/SuggestionList"
      via: "direct import and render"
      pattern: "import.*SuggestionList.*from.*suggestions/SuggestionList"
---

<objective>
Fix route regression: restore topics.tsx and archive.tsx from Phase 1 placeholders to their correct Phase 2 implementations that render SuggestionList.

Purpose: Commit dfccc11 (Plan 03 seed data) accidentally reverted both route files to Phase 1 placeholder content, breaking 4/7 success criteria and blocking 4/13 requirements. All components, hooks, Edge Function, and tests are fully implemented -- only the two route files need restoration.

Output: Two corrected route files that wire SuggestionList into the app's routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-browsing-responding/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore topics.tsx route to render SuggestionList</name>
  <files>src/routes/topics.tsx</files>
  <read_first>
    - src/routes/topics.tsx
    - src/components/suggestions/SuggestionList.tsx (first 10 lines for import path confirmation)
  </read_first>
  <action>
Replace the entire content of src/routes/topics.tsx with the correct Phase 2 implementation. The file must:

1. Import `createFileRoute` from `@tanstack/react-router`
2. Import `AuthGuard` from `@/components/auth/AuthGuard`
3. Import `SuggestionList` from `@/components/suggestions/SuggestionList`
4. Export `Route` using `createFileRoute('/topics')` with component `TopicsPage`
5. Render `<AuthGuard>` wrapping `<SuggestionList status="active" />`

Remove all lucide-react imports (Inbox icon) and the static placeholder JSX (the "No active topics right now" empty state). The SuggestionList component already handles its own empty state internally via the EmptyState component.

Exact target content:
```typescript
import { createFileRoute } from '@tanstack/react-router'
import { AuthGuard } from '@/components/auth/AuthGuard'
import { SuggestionList } from '@/components/suggestions/SuggestionList'

export const Route = createFileRoute('/topics')({
  component: TopicsPage,
})

function TopicsPage() {
  return (
    <AuthGuard>
      <SuggestionList status="active" />
    </AuthGuard>
  )
}
```
  </action>
  <verify>
    <automated>cd "D:/Khai/Documents/wtcs-community-polls" && grep -q "SuggestionList" src/routes/topics.tsx && grep -q 'status="active"' src/routes/topics.tsx && ! grep -q "Inbox" src/routes/topics.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/topics.tsx contains `import { SuggestionList } from '@/components/suggestions/SuggestionList'`
    - src/routes/topics.tsx contains `<SuggestionList status="active" />`
    - src/routes/topics.tsx does NOT contain `Inbox` or `lucide-react`
    - src/routes/topics.tsx does NOT contain "No active topics right now"
  </acceptance_criteria>
  <done>topics.tsx imports and renders SuggestionList with status="active" inside AuthGuard, with no Phase 1 placeholder content remaining</done>
</task>

<task type="auto">
  <name>Task 2: Restore archive.tsx route to render SuggestionList</name>
  <files>src/routes/archive.tsx</files>
  <read_first>
    - src/routes/archive.tsx
    - src/components/suggestions/SuggestionList.tsx (first 10 lines for import path confirmation)
  </read_first>
  <action>
Replace the entire content of src/routes/archive.tsx with the correct Phase 2 implementation. The file must:

1. Import `createFileRoute` from `@tanstack/react-router`
2. Import `AuthGuard` from `@/components/auth/AuthGuard`
3. Import `SuggestionList` from `@/components/suggestions/SuggestionList`
4. Export `Route` using `createFileRoute('/archive')` with component `ArchivePage`
5. Render `<AuthGuard>` wrapping `<SuggestionList status="closed" />`

Remove all lucide-react imports (Archive icon) and the static placeholder JSX (the "No archived topics" empty state). The SuggestionList component already handles its own empty state internally via the EmptyState component.

Exact target content:
```typescript
import { createFileRoute } from '@tanstack/react-router'
import { AuthGuard } from '@/components/auth/AuthGuard'
import { SuggestionList } from '@/components/suggestions/SuggestionList'

export const Route = createFileRoute('/archive')({
  component: ArchivePage,
})

function ArchivePage() {
  return (
    <AuthGuard>
      <SuggestionList status="closed" />
    </AuthGuard>
  )
}
```
  </action>
  <verify>
    <automated>cd "D:/Khai/Documents/wtcs-community-polls" && grep -q "SuggestionList" src/routes/archive.tsx && grep -q 'status="closed"' src/routes/archive.tsx && ! grep -q "lucide-react" src/routes/archive.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/archive.tsx contains `import { SuggestionList } from '@/components/suggestions/SuggestionList'`
    - src/routes/archive.tsx contains `<SuggestionList status="closed" />`
    - src/routes/archive.tsx does NOT contain `Archive` import from `lucide-react`
    - src/routes/archive.tsx does NOT contain "No archived topics"
  </acceptance_criteria>
  <done>archive.tsx imports and renders SuggestionList with status="closed" inside AuthGuard, with no Phase 1 placeholder content remaining</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan restores existing wiring -- no new data flows, no new inputs, no new API calls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-04-01 | Information Disclosure | archive.tsx rendering closed suggestions | accept | RLS on vote_counts already enforces respondents-only visibility. SuggestionList + ChoiceButtons handle the closed-suggestion non-respondent case with a message. No new exposure. |
</threat_model>

<verification>
After both tasks complete:

1. `npx tsc --noEmit` -- TypeScript compiles without errors
2. `npx vitest run src/__tests__/suggestions/` -- all 19 existing tests pass
3. `grep -c "SuggestionList" src/routes/topics.tsx src/routes/archive.tsx` -- both files show count >= 1
4. `grep -c "lucide-react" src/routes/topics.tsx src/routes/archive.tsx` -- both files show count 0
</verification>

<success_criteria>
- topics.tsx renders `<SuggestionList status="active" />` inside `<AuthGuard>`
- archive.tsx renders `<SuggestionList status="closed" />` inside `<AuthGuard>`
- No Phase 1 placeholder content (Inbox icon, Archive icon, static empty states) remains in either file
- TypeScript compiles cleanly (`npx tsc --noEmit`)
- All 19 existing suggestion tests pass (`npx vitest run src/__tests__/suggestions/`)
- Previously blocked requirements CATG-02, CATG-03, CATG-04, RSLT-05 are unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/02-browsing-responding/02-04-SUMMARY.md`
</output>
