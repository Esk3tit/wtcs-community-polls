---
phase: 08-e2e-test-hygiene
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md
  - .planning/phases/03-response-integrity/03-UAT.md
autonomous: true
requirements:
  - TEST-10
tags:
  - documentation
  - uat
  - planning

must_haves:
  truths:
    - "`.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md` exists as a stepwise, copy-pasteable runbook for a second human (non-2FA-blocked, non-WTCS-member Discord tester) to execute Phase 03 UAT Tests 2 + 3 (D-13)"
    - "`.planning/phases/03-response-integrity/03-UAT.md` has a new `## Second-Human Verification` H2 section appended AFTER the existing `## Gaps` section, containing two H3 sub-blocks (Test 2 + Test 3) with placeholder fields for executor / verified-at / result / notes (D-12)"
    - "The original `result: skipped` records at L19-27 of `03-UAT.md` are PRESERVED byte-identical (D-12 — preserves history; the new section APPENDS, never modifies)"
    - "Phase 8 closure does NOT block on the placeholder fields being filled in — the artifact (runbook + template) IS the deliverable; the actual second-human session runs asynchronously when a non-2FA-blocked tester is available (D-11)"
    - "Plan touches NO source files (no `src/**`, no `supabase/**`, no `e2e/**`) — pure planning-doc edits (TEST-10-readonly gate)"
  artifacts:
    - path: ".planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md"
      provides: "Second-human runbook: prerequisites, Test 2 stepwise instructions, Test 3 stepwise instructions, evidence template, filled-in example"
      contains: "Second-Human"
    - path: ".planning/phases/03-response-integrity/03-UAT.md"
      provides: "Second-Human Verification H2 section appended; original skipped records preserved"
      contains: "## Second-Human Verification"
  key_links:
    - from: ".planning/phases/03-response-integrity/03-UAT.md"
      to: ".planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md"
      via: "doc cross-reference in the appended section's intro"
      pattern: "08-UAT-10-SCRIPT.md"
    - from: ".planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md"
      to: ".planning/phases/03-response-integrity/03-UAT.md"
      via: "runbook ends with copy-paste-into-this-file instruction"
      pattern: "03-UAT.md"
---

<objective>
Produce the TEST-10 deliverable: a stepwise, copy-pasteable runbook (`08-UAT-10-SCRIPT.md`) for a second human to execute Phase 03 UAT Tests 2 (Non-Member Login Rejection) and 3 (Error Page Invite Link), plus a new `## Second-Human Verification` section appended to `03-UAT.md` containing the evidence template and a filled-in example.

Purpose: Phase 03 UAT Tests 2 + 3 were skipped at v1.0 ship time because the original executor's burner Discord account lacked 2FA. The fix requires a SECOND human with a non-2FA-blocked, non-WTCS-member Discord account to run the tests live. Per D-11, Phase 8 produces the runbook + template artifact NOW (synchronous deliverable). The actual second-human session runs LATER (asynchronous evidence) when a tester is available. Phase 8 verification must NOT block on the placeholder fields being filled in.

Output:
- `.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md` (new file)
- `.planning/phases/03-response-integrity/03-UAT.md` extended with `## Second-Human Verification` section (existing skipped records preserved)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-e2e-test-hygiene/08-CONTEXT.md
@.planning/phases/08-e2e-test-hygiene/08-RESEARCH.md
@.planning/phases/08-e2e-test-hygiene/08-PATTERNS.md
@.planning/phases/08-e2e-test-hygiene/08-VALIDATION.md
@.planning/phases/03-response-integrity/03-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the second-human runbook at .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md</name>
  <files>.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md</files>
  <read_first>
    - .planning/phases/03-response-integrity/03-UAT.md (full file — confirms current Test 2 + Test 3 expected text and the H3 evidence-block shape)
    - .planning/phases/08-e2e-test-hygiene/08-PATTERNS.md (section: `.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md (CREATE — second-human runbook)`)
    - .planning/phases/08-e2e-test-hygiene/08-RESEARCH.md (§8 TEST-10 evidence template structure — provides the verbatim filled-in example)
    - .planning/phases/08-e2e-test-hygiene/08-CONTEXT.md (D-11, D-12, D-13 — TEST-10 coordination decisions)
  </read_first>
  <action>
Create the new file `.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md` with the verbatim contents below. Tone: stepwise, copy-pasteable, terse (matches `.planning/` doc voice — no exclamation marks, no marketing copy).

NOTE: This is a `.planning/` document, NOT source code. The CLAUDE.md `feedback_no_review_archaeology_in_source.md` rule applies to SOURCE files only — planning docs are explicitly the correct home for plan/round/D-XX/TEST-XX references. Citing `Phase 8 / TEST-10 / D-11 / D-13` in the runbook prose is REQUIRED for traceability and is allowed.

Verbatim content to write (everything between the BEGIN_FILE and END_FILE markers; do NOT include the markers themselves; replace each `[YAML_DELIM]` placeholder with three literal dashes `---` — the placeholder exists only because this PLAN.md frontmatter parser would otherwise mis-detect the embedded YAML block):

```
BEGIN_FILE
[YAML_DELIM]
phase: 08-e2e-test-hygiene
artifact: TEST-10 second-human runbook
created: 2026-05-02
target_file: .planning/phases/03-response-integrity/03-UAT.md
target_section: ## Second-Human Verification
[YAML_DELIM]

# Phase 8 / TEST-10 — Second-Human UAT Runbook

> Closes Phase 03 UAT Tests 2 + 3 (`Non-Member Login Rejection` and
> `Error Page Invite Link`), which were skipped at v1.0 ship time because
> the original executor's burner Discord account lacked 2FA (the 2FA gate
> triggered before the not-in-server rejection path could be exercised).
>
> Per D-11: Phase 8 ships the runbook + evidence template synchronously;
> the actual session runs ASYNCHRONOUSLY when a qualified tester is
> available. Phase 8 closure does NOT block on the evidence appearing.

## Tester profile (prerequisites)

The tester MUST satisfy ALL of the following:

- Has a Discord account with 2FA ENABLED. (The 2FA gate must clear so the
  not-in-server rejection path actually fires.)

  > Note: REQUIREMENTS.md TEST-10 reads "no 2FA" — that was the OLD blocker. The FIX requires 2FA enabled so the gate clears and the not-in-server rejection path actually fires.
- Is NOT a member of the WTCS Discord server. (If the tester is already a
  member, leave the server temporarily — or recruit someone who isn't.)
- Is NOT the original Phase 03 UAT executor. (The whole point of TEST-10
  is independent second-human evidence.)
- Has a fresh browser profile or incognito window (no existing
  polls.wtcsmapban.com session cookies).

If any prerequisite is unmet, STOP — recruit a different tester. Do not
work around any prerequisite (e.g., disabling 2FA temporarily) — the
verification only counts when the path was actually exercised.

## Pre-test setup (once)

1. Open the production site in a fresh browser profile or incognito:
   <https://polls.wtcsmapban.com>
2. Open browser DevTools → Application → Storage → Cookies. Confirm zero
   `polls.wtcsmapban.com` cookies are present. (If any exist, clear them.)
3. Have a notepad open to capture: executor Discord handle, exact
   timestamp (UTC), pass/fail observation per test, and any unexpected
   behavior to put in `notes:`.

## Test 2 — Non-Member Login Rejection

**What this verifies:** A Discord user who is NOT in the WTCS Discord
server is rejected at OAuth callback and shown the
`WTCS Server Membership Required` error page. They MUST NOT end up with
an active session.

**Steps:**

1. From the homepage, click the `Sign In` button (or whatever entry-point
   element triggers the Discord OAuth flow on the live site).
2. Complete the Discord OAuth handshake with your non-WTCS-member 2FA
   Discord account. Approve the OAuth scope grant when Discord prompts.
3. Wait for the redirect back to `polls.wtcsmapban.com`.

**Expected observation (PASS criteria — all must hold):**

- The browser lands on a URL containing `/auth/error?reason=not-in-server`.
- The page renders the heading `WTCS Server Membership Required`.
- A button labeled `Join the WTCS Discord Server` is visible.
- The button's `href` resolves to `https://discord.gg/aUe8NGP3U2`.
- DevTools → Application → Storage → Cookies shows NO Supabase auth
  cookie set for `polls.wtcsmapban.com` (i.e. session was rejected).
- Reloading the page does NOT log you in — you stay on the error page.

**Capture:** screenshot the error page; record the URL bar; record the
DevTools cookies pane.

If any expected observation is wrong, this is a FAIL — capture screenshots
and a textual description of what you observed instead.

## Test 3 — Error Page Invite Link

**What this verifies:** From the not-in-server error page (which you
reached at the end of Test 2), the `Join the WTCS Discord Server` and
`Try Signing In Again` controls behave correctly.

**Steps (continues from Test 2 — do not navigate away first):**

1. On the not-in-server error page reached at the end of Test 2, click
   `Join the WTCS Discord Server`.
2. Confirm the link opens (in a new tab or current tab — either is
   acceptable; capture which) at `https://discord.gg/aUe8NGP3U2`.
3. Return to the error page tab. Locate the `Try Signing In Again` button.
4. Click `Try Signing In Again`.

**Expected observation (PASS criteria — all must hold):**

- Step 2 lands on `discord.gg/aUe8NGP3U2` (the WTCS invite URL).
- `Try Signing In Again` is visible and clickable on the error page.
- Clicking `Try Signing In Again` returns the browser to the login flow
  (e.g., back to the homepage or to a Discord OAuth re-prompt).

If any expected observation is wrong, this is a FAIL.

## Recording the evidence

Once both tests are complete, fill in this template and paste it into
`.planning/phases/03-response-integrity/03-UAT.md` under the
`## Second-Human Verification` H2 section (which Phase 8 already
appended). Replace each `<…>` placeholder with your actual values.

    ### Test 2 — Non-Member Login Rejection

    - executor: <Discord handle, e.g. `MapCommittee#1234`>
    - verified-at: <UTC ISO 8601, e.g. `2026-05-09T18:42:00Z`>
    - result: <pass | fail>
    - notes: |
        <≥1 line — describe what was observed; if fail, attach screenshot
        path under .planning/phases/08-e2e-test-hygiene/artifacts/test-10-evidence/>

    ### Test 3 — Error Page Invite Link

    - executor: <Discord handle>
    - verified-at: <UTC ISO 8601>
    - result: <pass | fail>
    - notes: |
        <≥1 line>

## Filled-in example (for shape reference — do NOT paste this verbatim)

    ### Test 2 — Non-Member Login Rejection

    - executor: ExampleTester#0001
    - verified-at: 2026-05-09T18:42:00Z
    - result: pass
    - notes: |
        Logged in via Discord with an account NOT in the WTCS server. After the
        OAuth callback completed, the browser landed on /auth/error?reason=not-in-server
        and the heading "WTCS Server Membership Required" rendered. The "Join the
        WTCS Discord Server" CTA was visible and pointed at discord.gg/aUe8NGP3U2.
        Session was NOT established (no Supabase auth cookie present in DevTools).

    ### Test 3 — Error Page Invite Link

    - executor: ExampleTester#0001
    - verified-at: 2026-05-09T18:44:00Z
    - result: pass
    - notes: |
        From the not-in-server error page reached above, clicked "Join the WTCS
        Discord Server" — opened https://discord.gg/aUe8NGP3U2 in a new tab.
        "Try Signing In Again" button was also present and routed back to /login.

## Post-evidence cleanup

- Optionally bump `updated:` in the `03-UAT.md` frontmatter to the same
  ISO timestamp as `verified-at`. (Not blocking; planning-doc hygiene only.)
- If either test failed, file a follow-up GitHub issue against the v1.1
  milestone capturing the failure mode, screenshots, and a proposed
  remediation. Do NOT silently leave a `result: fail` record without a
  tracking issue.
- Drop a quick note in the project Discord/channel so the maintainer knows
  TEST-10 closure evidence has landed.
END_FILE
```

Note on the indented evidence/example blocks above: the runbook intentionally renders the evidence template as a 4-space-indented Markdown block (so the Markdown rendering shows it as a literal code block without using triple-fence syntax — this avoids fence-collision issues with this PLAN.md file which itself uses triple fences). The tester pastes the un-indented version into `03-UAT.md`. The acceptance criteria check that the runbook contains the literal `executor:`, `verified-at:`, etc. lines, regardless of indentation.

The file is roughly 145 lines. Adjust whitespace only; do not strip sections.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ -f .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md ] && LINES=$(wc -l < .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md) && [ "$LINES" -ge 100 ] && [ "$LINES" -le 250 ] && grep -q "Second-Human" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "Test 2 — Non-Member Login Rejection" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "Test 3 — Error Page Invite Link" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "discord.gg/aUe8NGP3U2" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "WTCS Server Membership Required" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "Join the WTCS Discord Server" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "ExampleTester#0001" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "verified-at:" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "result: pass" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "Tester profile" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && grep -q "03-UAT.md" .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md && echo "OK lines=$LINES"</automated>
  </verify>
  <acceptance_criteria>
    - `[ -f .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md ]` succeeds
    - `wc -l < .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md` returns a value in [100, 250]
    - Contains literal heading `# Phase 8 / TEST-10 — Second-Human UAT Runbook` (or equivalent H1 with `Phase 8` + `TEST-10`)
    - Contains tester profile section listing 2FA-enabled, non-WTCS-member, not-original-executor, fresh-browser-profile prerequisites
    - Contains `Test 2 — Non-Member Login Rejection` H2 (or H3) with stepwise instructions and PASS criteria
    - Contains `Test 3 — Error Page Invite Link` H2 (or H3) with stepwise instructions and PASS criteria
    - Contains the verbatim WTCS invite URL `discord.gg/aUe8NGP3U2`
    - Contains the verbatim heading text `WTCS Server Membership Required` (matches `src/components/auth/AuthErrorPage.tsx` per RESEARCH §8 EXAMPLE block)
    - Contains the bullet-list evidence template with `executor`, `verified-at`, `result`, `notes:` fields
    - Contains the filled-in EXAMPLE block with `ExampleTester#0001` placeholder values
    - Cross-references `03-UAT.md` as the paste target
  </acceptance_criteria>
  <done>
    `.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md` exists with prerequisites, Test 2 + Test 3 stepwise instructions, evidence template, filled-in example, and post-evidence cleanup notes. Tester can execute the runbook end-to-end without referring to other documents.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append `## Second-Human Verification` section to .planning/phases/03-response-integrity/03-UAT.md</name>
  <files>.planning/phases/03-response-integrity/03-UAT.md</files>
  <read_first>
    - .planning/phases/03-response-integrity/03-UAT.md (full file — confirms existing structure: H2 sections `## Tests`, `## Summary`, `## Gaps`; H3 records for each test; the `result: skipped` records at L19-27 to preserve)
    - .planning/phases/08-e2e-test-hygiene/08-PATTERNS.md (section: `.planning/phases/03-response-integrity/03-UAT.md (modify — append ## Second-Human Verification section)`)
    - .planning/phases/08-e2e-test-hygiene/08-RESEARCH.md (§8 — verbatim template structure)
    - .planning/phases/08-e2e-test-hygiene/08-CONTEXT.md (D-12 — preserves history; appends new section, never modifies existing records)
  </read_first>
  <action>
Append a new H2 section `## Second-Human Verification` to the END of `.planning/phases/03-response-integrity/03-UAT.md`, AFTER the existing `## Gaps` section (which currently ends at L51 with `[none yet]`).

DO NOT modify any of L1-51. The original `result: skipped` records at L19-27 and the `[none yet]` Gaps line at L51 are PRESERVED byte-identical (D-12). Note that the original H3 headings at L19 (`### 2. Non-Member Login Rejection`) and L24 (`### 3. Error Page Invite Link`) use the legacy `### 2.` / `### 3.` numbered format — the NEW sub-blocks in this appended section use the `### Test 2 — Non-Member Login Rejection` / `### Test 3 — Error Page Invite Link` format (with the em-dash). Both formats coexist: the legacy headings remain as historical markers, and the new headings are the active verification surface.

Append the following content (preserving any existing trailing newline so the file ends with exactly one newline):

```
## Second-Human Verification

> Phase 8 / TEST-10 closure. The original `result: skipped` records above
> are preserved. This section appends executor-by-executor evidence per
> test. Runbook: `.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md`
> (D-13). Phase 8 closure does NOT block on these fields being filled in
> (D-11) — the artifact (template + runbook) is the deliverable; evidence
> appears asynchronously when a qualified tester is available.

### Test 2 — Non-Member Login Rejection

- executor: <Discord handle, e.g. `MapCommittee#1234`>
- verified-at: <UTC ISO 8601, e.g. `2026-05-09T18:42:00Z`>
- result: <pass | fail>
- notes: |
    <≥1 line — describe what was observed; if fail, attach screenshot path under
    .planning/phases/08-e2e-test-hygiene/artifacts/test-10-evidence/>

### Test 3 — Error Page Invite Link

- executor: <Discord handle>
- verified-at: <UTC ISO 8601>
- result: <pass | fail>
- notes: |
    <≥1 line>
```

The `<...>` placeholders are intentional — D-11 explicitly defers actual values until a qualified tester runs the runbook. The verification gate (TEST-10-artifacts in VALIDATION.md) only checks that the section exists and the placeholders are present, not that they are filled.

Bump `updated:` in the file frontmatter? NO — leave the frontmatter `updated:` unchanged. Per RESEARCH §8 + Task 1 runbook's "Post-evidence cleanup" section, the frontmatter `updated:` bump is the TESTER'S responsibility when they paste their actual evidence in. Bumping it now (with placeholder evidence) would be misleading.

This is a `.planning/` document — D-XX references in the appended quote-block intro are intentional and traceability-required. Do NOT scrub them.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "^## Second-Human Verification$" .planning/phases/03-response-integrity/03-UAT.md && [ "$(grep -c '^### Test 2 — Non-Member Login Rejection$' .planning/phases/03-response-integrity/03-UAT.md)" = "1" ] && [ "$(grep -c '^### Test 3 — Error Page Invite Link$' .planning/phases/03-response-integrity/03-UAT.md)" = "1" ] && [ "$(grep -c '^### 2\. Non-Member Login Rejection$' .planning/phases/03-response-integrity/03-UAT.md)" = "1" ] && [ "$(grep -c '^### 3\. Error Page Invite Link$' .planning/phases/03-response-integrity/03-UAT.md)" = "1" ] && grep -q "08-UAT-10-SCRIPT.md" .planning/phases/03-response-integrity/03-UAT.md && grep -q "result: skipped" .planning/phases/03-response-integrity/03-UAT.md && grep -q "Burner account lacks 2FA" .planning/phases/03-response-integrity/03-UAT.md && grep -q "executor: <Discord handle" .planning/phases/03-response-integrity/03-UAT.md && grep -q "verified-at: <UTC ISO 8601" .planning/phases/03-response-integrity/03-UAT.md && [ "$(grep -c '^## ' .planning/phases/03-response-integrity/03-UAT.md)" -ge 4 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## Second-Human Verification$" .planning/phases/03-response-integrity/03-UAT.md` exits 0 (new H2 added)
    - `grep -c "^### Test 2 — Non-Member Login Rejection$" .planning/phases/03-response-integrity/03-UAT.md` returns **1** (only the new sub-block uses the `### Test 2 — ...` em-dash format; the original L19 heading uses `### 2.` numbered form)
    - `grep -c "^### 2\\. Non-Member Login Rejection$" .planning/phases/03-response-integrity/03-UAT.md` returns **1** (original L19 heading preserved byte-identical)
    - `grep -c "^### Test 3 — Error Page Invite Link$" .planning/phases/03-response-integrity/03-UAT.md` returns **1** (only the new sub-block uses the em-dash format)
    - `grep -c "^### 3\\. Error Page Invite Link$" .planning/phases/03-response-integrity/03-UAT.md` returns **1** (original L24 heading preserved byte-identical)
    - `grep -q "08-UAT-10-SCRIPT.md" .planning/phases/03-response-integrity/03-UAT.md` exits 0 (cross-reference present)
    - `grep -q "result: skipped" .planning/phases/03-response-integrity/03-UAT.md` exits 0 (history preserved)
    - `grep -q "Burner account lacks 2FA" .planning/phases/03-response-integrity/03-UAT.md` exits 0 (original Test 2 reason text preserved byte-identical)
    - `grep -q "Cannot reach not-in-server error page without 2FA-enabled non-member account" .planning/phases/03-response-integrity/03-UAT.md` exits 0 (original Test 3 reason text preserved byte-identical)
    - `grep -q "executor: <Discord handle" .planning/phases/03-response-integrity/03-UAT.md` exits 0 (placeholder template present)
    - `grep -c "^## " .planning/phases/03-response-integrity/03-UAT.md` returns at least 4 (Tests, Summary, Gaps, Second-Human Verification)
    - The original L19-27 lines (`### 2.`, `expected:`, `result:`, `reason:` for Test 2 and Test 3) appear unchanged: verifiable via `git diff -U0 .planning/phases/03-response-integrity/03-UAT.md` should show ONLY additive lines (no `^-` lines except the file-end-newline-shift artifact)
    - Plan 04 task 2 touches NO `src/**`, `supabase/**`, or `e2e/**` files (TEST-10-readonly gate; verifiable via `git diff --name-only` for this commit showing only `.planning/` paths)
  </acceptance_criteria>
  <done>
    `.planning/phases/03-response-integrity/03-UAT.md` has the new `## Second-Human Verification` H2 section appended at end-of-file. Original Tests 2 + 3 skipped records preserved byte-identical (legacy `### 2.` / `### 3.` numbered H3 headings unchanged). Two NEW H3 sub-blocks (`### Test 2 — Non-Member Login Rejection`, `### Test 3 — Error Page Invite Link` — em-dash format) with placeholder evidence fields are in place, ready for the tester to fill in via the runbook from Task 1. Cross-reference to the runbook is present in the section's intro.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tester → live production site (`polls.wtcsmapban.com`) | Tester exercises the real OAuth flow with a real Discord account; no test/staging surface |
| Runbook → tester | Misleading instructions cause incorrect verification claims to be recorded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Spoofing | Tester records pass without actually exercising the path | mitigate | Runbook explicitly requires capturing screenshots of: error page, URL bar, DevTools cookies pane. Recording false evidence requires fabricating screenshots — high social-friction. PR review of the appended `03-UAT.md` evidence is the human-layer enforcement. |
| T-08-15 | Tampering | Plan accidentally modifies the original L19-27 skipped records | mitigate | Acceptance criteria use `grep -q "Burner account lacks 2FA"` and `grep -q "Cannot reach not-in-server error page without 2FA-enabled non-member account"` to confirm byte-identical preservation. Action explicitly says "DO NOT modify any of L1-51." `git diff -U0` cross-check confirms only additive lines. |
| T-08-16 | Information Disclosure | Runbook leaks production OAuth/test credentials | accept | The runbook does NOT cite any credentials. Tester uses their OWN Discord account; no shared/test credentials. Production OAuth client ID is already public (visible in any login flow). Risk: none. |
| T-08-17 | Denial of Service | Tester performs the test against a misconfigured production env | accept | Phase 8 ships ASYNCHRONOUSLY (D-11). If production is misconfigured at the moment of testing, the test FAILS — which is the correct outcome (records `result: fail` + a follow-up GitHub issue per the runbook's "Post-evidence cleanup" section). The runbook does NOT require pre-flight environment checks beyond "site loads." |
| T-08-18 | Elevation of Privilege | Tester gains unintended access by exercising the OAuth path | accept | Test 2 explicitly verifies REJECTION. If the tester's account is rejected (the expected PASS outcome), no elevation occurs. If the tester's account is INCORRECTLY accepted (a security regression), this is the FAIL signal — record it and file the follow-up issue. The verification surfaces precisely this class of regression. |

</threat_model>

<verification>
**Per-task automated:** see each task's `<verify>` block.

**Plan-level (after both tasks complete):**

1. Both files exist and pass content gates (Task 1 + Task 2 acceptance criteria).
2. TEST-10-artifacts gate (from VALIDATION.md):
   ```bash
   [ -f .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md ] && grep -q '^## Second-Human Verification' .planning/phases/03-response-integrity/03-UAT.md
   ```
   EXPECTED: exit 0.
3. TEST-10-readonly gate (from VALIDATION.md):
   ```bash
   git diff --name-only HEAD~1..HEAD -- 'src/**' 'supabase/**' 'e2e/**' | wc -l
   ```
   EXPECTED: 0 (this plan touches only `.planning/` paths).
4. Original `03-UAT.md` records preserved byte-identical:
   ```bash
   git show HEAD:.planning/phases/03-response-integrity/03-UAT.md | sed -n '1,51p' > /tmp/before.txt
   sed -n '1,51p' .planning/phases/03-response-integrity/03-UAT.md > /tmp/after.txt
   diff /tmp/before.txt /tmp/after.txt
   ```
   EXPECTED: empty diff.
</verification>

<success_criteria>
- `.planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md` exists, is 100–250 lines, contains all required content gates from Task 1 acceptance criteria
- `.planning/phases/03-response-integrity/03-UAT.md` has the new `## Second-Human Verification` H2 section appended at end-of-file
- Original L1-51 of `03-UAT.md` preserved byte-identical (verified via diff against pre-plan version)
- Plan touches no `src/**`, `supabase/**`, or `e2e/**` files (TEST-10-readonly gate)
- TEST-10 deliverable artifacts are in place; the actual second-human session can run asynchronously per D-11
</success_criteria>

<output>
After completion, create `.planning/phases/08-e2e-test-hygiene/08-04-SUMMARY.md` documenting:
- The two artifacts created/modified
- A note that Phase 8 closure does NOT block on the placeholder fields being filled in (D-11)
- A pointer to the runbook + a recruiting note for finding a qualified tester
- Confirmation that no `src/**`, `supabase/**`, or `e2e/**` files were touched
</output>
</output>
