---
phase: 17-planning-doc-ui-hygiene-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/AdminsList.tsx
  - src/components/admin/CategoriesList.tsx
  - src/components/admin/PromoteAdminDialog.tsx
  - src/__tests__/admin/admins-tab.test.tsx
  - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-02-SUMMARY.md
autonomous: true
requirements: [UIDN-04, UIDN-05]

must_haves:
  truths:
    - "AdminsList and CategoriesList render their list section inside a shadcn Card (header title + action button in CardHeader, rows full-bleed in CardContent)"
    - "PromoteAdminDialog search-results container renders inside a Card/CardContent"
    - "All existing component states (loading, error, empty, populated, edit/new-row, dialogs) survive with no behavioral regression"
    - "Dialog ARIA roles survive the PromoteAdminDialog migration, PROVEN behaviorally: the admins-tab test asserts screen.getByRole('dialog', { name: /promote admin/i }) after the dialog opens (not merely duplicate-text count). Runtime role=dialog + aria-labelledby are Radix-applied from the surviving DialogContent/DialogTitle wrappers; no test previously asserted this, so this plan ADDS the assertion."
    - "npm run lint, tsc -b, and npm run test all pass with zero errors after migration"
    - "Card vertical-inset decision is EXPLICIT, not left to executor discretion: each migrated Card with a full-bleed (p-0 CardContent) row list either carries py-0 (or an equivalent vertical override) on the <Card> for true edge-to-edge rows, OR the SUMMARY records an explicit decision to keep the native py-6 inset with rationale. The plan no longer defers this to 'Claude's Discretion'."
    - "Heading-semantics handling for the <h2> → CardTitle change is decided and documented: CardTitle renders a <div> (card.tsx line 31), so the migration drops the <h2> landmark. The chosen disposition (accept the shadcn pattern as the app-wide convention for section cards, matching every other Card consumer) is stated in the SUMMARY; if an accessible heading is required it is preserved explicitly. Decision is not silent."
    - "Decision traceability (Plan B): D-01: native shadcn Card styling (rounded-xl / shadow-sm, no pixel-parity overrides); D-02: section heading moves into CardHeader/CardTitle with the section action Button in the header row; D-03: rows full-bleed via CardContent p-0 so divide-y reaches the card border; D-04: one Card per section, never one Card per row; D-05: PromoteAdminDialog migration is audit-gated (flag confirmed still applies) and MUST verify the Dialog ARIA roles + aria-labelledby survive — verified BEHAVIORALLY via a newly-added getByRole('dialog', { name: /promote admin/i }) assertion plus a source grep that DialogContent/DialogTitle wrappers survive (not by trust alone); D-09: two-plan split — Plan B = UI Card migration (code-only, files disjoint from Plan A), runs in parallel; D-10: src/ comments are WHY-only — NO phase-ID / UIDN / round archaeology tags in component source; D-11: test verification lands in the same commit as the component change — re-scoped to behavioral admin tests staying green since NO .snap files exist; D-12: PR merge gate — all configured bots reviewed AND explicit user OK before merge"
  artifacts:
    - path: "src/components/admin/AdminsList.tsx"
      provides: "Admins list section wrapped in shadcn Card"
      contains: "CardContent"
    - path: "src/components/admin/CategoriesList.tsx"
      provides: "Categories list section wrapped in shadcn Card"
      contains: "CardContent"
    - path: "src/components/admin/PromoteAdminDialog.tsx"
      provides: "Search-results container wrapped in shadcn Card"
      contains: "CardContent"
  key_links:
    - from: "src/components/admin/AdminsList.tsx"
      to: "src/components/ui/card.tsx"
      via: "import { Card, CardHeader, CardTitle, CardAction, CardContent } from '@/components/ui/card'"
      pattern: "from '@/components/ui/card'"
    - from: "src/components/admin/CategoriesList.tsx"
      to: "src/components/ui/card.tsx"
      via: "import { Card, CardHeader, CardTitle, CardAction, CardContent } from '@/components/ui/card'"
      pattern: "from '@/components/ui/card'"
    - from: "src/components/admin/PromoteAdminDialog.tsx"
      to: "src/components/ui/card.tsx"
      via: "import { Card, CardContent } from '@/components/ui/card'"
      pattern: "from '@/components/ui/card'"
---

<objective>
Plan B — UI Card migration (UIDN-04 / UIDN-05). Replace the hand-rolled `<div className="... border rounded-md">` list containers in three admin components with the vendored shadcn `Card` primitive, matching the native-Card look already used by LandingPage / AuthErrorPage (D-01: native shadcn Card styling, no pixel-parity overrides). Structural-only change — no behavior, props, event handlers, or copy change. This is the UI half of the two-plan split (D-09: Plan B = code-only Card migration, file-disjoint from Plan A, parallel-safe).

Purpose: Visual consistency — make the admin lists look like the rest of the app's cards (D-01), eliminating the last hand-rolled list containers flagged since the Phase 12 UIDN-03 sweep.
Output: 3 modified component files + 1 modified test file (`admins-tab.test.tsx`, to ADD the dialog-role ARIA assertion that proves D-05 — it does not exist today). Verified green via the behavioral admin tests (admins-tab.test.tsx, categories-tab.test.tsx), lint, and tsc.

CRITICAL — TEST REALITY (overrides the UI-SPEC § "Snapshot Test Contract"): there are NO .snap files in this repo. The admin tests are behavioral (role/text queries, fireEvent, mocked hooks) with zero toMatchSnapshot and zero DOM-structure assertions. Decision D-11 ("update snapshots in the same commit") has NO snapshot artifact to update and is RE-SCOPED: the verification criterion is the behavioral admin tests stay GREEN in the same commit as the component change (the Card migration is structural-only and must not change runtime behavior), plus lint + tsc pass with zero errors. Do NOT plan or attempt to create/update any .snap file. ONE test-file edit IS in scope (Task 3): adding a `getByRole('dialog')` assertion to `admins-tab.test.tsx` to PROVE D-05 ARIA survival — this is a test ADDITION, not a behavior change to the components, and it is the review-mandated fix for the previously-overstated ARIA-coverage claim. The only structural hooks the tests may rely on — data-testid="admin-skeleton" and data-testid="category-skeleton" — MUST be preserved.

CRITICAL — IMPORT CONVENTION: this repo uses the `@/*` TypeScript path alias (configured in tsconfig.app.json `"@/*": ["./src/*"]`, vite.config.ts, and components.json). Every sibling import in these three files already reads `@/components/ui/...` (Button, Alert, Input, Dialog), and LandingPage/AuthErrorPage import Card via `@/components/ui/card`. All Card imports added by this plan MUST use `import { ... } from '@/components/ui/card'` — NOT a relative path. Do NOT introduce `../ui/card`.

Cross-cutting decisions governing this plan: D-10 (src/ comments WHY-only, no phase/decision/round archaeology tags in component source — plan refs belong in PR/commit, never in .tsx), D-11 (re-scoped to behavioral-test-green in the same commit), D-12 (PR merge gate — bots reviewed + explicit user OK before merge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-planning-doc-ui-hygiene-sweep/17-CONTEXT.md
@.planning/phases/17-planning-doc-ui-hygiene-sweep/17-UI-SPEC.md
@.planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md

<interfaces>
<!-- shadcn Card primitive exports (src/components/ui/card.tsx) the executor composes against.
     The executor MUST read the live primitive — do not assume default paddings. Key facts: -->
<!-- Import via the `@/` alias: import { Card, CardHeader, CardTitle, CardAction, CardContent } from '@/components/ui/card' -->
<!-- Card: `flex flex-col gap-6 rounded-xl border bg-card py-6 text-card-foreground shadow-sm`
       (vertical padding py-6 is on Card itself; horizontal padding lives on Header/Content via px-6). -->
<!-- CardHeader: CSS grid that auto-promotes 2-column layout when a CardAction child is present
       (`has-data-[slot=card-action]:grid-cols-[1fr_auto]`) — this replaces the hand-rolled
       `flex items-center justify-between` header row; put the section Button inside <CardAction>. -->
<!-- CardTitle: `leading-none font-semibold` (600) — same weight the current <h2 ...font-semibold> renders at. -->
<!-- CardContent: `px-6` only. Override with p-0 (D-03) so divide-y rows reach the card border.
       Card's own py-6 still applies; diff the live render and address top/bottom edge gap there if needed. -->
Exports (from src/components/ui/card.tsx): Card, CardHeader, CardTitle, CardAction, CardContent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: UIDN-04 — Migrate AdminsList.tsx list section to shadcn Card</name>
  <files>src/components/admin/AdminsList.tsx</files>
  <read_first>
    - src/components/admin/AdminsList.tsx (CURRENT structure — outer div + flex header + h2 + Promote-admin Button; loading/empty/populated branches; error early-return Alert; PromoteAdminDialog + DemoteAdminDialog siblings. NOTE its existing imports all use the `@/components/ui/...` alias)
    - src/components/ui/card.tsx (the live primitive — read it; do not assume paddings)
    - src/components/auth/LandingPage.tsx (line ~11 — the native-Card look target, D-01; imports Card via `@/components/ui/card`)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-UI-SPEC.md § "UIDN-04" + § "State Coverage > AdminsList.tsx States" + § "Copywriting Contract > AdminsList" + § "Accessibility Contract"
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md § AdminsList.tsx (exact before/after + "do NOT touch" deltas)
  </read_first>
  <action>
    Add `import { Card, CardHeader, CardTitle, CardAction, CardContent } from '@/components/ui/card'` — matching the live `@/` path-alias convention this file's neighbors already use (the existing Button import in this same file reads `@/components/ui/button`; the `@/*` alias is configured in tsconfig.app.json + vite.config.ts + components.json). Do NOT use a relative `../ui/card` path.

    Structural deltas (per UI-SPEC § UIDN-04 delta table):
    - Outer wrapper `<div>` → `<Card>` (D-01: native shadcn Card styling — adopt rounded-xl / shadow-sm, do NOT override to chase the old border rounded-md pixel-parity).
    - D-02: the section heading + its action Button move into the CardHeader. Header `<div className="flex items-center justify-between mb-4">` → `<CardHeader>`.
    - `<h2 className="text-base font-semibold">Admins</h2>` → `<CardTitle>Admins</CardTitle>` (keep the literal text "Admins"; D-02 heading-into-CardTitle).
    - The Promote-admin `<Button onClick={() => setPromoteOpen(true)} size="sm" className="h-9">` (with the UserPlus icon + "Promote admin" label) → wrap it in `<CardAction>...</CardAction>` inside the CardHeader (D-02: action Button in the header row alongside the title). Keep the Button props, icon, and label byte-identical.
    - D-03: loading, empty, and populated branches all move inside ONE `<CardContent className="p-0">` (full-bleed). On each inner row container, DROP the `border rounded-md` classes (the Card supplies border + radius); KEEP `divide-y`. Empty-state branch is the bare `flex flex-col items-center justify-center py-16 text-center` block, also inside CardContent.

    PRESERVE exactly (do NOT touch):
    - The `if (error) return <Alert variant="destructive" role="alert">...` early-return — it runs BEFORE Card JSX and stays OUTSIDE the Card (UI-SPEC State Coverage confirms). Keep role="alert" and the Retry Button.
    - Row internals: `flex items-center justify-between p-4 min-h-[64px]`, avatar block, isSelf → "You" badge vs Demote Button.
    - data-testid="admin-skeleton" on the skeleton divs.
    - PromoteAdminDialog + DemoteAdminDialog: keep their current sibling relationship (Radix portals to body — placement is functionally irrelevant). Wrap ONLY the list section in the Card.
    - Existing WHY-comments (e.g. "Surface fetch failures explicitly", self-demotion guard).

    D-04: ONE Card for the whole Admins section — never one Card per row. D-10: NO phase-ID / UIDN-04 / round archaeology comments in src/ — WHY-only comments only, do not add migration tags to the .tsx source.

    CARD VERTICAL-INSET DECISION (must be explicit, NOT executor discretion — review consensus): `CardContent className="p-0"` only removes HORIZONTAL padding; `Card`'s own `py-6` (24px top/bottom) still insets the first/last rows from the card border. The old hand-rolled container was flush. To keep the list visually flush like the old container, add `py-0` to the `<Card>` (i.e. `<Card className="py-0">`) so full-bleed rows reach the top/bottom card border — OR, if the native py-6 inset is acceptable, KEEP it and record that explicit choice in the SUMMARY with rationale. Do NOT ship without making this call. Recommended default: `py-0` for parity with the old flush list; CardHeader keeps its own padding so the title row is unaffected. Apply the SAME decision to CategoriesList (Task 2) and PromoteAdminDialog (Task 3) for consistency.

    HEADING-SEMANTICS DECISION (must be explicit — review consensus): `CardTitle` renders a `<div>` (card.tsx line 31), so `<h2>Admins</h2>` → `<CardTitle>Admins</CardTitle>` DROPS the section's `<h2>` heading landmark. Disposition (locked): ACCEPT the shadcn CardTitle pattern as the app-wide convention for section cards — it matches every other Card consumer (LandingPage/AuthErrorPage) and the admin lists are inside an already-labeled admin route, so the lost `<h2>` is not a navigation landmark regression in practice. Record this accepted tradeoff in the SUMMARY. (Do NOT silently drop it; do NOT add an extra hidden h2 unless a later a11y audit requires it.) No font-weight change: CardTitle's `font-semibold` matches the old `<h2 className="text-base font-semibold">` weight — parity unchanged. Add `className="text-base"` to CardTitle only if the live render shows a size drift from the old h2.
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run test -- src/__tests__/admin/admins-tab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AdminsList.tsx imports Card, CardHeader, CardTitle, CardAction, CardContent via `import { ... } from '@/components/ui/card'` (the `@/` alias — NOT a relative path). `grep -q "from '@/components/ui/card'" src/components/admin/AdminsList.tsx` succeeds.
    - File contains `<CardContent className="p-0">` and `<CardTitle>Admins</CardTitle>`.
    - The Card vertical-inset decision is applied explicitly: the `<Card>` either carries `py-0` (or equivalent vertical override) for flush full-bleed rows, OR the native `py-6` inset is intentionally kept AND the SUMMARY records that choice with rationale. It is NOT left undecided.
    - File no longer contains `border rounded-md` on the list/loading row containers; `divide-y` is retained.
    - The Promote-admin Button is nested inside `<CardAction>` within `<CardHeader>`; its onClick, size="sm", and "Promote admin" label are unchanged.
    - `data-testid="admin-skeleton"` is still present.
    - The error `<Alert variant="destructive" role="alert">` early-return is still present and outside the Card.
    - No `UIDN-04` / `Phase 17` / review-round string appears anywhere in the file (D-10).
    - The chained verify command `npx tsc -b && npm run lint && npm run test -- src/__tests__/admin/admins-tab.test.tsx` exits 0 (any non-zero tsc/lint/test fails the gate — no `tail` masking).
  </acceptance_criteria>
  <done>AdminsList list section renders inside a shadcn Card with native styling; all states preserved; tsc + lint + the admins-tab behavioral test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UIDN-04 — Migrate CategoriesList.tsx list section to shadcn Card</name>
  <files>src/components/admin/CategoriesList.tsx</files>
  <read_first>
    - src/components/admin/CategoriesList.tsx (CURRENT structure — outer div + flex header + h2 + New-category Button; loading/empty/populated three-way branch with inline edit + new-row Inputs; error early-return Alert; delete-confirmation Dialog. NOTE its existing imports all use the `@/components/ui/...` alias)
    - src/components/admin/AdminsList.tsx (use the just-migrated sibling as the in-repo pattern — same Card/Header/Title/Action/Content shell and same `@/components/ui/card` import)
    - src/components/ui/card.tsx (live primitive)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-UI-SPEC.md § "UIDN-04" CategoriesList delta table + § "State Coverage > CategoriesList.tsx States" + § "Copywriting Contract > CategoriesList" + § "Accessibility Contract"
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md § CategoriesList.tsx (exact deltas + empty-state CTA nuance)
  </read_first>
  <action>
    Identical migration to AdminsList — reuse that result as the pattern. Add `import { Card, CardHeader, CardTitle, CardAction, CardContent } from '@/components/ui/card'` (the `@/` alias, matching this file's existing Button/Input/Alert imports — NOT a relative path).

    Structural deltas (per UI-SPEC CategoriesList delta table):
    - Outer `<div>` → `<Card>` (D-01 native styling); header `<div className="flex items-center justify-between mb-4">` → `<CardHeader>` (D-02 heading + action into the header).
    - `<h2 className="text-base font-semibold">Categories</h2>` → `<CardTitle>Categories</CardTitle>` (keep literal "Categories"; D-02).
    - The header New-category `<Button>` → wrapped in `<CardAction>` (D-02 action Button in header row). Keep its props + "New category" label.
    - D-03: all three state branches (loading skeletons / empty placeholder / populated divide-y) move inside ONE `<CardContent className="p-0">`. Drop `border rounded-md` from the populated branch container; keep `divide-y`. The loading-skeleton branch currently lacks `border rounded-md` (just `divide-y`) — after migration both branches are bare `divide-y` inside CardContent, which normalizes the existing inconsistency.

    PRESERVE exactly:
    - data-testid="category-skeleton", `h-[56px]` skeletons, row `p-4 min-h-[56px] gap-3`.
    - Inline edit-row and new-row Input blocks with their Save/Cancel/Edit/Delete icon buttons.
    - ALL SIX aria-labels on icon buttons must survive — the new-row pair `Save new category` / `Cancel new category`, the EDIT-ROW pair `Save ${cat.name}` / `Cancel ${cat.name}` (do NOT overlook these — they are on the inline edit row, distinct from the new-row pair), and the row-action pair `Edit category ${cat.name}` / `Delete category ${cat.name}`.
    - Destructive styling `text-destructive hover:text-destructive` on the delete button.
    - The empty-state's OWN "New category" Button (Categories empty state has a CTA unlike AdminsList) — keep it inside the empty-state block within CardContent.
    - Keep OUTSIDE the Card: the error `<Alert>` early-return (preserve role="alert" + Retry) and the delete-confirmation `<Dialog>` overlay.

    D-04: one Card per section (never per row); D-10: WHY-only comments, no migration/phase/UIDN tags in the .tsx source.

    Apply the SAME explicit Card vertical-inset decision as Task 1 (`py-0` on `<Card>` for flush rows, or keep native `py-6` and record it in the SUMMARY — not executor discretion) and the SAME accepted heading-semantics disposition (CardTitle renders a `<div>`; the `<h2>` landmark is intentionally dropped per the app-wide shadcn convention, recorded in SUMMARY).
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run test -- src/__tests__/admin/categories-tab.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - CategoriesList.tsx imports Card, CardHeader, CardTitle, CardAction, CardContent via `import { ... } from '@/components/ui/card'` (the `@/` alias — NOT a relative path). `grep -q "from '@/components/ui/card'" src/components/admin/CategoriesList.tsx` succeeds.
    - File contains `<CardContent className="p-0">` and `<CardTitle>Categories</CardTitle>`.
    - File no longer contains `border rounded-md` on the populated list container; `divide-y` retained.
    - All SIX icon-button aria-labels are still present: `Save new category`, `Cancel new category`, `Save ${cat.name}`, `Cancel ${cat.name}` (edit-row pair), `Edit category ${cat.name}`, `Delete category ${cat.name}`. `grep -c 'aria-label' src/components/admin/CategoriesList.tsx` returns the same count as before the migration (6).
    - The Card vertical-inset decision is applied explicitly (py-0 override or recorded native-py-6 choice), same as Task 1.
    - `data-testid="category-skeleton"` still present; empty-state "New category" Button still present inside CardContent.
    - The error `<Alert ... role="alert">` and the delete-confirmation `<Dialog>` remain outside the Card.
    - No `UIDN-04` / `Phase 17` / review-round string in the file (D-10).
    - The chained verify command `npx tsc -b && npm run lint && npm run test -- src/__tests__/admin/categories-tab.test.tsx` exits 0 (any non-zero step fails the gate — no `tail` masking).
  </acceptance_criteria>
  <done>CategoriesList list section renders inside a shadcn Card; all CRUD/edit/empty/error states preserved; tsc + lint + the categories-tab behavioral test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: UIDN-05 — Migrate PromoteAdminDialog search-results container to Card + add+verify dialog-role ARIA test + full suite</name>
  <files>src/components/admin/PromoteAdminDialog.tsx, src/__tests__/admin/admins-tab.test.tsx</files>
  <read_first>
    - src/components/admin/PromoteAdminDialog.tsx (CURRENT — search-results `<div className="mt-2 border rounded-md divide-y max-h-64 overflow-auto">` with per-row `hover:bg-accent rounded-md min-h-[44px]`; Section 2 paste-Discord-ID block; showNoMatches <p>; the dialog is built from `<DialogContent>` / `<DialogTitle>Promote admin</DialogTitle>` JSX wrappers — Radix applies role="dialog" + aria-labelledby at RUNTIME, the source has no literal attribute strings. Existing imports use the `@/components/ui/...` alias)
    - src/__tests__/admin/admins-tab.test.tsx (the "opens promote dialog" test ~line 112 currently asserts ONLY getAllByText(/promote admin/i) count — NO getByRole('dialog'). This task strengthens it with a getByRole/findByRole('dialog', { name: /promote admin/i }) assertion to behaviorally prove D-05 runtime-ARIA survival)
    - src/components/ui/card.tsx (live primitive — Card/CardContent are generic data-slot divs with NO role attribute)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-UI-SPEC.md § "UIDN-05 Audit Protocol" + § "State Coverage > PromoteAdminDialog.tsx States" + § "Accessibility Contract" (Dialog ARIA chain row)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md § PromoteAdminDialog.tsx (audit outcome locked: flag still applies; exact deltas; mandatory ARIA verification)
  </read_first>
  <action>
    D-05: UIDN-05 audit outcome is LOCKED (audit-gated disposition) — the flag still applies: the hand-rolled `border rounded-md` search-results container inside the dialog body is migrated to Card/CardContent matching D-01–D-04. (Do NOT re-litigate the audit; do confirm-and-record the audit finding in the SUMMARY.)

    Add `import { Card, CardContent } from '@/components/ui/card'` (the `@/` alias, matching this file's existing Dialog/Button/Input imports — NOT a relative path).

    Deltas (per UI-SPEC § UIDN-05 target structure):
    - `<div className="mt-2 border rounded-md divide-y max-h-64 overflow-auto">` (the `results.length > 0` container) → `<Card className="mt-2 max-h-64 overflow-auto"><CardContent className="p-0"><div className="divide-y">` (D-01 native Card, D-03 full-bleed p-0). The scroll containment (`max-h-64 overflow-auto`) and `mt-2` margin move onto the Card wrapper.
    - On each result row, REMOVE the per-row `rounded-md` (rows inside a Card don't need individual rounding). KEEP `hover:bg-accent`, `p-2`, `min-h-[44px]`, the avatar/username/Discord-ID block, and the Promote Button unchanged.
    - LEAVE UNTOUCHED: Section 2 (paste Discord ID block), the `showNoMatches` `<p className="text-xs text-muted-foreground">`, the snowflake validation logic, and the `<DialogContent>` / `<DialogHeader>` / `<DialogTitle>` structure.

    MANDATORY ARIA verification (D-05: the migration MUST verify the Dialog's ARIA roles + aria-labelledby survive). IMPORTANT CORRECTION (review consensus, HIGH): the existing admin test suite does NOT currently assert runtime dialog ARIA — `admins-tab.test.tsx` (the "opens promote dialog" test at ~line 112) only asserts a duplicate-text count via `getAllByText(/promote admin/i)`. There is NO `getByRole('dialog')` and NO `aria-labelledby` assertion anywhere under `src/__tests__/`. So D-05 ARIA survival is NOT proven by the current tests — it must be made true, not assumed.

    Two-part verification:
    1. SOURCE-LEVEL: Card / CardContent render as generic `<div data-slot="...">` with NO role attribute. The dialog's ARIA semantics (role="dialog" + aria-labelledby) are applied by Radix at RUNTIME from the `<DialogContent>` / `<DialogTitle>` JSX wrappers — not literal strings in the source. Confirm via grep that the `<DialogContent` and `<DialogTitle` JSX wrappers survive the edit and that NO `role=` attribute was hand-added to the Card/CardContent.
    2. BEHAVIORAL (ADD THE ASSERTION — this is the HIGH fix): strengthen the existing "opens promote dialog" test in `src/__tests__/admin/admins-tab.test.tsx`. Replace (or augment) the weak `getAllByText(/promote admin/i)` count with a real runtime-ARIA assertion: after `fireEvent.click(promoteBtn)`, assert `expect(await screen.findByRole('dialog', { name: /promote admin/i })).toBeInTheDocument()`. This proves Radix applies `role="dialog"` + the accessible name (from `<DialogTitle>Promote admin</DialogTitle>` via aria-labelledby) survives the Card migration. `admins-tab.test.tsx` is in this plan's `files_modified`. Keep the rest of that test file unchanged.

    Record the ARIA-survival confirmation in the SUMMARY (both the source grep result and the new behavioral assertion passing). Do NOT overclaim "the suite already asserts runtime ARIA" — state accurately that this plan ADDED the assertion.

    Then run the FULL admin test suite + lint + tsc as the plan-level gate (this task is the consolidation point for Plan B verification), INCLUDING the newly-added getByRole('dialog') assertion. D-11 is RE-SCOPED: there are no .snap files — success = behavioral admin tests green in the same commit as the change, NOT snapshots updated. Do NOT create any .snap file.

    D-04: one Card per section; D-10: WHY-only comments, no migration/phase/UIDN tags in the .tsx source. Apply the SAME explicit Card vertical-inset decision as Tasks 1–2 to the search-results `<Card>` (py-0 for flush rows, or recorded native-py-6 choice — the dialog list already constrains height via max-h-64/overflow-auto, so favor py-0 for a flush scroll area unless the live render argues otherwise).
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run test && grep -n '<DialogContent\|<DialogTitle' src/components/admin/PromoteAdminDialog.tsx && grep -q "getByRole('dialog'\|findByRole('dialog'" src/__tests__/admin/admins-tab.test.tsx && echo "ARIA ASSERTION PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - PromoteAdminDialog.tsx imports Card, CardContent via `import { Card, CardContent } from '@/components/ui/card'` (the `@/` alias — NOT a relative path). `grep -q "from '@/components/ui/card'" src/components/admin/PromoteAdminDialog.tsx` succeeds.
    - The `results.length > 0` block renders `<Card className="mt-2 max-h-64 overflow-auto">` wrapping `<CardContent className="p-0"><div className="divide-y">`.
    - The container no longer contains `border rounded-md` and per-row `rounded-md` is removed; `hover:bg-accent`, `p-2`, `min-h-[44px]` retained on rows.
    - The `<DialogContent>` and `<DialogTitle>` JSX wrappers remain in the source (`grep -n '<DialogContent\|<DialogTitle' src/components/admin/PromoteAdminDialog.tsx` returns both); no `role=` attribute was hand-added to the Card/CardContent.
    - A behavioral ARIA assertion now EXISTS: `src/__tests__/admin/admins-tab.test.tsx` asserts `getByRole('dialog', { name: /promote admin/i })` (or `findByRole`) after the promote dialog opens — `grep -q "getByRole('dialog'\|findByRole('dialog'" src/__tests__/admin/admins-tab.test.tsx` succeeds. The "opens promote dialog" test no longer relies solely on `getAllByText` count. This is the added proof of D-05 runtime-ARIA survival (it did not exist before this plan).
    - The search-results `<Card>` applies the explicit vertical-inset decision (py-0 or recorded native-py-6 choice), consistent with Tasks 1–2.
    - Section 2 paste-Discord-ID block, showNoMatches `<p>`, and snowflake validation are unchanged.
    - No `UIDN-05` / `Phase 17` / review-round string in the file (D-10).
    - The chained verify command `npx tsc -b && npm run lint && npm run test` exits 0 (full suite passes with zero failures and no new skips; any non-zero step fails the gate — no `tail` masking). No `.snap` files were created (`find src -name '*.snap'` returns empty).
  </acceptance_criteria>
  <done>PromoteAdminDialog search-results container migrated to Card/CardContent with the DialogContent/DialogTitle JSX wrappers intact; a new getByRole('dialog', { name: /promote admin/i }) assertion in admins-tab.test.tsx behaviorally proves runtime ARIA survival (D-05); full admin behavioral suite + lint + tsc green; no snapshot artifacts created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies three admin React components, replacing hand-rolled `<div>` list containers with the already-vendored shadcn `Card` primitive. No new dependency is installed (Card is in-repo at `src/components/ui/card.tsx`). No data flow, network call, auth check, Edge Function, RLS policy, or input-validation path is added or altered. The admin route is already role-gated upstream; this change is presentational only.

| Boundary | Description |
|----------|-------------|
| client → admin route (pre-existing) | Already gated by the admin role check; this plan does NOT touch that gate — it only restyles list containers rendered after the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17B-01 | Tampering | Elevation/Information — Spoofing of ARIA semantics in PromoteAdminDialog | mitigate | Card/CardContent are generic role-less `<div>`s; Task 3 explicitly verifies (D-05) the `<DialogContent>` / `<DialogTitle>` JSX wrappers survive (source grep) so Radix still applies role="dialog" + aria-labelledby at runtime, and that no role is hand-added to the Card. NOTE (corrected per review): the prior admin tests did NOT assert runtime dialog ARIA — Task 3 ADDS a `getByRole('dialog', { name: /promote admin/i })` assertion to `admins-tab.test.tsx` so runtime ARIA survival is behaviorally proven, not merely trusted. Prevents accidental a11y semantic regression |
| T-17B-02 | Tampering | Admin component behavior regression via structural edit | mitigate | Structural-only change with no prop/handler/state edits; existing behavioral admin tests (admins-tab, categories-tab) + full `npm run test` + tsc + lint act as the regression gate before commit (D-11 re-scoped); D-12 PR merge gate (bots reviewed + explicit user OK) is the final landing control |
| T-17B-03 | Elevation of Privilege | Admin self-demotion guard in AdminsList | accept | The isSelf → "You" badge vs Demote guard is explicitly preserved (not modified); no change to the privilege logic — only the surrounding container is restyled |
| T-17B-SC | Tampering | npm package installs | n/a — accept | ZERO installs in this plan — Card is already vendored. No package legitimacy gate applies; RESEARCH Package Legitimacy Audit not required because no `npm install` task exists |

Per the ROADMAP Phase 17 risk classification (NIL production risk), this cosmetic Card-wrapper swap introduces no schema, Edge Function, auth-path, or data-flow change. The two `mitigate` threats are covered by the existing test/lint/tsc/ARIA-grep gates already embedded in the tasks, plus the D-12 merge gate.
</threat_model>

<verification>
- `npx tsc -b` exits 0 and `npm run lint` exits 0 after all three migrations (chained with `&&`, no `tail` masking).
- `npm run test` (full suite) passes — specifically admins-tab.test.tsx and categories-tab.test.tsx behavioral tests stay green (D-11 re-scoped to behavioral-green in the same commit).
- All three components import Card from `@/components/ui/card` (the `@/` alias) and contain `<CardContent>`; none contain `border rounded-md` on their migrated list containers.
- `data-testid="admin-skeleton"` and `data-testid="category-skeleton"` preserved.
- PromoteAdminDialog's `<DialogContent>` / `<DialogTitle>` JSX wrappers survive (source grep); no role hand-added to Card. A NEW `getByRole('dialog', { name: /promote admin/i })` assertion in `admins-tab.test.tsx` proves runtime ARIA survival (the prior suite did not assert it) — D-05 satisfied behaviorally, not by trust.
- The Card vertical-inset decision (py-0 flush vs recorded native py-6) is applied explicitly across all three components — not left to executor discretion.
- The `<h2>` → CardTitle heading-semantics tradeoff is accepted per the app-wide shadcn convention and recorded in the SUMMARY (not silently dropped).
- No `.snap` files created anywhere (`find src -name '*.snap'` empty).
- No `UIDN-04`/`UIDN-05`/`Phase 17`/review-round archaeology strings in any src/ file (D-10).
</verification>

<success_criteria>
- UIDN-04: AdminsList + CategoriesList render shadcn Card wrappers (D-02 header title + action; D-03 full-bleed rows in CardContent); lint + tsc + behavioral tests green.
- UIDN-05: PromoteAdminDialog search-results container migrated to Card/CardContent (D-05 audit-gated) with the DialogContent/DialogTitle JSX wrappers (runtime ARIA) verified intact.
- D-01 native-Card look (rounded-xl / shadow-sm) adopted — no pixel-parity overrides.
- D-04 one Card per section; D-09 code-only half of the two-plan split (file-disjoint from Plan A); D-10 no archaeology comments in src/; D-11 re-scoped to behavioral-test-green (no snapshots); D-12 PR merge gate (bots + user OK) before landing.
- All Card imports use the `@/components/ui/card` alias (repo convention).
- Zero behavioral regression; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/17-planning-doc-ui-hygiene-sweep/17-02-SUMMARY.md` when done (this file and `src/__tests__/admin/admins-tab.test.tsx` are both in `files_modified` — expected outputs). Record:
- The UIDN-05 audit confirmation (flag applied, container migrated).
- The ARIA-survival verification: DialogContent/DialogTitle JSX wrappers intact (source grep) AND the newly-added `getByRole('dialog', { name: /promote admin/i })` assertion passing (state plainly that this assertion was ADDED — the prior suite did not assert runtime dialog ARIA).
- The Card vertical-inset decision actually applied (py-0 flush, or native py-6 kept) with the rationale, for each of the three components.
- The accepted `<h2>` → CardTitle heading-semantics tradeoff (div landmark dropped per the app-wide shadcn convention).
- The full-test-suite + lint + tsc outcomes, and confirmation that no .snap artifacts were created (D-11 re-scope honored).
</output>
</output>
