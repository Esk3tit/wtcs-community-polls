---
phase: 15-observability-e2e-verify-close
plan: 04
type: execute
wave: 3
depends_on: ['15-01', '15-02', '15-03']
files_modified:
  - .planning/phases/15-observability-e2e-verify-close/15-04-preflight.json
  - .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-03-event.png
  - .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-04-stack.png
  - .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-05-dedupe.png
  - .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-05-counts.png
  - .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-14-pass.png
  - .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-15-pass.png
  - .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-16-pass.png
  - .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md
autonomous: false
requirements: [OBSV-03, OBSV-04, OBSV-05, TEST-14, TEST-15, TEST-16]
tags: [evidence-capture, manual-verification, sentry, playwright]

must_haves:
  truths:
    - "A Sentry event from the Netlify deploy preview release exists and carries the `boundary: app-root` tag, fired from `/__smoke?fire=render` with literal message `OBSV-03 render`"
    - "A second, distinct Sentry event exists (not collapsed by Dedupe) carrying literal message `OBSV-05 dedupe` from `/__smoke?fire=dedupe`"
    - "Per-message Sentry event count is exactly 1 for each distinct message in a tight time window — proving the triple-handler path collapsed to one event per scenario, not just that the two scenarios are distinct"
    - "At least one Sentry event from the preview release shows a stack frame containing a real component name (e.g., `RenderThrowSmoke` or one of the allowlist names) rather than `$M`"
    - "`sentry-cli releases files <VITE_COMMIT_SHA> list` output for the preview release shows uploaded source map artifacts (`.js.map` files) for the current release"
    - "sentry-cli is invoked via the lockfile-resolved transitive `@sentry/cli` from `@sentry/vite-plugin` (resolve via `npx --no-install @sentry/cli --version`). If the transitive CLI cannot be resolved, fall back to an EXACT pinned version (e.g., `npx @sentry/cli@2.58.5`) — NEVER `@latest` and NEVER an unpinned `@2` major range (CLI-flag drift across the major). Exact resolved version captured for traceability."
    - "OBSV-04(b) sourcemap-upload evidence prefers the artifact-bundle / debug-IDs surface (`npx --no-install @sentry/cli sourcemaps list`) over the deprecated `releases files <release> list` surface; if only the deprecated surface is available, the plan explicitly justifies its use in the EVIDENCE draft"
    - "Release-consistency verified: event.release == artifact.release == head_sha (literal 40-char hex match) before treating the artifacts query as authoritative"
    - "Netlify deploy-preview build context has `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and `SENTRY_PROJECT` env vars set BEFORE sentry-cli is invoked (so empty-list output disambiguates from auth failure)"
    - "Task 4 only runs AFTER Task 2's resume-signal (`preview-env-confirmed` or `preview-env-remediated`) is present in chat"
    - "The latest CI run on the Phase 15 PR shows the `e2e` job green, with PASS lines for `admin-create.spec.ts`, `browse-respond.spec.ts`, and `filter-search.spec.ts`"
    - "All 7 screenshots (4 Sentry + 3 CI) are committed under `.planning/phases/15-observability-e2e-verify-close/artifacts/` as PNG files, each under ~200 KB"
  artifacts:
    - path: ".planning/phases/15-observability-e2e-verify-close/15-04-preflight.json"
      provides: "Tracked pre-flight discovery output — PR URL, deploy-preview URL, CI run URL, head SHA — read by subsequent tasks"
    - path: ".planning/phases/15-observability-e2e-verify-close/artifacts/"
      provides: "Seven committed PNG screenshots — four Sentry dashboard captures (event, stack, dedupe-list, per-message-counts) + three CI PASS-line captures"
    - path: ".planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md"
      provides: "Draft evidence file with event IDs, CI run URL, sentry-cli output captured — finalized in Plan 05"
  key_links:
    - from: "deploy preview URL"
      to: "Sentry dashboard release"
      via: "VITE_COMMIT_SHA Netlify env → Sentry `release` field"
      pattern: "VITE_COMMIT_SHA"
    - from: "15-EVIDENCE-DRAFT.md"
      to: "artifacts/*.png"
      via: "relative markdown links"
      pattern: "\\./artifacts/.*\\.png"
---

<objective>
Drive the manual smoke verification on the Phase 15 PR's Netlify deploy preview AND capture E2E CI evidence, producing SEVEN committed PNG screenshots and a `15-EVIDENCE-DRAFT.md` file with all the per-requirement evidence anchors filled in. This plan contains operator-run checkpoints (autonomous: false) because Sentry dashboard inspection, `sentry-cli` invocation with a personal `SENTRY_AUTH_TOKEN`, and screenshot cropping all require human action.

Purpose: Capture the smoke-test evidence and CI-pass evidence that prove OBSV-03/04/05 + TEST-14/15/16 are satisfied on the Phase 15 branch's preview deploy and on the latest green CI run for the PR. Plan 05 will compile the final `15-EVIDENCE.md` and close the GitHub issues.

Output: 7 committed PNGs under `artifacts/` (4 Sentry — event, stack, dedupe-list, per-message-counts — + 3 CI PASS lines), a tracked `15-04-preflight.json` recording the four discovered values, a draft `15-EVIDENCE-DRAFT.md` (NOT the final file — Plan 05 produces `15-EVIDENCE.md` finalized post-merge per D-12 freshness rule), and chat-output evidence anchors (event IDs, CI run URL, sentry-cli output transcript with EXACT pinned version + verified-equal release triple).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-observability-e2e-verify-close/15-CONTEXT.md
@.planning/phases/15-observability-e2e-verify-close/15-RESEARCH.md
@.planning/phases/15-observability-e2e-verify-close/15-PATTERNS.md
@.planning/phases/15-observability-e2e-verify-close/15-VALIDATION.md
@./CLAUDE.md
@netlify.toml

<interfaces>
<!-- Pre-checkpoint setup is automated by Claude: the PR must exist with the Wave-1+2 commits pushed, -->
<!-- and the Netlify auto-build for the PR must be green. The operator then drives the dashboard. -->
<!-- Sentry event surface: each captured event has `boundary: app-root` tag, a release equal to VITE_COMMIT_SHA, -->
<!--   and a literal message either `OBSV-03 render` or `OBSV-05 dedupe`. -->
<!-- sentry-cli invocation (cycle-3 cross-AI MEDIUM #6 — pin to exact resolved version, not a major range): -->
<!--   PREFER the lockfile-resolved transitive CLI: `npx --no-install @sentry/cli sourcemaps list ...` -->
<!--   FALLBACK to an EXACT pinned version: `npx @sentry/cli@<exact-version> ...` (e.g., `@sentry/cli@2.58.5`). -->
<!--   NEVER `@latest` and NEVER an unpinned `@2` major range — sentry-cli has had breaking CLI-flag changes within the 2.x major. -->
<!--   Requires SENTRY_AUTH_TOKEN set locally by the operator. -->
<!-- Sentry sourcemap-upload evidence surface (cycle-3 cross-AI MEDIUM #2 — `releases files <release> list` is deprecated in 2.x): -->
<!--   PREFER `npx --no-install @sentry/cli sourcemaps list` (artifact-bundle / debug-IDs surface — the current sourcemap model). -->
<!--   FALLBACK to `releases files <release> list` ONLY if the artifact-bundle surface returns empty OR the installed CLI does not support it; in that case the EVIDENCE draft must explicitly note the surface used + why the artifact-bundle surface was unavailable. -->
<!-- Netlify env scoping (cycle-3 cross-AI MEDIUM #5 — Sentry build-time vars are scoped to `builds`): -->
<!--   Confirm via `netlify env:list --context deploy-preview --scope builds` (NOT just `--context deploy-preview`). -->
<!--   The `--scope builds` filter prevents a false-pass from vars scoped only to functions/runtime. -->
<!--   If `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` are set only for Production, -->
<!--   the preview build's sourcemap upload silently no-ops and OBSV-04(b) cannot be proved. -->
<!-- CI evidence surface (per D-09/D-10): the Playwright HTML reporter or --reporter list output from -->
<!--   the latest workflow run on the PR branch. Three specs to capture PASS lines from. -->
<!-- Pre-flight discovery output: `15-04-preflight.json` schema: -->
<!--   { "pr_url": string, "pr_number": number, "preview_url": string, "ci_run_url": string, "head_sha": string, -->
<!--     "captured_at": ISO timestamp, "ci_conclusion": "success" | "pending" | "..." } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight automation — discover PR + preview deploy + CI run + head SHA, write to tracked JSON</name>
  <files>.planning/phases/15-observability-e2e-verify-close/15-04-preflight.json</files>
  <read_first>
    - .planning/STATE.md (current branch should be the Phase 15 work branch)
    - .github/workflows/ci.yml (just modified in Plan 03 — confirm the lint-and-unit job now includes the build + verify steps from that plan)
    - netlify.toml (confirms VITE_COMMIT_SHA + VITE_NETLIFY_CONTEXT shell-substitution; preview builds inherit Sentry release tagging from $COMMIT_REF)
  </read_first>
  <action>
    Discover the verification preconditions and emit them to a tracked JSON file that Tasks 2–5 read. This task has TWO independent gates split per Warning 2 of the checker review:

    **Gate A — PR exists with auto-close keywords (auto-completable in this task):**

    1. Run `gh pr view --json url,number,headRefName,headRefOid,body --jq '{url: .url, number: .number, head: .headRefName, sha: .headRefOid, has_closes: (.body | test("Closes #(11|12|13|17|19)"))}'`. If no PR exists yet, run `gh pr create --draft --title "Phase 15: Observability + E2E Verify & Close" --body "Closes #11\nCloses #12\nCloses #13\nCloses #17\nCloses #19\n\nDraft — evidence pending"`. Auto-close keywords on a draft PR are PRE-positioned per D-16; they fire only on merge.

    2. If the PR already existed but the body lacks the five `Closes #N` lines, update the body: `gh pr edit --body "$(gh pr view --json body --jq '.body')\n\nCloses #11\nCloses #12\nCloses #13\nCloses #17\nCloses #19"`.

    3. Verify with `gh pr view --json body --jq '.body' | grep -cE "Closes #(11|12|13|17|19)"` returning ≥ 5.

    **Gate B — Build artifacts exist and CI is green (poll/wait — NOT a precondition for Gate A):**

    4. Determine the Netlify deploy preview URL for the PR. Run `gh pr view --json comments --jq '.comments[]?.body' | grep -oE 'https://deploy-preview-[0-9]+--[a-z0-9-]+\\.netlify\\.app' | head -1`, OR check the Netlify deploys list, OR use the PR statusCheckRollup output to find the Netlify preview URL. Capture the URL. The preview URL is the surface for OBSV-03/04/05 smoke verification.

    5. Determine the latest CI run on the PR branch. Run `gh run list --branch <branch> --limit 5 --json databaseId,conclusion,status,url,headSha,workflowName --jq '[.[] | select(.workflowName == "CI")] | .[0]'`. Capture `databaseId`, `conclusion`, `status`, `url`, `headSha`.

    6. If `status` is `in_progress` or `queued`, run `gh run watch <databaseId> --exit-status` to pause until the run finishes (the `--exit-status` flag makes `gh` propagate the conclusion as the process exit code — safer than re-querying). Do NOT fail the task while CI is still running — wait for terminal state, then check conclusion. After watch returns, re-query `gh run view <databaseId> --json conclusion --jq '.conclusion'`.
       **Timeout guidance (cycle-2 CR Finding 4; cycle-3 LOW #8 portability):** wrap the `gh run watch` invocation with a 30-minute upper bound. `timeout(1)` is GNU coreutils — NOT present on stock macOS (the operator's workstation per CLAUDE.md `Platform: darwin`). Use whichever of these is available:
       - **macOS with Homebrew coreutils:** `gtimeout 1800 gh run watch <databaseId> --exit-status` (install via `brew install coreutils` if absent).
       - **Linux runners or CI:** `timeout 1800 gh run watch <databaseId> --exit-status` (GNU `timeout`, standard).
       - **Portable shell-loop fallback (works on macOS+Linux+WSL, no coreutils dependency):**
         ```bash
         start=$(date +%s)
         while true; do
           conclusion=$(gh run view <databaseId> --json status,conclusion --jq '{status: .status, conclusion: .conclusion}')
           status=$(echo "$conclusion" | jq -r .status)
           if [ "$status" = "completed" ]; then break; fi
           if [ $(($(date +%s) - start)) -ge 1800 ]; then echo "Timeout after 30 min"; exit 1; fi
           sleep 30
         done
         ```
       If the timeout fires or the run is still `in_progress`/`queued` after 30 minutes, STOP this task — investigate the CI run in the GitHub Actions UI before retrying (typical causes: stuck runner, branch protection deadlock, or unrelated org-wide outage). To abort manually: Ctrl-C the watch and re-run the workflow via `gh run rerun <databaseId>` once the underlying cause is resolved.

    7. If `conclusion` is not `success` after the wait, FAIL this task with an error directing the operator to investigate the red CI run before proceeding. (Phase 15 evidence cannot cite a red CI run.)

    8. Determine the commit SHA the Netlify preview was built from: `gh pr view --json headRefOid --jq '.headRefOid'`. This becomes the `VITE_COMMIT_SHA` value used in Task 4's `sentry-cli` invocation.

    **Emit the tracked JSON file:**

    9. Write `.planning/phases/15-observability-e2e-verify-close/15-04-preflight.json` with shape:
       ```json
       {
         "pr_url": "<gh pr view url>",
         "pr_number": <number>,
         "preview_url": "<deploy-preview URL>",
         "ci_run_url": "<gh run list url>",
         "ci_run_id": <databaseId>,
         "ci_conclusion": "success",
         "head_sha": "<headRefOid>",
         "captured_at": "<ISO timestamp>"
       }
       ```
       Pretty-print with 2-space indentation. This file is committed alongside the evidence artifacts; subsequent tasks read it via `<read_first>`.

    Emit a chat-output summary block mirroring the file contents. The operator reads this in the checkpoint tasks.

    If any of the four values cannot be determined after the wait (PR not pushed, preview not built, etc.) — STOP and emit a clear error explaining what state is missing. The next tasks (checkpoints) will not be useful without this data.
  </action>
  <acceptance_criteria>
    - **Gate A:** A Phase 15 PR exists with auto-close keywords in the body — `gh pr view --json body --jq '.body' | grep -cE "Closes #(11|12|13|17|19)"` returns at least 5
    - **Gate B:** The latest CI run on the PR branch reached terminal state (after `gh run watch` if needed) and its `conclusion` is `success`. If still red after wait, this task FAILS.
    - The tracked JSON file `.planning/phases/15-observability-e2e-verify-close/15-04-preflight.json` exists with all required keys (`pr_url`, `pr_number`, `preview_url`, `ci_run_url`, `ci_run_id`, `ci_conclusion`, `head_sha`, `captured_at`)
    - `ci_conclusion` value in the JSON file equals `"success"` literally
    - The head commit SHA (`VITE_COMMIT_SHA` equivalent) in the JSON file is a 40-character hex string
  </acceptance_criteria>
  <verify>
    <!-- Cross-AI replan (Codex review): the original `^[1-5]$` regex caps the count at 5 — which IS the expected pass count today,
         but it would fail on counts >=10 if a future hygiene phase adds more issues to the closure set. CodeRabbit Finding 8 raised
         the parallel concern for the single-digit `^\s<sha>[2-9]` regex in Plan 03. Use numeric comparison instead of regex matching. -->
    <automated>test -f .planning/phases/15-observability-e2e-verify-close/15-04-preflight.json &amp;&amp; test $(gh pr view --json body --jq '.body' | grep -cE "Closes #(11|12|13|17|19)") -ge 5</automated>
  </verify>
  <done>
    `15-04-preflight.json` exists with all four discovered values + CI conclusion `success`; PR body carries auto-close keywords for all five issues; operator now has everything they need to drive Tasks 2–5.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms Netlify deploy-preview context has SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT</name>
  <what-built>
    Netlify scopes environment variables per context (Production / Deploy preview / Branch). If `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, or `SENTRY_PROJECT` are NOT set for the "Deploy previews" scope, the preview build's `@sentry/vite-plugin` short-circuits the sourcemap upload silently. OBSV-04(b) cannot be evidenced without those vars in the preview-build context. RESEARCH.md Open Question Q3 (RESOLVED) documents this; the checker review flagged it as a missing pre-flight in Plan 04.
  </what-built>
  <how-to-verify>
    Operator confirms env-var inheritance BEFORE running sentry-cli in Task 4. Two equivalent paths — pick either.

    **Path A (preferred — CLI, scriptable):**
    (a) Operator authenticates with Netlify CLI if not already: `netlify login`.
    (b) Operator links the repo to the Netlify site if not already linked: `netlify link` (or confirm via `netlify status`).
    (c) Operator runs: `netlify env:list --context deploy-preview --scope builds`
        **Why `--scope builds` matters (cycle-3 cross-AI MEDIUM #5):** Netlify env vars are filtered by BOTH context (Production / Deploy preview / Branch) AND scope (Builds / Functions / Runtime / Post-processing). Sentry's `@sentry/vite-plugin` reads `SENTRY_AUTH_TOKEN` / `SENTRY_ORG` / `SENTRY_PROJECT` at BUILD time, so vars must be scoped to `builds` (not just to the runtime/functions scope). Omitting `--scope builds` produces a FALSE PASS when the vars are present in the deploy-preview context but only scoped to functions/runtime — the build then runs without the Sentry credentials and the sourcemap upload silently no-ops, which is exactly the failure mode OBSV-04(b) is meant to catch.
    (d) Confirm three rows appear in the output: `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`. Values themselves do NOT need to be revealed — presence in the list with `[set]` or non-empty marker is sufficient.
    (e) If ANY of the three is missing from the deploy-preview `builds` scope output, the operator MUST set it in Netlify before proceeding. `netlify env:set` writes to all scopes by default, which is fine — the explicit scope filter is only needed on the READ side:
       `netlify env:set SENTRY_AUTH_TOKEN "<token>" --context deploy-preview`
       `netlify env:set SENTRY_ORG "<org-slug>" --context deploy-preview`
       `netlify env:set SENTRY_PROJECT "<project-slug>" --context deploy-preview`
       If a var is already present but only in a non-`builds` scope, use `netlify env:set <KEY> "<value>" --context deploy-preview --scopes builds,functions,runtime,post-processing` (or via UI: edit the var → check "Builds" scope) to widen its scope. Then trigger a fresh deploy-preview build (push an empty commit or use Netlify UI → Deploys → Retry deploy) and wait for the new preview URL.

    **Path B (UI — visual):**
    (a) Open Netlify dashboard for the WTCS Community Polls site → Site settings → Environment variables.
    (b) For each of the three keys (`SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`), open the var's detail panel and confirm BOTH:
        - The "Contexts" list contains **Deploy previews** (in addition to or instead of Production).
        - The "Scopes" list contains **Builds** (Sentry's vite plugin reads these at build time; runtime/functions-only scoping causes a silent sourcemap-upload no-op — cycle-3 cross-AI MEDIUM #5).
    (c) If any key is missing the "Deploy previews" context OR the "Builds" scope, edit the key → check both → save → re-trigger the preview build → wait for the new preview URL.

    **After remediation (if any), update preflight JSON:**
    (f) If a fresh deploy-preview build was triggered, the previous `preview_url` and `head_sha` in `15-04-preflight.json` may be stale. Operator should ask Claude to re-run Task 1 to refresh those values before proceeding to Task 3. Otherwise, the existing preflight JSON remains valid.

    **Disambiguation tree for empty-list output later in Task 4** (per RESEARCH.md Open Question Q2 RESOLVED):
    Operator records the result of this pre-flight check in chat. If Task 4's `sentry-cli releases files <sha> list` later returns empty, the disambiguation order is:
    (1) Re-verify this Task 2 pre-flight passed (all three env vars present in deploy-preview scope).
    (2) Retry the sentry-cli call with explicit `--org <slug> --project <slug>` flags.
    (3) If still empty, capture the full sentry-cli error output (including any stderr) into `15-EVIDENCE-DRAFT.md` and treat OBSV-04(b) as a blocker for Phase 15 issue closure.
  </how-to-verify>
  <resume-signal>
    Type "preview-env-confirmed" with confirmation that all three env vars (`SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`) are set for the Netlify Deploy previews context. If you remediated any missing var and triggered a fresh build, type "preview-env-remediated: <new-preview-url>" and Task 1 will be re-run before continuing.
  </resume-signal>
  <files>(none — operator-verifiable Netlify state; no planner-tracked file changes in this checkpoint)</files>
  <action>OPERATOR CHECKPOINT — see how-to-verify above for the full step list. Operator verifies that `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and `SENTRY_PROJECT` are set for the Netlify Deploy previews context AND the `builds` scope via `netlify env:list --context deploy-preview --scope builds` (or Netlify UI Environment variables → "Deploy previews" context + "Builds" scope). If any var is missing or only present in a non-`builds` scope, sets/widens it via `netlify env:set ... --context deploy-preview --scopes builds,functions,runtime,post-processing`, triggers a fresh preview build, and waits for the new preview URL before proceeding. `--scope builds` is REQUIRED on the read-side filter (cycle-3 cross-AI MEDIUM #5) — without it, vars present only in functions/runtime scope will produce a false-pass and the preview build will silently no-op the Sentry sourcemap upload.</action>
  <verify>
    <human-check>Operator confirms `netlify env:list --context deploy-preview --scope builds` (or Netlify UI inspection of both "Deploy previews" context AND "Builds" scope) shows `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, and `SENTRY_PROJECT` all set for the deploy-preview context AND scoped to builds</human-check>
  </verify>
  <done>All three Sentry env vars confirmed present in the Netlify deploy-preview context AND scoped to `builds`. Task 4's sentry-cli invocation can now be trusted to return an authoritative `.js.map` list (or an authoritative empty list, with the disambiguation tree pinned for that case).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator fires OBSV-03 + OBSV-05 smokes on the deploy preview and captures evidence</name>
  <what-built>
    Plan 01 added `?fire=render` and `?fire=dedupe` triggers on the prod-gated `/__smoke` route. The Phase 15 PR's Netlify deploy preview includes those triggers AND the existing Sentry capture path (already on `main`). The Sentry release for the preview build is the head commit SHA (Netlify exports `$COMMIT_REF` → `VITE_COMMIT_SHA` → Sentry `release` field).
  </what-built>
  <how-to-verify>
    Operator runs the following steps. Each numbered step produces a specific artifact for the EVIDENCE draft.

    Step 1 — Fire OBSV-03 render-throw smoke:
    (a) Open DevTools Console.
    (b) In a fresh tab, navigate to `<preview-URL>/__smoke?fire=render`. The ErrorBoundary should catch the throw and render the fallback. The console should print one line carrying a Sentry event ID. `document.body` should have a `data-sentry-event-id="..."` attribute.
    (c) Copy the event ID from the console line. Record it.
    (d) Open the Sentry dashboard for the project. Filter by `release:<head-SHA>`. Locate the event matching the captured ID. Confirm it carries the `boundary: app-root` tag.
    (e) **Mandatory dashboard cross-check (cycle-3 cross-AI HIGH fix path #2 — Codex empirical probe).** The cycle-2 dual-capture race means the dataset-surfaced `eventId` may NOT be the event Sentry actually persisted (Dedupe may drop the local-boundary capture in favor of the root `onCaughtError` auto-capture, or vice versa). Plan 01's `beforeCapture={(scope) => scope.setTag('boundary', 'app-root')}` ensures whichever event survives carries the canonical tag — but the IDs may still diverge. Verify the divergence does NOT corrupt evidence:
        i. Look up the event whose `event_id` LITERALLY equals the dataset-surfaced ID from (c). Does it exist in the dashboard?
        ii. If YES (event found at the exact dataset ID): the dataset ID is authoritative. Proceed to (f).
        iii. If NO (dataset ID not found in dashboard — the local-boundary capture was dedup'd): find the event whose message is the literal `OBSV-03 render` AND whose release is `<head-SHA>` AND whose tag includes `boundary: app-root`. This is the persisted root-handler event. Record BOTH IDs in chat for the EVIDENCE draft: `dataset_event_id: <id-from-DOM>`, `persisted_event_id: <id-from-Sentry>`, and explicitly note "local-boundary capture was dedup'd; persisted ID below is the root-handler auto-capture". Plan 04 Task 5's EVIDENCE-DRAFT uses the PERSISTED ID (the one findable in Sentry).
        iv. STOP and file a bug if NEITHER ID maps to an event in the dashboard with the expected message + tag + release — that means OBSV-03 is unverified.
    (f) Screenshot the Sentry event detail view (the one corresponding to the PERSISTED event ID — same one used for EVIDENCE.md) showing the message + tag + at least the top of the stack. Save as `.planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-03-event.png`. Crop to keep under ~200 KB.

    Step 2 — Fire OBSV-04 sourcemap-stack inspection (uses the same event from Step 1):
    (a) In the same Sentry event detail view, expand the stack frames. Confirm at least one frame shows a REAL function name from the allowlist (e.g., `RenderThrowSmoke`, `SmokePage`, `ConsentBanner`) rather than mangled `$M`.
    (b) Screenshot the expanded stack frames panel. Save as `artifacts/sentry-obsv-04-stack.png`.
    (c) Record which component names appeared as real names in the stack — Plan 05 lists them verbatim in the final EVIDENCE.md.

    Step 3 — Fire OBSV-05 dedupe smoke:
    (a) In a NEW tab (so the first event ID is left intact for comparison), navigate to `<preview-URL>/__smoke?fire=dedupe`. The DOM `data-sentry-event-id` should now carry a DIFFERENT event ID.
    (b) Record the new dataset event ID. Compare to the Step-1 dataset ID — they MUST be different. If they are identical, the Dedupe contract is broken OR Plan 01 did not produce distinct messages — STOP and file a bug, do NOT proceed.
    (c) Apply the same dashboard cross-check as Step 1(e): look up the event whose `event_id` LITERALLY equals the Step-3 dataset ID. If found, that ID is authoritative. If not found, find the persisted event by `message:"OBSV-05 dedupe"` + `release:<head-SHA>` + `boundary: app-root` and record BOTH the dataset ID and the persisted ID, noting the dedup. Plan 04 Task 5 uses the persisted ID for OBSV-05 in EVIDENCE.md.
    (d) Confirm the persisted event ALSO carries `boundary: app-root` (Plan 01's `beforeCapture` guarantees this regardless of which capture survived Dedupe).
    (e) Screenshot the Sentry issues list view showing BOTH events (OBSV-03 + OBSV-05) under the same release. Save as `artifacts/sentry-obsv-05-dedupe.png`. The intent: visually prove TWO events were captured (one per scenario), not one collapsed event and not three (which would indicate Dedupe is off).
  </how-to-verify>
  <resume-signal>
    Type the explicit distinctness assertion + dashboard cross-check status (cycle-3 cross-AI HIGH fix path #2 — required, not optional):

    ```
    evidence captured:
      OBSV-03-DATASET-ID=<dataset-id-1>, OBSV-03-PERSISTED-ID=<persisted-id-1>, match: <yes|no>
      OBSV-05-DATASET-ID=<dataset-id-2>, OBSV-05-PERSISTED-ID=<persisted-id-2>, match: <yes|no>
      distinct-persisted: <persisted-id-1> != <persisted-id-2>
      tag-present: boundary:app-root on both persisted events: <yes|no>
    ```

    All four `<...>` placeholders must be literal hex strings from Sentry (do NOT paraphrase). The `match: yes` clause means the dataset-surfaced ID was found at the same `event_id` in the Sentry dashboard — i.e., the local-boundary capture survived Dedupe. `match: no` means the dataset ID was NOT findable in the dashboard but the message + tag + release lookup succeeded — i.e., the root-handler auto-capture is the persisted event and the local-boundary capture was dedup'd. EITHER outcome is acceptable AS LONG AS the persisted event carries `boundary: app-root` (Plan 01's `beforeCapture` makes this true unconditionally) — the `tag-present: yes` clause is non-negotiable.

    If `tag-present: no`, STOP and file a bug — the cycle-3 HIGH fix did not land. If `distinct-persisted` is false (the two persisted IDs are identical), STOP and type `blocked: dedupe-collapsed-events`. If any step failed for other reasons, type `blocked: <description>` and stop.
  </resume-signal>
  <files>.planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-03-event.png, .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-04-stack.png, .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-05-dedupe.png</files>
  <action>OPERATOR CHECKPOINT — see <how-to-verify> above for the full step list. Operator fires both smoke triggers on the deploy preview, captures three Sentry event IDs (with the OBSV-03 and OBSV-05 IDs being literally distinct), and saves three PNG screenshots into the phase artifacts/ directory. Each PNG under ~200 KB.

    **OBSV-05 per-message event count strengthening (cycle-3 cross-AI MEDIUM #4 — use Discover/Event Explorer, not Issues search):** in addition to confirming `<id1> != <id2>`, the operator must capture a Sentry events query screenshot showing exactly N=1 EVENTS (not issues) per distinct message in a tight time window. Sentry's **Issues** search with `message:"..."` returns the issue/group count (one issue per unique error fingerprint), which proves issue-group distinctness but NOT event count — Dedupe might still have collapsed three handler-firings into one event within a single issue, and Issues search would not detect that. Use Sentry's **Discover** (or "Event Explorer" / "Events" — the per-event view, not the per-issue view) instead:

    Step A (Discover query — preferred): open Sentry → Discover (or Explore → Events depending on the Sentry plan). Build two queries:
    - Query 1: `release:<head-sha> message:"OBSV-03 render"` with time window set to the tight ±5 minute window covering both smoke firings. Confirm the result table shows EXACTLY 1 row (1 event).
    - Query 2: `release:<head-sha> message:"OBSV-05 dedupe"` with the same time window. Confirm the result table shows EXACTLY 1 row.

    Step B (fallback — if Discover is not available on the Sentry plan): use the per-issue event list. Open the OBSV-03 issue → view its "Events" tab → confirm exactly 1 event in the time window. Repeat for OBSV-05.

    Step C (CLI fallback — if both above are unavailable): `npx --no-install @sentry/cli events list --query 'release:<head-sha> message:"OBSV-03 render"' --range '5m'` (verify exact flag syntax against the installed CLI version). Pipe to `wc -l` to count.

    Capture a single screenshot showing both queries' result tables (or the equivalent event-list views) — each returning exactly 1 event. Save as `artifacts/sentry-obsv-05-counts.png`. The intent (proven): the triple-handler path (Sentry Browser global handler + React 19 root `onCaughtError` + local `Sentry.ErrorBoundary`) collapsed to exactly ONE persisted event per scenario, AND the two scenarios are distinct from each other. Use of Issues search alone would only prove the latter.</action>
  <verify>
    <automated>test -f .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-03-event.png &amp;&amp; test -f .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-04-stack.png &amp;&amp; test -f .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-05-dedupe.png &amp;&amp; test -f .planning/phases/15-observability-e2e-verify-close/artifacts/sentry-obsv-05-counts.png</automated>
    <human-check>Operator types the cycle-3 expanded resume-signal block with: dataset+persisted ID pairs for both OBSV-03 and OBSV-05; explicit `match: yes|no` for each (whether the dataset ID was findable at the same event_id in Sentry); `distinct-persisted: <persisted-id-1> != <persisted-id-2>`; `tag-present: yes` for `boundary: app-root` on both persisted events. AND has captured the `sentry-obsv-05-counts.png` query screenshot showing N=1 per distinct message via Discover/Event Explorer (NOT Issues search) within the tight time window.</human-check>
  </verify>
  <done>Four Sentry PNGs exist in artifacts/ (sentry-obsv-03-event.png, sentry-obsv-04-stack.png, sentry-obsv-05-dedupe.png, sentry-obsv-05-counts.png); two distinct persisted Sentry event IDs captured AND each message-specific Discover/Event-Explorer query returns exactly 1 EVENT in the tight time window (proving the triple-handler path collapsed to one event per scenario, not just one issue-group per scenario); operator confirmed both persisted events carry the `boundary: app-root` tag (Plan 01 `beforeCapture` guarantees this regardless of which capture survived Dedupe); operator's resume-signal explicitly affirms `<persisted-id-1> != <persisted-id-2>` AND records the dataset-vs-persisted ID match status for both events.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator captures `sentry-cli releases files` output and three CI PASS lines</name>
  <what-built>
    Plan 03 wired `verify-sourcemap-names.mjs` into CI; the same CI workflow's `e2e` job already runs `admin-create`, `browse-respond`, and `filter-search` under the `@smoke` grep filter (existing, since v1.0). The latest green CI run on the PR branch is the evidence source for OBSV-04(b)/(c) and TEST-14/15/16.
  </what-built>
  <how-to-verify>
    **Pre-condition gate (cross-AI replan, CR Finding 13):** before running ANY step below, confirm Task 2's resume-signal was submitted. Re-read the Task 2 resume value in chat: the operator must have typed either `preview-env-confirmed` or `preview-env-remediated: <new-preview-url>`. If neither signal is present (e.g., Task 2 was skipped), STOP this task and re-run Task 2 first — sentry-cli output is ambiguous (empty vs auth-failed vs wrong-project) without that pre-flight.

    Operator runs the following steps locally.

    Step 1 — OBSV-04(b) Sentry Artifacts API evidence:
    (a) Ensure `SENTRY_AUTH_TOKEN` is set in the local environment. (Generate one in Sentry under Settings → Account → API → Auth Tokens with `project:releases` AND `project:read` scopes if not already present.)

    (b) **Resolve sentry-cli to an EXACT pinned version (cycle-3 cross-AI MEDIUM #6 — `@2` major range is not a real pin).** Use this resolution order:

        i. **PREFER lockfile-resolved transitive CLI** (no `npx` install round-trip; matches what `@sentry/vite-plugin` actually used during the build):
           ```bash
           SENTRY_CLI_VERSION=$(npx --no-install @sentry/cli --version 2>/dev/null | awk '{print $NF}')
           ```
           If this prints a version string (e.g., `2.58.5`), record it and use `npx --no-install @sentry/cli <subcommand>` for all subsequent invocations.

        ii. **FALLBACK: explicit exact pin** if the transitive CLI is not resolvable (e.g., `node_modules` not installed locally). First, query `package-lock.json` for the resolved version that CI uses:
           ```bash
           PINNED=$(node -e "const lf=require('./package-lock.json'); const e=Object.entries(lf.packages).find(([k])=>k.endsWith('node_modules/@sentry/cli')); console.log(e ? e[1].version : '')")
           ```
           If `$PINNED` is a version string (e.g., `2.58.5`), use `npx @sentry/cli@${PINNED} <subcommand>`. NEVER use `@latest` and NEVER use a major range like `@2` (cycle-2 cross-AI: CLI-flag changes within 2.x have shipped). Record the exact version literally for the EVIDENCE draft.

        iii. **Sanity-check the env** (cycle-2 Gemini Suggestion 1): run `<sentry-cli-invocation> info` (substituting whichever invocation form from (i) or (ii) resolved) and confirm the printed `Default Organization` and `Default Project` match the intended target (the values from `.sentryclirc` if present, OR the values the operator wants for this Phase). If they don't match, set them via `--org` / `--project` flags in the next steps.

    (c) **Run the sourcemap-upload evidence query — prefer the artifact-bundle / debug-IDs surface (cycle-3 cross-AI MEDIUM #2 — `releases files <release> list` is deprecated in 2.x):**

        i. **PREFERRED: artifact-bundle surface** (current Sentry sourcemap model, organized by debug ID):
           ```bash
           <sentry-cli> sourcemaps list --org <org-slug> --project <project-slug>
           ```
           Confirm the output lists `.js.map` artifacts uploaded for the preview release (filter by release if the CLI version supports `--release <head-SHA>`; otherwise visually scan for entries whose Debug ID maps to the preview build). If the CLI flags differ across versions, check `<sentry-cli> sourcemaps list --help` for the exact form.

        ii. **FALLBACK: deprecated release-files surface** ONLY if (i) returns empty OR the installed CLI does not support `sourcemaps list`:
           ```bash
           <sentry-cli> releases files "<head-SHA>" list
           ```
           If using this fallback, the EVIDENCE draft MUST record both: (a) the deprecation note (`releases files` is deprecated in `@sentry/cli` 2.x in favor of `sourcemaps list`), and (b) why the artifact-bundle surface was unavailable (e.g., installed CLI version predates `sourcemaps list`, or returned empty for a known reason). This justifies the legacy use.

        iii. **Disambiguation tree if BOTH surfaces return empty:**
           1. Re-verify Task 2 passed with `--scope builds` (Netlify deploy-preview context AND `builds` scope have `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`). If Task 2 was skipped or used the cycle-2 form without `--scope builds`, STOP and re-run Task 2.
           2. Retry with explicit `--org <slug>` and `--project <slug>` flags. (Get slugs from Sentry URL: `sentry.io/organizations/<org-slug>/projects/<project-slug>/`.)
           3. If still empty, capture the FULL CLI output (stdout + stderr) verbatim into a fenced code block in `15-EVIDENCE-DRAFT.md` and treat OBSV-04(b) as a BLOCKER for Phase 15 issue closure.

    (d) Confirm the output lists `.js.map` files (artifact-bundle surface: look for `.js.map` Debug IDs; release-files surface: look for `.js.map` filenames) for the preview release.

    (e) **Release-consistency check (cycle-2 Codex MEDIUM #5 — retained):** the Sentry events captured in Task 3 carry a `release` field equal to runtime `VITE_COMMIT_SHA`. The Sentry Vite plugin auto-detects the release name during build from CI/git unless overridden. Before treating the sourcemap-list output as authoritative, EXPLICITLY verify the event-release and artifact-release are the same string. Open the Sentry event detail view for one of the events captured in Task 3 (e.g., the OBSV-03 persisted event). In the event detail, find the `release` field value. Confirm it equals `<head-SHA>` from `15-04-preflight.json` LITERALLY (40-char hex). If they differ — even by one character — STOP. Record the verified-equal pair `event.release == artifact.release == head_sha = <40-char-hex>` in chat for the EVIDENCE draft. ALSO record the EXACT pinned CLI version captured in (b)(i) or (b)(ii) for traceability AND the surface used (`sourcemaps list` preferred OR `releases files list` fallback).

    (f) Once the output is non-empty AND the release-consistency check passes, copy the raw CLI output verbatim. Paste it into chat. Plan 05 will paste it verbatim into the final EVIDENCE.md inside a fenced code block.

    Step 2 — TEST-14/15/16 CI PASS lines:
    (a) Visit the CI run URL captured in `15-04-preflight.json` (latest green run on the PR branch).
    (b) Navigate to the `e2e` job logs OR download the Playwright HTML reporter artifact if uploaded.
    (c) For each of the three target specs, locate the PASS line in the `--reporter list` output (or the equivalent line in the HTML reporter):
       - `admin-create.spec.ts` — capture PASS line text. Screenshot to `artifacts/ci-test-14-pass.png`.
       - `browse-respond.spec.ts` — capture PASS line text. Screenshot to `artifacts/ci-test-15-pass.png`.
       - `filter-search.spec.ts` — capture PASS line text. Screenshot to `artifacts/ci-test-16-pass.png`.
    (d) Each screenshot should clearly show: the spec filename, the test title, the PASSED status, and the duration. Crop to keep each under ~200 KB.
    (e) Record each PASS line text verbatim in chat output for Task 5.

    Step 3 — Final pre-Task-5 sanity check:
    (a) Verify all 7 PNG files exist under `.planning/phases/15-observability-e2e-verify-close/artifacts/`:
       `ls -la .planning/phases/15-observability-e2e-verify-close/artifacts/*.png | wc -l` should return 7. (The 7 are: `sentry-obsv-03-event.png`, `sentry-obsv-04-stack.png`, `sentry-obsv-05-dedupe.png`, `sentry-obsv-05-counts.png`, `ci-test-14-pass.png`, `ci-test-15-pass.png`, `ci-test-16-pass.png`.)
    (b) Verify each is under ~200 KB:
       `find .planning/phases/15-observability-e2e-verify-close/artifacts/ -name "*.png" -size +200k` should return zero lines.
    (c) Stage the screenshots: `git add .planning/phases/15-observability-e2e-verify-close/artifacts/`. Do NOT commit yet — Plan 05 commits as part of the EVIDENCE.md finalization.
  </how-to-verify>
  <resume-signal>
    Type "evidence captured" and paste the three PASS line texts + the sentry-cli output transcript. If sentry-cli returned empty after the full disambiguation tree, type "blocked: obsv-04b empty after disambiguation" and paste the captured output — do NOT proceed to Task 5.
  </resume-signal>
  <files>.planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-14-pass.png, .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-15-pass.png, .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-16-pass.png</files>
  <action>OPERATOR CHECKPOINT — see <how-to-verify> above for the full step list. PRE-CONDITION: Task 2's resume-signal (`preview-env-confirmed` or `preview-env-remediated: ...`) MUST be present in chat — otherwise re-run Task 2 first. Operator resolves sentry-cli to an EXACT pinned version (prefer `npx --no-install @sentry/cli` from the lockfile; fallback to `npx @sentry/cli@<exact-version>` derived from package-lock.json — NEVER `@latest` and NEVER an unpinned `@2` major), runs the resolved CLI's `info` sanity check, then runs `<cli> sourcemaps list` (preferred — artifact-bundle / debug-IDs surface, the current Sentry sourcemap model) OR `<cli> releases files "<head-SHA>" list` ONLY as fallback with explicit deprecation justification (cycle-3 cross-AI MEDIUM #2 — `releases files list` is deprecated in 2.x). Applies the disambiguation tree if output is empty. Verifies event.release == artifact.release == head_sha (literal 40-char hex match). Pastes the raw output + EXACT version + surface-used + verified-release-string verbatim into chat. Then captures three CI PASS-line screenshots from the latest green CI run on the PR branch.</action>
  <verify>
    <automated>test -f .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-14-pass.png &amp;&amp; test -f .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-15-pass.png &amp;&amp; test -f .planning/phases/15-observability-e2e-verify-close/artifacts/ci-test-16-pass.png</automated>
    <human-check>Operator pastes (1) sentry-cli output showing .js.map artifacts uploaded for the preview release (preferred: `sourcemaps list` output; fallback: `releases files list` with deprecation justification recorded), (2) the EXACT pinned sentry-cli version string (e.g., `2.58.5` — NOT a range; NOT `@latest`) captured from `<cli> --version`, (3) the surface used (`sourcemaps list` or `releases files list` + justification), and (4) the verified-equal triple `event.release == artifact.release == head_sha = <40-char-hex>` proving the artifacts query targets the same release as the runtime events</human-check>
  </verify>
  <done>Three CI PASS-line PNGs exist in artifacts/; sentry-cli output captured into chat showing .js.map artifacts uploaded for the preview release SHA (after disambiguation tree if needed); release-consistency verified (event.release == artifact.release == head_sha); sentry-cli pinned to an EXACT version (lockfile-resolved or explicit pin — never `@latest`, never unpinned major); surface used recorded (artifact-bundle preferred over deprecated release-files surface); Task 2 `--scope builds` pre-condition was confirmed before Task 4 ran; all seven PNGs staged for commit in Plan 05.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Write draft `15-EVIDENCE-DRAFT.md` with all captured evidence anchors</name>
  <files>.planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md</files>
  <read_first>
    - .planning/closure/OBSV-02-bundle-delta.md (analog evidence file — same YAML frontmatter + per-method narrative pattern; 15-PATTERNS.md `.planning/.../15-EVIDENCE.md` section calls this out as the reference shape)
    - .planning/phases/15-observability-e2e-verify-close/15-PATTERNS.md `15-EVIDENCE.md` § Divergence (single-file consolidation, frontmatter contents, screenshot relative paths, per-spec PASS-line capture, freshness rule)
    - .planning/phases/15-observability-e2e-verify-close/15-CONTEXT.md ## Decisions § Evidence + issue closure (D-13/D-14/D-15/D-16)
    - .planning/phases/15-observability-e2e-verify-close/15-04-preflight.json (the pre-flight discovery output from Task 1 — read PR URL, preview URL, CI run URL, head SHA from here, not from chat history)
    - All chat output from Tasks 3 + 4 (event IDs, sentry-cli output, CI PASS lines, screenshot file paths)
  </read_first>
  <action>
    Write `15-EVIDENCE-DRAFT.md` (note: `-DRAFT` suffix — Plan 05 produces the final `15-EVIDENCE.md` after PR merge per D-12 freshness rule).

    Frontmatter (YAML):
    - `phase: 15-observability-e2e-verify-close`
    - `measured: <ISO date>`
    - `preview_sha: <head_sha from 15-04-preflight.json>`
    - `preview_url: <preview_url from 15-04-preflight.json>`
    - `ci_run_url: <ci_run_url from 15-04-preflight.json>` — flagged in the body as PRE-MERGE; Plan 05 replaces with POST-MERGE URL
    - `status: draft-pending-merge`
    - `requirements: [OBSV-03, OBSV-04, OBSV-05, TEST-14, TEST-15, TEST-16]`

    Body sections (per 15-PATTERNS.md per-requirement table pattern):

    ## OBSV-03 — Sentry React 19 ErrorBoundary render-phase capture
    - Trigger: `<preview-url>/__smoke?fire=render`
    - Dataset event ID (from `document.body.dataset.sentryEventId`): `<dataset-id from Task 3 Step 1c>`
    - Persisted event ID (the one findable in Sentry dashboard): `<persisted-id from Task 3 Step 1e>`
    - Dataset↔persisted match: `<yes if same; no if dedup'd — see Task 3 Step 1e>`
    - Tag verified: `boundary: app-root` (Plan 01 `beforeCapture` guarantees this regardless of which capture survived Dedupe)
    - Screenshot: `./artifacts/sentry-obsv-03-event.png`
    - Closes GitHub #17 (auto-close on merge)

    ## OBSV-04 — Vite/Rolldown sourcemap function-name preservation
    ### (a) Build-time function-name allowlist gate
    - CI evidence: `lint-and-unit` job step `Verify sourcemap function names preserved` PASS in run `<ci_run_url from preflight JSON>`
    ### (b) Sentry Artifacts API confirms upload
    - Pre-flight: Netlify deploy-preview context AND `builds` scope confirmed to have `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT` (Task 2, `--scope builds` filter)
    - sentry-cli EXACT pinned version (NEVER `@latest`, NEVER unpinned major): `<exact version string from Task 4 Step 1.b>` (e.g., `2.58.5`)
    - sentry-cli resolution source: `<lockfile-resolved transitive via npx --no-install` OR `explicit pin via npx @sentry/cli@<exact>>` (Task 4 Step 1.b.i or 1.b.ii)
    - Sourcemap-upload evidence surface used: `<sourcemaps list (preferred — artifact-bundle / debug-IDs surface)` OR `releases files list (fallback — deprecated in 2.x; justification: <why artifact-bundle surface was unavailable>>` (Task 4 Step 1.c)
    - Release-consistency verified: `event.release == artifact.release == head_sha = <40-char-hex>` (Task 4 Step 1.e — required before treating artifacts query as authoritative)
    - sentry-cli output (verbatim, fenced code block):
      ```
      <paste raw output from Task 4 Step 1.c here>
      ```
    ### (c) Real function names in stack frames
    - Same persisted event ID as OBSV-03 (`<persisted-id>`)
    - Names visible in stack: `<list from Task 3 Step 2>`
    - Screenshot: `./artifacts/sentry-obsv-04-stack.png`
    - Closes GitHub #19 (auto-close on merge)

    ## OBSV-05 — Dedupe integration triple-handler collapse with distinct messages
    - Trigger A: `<preview-url>/__smoke?fire=render` → dataset event ID `<dataset-id-A>` / persisted event ID `<persisted-id-A>` (message: `OBSV-03 render`)
    - Trigger B: `<preview-url>/__smoke?fire=dedupe` → dataset event ID `<dataset-id-B>` / persisted event ID `<persisted-id-B>` (message: `OBSV-05 dedupe`)
    - Distinct events verified (Task 3 resume-signal): `<persisted-id-A> != <persisted-id-B>` (Dedupe did not collapse because messages differ)
    - Both persisted events carry `boundary: app-root` tag (Plan 01 `beforeCapture` invariant)
    - Per-EVENT count verified via Discover / Event Explorer (NOT Issues search — Issues only proves issue-group distinctness, cycle-3 cross-AI MEDIUM #4): query `release:<head-sha> message:"OBSV-03 render"` time-window-bounded ±5min returns EXACTLY 1 event; query `release:<head-sha> message:"OBSV-05 dedupe"` same window returns EXACTLY 1 event. This proves the triple-handler path (Sentry browser global handler + React 19 root `onCaughtError` + local `Sentry.ErrorBoundary`) collapsed to exactly one persisted event per scenario.
    - Surface used for count query: `<Discover (preferred)` OR `per-issue Events tab (fallback)` OR `<cli> events list (CLI fallback)>` (Task 3 Step 3 final action)
    - Screenshot 1: `./artifacts/sentry-obsv-05-dedupe.png` (shows both persisted events in Sentry issue list)
    - Screenshot 2: `./artifacts/sentry-obsv-05-counts.png` (shows the two per-event queries each returning exactly 1 event in the time window — Discover/Event-Explorer surface preferred)
    - (OBSV-05 does not map 1:1 to a separate GitHub issue — it's implicit in #17 closure.)

    ## TEST-14 — admin-create.spec.ts passes in CI on `main`
    - CI run URL: `<ci_run_url>` (PRE-MERGE — Plan 05 replaces with post-merge URL)
    - PASS line (verbatim): `<text from Task 4 Step 2a>`
    - Screenshot: `./artifacts/ci-test-14-pass.png`
    - Closes GitHub #11 (auto-close on merge)

    ## TEST-15 — browse-respond.spec.ts passes in CI on `main`
    - CI run URL: `<ci_run_url>`
    - PASS line: `<text from Task 4 Step 2a>`
    - Screenshot: `./artifacts/ci-test-15-pass.png`
    - Closes GitHub #12 (auto-close on merge)

    ## TEST-16 — filter-search.spec.ts passes in CI on `main`
    - CI run URL: `<ci_run_url>`
    - PASS line: `<text from Task 4 Step 2a>`
    - Screenshot: `./artifacts/ci-test-16-pass.png`
    - Closes GitHub #13 (auto-close on merge)

    ## Closure plan
    - Phase 15 PR body uses `Closes #11, #12, #13, #17, #19` auto-close keywords (verified in Plan 04 Task 1)
    - Post-merge: operator runs `gh issue comment <N> --body "..."` on each of the 5 issues, linking to this EVIDENCE file at the anchor for that requirement. Plan 05 covers the manual operator step.
    - Freshness: Plan 05 re-captures the CI run URL from the post-merge `main` run per D-12 before finalizing.

    Comment style outside src/: PERMITTED to cite D-13/D-14/D-15/D-16 in the body or in commit messages. The EVIDENCE file is `.planning/`, not `src/`.

    Do NOT commit yet — Plan 05 batches the commit of the artifacts + the renamed `15-EVIDENCE.md` together.
  </action>
  <acceptance_criteria>
    - `.planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md` exists
    - Frontmatter contains `phase`, `measured`, `preview_sha`, `preview_url`, `ci_run_url`, `status: draft-pending-merge`, `requirements` (all six IDs)
    - Body contains one section per requirement (OBSV-03, OBSV-04, OBSV-05, TEST-14, TEST-15, TEST-16) — grep: `grep -cE "^## (OBSV-0[345]|TEST-1[456])" 15-EVIDENCE-DRAFT.md` returns 6
    - Each section references its corresponding `./artifacts/*.png` file via relative markdown link
    - The sentry-cli output is pasted verbatim inside a fenced code block
    - The sentry-cli EXACT pinned version is recorded literally in the OBSV-04(b) section (cycle-3 cross-AI MEDIUM #6 — must NOT be `@latest`, must NOT be an unpinned major range like `@2`)
    - The sentry-cli resolution source is recorded: either `lockfile-resolved transitive via npx --no-install` (preferred) OR `explicit pin via npx @sentry/cli@<exact>`
    - The sourcemap-upload surface used is recorded: `sourcemaps list` (preferred — artifact-bundle / debug-IDs) OR `releases files list` (fallback, with explicit deprecation justification — cycle-3 cross-AI MEDIUM #2)
    - The release-consistency triple `event.release == artifact.release == head_sha = <40-char-hex>` is recorded literally in the OBSV-04(b) section
    - Both OBSV-03 and OBSV-05 sections record BOTH the dataset event ID AND the persisted event ID, plus the dataset↔persisted match status (`yes` if the dataset ID equals the persisted ID; `no` if the dataset capture was dedup'd and the persisted ID is the root-handler auto-capture). The cycle-3 HIGH fix #2 (dashboard `event_id == dataset ID` cross-check) — required by Task 3.
    - The two PERSISTED event IDs (OBSV-03 and OBSV-05) MUST differ literally
    - Both persisted events recorded as carrying the `boundary: app-root` tag (Plan 01 `beforeCapture` invariant — cycle-3 HIGH fix #1)
    - The OBSV-05 section includes both `sentry-obsv-05-dedupe.png` AND `sentry-obsv-05-counts.png` references and notes the per-EVENT count = 1 verified via Discover / Event Explorer (NOT Issues search — cycle-3 cross-AI MEDIUM #4). The surface used (`Discover` / `per-issue Events tab` / `<cli> events list`) is recorded literally.
    - All five GitHub issue numbers (#11, #12, #13, #17, #19) appear in the file
    - The OBSV-04(b) section explicitly notes the Task 2 pre-flight with `--scope builds` filter (Netlify env-var presence in deploy-preview context AND `builds` scope) was confirmed before sentry-cli was invoked
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md &amp;&amp; grep -cE "^## (OBSV-0[345]|TEST-1[456])" .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md | grep -E "^6$"</automated>
  </verify>
  <done>
    Draft EVIDENCE file exists with all six per-requirement sections filled in from Task-3/4 captures. Frontmatter status is `draft-pending-merge`. OBSV-04(b) section records the Task 2 pre-flight pass. The file is ready for Plan 05 to finalize (rename + post-merge refresh + commit).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's local shell → Sentry API via `sentry-cli` | Operator's personal `SENTRY_AUTH_TOKEN` used; not committed; not in CI. Scope limited to `project:releases` (read-only). |
| Operator's local shell → Netlify API via `netlify env:list --context deploy-preview` | Operator-credentialed Netlify CLI session; the list call returns env-var keys + presence flag, not values. Setting any missing var (`netlify env:set`) uses the operator's session credentials. |
| Browser → deploy preview `/__smoke` route | Preview is publicly URL-discoverable (no auth); the route fires a render-phase throw on demand. The route is prod-gated, so this surface exists ONLY in preview/dev contexts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information Disclosure | `SENTRY_AUTH_TOKEN` leaked in chat output | mitigate | Operator pastes only the `sentry-cli` output (file listing), NEVER the token itself. Token is sourced from local env only; never echoed. The `sentry-cli` invocation prints releases-files JSON, not the token. Task 2's `netlify env:list` similarly shows key names + presence flags, not values. |
| T-15-08 | Repudiation | Operator could fabricate screenshots | accept | Screenshots are anchored to a SHA-specific Sentry release and a SHA-specific CI run URL. A fabricated PNG would not match the Sentry event ID that any future reader could re-query against the release. |
| T-15-09 | Information Disclosure | Committing screenshots of internal Sentry dashboard could leak internal URLs/usernames | accept | The project Sentry org is the dev's own; screenshots show the Sentry hostname (e.g., `sentry.io/organizations/<org>/...`) which is not sensitive; no PII. Confirmed acceptable in CONTEXT.md D-15 (operator chose committed PNGs over external links). |

No new package installs in this plan (`@sentry/cli` invoked via `npx`, no commit to `package.json`); package-supply-chain threat T-15-SC does not apply to a one-shot `npx` invocation of an [APPROVED] Sentry CLI per RESEARCH.md Package Legitimacy Audit.
</threat_model>

<verification>
- `15-04-preflight.json` exists with all four discovered values + `ci_conclusion: success`
- Netlify deploy-preview context AND `builds` scope confirmed to carry all three Sentry env vars before sentry-cli is invoked (cycle-3 cross-AI MEDIUM #5 — `--scope builds` filter)
- 7 PNG files exist under `artifacts/` (4 Sentry: event, stack, dedupe-list, per-message-counts + 3 CI), each under ~200 KB
- `15-EVIDENCE-DRAFT.md` has six per-requirement sections + frontmatter
- Two distinct PERSISTED Sentry event IDs are recorded; dataset↔persisted match status recorded for each (cycle-3 cross-AI HIGH fix #2 — dashboard `event_id == dataset ID` cross-check)
- Both persisted events confirmed to carry `boundary: app-root` tag (cycle-3 cross-AI HIGH fix #1 — Plan 01 `beforeCapture` invariant)
- Per-EVENT count = 1 via Discover / Event Explorer (NOT Issues search; cycle-3 cross-AI MEDIUM #4)
- sentry-cli EXACT pinned version recorded; sourcemap-upload surface used recorded (artifact-bundle `sourcemaps list` preferred over deprecated `releases files list`)
- sentry-cli output is in chat AND in the draft file
- Pre-merge CI run URL is in the draft file (Plan 05 replaces with post-merge URL)
</verification>

<success_criteria>
1. Pre-flight JSON captured all four discovery values (PR, preview, CI, head SHA) and CI is green at terminal state
2. Netlify deploy-preview context AND `builds` scope env-var presence confirmed for `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT` (cycle-3 cross-AI MEDIUM #5)
3. OBSV-03 PERSISTED event captured with `boundary: app-root` tag (screenshot + dataset event ID + persisted event ID + match status — cycle-3 HIGH fix paths #1 and #2)
4. OBSV-04 (b) sentry-cli output captures `.js.map` artifacts for the release via the artifact-bundle surface (`sourcemaps list`) or deprecated-with-justification fallback; EXACT pinned CLI version recorded
5. OBSV-04 (c) Sentry stack frame shows at least one allowlisted real function name
6. OBSV-05 captures TWO distinct PERSISTED events with distinct messages — Dedupe contract holds; per-EVENT count = 1 verified via Discover / Event Explorer (cycle-3 MEDIUM #4)
7. TEST-14/15/16 PASS lines captured from latest green CI run on the PR branch
8. Draft EVIDENCE file lists every requirement → evidence anchor mapping
</success_criteria>

<output>
Create `.planning/phases/15-observability-e2e-verify-close/15-04-SUMMARY.md` when done. Include: the two PERSISTED Sentry event IDs (plus dataset IDs + match status), the PR URL, the pre-merge CI run URL, the head SHA, the EXACT pinned sentry-cli version + surface used (artifact-bundle preferred), confirmation that all 7 PNGs are staged (`git status` shows them untracked or staged), and a note that the Netlify deploy-preview env-var pre-flight (Task 2 with `--scope builds` filter) passed.
</output>
