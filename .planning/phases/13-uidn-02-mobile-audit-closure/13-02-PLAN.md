---
phase: 13-uidn-02-mobile-audit-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .planning/closure/UIDN-02-mobile-evidence.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/closure/artifacts/MANIFEST.json
autonomous: false
requirements:
  - UIDN-02

must_haves:
  truths:
    - "D-11: Strict Performance ≥ 90 floor — no D-14 ship-anyway analog applied this phase; even a single route at 89 is a MISS (D-26 acknowledges 89 as 'technical miss within Lighthouse ±5-10pp noise band' in evidence wording)"
    - "D-12: Miss-branch follow-up trigger is 'next perf-budget change lands' (concrete trigger, not v1.3 calendar); appears in BOTH evidence Sign-off paragraph AND REQUIREMENTS.md UIDN-02 row"
    - "D-13: audit-mobile.sh runs exactly once (Pitfall 1 single-run policy) — no rerun-for-better-numbers; numbers from first and only run are accepted"
    - "D-14: Two-surface durability — follow-up trigger lives in BOTH evidence file Sign-off AND REQUIREMENTS.md UIDN-02 active row; no new PERF-FOLLOWUP-01 requirement created"
    - "D-15: Appends '## v1.2 Rerun (2026-05-XX)' section to existing UIDN-02-mobile-evidence.md (vs new file, vs split with PROJECT.md stat block); v1.1 baseline content unchanged"
    - "D-16: UIDN-02-mobile-evidence.md frontmatter status flips to 'complete-v1.2' (pass) or 'deferred-v1.2' (miss) — milestone-tagged"
    - "D-17: PROJECT.md line 211 Mobile-first responsive design row uses exact Phase 9 wording convention with v1.2 tag and inline evidence-file path"
    - "D-18: v1.2 Rerun section adds ONLY NEW cross-refs (Phase 13 CONTEXT/PLAN, harness diff, Phase 12 prod commit de15e33); v1.1 baseline cross-refs inherited above, not duplicated"
    - "D-20: MISS branch wording covers ANY threshold failure (Perf <90, A11y <95, BP <95, SEO <90) — audit-mobile.sh:54-58 already fails non-Perf categories; outcome wording uses 'N/5 routes under threshold' generically and names the failing category per route in the score table"
    - "D-21: All plan path references are repo-relative — no /Users/... absolute paths"
    - "D-22: Plan 13-02 <verify> inspects existing .report.json files with jq — NEVER re-invokes audit-mobile.sh (would violate D-13 single-run policy)"
    - "D-24: 13-01-SUMMARY.md and 13-02-SUMMARY.md are NOT in the atomic commit files_modified — they ship in a separate optional docs(13) commit; this plan's atomic commit covers the 5 closure artifacts only"
    - "D-25: No 'GitHub PR #XX' placeholder in committed text — the PR reference is added post-merge in a follow-up commit, not at closure-commit time"
    - "Lighthouse mobile audit completes for all 5 routes (`/`, `/topics`, `/archive`, `/auth/error`, `/admin`) and exits 0 (pass) or non-zero (miss) — run exactly once per D-13"
    - "REQUIREMENTS.md Phase Traceability line 74 flips from Pending to Complete (pass) or Active (miss) per outcome"
    - "All three documents (evidence, PROJECT.md, REQUIREMENTS.md) agree on the same pass/miss outcome (no mixed signals)"
  artifacts:
    - path: ".planning/closure/UIDN-02-mobile-evidence.md"
      provides: "v1.2 rerun evidence section appended (D-15)"
      contains: "v1.2 Rerun"
    - path: ".planning/PROJECT.md"
      provides: "Mobile-first Key Decision row updated (D-17)"
      contains: "v1.2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UIDN-02 row + Phase Traceability row updated (D-14)"
      contains: "v1.2 rerun"
    - path: ".planning/closure/artifacts/lighthouse/"
      provides: "5 Lighthouse HTML + JSON reports (gitignored)"
      min_files: 10
    - path: ".planning/closure/artifacts/MANIFEST.json"
      provides: "Lighthouse entries added (10 new entries)"
      contains: "lighthouse"
  key_links:
    - from: "UIDN-02-mobile-evidence.md § v1.2 Rerun"
      to: ".planning/PROJECT.md line 211"
      via: "same pass/miss outcome reflected in both documents"
      pattern: "v1.2"
    - from: "UIDN-02-mobile-evidence.md frontmatter status"
      to: ".planning/REQUIREMENTS.md UIDN-02 row"
      via: "consistent disposition: complete-v1.2 / deferred-v1.2"
      pattern: "v1.2"
    - from: "bash .planning/closure/audit-mobile.sh"
      to: "https://polls.wtcsmapban.com"
      via: "npx lighthouse@13.2.0 --form-factor=mobile --throttling-method=simulate"
      pattern: "Lighthouse Perf"
---

<objective>
Run the Lighthouse mobile audit (unchanged `audit-mobile.sh`), read the numeric scores, and update all three evidence/documentation artifacts with the outcome — applying the correct pass or miss wording branch.

Purpose: Phase 13 closes UIDN-02 by completing the audit and recording its results. The harness fix (Plan 13-01) removed the defect that caused loading-shell captures. The Lighthouse audit runs once (D-13 single-run policy). The score table drives two conditional outcome paths: if Perf ≥ 90 on all 5 routes (D-11 strict floor), the Key Decision row flips ⚠️ → ✓; otherwise the row stays ⚠️ with the follow-up trigger tied to the next perf-budget change (D-12).

Output: Lighthouse JSON/HTML reports in artifacts/; UIDN-02-mobile-evidence.md with v1.2 Rerun section appended; PROJECT.md Key Decision row updated; REQUIREMENTS.md UIDN-02 row + Traceability row updated.
</objective>

<execution_context>
@.planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md
@.planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md
@.planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md
@.planning/closure/UIDN-02-mobile-evidence.md
@.planning/closure/OBSV-02-bundle-delta.md

<interfaces>
<!-- Key identifiers and wording branches the executor needs. No codebase exploration required. -->

LIGHTHOUSE AUDIT COMMAND (unchanged per D-13):
  bash .planning/closure/audit-mobile.sh
  Runs: npx -y lighthouse@13.2.0 --form-factor=mobile --throttling-method=simulate --only-categories=performance,accessibility,best-practices,seo --chrome-flags="--headless=new --no-sandbox"
  Routes: / /topics /archive /auth/error /admin
  Thresholds: THRESHOLD_PERF=90, THRESHOLD_A11Y=95, THRESHOLD_BP=95, THRESHOLD_SEO=90
  Output: .planning/closure/artifacts/lighthouse/lh-mobile-{name}.report.{html,json}

OUTCOME-CONDITIONAL WORDING (D-11, D-14, D-16, D-17, D-20, D-26):

  OUTCOME DETERMINATION (D-11 + D-20): The audit produces 5 routes × 4 categories of scores. A route PASSES only if ALL FOUR thresholds are met (Perf ≥ 90, A11y ≥ 95, BP ≥ 95, SEO ≥ 90 — matches audit-mobile.sh:54-58). The MISS branch fires if ANY route × category combination is below threshold. Track two counts:
    - failingRoutes = number of routes with at least one category below threshold
    - failingCategoriesByRoute = per-route list of which categories failed (e.g., "/topics: Perf=89, A11y=100, BP=100, SEO=92 → Perf miss")
  The aggregate label uses "N/5 routes under threshold" (not "N/5 routes under 90") so the same wording covers any category mix.

  IF ALL 5 ROUTES PASS ALL 4 THRESHOLDS (PASS):

    UIDN-02-mobile-evidence.md frontmatter (replace status: line only):
      status: complete-v1.2

    PROJECT.md line 211 — third column of the "Mobile-first responsive design" row (replace only the cell content):
      ✓ Good (v1.2 — Lighthouse Perf 5/5 ≥ 90; see .planning/closure/UIDN-02-mobile-evidence.md § v1.2 Rerun)

    REQUIREMENTS.md UIDN-02 row — flip [ ] to [x] and append outcome note after the existing text:
      *(Plan 13-01 + 13-02, 2026-05-XX — v1.2 rerun: all 5 routes Perf ≥ 90; UIDN-02 closed.)*

    REQUIREMENTS.md Phase Traceability line 74:
      | UIDN-02 | Phase 13 | Complete (Plan 13-02, 2026-05-XX) |

    Evidence file v1.2 Rerun sign-off disposition line:
      _Disposition: PASS — UIDN-02 closed; row flipped ⚠️ → ✓_

  IF ANY ROUTE × CATEGORY UNDER THRESHOLD (MISS — D-11 strict floor + D-20 multi-category):

    UIDN-02-mobile-evidence.md frontmatter (replace status: line only):
      status: deferred-v1.2 — N/5 routes under threshold; follow-up tied to next perf-budget change

    PROJECT.md line 211 — third column (replace only the cell content):
      ⚠️ Revisit (v1.2 rerun — N/5 routes under threshold; follow-up tied to next perf-budget change; see .planning/closure/UIDN-02-mobile-evidence.md § v1.2 Rerun)
    (substitute actual failingRoutes count for N — generic "under threshold" wording covers Perf, A11y, BP, and SEO failures)

    REQUIREMENTS.md UIDN-02 row — [ ] stays unchecked; append outcome note after the existing text:
      *(Phase 13 v1.2 rerun complete 2026-05-XX — N/5 routes under threshold; follow-up: next perf-budget change. See .planning/closure/UIDN-02-mobile-evidence.md § v1.2 Rerun.)*

    REQUIREMENTS.md Phase Traceability line 74:
      | UIDN-02 | Phase 13 | Active (Phase 13 v1.2 rerun complete; pending next perf-budget change) |

    Evidence file v1.2 Rerun sign-off disposition line:
      _Disposition: DEFER — row stays ⚠️ Revisit; follow-up tied to next perf-budget change_

    Evidence file score table additions (D-20 + D-26):
      - The Lighthouse mobile scores table MUST name the specific failing category per route in the rightmost column or a "Status" cell (e.g., "FAIL Perf=89"). Aggregate failingRoutes is meaningless without per-route diagnosis.
      - If ANY route lands at Perf=89 exactly, the score table caption (or footnote) adds: "Technical miss within Lighthouse ±5-10pp noise band — follow-up trigger is the next perf-budget change, not a same-deploy rerun (D-13 single-run policy)." Per D-26. Wording is identical regardless of which route shows 89.

EVIDENCE FILE SECTION SHAPE (D-15, D-18):

  New section to append at end of .planning/closure/UIDN-02-mobile-evidence.md (BEFORE the final --- sign-off line is appended):
  Keep all existing content unchanged. Append after line 97 (the final _Disposition_ italics line), separated by two blank lines.

  Section header: ## v1.2 Rerun (2026-05-XX)
  [Replace XX with actual date; 2026-05-XX is the placeholder]

  Sub-sections required:
    ### Harness changes (Phase 13)
    [Document: sentinel fix (D-01..D-04), UNAUTH_ROUTES reduction from 5 to 3 (D-09), member context addition for /topics + /archive (D-07/D-08), sha256 dupe-check (D-05/D-06)]

    ### Lighthouse mobile scores (v1.2 production)
    [Same table shape as the v1.1 section: Route | Perf (≥90) | A11y (≥95) | BP (≥95) | SEO (≥90) | Final URL]
    [Fill in actual numeric scores from the audit run; placeholder: TBD until Lighthouse completes]

    ### Breakpoint matrix (42 PNGs — v1.2)
    [Updated table: 18 unauth (3 routes: /, /auth/error, /admin unauth) + 24 auth (2 admin routes via adminUser + 2 member routes via memberUser via local preview)]
    [Clarify which context captured each route: Pass-A unauth, Pass-B admin context, Pass-B member context]

    ### Cross-references (D-18 — new refs only, not duplicating v1.1 baseline refs)
    - `.planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md` — D-01..D-26
    - `.planning/phases/13-uidn-02-mobile-audit-closure/13-01-PLAN.md` — harness fix implementation
    - `.planning/phases/13-uidn-02-mobile-audit-closure/13-02-PLAN.md` — Lighthouse rerun + evidence update
    - `.planning/phases/13-uidn-02-mobile-audit-closure/13-REVIEWS.md` — cross-AI review (gemini + codex) feedback incorporated
    - Phase 12 prod commit `de15e33` — v1.2 deploy audited against
    (D-25: Phase 13 PR reference is intentionally OMITTED at closure-commit time — it is not knowable until push; operator may add it in a separate post-merge documentation commit. No placeholder text such as "PR #XX" is committed.)

    --- (horizontal rule)
    _Audited: 2026-05-XX against https://polls.wtcsmapban.com (Lighthouse 13.2.0 + Playwright 1.59.1)_
    _Method: 5-route Lighthouse mobile audit + 6-width × 42-PNG matrix (18 unauth-prod + 24 auth-local); harness at `.planning/closure/audit-{mobile.sh,screenshots.mjs}`_
    _Disposition: [PASS or DEFER wording from outcome-conditional table above]_

EVIDENCE FILE FRONTMATTER: Replace only the status: line (line 10). All other frontmatter fields (requirement, audited, audit_url, targets, phase, plan) remain unchanged. Add/update the audited date if desired (optional — keep v1.1 date or update to today).

PROJECT.MD EDIT: Edit only the third column of the table row at line 211. Do not change the first two columns ("Mobile-first responsive design" | "Discord users tap links from phones"). Preserve the Markdown table pipe characters and spacing.

REQUIREMENTS.MD EDIT 1 (UIDN-02 active row, approximately line 31): The existing text block ends with "_Carry-forward from v1.1; baseline at `.planning/closure/UIDN-02-mobile-evidence.md` (status: deferred)._". Append the outcome note (from the conditional table above) after this line, as a new line within the list item.

REQUIREMENTS.MD EDIT 2 (Phase Traceability, approximately line 74): Replace only the Status column value: "Pending" → appropriate outcome text from the conditional table.

V1.1 BASELINE SCORES (for comparison in the evidence file):
  / : Perf=82, A11y=100, BP=100, SEO=92
  /topics : Perf=88, A11y=100, BP=100, SEO=92
  /archive : Perf=86, A11y=100, BP=100, SEO=92
  /auth/error : Perf=85, A11y=100, BP=100, SEO=92
  /admin : Perf=86, A11y=100, BP=100, SEO=92
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Lighthouse mobile audit (single-run — D-13)</name>
  <read_first>
    - .planning/closure/audit-mobile.sh (UNCHANGED for Phase 13 — verify before running; threshold logic at lines 54-58 already enforces D-20 multi-category)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md (D-11 strict floor, D-13 single-run policy, D-20 multi-category MISS branch, D-22 verify must inspect reports never re-invoke the script, D-26 technical-miss-at-89 wording)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md (Q4: audit-mobile.sh is UNCHANGED; Q7: single-run policy honored)
  </read_first>
  <files>.planning/closure/artifacts/lighthouse/</files>
  <action>
    Run the Lighthouse audit EXACTLY ONCE (D-13 single-run policy — no rerun for better numbers). The verify block of THIS task MUST NOT re-invoke audit-mobile.sh per D-22 — it inspects the .report.json files produced by this single action run.

    1. Verify `audit-mobile.sh` is UNCHANGED before running: confirm it still targets `https://polls.wtcsmapban.com` and the same 5-route set (`/`, `/topics`, `/archive`, `/auth/error`, `/admin`) with `THRESHOLD_PERF=90 THRESHOLD_A11Y=95 THRESHOLD_BP=95 THRESHOLD_SEO=90` (audit-mobile.sh:20-23).

    2. Run, capturing stdout to a log so verify can inspect what was printed without re-running:
       `bash .planning/closure/audit-mobile.sh 2>&1 | tee .planning/closure/artifacts/lighthouse/audit-mobile.stdout.log; echo "exit=${PIPESTATUS[0]}" >> .planning/closure/artifacts/lighthouse/audit-mobile.stdout.log`
       (The `tee` + `PIPESTATUS[0]` pattern preserves both the stdout AND the script's true exit code despite the pipe — the appended `exit=N` line is the canonical record.)

    The script will:
    - Run `npx -y lighthouse@13.2.0` with `--form-factor=mobile --throttling-method=simulate --only-categories=performance,accessibility,best-practices,seo --chrome-flags="--headless=new --no-sandbox"` for each of the 5 routes
    - Extract scores via `jq` and compare against thresholds
    - Exit 0 if all routes pass all 4 thresholds; exit 1 if any route × category is below threshold (audit-mobile.sh:54-58 — Perf, A11y, BP, OR SEO miss all fail the route)

    3. Record the exit code and the per-route score table from the saved log. The summary table contains all 5 routes × 4 categories. Copy these scores exactly — they are the v1.2 audit record. Do NOT round, adjust, or re-run.

    4. Determine the outcome: if exit code = 0, outcome = PASS. If exit code != 0, outcome = MISS. Apply D-11 strictly — no D-14 ship-anyway analog. Per D-20, the MISS branch fires on any category failure, not just Perf. Per D-26, a 89 Perf is still a MISS but the evidence wording adds the "technical miss within Lighthouse ±5-10pp noise band" note.

    5. Note the exact date and time of the run (ISO format: 2026-05-XX). This goes into the evidence file Audited line.

    6. For each of the 5 routes, identify which category(ies) failed (if any) by reading the saved log's score lines. Build a structured record like:
       {"home":{"perf":92,"a11y":100,"bp":100,"seo":92,"failingCategories":[]},
        "topics":{"perf":89,"a11y":100,"bp":100,"seo":92,"failingCategories":["perf"]},
        ...}
       Pass this record to Task 2 so the evidence file's score table can name each failing category per route.

    Anti-patterns (D-13 + D-22):
    - Do NOT run audit-mobile.sh twice anywhere in this task or its verify block.
    - Do NOT cherry-pick the better run.
    - Accept the numbers from the first (and only) run.
    - The verify block below inspects the produced .report.json files — it does NOT re-invoke the audit script.
  </action>
  <verify>
    <automated>ls .planning/closure/artifacts/lighthouse/lh-mobile-*.report.json | wc -l</automated>
    Expected: exactly 5 (one per route: home, topics, archive, auth-error, admin). Verifies the action run produced all required reports without re-running.
    <automated>ls .planning/closure/artifacts/lighthouse/lh-mobile-*.report.html | wc -l</automated>
    Expected: exactly 5 (one HTML per route).
    <automated>test -f .planning/closure/artifacts/lighthouse/audit-mobile.stdout.log && tail -3 .planning/closure/artifacts/lighthouse/audit-mobile.stdout.log</automated>
    Expected: log file exists and the final line is `exit=0` or `exit=1` (the canonical exit-code record from the action run's tee+PIPESTATUS capture).
    <automated>for f in .planning/closure/artifacts/lighthouse/lh-mobile-*.report.json; do n=$(basename "$f" .report.json | sed 's/lh-mobile-//'); jq -r --arg n "$n" '"\($n): perf=" + (.categories.performance.score * 100 | floor | tostring) + " a11y=" + (.categories.accessibility.score * 100 | floor | tostring) + " bp=" + (.categories."best-practices".score * 100 | floor | tostring) + " seo=" + (.categories.seo.score * 100 | floor | tostring)' "$f"; done</automated>
    Expected: 5 lines, one per route, each containing four `category=NN` pairs. The output is the canonical per-route score record extracted from the audit run's JSON outputs (D-22: NEVER re-invokes audit-mobile.sh — inspects existing reports only).
    <automated>jq 'select(.categories.performance.score * 100 | floor < 90 or .categories.accessibility.score * 100 | floor < 95 or .categories."best-practices".score * 100 | floor < 95 or .categories.seo.score * 100 | floor < 90) | input_filename' .planning/closure/artifacts/lighthouse/lh-mobile-*.report.json | wc -l</automated>
    Expected: the count of files failing at least one threshold per D-20. 0 = PASS, ≥1 = MISS. Cross-check against the `exit=` line in the stdout log; if they disagree, the audit run was somehow non-deterministic — abort and rerun ONCE from a clean ARTIFACTS_DIR, treating that as the single canonical run.
  </verify>
  <acceptance_criteria>
    - `audit-mobile.sh` was invoked exactly once in this task (D-13 single-run policy — the script appears in the action block but NOT in the verify block per D-22)
    - `.planning/closure/artifacts/lighthouse/audit-mobile.stdout.log` exists and its last line matches `exit=0` or `exit=1`
    - `ls .planning/closure/artifacts/lighthouse/lh-mobile-*.report.json | wc -l` returns 5 (one per route)
    - `ls .planning/closure/artifacts/lighthouse/lh-mobile-*.report.html | wc -l` returns 5 (one HTML per route)
    - Each .report.json file is readable by jq and yields four numeric category scores (Perf, A11y, BP, SEO)
    - Executor has recorded the exact numeric Perf/A11y/BP/SEO values for each of the 5 routes (passed to Task 2)
    - Executor has recorded per-route failingCategories list (empty if route passed all 4 thresholds) per D-20
    - Outcome determination is recorded: PASS (all 20 route×category cells meet threshold) or MISS (≥1 cell under threshold)
    - The verify block does NOT contain `bash .planning/closure/audit-mobile.sh` anywhere — only jq/ls inspections of existing report files (D-22 compliance)
  </acceptance_criteria>
  <done>audit-mobile.sh ran exactly once; 5 .report.json + 5 .report.html files exist in artifacts/lighthouse/; per-route scores AND failingCategories recorded in structured form; outcome determined (PASS or MISS per D-20 multi-category logic); no rerun attempted; verify block confirmed all artifacts via jq/ls inspection without re-invoking the script.</done>
</task>

<task type="auto">
  <name>Task 2: Update evidence file, PROJECT.md, and REQUIREMENTS.md with outcome-conditional wording (D-20 multi-category, D-25 no PR placeholder, D-26 technical-miss)</name>
  <read_first>
    - .planning/closure/UIDN-02-mobile-evidence.md (append target — read all to understand structure)
    - .planning/PROJECT.md (line 211 — Mobile-first row to update per D-17)
    - .planning/REQUIREMENTS.md (lines 31 and 74 — UIDN-02 row + Traceability row per D-14)
    - .planning/closure/OBSV-02-bundle-delta.md (sign-off italics shape — last 3 lines)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md (D-15 through D-26 wording requirements; D-20 multi-category MISS, D-25 no PR placeholder, D-26 technical-miss-at-89 note)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md (Q5 evidence file structure, Q6 PROJECT.md/REQUIREMENTS.md edit precision)
    - .planning/closure/artifacts/lighthouse/audit-mobile.stdout.log (Task 1's tee'd output containing the score table and `exit=` line)
    - .planning/closure/artifacts/lighthouse/lh-mobile-*.report.json (raw per-category scores; jq-extractable as in Task 1 verify)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-01-SUMMARY.md (F6 warning count from harness run — for "N DOM-assertion warnings" in evidence section if applicable; OPTIONAL — may not exist yet if /gsd-execute-phase has not generated it)
  </read_first>
  <files>
    .planning/closure/UIDN-02-mobile-evidence.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/closure/artifacts/MANIFEST.json
  </files>
  <action>
    Using the Lighthouse scores recorded in Task 1 and the outcome determination (PASS or MISS), apply updates to four files. Use the exact outcome-conditional wording from the `<interfaces>` block of this plan — do not improvise alternative wording.

    STEP 1 — UIDN-02-mobile-evidence.md (D-15, D-16, D-18):

    a. Frontmatter: Replace ONLY the `status:` line (line 10) with the correct branch from the interfaces block. Do not modify any other frontmatter field (requirement, audited, audit_url, targets, phase, plan). If desired, add a second `audited_v1.2:` field with today's date in YYYY-MM-DD format after the existing `audited:` field — but do NOT change the existing `audited: 2026-05-05` line.

    b. Append new section: After the final `_Disposition: UIDN-02 deferred...` line at the end of the file, append the complete `## v1.2 Rerun (2026-05-XX)` section per the interfaces block. Replace XX with today's date. Fill in the actual Lighthouse scores from Task 1 in the table. The v1.1 baseline scores are provided in the interfaces block for comparison — include them in a "(v1.1 baseline)" sub-row or footnote if it aids readability, but do not replace the v1.2 table with them.

    The new section must include:
    - Intro paragraph explaining what changed (sentinel fix, route reduction, member context, sha256 dupe-check; references D-01..D-09)
    - ### Lighthouse mobile scores (v1.2 production) — 5-route table with actual scores
    - ### Breakpoint matrix (42 PNGs — v1.2) — updated matrix showing 3 unauth routes (Pass-A) + 2 admin routes (Pass-B adminUser) + 2 member routes (Pass-B memberUser)
    - ### Harness changes (Phase 13) — bullet list: sentinel (D-03/D-04), route drop (D-09), member context (D-07/D-08), sha256 gate (D-05/D-06)
    - ### Cross-references — only NEW refs per D-18 (Phase 13 CONTEXT.md, 13-01-PLAN.md, 13-02-PLAN.md, Phase 12 prod commit de15e33, GitHub PR #XX)
    - --- (horizontal rule)
    - Three italics sign-off lines per OBSV-02-bundle-delta.md convention

    STEP 2 — PROJECT.md (D-17):

    Find line 211 (the "Mobile-first responsive design" row). Edit ONLY the third column (the outcome/status cell). Replace "⚠️ Revisit (UIDN-02 closure evidence pending — issue #18)" with the correct branch wording from the interfaces block. Preserve the Markdown table pipe characters and column spacing to avoid corrupting the table.

    PASS branch: ✓ Good (v1.2 — Lighthouse Perf 5/5 ≥ 90; see .planning/closure/UIDN-02-mobile-evidence.md § v1.2 Rerun)
    MISS branch (D-20 generic "under threshold" covers any category failure): ⚠️ Revisit (v1.2 rerun — N/5 routes under threshold; follow-up tied to next perf-budget change; see .planning/closure/UIDN-02-mobile-evidence.md § v1.2 Rerun)
    (substitute actual failingRoutes count for N; use "under threshold" not "under 90" so the row reads correctly whether the failure is Perf, A11y, BP, or SEO)

    STEP 2.5 — D-20 multi-category + D-26 technical-miss verifications on the evidence file (DO BEFORE STEP 3):

    Confirm the v1.2 Rerun § Lighthouse mobile scores table:
    - Includes a Status (or rightmost) column naming the specific failing category(ies) per route (e.g., "FAIL Perf=89", "FAIL A11y=94", "PASS"). The aggregate "N/5 routes under threshold" wording elsewhere is meaningless without this per-route diagnosis (D-20).
    - If ANY route's Perf cell shows exactly 89, the section caption (or a footnote immediately below the table) reads exactly: "Technical miss within Lighthouse ±5-10pp noise band — follow-up trigger is the next perf-budget change, not a same-deploy rerun (D-13 single-run policy)." Wording is identical regardless of which route lands at 89 (D-26). Skip this footnote on a PASS outcome or if no route lands at 89.

    Confirm the Cross-references list (D-25):
    - Does NOT contain "GitHub PR #XX", "PR #XX", "PR #TBD", or any placeholder PR reference. The Phase 13 PR ref is added post-merge in a separate documentation commit; closure-commit text omits it entirely.
    - DOES contain `.planning/phases/13-uidn-02-mobile-audit-closure/13-REVIEWS.md` as a new cross-ref (D-18 — Phase 13 cross-AI review record is a new ref for v1.2).

    STEP 3 — REQUIREMENTS.md UIDN-02 row (D-14):

    Find the UIDN-02 list item (approximately line 31). The existing text block ends with the "_Carry-forward from v1.1_" italics line. On PASS: change the leading "- [ ]" to "- [x]". On MISS: leave "- [ ]" unchanged. In both cases: append a new line after the italics carry-forward line with the outcome note from the interfaces block (the `*(Plan 13-01 + 13-02, ...)* `  or `*(Phase 13 v1.2 rerun complete ...) *` line). Replace 2026-05-XX with today's date. Use the D-20 generic "N/5 routes under threshold" wording on MISS — never "under 90" — so the note reads correctly for non-Perf failures.

    STEP 4 — REQUIREMENTS.md Phase Traceability row (D-14):

    Find the UIDN-02 traceability row (approximately line 74): `| UIDN-02 | Phase 13 | Pending |`. Replace "Pending" with the correct branch status from the interfaces block.

    STEP 5 — MANIFEST.json update:

    The MANIFEST.json already has the screenshot entries from Plan 13-01. The audit-mobile.sh script should have added its own Lighthouse entries during Task 1. If audit-mobile.sh has its own MANIFEST upsert logic, it will have run automatically. Verify the MANIFEST now contains both screenshot entries AND lighthouse entries. If Lighthouse entries are missing (audit-mobile.sh may not upsert), note in the SUMMARY — do not manually add phantom entries.

    CONSISTENCY CHECK: After all edits, verify the three documents agree:
    - evidence file status field and sign-off disposition = PASS or MISS
    - PROJECT.md row = ✓ Good or ⚠️ Revisit (same outcome)
    - REQUIREMENTS.md UIDN-02 checkbox = [x] or [ ] (same outcome)
    If any document has a mixed signal (e.g., status says complete-v1.2 but PROJECT.md still says ⚠️ Revisit), fix it before proceeding.
  </action>
  <verify>
    <automated>grep "v1.2 Rerun" .planning/closure/UIDN-02-mobile-evidence.md</automated>
    Expected: non-empty (the new section header is present).
    <automated>grep "Mobile-first" .planning/PROJECT.md</automated>
    Expected: contains "v1.2" in the output line.
    <automated>grep "v1.2 rerun" .planning/REQUIREMENTS.md</automated>
    Expected: non-empty (the outcome note is present in the UIDN-02 row).
    <automated>grep -c "deferred\|complete" .planning/closure/UIDN-02-mobile-evidence.md</automated>
    Expected: at least 1 (the updated status field).
    Cross-doc consistency check:
    <automated>grep "status:" .planning/closure/UIDN-02-mobile-evidence.md | head -1</automated>
    Expected: contains "complete-v1.2" or "deferred-v1.2" (not "deferred — Lighthouse Perf...").
    D-25 no-PR-placeholder check:
    <automated>grep -E "PR #XX|PR #TBD|GitHub PR #X" .planning/closure/UIDN-02-mobile-evidence.md && echo "FAIL: PR placeholder present (violates D-25)" || echo "PASS: no PR placeholder"</automated>
    Expected: "PASS: no PR placeholder" (the grep must return zero matches; on MISS-branch outcome wording change, also re-verify the v1.2 cross-refs block).
    D-26 technical-miss conditional check (only meaningful on MISS outcome with at least one Perf=89):
    <automated>if grep -E "FAIL Perf=89|Perf=89.*FAIL" .planning/closure/UIDN-02-mobile-evidence.md > /dev/null; then grep "Technical miss within Lighthouse" .planning/closure/UIDN-02-mobile-evidence.md && echo "PASS: D-26 note present for 89-Perf route" || echo "FAIL: D-26 technical-miss note missing despite Perf=89 row"; else echo "SKIP: no Perf=89 routes — D-26 note not required"; fi</automated>
    Expected: "PASS" or "SKIP" depending on outcome.
    D-22 verify-does-not-rerun check (this plan's own discipline):
    <automated>python3 -c 'import re,sys; t=open(".planning/phases/13-uidn-02-mobile-audit-closure/13-02-PLAN.md").read(); body=re.split(r"^---\s*$", t, maxsplit=2, flags=re.M)[-1]; vs=re.findall(r"<verify>(.*?)</verify>", body, re.DOTALL); bad=[i+1 for i,v in enumerate(vs) if re.search(r"bash\s+\.planning/closure/audit-mobile\.sh", v)]; print("FAIL: D-22 — audit-mobile.sh invoked inside body verify block(s)", bad) if bad else print(f"PASS: D-22 — no body verify block (of {len(vs)} total) re-invokes audit-mobile.sh")'</automated>
    Expected: "PASS: D-22 — no body verify block (of 3 total) re-invokes audit-mobile.sh". The Python one-liner strips YAML frontmatter via `re.split` on the `^---$` delimiter (so the D-22 rule wording inside the must_haves frontmatter doesn't false-positive), extracts every `<verify>...</verify>` region from the body, and flags only bash invocations of audit-mobile.sh inside them — `<action>` and `<read_first>` mentions are intentionally allowed.
  </verify>
  <acceptance_criteria>
    - `.planning/closure/UIDN-02-mobile-evidence.md` contains `## v1.2 Rerun` section with actual Lighthouse scores for all 5 routes (not placeholder TBD)
    - `.planning/closure/UIDN-02-mobile-evidence.md` frontmatter `status:` line is `complete-v1.2` or `deferred-v1.2 — N/5 routes under threshold; ...` (the old "deferred — Lighthouse Perf..." text is gone; "under threshold" not "under 90" per D-20)
    - `.planning/closure/UIDN-02-mobile-evidence.md` Lighthouse mobile scores table has a Status column naming the failing category(ies) per route on MISS — `grep -E "FAIL [PABs]" .planning/closure/UIDN-02-mobile-evidence.md` returns non-empty on MISS, or `grep "PASS" .planning/closure/UIDN-02-mobile-evidence.md` returns non-empty on PASS (D-20 per-route diagnosis)
    - `.planning/closure/UIDN-02-mobile-evidence.md` ends with italics sign-off block matching OBSV-02-bundle-delta.md convention (Audited, Method, Disposition lines)
    - `grep -E "PR #XX|PR #TBD|GitHub PR #X" .planning/closure/UIDN-02-mobile-evidence.md` returns empty (D-25 no placeholder)
    - `grep '13-REVIEWS.md' .planning/closure/UIDN-02-mobile-evidence.md` is non-empty (new D-18 cross-ref)
    - If any route lands at Perf=89, the file contains the exact "Technical miss within Lighthouse" sentence (D-26); otherwise this check is skipped
    - `grep 'Mobile-first' .planning/PROJECT.md` output contains 'v1.2' on the same row; on MISS the wording uses "under threshold" not "under 90" (D-20)
    - `grep 'UIDN-02' .planning/REQUIREMENTS.md` output contains 'v1.2 rerun' or 'v1.2 rerun complete'; MISS wording uses "under threshold" not "under 90"
    - Phase Traceability row for UIDN-02 no longer says "Pending"
    - All three documents reflect the same pass/miss outcome (no mixed signals)
    - The v1.1 baseline content in UIDN-02-mobile-evidence.md is untouched (new section appended, not replaced)
    - Zero changes to any file under src/
    - This task's `<verify>` block contains zero invocations of `audit-mobile.sh` (D-22 — verified by the awk extraction above)
  </acceptance_criteria>
  <done>UIDN-02-mobile-evidence.md has v1.2 Rerun section with actual scores + per-route Status column (D-20) + D-26 technical-miss footnote when applicable + D-25 no-PR-placeholder + 13-REVIEWS.md cross-ref; PROJECT.md Mobile-first row updated per D-17 with D-20 "under threshold" wording on MISS; REQUIREMENTS.md UIDN-02 row and Traceability row updated per D-14 + D-20; all three documents agree on the same outcome; zero src/ edits; the verify block of this task does not invoke audit-mobile.sh (D-22).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence cross-doc review and commit authorization</name>
  <what-built>
    Tasks 1 and 2 ran the Lighthouse audit and updated all three evidence/documentation artifacts. This checkpoint asks the developer to:
    (a) confirm the three documents agree on the same outcome
    (b) optionally spot-check one authenticated screenshot for session quality
    (c) authorize the atomic commit (Phase 9 Path-3 pattern)
  </what-built>
  <how-to-verify>
    1. Cross-doc consistency (run these grep commands):
       `grep "status:" .planning/closure/UIDN-02-mobile-evidence.md | head -1`
       → should show complete-v1.2 OR deferred-v1.2

       `grep "Mobile-first" .planning/PROJECT.md`
       → should show ✓ Good (v1.2 ...) OR ⚠️ Revisit (v1.2 rerun ...)

       `grep "UIDN-02" .planning/REQUIREMENTS.md`
       → UIDN-02 row should have the same pass/miss wording

       All three must agree on the same outcome. If they disagree, note the discrepancy.

    2. Spot-check one authenticated member screenshot (visual):
       Open `.planning/closure/artifacts/screenshots/bp-375-topics.png` (or any bp-XXX-topics.png).
       Confirm: the page shows the WTCS topics/suggestions list, the Navbar is visible with the theme-toggle button, and there is no "Sign in with Discord" CTA as the primary content (proving the member session was honored, not an auth redirect).

    3. Spot-check the evidence file:
       Read the last 30 lines of `.planning/closure/UIDN-02-mobile-evidence.md`.
       Confirm: the v1.2 Rerun section is present with numeric Lighthouse scores for all 5 routes (not "TBD"), and the sign-off line includes today's date.

    4. Authorize commit:
       Review the changed files: `.planning/closure/audit-screenshots.mjs`, `.planning/closure/UIDN-02-mobile-evidence.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/closure/artifacts/MANIFEST.json`.
       Confirm zero src/ files in `git diff --name-only`.
  </how-to-verify>
  <resume-signal>
    Type "approved" to authorize the atomic commit of all changed files (Phase 9 Path-3 pattern). If any document has mixed signals or the authenticated screenshot shows a login redirect instead of the member UI, type "fix: [describe issue]" and the executor will correct before committing.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Atomic commit (Phase 9 Path-3 pattern) — closure artifacts only, summaries excluded per D-24</name>
  <read_first>
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-CONTEXT.md (discretion note on commit shape — single atomic commit preferred; D-24 excludes summaries from this commit)
    - .planning/phases/13-uidn-02-mobile-audit-closure/13-RESEARCH.md (Q7 atomic vs wave-split analysis)
  </read_first>
  <files>
    .planning/closure/audit-screenshots.mjs
    .planning/closure/UIDN-02-mobile-evidence.md
    .planning/closure/artifacts/MANIFEST.json
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Stage and commit exactly five files in one atomic commit (Phase 9 Path-3 pattern). Binary PNGs in .planning/closure/artifacts/screenshots/ are gitignored — do not attempt to stage them. Lighthouse HTML/JSON reports in .planning/closure/artifacts/lighthouse/ are also gitignored — do not attempt to stage them. MANIFEST.json is NOT gitignored and MUST be staged.

    Per D-24: 13-01-SUMMARY.md and 13-02-SUMMARY.md are NOT included in this atomic commit. /gsd-execute-phase produces them after Task 3 approval; they ship in a separate optional `docs(13): ship summaries` commit (or stay uncommitted as local operator notes — the project's planner does not require commit). This keeps the closure commit focused on the 5 artifacts that ship UIDN-02 evidence.

    Verify the staged set before committing:
    - `git diff --name-only --cached` should show exactly:
      .planning/closure/audit-screenshots.mjs
      .planning/closure/artifacts/MANIFEST.json
      .planning/closure/UIDN-02-mobile-evidence.md
      .planning/PROJECT.md
      .planning/REQUIREMENTS.md
    - Confirm zero src/ files in staged set
    - Confirm no binary .png or .html or .json Lighthouse files are staged
    - Confirm no `13-0[12]-SUMMARY.md` is staged (D-24)
    - Confirm MANIFEST.json IS staged (Gemini suggestion in REVIEWS.md — it is the primary record of the audit run's validity)

    Commit message format (adapt outcome verb per actual result; D-20 multi-category wording):
    On PASS: "feat(13): UIDN-02 closure — v1.2 Lighthouse 5/5 routes ≥ threshold; harness sentinel fix + 42-PNG corpus + row flip ⚠️ → ✓"
    On MISS: "feat(13): UIDN-02 v1.2 rerun — harness sentinel fix + 42-PNG corpus; row stays ⚠️ (N/5 routes under threshold)"
    (substitute actual failingRoutes count for N in the MISS case; use "under threshold" not "under 90" so the message reads correctly for non-Perf failures per D-20)

    Do NOT push to origin — local commit only. The developer will push + open a PR separately. Per D-25, do not include a "PR #XX" placeholder in the commit message.
  </action>
  <verify>
    <automated>git log --oneline -1</automated>
    Expected: the commit message from above is the most recent commit.
    <automated>git show --name-only HEAD | tail -10</automated>
    Expected: lists the 5 changed planning files, no src/ files, no binary PNGs.
    <automated>git diff --name-only HEAD~1 HEAD</automated>
    Expected: exactly the 5 files listed in files_modified (or a subset if MANIFEST was already up-to-date).
    <automated>git show --name-only HEAD | grep -E "13-0[12]-SUMMARY\.md" && echo "FAIL: D-24 — SUMMARY in atomic commit" || echo "PASS: D-24 — summaries excluded"</automated>
    Expected: "PASS: D-24 — summaries excluded"
    <automated>git show --name-only HEAD | grep "MANIFEST.json" && echo "PASS: MANIFEST staged" || echo "FAIL: MANIFEST.json not in atomic commit (Gemini REVIEWS.md suggestion)"</automated>
    Expected: "PASS: MANIFEST staged"
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` shows the Phase 13 commit message
    - `git show --name-only HEAD` contains .planning/closure/audit-screenshots.mjs
    - `git show --name-only HEAD` contains .planning/closure/UIDN-02-mobile-evidence.md
    - `git show --name-only HEAD` contains .planning/PROJECT.md
    - `git show --name-only HEAD` contains .planning/REQUIREMENTS.md
    - `git show --name-only HEAD` contains .planning/closure/artifacts/MANIFEST.json
    - `git show --name-only HEAD | grep "src/"` returns empty (zero src/ files in commit)
    - `git show --name-only HEAD | grep "\.png"` returns empty (no binary PNGs in commit)
    - REQUIREMENTS.md Traceability row for UIDN-02 no longer says "Pending"
  </acceptance_criteria>
  <done>All five planning artifacts committed atomically; zero src/ edits; binary artifacts excluded; UIDN-02 evidence closed at the correct pass/miss outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell → Lighthouse CLI | Lighthouse runs against prod CDN unauthenticated — no user data in scope |
| executor → planning docs | Text edits to .planning/ Markdown files — no user-facing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-02 | Tampering | MANIFEST.json audit record | accept | MANIFEST is committed but only sha256+size records; binary PNGs are gitignored. An attacker who modifies MANIFEST without re-running the harness would produce a manifest inconsistent with the actual PNG corpus, detectable by re-running the harness. Low-value target. |
| T-13-03 | Repudiation | Lighthouse score record | accept | Single-run policy (D-13) means scores cannot be re-run. The JSON reports (gitignored) are the raw evidence; the numeric values in the evidence file are transcribed from them. Score transcription error is a human-error risk, not a security threat. |

No security-relevant changes in scope for this phase. This plan modifies `.planning/closure/UIDN-02-mobile-evidence.md`, `.planning/PROJECT.md`, and `.planning/REQUIREMENTS.md` — all planning artifacts. No authentication flows, no user-facing surfaces, no API endpoints, no database schema changes.

ASVS V2 (Authentication): N/A — no auth flow changes.
ASVS V3 (Session): N/A — no session management changes.
ASVS V4 (Access Control): N/A — no authorization changes.
ASVS V5 (Input Validation): N/A — Lighthouse CLI validates its own input; no user-supplied input processed.
</threat_model>

<verification>
Overall phase verification (both plans complete):

1. `node --check .planning/closure/audit-screenshots.mjs` → exits 0
2. `grep -c 'Toggle color theme' .planning/closure/audit-screenshots.mjs` → 3
3. `grep -c "body.*filter.*hasText" .planning/closure/audit-screenshots.mjs` → 0
4. `grep 'v1.2 Rerun' .planning/closure/UIDN-02-mobile-evidence.md` → non-empty
5. `grep 'status:' .planning/closure/UIDN-02-mobile-evidence.md | head -1` → contains 'complete-v1.2' or 'deferred-v1.2' (and on MISS, the wording is "under threshold" per D-20 — never "under 90")
6. `grep 'Mobile-first' .planning/PROJECT.md` → contains 'v1.2'
7. `grep 'UIDN-02' .planning/REQUIREMENTS.md | head -3` → UIDN-02 row contains 'v1.2 rerun'
8. `grep 'UIDN-02' .planning/REQUIREMENTS.md | grep 'Pending'` → empty (Pending gone)
9. `git show --name-only HEAD | grep "src/"` → empty (zero src/ edits)
10. `git diff --name-only HEAD~1 HEAD | grep "\.planning"` → 5 planning files changed (D-24 — no 13-0[12]-SUMMARY.md in this commit)
11. `grep -E "PR #XX|PR #TBD|GitHub PR #X" .planning/closure/UIDN-02-mobile-evidence.md` → empty (D-25 no placeholder)
12. Inside both PLAN.md files, `grep "/Users/khaiphan"` returns only meta-doc rule statements (the D-21 rule wording itself), never inside a `<read_first>`, `<action>`, `<verify>`, `<files>`, `<execution_context>`, or `<context>` block. Quick automated form: `grep -nE "/Users/khaiphan" .planning/phases/13-uidn-02-mobile-audit-closure/13-0[12]-PLAN.md | grep -vE "D-21|under threshold|absolute paths"` should return empty — every remaining match is the rule statement, not an operational path.
13. `awk '/<verify>/,/<\/verify>/' .planning/phases/13-uidn-02-mobile-audit-closure/13-02-PLAN.md | grep "bash .planning/closure/audit-mobile.sh"` → empty (D-22)
14. The sha256 dupe-check log line printed by the harness matches "0 unexpected collisions" with a non-zero count of "allowed home↔admin collision pairs per D-19" (D-19)

ROADMAP Success Criteria mapping:
- SC1 (harness exits 0, no F6 warnings, sha256 passed with D-19 whitelist) → Plan 13-01 Task 1 + Task 2 checkpoint
- SC2 (42 PNGs, /topics + /archive captured authenticated) → Plan 13-01 Task 1 + Task 2 checkpoint
- SC3 (Lighthouse scores for all 5 routes archived; per-category recorded per D-20) → Plan 13-02 Task 1 + Task 2
- SC4 (PROJECT.md row flipped or stays ⚠️ per strict floor + D-20 multi-category wording) → Plan 13-02 Task 2
</verification>

<success_criteria>
- Lighthouse audit produces numeric Perf/A11y/BP/SEO scores for all 5 routes (`/`, `/topics`, `/archive`, `/auth/error`, `/admin`); per-route failingCategories recorded per D-20
- UIDN-02-mobile-evidence.md has `## v1.2 Rerun (2026-05-XX)` section with actual scores (not placeholders) AND a Status column naming the failing category per route on MISS (D-20)
- Evidence file frontmatter `status:` is `complete-v1.2` (pass) or `deferred-v1.2 — N/5 routes under threshold; follow-up tied to next perf-budget change` (miss — D-20 wording, not "under 90")
- If any route lands at Perf=89, the evidence file contains the exact "Technical miss within Lighthouse ±5-10pp noise band" sentence (D-26); otherwise this check does not apply
- PROJECT.md `Mobile-first responsive design` row is `✓ Good (v1.2 ...)` (pass) or `⚠️ Revisit (v1.2 rerun — N/5 routes under threshold; ...)` (miss) per D-17 + D-20 exact wording
- REQUIREMENTS.md UIDN-02 active row has outcome note per D-14 + D-20 ("under threshold"); Phase Traceability row is no longer "Pending"
- All three documents agree on the same pass/miss outcome
- The Lighthouse audit was invoked exactly once (D-13); verify blocks inspect `.report.json` via `jq`, never re-invoke `audit-mobile.sh` (D-22)
- The sha256 dupe-check whitelists exactly the per-width home↔admin pair (D-19) and fails the harness only on unexpected collisions
- All plan-internal path references are repo-relative (D-21); no `/Users/khaiphan/...` paths in operational commands
- No "PR #XX" placeholder in any committed text (D-25)
- 13-01-SUMMARY.md and 13-02-SUMMARY.md are NOT in the atomic commit (D-24)
- Member fixture constants in audit-screenshots.mjs are inline-mirrored from e2e/fixtures/test-users.ts with a D-23 source comment, not imported
- Zero `src/` edits across both plans (Phase 9 zero-src/-edits closure invariant)
- Atomic commit includes exactly the 5 changed planning files and zero binary artifacts (MANIFEST.json is staged — Gemini REVIEWS.md suggestion)
- v1.1 baseline content in UIDN-02-mobile-evidence.md is untouched (new section appended, not replaced)
</success_criteria>

<output>
After completion, create `.planning/phases/13-uidn-02-mobile-audit-closure/13-02-SUMMARY.md` documenting (D-24: this file is NOT in the atomic commit — operator may ship it separately or keep as a local note):
- Lighthouse scores for all 5 routes (actual numeric Perf/A11y/BP/SEO values per route)
- Per-route failingCategories list (empty if route passed all 4 thresholds; D-20)
- Outcome: PASS or MISS; "N/5 routes under threshold" (D-20 wording)
- If applicable, D-26 technical-miss-at-89 note (which route landed at 89, if any)
- D-19 dupe-check result: "X allowed home↔admin collision pairs, 0 unexpected collisions"
- Which files were updated and the exact wording used in each
- Commit hash from Task 4
- Whether the REQUIREMENTS.md Traceability row now reads Complete or Active
- requirements-completed: [UIDN-02] (if PASS) or requirements-completed: [] (if MISS — UIDN-02 remains open)
</output>
