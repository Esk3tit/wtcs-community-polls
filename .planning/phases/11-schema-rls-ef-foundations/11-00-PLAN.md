---
phase: 11-schema-rls-ef-foundations
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.integration.ts
  - e2e/integration/helpers.ts
  - e2e/integration/vote-counts-rls.test.ts
  - e2e/integration/toggle-results-visibility.test.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-11
  - TEST-12
tags:
  - testing
  - integration
  - vitest
  - ci

must_haves:
  truths:
    - "Running `npm run test:integration` resolves the new vitest config and exits 0 with 'no test files' or with the suite skeletons listed (not unhandled module errors)"
    - "`vitest.config.integration.ts` exists, uses `environment: 'node'`, `include: ['e2e/integration/**/*.test.ts']`, `passWithNoTests: false`, and does NOT inject placeholder SUPABASE env vars (real env required)"
    - "`e2e/integration/helpers.ts` exports `mintClients()` returning `{ anon, authed, serviceRole }`, plus `createFreshPoll()`, `castVote()`, `invokeEF()`, `readAuditLog()`, `cleanupPoll()`"
    - "`e2e/integration/vote-counts-rls.test.ts` exists with `describe.each` skeleton over a 12-cell matrix (3 roles × 2 hidden × 2 voted), each `it()` currently calls `it.todo()` so the file parses without DB"
    - "`e2e/integration/toggle-results-visibility.test.ts` exists with 4 `it.todo()` cases (403, 200+row, audit on change, no-op no-audit)"
    - "`package.json` has new script `test:integration` running `vitest run --config vitest.config.integration.ts`; existing `test` script untouched"
    - "`.github/workflows/ci.yml` has a new `test-integration` job placed parallel to `e2e` (both `needs: lint-and-unit`); job re-uses the `supabase start` + `Derive local Supabase keys` + `Apply fixture seed` steps from the existing `e2e` job and ends with `npm run test:integration`"
    - "D-20: TEST-11 RLS invariant test runs as Vitest (not Playwright) at `e2e/integration/vote-counts-rls.test.ts` using `describe.each` over 12 cells, matching the precedent set by `polls-effective-invariant.test.ts` (Vitest + supabase-js for RLS invariants — no browser, no Playwright fixture machinery)"
    - "D-21: `e2e/integration/helpers.ts` mints three pre-scoped clients (anon / authenticated test user / service-role) and provides a freshPoll-equivalent setup/teardown that does NOT depend on Playwright's `test.extend` lifecycle; cleanup deletes the poll on test exit and rethrows on teardown failure"
    - "D-22: A new `npm run test:integration` script + new `vitest.config.integration.ts` scoped to `e2e/integration/` exist, so the existing `npm run test` still runs only `src/__tests__/` (no overlap; integration env requires real service-role credentials)"
  artifacts:
    - path: "vitest.config.integration.ts"
      provides: "Integration vitest config (node env, e2e/integration scope)"
      contains: "environment: 'node'"
    - path: "e2e/integration/helpers.ts"
      provides: "mintClients/createFreshPoll/castVote/invokeEF/readAuditLog/cleanupPoll"
      exports: ["mintClients", "createFreshPoll", "castVote", "invokeEF", "readAuditLog", "cleanupPoll"]
    - path: "e2e/integration/vote-counts-rls.test.ts"
      provides: "TEST-11 12-cell matrix scaffold (it.todo placeholders)"
      contains: "describe.each"
    - path: "e2e/integration/toggle-results-visibility.test.ts"
      provides: "TEST-12 admin EF authz scaffold"
      contains: "it.todo"
    - path: "package.json"
      provides: "test:integration script"
      contains: "test:integration"
    - path: ".github/workflows/ci.yml"
      provides: "test-integration CI job"
      contains: "test-integration:"
  key_links:
    - from: "vitest.config.integration.ts"
      to: "e2e/integration/**/*.test.ts"
      via: "include glob"
      pattern: "e2e/integration/\\*\\*/\\*\\.test\\.ts"
    - from: "e2e/integration/helpers.ts"
      to: "e2e/helpers/auth.ts"
      via: "lazy-singleton service-role client pattern (lifted, no Playwright dependency)"
      pattern: "createClient.*SUPABASE_SERVICE_ROLE_KEY"
    - from: ".github/workflows/ci.yml"
      to: "npm run test:integration"
      via: "new test-integration job"
      pattern: "npm run test:integration"
---

<objective>
Land the integration-test infrastructure (Wave 0) so Wave 1 schema work and Wave 3 invariant/authz tests can be written against a real `npm run test:integration` runner. No test bodies in this plan — only the scaffolds, config, package script, and CI job.

Purpose: Per VALIDATION.md, Wave 0 must be installable before any test bodies. The unit suite (`npm run test`) MUST continue to run only `src/__tests__/` and MUST NOT pick up `e2e/integration/`. Real `SUPABASE_SERVICE_ROLE_KEY` is required by integration tests — placeholder env vars are explicitly NOT injected.

Output: New vitest config, integration test helpers, 2 test scaffolds with `it.todo()` placeholders, `test:integration` npm script, new `test-integration` CI job parallel to `e2e`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md
@.planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md
@.planning/phases/11-schema-rls-ef-foundations/11-VALIDATION.md

<!-- Source-of-truth files the executor MUST read (analogs to copy structure from) -->
@vite.config.ts
@package.json
@.github/workflows/ci.yml
@e2e/helpers/auth.ts
@e2e/fixtures/poll-fixture.ts
@e2e/fixtures/test-users.ts
@src/__tests__/admin/polls-effective-invariant.test.ts

<interfaces>
<!-- Key identifiers Wave 1+ tests will depend on. Executor implements these as helper exports. -->

Helper exports required by Wave 3 tests (e2e/integration/helpers.ts):

```
export type IntegrationClients = {
  anon: SupabaseClient
  authed: SupabaseClient        // pre-signed in as fixtureUsers.memberUser
  serviceRole: SupabaseClient
}

export function mintClients(opts?: { authAs?: 'memberUser' | 'adminUser' }): Promise<IntegrationClients>
export function createFreshPoll(opts: { hidden?: boolean; suiteSlug: string }): Promise<{ id: string; title: string }>
export function castVote(opts: { pollId: string; client: SupabaseClient; choiceId?: string }): Promise<void>
export function invokeEF(opts: { client: SupabaseClient; name: string; body: unknown }): Promise<{ status: number; data: unknown; error: unknown }>
export function readAuditLog(opts: { serviceRole: SupabaseClient; targetId: string; action?: string }): Promise<AuditRow[]>
export function cleanupPoll(opts: { serviceRole: SupabaseClient; pollId: string }): Promise<void>
```

From e2e/fixtures/test-users.ts (already exists — reuse):

```
fixtureUsers.memberUser    // authenticated non-admin (for 403 + "authed × voted" cells)
fixtureUsers.adminUser     // admin (for 200 + service-role-bypass comparison)
```

From vite.config.ts (existing `test:` block — lift structure, then apply deltas):

```
test: {
  globals: true,
  environment: 'jsdom',         // CHANGE to 'node' in integration config
  setupFiles: './src/test/setup.ts',  // DROP in integration config
  passWithNoTests: true,        // CHANGE to false in integration config
  exclude: ['e2e/**'],          // REMOVE in integration config
  env: { VITE_SUPABASE_URL: 'http://localhost:54321', ... },  // DROP in integration config (real env required)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 00-01: Vitest integration config + package.json script + CI job</name>
  <files>vitest.config.integration.ts, package.json, .github/workflows/ci.yml</files>
  <read_first>
    - vite.config.ts (existing embedded `test:` block at lines 41-54 — lift structure, then apply deltas per PATTERNS.md "vitest.config.integration.ts" section)
    - package.json (existing `scripts` block, lines 7-15 — add one line)
    - .github/workflows/ci.yml (existing `e2e` job — copy first half of steps for the new `test-integration` job)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "vitest.config.integration.ts" and § "package.json modification" and § ".github/workflows/ci.yml modification"
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-22 (test:integration script + scoped config rationale)
  </read_first>
  <action>
    Create `vitest.config.integration.ts` at repo root using `defineConfig` from `vitest/config`. Set `test.globals: true`, `test.environment: 'node'`, `test.include: ['e2e/integration/**/*.test.ts']`, `test.passWithNoTests: false` (per D-22 — integration suite empty result is a regression), `test.testTimeout: 30_000` (DB round-trips). Do NOT set `setupFiles`, `exclude`, or any `env:` block — integration tests require real `SUPABASE_SERVICE_ROLE_KEY` + `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` from the shell environment (per PATTERNS.md — placeholder env breaks RLS tests).

    In `package.json` add the new script line `"test:integration": "vitest run --config vitest.config.integration.ts"` immediately after the existing `"test": "vitest run"` entry. Do not change `"test"`, `"test:watch"`, or `"test:coverage"`.

    In `.github/workflows/ci.yml` add a new top-level job named `test-integration` placed parallel to the existing `e2e` job (both `needs: lint-and-unit`). Copy verbatim from the existing `e2e` job: `runs-on`, `timeout-minutes` (set to 12), `actions/checkout@v4`, `actions/setup-node@v4` with node 22 + npm cache, `npm ci`, `supabase/setup-cli@v1` step, `supabase start` step, the "Wait for stack ready" + "Derive local Supabase keys" (with `id: supabase-keys`) + "Apply fixture seed" steps. Final step name "Run integration suite" with `env:` block setting `VITE_SUPABASE_URL: http://localhost:54321`, `VITE_SUPABASE_ANON_KEY: ${{ steps.supabase-keys.outputs.anon_key }}`, `SUPABASE_SERVICE_ROLE_KEY: ${{ steps.supabase-keys.outputs.service_role_key }}`, running `npm run test:integration`.

    Do not create the test files yet — that is Task 00-02 + 00-03. Do not modify `vite.config.ts` (the existing embedded `test:` block stays — unit suite continues to use it).
  </action>
  <acceptance_criteria>
    - `vitest.config.integration.ts` exists at repo root and contains the literal string `environment: 'node'`
    - `vitest.config.integration.ts` contains `include: ['e2e/integration/**/*.test.ts']`
    - `vitest.config.integration.ts` contains `passWithNoTests: false`
    - `vitest.config.integration.ts` does NOT contain `VITE_SUPABASE_URL:` or `VITE_SUPABASE_ANON_KEY:` literal strings (no env placeholders)
    - `vitest.config.integration.ts` does NOT contain `setupFiles:` or `jsdom`
    - `package.json` `scripts` block contains `"test:integration": "vitest run --config vitest.config.integration.ts"`
    - `package.json` `scripts` block still contains the original `"test": "vitest run"` line unchanged
    - `.github/workflows/ci.yml` contains a job key `test-integration:` at top level under `jobs:`
    - `.github/workflows/ci.yml` `test-integration` job has `needs: lint-and-unit`
    - `.github/workflows/ci.yml` `test-integration` job includes `supabase start` and `npm run test:integration` steps
    - `npm run test:integration -- --reporter=verbose` returns non-zero exit due to `passWithNoTests: false` AND no test files existing yet (this is the expected pre-Task-00-02 state) — verify the failure mode is "no test files matched" NOT "config parse error" or "module not found"
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c = require('./vitest.config.integration.ts'); console.log('config loadable')" 2>&1 | grep -E "(loadable|environment)" || npx vitest run --config vitest.config.integration.ts --reporter=verbose 2>&1 | grep -E "(No test files|passWithNoTests|environment)"</automated>
  </verify>
  <done>
    Integration vitest config loads without error, package.json has the new script, CI workflow has the `test-integration` job parallel to `e2e`. Unit `npm run test` still runs only `src/__tests__/`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 00-02: Integration test helpers (mintClients + freshPoll + EF invoke + audit reader)</name>
  <files>e2e/integration/helpers.ts</files>
  <read_first>
    - e2e/helpers/auth.ts (lines 26-46 env var resolution; lines 86-95 signInWithPassword; lines 122-138 lazy-singleton `getAdminClient()` — lift verbatim and adapt for anon + authed minters)
    - e2e/fixtures/test-users.ts (full file — `fixtureUsers.memberUser` and `fixtureUsers.adminUser` shape + `FIXTURE_PASSWORD`)
    - e2e/fixtures/poll-fixture.ts (lines 64-118 setup/teardown shape; lines 126-136 normalize-error helper — lift try/catch/finally discipline)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "e2e/integration/helpers.ts"
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-21 (cleanup rethrows on teardown failure)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pattern 2" for `invokeEF` shape against the upcoming `toggle-results-visibility` EF
  </read_first>
  <action>
    Create `e2e/integration/helpers.ts` exporting the functions named in the `<interfaces>` block above:

    1. `mintClients(opts?: { authAs?: 'memberUser' | 'adminUser' }): Promise<{ anon, authed, serviceRole }>` — creates three `SupabaseClient` instances via `createClient` from `@supabase/supabase-js`. `serviceRole` is built lazily and reused across calls (lift the `getAdminClient()` singleton at `e2e/helpers/auth.ts:122-138` verbatim, swap module-scope singleton, drop Playwright import). `anon` uses `VITE_SUPABASE_ANON_KEY` only. `authed` uses anon key + completes `signInWithPassword({ email, password: FIXTURE_PASSWORD })` against `fixtureUsers.memberUser` (or `adminUser` if `opts.authAs === 'adminUser'`). All three set `auth: { autoRefreshToken: false, persistSession: false }`.

    2. `createFreshPoll(opts: { hidden?: boolean; suiteSlug: string }): Promise<{ id: string; title: string }>` — INSERTs via the service-role client into `polls`. Title prefix `[TEST-11] ${suiteSlug} ${Date.now()}` (RESEARCH Pitfall 6 — deterministic prefix for pre-suite DELETE safety). Required fields: `title`, `description`, `status: 'active'`, `is_pinned: true`, `category_id: 'a0000000-0000-0000-0000-000000000001'` (fixture seed category), `created_by: fixtureUsers.adminUser.id`, `closes_at: new Date(Date.now() + 7 * 24 * 60 * 60 * 1000).toISOString()`. Pass `results_hidden: opts.hidden ?? false` so this helper works both pre-migration (column ignored) and post-migration (column accepted). Also INSERT at least one `choices` row tied to the new poll so `castVote()` can target a real choice — choice text `'option-a'`. Returns `{ id, title }`.

    3. `castVote(opts: { pollId, client, choiceId? }): Promise<void>` — looks up the first choice for `pollId` if `choiceId` is omitted (service-role SELECT), then INSERTs into `votes` table using the provided `client` (which must be authed — the caller decides whether to use `authed` or `serviceRole`). Throws on insert error.

    4. `invokeEF(opts: { client, name, body }): Promise<{ status, data, error }>` — wraps `client.functions.invoke(name, { body })`. supabase-js does not surface HTTP status directly when `error` is set — call the EF via `fetch` to `${SUPABASE_URL}/functions/v1/${name}` with `Authorization: Bearer ${session.access_token}` (extract via `client.auth.getSession()`) so the test can assert the literal 403/200 status code required by TEST-12. Return `{ status: response.status, data: parsed JSON body, error: null|errorBody }`.

    5. `readAuditLog(opts: { serviceRole, targetId, action? }): Promise<AuditRow[]>` — `.from('audit_log').select('*').eq('target_id', targetId)` with optional `.eq('action', action)`. Returns `data ?? []`. Throws on supabase error.

    6. `cleanupPoll(opts: { serviceRole, pollId }): Promise<void>` — `await serviceRole.from('polls').delete().eq('id', pollId)`. On error, rethrow with a normalize-error helper lifted from `poll-fixture.ts:126-136`. Cascade in migration 0 handles choices/votes/vote_counts.

    Export an `AuditRow` type matching the migration 10 columns: `id, actor_id, action, target_type, target_id, before, after, created_at`. Use TypeScript `type` (project convention — no `interface`).

    Env var resolution: lazy getters that throw with actionable error if `SUPABASE_URL` (default `http://localhost:54321`), `VITE_SUPABASE_ANON_KEY`, or `SUPABASE_SERVICE_ROLE_KEY` missing — copy the messaging pattern from `e2e/helpers/auth.ts:26-46`.
  </action>
  <acceptance_criteria>
    - `e2e/integration/helpers.ts` exists and exports: `mintClients`, `createFreshPoll`, `castVote`, `invokeEF`, `readAuditLog`, `cleanupPoll`, and a type `AuditRow`
    - File imports `createClient` from `@supabase/supabase-js` (matching the project's pinned version pattern — `import` not `require`)
    - `invokeEF` uses `fetch` against `/functions/v1/<name>` (not `client.functions.invoke`) so HTTP status is recoverable — verify with `grep -nE "fetch\\(.*functions/v1" e2e/integration/helpers.ts` returns at least 1 match
    - `cleanupPoll` rethrows on delete error (no silent swallow) — `grep -nE "throw" e2e/integration/helpers.ts` returns ≥1 match in the cleanupPoll function body
    - `mintClients()` `serviceRole` is a lazy singleton — module-scope `let _serviceRoleClient: SupabaseClient | null = null` pattern present
    - Service-role key resolution throws with actionable error if missing — `grep -nE "SUPABASE_SERVICE_ROLE_KEY" e2e/integration/helpers.ts` returns ≥2 matches (one for read, one for error message)
    - No import from `@playwright/test`, no import from `e2e/fixtures/poll-fixture` (this is a Vitest-only helper — Playwright dependencies must NOT leak in)
    - `tsc --noEmit -p tsconfig.app.json` (or equivalent type-check) passes with the new file present
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "export (function|type) (mintClients|createFreshPoll|castVote|invokeEF|readAuditLog|cleanupPoll|AuditRow)" e2e/integration/helpers.ts | wc -l | xargs -I{} test {} -ge 7 && grep -L "@playwright/test" e2e/integration/helpers.ts && grep -nE "fetch\\(.*functions/v1" e2e/integration/helpers.ts</automated>
  </verify>
  <done>
    Helpers file compiles, exports all 6 functions + AuditRow type, uses `fetch` for EF status recovery, rethrows on cleanup failure, has zero Playwright imports.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 00-03: TEST-11 + TEST-12 scaffolds with it.todo placeholders</name>
  <files>e2e/integration/vote-counts-rls.test.ts, e2e/integration/toggle-results-visibility.test.ts</files>
  <read_first>
    - src/__tests__/admin/polls-effective-invariant.test.ts (full file — Vitest `describe`/`it` structure + failure-message convention)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "e2e/integration/vote-counts-rls.test.ts" and § "e2e/integration/toggle-results-visibility.test.ts" (case tables verbatim)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-20, D-23, D-24
  </read_first>
  <action>
    Create `e2e/integration/vote-counts-rls.test.ts`. Top of file: `import { describe, it } from 'vitest'` (no `expect` yet — bodies are `it.todo`). Single `describe('vote_counts RLS 12-cell matrix (TEST-11)', ...)`. Inside, declare a `cases` const array with 12 entries `[role, hidden, voted, expectRows]`:

    - `['anon', false, false, 0]`, `['anon', false, true, 0]`, `['anon', true, false, 0]`, `['anon', true, true, 0]`
    - `['authed', false, false, 0]`, `['authed', false, true, '>0']` (ONLY allowed cell), `['authed', true, false, 0]`, `['authed', true, true, 0]`
    - `['serviceRole', false, false, '>0']`, `['serviceRole', false, true, '>0']`, `['serviceRole', true, false, '>0']`, `['serviceRole', true, true, '>0']` (bypass — all 4 cells)

    Use `describe.each(cases)('%s × hidden=%s × voted=%s', (role, hidden, voted, expected) => { it.todo(\`returns \${expected} rows\`); })`. Do not write the body — Wave 3 Plan 04 wires the bodies against the helpers.

    Create `e2e/integration/toggle-results-visibility.test.ts`. Top: `import { describe, it } from 'vitest'`. Single `describe('toggle-results-visibility (TEST-12)', ...)` with exactly four `it.todo()` cases:
    1. `'non-admin caller returns 403'`
    2. `'admin caller returns 200 with updated poll row + non-null results_hidden_changed_at'`
    3. `'audit_log row written on state change'`
    4. `'no audit_log row on no-op call (idempotency, D-11)'`

    Both files contain a top-of-file WHY comment block (no review-round / phase-ID archaeology per CLAUDE.md memory `feedback_no_review_archaeology_in_source.md` — describe what the suite enforces, not which plan/round/phase added it).
  </action>
  <acceptance_criteria>
    - `e2e/integration/vote-counts-rls.test.ts` exists and contains the literal string `describe.each`
    - The 12-cell case array in vote-counts-rls.test.ts has exactly 12 entries — `grep -c "^\\s*\\[.*,.*,.*,.*\\]," e2e/integration/vote-counts-rls.test.ts` returns ≥12 (allow up to 12 with trailing commas)
    - The 12-cell case array includes exactly 4 `'anon'` entries, 4 `'authed'` entries, 4 `'serviceRole'` entries
    - `e2e/integration/toggle-results-visibility.test.ts` has exactly 4 `it.todo(` occurrences (no more, no less)
    - Neither file contains a fenced `expect(` call yet — `grep -c "expect(" e2e/integration/vote-counts-rls.test.ts e2e/integration/toggle-results-visibility.test.ts` returns 0 across both
    - Neither file imports from `e2e/integration/helpers` yet (scaffolds only — Wave 3 wires helpers)
    - Neither file references "review round", "Round 1", or "Plan 04" in source comments (per CLAUDE.md project comment policy)
    - `npm run test:integration` exits 0 (todo tests are considered passing in Vitest, and `passWithNoTests: false` is satisfied by these two files)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:integration 2>&1 | grep -E "(2 passed|0 failed|todo)" && grep -c "it.todo" e2e/integration/toggle-results-visibility.test.ts | xargs test 4 -eq && grep -E "(anon|authed|serviceRole).*hidden=.*voted=" e2e/integration/vote-counts-rls.test.ts | wc -l | xargs -I{} test {} -ge 1</automated>
  </verify>
  <done>
    Both scaffolds parse cleanly, `npm run test:integration` reports a passing run with all cases as `todo`, no `expect()` or helper imports yet (Wave 3 fills the bodies).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → secrets | `SUPABASE_SERVICE_ROLE_KEY` flows through GitHub Actions step outputs into the new `test-integration` job env block |
| Local dev → real Supabase | Developers running `npm run test:integration` need real env vars in their shell — placeholder injection deliberately removed so a missing service-role key fails loud (not silent green) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-W0-01 | Information Disclosure | `vitest.config.integration.ts` env block | mitigate | DO NOT inject placeholder `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` in the config — require real values from shell env (per PATTERNS.md and D-22 rationale). Placeholder injection would mask broken RLS in CI as a false green. Verified by acceptance criterion grepping for absent literal `VITE_SUPABASE_URL:` in the config file. |
| T-11-W0-02 | Tampering | CI `test-integration` job | mitigate | Re-use `supabase/setup-cli@v1` + `supabase start` from the existing `e2e` job verbatim (same Supabase CLI version, same key derivation) — no bespoke local-stack init. Reduces drift between integration and e2e jobs. |
| T-11-W0-03 | Repudiation | Audit log future reads | accept | `readAuditLog` helper is read-only and uses service-role — no audit-of-audit needed in v1.2. The audit log itself is the forensics surface. |
</threat_model>

<verification>
- `npm run test` (unit, existing) continues to pass with zero changes in result count
- `npm run test:integration` exits 0 with the 4 it.todo cases (and the 12 describe.each todos) reported as passing/todo
- `vitest.config.integration.ts` does not inject SUPABASE env placeholders
- `e2e/integration/helpers.ts` exports the 6 helpers + AuditRow type, has zero Playwright imports
- CI workflow `test-integration` job is parallel to `e2e` (same `needs: lint-and-unit`)
</verification>

<success_criteria>
- Wave 0 installable before any Wave 1+ test bodies land
- Unit + integration suites cleanly separated (unit excludes `e2e/**`, integration scoped to `e2e/integration/`)
- CI gains an integration-suite gate without doubling wall time (integration job runs in parallel to e2e)
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-rls-ef-foundations/11-00-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Declare `requirements-completed: [TEST-11 (scaffold only), TEST-12 (scaffold only)]` and `wave_0_complete: true` so VALIDATION.md frontmatter can flip.
</output>
