---
phase: 11-schema-rls-ef-foundations
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, "03b", 04]
files_modified: []
autonomous: false
requirements: []
tags:
  - deploy
  - schema-push
  - blocking
  - supabase

must_haves:
  truths:
    - "Migration 10 has been applied to the production Supabase project — `polls.results_hidden`, `polls.results_hidden_changed_at`, `audit_log` table, rewritten `polls_effective` view, and rewritten `vote_counts` policy are all live in production"
    - "REVIEW-FIX-H7: Local pre-merge sanity gate uses `supabase db reset` ONLY — NOT `supabase db push`. The `db push` command targets the LINKED remote database, not the local stack. Running `db push` during the local pre-merge step would push migration 10 to the linked project (which may be production) PRIOR to the integration-test gate completing. The local gate is now: `supabase db reset && npm run test && npm run test:integration` — all three steps run against the local Supabase stack only. Addresses Codex HIGH concern."
    - "REVIEW-FIX-H8: Production deploy commands use `--project-ref <prod-ref> --use-api --no-verify-jwt` flags consistent with the existing `package.json` `deploy:functions:all` script (if present) or the established `supabase functions deploy` workflow for this project. Specifically: `supabase functions deploy toggle-results-visibility --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt` and the same flags for the 12 retrofits. The `--no-verify-jwt` flag is required because the EFs use service-role bypass for some operations (and verify JWTs themselves via the `requireAdmin` helper); the platform-level JWT verification on the Functions runtime would double-check and reject valid admin requests. The `--use-api` flag uses the management API path which is more reliable for CI/non-TTY deploys. The `--project-ref` flag explicitly targets production (and would error if the access token were scoped to the wrong project). Addresses Codex HIGH concern."
    - "All 12 retrofitted EFs (11 from Plan 11-03 + create-poll from Plan 11-03b) deploy successfully via per-function `supabase functions deploy <name> --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt` — total of 13 EFs deployed when combined with the new toggle EF"
    - "REVIEW-FIX-M8: Smoke `curl` includes BOTH admin AND non-admin probes — admin JWT against `toggle-results-visibility` returns 200 + updated poll row; non-admin (memberUser) JWT returns 403. Both probes use a dedicated test poll (see REVIEW-FIX-M9 below) so the smoke does not affect any community-facing poll. Addresses Codex MEDIUM concern."
    - "REVIEW-FIX-M9: Smoke test uses a DEDICATED test poll created by the deploy step itself — not an existing community poll. The smoke creates a poll via the deployed `create-poll` EF (also exercising the Plan 11-03b retrofit), runs the admin+non-admin toggle probes against it, archives it, then DELETEs both the audit_log rows and the poll itself. Prevents the deploy from briefly toggling visibility on a real user-visible poll (Codex MEDIUM concern about smoke affecting real user-visible state)."
    - "REVIEW-FIX-L4: Post-deploy DB verification of the `polls.results_hidden` column includes `column_default` — the verification query is `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='polls' AND column_name='results_hidden'` and the assertion is `column_default = 'false'` (or `false` depending on Postgres's literal-emit form). Addresses Codex LOW concern."
    - "Post-deploy: `SELECT pg_get_viewdef('public.polls_effective')` in the prod DB shows `results_hidden` and `results_hidden_changed_at` projected; `SELECT * FROM pg_policies WHERE tablename='vote_counts'` shows exactly one policy named `Vote counts visible to voters when not hidden`"
  artifacts: []
  key_links:
    - from: "migration 10 file (Plan 01)"
      to: "production Supabase project schema"
      via: "supabase db push"
      pattern: "00000000000010_results_hidden_audit"
    - from: "toggle-results-visibility EF (Plan 02)"
      to: "production Supabase Edge Functions runtime"
      via: "supabase functions deploy toggle-results-visibility"
      pattern: "toggle-results-visibility"
---

<objective>
[BLOCKING] Deploy gate. Apply migration 10 to the production Supabase project and deploy the new `toggle-results-visibility` EF + the 12 retrofitted EFs (11 from Plan 11-03 batches A+B and `create-poll` from Plan 11-03b). Without this plan, the Phase 11 verification state is a false positive — build and type checks pass because types come from local config, not the live database; the prod DB still has the v1.1 schema and the old `vote_counts` policy until `supabase db push` is run.

Purpose: ROADMAP SC-3 and SC-5 require the EF to be deployed and reachable. SC-1 and SC-2 require migration 10 applied to the production DB. This plan is the bridge from "code merged" to "phase shipped."

Output: No file changes. Successful deploy commands logged in the SUMMARY.

NB: This plan is `autonomous: false` because `supabase db push` and `supabase functions deploy` may prompt for confirmation in TTY mode. The schema_push_requirement block in the planning context indicates `SUPABASE_ACCESS_TOKEN` is the env-var workaround for CI; in interactive local execution, the user authorizes the prompts directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-VALIDATION.md

<!-- Plan outputs that this plan deploys -->
@supabase/migrations/00000000000010_results_hidden_audit.sql
@supabase/functions/toggle-results-visibility/index.ts
@supabase/functions/_shared/audit.ts

<interfaces>
<!-- Deploy commands and expected outputs -->

Local pre-merge sanity (REVIEW-FIX-H7 — no `db push` in this gate; targets LOCAL only):
  supabase db reset                    # drops + re-applies all 11 migrations against local stack ONLY
  npm run test                         # unit suite green
  npm run test:integration             # TEST-11 (13 cases) + TEST-12 (7 cases) + create-poll-results-hidden (4 cases) green against local stack
  supabase projects list               # confirm production project is linked (no remote action)

Production deploy (13 EFs total — 1 new + 12 retrofitted) — REVIEW-FIX-H8 deploy flags:
  export PROD_PROJECT_REF="<production ref>"
  export SUPABASE_ACCESS_TOKEN="<from CI secrets or `supabase auth login`>"
  supabase db push --linked                                                                              # applies migration 10 to prod (the ONLY db push in this phase)
  supabase functions deploy toggle-results-visibility --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt   # new EF (Plan 02)
  for ef in close-expired-polls close-poll create-category create-poll \
            delete-category delete-poll demote-admin pin-poll \
            promote-admin rename-category set-resolution update-poll; do
    supabase functions deploy "$ef" --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt           # 12 retrofitted EFs
                                                                                                         # (11 from Plan 11-03 + create-poll from Plan 11-03b)
  done

Post-deploy smoke (REVIEW-FIX-M9 — uses a dedicated test poll, NOT a real prod poll):
  # Step 1: create the smoke poll via create-poll EF (also exercises Plan 11-03b retrofit)
  SMOKE_POLL_ID=$(curl ... create-poll ... | jq -r '.poll.id')

  # Step 2: admin probe — REVIEW-FIX-M8
  curl -X POST "https://${PROD_PROJECT_REF}.functions.supabase.co/toggle-results-visibility" \
    -H "Authorization: Bearer $ADMIN_JWT" -H "Content-Type: application/json" \
    -d "{\"poll_id\":\"$SMOKE_POLL_ID\", \"hidden\": true}"
  # expect: HTTP/2 200 + { "poll": { ..., "results_hidden": true, ... } }

  # Step 3: non-admin probe — REVIEW-FIX-M8
  curl -X POST "https://${PROD_PROJECT_REF}.functions.supabase.co/toggle-results-visibility" \
    -H "Authorization: Bearer $MEMBER_JWT" -H "Content-Type: application/json" \
    -d "{\"poll_id\":\"$SMOKE_POLL_ID\", \"hidden\": false}"
  # expect: HTTP/2 403

  # Step 4: cleanup the smoke poll
  curl ... delete-poll ... ; DELETE FROM audit_log WHERE target_id='$SMOKE_POLL_ID' (service-role SQL)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 05-01: [BLOCKING] Local pre-merge sanity check</name>
  <files></files>
  <read_first>
    - .planning/phases/11-schema-rls-ef-foundations/11-VALIDATION.md § "Manual-Only Verifications" (the row "Migration 10 applies cleanly against a fresh DB")
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § "Integration Points" (migration apply order; CI job structure)
  </read_first>
  <what-built>
    Phase 11 code is merged or queued for merge: migration 10 + audit helper + new EF + 12 EF retrofits (11 from Plan 11-03 + create-poll from Plan 11-03b) + integration test suite. This task is the local sanity gate BEFORE pushing to production.
  </what-built>
  <action>
    REVIEW-FIX-H7: Run the local pre-merge sequence in order; STOP on any non-zero exit. CRITICAL: do NOT run `supabase db push` in this local gate — that command pushes to the LINKED remote project (potentially production), not the local stack. The local-stack-only validation uses `db reset` exclusively:

    1. `supabase db reset` — drops the local Supabase Postgres and re-applies all 11 migrations (00 through 10). Expect: log shows 11 migrations applied in order with no DDL errors. This is the LOCAL apply test for migration 10; no remote interaction.
    2. `npm run test` — unit suite (includes `polls-effective-invariant.test.ts` which probes the new view shape). Expect: all unit tests pass.
    3. `npm run test:integration` — TEST-11 (13 cases: 12-cell matrix + 1 admin-JWT sanity, REVIEW-FIX-M7) + TEST-12 (7 cases: 4 happy + 3 negative, REVIEW-FIX-L3) + create-poll-results-hidden (4 cases, REVIEW-FIX-M5) against the local stack. Expect: 24 tests pass total (13 + 7 + 4).
    4. Confirm production project linkage WITHOUT pushing: `supabase projects list` — verify the linked-project row (marked with ●) matches the production polls.wtcsmapban.com Supabase project ref. If unlinked or linked to the wrong project, run `supabase link --project-ref <prod-ref>` BUT DO NOT RUN `db push` IN THIS TASK — that is Task 05-02 step 2's job.

    If any of steps 1-3 fail, STOP — do not proceed to Task 05-02 (prod deploy). Surface the failure in CONTEXT.md as a blocker and replan.
  </action>
  <how-to-verify>
    Run these commands in order and capture each exit code + the last 20 lines of output:

    1. `supabase db reset` → exit 0; output contains all 11 migration filenames in order including `00000000000010_results_hidden_audit`
    2. `npm run test` → exit 0; output reports unit pass count
    3. `npm run test:integration` → exit 0; output reports 13 + 7 + 4 = 24 integration cases passing
    4. `supabase projects list` shows the production project linked

    REVIEW-FIX-H7: Confirm `supabase db push` was NOT run in this task — production schema apply happens in Task 05-02 step 2 ONLY, after local sanity has fully passed.
  </how-to-verify>
  <verify>
    <automated>supabase db reset 2>&1 | tail -20 | grep -E "Applying migration.*00000000000010" && npm run test 2>&1 | grep -E "(passed|PASS)" | tail -3 && npm run test:integration 2>&1 | grep -E "(passed|PASS)" | tail -3 && supabase projects list 2>&1 | grep -E "●"</automated>
  </verify>
  <resume-signal>Type "local-green" to proceed to production deploy, or paste failure output if any step failed.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 05-02: [BLOCKING] Production schema push + EF deploy (13 EFs)</name>
  <files></files>
  <read_first>
    - .planning/phases/11-schema-rls-ef-foundations/11-VALIDATION.md § "Manual-Only Verifications" (the row "toggle-results-visibility EF reachable in deployed Supabase project")
    - .planning/PROJECT.md § Constraints (production Supabase project ref is the polls.wtcsmapban.com project — NOT the sister map-vote project)
  </read_first>
  <what-built>
    Local sanity passed. This task pushes migration 10 to production Supabase AND deploys 13 EFs total: the new `toggle-results-visibility` (Plan 02) + 11 retrofitted EFs from Plan 11-03 + the retrofitted `create-poll` from Plan 11-03b.
  </what-built>
  <action>
    Run these commands in order against the linked PRODUCTION project. STOP on any non-zero exit and surface as a blocker.

    Set environment variables FIRST so deploy commands are deterministic:
      ```
      export PROD_PROJECT_REF="<from supabase projects list — the production polls.wtcsmapban.com project ref>"
      export SUPABASE_ACCESS_TOKEN="<from supabase auth login or CI secrets>"
      ```

    1. Confirm production link: `supabase projects list` — verify the row with "●" (linked) marker is the production polls.wtcsmapban.com project AND that the ref matches `$PROD_PROJECT_REF`. If not, `supabase link --project-ref $PROD_PROJECT_REF` first.

    2. REVIEW-FIX-H7: Push migration 10 to prod: `supabase db push --linked` (the `--linked` flag is the explicit form; without it, recent CLI versions may print a confirmation prompt naming the linked project, which is informational only). This is the FIRST time `db push` is run in this phase's deploy — Task 05-01 deliberately omits it. In CI / non-TTY mode the `SUPABASE_ACCESS_TOKEN` env var avoids the interactive auth prompt; the push-confirmation prompt is still TTY-only and requires the user in interactive mode.

    3. REVIEW-FIX-H8: Deploy the new EF (Plan 02 output) with explicit production flags:
       ```
       supabase functions deploy toggle-results-visibility --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt
       ```
       Expect: log shows "Deployed Function toggle-results-visibility..." + a function URL. The `--no-verify-jwt` flag is required because the EF verifies JWTs itself via `requireAdmin` (platform-level JWT verification would double-check and reject admin requests on the service-role-backed paths). The `--use-api` flag uses the management API path which is more reliable for CI/non-TTY deploys. The `--project-ref` flag explicitly targets production.

    4. REVIEW-FIX-H8: Deploy the 12 retrofitted EFs (11 from Plan 11-03 + create-poll from Plan 11-03b) with the same flag set. Run as a shell loop to keep it deterministic:
       ```
       for ef in close-expired-polls close-poll create-category create-poll delete-category delete-poll demote-admin pin-poll promote-admin rename-category set-resolution update-poll; do
         supabase functions deploy "$ef" --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt \
           || { echo "FAILED: $ef"; exit 1; }
       done
       ```
       Expect: 12 successful deploys, no FAILED line. Total deployed in this plan: 13 EFs (1 new + 12 retrofitted, including `create-poll`).

    Cross-reference: If the project has a `package.json` script `deploy:functions:all` (or similar) that wraps the existing deploy workflow, REUSE that script rather than the per-function loop. Codex correctly noted that the project may already have an established deploy workflow with the same flag set; reusing it eliminates drift between Phase 11 and prior deploys.

    5. REVIEW-FIX-L4: Post-deploy DB verification via `psql` (or Supabase SQL editor):
       - `SELECT column_name, data_type, is_nullable, column_default FROM information_schema.columns WHERE table_schema='public' AND table_name='polls' AND column_name IN ('results_hidden', 'results_hidden_changed_at')` — expect 2 rows; for `results_hidden`: type=`boolean`, is_nullable=`NO`, column_default=`false` (or `false`::boolean); for `results_hidden_changed_at`: type=`timestamp with time zone`, is_nullable=`YES`, column_default=`NULL`. The `column_default = 'false'` assertion is the REVIEW-FIX-L4 addition — confirms migration 10's DEFAULT clause applied.
       - `SELECT * FROM pg_policies WHERE tablename='vote_counts'` — expect exactly one policy named `Vote counts visible to voters when not hidden`, `roles={authenticated}` (no `public` or `anon`). REVIEW-FIX-H3 assertion: the policy's `qual` (USING expression) must NOT contain `is_current_user_admin()` — `grep` for it in the qual output: `qual` should match `EXISTS (...votes...) AND EXISTS (...polls.results_hidden = false...)` only.
       - `SELECT pg_get_viewdef('public.polls_effective')` — expect output contains `results_hidden` and `results_hidden_changed_at` in the column list.
       - `SELECT relname, relrowsecurity FROM pg_class WHERE relname='audit_log'` — expect `relrowsecurity=t` (RLS enabled).

    6. REVIEW-FIX-M8 + M9: Post-deploy EF smoke test using a dedicated test poll (NOT a real community poll):

       a. Obtain admin and member JWTs (Discord-login in the prod UI for each, copy from devtools). Store as `$ADMIN_JWT` and `$MEMBER_JWT` in the shell environment.

       b. Create a dedicated smoke test poll via the deployed `create-poll` EF (also exercises Plan 11-03b's retrofit):
          ```
          SMOKE_POLL=$(curl -s -X POST "https://${PROD_PROJECT_REF}.functions.supabase.co/create-poll" \
            -H "Authorization: Bearer $ADMIN_JWT" \
            -H "Content-Type: application/json" \
            -d '{"title":"[SMOKE 11-05] post-deploy verification","description":"deploy-time smoke; safe to archive","category_id":"<archived/internal category uuid>","choices":[{"text":"a"},{"text":"b"}],"closes_at":"<now+1d ISO>","results_hidden":false}')
          SMOKE_POLL_ID=$(echo "$SMOKE_POLL" | jq -r '.poll.id')
          echo "Smoke poll: $SMOKE_POLL_ID"
          ```

       c. REVIEW-FIX-M8 — admin probe (expect 200 + updated row):
          ```
          curl -i -X POST "https://${PROD_PROJECT_REF}.functions.supabase.co/toggle-results-visibility" \
            -H "Authorization: Bearer $ADMIN_JWT" \
            -H "Content-Type: application/json" \
            -d "{\"poll_id\":\"$SMOKE_POLL_ID\", \"hidden\": true}"
          ```
          Expect: HTTP/2 200 + body `{ "poll": { ..., "results_hidden": true, "results_hidden_changed_at": "..." } }`.

       d. REVIEW-FIX-M8 — non-admin probe (expect 403):
          ```
          curl -i -X POST "https://${PROD_PROJECT_REF}.functions.supabase.co/toggle-results-visibility" \
            -H "Authorization: Bearer $MEMBER_JWT" \
            -H "Content-Type: application/json" \
            -d "{\"poll_id\":\"$SMOKE_POLL_ID\", \"hidden\": false}"
          ```
          Expect: HTTP/2 403. Confirm via a subsequent admin SELECT (or service-role SQL) that `results_hidden` is STILL `true` (the non-admin call was a hard stop, not a partial execute).

       e. Confirm audit rows: `SELECT count(*), action FROM audit_log WHERE target_id='$SMOKE_POLL_ID' AND created_at > now() - interval '5 minutes' GROUP BY action` — expect ≥2 rows total, exactly 1 `poll_created` and exactly 1 `results_hidden_toggled` (none for the failed 403 — REVIEW-FIX-M6 enforcement).

       f. REVIEW-FIX-M9 cleanup — delete the smoke poll AND its audit rows:
          ```
          curl -X POST "https://${PROD_PROJECT_REF}.functions.supabase.co/delete-poll" \
            -H "Authorization: Bearer $ADMIN_JWT" \
            -H "Content-Type: application/json" \
            -d "{\"poll_id\":\"$SMOKE_POLL_ID\"}"
          # Then via service-role SQL (audit_log has no FK to polls — cascade does not clean it):
          DELETE FROM audit_log WHERE target_id = '$SMOKE_POLL_ID';
          ```
          The `delete-poll` EF itself writes a `poll_deleted` audit row before the cascade — accept this row but include it in the cleanup DELETE above.

    If any of the above fails, STOP. Document the failure mode in CONTEXT.md and replan (likely a hotfix Plan 06 or a rollback via a follow-up migration).
  </action>
  <how-to-verify>
    Capture the output of each command. Specifically confirm:
    - `supabase db push --linked` log includes "Applying migration 00000000000010" (or equivalent post-push confirmation)
    - 13 `supabase functions deploy <name> --project-ref $PROD_PROJECT_REF --use-api --no-verify-jwt` commands return exit 0 (1 new EF + 12 retrofits, including `create-poll`) — REVIEW-FIX-H8
    - psql verification queries return the expected shape (per Action steps 5a-d), including REVIEW-FIX-L4's `column_default='false'` check
    - REVIEW-FIX-M8: BOTH admin probe (200) AND non-admin probe (403) succeed
    - REVIEW-FIX-M9: Smoke poll was created via deploy step (not a real community poll) and cleaned up at the end
    - Audit row count is correct after the smoke test (1 `poll_created` from create + 1 `results_hidden_toggled` from admin probe + 0 from the 403 probe + 1 `poll_deleted` from cleanup)
  </how-to-verify>
  <verify>
    <automated>supabase functions list 2>&1 | grep -E "toggle-results-visibility" && echo "EF deployed"</automated>
  </verify>
  <resume-signal>Type "prod-deployed" once migration 10 is live and all 13 EFs are deployed + smoke-tested, or paste failure output to trigger a hotfix replan.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 05-03: Phase 11 SUMMARY + ROADMAP marker</name>
  <files>.planning/phases/11-schema-rls-ef-foundations/11-05-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/11-schema-rls-ef-foundations/11-00-SUMMARY.md through 11-04-SUMMARY.md plus 11-03b-SUMMARY.md (all six prior plan summaries — for the consolidated requirements-completed list)
    - .planning/ROADMAP.md (Phase 11 entry — mark as in-progress / complete based on user signal)
    - .planning/STATE.md (Current Position section — update to reflect Phase 11 shipped)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Create `.planning/phases/11-schema-rls-ef-foundations/11-05-SUMMARY.md` per the template:
    - Capture the local sanity log + production deploy log + smoke-test evidence (psql shapes + curl output)
    - List `requirements-completed: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05, VIS-09, TEST-11, TEST-12]` (all 8 Phase 11 reqs)
    - List `requirements-deferred: []`
    - "Open follow-ups" section: any items flagged by Plan 11-03b's SUMMARY (e.g., whether Plan 04 grew a fifth integration case for the `create-poll → { results_hidden: true }` path), plus the ROADMAP SC-1 "14 migrations" typo (codebase has 10 pre-existing + 1 new = 11)

    Update `.planning/ROADMAP.md`:
    - Mark Phase 11 row in the Progress table as `7/7 complete` and `Status: Shipped` with today's date (was 6/6 prior to the Plan 11-03b split).
    - In the Phase 11 detailed section, fix the SC-1 wording: "all 14 pre-existing migrations" → "all 10 pre-existing migrations (00–09)" (the typo flagged in Plan 01's success criteria).
    - Update the v1.2 milestone progress bar at the bottom of the file.

    Update `.planning/STATE.md`:
    - Bump `progress.completed_phases: 1` and `progress.completed_plans: 7` (Plans 00, 01, 02, 03, 03b, 04, 05).
    - Update `last_activity` and `status` to indicate Phase 11 shipped.
    - Move `Current Position` to "Phase 12 — Admin UI + User UI + UIDN-03 Sweep — Ready to plan".
  </action>
  <acceptance_criteria>
    - `.planning/phases/11-schema-rls-ef-foundations/11-05-SUMMARY.md` exists with frontmatter `requirements-completed` listing all 8 Phase 11 reqs
    - `.planning/ROADMAP.md` Phase 11 row marked as shipped: `grep -E "Phase 11.*Shipped" .planning/ROADMAP.md` returns ≥1 match
    - ROADMAP SC-1 wording is corrected from "14 pre-existing migrations" to "10 pre-existing migrations" (or equivalent that names the actual count): `grep -c "14 pre-existing migrations" .planning/ROADMAP.md` returns 0
    - `.planning/STATE.md` `completed_phases` is at least 1 and `completed_plans` is at least 7: parse YAML frontmatter and confirm
    - `.planning/STATE.md` Current Position section names Phase 12 as the next action
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/11-schema-rls-ef-foundations/11-05-SUMMARY.md && grep -c "14 pre-existing migrations" .planning/ROADMAP.md | xargs test 0 -eq && grep -E "Phase 11.*(Shipped|✅)" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    SUMMARY captures the deploy evidence; ROADMAP marks Phase 11 shipped + corrects the migration-count typo; STATE.md hands off to Phase 12.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → production Supabase | The deploy commands run by the user/CI cross this boundary; misdirected pushes (e.g., to a staging project) would corrupt the prod schema |
| Migration 10 → prod DB | Single atomic apply; any failure leaves the prod DB in an inconsistent intermediate state if statements are non-transactional |
| EF deploy → prod Functions runtime | New + retrofitted EFs are public-facing once deployed; an EF that fails to import `_shared/audit.ts` would 500 at runtime, breaking admin operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-DEPLOY-WRONG-PROJECT | Tampering | `supabase db push` linked project | mitigate | Task 05-02 step 1 explicitly verifies the linked project ref via `supabase projects list` before push. The polls.wtcsmapban.com project is the only valid push target for this phase. REVIEW-FIX-H7: Task 05-01 deliberately omits `supabase db push` to avoid an accidental remote push during the local sanity gate; `db push` runs ONLY in Task 05-02 step 2, AFTER the production-link confirmation in step 1. REVIEW-FIX-H8: Deploy commands pass `--project-ref $PROD_PROJECT_REF` explicitly, which fails fast if the access token is scoped to a different project. |
| T-MIGRATION-PARTIAL-APPLY | Tampering | Migration 10 apply | mitigate | Migration 10 is a single SQL file; Supabase wraps each migration in a transaction (verified in past v1.0 migrations 5, 7, 8, 9). If any statement fails, the whole migration rolls back — no partial state. Task 05-01 local `db reset` proves the full migration applies cleanly first. |
| T-EF-IMPORT-DRIFT | Tampering | 13 EFs depend on `_shared/audit.ts` | mitigate | All 13 deploys run sequentially; a deploy failure on any single EF aborts the loop (`|| exit 1`). The shared helper is deployed implicitly with each EF (Deno bundles imports). Smoke curl confirms the new EF returns 200 (not 500-on-import-error). |
| T-AUTH-LEAK-IN-LOG | Information Disclosure | Smoke curl logs admin JWT | mitigate | Smoke test uses a real admin JWT, which is sensitive. The SUMMARY captures evidence as `Bearer eyJ...` truncated to first 10 chars; full token is NOT pasted into the SUMMARY or commit messages. |
| T-AUDIT-SMOKE-LEAK | Information Disclosure | Smoke audit rows in prod audit_log | accept | The smoke test creates 2 audit rows on the test poll. These rows persist (audit_log is append-only by design). Acceptable because the audit_log is admin-only-visible and the rows are accurately attributed to the admin who ran the smoke. |
| T-ROLLBACK-RISK | Denial of Service | Mis-applied migration 10 | mitigate (procedural) | If smoke test fails: do NOT attempt to manually DROP the new policy / view / table — write a rollback migration 10.1 that reverses the changes (DROP POLICY + restore old policy from migration 5; ALTER TABLE polls DROP COLUMN; DROP TABLE audit_log) and push it. Plan 05 itself does NOT contain a rollback SQL because rollback design depends on the failure mode; surface as a hotfix plan when needed. |
</threat_model>

<verification>
- `supabase db reset` (local) applies all 11 migrations cleanly
- `supabase db push` (prod) applies migration 10 to the production project
- `supabase functions deploy` succeeds for 13 EFs (1 new + 12 retrofits, including `create-poll`)
- Post-deploy psql confirms the new columns, the new policy, the rewritten view, and the new table with RLS enabled
- Smoke curl returns 200 on admin invocation; the no-op idempotency holds in production
- Phase 11 SUMMARY committed; ROADMAP marks Phase 11 shipped; STATE.md hands off to Phase 12
</verification>

<success_criteria>
- ROADMAP SC-1 satisfied (migration 10 applied; all prior migrations continue to pass)
- ROADMAP SC-2 satisfied (12-cell matrix passes — already proven in Plan 04 against local; production schema matches local so the assertion holds)
- ROADMAP SC-3 satisfied (admin EF authz: 403/200; audit row written — proven by smoke + Plan 04)
- ROADMAP SC-4 satisfied (`polls_effective` exposes new columns; `security_invoker = on` preserved — psql verifies)
- ROADMAP SC-5 satisfied (EF deployed and reachable — smoke curl confirms)
- Phase 11 shipped; v1.2 milestone advances from 0/3 to 1/3 phases complete
</success_criteria>

<output>
This task's output IS the Phase 11 SUMMARY (Task 05-03). Commit message: `feat(11): ship Phase 11 — Schema + RLS + EF foundations`. Include the Co-Authored-By footer per CLAUDE.md GSD policy.

After commit, surface to the user: "Phase 11 shipped. Ready to `/gsd-plan-phase 12` for Admin UI + User UI + UIDN-03 Sweep."
</output>
</content>
</invoke>