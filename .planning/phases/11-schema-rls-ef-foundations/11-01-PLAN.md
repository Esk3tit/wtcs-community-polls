---
phase: 11-schema-rls-ef-foundations
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - supabase/migrations/00000000000010_results_hidden_audit.sql
autonomous: true
requirements:
  - VIS-01
  - VIS-04
  - VIS-09
tags:
  - migration
  - rls
  - supabase
  - schema

must_haves:
  truths:
    - "Migration 10 applies cleanly against a fresh DB after `supabase db reset` — all 10 prior migrations (00–09) run first, then migration 10 with no DDL errors"
    - "`polls.results_hidden boolean NOT NULL DEFAULT false` column exists and defaults to `false` for all pre-existing poll rows (backwards compat — preserves RSLT-05 v1.0 behavior)"
    - "`polls.results_hidden_changed_at timestamptz` column exists and is NULL for all pre-existing rows"
    - "`audit_log` table exists with columns id (uuid PK gen_random_uuid), actor_id (uuid FK profiles, NULLABLE), action (text NOT NULL), target_type (text NOT NULL), target_id (uuid), before (jsonb), after (jsonb), created_at (timestamptz NOT NULL DEFAULT now())"
    - "`audit_log` has indexes on `(target_type, target_id)` and on `(actor_id, created_at DESC)` — NOT on `before`/`after` JSONB (Pitfall 6)"
    - "`audit_log` has RLS ENABLED with an admin-only SELECT policy gated by `public.is_current_user_admin()` from migration 9; NO INSERT/UPDATE/DELETE policies (writes are service-role only)"
    - "`polls_effective` view rewritten via `CREATE OR REPLACE VIEW` — re-projects every prior column in explicit list (D-18 form, not `SELECT *`), PLUS `results_hidden` and `results_hidden_changed_at` from `polls`"
    - "`ALTER VIEW public.polls_effective SET (security_invoker = on)` re-applied IN THE SAME MIGRATION FILE immediately after `CREATE OR REPLACE VIEW` (Pitfall 2 — Postgres 15+ resets the flag on REPLACE)"
    - "Existing `polls-effective-invariant.test.ts` at `src/__tests__/admin/` continues to pass — no new `from('polls')` reads anywhere in `src/`"
    - "The old `vote_counts` SELECT policy is DROPPED (both `\"Vote counts visible to voters\"` from migration 1 and `\"Vote counts visible to voters or admin\"` from migration 5 names — use `DROP POLICY IF EXISTS` for both) BEFORE the new policy is CREATEd (no OR-permissive combination — locked by VIS-04)"
    - "The new `vote_counts` SELECT policy is scoped `TO authenticated` (Pitfall 1 — anon must have NO policy granting SELECT, so anon returns 0 rows by absence; do not add a public/anon policy)"
    - "REVIEW-FIX-H3: The new policy expression is `EXISTS (SELECT 1 FROM votes v WHERE v.poll_id = vote_counts.poll_id AND v.user_id = auth.uid()) AND EXISTS (SELECT 1 FROM polls p WHERE p.id = vote_counts.poll_id AND p.results_hidden = false)` — NO `is_current_user_admin() OR` branch. VIS-04 explicitly states 'Admin service-role bypass remains' (service-role only, not JWT-authenticated admin). All admin reads of `vote_counts` in production go through Edge Functions that use the service-role client (which bypasses RLS automatically). Removing the `is_current_user_admin() OR` clause eliminates the policy/spec ambiguity Codex flagged HIGH and makes the 12-cell matrix (authenticated × *) deterministically apply the voter+hidden rule with no admin-bypass loophole. This is a strict reading of VIS-04 — service-role bypass remains; JWT-authenticated admins must follow the same voter+hidden gate as any other authenticated user when reading via their own JWT."
    - "D-01: `audit_log` is CREATED in migration 10 (not pre-existing); the same atomic migration also adds the `results_hidden` column, rewrites `polls_effective`, and rewrites the `vote_counts` policy — single migration file, no split"
    - "D-02: `audit_log` schema matches the locked baseline columns exactly — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `actor_id uuid REFERENCES public.profiles(id)` (NULLABLE), `action text NOT NULL`, `target_type text NOT NULL`, `target_id uuid` (NULLABLE), `before jsonb` (NULLABLE), `after jsonb` (NULLABLE), `created_at timestamptz NOT NULL DEFAULT now()`"
    - "D-04: `audit_log` RLS is admins-only SELECT, gated by `public.is_current_user_admin()` from migration 9; no public reads; no INSERT/UPDATE/DELETE policies declared (writes are service-role only via the EFs, which bypass RLS automatically)"
    - "D-14: The new `vote_counts` SELECT policy is implemented as DROP-then-CREATE (no OR-permissive layering with the prior policy); both legacy policy names (`\"Vote counts visible to voters\"` and `\"Vote counts visible to voters or admin\"`) are dropped before the rewritten policy is created — wording locked by VIS-04"
    - "D-15: The new policy expression checks BOTH that the caller has cast a vote (existing `EXISTS (SELECT 1 FROM votes ...)` clause kept in shape) AND that `polls.results_hidden = false` (new clause); service-role bypasses RLS automatically (no explicit service_role policy added). REVIEW-FIX-H3: NO `is_current_user_admin() OR` branch — VIS-04 mandates service-role bypass only, not JWT-authenticated admin bypass."
    - "REVIEW-FIX-M3: Supabase DB types (`src/lib/database.types.ts` or equivalent) regeneration is DEFERRED to Phase 12 (the consumer phase). Phase 11 plan body explicitly notes that `npx supabase gen types typescript --linked > src/lib/database.types.ts` (or the project's existing generator path) is a Phase 12 Wave 1 prerequisite. This keeps Phase 11 scoped strictly to DB+EF foundations and avoids carrying a frontend-typed dependency across the Phase 11 ship. Addresses Codex MEDIUM concern by making the deferral explicit and assigning ownership."
    - "D-16: The `polls.results_hidden = false` check is implemented via subquery or JOIN form (planner picked subquery per RESEARCH Pattern 1); PK-lookup overhead is acceptable at the project's 20–30 concurrent user scale per RESEARCH Performance Traps"
    - "D-17: `polls_effective` is rewritten with `CREATE OR REPLACE VIEW` that re-projects every existing column (preserving D-12 lazy-close pattern from migration 5) PLUS `results_hidden` and `results_hidden_changed_at` from `polls`; `ALTER VIEW polls_effective SET (security_invoker = on)` is re-applied immediately after in the same migration file"
  artifacts:
    - path: "supabase/migrations/00000000000010_results_hidden_audit.sql"
      provides: "Atomic migration: polls columns + audit_log + view rewrite + vote_counts policy"
      min_lines: 80
      contains: "ALTER VIEW public.polls_effective SET (security_invoker = on)"
  key_links:
    - from: "vote_counts SELECT policy"
      to: "polls.results_hidden column"
      via: "EXISTS subquery on polls.results_hidden = false"
      pattern: "polls\\.results_hidden\\s*=\\s*false"
    - from: "polls_effective view"
      to: "polls.results_hidden + polls.results_hidden_changed_at"
      via: "explicit column list in CREATE OR REPLACE VIEW"
      pattern: "results_hidden,\\s*\\n\\s*results_hidden_changed_at"
    - from: "audit_log RLS"
      to: "is_current_user_admin() (migration 9)"
      via: "SELECT policy USING clause"
      pattern: "public\\.is_current_user_admin\\(\\)"
---

<objective>
Land migration 10 — the single atomic schema change for Phase 11. Adds `results_hidden` + `results_hidden_changed_at` columns to `polls`, creates the `audit_log` table + its admin-only SELECT RLS, rewrites `polls_effective` to project the two new columns (preserving the explicit-column form + re-applying `security_invoker = on`), and rewrites the `vote_counts` SELECT policy to honor `results_hidden`.

Purpose: This migration is the security boundary that v1.2 hangs on. The `vote_counts` RLS rewrite is a merge blocker (P0 callout in STATE.md). Atomicity matters because splitting the column ADD, policy DROP+CREATE, and view rewrite across migrations creates a window where the policy references a column not yet projected through the view.

Output: One new SQL file at `supabase/migrations/00000000000010_results_hidden_audit.sql`. No EF or test changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md
@.planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md

<!-- Migration analog sources the executor MUST read before writing migration 10 -->
@supabase/migrations/00000000000000_schema.sql
@supabase/migrations/00000000000001_rls.sql
@supabase/migrations/00000000000005_admin_phase4.sql
@supabase/migrations/00000000000009_admin_integrity_rls.sql
@src/__tests__/admin/polls-effective-invariant.test.ts

<interfaces>
<!-- Identifiers downstream plans (02 EF, 03 retrofit, 04 tests) depend on -->

Column adds (referenced by Plan 02, Plan 03, Plan 04):
  polls.results_hidden        boolean NOT NULL DEFAULT false
  polls.results_hidden_changed_at timestamptz NULL

Table (referenced by Plan 02 audit helper, Plan 03 retrofit, Plan 04 audit assertions):
  public.audit_log (id uuid PK, actor_id uuid? FK profiles, action text, target_type text, target_id uuid?, before jsonb, after jsonb, created_at timestamptz)

Policy names (Plan 04 12-cell matrix probes the post-rewrite policy):
  DROP: "Vote counts visible to voters"            -- migration 1
  DROP: "Vote counts visible to voters or admin"   -- migration 5
  CREATE: "Vote counts visible to voters when not hidden"

View columns (Plan 02 toggle EF returns updated poll row; Phase 12 React reads):
  polls_effective.results_hidden
  polls_effective.results_hidden_changed_at

Function reused (defined in migration 9, called by audit_log RLS USING clause):
  public.is_current_user_admin() returns boolean (SECURITY DEFINER STABLE)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 01-01: Migration 10 — column adds + audit_log table + RLS</name>
  <files>supabase/migrations/00000000000010_results_hidden_audit.sql</files>
  <read_first>
    - supabase/migrations/00000000000000_schema.sql (confirm `pgcrypto` extension is loaded — `gen_random_uuid()` is available without redeclaration; confirm `profiles` table for FK reference)
    - supabase/migrations/00000000000005_admin_phase4.sql (lines 1-9 banner-comment style; lines 12-39 explicit-column view DDL — the exact column list to preserve)
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (full file — `is_current_user_admin()` signature for the audit_log RLS USING clause)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "supabase/migrations/00000000000010_results_hidden_audit.sql" (full section — banner, section header, view rewrite verbatim, policy DROP+CREATE pattern)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pattern 1: Atomic Migration 10" (full pattern, lines 246-360 — DDL skeleton)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-01 through D-19 (locked schema decisions)
  </read_first>
  <action>
    Create `supabase/migrations/00000000000010_results_hidden_audit.sql` as a single file with four SECTION blocks in this exact order (statement order is load-bearing per RESEARCH Pattern 1):

    Top banner: `-- Phase 11: Schema + RLS + EF Foundations` + brief one-line description. Use the banner-divider style from `supabase/migrations/00000000000005_admin_phase4.sql:1-9`. Do not include plan/round/PR archaeology (per CLAUDE.md project comment policy — WHY-only, never review-round tags).

    SECTION 1 — polls columns (D-01, VIS-01):
      `ALTER TABLE public.polls ADD COLUMN results_hidden BOOLEAN NOT NULL DEFAULT FALSE;`
      `ALTER TABLE public.polls ADD COLUMN results_hidden_changed_at TIMESTAMPTZ;`
      Add `COMMENT ON COLUMN` for both (per RESEARCH Pattern 1 verbatim comment text).

    SECTION 2 — audit_log table + RLS (D-01, D-02, D-03, D-04, Pitfall 6):
      `CREATE TABLE public.audit_log (...)` exactly per D-02 schema baseline — `id uuid PK DEFAULT gen_random_uuid()`, `actor_id uuid REFERENCES public.profiles(id)` (NULLABLE — no NOT NULL — to admit cron `actor_id=NULL` writes per D-03), `action text NOT NULL`, `target_type text NOT NULL`, `target_id uuid` (NULLABLE), `before jsonb` (NULLABLE), `after jsonb` (NULLABLE), `created_at timestamptz NOT NULL DEFAULT now()`.
      Indexes (Pitfall 6 — do NOT index `before`/`after` JSONB): `CREATE INDEX idx_audit_log_target ON public.audit_log(target_type, target_id);` and `CREATE INDEX idx_audit_log_actor_created ON public.audit_log(actor_id, created_at DESC);`.
      `COMMENT ON TABLE public.audit_log IS 'Append-only admin-action audit trail. Written by admin-gated EFs (13 emitters in v1.2). Admins-only SELECT. actor_id NULL on cron-driven writes.';`
      `ALTER TABLE public.audit_log ENABLE ROW LEVEL SECURITY;`
      `CREATE POLICY "Audit log visible to admins" ON public.audit_log FOR SELECT TO authenticated USING (public.is_current_user_admin());` (D-04 — admin-only).
      Add an SQL comment line documenting: NO INSERT/UPDATE/DELETE policies declared — writes are service-role only via EFs (RLS bypass automatic for service-role).

    SECTION 3 — polls_effective view rewrite (D-17, D-18, D-19, VIS-09, Pitfall 2):
      `CREATE OR REPLACE VIEW public.polls_effective AS SELECT ...` — preserve the explicit-column form from migration 5. The exact column list (in order): `id, title, description, category_id, image_url, CASE WHEN status = 'active' AND closes_at < now() THEN 'closed' ELSE status END AS status, resolution, is_pinned, created_by, closes_at, closed_at, created_at, updated_at, status AS raw_status, results_hidden, results_hidden_changed_at`. The lazy-close CASE expression is preserved verbatim from migration 5.
      `ALTER VIEW public.polls_effective SET (security_invoker = on);` — MUST appear IN THE SAME MIGRATION FILE IMMEDIATELY AFTER the `CREATE OR REPLACE VIEW` statement (Pitfall 2 — Postgres 15+ resets this flag on REPLACE; if this `ALTER VIEW` is in a separate migration the view runs with definer-rights for one prod-window and bypasses RLS on `polls`).
      `COMMENT ON VIEW public.polls_effective IS 'Lazy-close read view. v1.2: projects results_hidden + results_hidden_changed_at so the React read path branches on visibility without bypassing the view boundary. INVARIANT: all non-admin code paths MUST read this view, never polls directly.';`

    SECTION 4 — vote_counts SELECT policy DROP + CREATE (D-14, D-15, D-16, VIS-04, Pitfall 1):
      `DROP POLICY IF EXISTS "Vote counts visible to voters" ON public.vote_counts;` (migration 1 name)
      `DROP POLICY IF EXISTS "Vote counts visible to voters or admin" ON public.vote_counts;` (migration 5 name — drop both because the codebase may have either depending on apply history; `IF EXISTS` makes this idempotent).
      `CREATE POLICY "Vote counts visible to voters when not hidden" ON public.vote_counts FOR SELECT TO authenticated USING (EXISTS (SELECT 1 FROM public.votes WHERE votes.poll_id = vote_counts.poll_id AND votes.user_id = auth.uid()) AND EXISTS (SELECT 1 FROM public.polls WHERE polls.id = vote_counts.poll_id AND polls.results_hidden = false));`
      REVIEW-FIX-H3: NO `public.is_current_user_admin() OR` branch. VIS-04 mandates service-role bypass only; JWT-authenticated admins follow the same voter+hidden gate. Service-role bypasses RLS automatically — explicit service_role policy not added.
      Scope is `TO authenticated` (Pitfall 1) — do NOT add a public or anon policy; anon returns 0 rows by absence of any granting policy.
      Add a SQL comment explaining the gate: "Authenticated users see vote_counts iff they have voted on this poll AND results are not hidden. Admin reads of vote_counts in production go through service-role-backed Edge Functions (which bypass RLS automatically) — no JWT-admin OR-branch (VIS-04, REVIEW-FIX-H3)."

    Do not use `BEGIN; ... COMMIT;` — Supabase migrations are wrapped in a transaction by the apply tool. Do not include `END;` markers between sections — section headers are comment-only dividers.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/00000000000010_results_hidden_audit.sql` exists
    - File contains exactly one occurrence of `ALTER TABLE public.polls ADD COLUMN results_hidden BOOLEAN NOT NULL DEFAULT FALSE` (case insensitive on keywords): `grep -ic "ADD COLUMN results_hidden BOOLEAN NOT NULL DEFAULT FALSE" supabase/migrations/00000000000010_results_hidden_audit.sql` returns ≥1
    - File contains exactly one `ALTER TABLE public.polls ADD COLUMN results_hidden_changed_at TIMESTAMPTZ`: `grep -ic "ADD COLUMN results_hidden_changed_at TIMESTAMPTZ" supabase/migrations/00000000000010_results_hidden_audit.sql` returns ≥1
    - File contains `CREATE TABLE public.audit_log` and the table has `actor_id UUID REFERENCES public.profiles(id)` WITHOUT `NOT NULL` on actor_id: `grep -nE "actor_id\\s+UUID\\s+REFERENCES" supabase/migrations/00000000000010_results_hidden_audit.sql` returns a line that does NOT contain `NOT NULL`
    - File contains TWO `CREATE INDEX` statements on `audit_log` — one on `(target_type, target_id)`, one on `(actor_id, created_at DESC)`: `grep -c "CREATE INDEX.*audit_log" supabase/migrations/00000000000010_results_hidden_audit.sql` returns ≥2
    - File does NOT index `before` or `after` JSONB columns: `grep -E "CREATE INDEX.*audit_log.*(before|after)" supabase/migrations/00000000000010_results_hidden_audit.sql` returns nothing (exit code 1)
    - File contains `ALTER TABLE public.audit_log ENABLE ROW LEVEL SECURITY` and exactly one `CREATE POLICY` for audit_log (SELECT, admin-only): `grep -cE "CREATE POLICY.*audit_log" supabase/migrations/00000000000010_results_hidden_audit.sql` returns 1
    - File has `CREATE OR REPLACE VIEW public.polls_effective` followed by `ALTER VIEW public.polls_effective SET (security_invoker = on)` — and the two statements are within 30 lines of each other (Pitfall 2): `awk '/CREATE OR REPLACE VIEW public.polls_effective/{a=NR}/ALTER VIEW public.polls_effective SET .security_invoker = on./{print NR-a}' supabase/migrations/00000000000010_results_hidden_audit.sql | head -1 | awk '$1 < 30 && $1 > 0 {print "ok"}'` outputs `ok`
    - The view's explicit column list includes both `results_hidden` and `results_hidden_changed_at` (case sensitive — must be lower-case): `grep -E "^\\s*results_hidden,?\\s*(--.*)?$" supabase/migrations/00000000000010_results_hidden_audit.sql` and `grep -E "^\\s*results_hidden_changed_at" supabase/migrations/00000000000010_results_hidden_audit.sql` both return ≥1
    - File contains `DROP POLICY IF EXISTS "Vote counts visible to voters"` AND `DROP POLICY IF EXISTS "Vote counts visible to voters or admin"` (both old names dropped — migration may apply against a DB that has either)
    - File contains exactly one new vote_counts policy named `Vote counts visible to voters when not hidden` scoped `TO authenticated`: `grep -nE "CREATE POLICY \"Vote counts visible to voters when not hidden\" ON public\\.vote_counts" supabase/migrations/00000000000010_results_hidden_audit.sql` returns 1 match and the next 5 lines contain `TO authenticated`
    - The new policy USING clause references `polls.results_hidden = false`: `grep -E "polls\\.results_hidden\\s*=\\s*false" supabase/migrations/00000000000010_results_hidden_audit.sql` returns ≥1 match
    - File contains NO `TO anon` or `TO public` policy on `vote_counts` (Pitfall 1 — anon must have no granting policy): `grep -E "vote_counts.*FOR SELECT.*(TO anon|TO public)" supabase/migrations/00000000000010_results_hidden_audit.sql` returns nothing
    - File contains NO review-round / Plan / Round / PR archaeology in comments — only WHY explanations: `grep -iE "(Round [0-9]|Plan [0-9]+-[0-9]+|PR #[0-9]+|review-round)" supabase/migrations/00000000000010_results_hidden_audit.sql` returns nothing (per CLAUDE.md project comment policy)
    - Local apply succeeds (manual gate handled by Plan 05 BLOCKING task, but local `supabase db reset` smoke-checkable here): `supabase db reset --debug` exits 0 with migration 10 listed in apply log
    - Existing unit test `polls-effective-invariant.test.ts` passes against the post-migration view: `npm run test -- --run polls-effective-invariant.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -ic "ADD COLUMN results_hidden BOOLEAN NOT NULL DEFAULT FALSE" supabase/migrations/00000000000010_results_hidden_audit.sql | xargs test 1 -le && grep -ic "ADD COLUMN results_hidden_changed_at TIMESTAMPTZ" supabase/migrations/00000000000010_results_hidden_audit.sql | xargs test 1 -le && awk '/CREATE OR REPLACE VIEW public.polls_effective/{a=NR}/ALTER VIEW public.polls_effective SET .security_invoker = on./{if(a&&NR-a<30){print "ok";exit}}' supabase/migrations/00000000000010_results_hidden_audit.sql | grep -q ok && ! grep -E "vote_counts.*(TO anon|TO public)" supabase/migrations/00000000000010_results_hidden_audit.sql && npm run test -- --run polls-effective-invariant.test.ts 2>&1 | grep -E "(PASS|passed)"</automated>
  </verify>
  <done>
    Migration 10 SQL file exists, all 4 sections present in correct order, Pitfall 1 (anon no policy) + Pitfall 2 (security_invoker re-applied) + Pitfall 6 (no JSONB indexes) all enforced, the existing `polls-effective-invariant.test.ts` continues to pass against the post-migration view.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon role → vote_counts | Unauthenticated visitors must NOT see vote counts in any state — enforced by policy absence (no anon-granting policy) |
| authenticated role → vote_counts | Authenticated voters see counts ONLY when they have voted AND `polls.results_hidden = false`; otherwise 0 rows |
| service-role → vote_counts | Service-role bypasses RLS automatically (no explicit policy). JWT-authenticated admins receive NO OR-branch bypass per VIS-04 + REVIEW-FIX-H3; admin reads in production go through service-role-backed Edge Functions. |
| authenticated role → audit_log | Only admins see audit rows; non-admin authenticated users get 0 rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-RLS-LEAK | Information Disclosure | `vote_counts` SELECT policy | mitigate (P0 — merge blocker) | Policy scoped `TO authenticated` only (Pitfall 1); USING clause AND-joins voted-existence with `polls.results_hidden = false`. NO admin OR-bypass branch — VIS-04 mandates service-role bypass only (REVIEW-FIX-H3). Old policy names DROPPED before CREATE (no OR-permissive combination — locked by VIS-04). Verified by Plan 04 12-cell matrix at Wave 3. |
| T-VIEW-COLUMN-DRIFT | Tampering | `polls_effective` view rewrite | mitigate | Preserve the explicit-column form from migration 5 (no `SELECT *`); ADD only the two new columns at end of list. `ALTER VIEW ... SET (security_invoker = on)` re-applied IN THE SAME MIGRATION FILE immediately after `CREATE OR REPLACE VIEW` (Pitfall 2). Existing `polls-effective-invariant.test.ts` enforces no new `from('polls')` reads in `src/`. |
| T-MIGRATION-NONIDEMPOTENT | Tampering | Migration 10 apply | mitigate | All policy DROPs use `DROP POLICY IF EXISTS` (covers both migration 1 and migration 5 old names — either may be live depending on apply history). Plan 05 BLOCKING task runs `supabase db reset && supabase db push` to confirm full apply against fresh DB. |
| T-AUDIT-RLS-LEAK | Information Disclosure | `audit_log` SELECT policy | mitigate | RLS ENABLED; SELECT policy gates on `public.is_current_user_admin()` from migration 9; NO INSERT/UPDATE/DELETE policies declared so writes are service-role only. Anon and non-admin authenticated callers return 0 rows. |
| T-AUDIT-INDEX-COST | Denial of Service | `audit_log` indexes | accept | Indexes on `(target_type, target_id)` + `(actor_id, created_at DESC)` cover the expected admin-UI query shapes without indexing JSONB (Pitfall 6 — JSONB index would balloon free-tier storage). v1.3+ audit UI revisits if query shapes shift. |
</threat_model>

<verification>
- `supabase db reset` succeeds with all 11 migrations (00–10) applying in order
- `npm run test -- --run polls-effective-invariant.test.ts` passes (no new `from('polls')` reads in `src/`)
- Manual: `psql` query against local Supabase confirms the post-migration shape — `\d+ public.polls` shows the two new columns, `\d+ public.audit_log` shows the new table, `SELECT pg_get_viewdef('public.polls_effective')` shows the two new column projections, `SELECT * FROM pg_policies WHERE tablename = 'vote_counts'` shows exactly one policy named `"Vote counts visible to voters when not hidden"` scoped `TO authenticated`
- File contains zero review-round / phase-ID archaeology in comments
</verification>

<success_criteria>
- ROADMAP SC-1 satisfied (column adds + 10 prior migrations all pass)
- ROADMAP SC-4 satisfied (`polls_effective` exposes the two new columns; `security_invoker = on` re-applied; invariant test passes)
- Foundation laid for Plan 02 (EF writes the new column + audit_log row), Plan 03 (12 EF audit retrofit), Plan 04 (12-cell matrix tests the new policy + audit row presence)

> NB on ROADMAP discrepancy: ROADMAP SC-1 wording says "all 14 pre-existing migrations continue to pass" but the codebase has only 10 pre-existing migrations (00–09). Plan 11-01 covers all 10 cleanly. The "14" appears to be a ROADMAP typo — the planner did not silently change the SC count in plans. Recommend ROADMAP correction during Phase 11 SUMMARY write-up (not in scope for this plan's diff).
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-rls-ef-foundations/11-01-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Declare `requirements-completed: [VIS-01, VIS-04, VIS-09]`. Capture the manual `supabase db reset` evidence (e.g., paste the migration list from the apply log).
</output>
