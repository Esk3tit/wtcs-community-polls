---
phase: 11-schema-rls-ef-foundations
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/functions/_shared/audit.ts
  - supabase/functions/toggle-results-visibility/index.ts
autonomous: true
requirements:
  - VIS-02
  - VIS-03
tags:
  - edge-function
  - audit
  - supabase
  - admin

must_haves:
  truths:
    - "`supabase/functions/_shared/audit.ts` exports `writeAudit(supabaseAdmin, entry): Promise<void>` and an `AuditEntry` type"
    - "`writeAudit` NEVER throws — on `audit_log` INSERT error it logs to `console.error` and returns void (Pitfall 3 contract; non-negotiable for callers)"
    - "`supabase/functions/toggle-results-visibility/index.ts` mirrors the pin-poll skeleton structure (Deno.serve, CORS, anon-key auth.getUser, service-role admin client, requireAdmin gate, json helper)"
    - "EF accepts `{ poll_id: string, hidden: boolean }` body; validates poll_id as UUID and hidden as boolean; returns 400 on missing/invalid"
    - "REVIEW-FIX-H4: EF uses a conditional state-change UPDATE — `UPDATE polls SET results_hidden = $hidden, results_hidden_changed_at = now() WHERE id = $poll_id AND results_hidden IS DISTINCT FROM $hidden RETURNING *` — instead of a read-then-update sequence. This eliminates the Codex HIGH race where two concurrent same-direction toggles can both read `false`, both UPDATE to `true`, and both audit a phantom `false → true`. With `IS DISTINCT FROM`, the second concurrent UPDATE matches 0 rows and emits no audit row — Postgres's row-level locking serializes the two writes atomically. Addresses Codex HIGH concern."
    - "REVIEW-FIX-H4: When the conditional UPDATE returns 0 rows (no-op — already at target state), the EF does a separate `SELECT * FROM polls WHERE id = $poll_id` to return the current poll row (still 200, still includes `results_hidden_changed_at` as set by the previous successful toggle). No audit row written on this path. Preserves D-11 idempotent-200 behavior + D-12 no-op-no-409 behavior while making the gate race-safe."
    - "REVIEW-FIX-H4 (D-13 reconciliation): `results_hidden_changed_at = now()` is now updated ONLY on actual state changes (because the conditional UPDATE's WHERE clause guards it). This is a deliberate tightening of D-13 — the prior plan said 'update on every call' but Gemini's review correctly noted this obscured the actual last-change time. New behavior: timestamp reflects the actual last-change moment, which matches what an admin UI would want to display. Documented as a D-13 refinement in the plan; CONTEXT.md does not need a new D-entry because the new behavior is strictly more useful and was Gemini's suggestion as well."
    - "EF writes the audit row (`action: 'results_hidden_toggled'`) ONLY when the conditional UPDATE returned 1 row (state actually changed). Idempotent no-op (UPDATE returned 0 rows) skips the audit write."
    - "EF returns 200 with `{ poll: <row> }` — full row from the conditional UPDATE's RETURNING * (on state change) OR from a follow-up SELECT * (on no-op). Both paths return the canonical poll row shape for VIS-03 + ROADMAP SC-3 deliberate deviation from pin-poll's `{success:true}` minimal response."
    - "Non-admin caller receives HTTP 403 via the existing `requireAdmin` + `adminCheckResponse` pattern (no signature change)"
    - "PGRST116 (poll not found) returns 404 — applies to BOTH the conditional UPDATE (returns 0 rows because id missing) AND the follow-up no-op SELECT (returns 0 rows because id missing). Distinguish 'no-op same state' from '404 missing poll' by re-querying the poll row on the 0-row path: if the SELECT also returns no rows, return 404; if SELECT returns 1 row, return 200 with that row (no-op path). Other DB errors return 500; all branches write CORS headers."
    - "No try/catch wraps the `writeAudit` call (Pitfall 3 — emitter swallows; caller must not silence errors at the call site)"
    - "REVIEW-FIX-L2: Source comments in `_shared/audit.ts` and `toggle-results-visibility/index.ts` do NOT cite decision IDs (`D-11`, `D-13`, etc.), plan IDs, round numbers, or PR refs (per CLAUDE.md memory `feedback_no_review_archaeology_in_source.md`). WHY-only comments — explain rationale (e.g., 'audit is fail-open by contract — DB write failures must not fail user-facing mutations') without citing the artifact that locked the decision. Plan refs belong in PR/commit, not src/."
    - "D-12: The EF returns HTTP 200 (NOT 409 Conflict) on no-op calls — duplicate toggles to the same `hidden` value are tolerated without complaint. UI carries the burden of representing current state; the EF tolerates network retries and two-admin races by returning the updated poll row regardless of whether `before.results_hidden === hidden`."
  artifacts:
    - path: "supabase/functions/_shared/audit.ts"
      provides: "writeAudit + AuditEntry shared helper"
      exports: ["writeAudit", "AuditEntry"]
      min_lines: 25
    - path: "supabase/functions/toggle-results-visibility/index.ts"
      provides: "Admin-gated EF flipping polls.results_hidden + writing audit row on state change"
      exports: []
      min_lines: 80
  key_links:
    - from: "supabase/functions/toggle-results-visibility/index.ts"
      to: "supabase/functions/_shared/audit.ts"
      via: "import writeAudit"
      pattern: "import \\{ writeAudit \\} from '\\.\\./_shared/audit\\.ts'"
    - from: "supabase/functions/toggle-results-visibility/index.ts"
      to: "supabase/functions/_shared/admin-auth.ts"
      via: "requireAdmin + adminCheckResponse"
      pattern: "requireAdmin\\(supabaseAdmin, user\\.id\\)"
    - from: "supabase/functions/_shared/audit.ts"
      to: "audit_log table (migration 10)"
      via: ".from('audit_log').insert(...)"
      pattern: "from\\('audit_log'\\)\\.insert"
---

<objective>
Land the shared audit helper and the new admin-gated `toggle-results-visibility` Edge Function. The helper is a single ~30-line file with a Promise<void> contract. The EF is a ~100-line clone of `pin-poll/index.ts` with three deliberate deltas (pre-read for idempotency, timestamp on every UPDATE, full poll row in response) plus one audit call gated on state change.

Purpose: VIS-02 + VIS-03 + ROADMAP SC-3 + SC-5 all hinge on this EF behaving correctly: admin-only gate (403/200), atomic state flip with timestamp, audit row written exactly when state changed, updated poll row returned. The shared helper makes Plan 03's 12-EF retrofit a one-line insertion per file rather than a copy-paste of the insert logic.

Output: Two new files. No changes to existing EFs in this plan (Plan 03 owns the retrofit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md
@.planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md

<!-- Analog files the executor MUST read before writing -->
@supabase/functions/_shared/admin-auth.ts
@supabase/functions/_shared/cors.ts
@supabase/functions/pin-poll/index.ts
@supabase/functions/close-poll/index.ts

<interfaces>
<!-- Identifiers Plan 03 retrofit and Plan 04 tests depend on -->

From _shared/audit.ts (NEW):

```
export type AuditEntry = {
  actor_id: string | null      // string UUID for admin actions; null for cron (close-expired-polls)
  action: string               // e.g. 'results_hidden_toggled', 'poll_pinned', 'poll_auto_closed'
  target_type: string          // e.g. 'poll', 'category', 'profile'
  target_id: string | null     // text — UUID (profile/poll/category) OR Discord snowflake (admin_preauthorized) OR null (batch / non-row-targeted). Plan 01 column type is text per REVIEW-FIX-C3-H1.
  before: unknown              // JSONB shape per-EF (D-07 planner discretion)
  after: unknown               // JSONB shape per-EF
}

export async function writeAudit(
  supabaseAdmin: SupabaseClient,
  entry: AuditEntry,
): Promise<void>     // NEVER throws — logs to console.error on insert failure
```

From _shared/admin-auth.ts (existing, reused verbatim):

```
export type AdminCheckResult = { ok: true } | { ok: false; reason: string }
export async function requireAdmin(supabaseAdmin: SupabaseClient, userId: string): Promise<AdminCheckResult>
export function adminCheckResponse(result: AdminCheckResult): { status: number; error: string }
```

EF endpoint contract (consumed by Plan 04 TEST-12 + Phase 12 admin UI):

  POST /functions/v1/toggle-results-visibility
  Authorization: Bearer <user JWT>
  Body: { "poll_id": "<uuid>", "hidden": true|false }
  401 — Missing/invalid Authorization
  400 — Missing/invalid poll_id or hidden
  403 — Caller is not admin
  404 — Poll not found
  200 — { "poll": { id, title, ..., results_hidden, results_hidden_changed_at, ... } }
  500 — DB error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 02-01: Shared audit helper (_shared/audit.ts)</name>
  <files>supabase/functions/_shared/audit.ts</files>
  <read_first>
    - supabase/functions/_shared/admin-auth.ts (full file, 48 lines — imports, JSDoc style, export shape; mirror these conventions for the new helper)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "supabase/functions/_shared/audit.ts" (signature, body pattern, contract deviation rationale)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pattern 4: Shared audit helper" (lines 528-575 — full code skeleton)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-05 (13 emitters), D-07 (before/after shape freedom), Pitfall 3 in RESEARCH (never-throws contract)
  </read_first>
  <action>
    Create `supabase/functions/_shared/audit.ts` matching the file shape of `_shared/admin-auth.ts`:

    1. Import line: `import type { SupabaseClient } from 'https://esm.sh/@supabase/supabase-js@2.101.1'` (verbatim, pinned esm.sh version 2.101.1 — same line as admin-auth.ts:1).

    2. Export the `AuditEntry` type per the `<interfaces>` block above. Use `type` not `interface` (project convention).

    3. JSDoc block above `writeAudit` explaining: "Append-only audit row writer. Used by the 13 admin-action emitters in v1.2 (per Phase 11 D-05). Contract: NEVER throws — on `audit_log` INSERT error, logs to `console.error` and returns. Callers must NOT wrap this in try/catch (Pitfall 3 — audit failures must not fail user-facing mutations)." No review-round / Plan / Round / PR archaeology (per CLAUDE.md memory).

    4. `export async function writeAudit(supabaseAdmin: SupabaseClient, entry: AuditEntry): Promise<void>` — body INSERTs into `audit_log` with all 6 entry fields. On error: `console.error('audit_log INSERT failed:', { entry, error })`. Do NOT throw, do NOT rethrow, do NOT return the error to the caller. Return implicitly (Promise<void>).
  </action>
  <acceptance_criteria>
    - File `supabase/functions/_shared/audit.ts` exists
    - File exports `writeAudit` function and `AuditEntry` type: `grep -E "^export (async function writeAudit|type AuditEntry)" supabase/functions/_shared/audit.ts | wc -l` returns 2
    - Function signature is `Promise<void>` (not a discriminated union — deliberate deviation from admin-auth.ts contract per Pitfall 3): `grep -E "writeAudit\\([^)]+\\):\\s*Promise<void>" supabase/functions/_shared/audit.ts` returns 1 match
    - Function body contains `console.error` for the failure path: `grep -c "console.error" supabase/functions/_shared/audit.ts` returns ≥1
    - Function body contains NO `throw` statements in the error path: `grep -c "throw" supabase/functions/_shared/audit.ts` returns 0
    - File imports from the pinned `@supabase/supabase-js@2.101.1` URL (matches admin-auth.ts:1): `grep -c "esm.sh/@supabase/supabase-js@2.101.1" supabase/functions/_shared/audit.ts` returns 1
    - File uses `type` keyword (not `interface`) for `AuditEntry`: `grep -E "^export type AuditEntry" supabase/functions/_shared/audit.ts` returns 1 match
    - File has zero review-round / Plan / Round / PR archaeology: `grep -iE "(Round [0-9]|Plan [0-9]+-[0-9]+|PR #[0-9]+|review-round)" supabase/functions/_shared/audit.ts` returns nothing
    - File is under 50 lines (helper should stay compact): `wc -l supabase/functions/_shared/audit.ts` returns ≤50
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^export (async function writeAudit|type AuditEntry)" supabase/functions/_shared/audit.ts | wc -l | xargs test 2 -eq && grep -E "writeAudit\\([^)]+\\):\\s*Promise<void>" supabase/functions/_shared/audit.ts && ! grep -q "throw" supabase/functions/_shared/audit.ts && grep -c "console.error" supabase/functions/_shared/audit.ts | xargs test 1 -le</automated>
  </verify>
  <done>
    Helper exists, exports both symbols, Promise<void> contract enforced, no throws, no archaeology comments.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 02-02: toggle-results-visibility Edge Function</name>
  <files>supabase/functions/toggle-results-visibility/index.ts</files>
  <read_first>
    - supabase/functions/pin-poll/index.ts (full file, 83 lines — primary analog: skeleton, body validation, error envelope, json helper, single-boolean flip pattern)
    - supabase/functions/close-poll/index.ts (lines 70-80 — `closed_at: new Date().toISOString()` precedent for the timestamp write pattern, D-13)
    - supabase/functions/_shared/admin-auth.ts (signature reuse — `requireAdmin` + `adminCheckResponse` called verbatim)
    - supabase/functions/_shared/cors.ts (getCorsHeaders usage)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "supabase/functions/toggle-results-visibility/index.ts" (full section — copy-verbatim blocks identified, 4 deltas from pin-poll listed)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-11, D-12, D-13 (idempotency, no 409 on no-op, EF writes timestamp not trigger)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pattern 2" lines 367-485
  </read_first>
  <action>
    Create `supabase/functions/toggle-results-visibility/index.ts` by cloning `pin-poll/index.ts` and applying the four deltas from PATTERNS.md § "toggle-results-visibility":

    Copy verbatim from pin-poll (no behavioral change):
      - Top header comment style — substitute Phase 11's WHY: "Admin-gated toggle of polls.results_hidden. Idempotent — writes audit row only on actual state change (D-11)." NO review-round / plan-ID tags.
      - Import lines (createClient, getCorsHeaders, requireAdmin, adminCheckResponse) — PLUS new line: `import { writeAudit } from '../_shared/audit.ts'`.
      - `json(body, status, cors)` helper (verbatim).
      - `Deno.serve(async (req) => { ... })` outer envelope, OPTIONS / non-POST handling.
      - Authorization header read + 401 on missing.
      - `supabaseUser` anon-key client with the Authorization header, `auth.getUser()`, 401 on auth error.
      - `supabaseAdmin` service-role client.
      - `requireAdmin(supabaseAdmin, user.id)` + `adminCheckResponse` block — verbatim. This is the 403 path.
      - Body parser: `try { const parsed = await req.json(); ... } catch { return json({error:'Invalid JSON body'}, 400, corsHeaders) }`.
      - Body field extraction & validation — same shape as pin-poll's `poll_id`/`isPinned` block but with field name `hidden` not `isPinned`: `poll_id: string` trimmed, UUID regex check, return 400 with `'Missing poll_id'` / `'Invalid poll_id'`; `hidden: boolean` type check, return 400 with `'Missing or invalid hidden'`.
      - Outer try/catch envelope around the whole logic block with `console.error('toggle-results-visibility error:', err)` + 500 response.

    Four deliberate deltas from pin-poll (REVIEW-FIX-H4: race-safe rewrite):

    Delta 1 (conditional state-change UPDATE — race-safe replacement for read-then-update): Skip the pre-read entirely. Run a single conditional UPDATE with RETURNING:

      ```ts
      const { data: changed, error: updErr } = await supabaseAdmin
        .from('polls')
        .update({ results_hidden: hidden, results_hidden_changed_at: new Date().toISOString() })
        .eq('id', poll_id)
        .not('results_hidden', 'is', hidden)  // IS DISTINCT FROM equivalent via PostgREST
        .select('*')
        .maybeSingle();
      ```

      Note on PostgREST: supabase-js does not have a direct `IS DISTINCT FROM` operator builder. The closest expression is `.not('results_hidden', 'is', hidden)`. The two are not perfectly equivalent on a NULLable column, but `polls.results_hidden` is declared `NOT NULL DEFAULT false` in migration 10, so `NOT (results_hidden IS hidden)` and `results_hidden IS DISTINCT FROM hidden` produce identical results in this context.

      Alternative (if PostgREST `.not()` filter behavior changes between supabase-js versions): use `supabaseAdmin.rpc(...)` to call a small SQL function `toggle_results_hidden_if_changed(p_poll_id uuid, p_hidden boolean)` defined in migration 10 that runs `UPDATE polls SET results_hidden = p_hidden, results_hidden_changed_at = now() WHERE id = p_poll_id AND results_hidden IS DISTINCT FROM p_hidden RETURNING *`. The RPC form is the more defensive option; document both in the plan body and let the executor pick based on local supabase-js behavior testing. (If the RPC path is picked, migration 10 gains one SECURITY DEFINER function — Plan 01 task must be updated correspondingly.)

      Recommended default: PostgREST `.not('results_hidden', 'is', hidden)` form. If the local test (`.maybeSingle()` returns null on no-op, returns row on change) does not match expectations during Wave 2 execution, fall back to the RPC form.

      On `updErr`: console.error + 500. On `changed === null` (no-op — UPDATE matched 0 rows because either id missing OR already at target state): proceed to Delta 2's distinguish-no-op-from-404 path. On `changed !== null` (state changed): proceed to Delta 3 (audit) and Delta 4 (return 200 with `changed` as the poll).

    Delta 2 (distinguish no-op from 404, REVIEW-FIX-H4): When Delta 1 returns 0 rows, run a follow-up SELECT to determine whether the poll exists:

      ```ts
      if (changed === null) {
        const { data: current, error: selErr } = await supabaseAdmin
          .from('polls')
          .select('*')
          .eq('id', poll_id)
          .maybeSingle();
        if (selErr) { console.error('toggle-results-visibility select error:', selErr); return json({error: 'DB error'}, 500, corsHeaders); }
        if (current === null) return json({error: 'Poll not found'}, 404, corsHeaders);
        return json({ poll: current }, 200, corsHeaders);  // no-op idempotent return — no audit row
      }
      ```

    Delta 3 (audit only on actual state change, REVIEW-FIX-H4): When Delta 1 succeeded with `changed !== null`, write the audit row:

      ```ts
      await writeAudit(supabaseAdmin, {
        actor_id: user.id,
        action: 'results_hidden_toggled',
        target_type: 'poll',
        target_id: poll_id,
        before: { results_hidden: !hidden },  // we know it was the opposite — UPDATE only matched because IS DISTINCT FROM
        after: { results_hidden: hidden },
      });
      ```

      DO NOT wrap `writeAudit` in try/catch (Pitfall 3 — helper handles its own errors; wrapping would defeat the contract).

    Delta 4 (response shape, VIS-03 + ROADMAP SC-3): final success return is `return json({ poll: changed }, 200, corsHeaders)` — NOT `{ success: true }`. The full poll row in the response is what the Phase 12 React UI needs to update the card state without a separate re-fetch.

    REVIEW-FIX-L2 source-comment policy: All inline comments in this EF must be WHY-only — no D-XX, no Plan IDs, no Round/PR refs. Example acceptable comment: `// audit only on actual state change — fail-open contract: writeAudit never throws`. Example NOT acceptable: `// D-11 — idempotency pre-read`. The plan files (in `.planning/`) carry the decision archaeology; source code does not.
  </action>
  <acceptance_criteria>
    - File `supabase/functions/toggle-results-visibility/index.ts` exists
    - Imports the audit helper: `grep -E "import\\s+\\{\\s*writeAudit\\s*\\}\\s+from\\s+'\\.\\./\\_shared/audit\\.ts'" supabase/functions/toggle-results-visibility/index.ts` returns 1 match
    - Imports `requireAdmin` and `adminCheckResponse`: `grep -c "requireAdmin\\|adminCheckResponse" supabase/functions/toggle-results-visibility/index.ts` returns ≥2
    - Validates `hidden` as boolean (not string truthy): `grep -E "typeof body\\.hidden === 'boolean'" supabase/functions/toggle-results-visibility/index.ts` returns 1 match
    - Validates `poll_id` UUID shape (regex check): `grep -E "/\\^\\[0-9a-f\\]\\{8\\}-" supabase/functions/toggle-results-visibility/index.ts` returns 1 match
    - REVIEW-FIX-H4: Contains the conditional state-change UPDATE — `grep -E "\\.not\\('results_hidden',\\s*'is',\\s*hidden\\)" supabase/functions/toggle-results-visibility/index.ts` returns 1 match (the race-safe gate); OR if the RPC fallback was chosen, `grep -E "rpc\\('toggle_results_hidden_if_changed'" supabase/functions/toggle-results-visibility/index.ts` returns 1 match
    - REVIEW-FIX-H4: Does NOT contain a pre-read SELECT before the UPDATE — `grep -cE "\\.from\\('polls'\\)\\.select\\([^)]*results_hidden[^)]*\\)\\.eq\\('id', poll_id\\)" supabase/functions/toggle-results-visibility/index.ts` returns at most 1 (the post-no-op SELECT in Delta 2), NOT 2 (the old pre-read pattern + the post-update). Specifically: no SELECT precedes the UPDATE in execution order
    - Contains the UPDATE with both `results_hidden` and `results_hidden_changed_at`: `grep -E "results_hidden:\\s*hidden" supabase/functions/toggle-results-visibility/index.ts` AND `grep -E "results_hidden_changed_at:\\s*new Date\\(\\)\\.toISOString\\(\\)" supabase/functions/toggle-results-visibility/index.ts` BOTH return ≥1
    - Calls `.select('*')` on the UPDATE (not `.select('id')`) so the response includes the full poll row: `grep -E "\\.update\\(\\{[^}]*results_hidden" supabase/functions/toggle-results-visibility/index.ts` is on the same statement as `.select('*')` (within 4 lines)
    - REVIEW-FIX-H4: Audit call is gated on actual state change (returned non-null row from conditional UPDATE) — `grep -E "if\\s*\\(\\s*changed\\s*!==\\s*null\\s*\\)" supabase/functions/toggle-results-visibility/index.ts` returns ≥1 match (the audit-block gate)
    - Audit call uses action `'results_hidden_toggled'`: `grep -E "action:\\s*'results_hidden_toggled'" supabase/functions/toggle-results-visibility/index.ts` returns 1 match
    - Audit call is NOT wrapped in try/catch: `grep -B 2 "writeAudit" supabase/functions/toggle-results-visibility/index.ts | grep -c "try {"` returns 0 (no try inside the audit conditional)
    - Success response shape is `{ poll: changed }` (state-change path) OR `{ poll: current }` (no-op path): `grep -cE "json\\(\\{\\s*poll:\\s*(changed|current)" supabase/functions/toggle-results-visibility/index.ts` returns ≥2
    - PGRST116 → 404 on the no-op SELECT (current === null branch): `grep -c "404" supabase/functions/toggle-results-visibility/index.ts` returns ≥1 AND `grep -c "Poll not found" supabase/functions/toggle-results-visibility/index.ts` returns ≥1
    - REVIEW-FIX-L2: Source comments contain NO `D-XX`, `D-1X`, plan IDs, round numbers, or PR refs — `grep -iE "//.*\\bD-[0-9]+\\b|//.*\\bPlan [0-9]+|//.*\\bRound [0-9]+|//.*\\bPR #[0-9]+" supabase/functions/toggle-results-visibility/index.ts` returns nothing AND same check on `supabase/functions/_shared/audit.ts` returns nothing
    - Outer console.error log identifies the EF by name: `grep -E "console\\.error\\(['\"]toggle-results-visibility" supabase/functions/toggle-results-visibility/index.ts` returns ≥1
    - File has zero review-round / Plan / Round / PR archaeology: `grep -iE "(Round [0-9]|Plan [0-9]+-[0-9]+|PR #[0-9]+|review-round)" supabase/functions/toggle-results-visibility/index.ts` returns nothing
    - Total file size ≤ 120 lines (close to pin-poll's 83 + the 4 deltas): `wc -l supabase/functions/toggle-results-visibility/index.ts` returns ≤120
  </acceptance_criteria>
  <verify>
    <automated>grep -E "import\\s+\\{\\s*writeAudit\\s*\\}\\s+from\\s+'\\.\\./\\_shared/audit\\.ts'" supabase/functions/toggle-results-visibility/index.ts && (grep -E "\\.not\\('results_hidden',\\s*'is',\\s*hidden\\)" supabase/functions/toggle-results-visibility/index.ts || grep -E "rpc\\('toggle_results_hidden_if_changed'" supabase/functions/toggle-results-visibility/index.ts) && grep -E "results_hidden_changed_at:\\s*new Date\\(\\)\\.toISOString\\(\\)" supabase/functions/toggle-results-visibility/index.ts && grep -E "action:\\s*'results_hidden_toggled'" supabase/functions/toggle-results-visibility/index.ts && grep -cE "json\\(\\{\\s*poll:\\s*(changed|current)" supabase/functions/toggle-results-visibility/index.ts | xargs test 2 -le && ! grep -iE "//.*\\bD-[0-9]+\\b|//.*\\b(Round [0-9]|Plan [0-9]+-[0-9]+|review-round)" supabase/functions/toggle-results-visibility/index.ts</automated>
  </verify>
  <done>
    EF exists, mirrors pin-poll skeleton verbatim except for the 4 documented deltas; all of D-11, D-12, D-13 enforced; admin gate via `requireAdmin` reused unchanged; audit call sits after UPDATE success and is unwrapped (Pitfall 3); response shape returns the full poll row (VIS-03 + ROADMAP SC-3).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JWT → EF | Non-admin caller must receive 403 (not 200, not 401, not 500) — `requireAdmin` is the authoritative gate |
| EF → audit_log INSERT | Audit write failure must NOT fail the user-facing mutation; helper logs to console.error and continues |
| EF → polls UPDATE | Idempotent: same `hidden` value on consecutive calls must not double-write the audit row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-EF-ADMIN-BYPASS | Elevation of Privilege | `toggle-results-visibility` EF authn/authz path | mitigate | Re-use the existing `requireAdmin(supabaseAdmin, user.id)` from `_shared/admin-auth.ts` verbatim (no signature change). On `!ok`, return the status from `adminCheckResponse` (403 for non-admin). Plan 04 TEST-12 asserts the 403 for a `memberUser` JWT and the 200 for an `adminUser` JWT. |
| T-AUDIT-DRIFT | Repudiation | Audit row written on every state change | mitigate | EF reads `before.results_hidden` first, UPDATEs, then `if (before !== hidden) writeAudit(...)`. The audit row contains actor_id + before + after JSONB so a flipped state is traceable to its admin. Plan 04 TEST-12 asserts the row presence on state change AND the row absence on no-op (D-11). |
| T-AUDIT-FAIL-OPEN | Repudiation | `writeAudit` Promise<void> contract | accept | If `audit_log` INSERT fails (rare — only on DB issue), the user-facing mutation still returns 200. The audit gap is logged to Supabase Function Logs via `console.error`. Accepted because the alternative (failing the user mutation on audit failure) is a worse UX trade — pre-aggregated vote counts are not exposed by an audit gap. |
| T-IDEMPOTENT-DOUBLELOG | Repudiation | No-op duplicate calls | mitigate | EF pre-reads current state. If `before.results_hidden === hidden`, audit row is NOT written. `results_hidden_changed_at` is still updated (D-13 — timestamp is not gated on state change; cheap under MVCC, makes "last touched by admin" queryable even for no-ops). |
| T-INPUT-VALIDATION | Tampering | EF body parser | mitigate | `poll_id` validated against UUID regex; `hidden` validated as `typeof === 'boolean'` (NOT truthy coercion — `"true"` string and `1` number both 400). JSON parse failure → 400. Body shape derived from pin-poll's existing pattern (unchanged). |
| T-CORS-HEADERS | Information Disclosure | OPTIONS + error responses | mitigate | `getCorsHeaders(req)` called once; threaded through every `json(...)` response including error paths. Verified by analog pin-poll having the same pattern; copied verbatim. |
</threat_model>

<verification>
- Build the EFs locally (`supabase functions serve toggle-results-visibility`) and smoke `curl` with an admin JWT — expect 200 with `{ poll: { ..., results_hidden: true, results_hidden_changed_at: ... } }`
- `tsc --noEmit` (project type check) passes — the audit helper is Deno code, but local `tsc` over `supabase/functions/` is informational; the canonical type check is Deno's own (`deno check supabase/functions/toggle-results-visibility/index.ts`)
- Plan 04 (Wave 3) provides the runtime assertion via Vitest TEST-12 — no test bodies in Plan 02 itself
</verification>

<success_criteria>
- ROADMAP SC-3 + SC-5 foundation laid (EF body correct; deploy is Plan 05's job)
- VIS-02 + VIS-03 implementation complete
- Shared helper ready for Plan 03's 12-EF retrofit (one-line import + one-line call per existing EF)
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-rls-ef-foundations/11-02-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Declare `requirements-completed: [VIS-02, VIS-03]`. Note that VIS-02 is fully complete only after Plan 05 deploys the EF — call out the deploy dependency in the SUMMARY's "Open follow-ups" section.
</output>
