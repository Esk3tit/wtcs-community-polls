---
phase: 11-schema-rls-ef-foundations
plan: 04
type: execute
wave: 3
depends_on: [00, 01, 02, 03, "03b"]
files_modified:
  - e2e/integration/vote-counts-rls.test.ts
  - e2e/integration/toggle-results-visibility.test.ts
  - e2e/integration/create-poll-results-hidden.test.ts
autonomous: true
requirements:
  - VIS-05
  - TEST-11
  - TEST-12
tags:
  - testing
  - integration
  - vitest
  - rls
  - audit

must_haves:
  truths:
    - "TEST-11 12-cell matrix passes in full — every cell where `results_hidden = true` OR caller has not voted returns 0 rows from `vote_counts`; only `authenticated × hidden=false × voted` returns >0 rows; service-role returns >0 rows in all 4 of its cells (RLS bypass)"
    - "TEST-11 uses `describe.each` over a 12-row case table — no `it.skip`, no `it.todo`, every cell asserted; merge-blocker behavior matches REQUIREMENTS.md TEST-11 wording"
    - "TEST-12 case 1: invoking `toggle-results-visibility` with a `memberUser` (non-admin) JWT returns HTTP 403"
    - "TEST-12 case 2: invoking with an `adminUser` JWT returns HTTP 200 with `{ poll: { ..., results_hidden: <new>, results_hidden_changed_at: <ISO string>, ... } }` (non-null timestamp)"
    - "TEST-12 case 3: a state-changing toggle (e.g., false→true) writes exactly one `audit_log` row with `action='results_hidden_toggled'`, `actor_id=adminUser.id`, `before.results_hidden=false`, `after.results_hidden=true`"
    - "TEST-12 case 4: a no-op call (true→true on a poll already hidden) returns 200 but does NOT write an additional `audit_log` row (idempotency)"
    - "REVIEW-FIX-H1: Every TEST-11 cell uses the Plan 00 helpers with their REVISED signatures — `castVote({pollId, userId, choiceId, serviceRole})` (serviceRole-backed insert; userId defaults to memberUser.id), `createFreshPoll({hidden, suiteSlug, seedBaseline: true})` (seeds an adminUser baseline vote by default, returns `choiceIds: [optionA, optionB]`). The `castVote` call in the `voted=true` cells targets `option-b` (so it doesn't collide with the baseline `option-a` vote). Service-role bypass cells (4 of them) now see `>0` rows because the baseline vote produces a `vote_counts` row independent of the test's memberUser-vote state."
    - "REVIEW-FIX-H2: The `serviceRole × *` cells expecting `>0` rows pass because `createFreshPoll` (via `seedBaselineVote`) creates a baseline `vote_counts` row by adminUser — service-role bypass sees this row regardless of memberUser's voted state. Addresses the Codex HIGH concern that previously service-role cells would silently return 0 rows from an empty `vote_counts` table."
    - "REVIEW-FIX-H1-clarification: Authenticated voted-true cells (`authed × hidden=false × voted=true`) now see `>0` rows because `castVote` inserts a vote BY memberUser.id (via service-role). The vote-existence EXISTS clause in the policy matches because `votes.user_id = auth.uid()` where auth.uid() = memberUser.id (per the authed client's JWT)."
    - "REVIEW-FIX-M5 + REVIEW-FIX-C2-H6: A new third integration test file `e2e/integration/create-poll-results-hidden.test.ts` covers Plan 11-03b's `results_hidden=true` path end-to-end. CRITICAL CORRECTION FROM CYCLE 2 (Codex HIGH #6): the LIVE create-poll EF returns `{ success: true, id: pollId }` — NOT `{ poll: <row> }`. AND the LIVE EF expects `choices: string[]` — NOT `choices: { text: string }[]`. The prior plan body sent choice objects and read `data.poll.results_hidden`; both would fail at runtime. Corrected test body: (a) POST `create-poll` EF with `body: { ..., choices: ['option-a', 'option-b'], results_hidden: true }`, capture `result.data.id` (NOT `result.data.poll.id`), then verify `results_hidden` via a service-role SELECT on `polls` (`const { data: pollRow } = await adminClients.serviceRole.from('polls').select('results_hidden').eq('id', createdPollId).single(); expect(pollRow.results_hidden).toBe(true);`); (b) read `audit_log` for that `target_id` and assert EXACTLY two rows — one with `action='poll_created'` and `after.results_hidden === true`, one with `action='results_hidden_set_at_create'`; (c) repeat with `results_hidden: false` and assert ONE audit row (`poll_created`, `after.results_hidden === false`) AND `polls.results_hidden === false` via service-role SELECT; (d) assert the migration 10 column DEFAULT is `false` by creating a poll with `results_hidden` omitted entirely and asserting via service-role SELECT that `polls.results_hidden === false`. Addresses cycle-2 Codex HIGH #6 (test contract mismatch — choices shape AND response shape both corrected)."
    - "REVIEW-FIX-M6: TEST-12 test cases are made order-independent by using a fresh poll PER `it()` case via `beforeEach`/`afterEach` (instead of one shared poll in `beforeAll`). Each case creates its own poll, runs its assertion, and cleans up. This eliminates Codex's MEDIUM concern about Case 3 implicitly depending on Case 2 having toggled the poll. The no-op idempotency case (Case 4) creates a poll, runs an initial toggle to set `hidden=true`, asserts ONE audit row, runs the no-op toggle, then asserts STILL only ONE audit row — fully self-contained."
    - "REVIEW-FIX-M7 + REVIEW-FIX-C2-H5: TEST-12 adds an admin-authenticated `vote_counts` direct-read sanity case (in `vote-counts-rls.test.ts`, NOT a 13th matrix cell — added as an additional `it` block outside the 12-cell `describe.each`). CRITICAL CORRECTION FROM CYCLE 2: the prior plan body specified `seedBaseline: false` for this case, which left `vote_counts` empty and made the assertion a FALSE NEGATIVE — even if a regression re-introduced the `is_current_user_admin() OR ...` branch, the assertion `expect(rows).toHaveLength(0)` would still pass (0 rows from an empty source). The corrected design: (a) create the poll via `createFreshPoll({hidden: false, ..., seedBaseline: true})` — this seeds a baseline `vote_counts` row keyed on `adminUser.id` voting `option-a`, but that defeats the test (admin IS the voter); SO override by passing `seedBaseline: false` AND THEN explicitly call `castVote({pollId, userId: fixtureUsers.memberUser.id, choiceId: poll.choiceIds[0], serviceRole})` so a `vote_counts` row exists keyed on memberUser. (b) Mint an admin JWT client via `mintClients({authAs: 'adminUser'})` — admin has NOT voted on this poll. (c) SELECT `vote_counts` via the admin JWT client. (d) Assert `rows.length === 0`. The voter-EXISTS clause matches `votes.user_id = auth.uid()`; auth.uid() = adminUser.id, but the only `votes` row is keyed on memberUser.id, so the EXISTS clause is false for admin — `vote_counts` returns 0 rows for admin. If a regression re-introduces the admin-OR-bypass, the admin would see memberUser's vote_counts row (rows.length === 1) and the assertion FAILS as intended. This makes the test a real regression sentinel for the admin-OR removal in REVIEW-FIX-H3. Addresses cycle-2 Codex HIGH #5."
    - "Both suites use the `e2e/integration/helpers.ts` exports from Plan 00 — no Playwright imports, no duplicate client-minting code"
    - "Both suites clean up created polls in `afterEach` and rethrow on cleanup failure (Plan 00 helper handles this via `cleanupPoll`); TEST-12 also explicitly DELETEs `audit_log` rows in `afterEach` (no FK to polls — cascade does not clean these up)"
    - "REVIEW-FIX-L3: TEST-12 includes negative-case assertions — (a) invalid body (missing `poll_id` → 400); (b) invalid UUID for `poll_id` → 400; (c) 404 for non-existent `poll_id`. Three new `it()` blocks bringing Plan 04 TEST-12 case count to 7 (4 original + 3 negative). Addresses Codex LOW concern about missing negative tests."
    - "`npm run test:integration` exits 0 with the 12-cell matrix + 1 admin-authenticated sanity case + 7 TEST-12 cases (4 happy + 3 negative) + 4 create-poll-results-hidden cases all passing (no `.todo`, no `.skip`)"
    - "D-23: TEST-11 asserts the row count returned from `SELECT * FROM vote_counts WHERE poll_id = $1` for each of the 12 cells — 3 roles (anon, authenticated test user, service-role) × 2 hidden states (`results_hidden=true`, `results_hidden=false`) × 2 voter states (voted, not voted). Expected matrix: ONLY `(authenticated, hidden=false, voted)` returns rows; service-role returns rows in all 4 of its cells (bypass); all other cells return 0 rows. Any cell failing BLOCKS MERGE."
    - "D-24: TEST-12 lives alongside TEST-11 under `e2e/integration/` (same Vitest runner, same helpers). It asserts: non-admin caller → 403; admin caller → 200 with updated poll row including the new `results_hidden` value AND a non-null `results_hidden_changed_at`; an `audit_log` row is written for every state-changing toggle; an `audit_log` row is NOT written on no-op calls (per D-11)."
  artifacts:
    - path: "e2e/integration/vote-counts-rls.test.ts"
      provides: "TEST-11 12-cell RLS invariant matrix + admin-authenticated JWT sanity case (REVIEW-FIX-M7)"
      contains: "describe.each"
    - path: "e2e/integration/toggle-results-visibility.test.ts"
      provides: "TEST-12 admin EF authz + audit assertions + 3 negative cases (REVIEW-FIX-L3) — order-independent via beforeEach/afterEach (REVIEW-FIX-M6)"
      contains: "results_hidden_toggled"
    - path: "e2e/integration/create-poll-results-hidden.test.ts"
      provides: "REVIEW-FIX-M5: create-poll EF results_hidden=true path coverage (Plan 11-03b runtime evidence)"
      contains: "results_hidden_set_at_create"
  key_links:
    - from: "vote-counts-rls.test.ts"
      to: "e2e/integration/helpers.ts (Plan 00)"
      via: "import { mintClients, createFreshPoll, castVote, cleanupPoll }"
      pattern: "from '\\./helpers'"
    - from: "toggle-results-visibility.test.ts"
      to: "supabase/functions/toggle-results-visibility/index.ts (Plan 02)"
      via: "invokeEF helper hitting /functions/v1/toggle-results-visibility"
      pattern: "toggle-results-visibility"
    - from: "toggle-results-visibility.test.ts"
      to: "audit_log table (migration 10)"
      via: "readAuditLog helper SELECT"
      pattern: "readAuditLog"
---

<objective>
Wire the bodies of the two integration tests scaffolded in Plan 00. TEST-11 asserts the 12-cell RLS invariant matrix on `vote_counts` (P0 merge blocker per STATE.md and REQUIREMENTS.md). TEST-12 asserts the admin EF authorization contract for `toggle-results-visibility` plus the audit row presence/absence on state-change vs no-op.

Purpose: TEST-11 is the primary defense against RLS leakage at the `vote_counts` view — a leak would expose pre-aggregated vote counts to non-voters through the Supabase anon key. TEST-12 is the primary defense against admin-EF authz bypass + audit drift. Both suites block merge per REQUIREMENTS.md.

Output: Two test files with bodies filled in. The `it.todo` placeholders from Plan 00 become real `it` blocks with assertions. No new helper functions (Plan 00 already provided `mintClients`, `createFreshPoll`, `castVote`, `invokeEF`, `readAuditLog`, `cleanupPoll`); no changes to migration 10, the audit helper, or any EF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md
@.planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md
@.planning/phases/11-schema-rls-ef-foundations/11-VALIDATION.md

<!-- Plan 00 outputs (consumed verbatim) -->
@e2e/integration/helpers.ts
@e2e/integration/vote-counts-rls.test.ts
@e2e/integration/toggle-results-visibility.test.ts

<!-- Plan 01 and Plan 02 outputs that this plan exercises -->
@supabase/migrations/00000000000010_results_hidden_audit.sql
@supabase/functions/toggle-results-visibility/index.ts

<!-- Test-pattern analog -->
@src/__tests__/admin/polls-effective-invariant.test.ts
@e2e/fixtures/test-users.ts

<interfaces>
<!-- 12-cell matrix expected outcomes (D-23, RESEARCH Pitfall 1) -->

| role | hidden | voted | expected vote_counts rows |
|------|--------|-------|---------------------------|
| anon | false  | false | 0 |
| anon | false  | true  | 0 (no anon-granting policy — anon never sees vote_counts) |
| anon | true   | false | 0 |
| anon | true   | true  | 0 |
| authed (memberUser) | false | false | 0 (voted=false fails the voted-existence EXISTS clause) |
| authed (memberUser) | false | true  | >0 (ONLY allowed cell — voted AND hidden=false AND authenticated) |
| authed (memberUser) | true  | false | 0 |
| authed (memberUser) | true  | true  | 0 (voted but hidden=true) |
| serviceRole | false | false | >0 (RLS bypass) |
| serviceRole | false | true  | >0 (RLS bypass) |
| serviceRole | true  | false | >0 (RLS bypass) |
| serviceRole | true  | true  | >0 (RLS bypass) |

<!-- TEST-12 expected behaviors (D-24, VIS-03) -->

case 1: invoke as memberUser with valid body → status === 403
case 2: invoke as adminUser with { poll_id, hidden: true } against a poll currently false →
  status === 200
  data.poll.id === poll_id
  data.poll.results_hidden === true
  data.poll.results_hidden_changed_at is non-null + parseable as ISO timestamp
case 3: after case 2, readAuditLog({ targetId: poll_id, action: 'results_hidden_toggled' }) →
  rows.length === 1
  rows[0].actor_id === adminUser.id
  rows[0].before === { results_hidden: false }
  rows[0].after === { results_hidden: true }
case 4: invoke as adminUser with { poll_id, hidden: true } against a poll already hidden=true →
  status === 200
  readAuditLog rows for this poll_id with action='results_hidden_toggled' has the SAME count as before (no new row written)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01: TEST-11 12-cell RLS invariant matrix body</name>
  <files>e2e/integration/vote-counts-rls.test.ts</files>
  <behavior>
    - 12 cells in `describe.each`; each cell creates a fresh poll, optionally casts a vote, then SELECTs `* FROM vote_counts WHERE poll_id = $1` via the per-cell role client
    - anon × any state → 0 rows (no anon-granting policy exists)
    - authed × hidden=false × voted=true → >0 rows
    - authed × all other combinations → 0 rows
    - serviceRole × all combinations → >0 rows (RLS bypass)
    - afterEach cleans up the poll via `cleanupPoll`
  </behavior>
  <read_first>
    - e2e/integration/helpers.ts (Plan 00 — `mintClients`, `createFreshPoll`, `castVote`, `cleanupPoll`, `AuditRow`)
    - e2e/fixtures/test-users.ts (`fixtureUsers.memberUser`, `fixtureUsers.adminUser`)
    - e2e/integration/vote-counts-rls.test.ts (Plan 00 scaffold — the 12-row case array stays; replace `it.todo` with `it`)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "e2e/integration/vote-counts-rls.test.ts" (assertion shape; failure-message convention)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-20, D-23 (Vitest + describe.each layout; 12-cell expected matrix)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pitfall 1: P0 — RLS leakage" (rationale for each cell's expected outcome)
    - src/__tests__/admin/polls-effective-invariant.test.ts (full file — Vitest assertion + failure-message style to mirror)
    - supabase/migrations/00000000000010_results_hidden_audit.sql (Plan 01 — confirms policy name and USING clause shape for the assertion narrative)
  </read_first>
  <action>
    Open `e2e/integration/vote-counts-rls.test.ts` (the Plan 00 scaffold) and replace the `it.todo()` calls inside `describe.each` with real `it()` bodies.

    Top-of-file imports: `import { describe, it, expect, beforeAll, afterEach } from 'vitest'`; `import { mintClients, createFreshPoll, castVote, cleanupPoll } from './helpers'`.

    Outside the `describe.each`, mint the three clients once via `beforeAll` (lazy singletons inside `mintClients` per Plan 00 — calling repeatedly is cheap, but `beforeAll` is clearer): `const { anon, authed, serviceRole } = await mintClients({ authAs: 'memberUser' })`. Capture into module-scope `let` variables so the test bodies can pick the client by role.

    Cell body pattern (REVIEW-FIX-H1, H2): each `describe.each` iteration receives `(role, hidden, voted, expected)`. The body:
      1. Setup: `const poll = await createFreshPoll({ hidden, suiteSlug: \`rls-${role}\`, seedBaseline: true })`. The `seedBaseline: true` (default) ensures the new poll has a baseline `vote_counts` row from adminUser BEFORE the cell asserts anything — addresses REVIEW-FIX-H2: serviceRole cells expecting `>0` rows pass because the baseline row exists. Store the id in a `let` declared inside the inner `describe` so `afterEach` can clean up. Also capture `poll.choiceIds` to target the non-baseline choice in step 2.
      2. Optional memberUser vote (REVIEW-FIX-H1): `if (voted) { await castVote({ pollId: poll.id, userId: fixtureUsers.memberUser.id, choiceId: poll.choiceIds[1], serviceRole }) }`. The `serviceRole` client is used to INSERT the vote (no INSERT RLS policy on `votes` — authed clients cannot directly insert; production uses service-role via `submit-vote` EF). The vote targets `choiceIds[1]` (`option-b`) so it does NOT collide with the baseline vote on `option-a`. The userId is memberUser's ID so the authed-as-memberUser cell's `auth.uid()` matches the inserted `votes.user_id` row.
      3. Pick the client: `const client = role === 'anon' ? anon : role === 'authed' ? authed : serviceRole`.
      4. Query: `const { data, error } = await client.from('vote_counts').select('*').eq('poll_id', poll.id)`. On `error`, throw — RLS returns 0 rows, never an error.
      5. Assert: if `expected === 0`, `expect(data ?? []).toHaveLength(0)`. If `expected === '>0'`, `expect((data ?? []).length).toBeGreaterThan(0)`.
      6. Failure message: when an assertion fails, the default Vitest message names the iteration (`%s × hidden=%s × voted=%s`); add no extra messaging unless the assertion is too generic. The `describe.each` template captures the role × hidden × voted combination already, which makes CI failures self-describing.

    REVIEW-FIX-M7 + REVIEW-FIX-C2-H5 — admin-authenticated JWT sanity case (regression sentinel for REVIEW-FIX-H3): After the `describe.each` block, add ONE additional `it` block titled `'admin JWT direct-read of vote_counts is gated by the same voter+hidden rule (no admin-OR-bypass)'`. The case MUST be a REAL regression sentinel: vote_counts must contain a row that admin would see IF the admin-OR-bypass regressed back. The corrected body:

    ```ts
    it('admin JWT direct-read of vote_counts is gated by the same voter+hidden rule (no admin-OR-bypass)', async () => {
      // Seed vote_counts with a row keyed on memberUser (NOT admin) so that:
      //   - the policy's voter-EXISTS clause matches for memberUser but NOT for admin (since auth.uid() differs)
      //   - if a regression re-introduces is_current_user_admin() OR ... in the policy, admin would see this row
      //   - in the green state, admin (who has NOT voted on this poll) sees 0 rows
      const adminClients = await mintClients({ authAs: 'adminUser' });
      const poll = await createFreshPoll({ hidden: false, suiteSlug: 'admin-jwt-sentinel', seedBaseline: false });
      currentPollId = poll.id;  // for afterEach cleanup
      await castVote({
        pollId: poll.id,
        userId: fixtureUsers.memberUser.id,   // <-- memberUser is the voter, NOT admin
        choiceId: poll.choiceIds[0],
        serviceRole: adminClients.serviceRole,
      });
      // Admin reads vote_counts via their OWN JWT (not service-role).
      // auth.uid() === adminUser.id; admin has no votes row of their own; the EXISTS clause is false; result is 0 rows.
      // If a regression adds back `is_current_user_admin() OR ...`, the admin would see memberUser's vote_counts row.
      const { data, error } = await adminClients.authed.from('vote_counts').select('*').eq('poll_id', poll.id);
      expect(error).toBeNull();
      expect(data ?? []).toHaveLength(0);
    });
    ```

    Why this design works as a regression sentinel: the voter-EXISTS clause in the policy reads `EXISTS (SELECT 1 FROM votes v WHERE v.poll_id = vote_counts.poll_id AND v.user_id = auth.uid())`. By seeding a vote keyed on memberUser.id while reading via an admin JWT (auth.uid() = adminUser.id), the EXISTS clause is provably false for admin, so the visible-to-voter branch returns 0 rows. The ONLY way admin sees this row is if a regression adds a `OR is_current_user_admin()` disjunction to the policy expression — exactly the regression Plan 01's REVIEW-FIX-H3 forbids. The `seedBaseline: false` is correct here BECAUSE the test explicitly seeds a memberUser-keyed vote in the next line — vote_counts is NOT empty when the assertion runs (this was the cycle-2 bug: the prior plan left vote_counts empty, defeating the sentinel). Clean up via `afterEach` (no special handling — the `currentPollId` assignment ensures cleanup).

    `afterEach` (inside the outer describe): `if (currentPollId) { await cleanupPoll({ serviceRole, pollId: currentPollId }) }`. Use a module-scope `let currentPollId: string | null = null` set in the body and reset in afterEach.

    Do NOT add `it.skip` or `it.todo` to any cell — every cell must run (D-23 — merge blocker). Do NOT add `try/catch` around the assertions (let Vitest surface failures).

    For the special case `anon × voted=true`: the test still casts a vote (via serviceRole), BUT the assertion is `expected === 0` because anon has no granting policy on `vote_counts`. This mirrors Pitfall 1's guarantee.

    Acceptable performance: 12 cells × ~2 sec each = ~24 sec. The Plan 00 vitest config sets `testTimeout: 30_000` so single-cell DB round-trips are accommodated. Parallel cells via `describe.each` are vitest-default; if test isolation requires serial execution (e.g., to avoid poll-creation race conditions), set `{ concurrent: false }` on the inner describe.
  </action>
  <acceptance_criteria>
    - `e2e/integration/vote-counts-rls.test.ts` contains zero `it.todo(` calls: `grep -c "it\\.todo" e2e/integration/vote-counts-rls.test.ts` returns 0
    - `e2e/integration/vote-counts-rls.test.ts` contains zero `it.skip(` calls
    - File contains a `describe.each` block over a 12-row case array (the Plan 00 scaffold preserved): `grep -c "describe.each" e2e/integration/vote-counts-rls.test.ts` returns ≥1
    - File imports helpers from `./helpers`: `grep -E "from '\\./helpers'" e2e/integration/vote-counts-rls.test.ts` returns ≥1 match
    - File queries `vote_counts` table: `grep -E "from\\('vote_counts'\\)" e2e/integration/vote-counts-rls.test.ts` returns ≥1
    - File contains `cleanupPoll` invocation inside `afterEach`: `grep -B 2 "cleanupPoll" e2e/integration/vote-counts-rls.test.ts | grep -E "afterEach"` returns ≥1 match
    - REVIEW-FIX-H1: `castVote` call uses the new signature with `serviceRole` and explicit `userId` — `grep -E "castVote\\(\\{[^}]*serviceRole[^}]*\\}\\)" e2e/integration/vote-counts-rls.test.ts` returns ≥1 match AND `grep -E "userId:\\s*fixtureUsers\\.memberUser\\.id" e2e/integration/vote-counts-rls.test.ts` returns ≥1 match
    - REVIEW-FIX-H2: `createFreshPoll` calls use `seedBaseline: true` (default) for the matrix cells — `grep -cE "createFreshPoll\\(" e2e/integration/vote-counts-rls.test.ts` returns ≥1 (every cell creates a poll); none of the matrix-cell calls pass `seedBaseline: false` (only the admin-JWT sanity case does)
    - REVIEW-FIX-M7: Admin-JWT direct-read sanity case present — `grep -cE "admin JWT direct-read" e2e/integration/vote-counts-rls.test.ts` returns ≥1 AND `grep -E "mintClients\\(\\{\\s*authAs:\\s*'adminUser'" e2e/integration/vote-counts-rls.test.ts` returns ≥1
    - REVIEW-FIX-C2-H5 (regression sentinel): The admin-JWT sanity case seeds a vote_counts row BY memberUser (NOT admin) before asserting admin sees 0 rows — verify via: `awk '/admin JWT direct-read/,/^\\s*\\}\\)/{print}' e2e/integration/vote-counts-rls.test.ts | grep -qE "userId:\\s*fixtureUsers\\.memberUser\\.id"` exits 0 (the castVote call inside the sentinel block uses memberUser.id, not admin's). The case is NOT a false negative — if `seedBaseline: false` is passed to `createFreshPoll` it MUST be paired with a `castVote(...)` call keyed on memberUser.id in the same `it` block: `awk '/admin JWT direct-read/,/^\\s*\\}\\)/{print}' e2e/integration/vote-counts-rls.test.ts | grep -c "castVote\\(" | xargs test 1 -le` exits 0
    - `npm run test:integration -- --run vote-counts-rls.test.ts` exits 0 with 13 tests passing (12 matrix + 1 admin-JWT sanity): `npm run test:integration -- --run vote-counts-rls.test.ts 2>&1 | grep -E "13.*pass"` returns ≥1 match
    - The "anon × voted=true × hidden=false" cell returns 0 rows (Pitfall 1 — anon has no granting policy regardless of voted state) — verifiable by deliberately breaking the assertion temporarily and re-running; in the green state, the test passes
    - File has zero review-round / phase-ID archaeology
  </acceptance_criteria>
  <verify>
    <automated>grep -c "it\\.todo" e2e/integration/vote-counts-rls.test.ts | xargs test 0 -eq && grep -c "describe.each" e2e/integration/vote-counts-rls.test.ts | xargs test 1 -le && grep -qE "from\\('vote_counts'\\)" e2e/integration/vote-counts-rls.test.ts && grep -qE "userId:\\s*fixtureUsers\\.memberUser\\.id" e2e/integration/vote-counts-rls.test.ts && grep -qE "mintClients\\(\\{\\s*authAs:\\s*'adminUser'" e2e/integration/vote-counts-rls.test.ts && npm run test:integration -- --run vote-counts-rls.test.ts 2>&1 | grep -qE "13.*pass|✓.*vote_counts"</automated>
  </verify>
  <done>
    12-cell matrix passes in full + 1 admin-JWT direct-read sanity case (REVIEW-FIX-M7) confirms the JWT-admin path is gated like any other authenticated user (REVIEW-FIX-H3 enforcement). Every cell where `hidden=true` OR caller has not voted returns 0 rows from `vote_counts`. Only `authenticated × hidden=false × voted=true` returns >0. Service-role returns >0 in all 4 of its cells (baseline vote from REVIEW-FIX-H2). Cleanup runs after every cell.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02: TEST-12 admin EF authz + audit row body</name>
  <files>e2e/integration/toggle-results-visibility.test.ts</files>
  <behavior>
    - Case 1: memberUser JWT → status === 403
    - Case 2: adminUser JWT + `{ poll_id, hidden: true }` against `results_hidden=false` poll → status === 200, response.poll.results_hidden === true, response.poll.results_hidden_changed_at is parseable ISO timestamp (non-null)
    - Case 3: After Case 2 succeeds, `audit_log` has exactly one row with `action='results_hidden_toggled'`, `actor_id=adminUser.id`, `target_id=poll.id`, `before={results_hidden:false}`, `after={results_hidden:true}`
    - Case 4: After Case 2/3, invoking again with `{ poll_id, hidden: true }` (no-op) → status === 200, but the audit_log count for `action='results_hidden_toggled'` AND `target_id=poll.id` is still 1 (idempotency, D-11)
  </behavior>
  <read_first>
    - e2e/integration/helpers.ts (Plan 00 — `mintClients`, `createFreshPoll`, `invokeEF`, `readAuditLog`, `cleanupPoll`)
    - e2e/integration/toggle-results-visibility.test.ts (Plan 00 scaffold — 4 `it.todo` placeholders)
    - supabase/functions/toggle-results-visibility/index.ts (Plan 02 — confirms request body shape `{ poll_id, hidden }` and response shape `{ poll: <updated row> }`)
    - supabase/functions/_shared/admin-auth.ts (existing — confirms 403 status comes from `adminCheckResponse` on `requireAdmin` failure)
    - e2e/fixtures/test-users.ts (`fixtureUsers.memberUser`, `fixtureUsers.adminUser`)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "e2e/integration/toggle-results-visibility.test.ts" (audit_log read pattern)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-24 (TEST-12 cells), D-11 (no-op no-audit)
  </read_first>
  <action>
    Open `e2e/integration/toggle-results-visibility.test.ts` and replace the 4 `it.todo` placeholders with real `it` bodies.

    REVIEW-FIX-M6 — order-independent design: Each `it` case creates its OWN fresh poll via `beforeEach` and cleans up via `afterEach`. No `beforeAll` poll sharing across cases. This eliminates the Codex MEDIUM concern that Case 3 depends on Case 2 having toggled the poll.

    Top-of-file imports: `import { describe, it, expect, beforeAll, beforeEach, afterEach } from 'vitest'`; `import { mintClients, createFreshPoll, invokeEF, readAuditLog, cleanupPoll } from './helpers'`; `import { fixtureUsers } from '../fixtures/test-users'`.

    Module-scope state (minted ONCE — clients are stateless after mint): `let memberClients`, `let adminClients`. In `beforeAll`: `memberClients = await mintClients({ authAs: 'memberUser' }); adminClients = await mintClients({ authAs: 'adminUser' });`.

    Per-case state (fresh each `it`): `let pollId: string;`. In `beforeEach`: `const poll = await createFreshPoll({ hidden: false, suiteSlug: 'test-12', seedBaseline: false }); pollId = poll.id;` (seedBaseline=false because TEST-12 cares about audit-log rows for `results_hidden_toggled`, not vote_counts — the baseline vote would create an unrelated row, which is fine but adds noise to the audit-log target-id namespace if we ever extend assertions). In `afterEach`: explicitly `await adminClients.serviceRole.from('audit_log').delete().eq('target_id', pollId); await cleanupPoll({ serviceRole: adminClients.serviceRole, pollId });`. The audit_log DELETE is required because migration 10's `audit_log.target_id` has no FK to `polls.id` (per Plan 01) — cascades do not apply.

    Case 1 (`'non-admin caller returns 403'`): `const result = await invokeEF({ client: memberClients.authed, name: 'toggle-results-visibility', body: { poll_id: pollId, hidden: true } }); expect(result.status).toBe(403);`. The memberUser JWT will trip the `requireAdmin` gate in `_shared/admin-auth.ts`. The mutation MUST NOT have happened — assert by checking `const { data } = await adminClients.serviceRole.from('polls').select('results_hidden').eq('id', pollId).single(); expect(data.results_hidden).toBe(false);` to confirm the 403 was a hard stop. Also assert NO audit row was written for the failed toggle attempt: `const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: pollId, action: 'results_hidden_toggled' }); expect(rows).toHaveLength(0);`.

    Case 2 (`'admin caller returns 200 with updated poll row + non-null results_hidden_changed_at'`): `const result = await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { poll_id: pollId, hidden: true } }); expect(result.status).toBe(200);`. Then `expect(result.data.poll.id).toBe(pollId); expect(result.data.poll.results_hidden).toBe(true); expect(result.data.poll.results_hidden_changed_at).not.toBeNull(); expect(new Date(result.data.poll.results_hidden_changed_at).toISOString()).toBe(result.data.poll.results_hidden_changed_at);` (the round-trip-through-Date assertion proves the timestamp is a parseable ISO string).

    Case 3 (`'audit_log row written on state change'`, REVIEW-FIX-M6 independent): This case STARTS fresh — fresh poll from beforeEach has `results_hidden=false`. Run the toggle inside the test: `await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { poll_id: pollId, hidden: true } });`. Then `const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: pollId, action: 'results_hidden_toggled' }); expect(rows).toHaveLength(1); expect(rows[0].actor_id).toBe(fixtureUsers.adminUser.id); expect(rows[0].action).toBe('results_hidden_toggled'); expect(rows[0].target_type).toBe('poll'); expect(rows[0].before).toEqual({ results_hidden: false }); expect(rows[0].after).toEqual({ results_hidden: true });`.

    Case 4 (`'no audit_log row on no-op call (idempotency)'`, REVIEW-FIX-M6 independent): This case STARTS fresh. First, set the poll to hidden=true via a service-role direct UPDATE to bypass EF audit-row-writes (so the case can later assert "no audit row for the no-op without baseline-row noise"): `await adminClients.serviceRole.from('polls').update({ results_hidden: true, results_hidden_changed_at: new Date().toISOString() }).eq('id', pollId);`. Then invoke the EF with the SAME hidden value: `const result = await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { poll_id: pollId, hidden: true } }); expect(result.status).toBe(200);` (idempotent — no 409). Then `const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: pollId, action: 'results_hidden_toggled' }); expect(rows).toHaveLength(0);` — NO audit row was written because the conditional UPDATE (REVIEW-FIX-H4) matched 0 rows (state didn't change). The EF returned the current poll row via the no-op SELECT path.

    REVIEW-FIX-L3 — Case 5 (`'invalid body — missing poll_id returns 400'`): `const result = await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { hidden: true } }); expect(result.status).toBe(400);`. Case 6 (`'invalid UUID for poll_id returns 400'`): `const result = await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { poll_id: 'not-a-uuid', hidden: true } }); expect(result.status).toBe(400);`. Case 7 (`'404 for non-existent poll'`): `const result = await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { poll_id: '00000000-0000-0000-0000-000000000000', hidden: true } }); expect(result.status).toBe(404);` — distinguishes "no-op same state" (200) from "missing poll" (404).

    Do NOT mock the EF — invoke against the live local Supabase functions runtime. The Plan 00 `invokeEF` helper hits `${SUPABASE_URL}/functions/v1/toggle-results-visibility` with a real `Authorization: Bearer <session.access_token>`.
  </action>
  <acceptance_criteria>
    - `e2e/integration/toggle-results-visibility.test.ts` contains zero `it.todo(` calls
    - REVIEW-FIX-L3: File has exactly 7 `it(` blocks (4 happy-path + 3 negative): `grep -cE "^\\s*it\\(" e2e/integration/toggle-results-visibility.test.ts` returns 7
    - File imports from `./helpers`: `grep -E "from '\\./helpers'" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - File imports `fixtureUsers` from `../fixtures/test-users`: `grep -E "fixtureUsers.*test-users" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - REVIEW-FIX-M6: Uses `beforeEach`/`afterEach` (not just `beforeAll`/`afterAll`) for fresh-poll-per-case isolation: `grep -cE "beforeEach\\(" e2e/integration/toggle-results-visibility.test.ts` returns ≥1 AND `grep -cE "afterEach\\(" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - Case 1 asserts `expect(result.status).toBe(403)`: `grep -E "status.*toBe\\(403\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - Case 2 asserts `expect(result.status).toBe(200)` AND `results_hidden_changed_at` non-null check: `grep -E "results_hidden_changed_at" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - Case 3 reads `audit_log` via `readAuditLog`: `grep -c "readAuditLog" e2e/integration/toggle-results-visibility.test.ts` returns ≥4 (Cases 1 negative-audit-check, 3, 4 — at least 3 distinct calls)
    - Case 3 asserts the `before`/`after` JSONB shape: `grep -E "toEqual\\(\\{\\s*results_hidden:\\s*false\\s*\\}\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1 AND `grep -E "toEqual\\(\\{\\s*results_hidden:\\s*true\\s*\\}\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - REVIEW-FIX-H4 verification: Case 4 asserts `expect(rows).toHaveLength(0)` after no-op (no audit row at all on the conditional-UPDATE 0-row path): `grep -cE "toHaveLength\\(0\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥2 (Case 1 NO audit + Case 4 NO audit)
    - REVIEW-FIX-L3: Case 5 / 6 assert 400 status for malformed body and non-UUID poll_id: `grep -cE "status.*toBe\\(400\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥2
    - REVIEW-FIX-L3: Case 7 asserts 404 for non-existent poll: `grep -cE "status.*toBe\\(404\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - `afterEach` cleans up both `audit_log` rows AND the poll: `grep -E "from\\('audit_log'\\)\\.delete\\(\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1 match AND `grep -c "cleanupPoll" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - `npm run test:integration -- --run toggle-results-visibility.test.ts` exits 0 with 7 tests passing: output contains `7 passed` or `7 tests`
    - File has zero review-round / phase-ID archaeology
  </acceptance_criteria>
  <verify>
    <automated>grep -c "it\\.todo" e2e/integration/toggle-results-visibility.test.ts | xargs test 0 -eq && grep -cE "^\\s*it\\(" e2e/integration/toggle-results-visibility.test.ts | xargs test 7 -eq && grep -qE "status.*toBe\\(403\\)" e2e/integration/toggle-results-visibility.test.ts && grep -qE "status.*toBe\\(200\\)" e2e/integration/toggle-results-visibility.test.ts && grep -cE "status.*toBe\\(400\\)" e2e/integration/toggle-results-visibility.test.ts | xargs test 2 -le && grep -qE "status.*toBe\\(404\\)" e2e/integration/toggle-results-visibility.test.ts && grep -qE "from\\('audit_log'\\)\\.delete" e2e/integration/toggle-results-visibility.test.ts && npm run test:integration -- --run toggle-results-visibility.test.ts 2>&1 | grep -qE "7.*pass|✓.*toggle-results"</automated>
  </verify>
  <done>
    All 7 TEST-12 cases pass (4 happy-path + 3 negative): 403 for member with no side effect, 200 + updated poll for admin, audit row written on state change, NO audit row on no-op (REVIEW-FIX-H4 conditional UPDATE), 400 for malformed/invalid-UUID body, 404 for non-existent poll. Each case uses a fresh poll from beforeEach (REVIEW-FIX-M6). Cleanup deletes both audit rows and the poll.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-03: REVIEW-FIX-M5 — create-poll results_hidden=true path integration test</name>
  <files>e2e/integration/create-poll-results-hidden.test.ts</files>
  <behavior>
    - Create a poll via the `create-poll` EF with `body: { ..., results_hidden: true }` and assert: status 200, response.poll.results_hidden === true, audit_log has 2 rows (`poll_created` with `after.results_hidden === true`, AND `results_hidden_set_at_create`)
    - Repeat with `results_hidden: false` and assert: status 200, response.poll.results_hidden === false, audit_log has 1 row (`poll_created` only, with `after.results_hidden === false`)
    - Repeat with `results_hidden` omitted from body and assert: status 200, response.poll.results_hidden === false (column DEFAULT), audit_log has 1 row (`poll_created`)
    - Negative case: `results_hidden: 'true'` (string, not boolean) → 400
  </behavior>
  <read_first>
    - e2e/integration/helpers.ts (Plan 00 — `mintClients`, `invokeEF`, `readAuditLog`, `cleanupPoll`)
    - supabase/functions/create-poll/index.ts (Plan 11-03b — request body shape, especially the post-RPC UPDATE + 2 audit rows)
    - e2e/fixtures/test-users.ts (`fixtureUsers.adminUser` for the admin JWT client)
    - .planning/phases/11-schema-rls-ef-foundations/11-03b-PLAN.md (the action-string locks and the REVIEW-FIX-H5 compensation contract)
  </read_first>
  <action>
    Create `e2e/integration/create-poll-results-hidden.test.ts`.

    Top-of-file imports: `import { describe, it, expect, beforeAll, afterEach } from 'vitest'`; `import { mintClients, invokeEF, readAuditLog, cleanupPoll } from './helpers'`; `import { fixtureUsers } from '../fixtures/test-users'`.

    Module-scope: `let adminClients;`. `beforeAll`: `adminClients = await mintClients({ authAs: 'adminUser' });`.

    Per-case state: `let createdPollId: string | null = null;`. `afterEach`: `if (createdPollId) { await adminClients.serviceRole.from('audit_log').delete().eq('target_id', createdPollId); await cleanupPoll({ serviceRole: adminClients.serviceRole, pollId: createdPollId }); createdPollId = null; }`.

    Helper inline (REVIEW-FIX-C2-H6 — choices MUST be `string[]` matching the live EF contract verified at `supabase/functions/create-poll/index.ts:86-97`): `const buildBody = (extra: Record<string, unknown>) => ({ title: \`[TEST-M5] \${Date.now()}-\${Math.random().toString(36).slice(2,6)}\`, description: 'integration test', category_id: 'a0000000-0000-0000-0000-000000000001', choices: ['option-a', 'option-b'], closes_at: new Date(Date.now() + 7*24*60*60*1000).toISOString(), ...extra });` — the `choices` field is a plain array of strings, NOT an array of `{text}` objects (that imagined shape was the cycle-2 Codex HIGH #4 / HIGH #6 root cause).

    REVIEW-FIX-C2-H6 — response shape: the LIVE `create-poll` EF (`supabase/functions/create-poll/index.ts:135`) returns `{ success: true, id: pollId }` on success — NOT `{ poll: <row> }`. ALL three cases below read the new poll id via `result.data.id` (NOT `result.data.poll.id`) and verify the resulting `results_hidden` column value via a service-role SELECT on `polls` (the EF response does not carry the row).

    Case 1 — `'create-poll with results_hidden=true emits poll_created + results_hidden_set_at_create audit rows'`:
      ```ts
      const result = await invokeEF({ client: adminClients.authed, name: 'create-poll', body: buildBody({ results_hidden: true }) });
      expect(result.status).toBe(200);
      expect((result.data as { success: boolean }).success).toBe(true);
      const newPollId = (result.data as { id: string }).id;
      expect(typeof newPollId).toBe('string');
      createdPollId = newPollId;
      // Verify the resulting polls row has results_hidden = true via service-role SELECT (EF response does not carry the row):
      const { data: pollRow, error: selErr } = await adminClients.serviceRole.from('polls').select('results_hidden, results_hidden_changed_at').eq('id', newPollId).single();
      expect(selErr).toBeNull();
      expect(pollRow!.results_hidden).toBe(true);
      expect(pollRow!.results_hidden_changed_at).not.toBeNull();
      // Audit row assertions — exactly two rows:
      const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: newPollId });
      expect(rows).toHaveLength(2);
      const created = rows.find(r => r.action === 'poll_created');
      const setHidden = rows.find(r => r.action === 'results_hidden_set_at_create');
      expect(created).toBeDefined();
      expect(setHidden).toBeDefined();
      expect(created!.after).toMatchObject({ results_hidden: true });
      expect(setHidden!.after).toEqual({ results_hidden: true });
      ```

    Case 2 — `'create-poll with results_hidden=false emits only poll_created audit row'`:
      ```ts
      const result = await invokeEF({ client: adminClients.authed, name: 'create-poll', body: buildBody({ results_hidden: false }) });
      expect(result.status).toBe(200);
      const newPollId = (result.data as { id: string }).id;
      createdPollId = newPollId;
      const { data: pollRow } = await adminClients.serviceRole.from('polls').select('results_hidden').eq('id', newPollId).single();
      expect(pollRow!.results_hidden).toBe(false);
      const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: newPollId });
      expect(rows).toHaveLength(1);
      expect(rows[0].action).toBe('poll_created');
      expect(rows[0].after).toMatchObject({ results_hidden: false });
      ```

    Case 3 — `'create-poll with results_hidden omitted uses column DEFAULT (false)'`:
      ```ts
      const result = await invokeEF({ client: adminClients.authed, name: 'create-poll', body: buildBody({}) });  // no results_hidden field
      expect(result.status).toBe(200);
      const newPollId = (result.data as { id: string }).id;
      createdPollId = newPollId;
      const { data: pollRow } = await adminClients.serviceRole.from('polls').select('results_hidden').eq('id', newPollId).single();
      expect(pollRow!.results_hidden).toBe(false);
      const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: newPollId });
      expect(rows).toHaveLength(1);
      expect(rows[0].action).toBe('poll_created');
      ```

    Case 4 — REVIEW-FIX-H6 action-string check: `'audit row uses results_hidden_set_at_create (NOT _at_creation)'` is implicit in Case 1's `rows.find(r => r.action === 'results_hidden_set_at_create')`. If the implementer accidentally writes `_at_creation`, `setHidden` is `undefined` and `expect(setHidden).toBeDefined()` fails — surfacing the action-string drift Codex flagged.

    Case 5 (negative) — `'create-poll with results_hidden as string returns 400'`:
      ```ts
      const result = await invokeEF({ client: adminClients.authed, name: 'create-poll', body: buildBody({ results_hidden: 'true' }) });
      expect(result.status).toBe(400);
      ```

    REVIEW-FIX-H5 sanity (optional — execution-test only, not a unit assertion): document in a comment that if `create-poll` ever fails the post-RPC UPDATE while `results_hidden: true` is in the body, the compensation DELETE means NO poll appears in `polls` AND NO audit rows are written. This is hard to test directly without injecting a fault — flag in the SUMMARY as "manual fault-injection test deferred".
  </action>
  <acceptance_criteria>
    - File `e2e/integration/create-poll-results-hidden.test.ts` exists
    - File has exactly 4 `it(` blocks (or 5 if the explicit action-string check is added as its own block): `grep -cE "^\\s*it\\(" e2e/integration/create-poll-results-hidden.test.ts` returns 4 or 5
    - File asserts the dual-audit-row pattern with EXACT action strings — `grep -cE "results_hidden_set_at_create" e2e/integration/create-poll-results-hidden.test.ts` returns ≥1 AND `grep -cE "results_hidden_set_at_creation" e2e/integration/create-poll-results-hidden.test.ts` returns 0 (REVIEW-FIX-H6 — the wrong action string must NOT appear)
    - REVIEW-FIX-C2-H6 (choices contract): `buildBody` (or any test body) sends `choices: ['option-a', 'option-b']` as a string array — `grep -cE "choices:\\s*\\[\\s*'option-a'" e2e/integration/create-poll-results-hidden.test.ts` returns ≥1 AND `grep -cE "choices:\\s*\\[\\s*\\{\\s*text:" e2e/integration/create-poll-results-hidden.test.ts` returns 0 (the wrong `{text: ...}[]` shape must NOT appear)
    - REVIEW-FIX-C2-H6 (response contract): No test body reads `result.data.poll.id` or `result.data.poll.results_hidden` (the EF returns `{success, id}`, not `{poll: <row>}`) — `grep -cE "result\\.data\\.poll\\." e2e/integration/create-poll-results-hidden.test.ts` returns 0 AND `grep -cE "result\\.data as \\{[^}]*id:\\s*string" e2e/integration/create-poll-results-hidden.test.ts` returns ≥1 (the corrected `result.data.id` typecast appears)
    - REVIEW-FIX-C2-H6 (verification path): Each case fetches the created poll via service-role SELECT to verify `results_hidden` — `grep -cE "from\\('polls'\\)\\.select\\(.*results_hidden" e2e/integration/create-poll-results-hidden.test.ts` returns ≥3 (one per Case 1/2/3)
    - File asserts `toHaveLength(2)` (Case 1) AND `toHaveLength(1)` (Cases 2 and 3) on `readAuditLog` results: `grep -cE "toHaveLength\\((1|2)\\)" e2e/integration/create-poll-results-hidden.test.ts` returns ≥3
    - Negative case asserts 400: `grep -cE "status.*toBe\\(400\\)" e2e/integration/create-poll-results-hidden.test.ts` returns ≥1
    - `afterEach` cleans up audit_log + poll: `grep -qE "from\\('audit_log'\\)\\.delete" e2e/integration/create-poll-results-hidden.test.ts` AND `grep -qE "cleanupPoll" e2e/integration/create-poll-results-hidden.test.ts`
    - `npm run test:integration -- --run create-poll-results-hidden.test.ts` exits 0 with all cases passing
    - File has zero review-round / phase-ID archaeology in source comments
  </acceptance_criteria>
  <verify>
    <automated>test -f e2e/integration/create-poll-results-hidden.test.ts && grep -qE "results_hidden_set_at_create" e2e/integration/create-poll-results-hidden.test.ts && ! grep -qE "results_hidden_set_at_creation" e2e/integration/create-poll-results-hidden.test.ts && npm run test:integration -- --run create-poll-results-hidden.test.ts 2>&1 | grep -qE "(4|5).*pass"</automated>
  </verify>
  <done>
    All cases pass: create-poll with results_hidden=true emits 2 audit rows (poll_created + results_hidden_set_at_create); =false or omitted emits 1 (poll_created); string value returns 400. Action-string `results_hidden_set_at_create` is locked (no `_at_creation` drift). Plan 11-03b's runtime evidence is now captured at the integration-test layer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test caller role → DB | Tests intentionally probe the trust boundary that production code defends; assertions must be deterministic regardless of test ordering within a cell |
| Test setup → cleanup | Setup-time poll creation must not leak rows across tests; cleanup must rethrow on failure to surface DB issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-RLS-LEAK | Information Disclosure | TEST-11 12-cell matrix is the primary defense | mitigate (P0 merge blocker) | Every cell asserted (no `.skip`, no `.todo`); 12 cells cover all (role × hidden × voted) combinations including `anon × voted=true × hidden=false` which would otherwise be an easy-to-miss leak vector. Failure narrative names the failing combination via `describe.each` template. |
| T-EF-ADMIN-BYPASS | Elevation of Privilege | TEST-12 case 1 + post-403 state assertion | mitigate | Case 1 asserts 403 AND that `polls.results_hidden` did NOT flip (the EF must hard-stop at the admin gate, not partially execute). Without the post-state check, a passing 403 could mask a partial-execute bug. |
| T-AUDIT-DRIFT | Repudiation | TEST-12 case 3 audit shape assertion | mitigate | Asserts the full audit row shape: `actor_id`, `action`, `target_type`, `target_id`, `before`, `after`. Drift in any of these (e.g., a future EF change that renames the action string) breaks the test. |
| T-IDEMPOTENCY | Repudiation | TEST-12 case 4 no-op no-audit | mitigate | Case 4 asserts `toHaveLength(1)` AFTER the no-op (still only Case 2's row). The matching positive case (Case 3) and the no-op case (Case 4) together validate D-11. |
| T-TEST-LEAK | Information Disclosure (test data) | Poll + audit_log cleanup | mitigate | `afterAll` explicitly DELETEs `audit_log.target_id = pollId` rows BEFORE calling `cleanupPoll` (audit_log has no FK to polls per migration 10 schema — cascades do not apply). Cleanup failure rethrows via Plan 00 helper. |
| T-FLAKY-TIMESTAMP | Repudiation | Timestamp assertion strictness | accept | Test asserts `results_hidden_changed_at` is non-null and ISO-parseable, but does NOT assert strict inequality between Case 2 and Case 4 timestamps (millisecond resolution + serial cases could share a timestamp). Non-null is sufficient to verify D-13 (EF writes the timestamp on every UPDATE). |
</threat_model>

<verification>
- `npm run test:integration` exits 0 (full suite — both files combined)
- TEST-11 reports 12 passing cases; TEST-12 reports 4 passing cases
- After running, `audit_log` and `polls` have no `[TEST-11]` or `[TEST-12]`-prefixed rows leaked (verifiable via `psql` against local Supabase: `SELECT * FROM polls WHERE title LIKE '[TEST-1%]'` returns 0 rows; `SELECT * FROM audit_log WHERE action='results_hidden_toggled' AND created_at > now() - interval '1 hour'` returns rows ONLY for the most recent run's cleanup window)
- No `it.skip`, no `it.todo`, no `it.only` in either file (acceptance criterion enforced)
</verification>

<success_criteria>
- ROADMAP SC-2 satisfied (12-cell matrix passes; no cell skipped)
- ROADMAP SC-3 satisfied (admin EF authz: 403/200; updated poll row; audit row written)
- VIS-05 + TEST-11 + TEST-12 fully covered
- Phase 11 is execution-complete pending Plan 05's deploy gate
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-rls-ef-foundations/11-04-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Declare `requirements-completed: [VIS-05, TEST-11, TEST-12]`. Include the test-run output evidence (e.g., the final Vitest summary line: "12 passed in vote-counts-rls.test.ts / 4 passed in toggle-results-visibility.test.ts").
</output>
