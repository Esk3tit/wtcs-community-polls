---
phase: 11-schema-rls-ef-foundations
plan: 04
type: execute
wave: 3
depends_on: [00, 01, 02, 03, "03b"]
files_modified:
  - e2e/integration/vote-counts-rls.test.ts
  - e2e/integration/toggle-results-visibility.test.ts
autonomous: true
requirements:
  - VIS-05
  - TEST-11
  - TEST-12
tags:
  - testing
  - integration
  - vitest
  - rls
  - audit

must_haves:
  truths:
    - "TEST-11 12-cell matrix passes in full — every cell where `results_hidden = true` OR caller has not voted returns 0 rows from `vote_counts`; only `authenticated × hidden=false × voted` returns >0 rows; service-role returns >0 rows in all 4 of its cells (RLS bypass)"
    - "TEST-11 uses `describe.each` over a 12-row case table — no `it.skip`, no `it.todo`, every cell asserted; merge-blocker behavior matches REQUIREMENTS.md TEST-11 wording"
    - "TEST-12 case 1: invoking `toggle-results-visibility` with a `memberUser` (non-admin) JWT returns HTTP 403"
    - "TEST-12 case 2: invoking with an `adminUser` JWT returns HTTP 200 with `{ poll: { ..., results_hidden: <new>, results_hidden_changed_at: <ISO string>, ... } }` (non-null timestamp)"
    - "TEST-12 case 3: a state-changing toggle (e.g., false→true) writes exactly one `audit_log` row with `action='results_hidden_toggled'`, `actor_id=adminUser.id`, `before.results_hidden=false`, `after.results_hidden=true`"
    - "TEST-12 case 4: a no-op call (true→true on a poll already hidden) returns 200 but does NOT write an additional `audit_log` row (idempotency, D-11)"
    - "Both suites use the `e2e/integration/helpers.ts` exports from Plan 00 — no Playwright imports, no duplicate client-minting code"
    - "Both suites clean up created polls in `afterEach` and rethrow on cleanup failure (Plan 00 helper handles this via `cleanupPoll`)"
    - "`npm run test:integration` exits 0 with the 12-cell matrix + 4 EF cases all passing (no `.todo`, no `.skip`)"
    - "D-23: TEST-11 asserts the row count returned from `SELECT * FROM vote_counts WHERE poll_id = $1` for each of the 12 cells — 3 roles (anon, authenticated test user, service-role) × 2 hidden states (`results_hidden=true`, `results_hidden=false`) × 2 voter states (voted, not voted). Expected matrix: ONLY `(authenticated, hidden=false, voted)` returns rows; service-role returns rows in all 4 of its cells (bypass); all other cells return 0 rows. Any cell failing BLOCKS MERGE."
    - "D-24: TEST-12 lives alongside TEST-11 under `e2e/integration/` (same Vitest runner, same helpers). It asserts: non-admin caller → 403; admin caller → 200 with updated poll row including the new `results_hidden` value AND a non-null `results_hidden_changed_at`; an `audit_log` row is written for every state-changing toggle; an `audit_log` row is NOT written on no-op calls (per D-11)."
  artifacts:
    - path: "e2e/integration/vote-counts-rls.test.ts"
      provides: "TEST-11 12-cell RLS invariant matrix (TEST-11)"
      contains: "describe.each"
    - path: "e2e/integration/toggle-results-visibility.test.ts"
      provides: "TEST-12 admin EF authz + audit assertions (TEST-12)"
      contains: "results_hidden_toggled"
  key_links:
    - from: "vote-counts-rls.test.ts"
      to: "e2e/integration/helpers.ts (Plan 00)"
      via: "import { mintClients, createFreshPoll, castVote, cleanupPoll }"
      pattern: "from '\\./helpers'"
    - from: "toggle-results-visibility.test.ts"
      to: "supabase/functions/toggle-results-visibility/index.ts (Plan 02)"
      via: "invokeEF helper hitting /functions/v1/toggle-results-visibility"
      pattern: "toggle-results-visibility"
    - from: "toggle-results-visibility.test.ts"
      to: "audit_log table (migration 10)"
      via: "readAuditLog helper SELECT"
      pattern: "readAuditLog"
---

<objective>
Wire the bodies of the two integration tests scaffolded in Plan 00. TEST-11 asserts the 12-cell RLS invariant matrix on `vote_counts` (P0 merge blocker per STATE.md and REQUIREMENTS.md). TEST-12 asserts the admin EF authorization contract for `toggle-results-visibility` plus the audit row presence/absence on state-change vs no-op.

Purpose: TEST-11 is the primary defense against RLS leakage at the `vote_counts` view — a leak would expose pre-aggregated vote counts to non-voters through the Supabase anon key. TEST-12 is the primary defense against admin-EF authz bypass + audit drift. Both suites block merge per REQUIREMENTS.md.

Output: Two test files with bodies filled in. The `it.todo` placeholders from Plan 00 become real `it` blocks with assertions. No new helper functions (Plan 00 already provided `mintClients`, `createFreshPoll`, `castVote`, `invokeEF`, `readAuditLog`, `cleanupPoll`); no changes to migration 10, the audit helper, or any EF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md
@.planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md
@.planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md
@.planning/phases/11-schema-rls-ef-foundations/11-VALIDATION.md

<!-- Plan 00 outputs (consumed verbatim) -->
@e2e/integration/helpers.ts
@e2e/integration/vote-counts-rls.test.ts
@e2e/integration/toggle-results-visibility.test.ts

<!-- Plan 01 and Plan 02 outputs that this plan exercises -->
@supabase/migrations/00000000000010_results_hidden_audit.sql
@supabase/functions/toggle-results-visibility/index.ts

<!-- Test-pattern analog -->
@src/__tests__/admin/polls-effective-invariant.test.ts
@e2e/fixtures/test-users.ts

<interfaces>
<!-- 12-cell matrix expected outcomes (D-23, RESEARCH Pitfall 1) -->

| role | hidden | voted | expected vote_counts rows |
|------|--------|-------|---------------------------|
| anon | false  | false | 0 |
| anon | false  | true  | 0 (no anon-granting policy — anon never sees vote_counts) |
| anon | true   | false | 0 |
| anon | true   | true  | 0 |
| authed (memberUser) | false | false | 0 (voted=false fails the voted-existence EXISTS clause) |
| authed (memberUser) | false | true  | >0 (ONLY allowed cell — voted AND hidden=false AND authenticated) |
| authed (memberUser) | true  | false | 0 |
| authed (memberUser) | true  | true  | 0 (voted but hidden=true) |
| serviceRole | false | false | >0 (RLS bypass) |
| serviceRole | false | true  | >0 (RLS bypass) |
| serviceRole | true  | false | >0 (RLS bypass) |
| serviceRole | true  | true  | >0 (RLS bypass) |

<!-- TEST-12 expected behaviors (D-24, VIS-03) -->

case 1: invoke as memberUser with valid body → status === 403
case 2: invoke as adminUser with { poll_id, hidden: true } against a poll currently false →
  status === 200
  data.poll.id === poll_id
  data.poll.results_hidden === true
  data.poll.results_hidden_changed_at is non-null + parseable as ISO timestamp
case 3: after case 2, readAuditLog({ targetId: poll_id, action: 'results_hidden_toggled' }) →
  rows.length === 1
  rows[0].actor_id === adminUser.id
  rows[0].before === { results_hidden: false }
  rows[0].after === { results_hidden: true }
case 4: invoke as adminUser with { poll_id, hidden: true } against a poll already hidden=true →
  status === 200
  readAuditLog rows for this poll_id with action='results_hidden_toggled' has the SAME count as before (no new row written)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-01: TEST-11 12-cell RLS invariant matrix body</name>
  <files>e2e/integration/vote-counts-rls.test.ts</files>
  <behavior>
    - 12 cells in `describe.each`; each cell creates a fresh poll, optionally casts a vote, then SELECTs `* FROM vote_counts WHERE poll_id = $1` via the per-cell role client
    - anon × any state → 0 rows (no anon-granting policy exists)
    - authed × hidden=false × voted=true → >0 rows
    - authed × all other combinations → 0 rows
    - serviceRole × all combinations → >0 rows (RLS bypass)
    - afterEach cleans up the poll via `cleanupPoll`
  </behavior>
  <read_first>
    - e2e/integration/helpers.ts (Plan 00 — `mintClients`, `createFreshPoll`, `castVote`, `cleanupPoll`, `AuditRow`)
    - e2e/fixtures/test-users.ts (`fixtureUsers.memberUser`, `fixtureUsers.adminUser`)
    - e2e/integration/vote-counts-rls.test.ts (Plan 00 scaffold — the 12-row case array stays; replace `it.todo` with `it`)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "e2e/integration/vote-counts-rls.test.ts" (assertion shape; failure-message convention)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-20, D-23 (Vitest + describe.each layout; 12-cell expected matrix)
    - .planning/phases/11-schema-rls-ef-foundations/11-RESEARCH.md § "Pitfall 1: P0 — RLS leakage" (rationale for each cell's expected outcome)
    - src/__tests__/admin/polls-effective-invariant.test.ts (full file — Vitest assertion + failure-message style to mirror)
    - supabase/migrations/00000000000010_results_hidden_audit.sql (Plan 01 — confirms policy name and USING clause shape for the assertion narrative)
  </read_first>
  <action>
    Open `e2e/integration/vote-counts-rls.test.ts` (the Plan 00 scaffold) and replace the `it.todo()` calls inside `describe.each` with real `it()` bodies.

    Top-of-file imports: `import { describe, it, expect, beforeAll, afterEach } from 'vitest'`; `import { mintClients, createFreshPoll, castVote, cleanupPoll } from './helpers'`.

    Outside the `describe.each`, mint the three clients once via `beforeAll` (lazy singletons inside `mintClients` per Plan 00 — calling repeatedly is cheap, but `beforeAll` is clearer): `const { anon, authed, serviceRole } = await mintClients({ authAs: 'memberUser' })`. Capture into module-scope `let` variables so the test bodies can pick the client by role.

    Cell body pattern: each `describe.each` iteration receives `(role, hidden, voted, expected)`. The body:
      1. Setup: `const poll = await createFreshPoll({ hidden, suiteSlug: \`rls-${role}\` })`. Store the id in a `let` declared inside the inner `describe` so `afterEach` can clean up.
      2. Optional vote: `if (voted) { await castVote({ pollId: poll.id, client: serviceRole }) }`. The serviceRole client is used to insert the vote regardless of the test's caller role — what matters for the assertion is that a vote ROW exists for the `memberUser`. The vote is inserted with `user_id: fixtureUsers.memberUser.id` (this is how the `authed × voted` cell becomes "voted" from the authed caller's perspective — the `votes.user_id = auth.uid()` clause in the policy will match when `memberUser` makes the SELECT).
      3. Pick the client: `const client = role === 'anon' ? anon : role === 'authed' ? authed : serviceRole`.
      4. Query: `const { data, error } = await client.from('vote_counts').select('*').eq('poll_id', poll.id)`. On `error`, throw — RLS returns 0 rows, never an error.
      5. Assert: if `expected === 0`, `expect(data ?? []).toHaveLength(0)`. If `expected === '>0'`, `expect((data ?? []).length).toBeGreaterThan(0)`.
      6. Failure message: when an assertion fails, the default Vitest message names the iteration (`%s × hidden=%s × voted=%s`); add no extra messaging unless the assertion is too generic. The `describe.each` template captures the role × hidden × voted combination already, which makes CI failures self-describing.

    `afterEach` (inside the outer describe): `if (currentPollId) { await cleanupPoll({ serviceRole, pollId: currentPollId }) }`. Use a module-scope `let currentPollId: string | null = null` set in the body and reset in afterEach.

    Do NOT add `it.skip` or `it.todo` to any cell — every cell must run (D-23 — merge blocker). Do NOT add `try/catch` around the assertions (let Vitest surface failures).

    For the special case `anon × voted=true`: the test still casts a vote (via serviceRole), BUT the assertion is `expected === 0` because anon has no granting policy on `vote_counts`. This mirrors Pitfall 1's guarantee.

    Acceptable performance: 12 cells × ~2 sec each = ~24 sec. The Plan 00 vitest config sets `testTimeout: 30_000` so single-cell DB round-trips are accommodated. Parallel cells via `describe.each` are vitest-default; if test isolation requires serial execution (e.g., to avoid poll-creation race conditions), set `{ concurrent: false }` on the inner describe.
  </action>
  <acceptance_criteria>
    - `e2e/integration/vote-counts-rls.test.ts` contains zero `it.todo(` calls: `grep -c "it\\.todo" e2e/integration/vote-counts-rls.test.ts` returns 0
    - `e2e/integration/vote-counts-rls.test.ts` contains zero `it.skip(` calls
    - File contains a `describe.each` block over a 12-row case array (the Plan 00 scaffold preserved): `grep -c "describe.each" e2e/integration/vote-counts-rls.test.ts` returns ≥1
    - File imports helpers from `./helpers`: `grep -E "from '\\./helpers'" e2e/integration/vote-counts-rls.test.ts` returns ≥1 match
    - File queries `vote_counts` table: `grep -E "from\\('vote_counts'\\)" e2e/integration/vote-counts-rls.test.ts` returns ≥1
    - File contains `cleanupPoll` invocation inside `afterEach`: `grep -B 2 "cleanupPoll" e2e/integration/vote-counts-rls.test.ts | grep -E "afterEach"` returns ≥1 match
    - `npm run test:integration -- --run vote-counts-rls.test.ts` exits 0 with 12 tests passing (or 12 + the outer `describe` row). Output contains the literal `12 passed` or `12 tests`: `npm run test:integration -- --run vote-counts-rls.test.ts 2>&1 | grep -E "12.*pass"` returns ≥1 match
    - The "anon × voted=true × hidden=false" cell returns 0 rows (Pitfall 1 — anon has no granting policy regardless of voted state) — verifiable by deliberately breaking the assertion temporarily and re-running; in the green state, the test passes
    - File has zero review-round / phase-ID archaeology
  </acceptance_criteria>
  <verify>
    <automated>grep -c "it\\.todo" e2e/integration/vote-counts-rls.test.ts | xargs test 0 -eq && grep -c "describe.each" e2e/integration/vote-counts-rls.test.ts | xargs test 1 -le && grep -qE "from\\('vote_counts'\\)" e2e/integration/vote-counts-rls.test.ts && npm run test:integration -- --run vote-counts-rls.test.ts 2>&1 | grep -qE "12.*pass|✓.*vote_counts"</automated>
  </verify>
  <done>
    12-cell matrix passes in full. Every cell where `hidden=true` OR caller has not voted returns 0 rows from `vote_counts`. Only `authenticated × hidden=false × voted=true` returns >0. Service-role returns >0 in all 4 of its cells. Cleanup runs after every cell.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-02: TEST-12 admin EF authz + audit row body</name>
  <files>e2e/integration/toggle-results-visibility.test.ts</files>
  <behavior>
    - Case 1: memberUser JWT → status === 403
    - Case 2: adminUser JWT + `{ poll_id, hidden: true }` against `results_hidden=false` poll → status === 200, response.poll.results_hidden === true, response.poll.results_hidden_changed_at is parseable ISO timestamp (non-null)
    - Case 3: After Case 2 succeeds, `audit_log` has exactly one row with `action='results_hidden_toggled'`, `actor_id=adminUser.id`, `target_id=poll.id`, `before={results_hidden:false}`, `after={results_hidden:true}`
    - Case 4: After Case 2/3, invoking again with `{ poll_id, hidden: true }` (no-op) → status === 200, but the audit_log count for `action='results_hidden_toggled'` AND `target_id=poll.id` is still 1 (idempotency, D-11)
  </behavior>
  <read_first>
    - e2e/integration/helpers.ts (Plan 00 — `mintClients`, `createFreshPoll`, `invokeEF`, `readAuditLog`, `cleanupPoll`)
    - e2e/integration/toggle-results-visibility.test.ts (Plan 00 scaffold — 4 `it.todo` placeholders)
    - supabase/functions/toggle-results-visibility/index.ts (Plan 02 — confirms request body shape `{ poll_id, hidden }` and response shape `{ poll: <updated row> }`)
    - supabase/functions/_shared/admin-auth.ts (existing — confirms 403 status comes from `adminCheckResponse` on `requireAdmin` failure)
    - e2e/fixtures/test-users.ts (`fixtureUsers.memberUser`, `fixtureUsers.adminUser`)
    - .planning/phases/11-schema-rls-ef-foundations/11-PATTERNS.md § "e2e/integration/toggle-results-visibility.test.ts" (audit_log read pattern)
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md § D-24 (TEST-12 cells), D-11 (no-op no-audit)
  </read_first>
  <action>
    Open `e2e/integration/toggle-results-visibility.test.ts` and replace the 4 `it.todo` placeholders with real `it` bodies.

    Top-of-file imports: `import { describe, it, expect, beforeAll, afterAll } from 'vitest'`; `import { mintClients, createFreshPoll, invokeEF, readAuditLog, cleanupPoll } from './helpers'`; `import { fixtureUsers } from '../fixtures/test-users'`.

    Module-scope state: `let memberClients`, `let adminClients`, `let pollId`. In `beforeAll`: mint two sets of clients via `mintClients({ authAs: 'memberUser' })` and `mintClients({ authAs: 'adminUser' })`. Create one fresh poll with `results_hidden: false`: `const poll = await createFreshPoll({ hidden: false, suiteSlug: 'test-12' }); pollId = poll.id;`.

    Case 1 (`'non-admin caller returns 403'`): `const result = await invokeEF({ client: memberClients.authed, name: 'toggle-results-visibility', body: { poll_id: pollId, hidden: true } }); expect(result.status).toBe(403);`. The memberUser JWT will trip the `requireAdmin` gate in `_shared/admin-auth.ts`. The mutation MUST NOT have happened — assert by checking `const { data } = await adminClients.serviceRole.from('polls').select('results_hidden').eq('id', pollId).single(); expect(data.results_hidden).toBe(false);` to confirm the 403 was a hard stop.

    Case 2 (`'admin caller returns 200 with updated poll row + non-null results_hidden_changed_at'`): `const result = await invokeEF({ client: adminClients.authed, name: 'toggle-results-visibility', body: { poll_id: pollId, hidden: true } }); expect(result.status).toBe(200);`. Then `expect(result.data.poll.id).toBe(pollId); expect(result.data.poll.results_hidden).toBe(true); expect(result.data.poll.results_hidden_changed_at).not.toBeNull(); expect(new Date(result.data.poll.results_hidden_changed_at).toISOString()).toBe(result.data.poll.results_hidden_changed_at);` (the round-trip-through-Date assertion proves the timestamp is a parseable ISO string).

    Case 3 (`'audit_log row written on state change'`): `const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: pollId, action: 'results_hidden_toggled' }); expect(rows).toHaveLength(1); expect(rows[0].actor_id).toBe(fixtureUsers.adminUser.id); expect(rows[0].action).toBe('results_hidden_toggled'); expect(rows[0].target_type).toBe('poll'); expect(rows[0].before).toEqual({ results_hidden: false }); expect(rows[0].after).toEqual({ results_hidden: true });`. NB: this case relies on Case 2 having run first — set `describe`-level test ordering via Vitest's default top-to-bottom execution (do not use `describe.concurrent`).

    Case 4 (`'no audit_log row on no-op call (idempotency, D-11)'`): invoke again with the same `{ poll_id, hidden: true }` body — the poll is already hidden=true after Case 2. `const result = await invokeEF({ ... hidden: true ... }); expect(result.status).toBe(200);` (idempotent — no 409). Then `const rows = await readAuditLog({ serviceRole: adminClients.serviceRole, targetId: pollId, action: 'results_hidden_toggled' }); expect(rows).toHaveLength(1);` — STILL only 1 row from Case 2; Case 4's no-op did NOT add a new one. Additionally assert that `results_hidden_changed_at` DID update (per D-13 — timestamp is not gated on state change): `const { data: poll } = await adminClients.serviceRole.from('polls').select('results_hidden_changed_at').eq('id', pollId).single(); expect(poll.results_hidden_changed_at).not.toBeNull();`. (We do not assert the timestamp changed between Case 2 and Case 4 because the test cases run too fast for the millisecond resolution to guarantee strict inequality; the non-null assertion is sufficient and matches D-13.)

    `afterAll`: `if (pollId) { await cleanupPoll({ serviceRole: adminClients.serviceRole, pollId }) }`. Cascade cleans the audit_log rows via the audit_log → polls FK on `target_id`... wait, there is NO FK from audit_log.target_id to polls.id in the migration 10 schema (target_id is just `uuid`, no REFERENCES). So `audit_log` rows do NOT cascade. The cleanup must explicitly delete the audit rows: `await adminClients.serviceRole.from('audit_log').delete().eq('target_id', pollId);` BEFORE the `cleanupPoll(poll)` call. Document the explicit-cleanup pattern in a code comment WHY.

    Do NOT mock the EF — invoke against the live local Supabase functions runtime. The Plan 00 `invokeEF` helper hits `${SUPABASE_URL}/functions/v1/toggle-results-visibility` with a real `Authorization: Bearer <session.access_token>`.
  </action>
  <acceptance_criteria>
    - `e2e/integration/toggle-results-visibility.test.ts` contains zero `it.todo(` calls
    - File has exactly 4 `it(` blocks (the 4 cases): `grep -cE "^\\s*it\\(" e2e/integration/toggle-results-visibility.test.ts` returns 4
    - File imports from `./helpers`: `grep -E "from '\\./helpers'" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - File imports `fixtureUsers` from `../fixtures/test-users`: `grep -E "fixtureUsers.*test-users" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - Case 1 asserts `expect(result.status).toBe(403)`: `grep -E "status.*toBe\\(403\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - Case 2 asserts `expect(result.status).toBe(200)` AND `results_hidden_changed_at` non-null check: `grep -E "results_hidden_changed_at" e2e/integration/toggle-results-visibility.test.ts` returns ≥2 (Case 2 + Case 4)
    - Case 3 reads `audit_log` via `readAuditLog`: `grep -c "readAuditLog" e2e/integration/toggle-results-visibility.test.ts` returns ≥2 (Cases 3 and 4)
    - Case 3 asserts the `before`/`after` JSONB shape: `grep -E "toEqual\\(\\{\\s*results_hidden:\\s*false\\s*\\}\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1 AND `grep -E "toEqual\\(\\{\\s*results_hidden:\\s*true\\s*\\}\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - Case 4 asserts `expect(rows).toHaveLength(1)` AFTER the no-op call (proving no new row): the test has at least two `toHaveLength(1)` assertions on readAuditLog results
    - `afterAll` cleans up both `audit_log` rows AND the poll: `grep -E "from\\('audit_log'\\)\\.delete\\(\\)" e2e/integration/toggle-results-visibility.test.ts` returns ≥1 match AND `grep -c "cleanupPoll" e2e/integration/toggle-results-visibility.test.ts` returns ≥1
    - `npm run test:integration -- --run toggle-results-visibility.test.ts` exits 0 with 4 tests passing: output contains `4 passed` or `4 tests`
    - File has zero review-round / phase-ID archaeology
  </acceptance_criteria>
  <verify>
    <automated>grep -c "it\\.todo" e2e/integration/toggle-results-visibility.test.ts | xargs test 0 -eq && grep -cE "^\\s*it\\(" e2e/integration/toggle-results-visibility.test.ts | xargs test 4 -eq && grep -qE "status.*toBe\\(403\\)" e2e/integration/toggle-results-visibility.test.ts && grep -qE "status.*toBe\\(200\\)" e2e/integration/toggle-results-visibility.test.ts && grep -qE "from\\('audit_log'\\)\\.delete" e2e/integration/toggle-results-visibility.test.ts && npm run test:integration -- --run toggle-results-visibility.test.ts 2>&1 | grep -qE "4.*pass|✓.*toggle-results"</automated>
  </verify>
  <done>
    All 4 TEST-12 cases pass: 403 for member, 200 + updated poll for admin, audit row written on state change, audit row NOT duplicated on no-op. Cleanup deletes both audit rows and the poll.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test caller role → DB | Tests intentionally probe the trust boundary that production code defends; assertions must be deterministic regardless of test ordering within a cell |
| Test setup → cleanup | Setup-time poll creation must not leak rows across tests; cleanup must rethrow on failure to surface DB issues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-RLS-LEAK | Information Disclosure | TEST-11 12-cell matrix is the primary defense | mitigate (P0 merge blocker) | Every cell asserted (no `.skip`, no `.todo`); 12 cells cover all (role × hidden × voted) combinations including `anon × voted=true × hidden=false` which would otherwise be an easy-to-miss leak vector. Failure narrative names the failing combination via `describe.each` template. |
| T-EF-ADMIN-BYPASS | Elevation of Privilege | TEST-12 case 1 + post-403 state assertion | mitigate | Case 1 asserts 403 AND that `polls.results_hidden` did NOT flip (the EF must hard-stop at the admin gate, not partially execute). Without the post-state check, a passing 403 could mask a partial-execute bug. |
| T-AUDIT-DRIFT | Repudiation | TEST-12 case 3 audit shape assertion | mitigate | Asserts the full audit row shape: `actor_id`, `action`, `target_type`, `target_id`, `before`, `after`. Drift in any of these (e.g., a future EF change that renames the action string) breaks the test. |
| T-IDEMPOTENCY | Repudiation | TEST-12 case 4 no-op no-audit | mitigate | Case 4 asserts `toHaveLength(1)` AFTER the no-op (still only Case 2's row). The matching positive case (Case 3) and the no-op case (Case 4) together validate D-11. |
| T-TEST-LEAK | Information Disclosure (test data) | Poll + audit_log cleanup | mitigate | `afterAll` explicitly DELETEs `audit_log.target_id = pollId` rows BEFORE calling `cleanupPoll` (audit_log has no FK to polls per migration 10 schema — cascades do not apply). Cleanup failure rethrows via Plan 00 helper. |
| T-FLAKY-TIMESTAMP | Repudiation | Timestamp assertion strictness | accept | Test asserts `results_hidden_changed_at` is non-null and ISO-parseable, but does NOT assert strict inequality between Case 2 and Case 4 timestamps (millisecond resolution + serial cases could share a timestamp). Non-null is sufficient to verify D-13 (EF writes the timestamp on every UPDATE). |
</threat_model>

<verification>
- `npm run test:integration` exits 0 (full suite — both files combined)
- TEST-11 reports 12 passing cases; TEST-12 reports 4 passing cases
- After running, `audit_log` and `polls` have no `[TEST-11]` or `[TEST-12]`-prefixed rows leaked (verifiable via `psql` against local Supabase: `SELECT * FROM polls WHERE title LIKE '[TEST-1%]'` returns 0 rows; `SELECT * FROM audit_log WHERE action='results_hidden_toggled' AND created_at > now() - interval '1 hour'` returns rows ONLY for the most recent run's cleanup window)
- No `it.skip`, no `it.todo`, no `it.only` in either file (acceptance criterion enforced)
</verification>

<success_criteria>
- ROADMAP SC-2 satisfied (12-cell matrix passes; no cell skipped)
- ROADMAP SC-3 satisfied (admin EF authz: 403/200; updated poll row; audit row written)
- VIS-05 + TEST-11 + TEST-12 fully covered
- Phase 11 is execution-complete pending Plan 05's deploy gate
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-rls-ef-foundations/11-04-SUMMARY.md` per `@$HOME/.claude/get-shit-done/templates/summary.md`. Declare `requirements-completed: [VIS-05, TEST-11, TEST-12]`. Include the test-run output evidence (e.g., the final Vitest summary line: "12 passed in vote-counts-rls.test.ts / 4 passed in toggle-results-visibility.test.ts").
</output>
