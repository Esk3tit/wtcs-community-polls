---
phase: 20-live-human-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md
  - .planning/phases/20-live-human-uat/20-01-SUMMARY.md
autonomous: true
requirements:
  - UAT-01
must_haves:
  truths:
    - "D-01: 2026-05-03 second-human PASS accepted as satisfying UAT-01 — 03-UAT.md Summary rollup shows skipped: 0 and passed: 6 (Tests 2+3 accounted for)"
    - "D-03 acceptance basis is formally recorded in 03-UAT.md — non-member rejection path confirmed unchanged since 2026-05-03"
    - "D-04: Historical result: skipped rows for Tests 2 and 3 are verbatim-preserved (not rewritten to pass)"
    - "Each preserved skipped row has a one-line forward pointer to the Second-Human Verification pass"
    - "D-05: Acceptance basis section documents why a literal screenshot of the original historical run is N/A — narrative+verdict stands as the lightest artifact satisfying the criterion"
    - "D-06: No live test accounts required — verdict is no-fresh-run; any confirming screenshot from operator's own admin session only"
    - "Server-side vs client-side gate terminology reconciled: acceptance basis states the exercised path is the client-side OAuth guild check (rejects before any RPC), the same membership-enforcement outcome the ROADMAP/REQUIREMENTS 'server-side membership gate' wording refers to"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md"
      provides: "UAT-01 reconciled with debt-zero rollup and acceptance basis"
      contains: "skipped: 0"
  key_links:
    - from: "03-UAT.md § Tests (Test 2/3 skipped rows)"
      to: "03-UAT.md § Second-Human Verification"
      via: "one-line resolution pointer"
      pattern: "Phase 20"
    - from: "03-UAT.md § Acceptance Basis"
      to: "src/lib/auth-helpers.ts:191-202"
      via: "explicit line-range citation"
      pattern: "auth-helpers.ts"
---

<objective>
Reconcile 03-UAT.md (Phase 03 Response Integrity) so UAT-01 is debt-zero.

Purpose: The Second-Human Verification section already records Tests 2+3 as PASS (2026-05-03), but the file's Summary rollup still shows `skipped: 2` and neither skipped row has a forward pointer. The acceptance basis for why no fresh run is needed (D-03 gate-path-unchanged diff) has never been formally recorded. This plan closes all three gaps.

Output: 03-UAT.md with (1) a new § UAT-01 Acceptance Basis section recording the D-03 gate-path diff, (2) resolution pointers added to Tests 2 and 3 historical rows, (3) Summary rollup corrected to `skipped: 0` / `passed: 6`.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/khaiphan/code/wtcs-community-polls/.planning/PROJECT.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/STATE.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/phases/20-live-human-uat/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm UAT-01 acceptance basis line numbers</name>
  <read_first>
    src/lib/auth-helpers.ts (focus on lines 185-215 — non-member rejection block and the update_profile_after_auth RPC call; extract exact current line numbers for the `not-in-server` return and the RPC call)
    src/components/auth/AuthErrorPage.tsx (verify still present and unchanged — note any last-modified signals)
    src/routes/auth/error.tsx (verify `reason` param parsing is still present)
    supabase/migrations/00000000000014_harden_security_definer_search_path.sql (scan function names touched — note it hardens MULTIPLE SECURITY DEFINER functions, e.g. profile_self_update_allowed, handle_new_user, is_current_user_admin, increment_vote_count, validate_vote_choice, update_profile_after_auth. These are search_path/security hardening changes that are behavior-identical to the prior definitions. IMPORTANT: do NOT characterize all of these as "reached only after a successful member check" — handle_new_user fires AFTER INSERT ON auth.users via the on_auth_user_created trigger during OAuth signup, BEFORE the client-side non-member guild check, so it is NOT gated behind member success. The load-bearing fact is narrower: the post-success profile-write RPC update_profile_after_auth is the only one of these on the non-member rejection path, and a non-member never reaches it.)
    supabase/migrations/00000000000015_trusted_profile_update_guc.sql (scan function body — note it changes the trusted-context / session-GUC path for the post-success profile update RPC update_profile_after_auth; non-members never reach this RPC because the client-side guild check rejects and signs out first)
  </read_first>
  <action>
    Read the five acceptance-basis source files above (do NOT re-read any file already in context). From auth-helpers.ts, extract:
    - The exact current line range for the block that returns `{ success: false, reason: 'not-in-server' }` and calls `supabase.auth.signOut()`.
    - The exact current line number of the `update_profile_after_auth` RPC call (must be AFTER the non-member return — confirms non-members never reach it).

    From migrations 14 and 15, confirm the NARROW true claim only — do NOT overstate. Migration 14 hardens several SECURITY DEFINER functions — profile_self_update_allowed, handle_new_user, is_current_user_admin, increment_vote_count, validate_vote_choice, update_profile_after_auth — via search_path lockdown; these are behavior-identical security hardening, not the membership-rejection decision point. Migration 15 changes the trusted-context session GUC for the post-success profile-update RPC update_profile_after_auth. The load-bearing claim is NARROW and is specifically about the post-success profile-write path: a non-member never reaches the update_profile_after_auth RPC, because the client-side guild check rejects and signs out before that RPC runs.

    Do NOT assert that ALL migration-14 functions are "reached only after a successful member check" — that is FALSE. handle_new_user fires AFTER INSERT ON auth.users (via the on_auth_user_created trigger) during OAuth signup, BEFORE the client-side non-member guild check, so it is NOT gated behind member success. (review HIGH #1 — verified against live migration: handle_new_user is commented "fires from on_auth_user_created trigger on auth.users".) Equally, do NOT claim the migrations "only touch update_profile_after_auth" — also too broad. The correct, defensible statement is: the migrations are search_path/security hardening across several functions; of those, only update_profile_after_auth sits on the post-success profile-write path, and the non-member rejection branch returns and signs out before that RPC is reached.

    Record the exact current line numbers — they will be cited verbatim in the acceptance basis section written in Task 2. If the line numbers differ from CONTEXT.md's "191-202" estimate, use the actual numbers found.

    Do NOT modify any file in this task.
  </action>
  <verify>
    <automated>grep -n "not-in-server" /Users/khaiphan/code/wtcs-community-polls/src/lib/auth-helpers.ts</automated>
  </verify>
  <done>Exact current line numbers for the non-member rejection block and RPC call recorded; confirmed the non-member rejection branch returns and signs out before the update_profile_after_auth RPC (the post-success profile-write path migrations 14/15 touch), so the non-member never reaches it. Did NOT assert all migration-14 functions (e.g. handle_new_user, which fires on the auth.users insert trigger before the guild check) are post-member-check.</done>
</task>

<task type="auto">
  <name>Task 2: Update 03-UAT.md — acceptance basis, resolution pointers, rollup</name>
  <read_first>
    .planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md (read the FULL file — current state, all sections, exact line numbers for the skipped rows and Summary block)
  </read_first>
  <action>
    Edit `.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md` with three additive changes. Apply them in this order:

    **Change 1 — Resolution pointers on preserved historical rows.**
    In the Tests section, locate the two historical `result: skipped` rows (Test 2 and Test 3). Each row is a YAML-style block under its `### N. Title` heading. After each `reason:` line (but before the next `###` heading), append a single line:
    ```
    resolution: "Superseded by Second-Human Verification PASS (2026-05-03); Phase 20 closure. See § Second-Human Verification below."
    ```
    Do NOT change `result: skipped` — preserve verbatim per D-04.

    **Change 2 — Update the Summary rollup.**
    Locate the `## Summary` YAML block. Change:
    - `passed: 4` → `passed: 6`
    - `skipped: 2` → `skipped: 0`
    Leave `total: 6`, `issues: 0`, `pending: 0` unchanged.

    **Change 3 — Add § UAT-01 Acceptance Basis section.**
    Append a new top-level section at the END of the file (after the last line of § Second-Human Verification / Test 3 notes), titled:

    ```
    ## UAT-01 Acceptance Basis
    ```

    The section must record the D-03 gate-path-unchanged rationale in structured form:
    - Heading and purpose: states this section records WHY the 2026-05-03 second-human PASS satisfies UAT-01 without a fresh run per Phase 20 decision D-03.
    - Cite `src/lib/auth-helpers.ts` with the exact current line range from Task 1 where the non-member rejection fires and signs out BEFORE the `update_profile_after_auth` RPC (cite both line numbers).
    - Cite `src/components/auth/AuthErrorPage.tsx` and `src/routes/auth/error.tsx` as unchanged since April 2026.
    - State the NARROW, factually-correct migration basis (review HIGH #1 — Cycle 4): Migrations 14 and 15 landed after 2026-05-03. Migration 14 (harden_security_definer_search_path) is search_path/security hardening across several SECURITY DEFINER functions — profile_self_update_allowed, handle_new_user, is_current_user_admin, increment_vote_count, validate_vote_choice, and update_profile_after_auth — and is behavior-identical to the prior definitions (it does not change the membership-rejection decision logic). Migration 15 (trusted_profile_update_guc) changes the trusted-context session GUC for the post-success profile-write RPC update_profile_after_auth. The load-bearing, NARROW claim to write is: a non-member never reaches the update_profile_after_auth RPC (the post-success profile-write path), because the client-side guild check in auth-helpers.ts rejects and signs out before that RPC runs. DO NOT write that all migration-14 functions are "reached only after a successful member check" — that is FALSE for handle_new_user, which fires AFTER INSERT ON auth.users (via the on_auth_user_created trigger) during OAuth signup, BEFORE the client-side non-member guild check. Writing that blanket claim into this audit artifact would encode a false premise (flagged HIGH in Cycle 4 cross-AI review). Also do NOT write that the migrations "only alter update_profile_after_auth internals" — also too broad. State only what is true: handle_new_user and the other hardened functions are behavior-identical search_path/security hardening; of the hardened set, only update_profile_after_auth sits on the post-success profile-write path; and the non-member never reaches that RPC.
    - State the git-log finding: last gate-relevant change to the gate files was 2026-05-01 (comment-only archaeology strip).
    - State the conclusion: the non-member rejection path is fully client-side and unchanged since the second-human run; the 2026-05-03 PASS evidence remains valid.
    - Record Phase 20 as the formal closure date (2026-06-04).
    - **Server-side vs client-side terminology reconciliation (review MEDIUM):** Add an explicit clarifying sentence: ROADMAP/REQUIREMENTS describe a "server-side membership gate", but the path Tests 2+3 exercise is the **client-side OAuth guild check** in `auth-helpers.ts` that rejects and signs out before any RPC. State that both descriptions refer to the same membership-enforcement outcome (a non-member is denied access); the client-side guild check is the layer the live non-member run actually exercised, and the server-side `admin_discord_ids` / RLS layer is a defense-in-depth backstop that the non-member never reaches. The evidence remains valid under either label — the sentence exists only to remove the terminology drift the reviewers flagged.
    - **D-05 narrative-artifact paragraph (review MEDIUM):** Add an explicit paragraph documenting WHY a literal screenshot of the original 2026-05-03 run is not available/not reproducible: it was a one-time historical live event run by `test-dev-account` (a 2FA-on non-WTCS-member burner) that cannot be re-screenshotted after the fact. Per D-05, the lightest sufficient artifact is the existing narrative evidence + verdict already recorded in § Second-Human Verification, optionally supplemented by a current confirming screenshot of the reproducible `/auth/error?reason=not-in-server` page (which renders directly from its route per D-06, no special account needed). State that narrative+verdict is accepted as satisfying the success criterion's "screenshot or screen-recording reference" for this non-reproducible historical event.

    Use plain prose within the section — no fenced code blocks. Named line numbers/references are prose (e.g., "lines 191-202" or whatever the actual current numbers are from Task 1).
  </action>
  <verify>
    <automated>grep -c "skipped: 0" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md</automated>
    <automated>grep -c "passed: 6" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md</automated>
    <automated>grep -c "UAT-01 Acceptance Basis" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md</automated>
    <automated>grep -c "^result: skipped$" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md</automated>
    <automated>grep -c "^resolution:" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skipped: 0"` returns 1 (Summary rollup updated)
    - `grep -c "passed: 6"` returns 1 (Summary rollup updated)
    - `grep -c "UAT-01 Acceptance Basis"` returns 1 (new section present)
    - `grep -c "^result: skipped$"` returns 2 — ANCHORED count of the two preserved historical data rows (Test 2, Test 3). NOTE: the unanchored `grep -c "result: skipped"` returns 3 today because the § Second-Human Verification blockquote prose at line ~55 also contains the literal string `result: skipped`; that prose match is expected and is NOT a data row. Always anchor with `^...$` (or exclude blockquotes via `| grep -v '^>'`) when counting data rows. (review HIGH #1 — verified: unanchored=3, anchored=2.)
    - `grep -c "^resolution:" ` returns 2 — both preserved skipped rows carry a top-level `resolution:` forward pointer to § Second-Human Verification + Phase 20 closure
    - File does NOT contain "skipped: 2" anywhere (old rollup value gone)
    - File does NOT contain "passed: 4" anywhere (old rollup value gone)
    - The § Second-Human Verification section is unchanged (Test 2 and Test 3 PASS records intact)
    - src/lib/auth-helpers.ts is NOT modified
  </acceptance_criteria>
  <done>03-UAT.md Summary shows skipped: 0 / passed: 6; historical skipped rows preserved with Phase 20 resolution pointers; D-03 acceptance basis section present at end of file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only planning markdown files under .planning/ — no executable code, no auth path, no data handling, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | (null surface) | .planning/milestones markdown | accept | Phase changes no executable code, no migrations, no auth logic, no user data. No new attack surface introduced. Markdown-only changes under version control. |
</threat_model>

<verification>
After both tasks complete, run:

```bash
grep "skipped:" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md
grep "passed:" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md
grep -c "UAT-01 Acceptance Basis" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md
grep -c "^result: skipped$" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md
```

Expected: skipped: 0, passed: 6, acceptance basis section present (count = 1), two preserved `result: skipped` data rows via the ANCHORED count (count = 2 — the unanchored count is 3 because the § Second-Human Verification blockquote prose also contains the string; anchor or exclude `^>` blockquotes).
</verification>

<success_criteria>
- 03-UAT.md Summary rollup: `passed: 6`, `skipped: 0`, `total: 6`, `issues: 0`, `pending: 0`
- Both historical `result: skipped` rows (Test 2, Test 3) intact verbatim with `resolution:` forward-pointer lines appended
- § UAT-01 Acceptance Basis section present at end of file, citing auth-helpers.ts exact line range, the NARROW migration basis (non-member never reaches the update_profile_after_auth RPC; migration-14 functions incl. handle_new_user are behavior-identical search_path/security hardening, NOT a blanket "reached only after a successful member check" claim — review HIGH #1 Cycle 4), git-log anchor, Phase 20 closure date, the server-side/client-side terminology reconciliation sentence, and the D-05 narrative-artifact paragraph
- Anchored data-row count `grep -c "^result: skipped$"` = 2; both rows carry a `resolution:` forward pointer (the unanchored count is 3 due to blockquote prose — not a regression)
- No file outside `03-UAT.md` and `20-01-SUMMARY.md` is modified
</success_criteria>

<output>
Create `.planning/phases/20-live-human-uat/20-01-SUMMARY.md` when done.
</output>
