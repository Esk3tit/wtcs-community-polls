---
phase: 20-live-human-uat
plan: 03
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/20-live-human-uat/20-03-SUMMARY.md
autonomous: true
requirements:
  - UAT-01
  - UAT-02
must_haves:
  truths:
    - "REQUIREMENTS.md status table flips UAT-01 and UAT-02 from Pending → Validated once 20-01/20-02 have recorded the reconciled evidence (CONTEXT.md: UAT-01/02 → Validated at phase close)"
    - "REQUIREMENTS.md UAT-01/UAT-02 checkboxes are checked ([x]) AND the three contradictory GLOBAL 'executed live by the operator this milestone' lines (operator-closure-decisions bullet ~14, Live Human UAT section intro ~30, Out-of-Scope row ~85) are REWRITTEN (not merely appended-to) to the operator-locked accept-pre-existing-live-evidence pivot per D-01/D-02 — UAT-01 ran live 2026-05-03, UAT-02 ran live during the v1.0→v1.1 transition, no fresh run this milestone — so no internal contradiction remains for a v1.4 debt-zero audit"
    - "REQUIREMENTS.md UAT-01 'server-side membership gate' wording is reconciled with the verified client-side OAuth guild check (the same membership-enforcement outcome) so it no longer contradicts the 03-UAT.md acceptance basis"
    - "ROADMAP.md Phase 20 reflects 3 plans (not 2): the '**Plans**: 2 plans' line and the '| 20. Live Human UAT | 0/2 |' progress row are updated to '3 plans' / '0/3', and a Wave 2 grouping with a 20-03-PLAN.md checkbox is added (review MEDIUM #2, Cycle 3)"
    - "ROADMAP.md Phase 20 goal/top-level wording reconciled to evidence-reconciliation closure (accepted pre-existing live evidence per D-01/D-02; no fresh run this milestone)"
    - "Plan-checkbox ownership is explicit: 20-03 leaves the per-plan checkboxes UNCHECKED — execute-phase owns flipping them on execution (review MEDIUM #3, Cycle 3 checkbox-ownership ambiguity resolved)"
    - "STATE.md UAT-01/02 carry-forward is left to the phase-close/verify-phase transition (step 13b owns STATE planning status) — 20-03 does NOT hand-edit STATE status fields to avoid double-ownership; it only notes the dependency, and a verify-phase audit note confirms STATE.md no longer carries UAT-01/02 as open at close"
    - "Milestone-level closure is artifact-complete: no v1.4 debt-zero audit of REQUIREMENTS.md/ROADMAP.md finds UAT-01/02 still 'Pending'"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "UAT-01/UAT-02 marked Validated with reconciled wording"
      contains: "UAT-01 | Phase 20 | Validated"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 20 closure annotations consistent with reconciled UAT evidence"
      contains: "Phase 20"
  key_links:
    - from: ".planning/REQUIREMENTS.md UAT-01/02 status table"
      to: "03-UAT.md / 04-UAT.md reconciled evidence"
      via: "Validated status backed by Phase 20 closure sections"
      pattern: "Validated"
---

<objective>
Close the milestone-level loop for Phase 20 so UAT-01/UAT-02 are Validated across the project's
tracking artifacts — not just inside the two archived UAT files.

Purpose (review MEDIUM #3): 20-01 and 20-02 reconcile the evidence inside `03-UAT.md` / `04-UAT.md`,
but three reviewers noted Phase 20 is artifact-incomplete because `.planning/REQUIREMENTS.md` still
lists UAT-01/02 as "Pending" (and still says "executed live this milestone"), and ROADMAP.md Phase 20
wording is not reconciled. CONTEXT.md explicitly states UAT-01/02 flip to Validated at phase close.
This plan makes that flip an owned, verifiable step instead of an implicit assumption.

Cycle-3 HIGH (the one remaining cross-AI concern): the earlier draft of this plan only APPENDED a closure
clause to the UAT-01/02 bullets, leaving three GLOBAL REQUIREMENTS.md lines (~14, ~30, ~85) still asserting
the runs were "executed live by the operator THIS milestone." That produced an internal contradiction — the
file would simultaneously say "executed live this milestone" and "closed by accepting pre-existing evidence
(no fresh run)." Task 1 Change 3 now REWRITES those three global lines to the operator-locked
accept-pre-existing-live-evidence pivot (D-01/D-02), preserving the requirement IDs and the operator's
"live with real accounts; not E2E-mocked" intent while removing the false this-milestone timing.

Scope guard: This plan touches ONLY `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`. It does
NOT edit STATE.md status fields — the plan-phase/verify-phase workflow (step 13b `state.planned-phase`
and the verify-phase close) owns STATE planning-status transitions; hand-editing here would create a
double-ownership conflict. STATE's UAT carry-forward resolves at phase close.

Output: REQUIREMENTS.md with UAT-01/02 checkboxes checked, status-table rows = Validated, and the
"this milestone"/"server-side membership gate" wording reconciled; ROADMAP.md Phase 20 entry + plan
checkboxes annotated as reconciled/closed.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/khaiphan/code/wtcs-community-polls/.planning/PROJECT.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/STATE.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/phases/20-live-human-uat/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip UAT-01/UAT-02 to Validated in REQUIREMENTS.md and reconcile wording</name>
  <read_first>
    .planning/REQUIREMENTS.md (read the FULL file. Verify the exact current text of these contradictory GLOBAL lines before editing — line numbers may have shifted: (a) the operator-closure-decisions bullet "**Human UAT items = executed live by the operator** this milestone. Plans must produce exact step-by-step checklists and concrete evidence-recording targets." at line ~14; (b) the "### Live Human UAT (UAT-*)" section intro "The two second-human-gated UAT items deferred since v1.0/v1.1. Executed live by the operator this milestone with the required test accounts." at line ~30; (c) the Out-of-Scope table row "| Automating the human UAT instead of live runs | Operator chose to run UAT-01/02 live; E2E mocking is not a substitute for the second-human evidence here |" at line ~85; (d) the UAT-01 bullet at line ~32, UAT-02 bullet at line ~34, and the requirement-status table rows at lines ~99-100. Note exact current strings before editing.)
    .planning/phases/20-live-human-uat/20-CONTEXT.md (re-read decisions D-01 and D-02 — the operator-locked pivot: UAT-01's live run happened 2026-05-03; UAT-02's MapCommittee 6a PASS happened during the v1.0→v1.1 transition; BOTH are accepted as satisfying the requirement with NO fresh run this milestone. Keep the rewrite faithful to this wording.)
    .planning/milestones/v1.0-phases/03-response-integrity/03-UAT.md (confirm § UAT-01 Acceptance Basis exists — this plan runs in Wave 2 after 20-01; the Validated flip depends on it)
    .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md (confirm § UAT-02 Phase 20 Closure exists — depends on 20-02)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` with these changes (additive/precise — do not reflow unrelated lines):

    **Change 1 — Status table → Validated.**
    In the requirement-status table, change:
    - `| UAT-01 | Phase 20 | Pending |` → `| UAT-01 | Phase 20 | Validated |`
    - `| UAT-02 | Phase 20 | Pending |` → `| UAT-02 | Phase 20 | Validated |`

    **Change 2 — Check the requirement checkboxes.**
    - UAT-01 bullet (line ~32): change the leading `- [ ]` → `- [x]`.
    - UAT-02 bullet (line ~34): change the leading `- [ ]` → `- [x]`.

    **Change 3 — REWRITE the contradictory GLOBAL "executed live this milestone" framing (HIGH — Cycle 3).**
    Cycle 3's one remaining HIGH: the prior plan only APPENDED a closure clause to the UAT-01/02 bullets
    while leaving three GLOBAL lines asserting the runs were "executed live by the operator THIS milestone."
    After execution REQUIREMENTS.md would simultaneously claim "executed live this milestone" AND "closed by
    accepting pre-existing evidence (no fresh run)" — a contradiction a v1.4 debt-zero audit would flag.
    Per CONTEXT.md D-01/D-02 the operator-locked verdict is accept-pre-existing-live-evidence: UAT-01's live
    run happened 2026-05-03; UAT-02's MapCommittee 6a PASS happened during the v1.0→v1.1 transition; NEITHER
    was re-run this milestone. REWRITE the three global lines so they state that pivot (preserve the
    requirement IDs and the operator's "executed live with real accounts / not E2E-mocked" intent — only
    remove the false "THIS milestone / will run live now" timing). Faithful target wording:

    - **Global line ~14** (operator-closure-decisions bullet). Replace:
      `- **Human UAT items = executed live by the operator** this milestone. Plans must produce exact step-by-step checklists and concrete evidence-recording targets.`
      with:
      `- **Human UAT items = satisfied by pre-existing live operator runs**, accepted this milestone per D-01/D-02 (UAT-01 executed live 2026-05-03; UAT-02 executed live during the v1.0→v1.1 transition). No fresh runs were performed this milestone — the operator-locked "executed live with real accounts; not E2E-mocked" intent is met by those existing runs. Plans reconcile that evidence and record concrete acceptance-basis targets.`

    - **Global line ~30** (the `### Live Human UAT (UAT-*)` section intro). Replace:
      `The two second-human-gated UAT items deferred since v1.0/v1.1. Executed live by the operator this milestone with the required test accounts.`
      with:
      `The two second-human-gated UAT items deferred since v1.0/v1.1. Both were executed live by the operator with the required real accounts BEFORE this milestone (UAT-01: 2026-05-03; UAT-02: v1.0→v1.1 transition); v1.4 accepts that pre-existing live evidence as satisfying the requirement (D-01/D-02) — no fresh run was performed this milestone.`

    - **Global line ~85** (Out-of-Scope table row). Replace the row:
      `| Automating the human UAT instead of live runs | Operator chose to run UAT-01/02 live; E2E mocking is not a substitute for the second-human evidence here |`
      with:
      `| Automating the human UAT / treating E2E mocking as the evidence | UAT-01/02 are closed on pre-existing LIVE operator runs (D-01/D-02); E2E mocking is not a substitute for that second-human evidence. No fresh live run was performed this milestone — none was required, since accepted live evidence already exists. |`

    **Change 3b — Add the per-bullet closure pointer** (additive; complements the global rewrite above).
    Append a short closure clause to each UAT bullet (do NOT delete the original requirement text):
    - UAT-01: append ` — Closed Phase 20 (2026-06-04) by accepting the pre-existing 2026-05-03 second-human PASS (no fresh run); gate path verified unchanged (see 03-UAT.md § UAT-01 Acceptance Basis, D-01/D-03).`
    - UAT-02: append ` — Closed Phase 20 (2026-06-04) by backfilling the pre-existing Off-Record Verification PASS (MapCommittee, v1.0→v1.1 transition; no fresh run; see 04-UAT.md § UAT-02 Phase 20 Closure, D-02).`

    **Change 4 — Reconcile "server-side membership gate" terminology (UAT-01 bullet).**
    The UAT-01 bullet says the non-member is "blocked at the server-side membership gate". The exercised
    path is the client-side OAuth guild check that rejects before any RPC. Add a parenthetical clarifier
    to the UAT-01 bullet so REQUIREMENTS.md no longer contradicts the 03-UAT.md acceptance basis, e.g.
    `(enforced client-side via the OAuth guild check before the profile RPC; server-side RLS is a
    defense-in-depth backstop — same membership-enforcement outcome)`. Keep it concise; do not rewrite
    the whole bullet.

    Do NOT touch any non-UAT requirement rows or unrelated sections.
  </action>
  <verify>
    <automated>grep -c "UAT-01 | Phase 20 | Validated" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "UAT-02 | Phase 20 | Validated" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "UAT-0. | Phase 20 | Pending" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "client-side" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <!-- HIGH (Cycle 3): the contradictory global "executed live ... this milestone" framing must be gone -->
    <automated>grep -c "executed live by the operator\*\* this milestone" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "Executed live by the operator this milestone with the required test accounts" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "Operator chose to run UAT-01/02 live" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <!-- The accept-pre-existing-evidence wording must be present -->
    <automated>grep -c "pre-existing live" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
    <automated>grep -c "No fresh run was performed this milestone\|no fresh run was performed this milestone\|No fresh runs were performed this milestone" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UAT-01 | Phase 20 | Validated"` returns 1
    - `grep -c "UAT-02 | Phase 20 | Validated"` returns 1
    - `grep -c "UAT-0. | Phase 20 | Pending"` returns 0 (no UAT row still Pending)
    - The UAT-01 bullet line begins with `- [x]` and the UAT-02 bullet line begins with `- [x]`
    - The UAT-01 bullet contains a client-side/guild-check clarifier reconciling the "server-side membership gate" wording
    - Each UAT bullet contains a "Closed Phase 20 (2026-06-04)" closure clause citing the relevant UAT file section and decision (D-01/D-03 for UAT-01, D-02 for UAT-02)
    - **(HIGH — Cycle 3) The contradictory global "executed live this milestone" framing is GONE:**
      - `grep -c "executed live by the operator\*\* this milestone"` returns 0 (the operator-closure-decisions bullet, formerly line ~14, no longer claims THIS-milestone live execution)
      - `grep -c "Executed live by the operator this milestone with the required test accounts"` returns 0 (the Live Human UAT section intro, formerly line ~30, is rewritten)
      - `grep -c "Operator chose to run UAT-01/02 live"` returns 0 (the Out-of-Scope row, formerly line ~85, is rewritten)
    - **(HIGH — Cycle 3) The accept-pre-existing-evidence wording is PRESENT:**
      - `grep -c "pre-existing live"` returns >= 2 (the rewritten global lines + Out-of-Scope row reference accepting pre-existing live evidence)
      - `grep` finds a "no fresh run was performed this milestone" assertion (case-insensitive; singular or plural) in the rewritten global framing
      - The rewritten lines cite D-01/D-02 and preserve the requirement IDs UAT-01 / UAT-02 and the operator's "live with real accounts; not E2E-mocked" intent
    - No non-UAT requirement row is modified
  </acceptance_criteria>
  <done>REQUIREMENTS.md shows UAT-01/UAT-02 as Validated with checked boxes, closure clauses, the server-side/client-side terminology reconciled, AND the three global "executed live this milestone" lines (formerly ~14/~30/~85) rewritten to the operator-locked accept-pre-existing-live-evidence pivot (D-01/D-02) so no internal contradiction remains.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate ROADMAP.md Phase 20 as reconciled/closed</name>
  <read_first>
    .planning/ROADMAP.md (read the Phase 20 region. Verify exact current strings before editing — line numbers may shift: top-level entry "- [ ] **Phase 20: Live Human UAT** - Execute Phase 03 UAT tests 2+3 ..." at line ~81; § Phase 20 section at lines ~154-170 with goal/success-criteria; "**Plans**: 2 plans" at line ~165; "**Wave 1** ..." at line ~167; the two plan checkboxes at lines ~169-170; and the Progress-table row "| 20. Live Human UAT | 0/2 | Not started | - |" at line ~195.)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` to reflect that Phase 20 is evidence-reconciliation closure (not pending live
    execution) AND that it now has 3 plans (20-03 added). Precise changes only:

    **Change 1 — Plan count 2 → 3 (MEDIUM, Cycle 3).**
    - Replace `**Plans**: 2 plans` (line ~165) with `**Plans**: 3 plans`.
    - In the Progress table, replace the row `| 20. Live Human UAT | 0/2 | Not started | - |` (line ~195)
      with `| 20. Live Human UAT | 0/3 | Not started | - |`.

    **Change 2 — Add the 20-03 Wave 2 entry (MEDIUM, Cycle 3).**
    After the two existing Wave 1 plan checkboxes (lines ~169-170), add a Wave 2 grouping with the 20-03
    checkbox (leave it unchecked — execution status is owned by execute-phase):
    ```
    **Wave 2** *(blocked on Wave 1 completion)*

    - [ ] 20-03-PLAN.md — Flip UAT-01/02 to Validated in REQUIREMENTS.md (rewrite the global "this milestone" framing per D-01/D-02) + ROADMAP closure annotations
    ```
    Leave the two Wave 1 checkboxes (`20-01-PLAN.md ...`, `20-02-PLAN.md ...`) unchecked — do NOT pre-check
    them. (If `annotate-dependencies` has already inserted a Wave 2 header, do not duplicate it — add only
    the 20-03 checkbox under the existing Wave 2 header; this step is idempotent.)

    **Change 3 — Reconcile the accepted-evidence framing.**
    - In the Phase 20 § Success Criteria / goal narrative (lines ~159-163), add a single clarifying line (do
      NOT delete existing criteria) noting the accepted-evidence basis: UAT-01/02 are closed by accepting the
      pre-existing live PASS evidence (UAT-01: 2026-05-03 second-human run; UAT-02: MapCommittee Off-Record 6a
      PASS during the v1.0→v1.1 transition) per D-01/D-02, with the gate path verified unchanged — no fresh
      live runs were performed this milestone.
    - Reconcile the top-level Phase 20 entry (line ~81) wording from "Execute Phase 03 UAT tests 2+3 ... live
      with required accounts" so it no longer reads as outstanding live execution — append
      ` (closed by accepting the pre-existing live evidence; see 03/04-UAT.md closure sections — no fresh run this milestone)`.

    Do NOT modify any other phase's ROADMAP entry or Progress-table row.
  </action>
  <verify>
    <automated>grep -c "pre-existing" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md</automated>
    <!-- Scope the plan-count grep to the Phase 20 section ONLY: Phases 18 and 19 already say "Plans**: 3 plans" (ROADMAP lines ~125/~143), so a GLOBAL grep would return 3 after a correct Phase 20 edit, not 1. The awk range isolates the Phase-20 block (### Phase 20: → ### Phase 21:). -->
    <automated>awk '/^### Phase 20:/{f=1} /^### Phase 21:/{f=0} f' /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md | grep -c "Plans\*\*: 3 plans"</automated>
    <automated>awk '/^### Phase 20:/{f=1} /^### Phase 21:/{f=0} f' /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md | grep -c "Plans\*\*: 2 plans"</automated>
    <automated>grep -c "20. Live Human UAT | 0/3" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md</automated>
    <automated>grep -c "20-03-PLAN.md" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md</automated>
    <automated>grep -c "20. Live Human UAT | 0/2" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase-20-SCOPED plan-count check returns 1: `awk '/^### Phase 20:/{f=1} /^### Phase 21:/{f=0} f' ROADMAP.md | grep -c "Plans\*\*: 3 plans"` returns 1 AND the same scoped grep for `"Plans\*\*: 2 plans"` returns 0 (Phase 20 plan count bumped 2 → 3 within its own section). DO NOT use a GLOBAL `grep -c "Plans\*\*: 3 plans"` expecting 1 — Phases 18 and 19 already carry "Plans**: 3 plans" (ROADMAP lines ~125/~143), so a correct Phase 20 edit makes the global count 3, not 1. The grep MUST be section-scoped. review HIGH — Cycle 5: verified global count = 3 after edit, Phase-20-scoped count = 1.
    - `grep -c "20. Live Human UAT | 0/3"` returns 1 AND `grep -c "20. Live Human UAT | 0/2"` returns 0 (Progress row updated)
    - `grep -c "20-03-PLAN.md"` returns >= 1 (a Wave 2 checkbox for 20-03 exists)
    - ROADMAP.md Phase 20 section contains a clarifying line stating UAT-01/02 are closed by accepting pre-existing live evidence per D-01/D-02 (no fresh runs this milestone)
    - The top-level Phase 20 entry no longer reads as if live runs are still outstanding
    - No other phase's ROADMAP entry or Progress row is modified
  </acceptance_criteria>
  <done>ROADMAP.md Phase 20 reconciled to evidence-reconciliation closure: plan count 3, 0/3 progress, a Wave 2 20-03 entry, and accepted-evidence wording; other phases untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only planning markdown files (REQUIREMENTS.md, ROADMAP.md) under .planning/ — no executable code, no auth path, no data handling, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | (null surface) | .planning markdown | accept | Phase changes no executable code, no migrations, no auth logic, no user data. No new attack surface introduced. Markdown-only changes under version control. |
</threat_model>

<verification>
After both tasks complete, run:

```bash
# UAT rows Validated, none Pending
grep "UAT-0" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md | grep "Phase 20"
grep -c "UAT-0. | Phase 20 | Pending" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md   # expect 0

# HIGH (Cycle 3): the three contradictory global "executed live this milestone" lines must be GONE
grep -c "executed live by the operator\*\* this milestone" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md          # expect 0
grep -c "Executed live by the operator this milestone with the required test accounts" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md  # expect 0
grep -c "Operator chose to run UAT-01/02 live" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md                       # expect 0
# ...and the accept-pre-existing-evidence wording must be PRESENT
grep -c "pre-existing live" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md                                          # expect >= 2
grep -ci "no fresh run.* performed this milestone\|no fresh runs were performed this milestone" /Users/khaiphan/code/wtcs-community-polls/.planning/REQUIREMENTS.md  # expect >= 1

# ROADMAP plan count + Wave 2 + progress row (MEDIUM, Cycle 3)
# Phase-20-SCOPED plan-count (Phases 18/19 also say "Plans**: 3 plans" — a GLOBAL grep returns 3, not 1):
awk '/^### Phase 20:/{f=1} /^### Phase 21:/{f=0} f' /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md | grep -c "Plans\*\*: 3 plans"   # expect 1 (Phase 20 section only)
awk '/^### Phase 20:/{f=1} /^### Phase 21:/{f=0} f' /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md | grep -c "Plans\*\*: 2 plans"   # expect 0
grep -c "20. Live Human UAT | 0/3" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md   # expect 1
grep -c "20-03-PLAN.md" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md         # expect >= 1
grep -n "Phase 20" /Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md
```

Expected: both UAT rows show Validated; zero Pending UAT rows; the three global "this milestone" lines are
gone and the accept-pre-existing-evidence wording is present (no internal contradiction); ROADMAP Phase 20
shows 3 plans / 0/3 with a Wave 2 20-03 entry.

**Phase-close audit note (verify-phase):** verify-phase must confirm STATE.md no longer carries UAT-01/02 as
open/deferred at close. 20-03 deliberately does not hand-edit STATE status (step 13b / verify-phase owns that
transition); this note exists so the close audit catches any stale STATE UAT entry.
</verification>

<success_criteria>
- REQUIREMENTS.md: UAT-01 and UAT-02 status = Validated, checkboxes [x], closure clauses present, server-side/client-side wording reconciled
- REQUIREMENTS.md: the three contradictory global "executed live by the operator this milestone" lines (formerly ~14/~30/~85) are REWRITTEN to the accept-pre-existing-live-evidence pivot (D-01/D-02); no internal contradiction remains for a v1.4 debt-zero audit
- ROADMAP.md: Phase 20 reflects 3 plans / 0/3 progress, a Wave 2 20-03 entry, and accepted-evidence reconciliation closure wording
- Plan checkboxes left unchecked (execute-phase owns flipping them); STATE.md is intentionally NOT hand-edited for UAT status (owned by phase-close/verify-phase, with a close-audit note)
- No file outside REQUIREMENTS.md, ROADMAP.md, and 20-03-SUMMARY.md is modified
</success_criteria>

<output>
Create `.planning/phases/20-live-human-uat/20-03-SUMMARY.md` when done.
</output>
