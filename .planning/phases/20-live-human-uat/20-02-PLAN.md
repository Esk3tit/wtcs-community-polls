---
phase: 20-live-human-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md
  - .planning/phases/20-live-human-uat/20-02-SUMMARY.md
autonomous: true
requirements:
  - UAT-02
must_haves:
  truths:
    - "04-UAT.md top-level frontmatter result field is 'complete' (not 'partial')"
    - "04-UAT.md contains NO literal 'deferred: 1' anywhere — the only occurrence is the Summary block at line ~170 (there is no top-level frontmatter deferred field); after edit it reads deferred: 0"
    - "04-UAT.md Summary block shows deferred: 0 and partial: 0 and passed: 15"
    - "D-04: Historical result: deferred row (frontmatter re_run.results test 6a) and result: partial row (### 6 body) are verbatim-preserved (not rewritten to pass)"
    - "The actual result: deferred data row is the frontmatter re_run.results test 6a object (line ~25) — it receives a resolution: forward pointer; the ### 6 body result: partial row (line ~102) receives a SEPARATE resolution pointer (review HIGH #2)"
    - "Both 'Test 6a still pending' followup lines (frontmatter line ~52 + Summary re_run_remaining_followups line ~176) are resolved with a Phase 20 pointer"
    - "The residual ## Current Test annotation (line ~70) no longer reads 'test 6a deferred until second admin signs in' — grep -c 'deferred until second admin' returns 0; it is a current-status note (not a D-04 historical row) and is replaced with a Phase 20 closure note (review HIGH #2)"
    - "D-02: A formal Phase 20 closure marker ties the UAT-02 verdict to the Off-Record Verification PASS — off-record MapCommittee 6a PASS backfilled, no fresh live run"
    - "D-05: closure section documents why a literal screenshot/precise-UTC timestamp of the off-record historical demote event is unavailable — narrative+verdict stands"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md"
      provides: "UAT-02 reconciled with debt-zero rollup and formal Phase 20 closure"
      contains: "deferred: 0"
  key_links:
    - from: "04-UAT.md frontmatter (result, result_note fields)"
      to: "04-UAT.md § Off-Record Verification"
      via: "Phase 20 closure pointers in frontmatter result_note and Summary followup"
      pattern: "Phase 20"
    - from: "04-UAT.md frontmatter re_run.results test 6a (result: deferred data row)"
      to: "04-UAT.md § Off-Record Verification"
      via: "resolution pointer added inside the test 6a object — the ACTUAL deferred data row (not the ### 6 partial row)"
      pattern: "Off-Record Verification"
    - from: "04-UAT.md § Tests ### 6 (result: partial body row)"
      to: "04-UAT.md § Off-Record Verification"
      via: "separate resolution pointer appended to the partial row"
      pattern: "Off-Record Verification"
---

<objective>
Reconcile 04-UAT.md (Phase 04 Admin Panel) so UAT-02 is debt-zero.

Purpose: The Off-Record Verification section already records UAT test 6a as PASS (MapCommittee, v1.0→v1.1 transition), but the file's top-level frontmatter still reads `result: partial`, the Summary YAML block has `deferred: 1` / `partial: 1` (the ONLY literal `deferred: 1` in the file, at line ~170 — there is no top-level frontmatter `deferred` field), the frontmatter `re_run.results` test 6a object holds the actual `result: deferred` data row with no forward pointer, the `### 6` body row has `result: partial` with no forward pointer, two followup lines still read "Test 6a still pending", and the `## Current Test` annotation (line ~70) still reads "test 6a deferred until second admin signs in" (a residual current-status note, review HIGH #2). This plan clears all debt markers while preserving every historical row verbatim per D-04.

Output: 04-UAT.md with (0) the `## Current Test` annotation updated from "test 6a deferred until second admin signs in" to a Phase 20 closure note, (1) top-level frontmatter updated to `result: complete` + updated result_note, (2) Summary YAML block updated to `deferred: 0` / `partial: 0` / `passed: 15` (clearing the only `deferred: 1`), (3) a `resolution:` pointer added to the frontmatter `re_run.results` test 6a `result: deferred` object AND a separate `resolution:` pointer on the `### 6` `result: partial` body row, (4) both "still pending" followup lines resolved, (5) a § UAT-02 Phase 20 Closure section formally tying the verdict to the Off-Record PASS with a D-05 narrative-artifact paragraph and the corrected unit-test count (8).
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/khaiphan/code/wtcs-community-polls/.planning/PROJECT.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/ROADMAP.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/STATE.md
@/Users/khaiphan/code/wtcs-community-polls/.planning/phases/20-live-human-uat/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update 04-UAT.md — frontmatter, rollup, pointers, closure section</name>
  <read_first>
    .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md (read the FULL file — every section including frontmatter YAML, all test rows, Summary block, re_run block, Gaps section, and Off-Record Verification section; note exact line numbers for the deferred row and the two "still pending" followup strings)
  </read_first>
  <action>
    Edit `.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md` with six additive/update changes. Apply them in this order:

    **Change 0 — Resolve the residual `## Current Test` deferred annotation (review HIGH #2 — Cycle 4).**
    Under the `## Current Test` heading (line ~68), there is a current-status annotation line (line ~70) that reads verbatim:
    `[2026-04-25 re-run: 8 of 9 previously-blocked tests now pass on live prod; test 6a deferred until second admin signs in]`
    This is a CURRENT-STATUS annotation, NOT a D-04-preserved historical `result:` data row, so it IS in scope to update (and it must be — leaving "deferred until second admin" here violates Phase 20 success criterion 3 "no remaining pending/deferred" and the v1.4 debt-zero mandate). Replace that single line in full with a Phase 20 closure note:
    `[2026-04-25 re-run: 8 of 9 previously-blocked tests passed on live prod; test 6a (demote flow) RESOLVED — off-record MapCommittee PASS formalized via Phase 20 closure (2026-06-04), no fresh live run per D-02. See § Off-Record Verification + § UAT-02 Phase 20 Closure.]`
    Do NOT alter the `## Current Test` heading itself; only replace the annotation line beneath it. Preserve all D-04 historical `result:` rows verbatim — this change touches ONLY the current-status annotation, not any test data row.

    **Change 1 — Frontmatter update.**
    In the YAML frontmatter (between the `---` delimiters at the top of the file):
    - Change the TOP-LEVEL `result: partial` (line ~3) → `result: complete`. (Do NOT touch the per-test `result:` values inside `re_run.results` — those are historical data rows preserved per D-04.)
    - There is NO top-level frontmatter `deferred:` field — the only literal `deferred: 1` in the entire file is in the Summary block (line ~170), handled by Change 4. Do not invent a frontmatter deferred field.
    - Replace the `result_note:` string with:
      `"15/15 functional cases pass; test 6a (live demote-admin click flow) backfilled via Off-Record Verification PASS by MapCommittee (Discord ID 290377966251409410) during v1.0→v1.1 transition. Formally closed Phase 20 (2026-06-04). See § Off-Record Verification."`
    - Do NOT change any other frontmatter fields (status, phase, source, started, updated, re_run block contents).

    **Change 2 — Resolution pointers on BOTH preserved non-pass rows (review HIGH #2).**
    There are TWO distinct historical non-pass rows that each need a forward pointer per D-04. Add a pointer to each — do NOT conflate them:

    **2a — The ACTUAL `result: deferred` data row (frontmatter).**
    In the YAML frontmatter `re_run:` block, under `results:`, locate the list item with `test: 6a` (line ~24). Its object currently has `result: deferred` (line ~25) and a `reason:` (line ~26). Add a `resolution:` key INSIDE this same object, indented to match `result:`/`reason:` (same list-item indentation), immediately after the `reason:` line:
    ```
      resolution: "6a superseded by Off-Record Verification PASS (MapCommittee, Discord ID 290377966251409410, v1.0→v1.1 transition); Phase 20 closure 2026-06-04. See § Off-Record Verification + § UAT-02 Phase 20 Closure below."
    ```
    The `result: deferred` line in this object MUST remain verbatim — do NOT change it to `pass`. This is the row the reviewers found was being missed: it is in frontmatter, NOT under `### 6`.

    **2b — The `### 6` body `result: partial` row.**
    In the `## Tests` section under `### 6. Demote Admin + Self-Demote Guard`, the body row has `result: partial` (line ~102) followed by a `re_run_evidence:` block (line ~105). Immediately below the `re_run_evidence:` line (as a new sibling YAML key at the same indentation, preserving validity), add:
    ```
    resolution: "Test 6 6a sub-case superseded by Off-Record Verification PASS (MapCommittee, v1.0→v1.1 transition); Phase 20 closure 2026-06-04. See § Off-Record Verification below. (6b self-demote guard already passed.)"
    ```
    The `result: partial` line in this body row MUST remain verbatim — do NOT change it to `pass`.

    **Change 3 — Resolve the two "still pending" followup lines.**
    In the frontmatter `followups:` list (inside the `re_run:` block), locate the line:
    `"Test 6a still pending — second admin sign-in required."`
    Replace it with:
    `"Test 6a RESOLVED — Off-Record Verification PASS recorded (MapCommittee, v1.0→v1.1 transition); Phase 20 closure 2026-06-04. See § Off-Record Verification."`
    (This is the frontmatter followup entry — it is a data field, not a historical test result row, so updating it is consistent with D-04's "keep test result rows verbatim" rule.)

    In the `## Summary` YAML block's `re_run_remaining_followups:` list (after the Summary YAML), locate the line:
    `- Test 6a (demote click flow) — awaits a second logged-in admin in prod.`
    Replace it with:
    `- Test 6a RESOLVED — Off-Record Verification PASS (MapCommittee, v1.0→v1.1 transition); Phase 20 closure 2026-06-04.`

    **Change 4 — Update the Summary YAML block.**
    Locate the `## Summary` YAML block. Change:
    - `passed: 14` → `passed: 15`
    - `partial: 1` → `partial: 0`
    - `deferred: 1` → `deferred: 0`
    Leave `total: 15`, `issues: 0`, `pending: 0`, `skipped: 0`, `blocked: 0` unchanged.
    Also update the `re_run_partial` line: change `re_run_partial: 1 (test 6 — 6b pass, 6a deferred to second-admin sign-in)` → `re_run_partial: 0 (test 6 — both 6a and 6b pass; 6a backfilled via Off-Record Verification)`.

    **Change 5 — Add § UAT-02 Phase 20 Closure section.**
    Append a new top-level section at the END of the file (after the last line of § Off-Record Verification / UAT 6a Notes), titled:

    ```
    ## UAT-02 Phase 20 Closure
    ```

    The section must contain:
    - A statement that this section formally closes UAT-02 per Phase 20 (2026-06-04) decision D-02.
    - A statement that the Off-Record Verification PASS by MapCommittee (Discord ID 290377966251409410) during the v1.0→v1.1 transition is accepted as satisfying UAT-02's "executed live with a real second admin account" requirement per the D-02 backfill decision.
    - A statement that the no-fresh-run decision is D-02 (not D-01 — D-01 covers UAT-01).
    - A citation of the source-side coverage: **8 unit tests** (`it()` blocks) in `src/__tests__/admin/demote-admin.test.ts`. IMPORTANT (review MEDIUM): the historical evidence rows say "13 unit tests" — that figure is stale and is preserved verbatim ONLY inside the historical rows per D-04; this NEW closure section must state the verified current count of 8. Do not propagate "13" into the closure section.
    - **D-05 narrative-artifact paragraph (review MEDIUM):** an explicit paragraph stating that the off-record demote event has no precise UTC timestamp (only the v1.0→v1.1 transition window 2026-04-28 → 2026-05-07 is known from git/audit records) and no screenshot/screen-recording, because it was a historical off-record event that cannot be reconstructed after the fact. Per D-05, the narrative evidence + PASS verdict recorded in § Off-Record Verification is accepted as the lightest sufficient artifact. Optionally note that the operator may attach a current confirming screenshot of the reproducible Admins-list/demote UI from their own admin session (D-06), but it is not required for closure.
    - An explicit success-criterion-3 clarification: debt-zero applies to the Summary rollups and followups; the archival `result: deferred` / `result: partial` rows are intentionally preserved verbatim per D-04 and are NOT outstanding debt.
    - The debt-zero verdict: UAT-02 is PASS; `04-UAT.md` carries no remaining deferred or pending scenarios in any aggregate/rollup.

    Use plain prose. No fenced code blocks.
  </action>
  <verify>
    <automated>grep -c "deferred: 0" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep -c "^result: complete" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep -c "passed: 15" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep -c "UAT-02 Phase 20 Closure" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep -c "^      result: deferred$" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep -c "^result: partial$" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep -c "deferred: 1" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
    <automated>grep "result: deferred" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md | grep -vc '^>'</automated>
    <automated>grep -c "deferred until second admin" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deferred: 0"` returns exactly 1 (review HIGH #2 — verified: the file has only ONE `deferred:` aggregate, the Summary block at line ~170; a correct edit flips that single `deferred: 1` to `deferred: 0`, yielding exactly one match. Do NOT expect "at least 2" — there is no second `deferred:` aggregate field, so any count >1 would mean spurious text was added. The test-6a frontmatter row uses `result: deferred`, not a `deferred:` aggregate, so it does not match this grep.)
    - `grep -c "^result: complete"` returns 1 (TOP-LEVEL frontmatter result field updated; anchored to avoid matching prose)
    - `grep -c "passed: 15"` returns 1 (Summary block updated)
    - `grep -c "UAT-02 Phase 20 Closure"` returns 1 (new closure section present)
    - ANCHORED deferred data-row check `grep -c "^      result: deferred$"` returns 1 — the single preserved frontmatter test-6a data row (6-space indent under `results:`). NOTE: the UNANCHORED `grep -c "result: deferred"` returns 2 today and AFTER edits because the § Off-Record Verification blockquote prose at line ~212 also contains the literal string. That prose match is expected and is NOT a data row. The blockquote-excluded check `grep "result: deferred" file | grep -vc '^>'` returns 1. (review HIGH #1 — verified: unanchored=2, anchored/excluded=1.)
    - ANCHORED `grep -c "^result: partial$"` returns 1 — the single preserved `### 6` body data row. (Today the unanchored count is 2; the second match is the top-level frontmatter `result: partial` which Change 1 flips to `complete`, leaving exactly 1 body row.)
    - `grep -c "deferred: 1"` returns 0 — the only literal `deferred: 1` (Summary line ~170) is cleared by Change 4. (review MEDIUM — verified: the sole `deferred: 1` is at line 170, not frontmatter.)
    - File does NOT contain "passed: 14" anywhere
    - File does NOT contain "Test 6a still pending" anywhere (both pending followup strings — frontmatter line ~52 + Summary line ~176 — resolved)
    - `grep -c "deferred until second admin"` returns 0 — the residual `## Current Test` annotation (line ~70) no longer carries the "test 6a deferred until second admin signs in" phrasing; it now reads as a Phase 20 closure note (review HIGH #2 — Cycle 4)
    - The § Off-Record Verification section is unchanged (MapCommittee PASS record intact)
    - The closure section states the source-side count as 8 (not 13); historical rows retain "13" verbatim per D-04
    - src/__tests__/admin/demote-admin.test.ts is NOT modified
  </acceptance_criteria>
  <done>04-UAT.md frontmatter reads result: complete with deferred: 0; Summary block shows passed: 15 / deferred: 0 / partial: 0; historical deferred and partial rows preserved with resolution pointers; § UAT-02 Phase 20 Closure section present at end of file; no "still pending" lines remain; the ## Current Test annotation no longer reads "deferred until second admin" (Phase 20 closure note instead).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies only a planning markdown file under .planning/ — no executable code, no auth path, no data handling, no network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | (null surface) | .planning/milestones markdown | accept | Phase changes no executable code, no migrations, no auth logic, no user data. No new attack surface introduced. Markdown-only changes under version control. |
</threat_model>

<verification>
After the task completes, run:

```bash
grep -c "deferred: 1" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 0
grep -c "deferred: 0" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 1 (single Summary aggregate, line ~170)
grep -c "^result: complete" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 1
grep "passed:" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md
grep -c "^      result: deferred$" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 1 (anchored data row)
grep "result: deferred" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md | grep -vc '^>'   # expect 1 (blockquote prose excluded)
grep -c "^result: partial$" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 1
grep -c "UAT-02 Phase 20 Closure" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 1
grep -c "Test 6a still pending" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 0
grep -c "deferred until second admin" /Users/khaiphan/code/wtcs-community-polls/.planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-UAT.md   # expect 0 (residual ## Current Test annotation resolved — review HIGH #2)
```

Expected: no `deferred: 1` anywhere; exactly one `deferred: 0` (the single Summary aggregate at line ~170 — NOT "at least 2"); top-level frontmatter `result: complete`; Summary `passed: 15` / `deferred: 0` / `partial: 0`; exactly one preserved frontmatter `result: deferred` data row (anchored/blockquote-excluded = 1; unanchored = 2 due to Off-Record prose, expected); exactly one preserved `### 6` `result: partial` body row; closure section present (count = 1); "still pending" count = 0; "deferred until second admin" count = 0 (residual `## Current Test` annotation resolved).
</verification>

<success_criteria>
- 04-UAT.md top-level frontmatter: `result: complete`, updated `result_note` citing Off-Record Verification PASS and Phase 20 closure (no top-level `deferred:` field exists or is added)
- Summary YAML block: `passed: 15`, `partial: 0`, `deferred: 0`; no literal `deferred: 1` remains anywhere
- The frontmatter `re_run.results` test 6a `result: deferred` data row carries a `resolution:` pointer (the actual deferred row — review HIGH #2), AND the `### 6` `result: partial` body row carries a SEPARATE `resolution:` pointer; both `result:` values preserved verbatim per D-04
- Anchored counts: `^      result: deferred$` = 1, `^result: partial$` = 1 (unanchored `result: deferred` = 2 due to Off-Record blockquote prose — expected, not a regression)
- Both "Test 6a still pending" followup strings (frontmatter + Summary) replaced with resolved variants
- The residual `## Current Test` annotation (line ~70) no longer reads "test 6a deferred until second admin signs in" — `grep -c "deferred until second admin"` = 0; replaced with a Phase 20 closure note (current-status annotation, not a D-04 historical row — review HIGH #2)
- § UAT-02 Phase 20 Closure section present at end of file, with the D-05 narrative-artifact paragraph, the success-criterion-3 archival-rows clarification, and the source-side count stated as 8 (not 13)
- No file outside `04-UAT.md` and `20-02-SUMMARY.md` is modified
</success_criteria>

<output>
Create `.planning/phases/20-live-human-uat/20-02-SUMMARY.md` when done.
</output>
