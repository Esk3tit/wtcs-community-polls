---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/database.types.ts
  - package.json
  - package-lock.json
  - src/components/ui/checkbox.tsx
  - src/components/ui/switch.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - VIS-06
  - VIS-07
tags:
  - foundation
  - types
  - shadcn
  - requirements

must_haves:
  truths:
    - "`src/lib/types/database.types.ts` contains the `results_hidden` and `results_hidden_changed_at` columns under the `polls` table Row + Insert + Update types (Phase 11 migration 10 columns) and under the `polls_effective` view Row type (Phase 11 VIS-09 projection)"
    - "`npm run gen:types` is wired in package.json scripts and runs `supabase gen types typescript --linked > src/lib/types/database.types.ts` (D-18 — repeatability after future schema drift)"
    - "`src/components/ui/checkbox.tsx` exists, exports `Checkbox` as a named export, wraps `@radix-ui/react-checkbox`, and matches the shadcn new-york style (per `components.json`) — vendored via `npx shadcn@latest add checkbox` (D-17 enabling primitive)"
    - "`src/components/ui/switch.tsx` exists, exports `Switch` as a named export, wraps `@radix-ui/react-switch`, and matches the shadcn new-york style — vendored via `npx shadcn@latest add switch` (D-05 enabling primitive)"
    - "`@radix-ui/react-checkbox` and `@radix-ui/react-switch` appear under `dependencies` in package.json after the shadcn CLI completes (the CLI auto-installs Radix deps; no separate npm install needed)"
    - "REQUIREMENTS.md VIS-07 body is rewritten to drop the 'AlertDialog confirmation' prose and reflect the locked Switch + optimistic toast pattern per D-01 (CONTEXT.md decision; matches the REQUIREMENTS wording edit explicitly called out in Phase 12 scope)"
    - "`tsc -b` passes after type regen (D-18 — `SuggestionWithChoices` and the existing `useSuggestions` SELECT pick up the new columns automatically without code changes)"
    - "`npm run lint` passes — the new shadcn UI files conform to the existing ui/* lint shape (no console.log, no unused imports)"
    - "`src/__tests__/admin/polls-effective-invariant.test.ts` continues to pass — the type regen does not add any new direct `from('polls')` reads in `src/` (it only updates the generated types file)"
  artifacts:
    - path: "src/lib/types/database.types.ts"
      provides: "Regenerated Supabase types covering Phase 11 migration 10 columns + view projection"
      contains: "results_hidden"
    - path: "src/components/ui/checkbox.tsx"
      provides: "shadcn Checkbox primitive (new-york style, Neutral baseColor)"
      exports: ["Checkbox"]
    - path: "src/components/ui/switch.tsx"
      provides: "shadcn Switch primitive (new-york style, Neutral baseColor)"
      exports: ["Switch"]
    - path: "package.json"
      provides: "gen:types npm script for repeatable type regen (D-18)"
      contains: "gen:types"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VIS-07 wording edit (Button + AlertDialog → Switch + optimistic toast per D-01)"
      contains: "Switch"
  key_links:
    - from: "package.json scripts.gen:types"
      to: "supabase gen types typescript --linked"
      via: "npm script"
      pattern: "supabase gen types typescript --linked"
    - from: "src/components/ui/checkbox.tsx"
      to: "@radix-ui/react-checkbox"
      via: "import"
      pattern: "@radix-ui/react-checkbox"
    - from: "src/components/ui/switch.tsx"
      to: "@radix-ui/react-switch"
      via: "import"
      pattern: "@radix-ui/react-switch"
---

<objective>
Land the Phase 12 foundation: regenerate `database.types.ts` against the live Phase 11 schema, wire a `gen:types` npm script for future repeatability, vendor the two new shadcn primitives (Checkbox + Switch) every downstream plan consumes, and edit REQUIREMENTS.md VIS-07 to reflect the locked Switch + optimistic-toast pattern (D-01).

Purpose: Wave 1 must put types + primitives + the VIS-07 wording fix on disk before Wave 2 plans can compile the new admin UI (`<Checkbox>` in VIS-06 plan 02, `<Switch>` in VIS-07 plan 03) or branch on `results_hidden` (Plan 04). Without the type regen, `useSuggestions` and `useVoteCounts` cannot reference `results_hidden` type-safely. Without the REQUIREMENTS edit, the canonical spec contradicts the implementation Phase 12 is about to ship.

Output: 2 new shadcn primitives, 1 regenerated types file, 1 package.json script addition, 1 REQUIREMENTS.md wording edit. Zero behavioral change to runtime code — pure foundation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Phase 11 deliverables consumed by this regen (read-only references) -->
@supabase/migrations/00000000000010_results_hidden_audit.sql
@.planning/phases/11-schema-rls-ef-foundations/11-01-SUMMARY.md

<!-- Source-of-truth files the executor MUST mirror -->
@components.json
@package.json
@src/lib/types/database.types.ts
@src/components/ui/dialog.tsx
@src/components/ui/label.tsx

<interfaces>
<!-- shadcn new-york Checkbox + Switch components register at: -->
<!--   src/components/ui/checkbox.tsx — exports `Checkbox` -->
<!--   src/components/ui/switch.tsx   — exports `Switch` -->
<!-- These are vendored verbatim from the shadcn registry; do not hand-roll. -->

<!-- Regenerated types add these to `Database['public']['Tables']['polls']['Row']`: -->
<!--   results_hidden: boolean -->
<!--   results_hidden_changed_at: string | null -->
<!-- And to `Database['public']['Views']['polls_effective']['Row']` (Phase 11 VIS-09). -->

<!-- REQUIREMENTS.md VIS-07 (current wording — to be rewritten): -->
<!--   "Button uses shadcn `Button` with `variant=\"outline\"`. Click opens a -->
<!--    shadcn `AlertDialog` confirming the flip..." -->
<!-- New wording (per CONTEXT.md D-01): inline shadcn Switch, optimistic flip, -->
<!-- sonner toast on success, revert + error toast on failure. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-00-01: Vendor shadcn Checkbox + Switch primitives</name>
  <files>src/components/ui/checkbox.tsx, src/components/ui/switch.tsx, package.json, package-lock.json</files>
  <read_first>
    - components.json (confirm `style: "new-york"`, `baseColor: "neutral"` so the shadcn CLI vendors against the correct preset)
    - src/components/ui/label.tsx (existing shadcn primitive — confirms the file-shape, `import { cn } from '@/lib/utils'`, and `forwardRef` pattern that the new files must mirror)
    - src/components/ui/dialog.tsx (existing Radix-backed primitive — confirms how Radix imports are referenced and how default props are surfaced)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md (Decisions A1-D5 + A6-D17 — Switch and Checkbox vendor commitments; combined install per Claude's Discretion bullet)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "Registry Safety" (`npx shadcn@latest add checkbox switch` is the canonical install command)
    - package.json (confirm current `dependencies` shape so the CLI's auto-installed `@radix-ui/react-checkbox` + `@radix-ui/react-switch` slot in cleanly)
  </read_first>
  <action>
    Run `npx shadcn@latest add checkbox switch` from the repo root. The CLI: (1) writes `src/components/ui/checkbox.tsx` and `src/components/ui/switch.tsx` against the new-york / Neutral preset declared in `components.json`; (2) auto-installs `@radix-ui/react-checkbox` and `@radix-ui/react-switch` into `dependencies` and updates `package-lock.json`. Do NOT hand-author the primitive files — the shadcn registry is the source of truth and any drift defeats UIDN-03 closure.

    Accept the CLI defaults (it will detect `components.json` and skip the init prompt). If it asks about overwriting (it should not, since neither file exists), confirm. After the CLI completes, run `npm run lint` to confirm the new files conform to the existing ui/* lint shape — the vendored files use `forwardRef`, named exports, and `cn()` from `@/lib/utils` — all already lint-clean elsewhere in `src/components/ui/`.

    Do not edit the vendored files except for `import` path normalization if the CLI uses `~` vs `@` aliases inconsistently with the existing primitives. The existing primitives use `@/lib/utils` — verify the new files use the same.
  </action>
  <verify>
    <automated>test -f src/components/ui/checkbox.tsx && test -f src/components/ui/switch.tsx && grep -q '"@radix-ui/react-checkbox"' package.json && grep -q '"@radix-ui/react-switch"' package.json && npm run lint</automated>
  </verify>
  <done>
    Both files exist as named exports (`export { Checkbox }`, `export { Switch }`), both Radix deps are pinned in `package.json` dependencies, ESLint passes with zero new errors on the two new files.
  </done>
</task>

<task type="auto">
  <name>Task 12-00-02: Regen database.types.ts + add npm gen:types script</name>
  <files>src/lib/types/database.types.ts, package.json</files>
  <read_first>
    - src/lib/types/database.types.ts (current stale version — confirm the `polls` table Row type LACKS `results_hidden` and the `polls_effective` view Row type LACKS the projection)
    - supabase/migrations/00000000000010_results_hidden_audit.sql (Phase 11 schema source-of-truth — confirms the new columns `results_hidden boolean NOT NULL DEFAULT false` and `results_hidden_changed_at timestamptz`)
    - .planning/phases/11-schema-rls-ef-foundations/11-01-SUMMARY.md (Phase 11 Plan 01 SUMMARY — confirms migration 10 landed in main and `polls_effective` was rewritten via CREATE OR REPLACE VIEW + `security_invoker = on` re-applied)
    - package.json (current `scripts` block — add `gen:types` alongside the existing `dev`, `build`, `lint`, `test` entries; maintain alphabetical/grouping convention if present)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-18 (regen command verbatim; CI drift gate explicitly deferred)
  </read_first>
  <action>
    Step 1: Add a new entry to `package.json` `scripts`: `"gen:types": "supabase gen types typescript --linked > src/lib/types/database.types.ts"`. Place it adjacent to `dev`/`build` for discoverability. The flag `--linked` reads the active linked Supabase project (the user must have run `supabase link` previously; this should already be the case since Phase 11 used the CLI to deploy migrations to prod).

    Step 2: Run `npm run gen:types`. The command overwrites `src/lib/types/database.types.ts` with the freshly generated TypeScript types pulled from the linked Supabase project's introspection. After the command exits, open the file and verify (a) the `polls` table `Row` type now includes `results_hidden: boolean` and `results_hidden_changed_at: string | null`; (b) the `polls_effective` view `Row` type includes both columns under its `Row` shape (Phase 11 VIS-09 added these to the view projection). Also verify the `audit_log` table appears (Phase 11 created this table) — its inclusion is incidental evidence that the regen ran against the right schema.

    Step 3: Run `tsc -b` to confirm the type regen is consumed without errors by the rest of the codebase. The existing `useSuggestions` SELECT (`from('polls_effective').select('*')`) and `useVoteCounts` SELECT (`from('vote_counts').select('poll_id, choice_id, count')`) MUST still type-check — they pick up the new columns automatically via `Tables<'polls'>` / `Views<'polls_effective'>` resolution. If `tsc -b` surfaces a type error elsewhere (e.g., a place that destructures `polls` with a fixed shape), that file needs a follow-up edit; surface it and stop.

    Step 4: Run `npm run test` to confirm `src/__tests__/admin/polls-effective-invariant.test.ts` continues to pass (Phase 11 VIS-09 invariant: zero direct `from('polls')` reads in `src/` outside the invariant test itself). The regen only touches the generated types file — it does not introduce any new `from('polls')` calls.

    The CI drift gate (automatic detection that the committed `database.types.ts` matches `supabase gen types`) is explicitly deferred per D-18 — do NOT add any CI workflow changes in this task.
  </action>
  <verify>
    <automated>grep -q '"gen:types":' package.json && grep -q "results_hidden" src/lib/types/database.types.ts && grep -q "results_hidden_changed_at" src/lib/types/database.types.ts && npx tsc -b && npm run test</automated>
  </verify>
  <done>
    `npm run gen:types` script exists; regenerated `database.types.ts` contains `results_hidden` and `results_hidden_changed_at` under both `polls.Row` and `polls_effective.Row`; `tsc -b` exits 0; `polls-effective-invariant.test.ts` passes; no other tests regress.
  </done>
</task>

<task type="auto">
  <name>Task 12-00-03: REQUIREMENTS.md VIS-07 wording edit (drop AlertDialog, adopt Switch + toast)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines around VIS-07 entry — the current bullet still says "Button uses shadcn `Button` with `variant=\"outline\"`" + "Click opens a shadcn `AlertDialog`...")
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § A1-D1 (AlertDialog dropped; optimistic Switch + sonner toast + revert on error)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § A2-D5 + D-06 (inline Switch placement, action-form two-state label "Hide results" / "Show results")
    - .planning/phases/11-schema-rls-ef-foundations/11-CONTEXT.md (Phase 11 user-pivot rationale — Switch chosen over Button mid-Phase 11)
    - src/hooks/usePinPoll.ts (canonical precedent for the optimistic + sonner toast + revert pattern that D-01 calls out by name)
  </read_first>
  <action>
    Edit the VIS-07 bullet in `.planning/REQUIREMENTS.md` § "Admin Visibility Controls (SEED-002 reframed)". Replace the existing prose:

    OLD (verbatim — find and replace the entire bullet body, not just keywords):
    `Admin UI — Live + archived admin suggestion cards gain a "Hide results" / "Show results" toggle button (label flips per current state). Button uses shadcn \`Button\` with \`variant="outline"\`. Click opens a shadcn \`AlertDialog\` confirming the flip ("Hide" / "Show" + suggestion title + audit-trail note). Confirm calls \`toggle-results-visibility\` EF.`

    NEW: `Admin UI — Live + archived admin suggestion cards gain an inline "Hide results" / "Show results" toggle (label flips per current state; mobile sub-`sm` shows a lucide `Eye`/`EyeOff` icon instead of the label). Uses shadcn `Switch` placed between the response-count and the kebab menu on `AdminSuggestionRow`. Click flips optimistically and calls the `toggle-results-visibility` EF in the background — sonner toast confirms ("Results hidden for: {title}" / "Results visible for: {title}"); on failure the Switch reverts and an error toast surfaces. Switch is `disabled` + a small `Loader2` adjacent while the EF is in-flight to suppress double-click no-op writes in `audit_log`. Matches the existing `usePinPoll` optimistic + revert precedent.`

    Do NOT change the requirement ID (`VIS-07`), the surrounding bullet structure, or the traceability table entry at the bottom of the file (that table still tracks `VIS-07 | Phase 12 | Pending` until Plan 06 marks it complete after TEST-13 passes).

    Add a single trailing parenthetical attribution to anchor the edit in plan history: ` _(Wording revised in Phase 12 Plan 00 per CONTEXT.md A1-D1: AlertDialog confirmation pattern replaced with optimistic Switch + sonner toast.)_`. This single italicized note preserves traceability without inserting a separate decision-log entry.
  </action>
  <verify>
    <automated>grep -A1 "VIS-07" .planning/REQUIREMENTS.md | grep -q "Switch" && ! grep -A2 "VIS-07.*Admin UI" .planning/REQUIREMENTS.md | grep -q "AlertDialog"</automated>
  </verify>
  <done>
    VIS-07 bullet body contains "Switch" and "optimistic" / "sonner toast"; the words "AlertDialog" and "Confirm" no longer appear within the VIS-07 bullet; the trailing italicized attribution note is present; the traceability table row for VIS-07 is unchanged (still `Phase 12 | Pending`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → shadcn registry | `npx shadcn@latest add` fetches component source from the official shadcn CDN. Trust boundary: shadcn registry endpoint authenticity. |
| developer machine → Supabase project | `supabase gen types typescript --linked` reads schema metadata from the linked production project via Supabase Management API. Trust boundary: Supabase access token in `~/.supabase/access-token`. |
| no runtime trust boundaries introduced | Plan 00 is build-time / generation-time only; no new request paths, no new auth surfaces, no new data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-00-01 | Tampering | shadcn registry response (Checkbox + Switch source) | accept | Official shadcn/ui registry; new-york + Neutral preset is the project default (D-17, D-05); CONTEXT.md UI-SPEC.md "Registry Safety" section explicitly marks the safety gate as "not required — official shadcn/ui, no third-party vetting". |
| T-12-00-02 | Information Disclosure | `database.types.ts` regen via `supabase gen types --linked` | mitigate | The regen exposes only column names + types (already public per RLS-secured access). No row data, no secrets. Reviewer skims the diff before commit to confirm no policy DDL leakage (the CLI does not emit policy bodies). |
| T-12-00-03 | Tampering | REQUIREMENTS.md edit | accept | This is a documentation-only edit (not source code); the diff is reviewer-visible in PR. The wording change is sourced from a CONTEXT.md decision (D-01) traceable through the discuss-phase artifact. |
| T-12-00-04 | Repudiation | Type regen drift between committed file and live schema | accept | CI drift gate explicitly deferred per D-18. Risk: future migration lands without a corresponding `npm run gen:types` commit, types go stale, downstream type errors silently slip through. Mitigation deferred to a future hygiene phase; v1.2 scope stays tight. |
| T-12-00-05 | Denial of Service | Radix CSS bundle size after Checkbox + Switch vendor | mitigate | shadcn Checkbox and Switch are small Radix wrappers (~2 KB gzip each); Phase 12 OBSV-02 bundle-delta tracking (deferred to closure) will quantify the actual delta. No runtime impact. |

</threat_model>

<verification>
- `npm run gen:types` exists in package.json scripts and produces a `results_hidden`-containing `database.types.ts`
- `src/components/ui/{checkbox,switch}.tsx` both exist and export their named components
- `npm run lint` passes after the regen + vendor
- `tsc -b` passes — the new types are consumable by `useSuggestions`, `useVoteCounts`, and `SuggestionWithChoices` without code changes
- `polls-effective-invariant.test.ts` passes (no new direct `from('polls')` reads introduced)
- REQUIREMENTS.md VIS-07 reads with the new Switch + optimistic-toast wording; the word "AlertDialog" no longer appears within the VIS-07 bullet
</verification>

<success_criteria>
- Wave 2 plans (`12-02`, `12-03`, `12-04`) can import `Checkbox` and `Switch` from `@/components/ui/*` without compile errors
- Wave 2 plans can branch on `results_hidden` against the type from `Tables<'polls_effective'>` without `any` casts
- REQUIREMENTS.md VIS-07 wording matches what Phase 12 actually ships, not the v1.0 SEED-002 prose
- Zero behavioral regression in existing tests / lint / build
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-00-SUMMARY.md`
</output>
