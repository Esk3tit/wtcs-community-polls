---
phase: 12-admin-ui-user-ui-uidn-03-sweep
plan: 02
type: execute
wave: 2
depends_on: ["00"]
files_modified:
  - src/components/suggestions/form/SuggestionForm.tsx
  - src/hooks/useCreatePoll.ts
  - src/lib/validation/suggestion-form.ts
autonomous: true
requirements:
  - VIS-06
  - UIDN-03
tags:
  - vis-06
  - uidn-03
  - suggestion-form
  - shadcn

must_haves:
  truths:
    - "VIS-06: `SuggestionForm.tsx` renders a shadcn `<Checkbox id=\"results-hidden\">` row below `CategoryPicker` with Label text \"Hide results from voters\" and helper text \"Voters with responses will see a placeholder instead of counts. Toggle anytime from the admin list.\" (REQUIREMENTS-verbatim label per D-17; helper copy verbatim per CONTEXT)"
    - "VIS-06: Checkbox defaults to unchecked (`results_hidden = false`) on a fresh `mode=\"create\"` form — backwards-compat with VIS-01 default"
    - "VIS-06 edit-mode behavior: When `mode=\"edit\"`, the checkbox pre-populates from the loaded poll row's `results_hidden` value. When the poll is locked (vote_count > 0, existing Phase 4 behavior), the checkbox is `disabled` alongside the rest of the form fields — admins flip via the Switch in the admin list (Plan 03) instead"
    - "VIS-06: `SuggestionFormInput` (type in `src/lib/validation/suggestion-form.ts`) gains an optional `results_hidden?: boolean` field; `validateSuggestionForm` accepts and passes it through; `useCreatePoll` forwards it to the `create-poll` EF body (Phase 11 D-08: the EF already accepts the field)"
    - "VIS-06: Submitting `mode=\"create\"` with the checkbox checked produces a poll row where `results_hidden = true` (verified via direct DB SELECT or Plan 06 E2E); submitting unchecked produces `results_hidden = false`. The toast copy stays \"Suggestion created\" — no separate variant for the checked path per UI-SPEC.md"
    - "UIDN-03 D-14: Both `<button type=\"button\" onClick={() => navigate({ to: '/admin' })}>` instances at `SuggestionForm.tsx:140` (loadError branch) and `:163` (main render path) are replaced with TanStack `<Link to=\"/admin\">`. The inline-flex span + `ChevronLeft` icon + \"Back to admin\" text + className visual styling are preserved verbatim — visual zero-delta"
    - "UIDN-03 D-14: Both `<Link>` sites are direct TanStack Router `<Link>` components imported from `@tanstack/react-router` — middle-click and cmd-click open in a new tab (regression-blocker that the prior `<button>` could not provide)"
    - "The `useNavigate` import is removed from `SuggestionForm.tsx` if it is no longer referenced after the two back-link sites are replaced (the file has no other navigation imperatives — verify by grep before removing)"
    - "VIS-06 + UIDN-03: `npm run lint`, `tsc -b`, and `npm run test` all pass"
    - "The `data-testid=\"visibility-checkbox\"` attribute is added to the Checkbox so Plan 06 TEST-13 can target it deterministically (UI-SPEC.md Test Surfaces section)"
  artifacts:
    - path: "src/components/suggestions/form/SuggestionForm.tsx"
      provides: "VIS-06 checkbox row below CategoryPicker + UIDN-03 D-14 back-link replacements"
      contains: "results-hidden"
    - path: "src/hooks/useCreatePoll.ts"
      provides: "useCreatePoll passes results_hidden through to the create-poll EF body"
      contains: "results_hidden"
    - path: "src/lib/validation/suggestion-form.ts"
      provides: "SuggestionFormInput.results_hidden optional boolean field"
      contains: "results_hidden"
  key_links:
    - from: "src/components/suggestions/form/SuggestionForm.tsx"
      to: "src/hooks/useCreatePoll.ts"
      via: "createPoll(input) — input now carries optional results_hidden"
      pattern: "results_hidden"
    - from: "src/components/suggestions/form/SuggestionForm.tsx"
      to: "@tanstack/react-router Link"
      via: "import"
      pattern: "import.*Link.*from '@tanstack/react-router'"
    - from: "src/hooks/useCreatePoll.ts"
      to: "supabase/functions/create-poll/index.ts"
      via: "supabase.functions.invoke body"
      pattern: "results_hidden"
---

<objective>
Implement VIS-06 (admin-side "Hide results from voters" checkbox at creation) and the two UIDN-03 D-14 back-link replacements in `SuggestionForm.tsx`. Co-landing in the same file because file ownership prevents parallel Wave-2 plans from touching SuggestionForm.tsx simultaneously.

Purpose: VIS-06 is the create-time half of admin visibility control — the new poll inherits the checkbox state. UIDN-03 D-14 replaces two imperative `<button onClick={() => navigate(...)}>` sites with declarative TanStack `<Link>` to restore browser-native middle-click / cmd-click semantics (regression-blocker per closure footnote [c]). Both changes touch the same file; bundling them keeps the diff coherent.

Output: One enlarged `SuggestionForm.tsx` (new Visibility section + two `<Link>` replacements), one extended `useCreatePoll.ts` (passes `results_hidden`), one extended `SuggestionFormInput` type. Three files, all in `src/`. No new files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md
@.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md

<!-- Source-of-truth files (executor MUST read before editing) -->
@src/components/suggestions/form/SuggestionForm.tsx
@src/hooks/useCreatePoll.ts
@src/lib/validation/suggestion-form.ts
@src/components/ui/checkbox.tsx
@src/components/ui/label.tsx

<!-- Edge Function contract (Phase 11 D-08 already accepts results_hidden) -->
@supabase/functions/create-poll/index.ts

<!-- UIDN-03 closure audit baseline -->
@.planning/closure/UIDN-03-shadcn-audit.md

<interfaces>
<!-- Current SuggestionFormInput (src/lib/validation/suggestion-form.ts): -->
<!--
export type SuggestionFormInput = {
  title: string
  description: string
  choices: string[]
  category_id: string | null
  image_url: string | null
  closes_at: string // ISO8601
}
-->
<!-- Add: results_hidden?: boolean — optional, defaults to false at the EF and DB column level. -->

<!-- Phase 11 create-poll EF accepts the field (lines 126-136 of supabase/functions/create-poll/index.ts): -->
<!--   const results_hidden = body.results_hidden === undefined ? null
<!--     : typeof body.results_hidden === 'boolean' ? body.results_hidden : null -->
<!--   if (results_hidden === null && body.results_hidden !== undefined) return 400 -->
<!-- So passing `undefined` (omitted) yields the DEFAULT false; passing `true` opts in. -->

<!-- TanStack Link import (analogous to other declarative nav in the codebase): -->
<!--   import { Link } from '@tanstack/react-router' -->
<!--   <Link to="/admin" className="..."><ChevronLeft />Back to admin</Link> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 12-02-01: Extend SuggestionFormInput + useCreatePoll for results_hidden pass-through</name>
  <files>src/lib/validation/suggestion-form.ts, src/hooks/useCreatePoll.ts</files>
  <read_first>
    - src/lib/validation/suggestion-form.ts (entire file — confirms the `SuggestionFormInput` type shape and the `validateSuggestionForm` function; the new field must slot in without breaking any existing validator branch)
    - src/hooks/useCreatePoll.ts (entire file — confirms the `createPoll` signature passes the input directly to `supabase.functions.invoke('create-poll', { body: input })`; no per-field marshalling)
    - supabase/functions/create-poll/index.ts lines 40-200 (Phase 11 D-08 — confirms the EF already accepts optional `results_hidden?: boolean`; passing `undefined` defaults to false at the DB level, passing `true` triggers the post-RPC update path; passing `false` explicitly is also valid and yields the same row state as omitting it)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § "Integration Points" (`useCreatePoll` extension passes the VIS-06 checkbox value through; Phase 11's create-poll EF (D-08) already accepts the field)
    - .planning/phases/11-schema-rls-ef-foundations/11-03b-PLAN.md (Phase 11 reference — confirms the live EF contract for `results_hidden` at create time)
  </read_first>
  <action>
    Step 1 — extend the input type. In `src/lib/validation/suggestion-form.ts`, add an optional field to `SuggestionFormInput`:

    `results_hidden?: boolean`

    Place it after `closes_at`. The trailing `?` makes it optional so existing call sites (smoke tests, fixtures, the existing edit-mode update path) do not need to change. The validator (`validateSuggestionForm`) does NOT need a new branch: a boolean field has no length/format check; the EF revalidates that `body.results_hidden` is `boolean` or `undefined` (Phase 11 D-08). If anything is needed in the validator at all, it's a single line that asserts the field is `boolean | undefined` when present — but TypeScript already enforces this at compile time, so a runtime check inside `validateSuggestionForm` is redundant. Leave the validator unchanged.

    Step 2 — pass-through in `useCreatePoll`. The current hook calls `supabase.functions.invoke<{ id: string }>('create-poll', { body: input })`. Since `input` is already typed as `SuggestionFormInput`, adding the optional field flows through with zero code change in `useCreatePoll.ts` — the field is part of `input` and the EF reads `body.results_hidden`. Verify this by reading the hook body: the spread `{ body: input }` carries every field on `input` to the EF. **No edit to `useCreatePoll.ts` is required** beyond a confirmation read; if a future reviewer questions it, the trace is: SuggestionForm sets `results_hidden` on the form state → caller passes the full form object to `createPoll(input)` → hook invokes EF with `body: input` → EF reads `body.results_hidden` (Phase 11 D-08 line 46). However, to make the contract visible to future readers, add a single comment line above the `supabase.functions.invoke` call: `// VIS-06: SuggestionFormInput.results_hidden (optional) flows through; Phase 11 D-08 create-poll EF accepts it.` This is WHY-only per project source-comment policy.

    Do NOT change the hook's return shape, the toast copy ("Suggestion created"), or any other behavior. Edit mode (`useUpdatePoll`) is out of scope for VIS-06 — the locked decision per CONTEXT.md is that admins flip the visibility via the Switch in the admin list (Plan 03), not via the edit form. The checkbox renders in edit mode (pre-populated from the loaded row) but the edit-mode submit path is governed by `useUpdatePoll`, which is unchanged in this plan.

    Step 3 — edit-mode update-poll pass-through. The Phase 12 scope per UI-SPEC.md "1. VIS-06 Visibility Checkbox" notes that in edit mode the checkbox pre-populates from the current row's `results_hidden`. If the admin checks/unchecks and re-saves, the `useUpdatePoll` path must also carry the field through. Inspect `src/hooks/useUpdatePoll.ts` (read it as part of `<read_first>` confirmation) — if its body signature mirrors `useCreatePoll` (passing `input` straight to `supabase.functions.invoke('update-poll', { body: input })`), the same zero-edit pattern applies. If `useUpdatePoll` shapes the body differently (e.g., picks specific fields), add `results_hidden: input.results_hidden` to the shaped object. Surface the shape in your edit notes; the most likely outcome is identical zero-edit pass-through.
  </action>
  <verify>
    <automated>grep -q 'results_hidden\??\s*:\s*boolean' src/lib/validation/suggestion-form.ts && npx tsc -b && npm run test -- --run src/lib/validation 2>/dev/null || npx tsc -b</automated>
  </verify>
  <done>
    `SuggestionFormInput` type contains optional `results_hidden?: boolean`; `useCreatePoll` (and `useUpdatePoll` if it shapes the body) pass the field through to the EF body; `tsc -b` passes; no existing validation test regresses.
  </done>
</task>

<task type="auto">
  <name>Task 12-02-02: Add VIS-06 Checkbox row to SuggestionForm + two UIDN-03 D-14 back-link replacements</name>
  <files>src/components/suggestions/form/SuggestionForm.tsx</files>
  <read_first>
    - src/components/suggestions/form/SuggestionForm.tsx (entire file — 238 lines; this task adds one new vertical section below CategoryPicker AND replaces two `<button onClick={() => navigate({ to: '/admin' })}>` sites at lines 140-145 and 163-168)
    - src/components/ui/checkbox.tsx (Wave 1 Plan 00 vendored primitive — confirms `Checkbox` exports `onCheckedChange` and accepts `id`, `checked`, `disabled`)
    - src/components/ui/label.tsx (existing primitive — confirms `htmlFor` association pattern)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "1. VIS-06 Visibility Checkbox" (locked layout, verbatim label + helper copy, min-height ≥ 44px touch target on the row wrapper)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-UI-SPEC.md § "4b. SuggestionForm.tsx:140 + :163" (TanStack Link replacement; preserve inline-flex span + ChevronLeft + "Back to admin" verbatim)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-17 (Checkbox placement below CategoryPicker; verbatim label and helper)
    - .planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-CONTEXT.md § D-14 (TanStack Link replacement; search-param disposition is Claude's discretion)
    - src/components/suggestions/form/CategoryPicker.tsx (read the file's render block to find where it ends in the parent form; the new Visibility section must slot immediately after it)
    - src/components/suggestions/form/TimerPicker.tsx (read the file's render block; same purpose — confirm where Visibility sits relative to TimerPicker)
  </read_first>
  <action>
    Two edits, applied in any order to the same file. Both should be in one commit.

    **Edit A — UIDN-03 D-14: replace two back-link `<button>` sites with TanStack `<Link>`.**

    Add to imports at the top: `import { Link } from '@tanstack/react-router'`. Keep the existing `useNavigate` import for now; remove it ONLY if the file no longer uses `navigate(...)` after both replacements (run `grep -n 'navigate(' src/components/suggestions/form/SuggestionForm.tsx` after the edits to confirm). The current file has two distinct `<button>` blocks — at line 140 (the `loadError` branch) and at line 163 (main render block); both render the same pattern: a `<button type="button" onClick={() => navigate({ to: '/admin' })} className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground mb-4"><ChevronLeft className="h-4 w-4 mr-1" /> Back to admin</button>`.

    Replace each with: `<Link to="/admin" className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground mb-4"><ChevronLeft className="h-4 w-4 mr-1" /> Back to admin</Link>`. The className stays verbatim — visual zero-delta. The hover behavior is the same because TanStack `<Link>` renders an `<a>` element which inherits the same Tailwind classes. The `type="button"` attribute is dropped (Link is an anchor, not a button — no form-submit risk since these buttons had no `type="submit"` and were not inside the form's onSubmit boundary in any case).

    Search-param disposition (D-14, Claude's discretion): the current `navigate({ to: '/admin' })` does not pass `search={{ tab: 'suggestions' }}`. The admin index route reads `tab` from URL search params and defaults to the suggestions tab when absent — verify by reading the route definition at `src/routes/admin/index.tsx` and the `AdminSuggestionsTab` `useSearch` call (it falls through to a default filter when `tab` is missing). **Decision for this plan: do NOT add `search={{ tab: 'suggestions' }}`** — the existing behavior routes correctly to the suggestions tab by default; adding the search param would be a behavior change beyond the UIDN-03 "zero-delta" mandate. If admins later report missing-context behavior (e.g., the active tab is not preserved on re-entry), it lands as a follow-up per CONTEXT.md `<deferred>`.

    After both edits, run `grep -n 'navigate(' src/components/suggestions/form/SuggestionForm.tsx`. If zero matches, remove the `useNavigate` import. If matches remain (e.g., a third `navigate(` call deep in the file that performs a non-back-link navigation), keep the import.

    **Edit B — VIS-06: add the Visibility Checkbox row below CategoryPicker.**

    Add to imports at the top: `import { Checkbox } from '@/components/ui/checkbox'`. Confirm `Label` is already imported (it is — line 7 of the current file).

    The Visibility row sits between the `<CategoryPicker ... />` render and the `<TimerPicker ... />` render — confirm exact placement by reading the existing form body; the locked placement per D-17 is "below CategoryPicker, own vertical row". Insert the following block (matching the existing `space-y-2` form-section rhythm and the `space-y-6` between-sections rhythm — confirm both via the surrounding code):

    A `<div className="space-y-2">` wrapper containing: (a) a row `<div className="flex items-start gap-2 min-h-[44px]">` holding `<Checkbox id="results-hidden" data-testid="visibility-checkbox" checked={resultsHidden} onCheckedChange={(v) => setResultsHidden(v === true)} disabled={locked} />` and `<Label htmlFor="results-hidden" className="text-sm font-medium cursor-pointer">Hide results from voters</Label>` (REQUIREMENTS-verbatim label); (b) below the row, `<p className="text-xs text-muted-foreground">Voters with responses will see a placeholder instead of counts. Toggle anytime from the admin list.</p>` (CONTEXT-verbatim helper).

    Add a new state hook near the existing `useState` declarations at the top of the component: `const [resultsHidden, setResultsHidden] = useState(false)`. Initialize from the loaded row in edit mode — `loadPoll()` already fetches the poll row; add `setResultsHidden(Boolean(row.results_hidden))` to the success branch of that function alongside the other `set*` calls (e.g., `setTitle(row.title)`, `setDescription(row.description ?? '')`). The locked-form behavior (per D-17 + UI-SPEC) auto-disables the Checkbox when `locked === true` (the existing computed boolean that disables Title, Description, etc. when the poll has votes); reuse it directly.

    On submit, include `results_hidden: resultsHidden` in the form input object passed to `createPoll(input)` or `updatePoll(input)`. The exact location depends on how the existing handler shapes `SuggestionFormInput` — read the existing submit handler and add the field alongside `title`, `description`, etc. The shadcn `Checkbox` returns `boolean | "indeterminate"` from `onCheckedChange`; coerce to boolean via `v === true` to keep `resultsHidden` strictly boolean (avoids the indeterminate state which is not used in this form).

    The `data-testid="visibility-checkbox"` attribute is locked by UI-SPEC.md "Test Surfaces" — Plan 06 TEST-13 targets this exact testid.
  </action>
  <verify>
    <automated>grep -q 'from "@tanstack/react-router".*Link\|Link.*from "@tanstack/react-router"\|Link.*from .@tanstack/react-router.' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'Hide results from voters' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'data-testid="visibility-checkbox"' src/components/suggestions/form/SuggestionForm.tsx && grep -q 'results-hidden' src/components/suggestions/form/SuggestionForm.tsx && ! grep -E '^\s*<button[^>]*navigate\({\s*to:\s*.\/admin' src/components/suggestions/form/SuggestionForm.tsx && npm run lint && npx tsc -b && npm run test -- --run src/__tests__/admin/polls-effective-invariant 2>/dev/null || npx tsc -b</automated>
  </verify>
  <done>
    SuggestionForm.tsx imports `Link` from `@tanstack/react-router` and `Checkbox` from `@/components/ui/checkbox`; both `<button onClick={() => navigate({ to: '/admin' })}>` sites are replaced with `<Link to="/admin">`; the visibility section renders below CategoryPicker with the exact label, helper text, and testid; edit mode pre-populates from the loaded row; `locked` disables the checkbox; the form submit passes `results_hidden: resultsHidden` into the form input; lint + tsc pass; polls-effective-invariant test still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → React form state | The Checkbox value lives in component state; no new trust boundary introduced beyond existing form fields. |
| admin browser → create-poll EF | Phase 11 already validates `results_hidden` as `boolean \| undefined` in the EF (line 126-135 of `create-poll/index.ts`); rejects other types with 400. Phase 12 passes the field through without intermediate transformation. |
| browser → TanStack Link → URL | Replacing `<button onClick={() => navigate(...)}>` with `<Link to="/admin">` actually NARROWS the trust surface — the Link uses TanStack's compile-time-typed `to` prop, which prevents arbitrary URL injection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | `results_hidden` form input via browser DevTools | accept | The Phase 11 create-poll EF validates `body.results_hidden` is `boolean \| undefined` (line 126-135); the worst a tampered request achieves is the same outcome as a legitimate `true`/`false` submission. RLS on `polls` writes is enforced via the EF's admin-gate (Phase 11). |
| T-12-02-02 | Spoofing | Admin role check for results_hidden at create | mitigate | Already mitigated by the create-poll EF's existing `requireAdmin` gate (Phase 11 D-08). Phase 12 does not bypass — it only sets one new field on the same request flow. |
| T-12-02-03 | Information Disclosure | Helper text "Voters with responses will see a placeholder instead of counts" | accept | This is admin-facing copy describing voter-side UI; no PII or system internals exposed. |
| T-12-02-04 | Repudiation | The create-time `results_hidden=true` path | accept | Phase 11 D-08 already writes a second audit row `action='results_hidden_set_at_create'` when the field is `true` at create time. Phase 12 leverages that existing audit trail; no new repudiation surface. |
| T-12-02-05 | Tampering | TanStack `<Link to="/admin">` replacing imperative `navigate` | mitigate | The compile-time-typed `to` prop on TanStack `<Link>` actually narrows the attack surface compared to `navigate({ to: '/admin' })` — there is no dynamic path concatenation. |
| T-12-02-06 | Denial of Service | New form field rendering cost | accept | One Radix Checkbox + Label adds ~1 KB gzip; visual rendering is constant-time. |

</threat_model>

<verification>
- SuggestionForm.tsx imports `Link` from `@tanstack/react-router` and `Checkbox` from `@/components/ui/checkbox`
- The two `<button>`s containing `navigate({ to: '/admin' })` are replaced with `<Link to="/admin">`
- The Visibility section is below CategoryPicker, with the exact REQUIREMENTS-verbatim label "Hide results from voters" and the locked helper copy
- `data-testid="visibility-checkbox"` is on the Checkbox element
- `SuggestionFormInput.results_hidden?: boolean` exists
- The form submit handler passes `results_hidden` into the input object passed to `createPoll` / `updatePoll`
- Edit-mode pre-populates the checkbox from the loaded row's `results_hidden`
- Locked-mode (vote_count > 0) disables the checkbox alongside other fields
- `npm run lint`, `tsc -b`, `npm run test` all pass
- Manual smoke: create a poll with the checkbox checked in dev → confirm a DB SELECT shows `polls.results_hidden = true`
</verification>

<success_criteria>
- VIS-06: Admin can opt in to hidden-from-creation via the checkbox; default unchecked preserves v1.0/v1.1 backwards compat
- UIDN-03: 2 of 4 sites closed (SuggestionForm.tsx:140 + :163 — Plan 01 closed :22 SearchBar; Plan 05 will close ImageInput.tsx:108)
- Zero behavioral regression in existing form submission paths (Yes/No preset, choice editor, image input, timer picker, category picker)
- TEST-13 (Plan 06) can target the Checkbox via `[data-testid="visibility-checkbox"]`
</success_criteria>

<output>
After completion, create `.planning/phases/12-admin-ui-user-ui-uidn-03-sweep/12-02-SUMMARY.md`
</output>
