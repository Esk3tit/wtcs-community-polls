---
phase: 17-planning-doc-ui-hygiene-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/01-foundation-authentication/01-VALIDATION.md
  - .planning/milestones/v1.0-phases/02-browsing-responding/02-VALIDATION.md
  - .planning/milestones/v1.0-phases/03-response-integrity/03-VALIDATION.md
  - .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-VALIDATION.md
  - .planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [DOCS-05, DOCS-06, DOCS-07, DOCS-08]

must_haves:
  truths:
    - "All four v1.0 phase archives (01, 02, 03, 04) have VALIDATION.md frontmatter reading status: complete and nyquist_compliant: true"
    - "Phase 03 VERIFICATION.md has status: retrospective and a 'Subsequent evolution' section naming Migration 14"
    - "All 15 pre-Phase-05 SUMMARY frontmatters are audit-confirmed: each requirements-completed list is either populated with sourced REQ-IDs or carries the confirmed-empty rationale (both empty cases — 02-04 and 04-05 — already carry it; DOCS-07 is audit-confirm, no edit required)"
    - "MILESTONES.md contains a complete v1.1 entry in full structural parity with the v1.2 entry, manually curated"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "v1.1 milestone retrospective entry"
      contains: "## v1.1"
    - path: ".planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md"
      provides: "Phase 03 retrospective verification with Subsequent evolution"
      contains: "## Subsequent evolution"
  key_links:
    - from: ".planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md"
      to: "Migration 14 (DBHY-01)"
      via: "Subsequent evolution section auth-path migration list"
      pattern: "Migration 14"
    - from: ".planning/MILESTONES.md"
      to: ".planning/milestones/v1.1-ROADMAP.md"
      via: "manually curated v1.1 entry sourced from the v1.1 ROADMAP archive"
      pattern: "## v1\\.1"
---

<objective>
Plan A — Doc hygiene sweep (DOCS-05/06/07/08). Markdown-only, NIL production risk. Four audit-then-reconcile deliverables across the v1.0 planning archives:

1. DOCS-05 — Audit + reconcile VALIDATION.md frontmatter on phases 01–04 to the canonical shape.
2. DOCS-06 — Reconcile Phase 03 VERIFICATION.md status and append a "Subsequent evolution" section naming Migration 14.
3. DOCS-07 — Audit-confirm `requirements-completed` across ALL 15 pre-Phase-05 SUMMARY frontmatters (this is an audit, not necessarily an edit).
4. DOCS-08 (HARD REQ) — Write the v1.1 MILESTONES.md entry in full structural parity with the v1.2 entry.

Purpose: Close the documentation-truth gaps surfaced by v1.0 RETROSPECTIVE Lesson 1 and the v1.2 retrospective, permanently closing the "v1.1 entry never written" gap.
Output: Up to 6 modified Markdown files (the four VALIDATION.md only if drift is found, the 03-VERIFICATION.md, and MILESTONES.md). DOCS-07 is expected to be audit-confirm with ZERO SUMMARY edits — see Task 1. Zero code, zero schema, zero test impact.

CRITICAL — every deliverable here has LIVE STATE that diverges from the original CONTEXT.md description. The VALIDATION frontmatters already read `complete`; the 03-VERIFICATION.md already exists with `status: resolved`; every SUMMARY frontmatter already declares requirements-completed (the two empty ones already carry their confirmed-empty rationale). Each task MUST read the live file FIRST and act on actual current state — reconcile drift, do NOT blind-rewrite. Note: the pre-Phase-05 archive (phases 01–04) holds 15 SUMMARY files, NOT 17 — the DOCS-07/phase-goal "all 17 SUMMARY" wording is a mis-count; audit the 15 that exist and do not chase a nonexistent 17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-planning-doc-ui-hygiene-sweep/17-CONTEXT.md
@.planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOCS-05 + DOCS-07 — Reconcile VALIDATION frontmatter (01–04) and audit-confirm SUMMARY requirements-completed</name>
  <files>.planning/milestones/v1.0-phases/01-foundation-authentication/01-VALIDATION.md, .planning/milestones/v1.0-phases/02-browsing-responding/02-VALIDATION.md, .planning/milestones/v1.0-phases/03-response-integrity/03-VALIDATION.md, .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/01-foundation-authentication/01-VALIDATION.md (current frontmatter — target shape source)
    - .planning/milestones/v1.0-phases/02-browsing-responding/02-VALIDATION.md (current frontmatter)
    - .planning/milestones/v1.0-phases/03-response-integrity/03-VALIDATION.md (current frontmatter)
    - .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-VALIDATION.md (current frontmatter)
    - .planning/milestones/v1.0-phases/02-browsing-responding/02-04-SUMMARY.md (requirements-completed: [] — ALREADY carries the confirmed-empty rationale comment "No v1.0 REQ-ID maps to this gap-closure plan per D-08 cross-check")
    - .planning/milestones/v1.0-phases/04-admin-panel-suggestion-management/04-05-SUMMARY.md (requirements-completed: [] — ALSO already carries the SAME confirmed-empty rationale comment)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md § DOCS-05 + § DOCS-07 (the state-drift findings)
  </read_first>
  <action>
    DOCS-05 — AUDIT, then reconcile only what drifts. The canonical target frontmatter shape for each VALIDATION.md is: status: complete, nyquist_compliant: true, plus the existing keys (phase, slug, wave_0_complete, created, last_updated). As of the live audit all four files already read status: complete + nyquist_compliant: true. For each of the four files: confirm both keys are present and correct. If a file already matches, leave it byte-identical — do NOT touch last_updated cosmetically. Only edit a file whose status or nyquist_compliant value drifts from the target. Record in the SUMMARY which of the four needed an edit and which were already correct.

    DOCS-07 — AUDIT-CONFIRM ONLY (expected to require ZERO SUMMARY edits). Audit requirements-completed across ALL 15 pre-Phase-05 SUMMARY frontmatters (phases 01–04 archives). The live audit shows: most are populated — 01-04 (AUTH-04/AUTH-05/TEST-02), 02-01 (CATG-02/03/04), 02-02 (VOTE-01/02/03 + RSLT-01..05 + INFR-04), 02-03 (TEST-03), 03-01 (AUTH-03/TEST-04), 03-02 (VOTE-04), 04-02 (ADMN/CATG/POLL/LIFE/TEST set), 04-04 (POLL/LIFE/TEST set). TWO SUMMARYs read requirements-completed: [] — 02-04-SUMMARY.md AND 04-05-SUMMARY.md — and BOTH already carry the confirmed-empty rationale comment ("No v1.0 REQ-ID maps to this gap-closure plan per D-08 cross-check; see 10-DOCS-03-DISCREPANCIES.md"). Both are gap-closure plans with no v1.0 REQ-ID, so the empty list is verified-correct, not an oversight.

    Therefore DOCS-07 is satisfied by AUDIT, not edit: walk all 15 SUMMARYs and confirm each requirements-completed is either populated with sourced REQ-IDs OR carries the confirmed-empty rationale. Expectation: every one of the 15 already passes — make NO edit to any SUMMARY. Do NOT invent REQ-IDs, do NOT rewrite already-correct frontmatters, do NOT re-add a rationale comment that is already present. ONLY if the audit surfaces a SUMMARY that is empty WITHOUT a rationale (none expected) would you backfill — and that SUMMARY would then need to be added to files_modified. Record the full 15-file audit result (populated / confirmed-empty) in this plan's SUMMARY, and record DOCS-07 as audit-confirmed.

    Note: phases 01–04 hold 15 SUMMARY files, not 17. Audit the 15 that exist; the "17" in the phase goal / DOCS-07 wording is a mis-count — do not chase a nonexistent 17.
  </action>
  <verify>
    <automated>for f in 01-foundation-authentication/01 02-browsing-responding/02 03-response-integrity/03 04-admin-panel-suggestion-management/04; do grep -qE '^status: complete' ".planning/milestones/v1.0-phases/${f}-VALIDATION.md" && grep -qE '^nyquist_compliant: true' ".planning/milestones/v1.0-phases/${f}-VALIDATION.md" || { echo "FAIL ${f}"; exit 1; }; done; n=$(find .planning/milestones/v1.0-phases -name '*-SUMMARY.md' | wc -l | tr -d ' '); empty_unconfirmed=0; for s in $(find .planning/milestones/v1.0-phases -name '*-SUMMARY.md'); do if grep -qE '^requirements-completed: \[\]' "$s"; then grep -q 'No v1.0 REQ-ID maps' "$s" || { echo "UNCONFIRMED EMPTY: $s"; empty_unconfirmed=1; }; fi; done; [ "$empty_unconfirmed" = 0 ] && echo "DOCS-07 AUDIT PASS ($n summaries; all empty lists confirmed)"</automated>
  </verify>
  <acceptance_criteria>
    - All four VALIDATION.md files contain a frontmatter line matching `^status: complete` and a line matching `^nyquist_compliant: true`.
    - Any VALIDATION.md that already matched the target shape is unchanged (no cosmetic edits, including no last_updated bump).
    - All 15 pre-Phase-05 SUMMARY frontmatters are audited: every requirements-completed list is either populated with sourced REQ-IDs or is `[]` accompanied by the confirmed-empty rationale comment. BOTH empty cases (02-04 and 04-05) already carry the rationale — no edit is made to either, nor to any other already-correct SUMMARY.
    - DOCS-07 is recorded as audit-confirmed (no SUMMARY edited). The plan SUMMARY contains the 15-file audit log (per file: populated REQ-IDs, or confirmed-empty).
    - No new REQ-ID is invented; no rationale comment is duplicated onto a SUMMARY that already has one.
    - The verify gate passes WITHOUT requiring any SUMMARY to be edited (the empty-but-confirmed cases pass the gate as-is).
  </acceptance_criteria>
  <done>DOCS-05 audited; the four VALIDATION frontmatters confirmed correct (or reconciled). DOCS-07 audit-confirmed across all 15 pre-Phase-05 SUMMARYs — every requirements-completed is populated or confirmed-empty (02-04 and 04-05 already carry their rationale); no SUMMARY edit was required.</done>
</task>

<task type="auto">
  <name>Task 2: DOCS-06 — Reconcile 03-VERIFICATION.md status and append Subsequent evolution section</name>
  <files>.planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md</files>
  <read_first>
    - .planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md (EXISTING file — frontmatter reads status: resolved, retroactive: true; sections: Executive Summary, Observable Truths, UAT Summary, Requirements Coverage, Plan-Level Verdicts, Deferred Items, Cross-Phase Integration)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md § DOCS-06 (state-drift finding: no Subsequent evolution section exists anywhere yet; status must reconcile resolved → retrospective)
    - .planning/ROADMAP.md § "Phase 14: Security-Definer Search-Path Migration" (Migration 14 / DBHY-01 facts: update_profile_after_auth + is_current_user_admin rewrite, CREATE OR REPLACE, SET search_path = '')
    - .planning/STATE.md § Decisions (Phase 14 entries — rls_auto_enable carve-out, 3-param overload drop, prod smoke PASS)
  </read_first>
  <action>
    This is an APPEND + status-reconcile on the EXISTING 03-VERIFICATION.md — NOT a greenfield write. Two edits:

    1. Frontmatter status reconcile: DOCS-06 spec requires status: retrospective. The live file reads status: resolved. Change the status value to retrospective. Preserve every other frontmatter key verbatim (verified, score, retroactive, retroactive_rationale, deferred_items). Do not delete the retroactive_rationale — it explains the backfill provenance and remains accurate.

    2. Add a new "## Subsequent evolution" section. Place it adjacent to the existing "## Cross-Phase Integration" section (its natural neighbor) — either immediately before or after it. The section documents how the Phase 03 auth/integrity path evolved AFTER Phase 03 shipped. Content: a chronological list of the Postgres migrations that touched the auth path post-Phase-03 — migrations 3 through 9 (the v1.0 schema build-out) and, as the MOST RECENT auth-path change, Migration 14 (DBHY-01). For Migration 14, name it explicitly and state what it changed: the SECURITY DEFINER search-path hardening that rewrote update_profile_after_auth and is_current_user_admin to SET search_path = '' with fully-qualified body references (source: Phase 14 archive). Frame it as "this VERIFICATION.md verifies Phase 03 as-shipped; the auth path it covers has since evolved through the following migrations, most recently Migration 14." Use prose + a list, matching the existing document's heading depth (## for the section, ### or list items beneath). WHY-only narrative — this is audit truth, not changelog padding.
  </action>
  <verify>
    <automated>grep -qE '^status: retrospective' .planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md && grep -q '## Subsequent evolution' .planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md && grep -q 'Migration 14' .planning/milestones/v1.0-phases/03-response-integrity/03-VERIFICATION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter line `status: retrospective` is present; `status: resolved` is gone.
    - All other pre-existing frontmatter keys (verified, score, retroactive, retroactive_rationale, deferred_items) are preserved unchanged.
    - The file contains a `## Subsequent evolution` heading.
    - The Subsequent evolution section contains the literal string `Migration 14` and describes it as the most recent auth-path change (DBHY-01, search_path hardening of update_profile_after_auth + is_current_user_admin).
    - The section references migrations 3–9 as the intervening auth-path changes.
    - The original verification sections (Executive Summary through Cross-Phase Integration) are intact.
  </acceptance_criteria>
  <done>03-VERIFICATION.md status reconciled to retrospective and a Subsequent evolution section naming Migration 14 appended, without disturbing the existing retroactive-closure content.</done>
</task>

<task type="auto">
  <name>Task 3: DOCS-08 (HARD REQ) — Write the v1.1 MILESTONES.md entry at full structural parity</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md (the v1.2 entry, lines 3–92, is the CANONICAL TEMPLATE; v1.0 entry begins line 94 — insert v1.1 between them)
    - .planning/milestones/v1.1-ROADMAP.md (PRIMARY source content: phases 7–10, their requirements, plans, shipped PRs, Key Decisions, Issues Resolved, Issues Deferred, Reconciliation History)
    - .planning/RETROSPECTIVE.md (v1.2 retrospective lesson driving manual-curation rule; v1.0 Lesson 1)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-CONTEXT.md § D-06, D-07, D-08 (full structural parity; retroactive graded outcomes; manual curation only)
    - .planning/phases/17-planning-doc-ui-hygiene-sweep/17-PATTERNS.md § DOCS-08 (section skeleton + outcome-grading style)
  </read_first>
  <action>
    MANUAL CURATION ONLY (D-08) — do NOT run gsd-sdk query milestone.complete or any CLI auto-extraction; it produces noisy stubs. Hand-write the entry using the v1.2 entry as the byte-structural template.

    Insert a new "## v1.1 — <name>" block between the v1.2 entry (ends ~line 92) and the v1.0 entry (begins line 94), preserving reverse-chronological order. The v1.1 milestone is "Hygiene & Polish" (phases 7–10), shipped 2026-05-11, tag v1.1.

    Reproduce EVERY section the v1.2 entry has (D-06 full parity). Required sections in order:
    - Header block: **Shipped:** 2026-05-11 / **Phases:** 7 → 10 (4 phases, 16 plans — confirm counts from v1.1-ROADMAP + ROADMAP progress table) / **Tag:** `v1.1` / **Production URL:** https://polls.wtcsmapban.com / **Known deferred items at close:** (count + pointer).
    - ### Delivered
    - ### Key Accomplishments (numbered, one per phase 7/8/9/10 — Observability Hardening, E2E Test Hygiene, UI Closure Evidence, Planning Hygiene Backfill)
    - ### Stats (| Metric | Value | table)
    - ### Decimal Phases (write "None for v1.1 — no decimal phases were inserted" if empty, per D-06 — do NOT drop the section)
    - ### Key Decisions (with outcomes) — backfill RETROACTIVE ✓/⚠️ graded verdicts (D-07) using hindsight from phases 12–16. Source the decisions from v1.1-ROADMAP § Key Decisions. Grade whether they held up — e.g. did the Phase 9 UIDN-03 button sweep / E2E locator scoping survive into v1.2/v1.3? Use the verdict-column form from the v1.2 entry: "✓ Good — <rationale>" / "⚠️ Revisit — <follow-up + trigger>".
    - ### Issues Resolved During Milestone (from v1.1-ROADMAP § Issues Resolved)
    - ### Known Gaps Carried Forward
    - ### Known Tech Debt
    - ### Issues Deferred to v1.2+ (from v1.1-ROADMAP § Issues Deferred)
    - Footer: *ROADMAP archive: [...]* / *REQUIREMENTS archive: [...]* pointing at the v1.1 archive paths.

    Omit a section ONLY if genuinely empty, and when omitting state it explicitly ("None for v1.1 — ...") rather than dropping the heading (D-06). Match the v1.2 entry's heading depth, table formatting, and trailing `---` separator exactly.
  </action>
  <verify>
    <automated>grep -q '^## v1.1' .planning/MILESTONES.md && grep -q '### Key Decisions (with outcomes)' .planning/MILESTONES.md && grep -q '### Key Accomplishments' .planning/MILESTONES.md && grep -q '### Stats' .planning/MILESTONES.md && grep -q '### Decimal Phases' .planning/MILESTONES.md && awk '/^## v1.2/{a=NR} /^## v1.1/{b=NR} /^## v1.0/{c=NR} END{exit !(a<b && b<c)}' .planning/MILESTONES.md && echo "PARITY+ORDER PASS"</automated>
  </verify>
  <acceptance_criteria>
    - MILESTONES.md contains a `## v1.1` heading positioned strictly between `## v1.2` and `## v1.0` (reverse-chronological order preserved).
    - The v1.1 entry contains all nine section headings present in the v1.2 entry: Delivered, Key Accomplishments, Stats, Decimal Phases, Key Decisions (with outcomes), Issues Resolved During Milestone, Known Gaps Carried Forward, Known Tech Debt, Issues Deferred.
    - The Key Decisions table uses ✓/⚠️ graded verdicts with hindsight rationale (not raw decision restatements).
    - Any empty section is present with an explicit "None for v1.1 — ..." note rather than omitted.
    - Header block declares Shipped 2026-05-11, Phases 7 → 10, Tag v1.1, with plan/phase counts consistent with the ROADMAP milestone table (v1.1 = phases 7–10, 16 plans).
    - Entry is hand-written prose/tables — no auto-extraction stub artifacts (no orphaned placeholder tokens like TBD/`<...>`).
  </acceptance_criteria>
  <done>A complete, manually-curated v1.1 MILESTONES.md entry at full structural parity with the v1.2 entry exists in reverse-chronological position, with retroactively-graded Key Decision outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies only Markdown files under `.planning/` (planning archive + milestone log). No code, no schema, no Edge Functions, no auth paths, no runtime data flows, no dependency installs are touched. There is no client→API boundary, no untrusted input, and no production surface in scope.

| Boundary | Description |
|----------|-------------|
| (none) | No trust boundary is crossed — additive/audit edits to local planning Markdown only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17A-01 | Tampering | `.planning/` Markdown archive integrity | accept | NIL production risk — files are developer-facing planning docs, not runtime artifacts; git history is the integrity control; reviewer + user OK gate (D-12) catches accidental content drift before merge |
| T-17A-02 | Information Disclosure | Milestone/verification docs | accept | Docs contain only project-internal planning data already committed to the repo; no secrets, PII, or credentials introduced |

**No new attack surface is introduced by this plan.** Per the ROADMAP Phase 17 risk classification (NIL), the doc-hygiene workstream adds no executable code, no network calls, and no package installs. No package legitimacy gate applies (zero installs). The two threats above are enumerated only to honor the security-enforcement requirement and are dispositioned `accept` with rationale.
</threat_model>

<verification>
- All four VALIDATION.md frontmatters read `status: complete` + `nyquist_compliant: true` (grep gate in Task 1).
- DOCS-07 audit-confirm: all 15 pre-Phase-05 SUMMARYs have requirements-completed populated or confirmed-empty (02-04 and 04-05 already carry their rationale); the Task 1 gate passes with no SUMMARY edit.
- 03-VERIFICATION.md reads `status: retrospective` and contains `## Subsequent evolution` + `Migration 14` (grep gate in Task 2).
- MILESTONES.md contains a `## v1.1` entry between v1.2 and v1.0 with all nine parity sections (grep + order gate in Task 3).
- No files outside `files_modified` are touched. `git status` shows only the listed Markdown paths (and no SUMMARY edits, since DOCS-07 is audit-confirm).
</verification>

<success_criteria>
- DOCS-05: phases 01–04 VALIDATION.md confirmed/reconciled to `status: complete` + `nyquist_compliant: true`.
- DOCS-06: 03-VERIFICATION.md status reconciled to `retrospective` with a Subsequent evolution section naming Migration 14.
- DOCS-07: all 15 pre-Phase-05 SUMMARY requirements-completed audit-confirmed; both empty cases (02-04, 04-05) already carry the confirmed-empty rationale — no SUMMARY edit required.
- DOCS-08: v1.1 MILESTONES.md entry written at full structural parity with graded outcomes.
- Markdown-only diff; no code/test/schema files changed.
</success_criteria>

<output>
Create `.planning/phases/17-planning-doc-ui-hygiene-sweep/17-01-SUMMARY.md` when done. Record the DOCS-05 per-file audit result (edited vs already-correct), the DOCS-07 15-file audit log (per file: populated REQ-IDs or confirmed-empty — noting that both 02-04 and 04-05 were already confirmed-empty and DOCS-07 was satisfied by audit with no edit), and confirmation the v1.1 entry sections match the v1.2 template.
</output>
