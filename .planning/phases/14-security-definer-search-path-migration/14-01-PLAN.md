---
phase: 14-security-definer-search-path-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00000000000014_harden_security_definer_search_path.sql
  - .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md
  - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md
  - .planning/phases/14-security-definer-search-path-migration/evidence/
  - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql
  - tests/sql/is_current_user_admin_regression.sql
autonomous: false
requirements:
  - DBHY-01
  - DBHY-02
  - DBHY-03
  - DBHY-04

must_haves:
  truths:
    - "`supabase db lint --linked` against production shows zero `0011_function_search_path_mutable` WARNs for all user-owned target functions (with explicit DBHY-02 carve-out for `rls_auto_enable` ONLY if W0 proves system-owned and the carve-out is recorded in the W0 checkpoint output)"
    - "One production `submit-vote` round-trip succeeds — vote row inserted, `increment_vote_count` trigger fires, `vote_counts` row incremented"
    - "TEST-11 12-cell RLS matrix (`e2e/integration/vote-counts-rls.test.ts`) passes with zero regressions post-migration (exit code 0, NOT a PASS/pass string grep)"
    - "Direct `is_current_user_admin()` regression: `tests/sql/is_current_user_admin_regression.sql` passes against local stack — admin fixture returns TRUE, non-admin returns FALSE, mfa_verified=false returns FALSE, guild_member=false returns FALSE; at least one admin-gated RLS table beyond `vote_counts` (e.g., `audit_log`) is exercised"
    - "Catalog assertion: every target function has the expected `prosecdef=true`, `provolatile`=`s` (for is_current_user_admin) or `v` (default for others), `proconfig` contains `search_path=`, and only ONE entry per function name (no accidental overloads)"
    - "Machine-enforced `pg_get_functiondef` diff: post-deploy production functiondef for every target function differs from the pre-deploy snapshot ONLY in the `SET search_path` value line (after normalization); `diff -u` returns the expected single-line change for each function"
    - "Extension function audit: no bare `gen_random_uuid(`, `uuid_generate_v4(`, or unqualified `citext` cast appears in Migration 14 (must be `extensions.<fn>` if used)"
    - "Emergency restore SQL pre-staged: `evidence/pre-deploy-functiondef-snapshot.sql` captured BEFORE `supabase db push`, containing current production `pg_get_functiondef` for every target function — usable as rollback artifact via service-role if Studio admin path is locked out"
    - "Migration 14 SQL file exists with `CREATE OR REPLACE FUNCTION … SECURITY DEFINER … SET search_path = ''` for every target function and `supabase db reset --local` applies all migrations cleanly"
    - "`11-PATTERNS.md` line 83 no longer implies the admin-OR bypass form may be current; REVIEW-FIX-H3 is named as the decision"
  artifacts:
    - path: "supabase/migrations/00000000000014_harden_security_definer_search_path.sql"
      provides: "search_path hardening for 6 (or 7) pre-Phase-11 SECURITY DEFINER functions"
      contains: "CREATE OR REPLACE FUNCTION"
    - path: ".planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md"
      provides: "corrected vote_counts policy prose (DBHY-04)"
      contains: "REVIEW-FIX-H3"
    - path: ".planning/phases/14-security-definer-search-path-migration/evidence/"
      provides: "pre/post-deploy lint output + Studio screenshots + TEST-11 pass output + direct is_current_user_admin regression output + catalog assertion output + functiondef diff output"
    - path: ".planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql"
      provides: "emergency rollback artifact — current production pg_get_functiondef for every target function, captured BEFORE supabase db push; usable via service-role bypass if admin Studio path locked out"
      contains: "CREATE OR REPLACE FUNCTION public."
    - path: "tests/sql/is_current_user_admin_regression.sql"
      provides: "direct regression test for is_current_user_admin() with admin / non-admin / mfa_verified=false / guild_member=false fixtures; tests at least one admin-gated RLS table beyond vote_counts (audit_log)"
      contains: "is_current_user_admin"
  key_links:
    - from: "supabase/migrations/00000000000014_harden_security_definer_search_path.sql"
      to: "public.is_current_user_admin()"
      via: "CREATE OR REPLACE FUNCTION — only SET search_path value changes"
      pattern: "SET search_path = ''"
    - from: "supabase/migrations/00000000000014_harden_security_definer_search_path.sql"
      to: "public.increment_vote_count()"
      via: "CREATE OR REPLACE FUNCTION — body verbatim from migration 2; no body change"
      pattern: "INSERT INTO public.vote_counts"
    - from: "e2e/integration/vote-counts-rls.test.ts"
      to: "public.is_current_user_admin()"
      via: "Postgres RLS policy evaluated by supabase-js SELECT"
      pattern: "vote-counts-rls"

user_setup:
  - service: supabase-production
    why: "Production push (supabase db push) and post-deploy lint require authenticated CLI session"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"
    dashboard_config:
      - task: "Verify rls_auto_enable ownership (Wave 0)"
        location: "Supabase Dashboard → Database → Functions → filter by rls_auto_enable"
      - task: "Post-deploy evidence screenshot"
        location: "Supabase Dashboard → Database → Advisors → Security (confirm zero 0011 WARNs)"
      - task: "Post-deploy smoke vote evidence"
        location: "Supabase Dashboard → Table Editor → public.votes and public.vote_counts"
---

<objective>
Harden all pre-Phase-11 SECURITY DEFINER Postgres functions by adding `SET search_path = ''` and confirming body references are fully schema-qualified. Clear all `0011_function_search_path_mutable` advisor WARNs from production. Fix one prose drift in `11-PATTERNS.md` (DBHY-04).

Purpose: The Supabase security advisor flags 7 functions created before the project standardised on `SET search_path = ''`. An attacker or misconfigured session with write access to `public` schema could shadow a builtin and redirect these functions' behavior. Clearing the WARNs also removes noise from the advisor dashboard and demonstrates security hygiene for v1.3.

Output: `supabase/migrations/00000000000014_harden_security_definer_search_path.sql` (6 or 7 functions), corrected `11-PATTERNS.md` prose at line 83, `tests/sql/is_current_user_admin_regression.sql` fixture, evidence folder with pre/post-deploy lint output + pre/post-deploy `pg_get_functiondef` snapshots + machine-enforced functiondef diffs + catalog assertions + TEST-11 exit-code output + is_current_user_admin regression output + Studio/advisor screenshots.

**Review-incorporation summary (REVIEWS.md → this PLAN):** This plan was revised via two cycles of `/gsd:plan-phase 14 --reviews` to address cross-AI review feedback (Gemini + Codex). Key hardenings:

**Cycle 1 hardenings (carried forward):**
- **Codex HIGH — TEST-11 inadequacy:** Added Task 07b — direct `is_current_user_admin()` regression on `audit_log` with 4 identity branches.
- **Codex HIGH — rls_auto_enable / DBHY-02 ambiguity:** W0 Check 1 forces an R1/R2/R3 outcome decision; R2 requires a separate REQUIREMENTS.md amendment commit before Task 01.
- **Gemini/Codex MEDIUM — rollback robustness:** W0 Check 3 pre-stages `evidence/pre-deploy-functiondef-snapshot.sql` (service-role usable) before push; Task 08a verifies it is committed.
- **Codex MEDIUM — machine-enforced functiondef diff:** Tasks 02-05 (local) and 08b (production) capture functiondef snapshots and run `diff -u` with `SET search_path` line normalization.
- **Codex MEDIUM — catalog assertions:** Tasks 02-05 and 08b query `prosecdef`/`provolatile`/`proconfig`/`proowner`/`proacl` for every target function, catching missing SECURITY DEFINER, stripped STABLE, wrong owner, and accidental overloads in one query.
- **Gemini MEDIUM — extension function audit:** W0 Check 2 greps for bare `gen_random_uuid`/`uuid_generate_v4`/`citext` and bare `uid()` in source bodies before Task 01.
- **Codex LOW — exit-code-based TEST-11:** Task 07a switched from `grep -i 'PASS\|pass'` to direct `npx vitest run … --reporter=verbose` with PIPESTATUS check.

**Cycle 2 hardenings (this revision):**
- **Codex HIGH H1 — Task 07b fixture broken (FK + wrong columns + invalid PERFORM):** Rewrote the fixture to (a) seed `auth.users` rows BEFORE `public.profiles` rows (FK from `profiles.id` to `auth.users(id)` is now satisfied), (b) use the actual `audit_log` columns `before` / `after` (NOT `payload`), (c) move every `PERFORM set_config(...)` inside a `DO $$ ... $$` block (`PERFORM` is PL/pgSQL only; invalid at SQL script top level). Task 07b's `<verify>` block now greps for `INSERT INTO auth.users`, `before, after`, absence of `payload`, and absence of top-level `^PERFORM`.
- **Codex HIGH H3 / Gemini MEDIUM M1 — Task 07b RLS bypassed under privileged psql session:** Fixture now wraps every RLS-asserting `SELECT` in `SET LOCAL ROLE authenticated; ... RESET ROLE;` so the role is actually `authenticated` (not the privileged psql connection role that bypasses RLS). Setup INSERTs run as the privileged role; only reads switch to `authenticated`. `<verify>` block requires `grep -c "SET LOCAL ROLE authenticated" tests/sql/is_current_user_admin_regression.sql >= 3`.
- **Codex HIGH H4 — `update_profile_after_auth` overload handling (CREATE OR REPLACE does not replace across signatures):** Added W0 Check 1B — catalog query enumerating ALL `update_profile_after_auth` overloads by `pg_get_function_identity_arguments`. Decision tree U1/U2/U3: U1 (only 4-param) ⇒ harden 4-param only; U2 (both 3- and 4-param) ⇒ DROP the stale 3-param OR harden both (Task 01 explicitly handles both paths); U3 (only 3-param) ⇒ STOP. Task 01 updated to honor U2 with `DROP FUNCTION IF EXISTS public.update_profile_after_auth(BOOLEAN, TEXT, TEXT);` or an additional 3-param `CREATE OR REPLACE` block.
- **Codex MEDIUM M3 — W0 Query A missing `pg_get_functiondef`:** Added `pg_get_functiondef(p.oid)` to Query A's SELECT list so the R1 outcome's "copy body from Query A" instruction is executable.
- **Codex LOW L1 — R2/R3 overlap for extension-owned:** Clarified that `owning_extension` non-NULL is unambiguously R2; R3 reserved for ambiguous user-writable cases requiring Supabase support.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-security-definer-search-path-migration/14-CONTEXT.md
@.planning/phases/14-security-definer-search-path-migration/14-RESEARCH.md
@.planning/phases/14-security-definer-search-path-migration/14-PATTERNS.md
@.planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md

<interfaces>
<!-- Key migration bodies the executor needs. Extracted from prior research. No codebase exploration needed. -->

From supabase/migrations/00000000000009_admin_integrity_rls.sql (authoritative is_current_user_admin):
```sql
CREATE OR REPLACE FUNCTION public.is_current_user_admin()
RETURNS BOOLEAN
LANGUAGE sql
SECURITY DEFINER
STABLE
SET search_path = public   -- Migration 14: change ONLY this value to ''
AS $$
  SELECT COALESCE(
    (SELECT is_admin AND mfa_verified AND guild_member
     FROM public.profiles WHERE id = auth.uid()),
    false
  );
$$;
```

From supabase/migrations/00000000000002_triggers.sql lines 172-188 (authoritative increment_vote_count):
-- CRITICAL: INSERT INTO public.vote_counts is ALREADY fully qualified.
-- CONTEXT.md claim of bare reference is INCORRECT per RESEARCH.md.
-- Migration 14 adds ONLY SET search_path = ''. No body change.
```sql
CREATE OR REPLACE FUNCTION public.increment_vote_count()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.vote_counts (poll_id, choice_id, count)
  VALUES (NEW.poll_id, NEW.choice_id, 1)
  ON CONFLICT (poll_id, choice_id) DO UPDATE
  SET count = public.vote_counts.count + 1;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

From supabase/migrations/00000000000003_guild_membership.sql lines 41-62 (authoritative update_profile_after_auth — 4-param signature):
-- CRITICAL: Use 4-param signature. 3-param would create a new overload and clear zero WARNs.
```sql
CREATE OR REPLACE FUNCTION public.update_profile_after_auth(
  p_mfa_verified BOOLEAN,
  p_discord_username TEXT,
  p_avatar_url TEXT,
  p_guild_member BOOLEAN DEFAULT FALSE
)
RETURNS void AS $$
BEGIN
  UPDATE public.profiles
  SET
    mfa_verified = p_mfa_verified,
    discord_username = p_discord_username,
    avatar_url = p_avatar_url,
    guild_member = p_guild_member,
    updated_at = NOW()
  WHERE id = auth.uid();
  IF NOT FOUND THEN
    RAISE EXCEPTION 'Profile not found for current user';
  END IF;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

From 14-PATTERNS.md — 11-PATTERNS.md DBHY-04 drift location:
Line 83 current (drift): "...and the v1.0.5 admin-bypass policy in `migrations/00000000000005_admin_phase4.sql:75-88` (whichever is current)..."
Line 83 replacement: "...and the v1.0.5 admin-bypass policy at `migrations/00000000000005_admin_phase4.sql:75-88` (both superseded). The shipped policy (migration 10, REVIEW-FIX-H3) has NO `is_current_user_admin()` OR-bypass..."
Lines 85-101 SQL block: CORRECT as-is — DO NOT MODIFY.
</interfaces>
</context>

<tasks>

<!-- ================================================================== -->
<!-- WAVE 0: Pre-flight — rls_auto_enable ownership + local stack check  -->
<!-- ================================================================== -->

<task type="checkpoint:human-verify" id="14-01-W0" gate="blocking">
  <name>Task W0 [BLOCKING]: Pre-flight gate — rls_auto_enable resolution, DBHY-02 carve-out decision, extension-function audit, pre-deploy snapshot, local stack reachability</name>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-RESEARCH.md (Open Questions — rls_auto_enable; Common Pitfalls — Pitfall 2)
    - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/14-security-definer-search-path-migration/14-REVIEWS.md (HIGH concerns: TEST-11 inadequacy, rls_auto_enable/DBHY-02 ambiguity, rollback robustness)
    - .planning/REQUIREMENTS.md (DBHY-02 acceptance criterion exact wording — must reconcile against W0 outcome)
    - supabase/migrations/00000000000002_triggers.sql (lines 86-188 — bodies to grep for extension functions)
    - supabase/migrations/00000000000003_guild_membership.sql (lines 41-62 — update_profile_after_auth body)
    - supabase/migrations/00000000000004_fix_trigger_rpc_context.sql (lines 12-45 — profile_self_update_allowed body)
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (lines 11-26 — is_current_user_admin body)
    - supabase/config.toml (to confirm linked project ref and local stack config)
  </read_first>
  <what-built>
    Five blocking pre-flight verifications that must ALL resolve before Migration 14 is written. This gate addresses the HIGH-severity concerns raised in cross-AI review (rls_auto_enable / DBHY-02 ambiguity; rollback robustness) plus MEDIUM-severity concerns (extension function qualification; narrow W0 schema query).

    ---
    **CHECK 1 — `rls_auto_enable` ownership resolution (HIGH; addresses DBHY-02 carve-out)**

    Run BOTH queries below in Supabase Studio SQL editor against the linked production project (not local). The dual query captures schema, owner, identity-args, ACL, extension dependency, AND advisor output context — wider than the original W0 schema-only check.

    Query A — full catalog inspection across ALL schemas (not just public), with `pg_get_functiondef` so R1 has the executable body to copy:
      SELECT
        n.nspname                              AS schema,
        p.proname                              AS function_name,
        pg_get_function_identity_arguments(p.oid) AS identity_args,
        p.proowner::regrole                    AS owner,
        p.proacl                               AS acl,
        p.prosecdef                            AS is_security_definer,
        p.proconfig                            AS config,
        e.extname                              AS owning_extension,
        pg_get_functiondef(p.oid)              AS functiondef
      FROM pg_proc p
      JOIN pg_namespace n ON n.oid = p.pronamespace
      LEFT JOIN pg_depend d
        ON d.objid = p.oid
        AND d.deptype = 'e'
      LEFT JOIN pg_extension e
        ON e.oid = d.refobjid
      WHERE p.proname = 'rls_auto_enable';

    Query B — current advisor finding for rls_auto_enable (must capture verbatim):
      npx supabase db lint --linked 2>&1 | grep -B 2 -A 5 rls_auto_enable

    Interpret the results into ONE of three OUTCOMES (must pick exactly one before proceeding):

      OUTCOME R1 — User-owned in `public` (or any user-writable schema):
        Migration 14 includes `rls_auto_enable` as Function 7. DBHY-02 stands literally — zero WARNs expected.
        Action: Include the user-owned function in Task 01 with body copied from Query A's `pg_get_functiondef(p.oid)` output.

      OUTCOME R2 — Extension-owned OR system-owned (any of: `owning_extension` is non-NULL ⇒ unambiguously extension-owned ⇒ R2; OR `proowner` is `supabase_admin` / `postgres` / a non-user role; OR schema is non-public; OR function does not appear in pg_proc at all from a user role's perspective):
        Migration 14 targets 6 functions. DBHY-02 must be formally amended to allow ONE residual `0011_function_search_path_mutable` WARN for `rls_auto_enable` ONLY. Update `.planning/REQUIREMENTS.md` DBHY-02 acceptance criterion to read: "Zero `0011_function_search_path_mutable` WARNs for all user-owned target functions; one residual WARN for `rls_auto_enable` is permitted if and only if it is extension-owned or system-owned per Phase 14 W0 finding." Add a one-line changelog entry: "Updated 2026-05-17 (Phase 14, W0 finding) — explicit carve-out for system-owned `rls_auto_enable`."
        Commit this REQUIREMENTS.md amendment as a SEPARATE commit BEFORE Task 01 begins: `docs(req): amend DBHY-02 with rls_auto_enable carve-out (W0 finding)`. This makes the carve-out auditable.
        NOTE: Extension-owned (owning_extension non-NULL) is unambiguously R2 — do not branch to R3 in that case (per Codex LOW concern on R2/R3 overlap).

      OUTCOME R3 — Ambiguous case ONLY: function appears in `public` with a user-writable owner BUT with admin-only ACL or other non-determinable state; Supabase support guidance required to disambiguate. Extension-owned is always R2, not R3.
        Stop. Open a Supabase support ticket or escalate to project owner. Do NOT proceed to Task 01 until OUTCOME R1 or R2 can be unambiguously declared.

    ---
    **CHECK 1B — `update_profile_after_auth` overload enumeration (HIGH; addresses Codex HIGH H4-cycle2)**

    `CREATE OR REPLACE FUNCTION` only replaces an EXACT signature match — it does NOT drop or replace overloads. Migration 2 originally defined a 3-param `update_profile_after_auth(p_mfa_verified BOOLEAN, p_discord_username TEXT, p_avatar_url TEXT)`; migration 3 added a 4-param signature `update_profile_after_auth(p_mfa_verified, p_discord_username, p_avatar_url, p_guild_member BOOLEAN DEFAULT FALSE)`. If the 3-param SECURITY DEFINER overload is still present in production, Migration 14 hardens only the 4-param variant and the 3-param overload remains unhardened → residual `0011_function_search_path_mutable` WARN against `rls_auto_enable`-style baseline → DBHY-02 not met.

    Run this catalog query against the linked production project (and again locally after `supabase db reset --local`):
      SELECT
        n.nspname                                  AS schema,
        p.proname                                  AS function_name,
        pg_get_function_identity_arguments(p.oid)  AS identity_args,
        p.pronargs                                 AS num_args,
        p.prosecdef                                AS is_security_definer,
        p.proconfig                                AS config,
        p.proowner::regrole                        AS owner,
        pg_get_functiondef(p.oid)                  AS functiondef
      FROM pg_proc p
      JOIN pg_namespace n ON n.oid = p.pronamespace
      WHERE n.nspname = 'public'
        AND p.proname = 'update_profile_after_auth'
      ORDER BY p.pronargs;

    Interpret into ONE of three OUTCOMES (must pick exactly one before proceeding):

      OUTCOME U1 — Only the 4-param overload exists (`num_args` = 4, `identity_args` contains `p_guild_member`):
        Migration 14 hardens the 4-param signature only. No additional action required. This is the expected case if production migrations include migration 3.

      OUTCOME U2 — Both 3-param AND 4-param overloads exist:
        Migration 14 MUST harden BOTH overloads OR drop the stale 3-param overload. Recommended: DROP the stale 3-param overload, because the app calls the 4-param RPC (per migration 3 lines 41-62). Add this DROP statement to Migration 14 BEFORE the 4-param `CREATE OR REPLACE FUNCTION` block:
          DROP FUNCTION IF EXISTS public.update_profile_after_auth(BOOLEAN, TEXT, TEXT);
        Alternative (if app still has callers on the 3-param form): add a SECOND `CREATE OR REPLACE FUNCTION public.update_profile_after_auth(p_mfa_verified BOOLEAN, p_discord_username TEXT, p_avatar_url TEXT) ... SET search_path = ''` block to Migration 14 with the migration-2 body. Confirm via `git grep -n "update_profile_after_auth" src/` whether any caller uses 3-param invocation.

      OUTCOME U3 — Only the 3-param overload exists (`num_args` = 3, no `p_guild_member` parameter):
        Production migrations are not at migration 3+. This is an environment mismatch — STOP. Investigate why production is behind. Do NOT proceed until U1 or U2 can be declared.

    Document the outcome (U1, U2, or U3) and the executable action in the resume-signal output below.

    ---
    **CHECK 2 — Extension-function body audit (MEDIUM; addresses Gemini concern)**

    Under `search_path = ''`, calls to `gen_random_uuid()`, `uuid_generate_v4()`, and `citext` casts must be qualified as `extensions.<fn>`. Bare references will fail at runtime with "function does not exist" errors.

    Grep all source migration bodies for the target functions before writing Migration 14:
      grep -nE 'gen_random_uuid|uuid_generate_v4|::citext|citext\(' \
        supabase/migrations/00000000000002_triggers.sql \
        supabase/migrations/00000000000003_guild_membership.sql \
        supabase/migrations/00000000000004_fix_trigger_rpc_context.sql \
        supabase/migrations/00000000000009_admin_integrity_rls.sql

    Expected: zero matches (the 6 target functions do not use extension functions). If matches found, document each occurrence and the corresponding qualification (`extensions.gen_random_uuid()` etc.) in the W0 checkpoint output — Task 01 MUST include the `extensions.` prefix in those bodies.

    Also grep for bare `auth.uid()` regressions (LOW per Gemini): confirm authoritative source bodies use the `auth.` qualifier; not bare `uid()`:
      grep -nE '(^|[^.])uid\(\)' \
        supabase/migrations/00000000000003_guild_membership.sql \
        supabase/migrations/00000000000004_fix_trigger_rpc_context.sql \
        supabase/migrations/00000000000009_admin_integrity_rls.sql

    Expected: zero matches (the source bodies use `auth.uid()` not bare `uid()`). If a bare match is found, flag it — Task 01 must use the `auth.` qualifier.

    ---
    **CHECK 3 — Pre-deploy emergency restore SQL snapshot (MEDIUM; addresses rollback robustness)**

    Both reviewers (Gemini, Codex) raised that "fix-forward via Studio" assumes the developer retains admin RLS access. If `is_current_user_admin()` is broken by Migration 14, the developer may be locked out of the Studio admin path. Pre-stage a service-role-usable rollback artifact NOW, before any production change.

      mkdir -p .planning/phases/14-security-definer-search-path-migration/evidence
      psql "$PROD_DB_URL_OR_LINKED_STUDIO_CONNECTION" -At -c "
        SELECT pg_get_functiondef(p.oid) || ';'
        FROM pg_proc p
        JOIN pg_namespace n ON n.oid = p.pronamespace
        WHERE n.nspname = 'public'
          AND p.proname IN (
            'is_current_user_admin',
            'increment_vote_count',
            'update_profile_after_auth',
            'handle_new_user',
            'validate_vote_choice',
            'profile_self_update_allowed'
          )
        ORDER BY p.proname;
      " > .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql

    If the production connection string is not available locally, this query can be run in Supabase Studio SQL editor and the result pasted into the file. The artifact MUST be present and committed before Task 08a (`supabase db push`).

    If OUTCOME R1 was selected in Check 1, include `rls_auto_enable` in the IN-list. If R2, omit it (system-owned bodies are not user-rewritable anyway).

    ---
    **CHECK 4 — Local stack reachability**

    Confirm `supabase db reset --local` applies migrations 0–13 cleanly before Migration 14 is written:
      npx supabase db reset --local
    Exit 0 = local stack healthy. If this fails, fix the stack before Wave 1.

    ---
    **CHECK 5 — Test fixture directories**

    Confirm fixture directories exist:
      mkdir -p tests/sql
      mkdir -p .planning/phases/14-security-definer-search-path-migration/evidence

    Smoke fixture file `tests/sql/increment_vote_count_smoke.sql` is a reference artifact for Task 01's grep assertion. It does not need to be runnable end-to-end in local Postgres without seeded data.

    Also note: a new regression fixture `tests/sql/is_current_user_admin_regression.sql` will be authored in Task 07b — its directory must exist now.
  </what-built>
  <how-to-verify>
    1. Outcome selected from Check 1 (R1, R2, or R3) is documented in the resume-signal output below; if R2, REQUIREMENTS.md amendment is committed as a separate `docs(req): amend DBHY-02` commit BEFORE Task 01.
    2. Outcome selected from Check 1B (U1, U2, or U3) is documented; if U2, Task 01 plan includes either a `DROP FUNCTION` for the 3-param overload OR a SECOND `CREATE OR REPLACE FUNCTION` block hardening the 3-param signature; the U-outcome SQL output is captured to `evidence/update_profile_after_auth_overloads.txt`.
    3. Check 2 greps return zero matches OR each match is documented with the required `extensions.` / `auth.` qualification.
    4. `evidence/pre-deploy-functiondef-snapshot.sql` exists and contains `CREATE OR REPLACE FUNCTION public.` for each of the 6 (or 7) target functions — `grep -c 'CREATE OR REPLACE FUNCTION' evidence/pre-deploy-functiondef-snapshot.sql` returns 6 or 7.
    5. `npx supabase db reset --local` exits 0.
    6. `tests/sql/` and `evidence/` directories exist.
  </how-to-verify>
  <resume-signal>
    Type:
      "W0:
        rls_auto_enable_outcome = [R1 user-owned in public | R2 system/extension-owned, REQUIREMENTS.md amended in commit <hash> | R3 escalated, stop],
        update_profile_after_auth_overload = [U1 only 4-param | U2 3+4-param, action = DROP 3-param OR harden both | U3 only 3-param, stop],
        function_count = [6 | 7],
        extension_audit = [CLEAN | matches: <list>],
        auth.uid_audit = [CLEAN | matches: <list>],
        pre_deploy_snapshot = [CAPTURED at evidence/pre-deploy-functiondef-snapshot.sql with <N> functions | NOT CAPTURED — stop],
        local_stack = [OK | BROKEN — fix before proceeding],
        fixtures = [DIRECTORIES READY]"
    Then proceed to Wave 1.
  </resume-signal>
  <acceptance_criteria>
    - rls_auto_enable outcome documented (R1, R2, or R3); if R2, REQUIREMENTS.md amended via separate commit with the carve-out wording specified above
    - `update_profile_after_auth` overload outcome documented (U1, U2, or U3); if U2, Migration 14 plan updated to either DROP the stale 3-param overload or harden both signatures (executable action recorded in the resume-signal)
    - Extension-function grep returns zero matches OR each match has a documented `extensions.<fn>` qualification target for Task 01
    - `auth.uid()` qualification grep returns zero bare `uid()` matches in target function bodies
    - `evidence/pre-deploy-functiondef-snapshot.sql` exists; `grep -c 'CREATE OR REPLACE FUNCTION' ...` returns the locked function count from Check 1 (6 or 7)
    - `npx supabase db reset --local` exits 0 with migrations 0–13 applied
    - `tests/sql/` and `.planning/phases/14-security-definer-search-path-migration/evidence/` directories exist
  </acceptance_criteria>
  <done>rls_auto_enable resolution selected and (if R2) DBHY-02 amendment committed; `update_profile_after_auth` overload enumeration complete with explicit U1/U2/U3 decision (and U2 ⇒ DROP-or-harden action locked into Task 01); extension/auth qualification audit complete; pre-deploy emergency restore SQL captured; local stack healthy; fixture directories ready. Function count (6 or 7) and DBHY-02 wording are locked for Wave 1.</done>
</task>

<!-- ================================================================== -->
<!-- WAVE 1: Author Migration 14 + local apply + local lint              -->
<!-- ================================================================== -->

<task type="auto" id="14-01-01" tdd="false">
  <name>Task 01: Author Migration 14 — CREATE OR REPLACE FUNCTION for 6 (or 7) target functions</name>
  <files>supabase/migrations/00000000000014_harden_security_definer_search_path.sql</files>
  <read_first>
    - supabase/migrations/00000000000002_triggers.sql (authoritative bodies for handle_new_user, validate_vote_choice, increment_vote_count; lines 86-188)
    - supabase/migrations/00000000000003_guild_membership.sql (authoritative 4-param update_profile_after_auth; lines 41-62)
    - supabase/migrations/00000000000004_fix_trigger_rpc_context.sql (authoritative profile_self_update_allowed; lines 12-45)
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (authoritative is_current_user_admin with SET search_path = public; lines 11-26)
    - supabase/migrations/00000000000013_demote_admin_guarded_advisory_lock.sql (file-header comment style template; lines 1-36)
    - supabase/migrations/00000000000007_fix_pr_review.sql (section-divider style; lines 16-18)
    - .planning/phases/14-security-definer-search-path-migration/14-PATTERNS.md (per-function templates, ordering guidance, REVOKE guidance)
  </read_first>
  <action>
    Create `supabase/migrations/00000000000014_harden_security_definer_search_path.sql`.

    FILE-LEVEL HEADER (per migration 13 style, no phase/plan rot-tags in source):
    A multi-line `--` block describing: adds `SET search_path = ''` to pre-Phase-11 SECURITY DEFINER functions flagged by advisor `0011_function_search_path_mutable`; uses `CREATE OR REPLACE FUNCTION` to preserve OIDs (trigger references); lists the 6 (or 7) cleared functions by name; notes local verification via `supabase db reset` + `supabase db lint` per D-04.

    SECTION ORDERING (lowest-risk first per RESEARCH.md — enables bisection during local testing):
    1. validate_vote_choice
    2. handle_new_user
    3. profile_self_update_allowed
    4. update_profile_after_auth
    5. is_current_user_admin
    6. increment_vote_count
    7. rls_auto_enable (ONLY if W0 confirmed user-owned in public schema)

    EACH FUNCTION BLOCK must:
    - Open with a `-- =====================================================================` section divider: `-- FUNCTION N: <name>`
    - Use `CREATE OR REPLACE FUNCTION public.<name>(...)` with the EXACT signature from the source migration listed in read_first (matching the latest migration that defines that function)
    - Include `SECURITY DEFINER` — never omit; its absence silently converts to SECURITY INVOKER
    - Include `SET search_path = ''` in the pre-AS options block (not post-`$$`)
    - Use the pre-AS options form: `LANGUAGE plpgsql / SECURITY DEFINER / SET search_path = ''` then `AS $$ ... $$;`
    - Copy the function body VERBATIM from the authoritative source migration — the only change is adding `SET search_path = ''`

    SPECIFIC FUNCTION NOTES:
    - `is_current_user_admin`: copy body verbatim from migration 9 (lines 11-26). Change ONLY `SET search_path = public` → `SET search_path = ''`. Retain `STABLE` keyword — omitting it degrades to VOLATILE and disables per-statement RLS caching.
    - `increment_vote_count`: body copied verbatim from migration 2 (lines 172-188). The `INSERT INTO public.vote_counts` and `public.vote_counts.count` are ALREADY fully qualified — no body change. Per RESEARCH.md CRITICAL FINDING, CONTEXT.md's bare-reference claim is incorrect. Only add `SET search_path = ''`.
    - `update_profile_after_auth`: use the 4-param signature from migration 3 `(p_mfa_verified BOOLEAN, p_discord_username TEXT, p_avatar_url TEXT, p_guild_member BOOLEAN DEFAULT FALSE)`. Using the 3-param signature creates a separate OID overload and clears zero WARNs for the live function. **If W0 Check 1B selected OUTCOME U2** (both 3-param and 4-param overloads present): add `DROP FUNCTION IF EXISTS public.update_profile_after_auth(BOOLEAN, TEXT, TEXT);` IMMEDIATELY BEFORE the 4-param `CREATE OR REPLACE FUNCTION` block. This removes the stale 3-param SECURITY DEFINER overload that would otherwise remain unhardened and leave a residual `0011_function_search_path_mutable` WARN. Alternative path (only if app callers depend on 3-param invocation, per `git grep -n "update_profile_after_auth" src/`): keep both overloads by emitting a SECOND `CREATE OR REPLACE FUNCTION public.update_profile_after_auth(p_mfa_verified BOOLEAN, p_discord_username TEXT, p_avatar_url TEXT) RETURNS void LANGUAGE plpgsql SECURITY DEFINER SET search_path = '' AS $$ ... $$;` block (body copied from migration 2). **If W0 selected U1** (only 4-param exists): no DROP needed; harden only the 4-param signature. **If U3** (only 3-param): STOP, executor must NOT have reached Task 01.
    - `profile_self_update_allowed`: body from migration 4 (lines 12-45). The `current_user = session_user` comparison uses Postgres `pg_catalog` session variables — they resolve correctly under `search_path = ''` without schema qualification.
    - `validate_vote_choice` and `handle_new_user`: bodies from migration 2 (lines 86-159). All DML targets already use `public.<table>` — no body changes needed.
    - `rls_auto_enable` (conditional): include ONLY if W0 confirmed user-owned in public schema. Copy body from the source verified in W0. If system-owned, omit entirely — do not add a placeholder block.

    REVOKE: Do not add new REVOKE statements. Migration 14's sole purpose is `search_path` hardening. If any prior migration applied `REVOKE EXECUTE` for a function in this list, that grant persists after `CREATE OR REPLACE` — no re-application needed unless the prior migration explicitly used `GRANT EXECUTE` after the `REVOKE`.

    After writing the file, run these verification greps (expected values depend on W0 outcome):
    - `grep -c 'CREATE OR REPLACE FUNCTION' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` → must equal 6 or 7
    - `grep -c 'SECURITY DEFINER' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` → must equal 6 or 7 (same count)
    - `grep -c "SET search_path = ''" supabase/migrations/00000000000014_harden_security_definer_search_path.sql` → must equal 6 or 7
  </action>
  <verify>
    <automated>
      grep -c 'CREATE OR REPLACE FUNCTION' supabase/migrations/00000000000014_harden_security_definer_search_path.sql &&
      grep -c 'SECURITY DEFINER' supabase/migrations/00000000000014_harden_security_definer_search_path.sql &&
      grep -c "SET search_path = ''" supabase/migrations/00000000000014_harden_security_definer_search_path.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/00000000000014_harden_security_definer_search_path.sql` exists
    - `grep -c 'CREATE OR REPLACE FUNCTION' ... == 6` (or 7 if rls_auto_enable confirmed user-owned in W0)
    - `grep -c 'SECURITY DEFINER' ...` equals the same count as CREATE OR REPLACE FUNCTION (every function block has it)
    - `grep -c "SET search_path = ''" ...` equals the same count (every function block has it)
    - `grep -c 'INSERT INTO public.vote_counts' ...` == 1 (increment_vote_count body uses already-qualified reference; no bare `INSERT INTO vote_counts` appears anywhere in the file)
    - `grep -n 'STABLE' ...` shows STABLE on the is_current_user_admin block (not stripped)
    - `grep -n 'p_guild_member' ...` shows the 4th parameter of update_profile_after_auth is present (confirms 4-param signature)
    - File contains no fenced code block markers (```); it is valid SQL only
    - File contains no plan/phase rot-tag comments (per CLAUDE.md: WHY comments only, no round/phase IDs in source)
  </acceptance_criteria>
  <done>Migration 14 SQL file authored with correct per-function CREATE OR REPLACE blocks; grep counts match the W0-determined function count; SECURITY DEFINER and SET search_path = '' appear in every block.</done>
</task>

<task type="auto" id="14-01-02-05">
  <name>Task 02–05: Local apply + local lint + machine-enforced is_current_user_admin functiondef diff + catalog assertion + update_profile_after_auth signature check</name>
  <files>supabase/migrations/00000000000014_harden_security_definer_search_path.sql</files>
  <read_first>
    - supabase/migrations/00000000000014_harden_security_definer_search_path.sql (just authored in Task 01 — do NOT re-read the source migrations; use this file as the canonical state)
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (lines 11-26 — is_current_user_admin authoritative body for diff comparison)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql (W0 artifact — production baseline for the functiondef diff)
  </read_first>
  <action>
    Run the full local validation sequence in order. Fix any error in Migration 14 before proceeding to the next step. This task addresses Codex's MEDIUM concerns on machine-enforced diff and catalog assertions.

    STEP 1 — Apply locally (DBHY-01 task 14-01-02):
      npx supabase db reset --local
    Expected: exit 0, all 14 migrations apply without error. If Migration 14 fails, read the error carefully — common causes: (a) wrong function signature creates a new overload rather than replacing the existing one, (b) missing `public.` prefix on a table reference under `search_path = ''`, (c) `STABLE` omitted on is_current_user_admin, (d) unqualified `extensions.<fn>` if W0 audit flagged any. Fix Migration 14 and re-run.

    STEP 2 — Machine-enforced is_current_user_admin functiondef diff (DBHY-01 task 14-01-02; addresses Codex MEDIUM):
    Capture the local post-migration body, capture the migration-9 baseline, and run `diff -u` with `SET search_path` line normalization. Manual visual inspection is NOT sufficient.

      mkdir -p .planning/phases/14-security-definer-search-path-migration/evidence

      # Local post-migration body:
      psql "$LOCAL_DB_URL" -At -c "SELECT pg_get_functiondef('public.is_current_user_admin()'::regprocedure);" \
        > .planning/phases/14-security-definer-search-path-migration/evidence/local-functiondef-is_current_user_admin.sql

      # Extract migration-9 baseline body (signature + body, without the SET search_path = public line):
      awk '/CREATE OR REPLACE FUNCTION public\.is_current_user_admin/,/\$\$;/' \
        supabase/migrations/00000000000009_admin_integrity_rls.sql \
        > .planning/phases/14-security-definer-search-path-migration/evidence/migration9-is_current_user_admin.sql

      # Normalize the search_path line out of both files, then diff:
      sed -E "s/SET search_path = ('?)[a-zA-Z']*\1/SET search_path = <NORMALIZED>/" \
        .planning/phases/14-security-definer-search-path-migration/evidence/local-functiondef-is_current_user_admin.sql \
        > /tmp/local-norm.sql
      sed -E "s/SET search_path = ('?)[a-zA-Z']*\1/SET search_path = <NORMALIZED>/" \
        .planning/phases/14-security-definer-search-path-migration/evidence/migration9-is_current_user_admin.sql \
        > /tmp/m9-norm.sql

      diff -u /tmp/m9-norm.sql /tmp/local-norm.sql \
        > .planning/phases/14-security-definer-search-path-migration/evidence/functiondef-diff-local.txt
      DIFF_LINES=$(wc -l < .planning/phases/14-security-definer-search-path-migration/evidence/functiondef-diff-local.txt)

    Expected behavior: After normalization, the diff should show ONLY whitespace/formatting differences that come from `pg_get_functiondef`'s canonical output form (e.g., uppercase keywords, parenthesization). Body SQL between `$$` markers must be functionally identical. If `DIFF_LINES > 0` with non-whitespace differences in the body, REVERT — the executor introduced an unintended body change. (Some non-zero `DIFF_LINES` from formatting alone is expected; the executor must read the diff and confirm no semantic change.)

    STEP 3 — Verify update_profile_after_auth signature (DBHY-01 task 14-01-04):
      psql "$LOCAL_DB_URL" -c "\df+ public.update_profile_after_auth"
    Confirm: exactly ONE function entry exists with 4 parameters (`p_mfa_verified bool, p_discord_username text, p_avatar_url text, p_guild_member bool`). If TWO entries appear (4-param and 3-param overload), Migration 14 used the wrong signature — the function with `DEFAULT FALSE` on the 4th param should replace only the 4-param form. Check Migration 14's signature block and fix if needed.

    STEP 4 — Catalog assertion query for ALL target functions (addresses Codex MEDIUM):
    One catalog query proves every target function has the right `prosecdef`, `provolatile`, `proconfig`, `proowner`, `proacl`, AND that there is exactly ONE entry per name (no accidental overloads).

      psql "$LOCAL_DB_URL" -c "
        SELECT
          p.proname,
          pg_get_function_identity_arguments(p.oid) AS args,
          p.prosecdef,
          p.provolatile,
          p.proconfig,
          p.proowner::regrole AS owner,
          p.proacl
        FROM pg_proc p
        JOIN pg_namespace n ON n.oid = p.pronamespace
        WHERE n.nspname = 'public'
          AND p.proname IN (
            'is_current_user_admin',
            'increment_vote_count',
            'update_profile_after_auth',
            'handle_new_user',
            'validate_vote_choice',
            'profile_self_update_allowed'
          )
        ORDER BY p.proname, p.oid;
      " | tee .planning/phases/14-security-definer-search-path-migration/evidence/catalog-assertion-local.txt

    Verify in the output:
      - Each function name appears exactly ONCE (no overloads — Codex MEDIUM trap on update_profile_after_auth)
      - `prosecdef` is `t` (true) for every row — SECURITY DEFINER attribute preserved
      - `provolatile` is `s` (stable) for `is_current_user_admin`; `v` (volatile, the default) for the others — STABLE keyword not stripped from is_current_user_admin
      - `proconfig` contains `search_path=` for every row — SET search_path applied
      - `proowner` is the same role for all rows — no ownership drift
      - If `rls_auto_enable` is included per W0 OUTCOME R1, it appears here as the 7th row with the same attributes

    STEP 5 — Local lint (DBHY-01 task 14-01-05):
      npx supabase db lint --local 2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/local-lint.txt
      LOCAL_WARN_COUNT=$(grep -c '0011_function_search_path_mutable' .planning/phases/14-security-definer-search-path-migration/evidence/local-lint.txt || echo 0)
    Expected: $LOCAL_WARN_COUNT == 0 if W0 OUTCOME R1 (all 7 included); == 0 if W0 OUTCOME R2 (6 included; rls_auto_enable WARN is acceptable per the amended DBHY-02 carve-out only against `--linked`; local lint should still show 0 because the local stack doesn't have system-owned rls_auto_enable).

    STEP 6 — increment_vote_count trigger smoke (DBHY-01 task 14-01-03 — reference grep):
      grep -n 'INSERT INTO' supabase/migrations/00000000000014_harden_security_definer_search_path.sql
    Expected: one line containing `INSERT INTO public.vote_counts`; no line containing bare `INSERT INTO vote_counts`.
  </action>
  <verify>
    <automated>
      npx supabase db reset --local &&
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/functiondef-diff-local.txt &&
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/catalog-assertion-local.txt &&
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/local-lint.txt &&
      grep -c '0011_function_search_path_mutable' .planning/phases/14-security-definer-search-path-migration/evidence/local-lint.txt | grep -x '0' &&
      grep -n 'INSERT INTO public.vote_counts' supabase/migrations/00000000000014_harden_security_definer_search_path.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db reset --local` exits 0 (all 14 migrations applied cleanly; task ID 14-01-02 pass)
    - `evidence/functiondef-diff-local.txt` exists; after `SET search_path` normalization the body SQL between `$$` markers shows no semantic change vs migration 9 baseline (formatting-only differences are acceptable; executor reads the diff and confirms)
    - `evidence/catalog-assertion-local.txt` exists; for each target function name there is exactly ONE row with `prosecdef=t`, `proconfig` containing `search_path=`, `proowner` consistent across rows; `is_current_user_admin` has `provolatile=s` (STABLE preserved)
    - `\df+ public.update_profile_after_auth` returns exactly one entry with 4 parameters including `p_guild_member` (task ID 14-01-04 pass)
    - `evidence/local-lint.txt` exists; `grep -c '0011_function_search_path_mutable' evidence/local-lint.txt` == 0 (task ID 14-01-05 pass)
    - `grep -n 'INSERT INTO' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` returns only `INSERT INTO public.vote_counts`; no bare `INSERT INTO vote_counts` line (task ID 14-01-03 pass)
    - `grep -n 'STABLE' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` returns the STABLE line for is_current_user_admin (not stripped)
  </acceptance_criteria>
  <done>Local stack applies Migration 14 cleanly; zero 0011 WARNs on local lint; machine-enforced functiondef diff confirms is_current_user_admin body-identical (post-normalization); catalog assertion confirms prosecdef/provolatile/proconfig/proowner correct and no accidental overloads; update_profile_after_auth 4-param signature confirmed; increment_vote_count INSERT target is public.vote_counts.</done>
</task>

<!-- ================================================================== -->
<!-- WAVE 2: DBHY-04 doc fix + pre-deploy lint + TEST-11 matrix re-run  -->
<!-- ================================================================== -->

<task type="auto" id="14-01-06">
  <name>Task 06: DBHY-04 — Fix prose drift in 11-PATTERNS.md (line 83)</name>
  <files>.planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md (entire file — read to confirm current state of line 83 and the SQL block at lines 85-101 before editing)
    - .planning/phases/14-security-definer-search-path-migration/14-PATTERNS.md (DBHY-04 section — exact source and replacement prose, changelog note format, placement)
  </read_first>
  <action>
    Make two edits to `11-PATTERNS.md`. The SQL code block at lines 85-101 is already correct and must not be touched.

    EDIT 1 — Add changelog note (D-06 canonical form):
    Insert the following as the SECOND LINE of the file (immediately after the `# Phase 11:…` H1 heading, before the `**Mapped:**` metadata line), with a blank line before and after:
      Updated 2026-05-16 (Phase 14, DBHY-04) — original form had admin-OR drift; see REVIEW-FIX-H3.

    EDIT 2 — Replace the drift phrase at line 83 (after the changelog insertion, line numbers shift; search by content not line number):
    Current phrase to locate: "and the v1.0.5 admin-bypass policy in `migrations/00000000000005_admin_phase4.sql:75-88` (whichever is current)."
    The replacement substitutes "(whichever is current)" with "(both superseded)" and adds "The shipped policy (migration 10, REVIEW-FIX-H3) has NO `is_current_user_admin()` OR-bypass —" before the existing "admin reads go through..." clause.

    FULL REPLACEMENT LINE (replace the entire sentence that contains "whichever is current"):
    Old: "...and the v1.0.5 admin-bypass policy in `migrations/00000000000005_admin_phase4.sql:75-88` (whichever is current). The shipped policy has NO `is_current_user_admin()` OR-bypass..."
    New: "...and the v1.0.5 admin-bypass policy at `migrations/00000000000005_admin_phase4.sql:75-88` (both superseded). The shipped policy (migration 10, REVIEW-FIX-H3) has NO `is_current_user_admin()` OR-bypass..."

    The key changes are:
    - "in" → "at" (minor style fix)
    - "(whichever is current)" → "(both superseded)"
    - Insert "(migration 10, REVIEW-FIX-H3)" after "The shipped policy"

    After editing, verify:
      grep -n 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md
    Expected: no output (the drift phrase is gone).
      grep -n 'REVIEW-FIX-H3' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md
    Expected: at least 2 matches (changelog note + the prose fix; the original reference in the SQL block comment may add more).
  </action>
  <verify>
    <automated>
      grep -n 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md | wc -l | grep -x '       0' || grep -c 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md | grep -x '0'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` == 0 (drift phrase removed; task ID 14-01-06 pass)
    - `grep -n 'Updated 2026-05-16 (Phase 14, DBHY-04)' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns a match in the first 5 lines of the file (changelog note present at top)
    - `grep -n 'both superseded' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns a match (replacement phrase present)
    - `grep -n 'migration 10, REVIEW-FIX-H3' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns a match (policy source cited)
    - Lines 85-101 SQL block is unchanged — `grep -c 'is_current_user_admin' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns 0 inside the SQL block section (the SQL block has no admin OR-bypass; if this grep returns > 0, investigate whether the SQL was accidentally modified)
  </acceptance_criteria>
  <done>11-PATTERNS.md line 83 drift phrase removed; changelog note added at top; SQL block at lines 85-101 untouched; REVIEW-FIX-H3 named as the shipped form.</done>
</task>

<task type="auto" id="14-01-07a">
  <name>Task 07a: Pre-deploy lint against linked project + TEST-11 12-cell RLS matrix re-run (exit-code based)</name>
  <files>.planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt</files>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-RESEARCH.md (Environment Availability section — TEST-11 note on ES256 bug; Validation Architecture section)
    - e2e/integration/vote-counts-rls.test.ts (read first 30 lines to confirm TEST-11 header and describe block structure; do not re-read the full test file)
    - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md (Per-Task Verification Map row 14-01-07)
    - package.json (confirm `test:integration` script invocation form for direct vitest call)
  </read_first>
  <action>
    Run two pre-deploy checks. Capture output to evidence folder. Verification is EXIT-CODE based, not PASS/pass string grep (addresses Codex LOW concern on noisy grep matching).

    STEP 1 — Create evidence directory (no-op if W0 already created it):
      mkdir -p .planning/phases/14-security-definer-search-path-migration/evidence

    STEP 2 — Pre-deploy lint against linked project (D-04, first of two lint runs):
    This runs `supabase db lint` against the linked PRODUCTION project (before Migration 14 is pushed). It shows the current pre-migration WARN count as a baseline, and also validates that the CLI is authenticated and the linked project is reachable.
      npx supabase db lint --linked 2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt
    Save this output — it is the baseline WARN count before Migration 14 deploys. Expected: 7 (or 6) entries for `0011_function_search_path_mutable` for the target functions.

    STEP 3 — TEST-11 12-cell RLS matrix re-run via direct vitest invocation (DBHY-03, automated portion):
    TEST-11 uses `supabase.from()` (direct Postgres RLS, not Edge Functions) and is unaffected by the local ES256 supabase-edge-runtime bug. Invoke vitest directly on the target test file and rely on its exit code (0 = all pass).

      npx vitest run e2e/integration/vote-counts-rls.test.ts --reporter=verbose \
        2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt
      TEST_EXIT=${PIPESTATUS[0]}
      echo "TEST-11 vitest exit code: $TEST_EXIT" \
        >> .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt
      test "$TEST_EXIT" = "0" || { echo "TEST-11 FAILED (exit $TEST_EXIT) — investigate before Wave 3"; exit 1; }

    Expected: TEST_EXIT == 0 (all 12 cells pass). This is a pre-deploy run against the local stack after `supabase db reset --local` (Migration 14 applied). If TEST_EXIT != 0, a cell failed — investigate before proceeding to Wave 3. A failure here means Migration 14 introduced a body drift in `is_current_user_admin` or another semantic regression.

    Note on TEST-11 scope: TEST-11's 12-cell matrix tests the `vote_counts` policy, which intentionally has NO admin OR-bypass (REVIEW-FIX-H3). It proves the *absence* of the admin branch on that table, but does NOT directly exercise `is_current_user_admin()` returning TRUE/FALSE on admin-gated tables. The direct regression for `is_current_user_admin()` happens in Task 07b below.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt &&
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt &&
      grep -q 'TEST-11 vitest exit code: 0' .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/pre-deploy-lint.txt` exists and contains output from `npx supabase db lint --linked` (task ID 14-01-07 pass)
    - `evidence/test-11-pre-deploy.txt` exists and contains TEST-11 run output AND a line `TEST-11 vitest exit code: 0` (exit-code based verification — addresses Codex LOW concern)
    - `grep -i 'vote-counts-rls' evidence/test-11-pre-deploy.txt` returns a match confirming the correct test file ran
    - If any 0011 WARNs in `pre-deploy-lint.txt` are for functions OTHER than `rls_auto_enable` (when W0 OUTCOME R2 amended DBHY-02), flag as a blocker before Wave 3 — they should already be cleared by Task 02-05 local apply, but pre-deploy lint runs against the LINKED project (still pre-migration), so the baseline is expected to show 6 or 7 WARNs PRE-deploy — the zero-WARN check is the POST-deploy run in Task 08b
  </acceptance_criteria>
  <done>Pre-deploy lint output captured; TEST-11 matrix passes with exit code 0 against local stack with Migration 14 applied; evidence folder populated.</done>
</task>

<task type="auto" id="14-01-07b">
  <name>Task 07b: Direct `is_current_user_admin()` regression test on admin-gated RLS table (addresses HIGH review concern)</name>
  <files>tests/sql/is_current_user_admin_regression.sql</files>
  <read_first>
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (lines 11-26 — is_current_user_admin body; understand the AND chain: is_admin AND mfa_verified AND guild_member)
    - supabase/migrations/00000000000010_results_hidden_audit.sql (audit_log table + RLS policies — audit_log is the admin-gated table this fixture exercises)
    - supabase/migrations/00000000000014_harden_security_definer_search_path.sql (just authored — confirm is_current_user_admin block uses the same body)
    - .planning/phases/14-security-definer-search-path-migration/14-REVIEWS.md (Codex HIGH concern: "TEST-11 is not enough to prove is_current_user_admin() behavior")
  </read_first>
  <action>
    Author a direct regression fixture for `is_current_user_admin()` that exercises the four identity branches (admin, non-admin, mfa_verified=false, guild_member=false) AND exercises at least one admin-gated RLS table beyond `vote_counts` (e.g., `audit_log`). This addresses the Codex HIGH concern that the 12-cell TEST-11 matrix mostly proves the *absence* of the admin OR-branch on `vote_counts`, not the correctness of `is_current_user_admin()` itself.

    Create `tests/sql/is_current_user_admin_regression.sql` with the following structure. The fixture sets up 4 test profiles in a transaction, sets the JWT claim for each via Postgres session-local `request.jwt.claims`, calls `public.is_current_user_admin()` directly, and also queries an admin-gated table (e.g., `public.audit_log`) to confirm RLS gating matches. The transaction is rolled back at the end so no permanent test data is written.

    Fixture content (write as a single SQL script). The fixture has been hardened to address Codex HIGH H3 (RLS-not-actually-tested), Codex HIGH H1 (FK to auth.users + wrong audit_log columns + invalid top-level PERFORM), and Gemini MEDIUM (regression role context). Three execution-correctness invariants:

    1. **FK seeding**: `public.profiles.id` REFERENCES `auth.users(id) ON DELETE CASCADE`. The fixture MUST insert matching `auth.users` rows BEFORE the `public.profiles` rows; otherwise the script fails with `insert or update on table "profiles" violates foreign key constraint`.
    2. **Correct audit_log columns**: per `supabase/migrations/00000000000010_results_hidden_audit.sql` lines 47-56, columns are `id, actor_id, action, target_type, target_id, before, after, created_at` — there is NO `payload` column. The fixture must INSERT into `(actor_id, action, target_type, target_id, before, after)`.
    3. **RLS actually exercised**: a direct `psql "$LOCAL_DB_URL"` session runs as a privileged role (postgres / supabase_admin) that bypasses RLS by default. The fixture MUST `SET LOCAL ROLE authenticated` BEFORE every `SELECT` that intends to exercise an RLS-gated table, and `RESET ROLE` before privileged setup (INSERT/seed). Otherwise the "non-admin sees 0 audit_log rows" assertion passes for the wrong reason (superuser bypass) instead of actually proving `is_current_user_admin() = FALSE` denies the row. Plain-SQL scripts also cannot use `PERFORM` at the top level (PL/pgSQL only) — use `SELECT set_config(...)` instead, or wrap inside a `DO $$ ... $$` block where `PERFORM` is valid.

```sql
-- tests/sql/is_current_user_admin_regression.sql
-- Direct regression for public.is_current_user_admin() across 4 identity branches
-- AND exercises one admin-gated RLS table beyond vote_counts (audit_log).
-- Addresses Phase 14 cross-AI review HIGH concerns:
--   H1: TEST-11 12-cell matrix tests vote_counts (REVIEW-FIX-H3 form, no admin
--       OR-branch) and does NOT strongly prove is_current_user_admin() behavior
--       on admin-gated tables.
--   H3: A direct psql session is privileged and bypasses RLS unless the role
--       is explicitly switched to `authenticated`. This fixture uses
--       `SET LOCAL ROLE authenticated` around every RLS-asserting SELECT.
--
-- Run: psql "$LOCAL_DB_URL" -v ON_ERROR_STOP=1 -f tests/sql/is_current_user_admin_regression.sql
--
-- The whole script runs in BEGIN; ... ROLLBACK; so no permanent state is
-- written. ON_ERROR_STOP=1 ensures any RAISE EXCEPTION in a DO block fails
-- the script (non-zero exit).

BEGIN;

-- ---------------------------------------------------------------------------
-- Privileged setup: insert auth.users (FK target) and public.profiles rows.
-- This block runs as the connection's privileged role (postgres /
-- supabase_admin). RESET ROLE is a no-op here because we have not yet SET
-- LOCAL ROLE, but is included for symmetry.
-- ---------------------------------------------------------------------------
RESET ROLE;

-- profiles.id has FK to auth.users(id) ON DELETE CASCADE. Seed the auth.users
-- rows first. Minimal columns: id is the only NOT NULL without default in
-- auth.users for our purposes; Supabase will accept rows with id only when
-- inserted by a privileged role.
INSERT INTO auth.users (id, instance_id, aud, role, email, created_at, updated_at)
VALUES
  ('00000000-0000-0000-0000-00000000a001'::uuid, '00000000-0000-0000-0000-000000000000'::uuid, 'authenticated', 'authenticated', 'fixture-admin@phase14.test',     NOW(), NOW()),
  ('00000000-0000-0000-0000-00000000a002'::uuid, '00000000-0000-0000-0000-000000000000'::uuid, 'authenticated', 'authenticated', 'fixture-non-admin@phase14.test', NOW(), NOW()),
  ('00000000-0000-0000-0000-00000000a003'::uuid, '00000000-0000-0000-0000-000000000000'::uuid, 'authenticated', 'authenticated', 'fixture-no-mfa@phase14.test',    NOW(), NOW()),
  ('00000000-0000-0000-0000-00000000a004'::uuid, '00000000-0000-0000-0000-000000000000'::uuid, 'authenticated', 'authenticated', 'fixture-no-guild@phase14.test',  NOW(), NOW());

-- profiles fixture — 4 identity branches for is_current_user_admin().
INSERT INTO public.profiles (id, discord_id, is_admin, mfa_verified, guild_member, discord_username, avatar_url)
VALUES
  ('00000000-0000-0000-0000-00000000a001'::uuid, '900000000000000001', TRUE,  TRUE,  TRUE,  'fixture_admin',     ''),
  ('00000000-0000-0000-0000-00000000a002'::uuid, '900000000000000002', FALSE, TRUE,  TRUE,  'fixture_non_admin', ''),
  ('00000000-0000-0000-0000-00000000a003'::uuid, '900000000000000003', TRUE,  FALSE, TRUE,  'fixture_no_mfa',    ''),
  ('00000000-0000-0000-0000-00000000a004'::uuid, '900000000000000004', TRUE,  TRUE,  FALSE, 'fixture_no_guild',  '');

-- Canary audit_log row — actor is the admin fixture. Inserted while
-- privileged, before RLS is enforced.
-- NOTE: columns are (actor_id, action, target_type, target_id, before, after)
-- per migration 10 lines 47-56. There is NO `payload` column.
INSERT INTO public.audit_log (actor_id, action, target_type, target_id, before, after)
VALUES (
  '00000000-0000-0000-0000-00000000a001'::uuid,
  'fixture-canary',
  'phase-14',
  NULL,
  NULL,
  '{}'::jsonb
);

-- ---------------------------------------------------------------------------
-- Helper: assert that public.is_current_user_admin() returns the expected
-- boolean when called under a given JWT sub. PERFORM is valid INSIDE a DO
-- block / function body, but not at script top level.
--
-- The helper SETs LOCAL ROLE authenticated, then SETs the JWT claim, then
-- calls is_current_user_admin(). is_current_user_admin() is SECURITY DEFINER
-- so it reads profiles as the function owner — the RLS-policy concern here is
-- about the surrounding session role, not the function body.
-- ---------------------------------------------------------------------------
CREATE OR REPLACE FUNCTION pg_temp.assert_admin(p_user_id UUID, p_expected BOOLEAN, p_label TEXT)
RETURNS void LANGUAGE plpgsql AS $$
DECLARE
  v_actual BOOLEAN;
BEGIN
  PERFORM set_config('request.jwt.claim.sub', p_user_id::text, true);
  PERFORM set_config('request.jwt.claims', json_build_object('sub', p_user_id::text)::text, true);
  SET LOCAL ROLE authenticated;
  SELECT public.is_current_user_admin() INTO v_actual;
  RESET ROLE;
  IF v_actual IS DISTINCT FROM p_expected THEN
    RAISE EXCEPTION 'FAIL %: expected % got %', p_label, p_expected, v_actual;
  ELSE
    RAISE NOTICE 'PASS %: % == %', p_label, p_expected, v_actual;
  END IF;
END;
$$;

-- Branch 1: admin AND mfa_verified AND guild_member → TRUE
SELECT pg_temp.assert_admin('00000000-0000-0000-0000-00000000a001'::uuid, TRUE,  'admin');

-- Branch 2: is_admin=FALSE → FALSE
SELECT pg_temp.assert_admin('00000000-0000-0000-0000-00000000a002'::uuid, FALSE, 'non_admin');

-- Branch 3: mfa_verified=FALSE → FALSE
SELECT pg_temp.assert_admin('00000000-0000-0000-0000-00000000a003'::uuid, FALSE, 'mfa_false');

-- Branch 4: guild_member=FALSE → FALSE
SELECT pg_temp.assert_admin('00000000-0000-0000-0000-00000000a004'::uuid, FALSE, 'guild_false');

-- ---------------------------------------------------------------------------
-- Cross-table RLS assertion on public.audit_log.
-- audit_log SELECT policy: USING (public.is_current_user_admin())
-- Under role `authenticated`:
--   - admin JWT sub  → is_current_user_admin() = TRUE  → row visible
--   - non-admin JWT  → is_current_user_admin() = FALSE → row hidden
-- We MUST SET LOCAL ROLE authenticated because a direct psql session as
-- postgres/supabase_admin bypasses RLS, which would make the non-admin
-- assertion pass for the wrong reason.
-- ---------------------------------------------------------------------------

-- Admin branch — expect to see the canary row.
DO $$
DECLARE n INT;
BEGIN
  PERFORM set_config('request.jwt.claim.sub', '00000000-0000-0000-0000-00000000a001', true);
  PERFORM set_config('request.jwt.claims', json_build_object('sub', '00000000-0000-0000-0000-00000000a001')::text, true);
  SET LOCAL ROLE authenticated;
  SELECT COUNT(*) INTO n FROM public.audit_log WHERE action = 'fixture-canary';
  RESET ROLE;
  IF n = 0 THEN
    RAISE EXCEPTION 'FAIL audit_log/admin: expected >=1 canary row, got 0 (is_current_user_admin returned FALSE for admin fixture, or RLS blocked the row)';
  ELSE
    RAISE NOTICE 'PASS audit_log/admin: % canary row(s) visible under authenticated role', n;
  END IF;
END $$;

-- Non-admin branch — expect to NOT see the canary row.
DO $$
DECLARE n INT;
BEGIN
  PERFORM set_config('request.jwt.claim.sub', '00000000-0000-0000-0000-00000000a002', true);
  PERFORM set_config('request.jwt.claims', json_build_object('sub', '00000000-0000-0000-0000-00000000a002')::text, true);
  SET LOCAL ROLE authenticated;
  SELECT COUNT(*) INTO n FROM public.audit_log WHERE action = 'fixture-canary';
  RESET ROLE;
  IF n != 0 THEN
    RAISE EXCEPTION 'FAIL audit_log/non_admin: expected 0 canary rows under authenticated role, got % (RLS not enforced, or is_current_user_admin returned TRUE for non-admin fixture)', n;
  ELSE
    RAISE NOTICE 'PASS audit_log/non_admin: 0 canary rows visible under authenticated role (RLS correctly hid the row)';
  END IF;
END $$;

-- Roll back all fixture data. auth.users rows cascade-delete to profiles
-- via the FK; audit_log canary row also rolls back (entire transaction).
ROLLBACK;
```

    Notes for the executor:
    - If `INSERT INTO auth.users` is rejected because the psql session does not have privileges to write to `auth.users`, run the fixture as the local Supabase superuser. Locally, `supabase db reset --local` exposes a connection string that has these privileges (e.g., `postgres://postgres:postgres@127.0.0.1:54322/postgres`). Use `npx supabase status` to print `DB URL` for the local stack — set `LOCAL_DB_URL` to that value.
    - The fixture uses temp helper functions (`pg_temp.*`) and `SET LOCAL` for both role and config — neither persists beyond the transaction.
    - `SET LOCAL ROLE authenticated` requires the `authenticated` role to be GRANTed to the current session role. In a Supabase local stack this is true by default for `postgres`.
    - The whole script runs inside `BEGIN; … ROLLBACK;` — no permanent state.
    - If a future audit_log schema change adds NOT NULL columns beyond `(actor_id, action, target_type, target_id, before, after)`, the canary INSERT must be amended.
    - Alternative path (if the SQL approach proves brittle in a particular environment): a Vitest integration test under `e2e/integration/is_current_user_admin_regression.test.ts` that uses existing supabase-js fixture auth clients can substitute. The acceptance criteria below remain the same — 4 identity branches + 2 audit_log RLS branches.

    Run the fixture against the local stack with Migration 14 applied (after Task 02-05 passes):
      psql "$LOCAL_DB_URL" -v ON_ERROR_STOP=1 \
        -f tests/sql/is_current_user_admin_regression.sql \
        2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/is_current_user_admin_regression.txt
      REGRESSION_EXIT=${PIPESTATUS[0]}
      echo "is_current_user_admin regression exit code: $REGRESSION_EXIT" \
        >> .planning/phases/14-security-definer-search-path-migration/evidence/is_current_user_admin_regression.txt
      test "$REGRESSION_EXIT" = "0" || { echo "FAIL is_current_user_admin regression"; exit 1; }
  </action>
  <verify>
    <automated>
      test -f tests/sql/is_current_user_admin_regression.sql &&
      grep -q 'INSERT INTO auth.users' tests/sql/is_current_user_admin_regression.sql &&
      grep -q 'before, after' tests/sql/is_current_user_admin_regression.sql &&
      ! grep -q 'payload' tests/sql/is_current_user_admin_regression.sql &&
      ! grep -nE '^PERFORM ' tests/sql/is_current_user_admin_regression.sql &&
      [ "$(grep -c 'SET LOCAL ROLE authenticated' tests/sql/is_current_user_admin_regression.sql)" -ge 3 ] &&
      psql "$LOCAL_DB_URL" -v ON_ERROR_STOP=1 -f tests/sql/is_current_user_admin_regression.sql > /dev/null &&
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/is_current_user_admin_regression.txt &&
      grep -q 'is_current_user_admin regression exit code: 0' .planning/phases/14-security-definer-search-path-migration/evidence/is_current_user_admin_regression.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/sql/is_current_user_admin_regression.sql` exists and asserts FOUR identity branches (admin TRUE; non-admin FALSE; mfa_verified=false FALSE; guild_member=false FALSE) plus TWO RLS branches on `public.audit_log` (admin sees canary row; non-admin does not)
    - Fixture FK-seeds `auth.users` rows BEFORE inserting `public.profiles` rows — `grep -c "INSERT INTO auth.users" tests/sql/is_current_user_admin_regression.sql` >= 1 (addresses Codex HIGH H1 FK gap)
    - Fixture uses correct `audit_log` columns — `grep -c "before, after" tests/sql/is_current_user_admin_regression.sql` >= 1 AND `grep -c "payload" tests/sql/is_current_user_admin_regression.sql` == 0 (addresses Codex HIGH H1 wrong-columns gap)
    - Fixture uses no top-level `PERFORM` — every `PERFORM` appears inside a `DO $$` block or a function body; `grep -nE "^PERFORM" tests/sql/is_current_user_admin_regression.sql` returns 0 lines (addresses Codex MEDIUM M2 invalid-SQL gap)
    - Fixture uses `SET LOCAL ROLE authenticated` around every RLS-asserting SELECT — `grep -c "SET LOCAL ROLE authenticated" tests/sql/is_current_user_admin_regression.sql` >= 3 (one per assert_admin helper SELECT path, plus one per audit_log admin/non-admin SELECT — at minimum 3 occurrences; fixture as written has 3) (addresses Codex HIGH H3 + Gemini MEDIUM M1 RLS-bypass gap)
    - `psql -v ON_ERROR_STOP=1 -f tests/sql/is_current_user_admin_regression.sql` exits 0 (every assertion raises NOTICE, none raises EXCEPTION)
    - `evidence/is_current_user_admin_regression.txt` contains the line `is_current_user_admin regression exit code: 0`
    - Output contains 6 `PASS` notice lines (4 identity branches + 2 RLS branches) and 0 `FAIL` exception lines
    - Fixture is wrapped in `BEGIN; … ROLLBACK;` — no permanent test data persists after the run
  </acceptance_criteria>
  <done>Direct is_current_user_admin() regression fixture authored and passing locally; FK-correct (auth.users seeded), schema-correct (audit_log before/after columns), valid SQL (no top-level PERFORM), and RLS-correct (SET LOCAL ROLE authenticated around RLS-asserting SELECTs); 4 identity branches + 2 audit_log RLS branches asserted; addresses Codex HIGH concerns H1 (FK + columns + PERFORM) + H3 (RLS bypass) and Gemini MEDIUM M1 (regression role context).</done>
</task>

<!-- ================================================================== -->
<!-- WAVE 3: [BLOCKING] Production push + post-deploy verification       -->
<!-- ================================================================== -->

<task type="checkpoint:human-action" id="14-01-08-push" gate="blocking">
  <name>Task 08a [BLOCKING]: Verify pre-deploy artifacts committed, then push Migration 14 to production</name>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-CONTEXT.md (D-09 local replay requirement, D-10 deploy timing, D-08 rollback strategy)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt (confirm pre-deploy baseline captured before pushing)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql (emergency restore artifact — must exist and be committed before push)
  </read_first>
  <what-built>
    Migration 14 has been authored (Task 01), applied locally (Task 02–05) with machine-enforced functiondef diff and catalog assertion, DBHY-04 doc fix is in (Task 06), pre-deploy lint + TEST-11 captured (Task 07a), and direct is_current_user_admin regression passes (Task 07b). Before the production push, confirm:

    PRE-PUSH GATE 1 — Pre-deploy emergency restore SQL exists and is committed:
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql || { echo "BLOCKER: pre-deploy snapshot missing"; exit 1; }
      grep -c 'CREATE OR REPLACE FUNCTION' .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql
      # Must be 6 (W0 R2) or 7 (W0 R1).

      git log --oneline -- .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql
      # Must show at least one commit. If empty, commit it now:
      #   git add .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql
      #   git commit -m "chore(phase-14): capture pre-deploy functiondef snapshot for emergency restore"

    PRE-PUSH GATE 2 — If W0 OUTCOME R2 was selected, REQUIREMENTS.md amendment commit must be in git history:
      git log --oneline --grep='amend DBHY-02' -- .planning/REQUIREMENTS.md
      # Must show at least one commit if W0 outcome was R2. If R1, this is N/A.

    Two pre-push commits are required per D-07:
    1. `feat(db): migration 14 — security definer search_path lockdown` — commits `supabase/migrations/00000000000014_harden_security_definer_search_path.sql`
    2. `docs(11-patterns): align vote_counts skeleton with shipped REVIEW-FIX-H3 form` — commits `.planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md`

    Then push to production:
      SUPABASE_ACCESS_TOKEN=<your-token> npx supabase db push
    Or if authenticated via `npx supabase login`:
      npx supabase db push

    Note: `supabase db push` is atomic per migration. If it fails partway, Migration 14 uses `CREATE OR REPLACE FUNCTION` statements which are idempotent — safe to re-run. The rollback path (D-08, hardened): use `evidence/pre-deploy-functiondef-snapshot.sql` via Supabase Studio SQL editor, OR if admin Studio path is locked out, via service-role key (`SUPABASE_SERVICE_ROLE_KEY` + direct psql against the production DB) — the snapshot is service-role usable because it executes plain `CREATE OR REPLACE FUNCTION` DDL.

    If the push requires interactive confirmation that cannot be suppressed, type `y` and confirm.
  </what-built>
  <how-to-verify>
    1. PRE-PUSH GATE 1 passes: `evidence/pre-deploy-functiondef-snapshot.sql` exists with 6 or 7 `CREATE OR REPLACE FUNCTION` entries and is in git history.
    2. PRE-PUSH GATE 2 passes if W0 R2: REQUIREMENTS.md DBHY-02 amendment commit exists.
    3. Confirm `npx supabase db push` exits 0 and shows Migration 14 applied.
    4. In Supabase Dashboard, go to Database → Migrations and confirm `00000000000014_harden_security_definer_search_path` appears in the applied list.
  </how-to-verify>
  <resume-signal>Type "PUSHED: pre-deploy snapshot committed in <hash>, DBHY-02 amendment [N/A | committed in <hash>], migration 14 applied to production" to proceed to post-deploy verification (Task 08b).</resume-signal>
  <acceptance_criteria>
    - `evidence/pre-deploy-functiondef-snapshot.sql` exists, contains 6 or 7 `CREATE OR REPLACE FUNCTION public.` entries, AND has a commit in git history BEFORE the `feat(db): migration 14` commit
    - If W0 R2: `.planning/REQUIREMENTS.md` has an `amend DBHY-02` commit in git history before the migration commit
    - Migration 14 appears in the Supabase Dashboard → Database → Migrations list with status applied
    - Two pre-push commits (in addition to the snapshot/amendment commits) made: feat(db) commit for the SQL file, docs(11-patterns) commit for the PATTERNS.md fix
    - `npx supabase db push` exit 0
  </acceptance_criteria>
  <done>Pre-deploy emergency restore SQL committed; (if applicable) DBHY-02 amendment committed; Migration 14 applied to production via supabase db push; commits in git history per D-07.</done>
</task>

<task type="checkpoint:human-verify" id="14-01-08" gate="blocking">
  <name>Task 08b: Post-deploy verification — lint + post-deploy functiondef diff + post-deploy catalog assertion + smoke vote + evidence capture</name>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-CONTEXT.md (D-01 smoke vote instructions, D-03 evidence format, D-04 post-deploy lint)
    - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md (Manual-Only Verifications section)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt (for before/after comparison)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql (W0 production baseline — for the functiondef diff)
    - .planning/phases/14-security-definer-search-path-migration/evidence/catalog-assertion-local.txt (Task 02-05 local catalog baseline — for cross-check)
  </read_first>
  <what-built>
    Post-deploy verification is the phase gate. FIVE checks must pass. Codex MEDIUM "make the body-identical check machine-enforced post-deploy" is addressed here.

    CHECK 1 — Post-deploy lint (D-04, second of two lint runs):
      npx supabase db lint --linked 2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/post-deploy-lint.txt
      POST_WARN_COUNT=$(grep -c '0011_function_search_path_mutable' .planning/phases/14-security-definer-search-path-migration/evidence/post-deploy-lint.txt || echo 0)
    Expected:
      - If W0 OUTCOME R1: POST_WARN_COUNT == 0 (all 7 functions cleared, DBHY-02 literal)
      - If W0 OUTCOME R2: POST_WARN_COUNT == 1 (only rls_auto_enable residual; DBHY-02 amended carve-out applies); the residual WARN must be FOR `rls_auto_enable` ONLY — verify with `grep rls_auto_enable evidence/post-deploy-lint.txt`. Any OTHER function in the WARN list is a blocker.

    CHECK 2 — Post-deploy machine-enforced `pg_get_functiondef` diff against pre-deploy snapshot (addresses Codex MEDIUM):
    Pull the post-deploy production functiondef for every target function and diff against the pre-deploy snapshot from W0. The ONLY expected difference is the `SET search_path` value line.

      psql "$PROD_DB_URL_OR_LINKED_CONNECTION" -At -c "
        SELECT pg_get_functiondef(p.oid) || ';'
        FROM pg_proc p
        JOIN pg_namespace n ON n.oid = p.pronamespace
        WHERE n.nspname = 'public'
          AND p.proname IN (
            'is_current_user_admin',
            'increment_vote_count',
            'update_profile_after_auth',
            'handle_new_user',
            'validate_vote_choice',
            'profile_self_update_allowed'
          )
        ORDER BY p.proname;
      " > .planning/phases/14-security-definer-search-path-migration/evidence/post-deploy-functiondef-snapshot.sql

      # Normalize the SET search_path line out of both snapshots and diff:
      sed -E "s/SET search_path = ('?)[^'$]*\1/SET search_path = <NORMALIZED>/g" \
        .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-functiondef-snapshot.sql \
        > /tmp/pre-norm.sql
      sed -E "s/SET search_path = ('?)[^'$]*\1/SET search_path = <NORMALIZED>/g" \
        .planning/phases/14-security-definer-search-path-migration/evidence/post-deploy-functiondef-snapshot.sql \
        > /tmp/post-norm.sql

      diff -u /tmp/pre-norm.sql /tmp/post-norm.sql \
        > .planning/phases/14-security-definer-search-path-migration/evidence/functiondef-diff-production.txt
      DIFF_EXIT=$?

    Expected: After `SET search_path` normalization, diff returns 0 OR only minor whitespace/canonicalization differences from `pg_get_functiondef`'s output normalization. Body SQL between `$$` markers MUST be functionally identical. If a function body shows non-trivial changes, REVERT — production drift detected.

    CHECK 3 — Post-deploy catalog assertion (cross-check against local):
      psql "$PROD_DB_URL_OR_LINKED_CONNECTION" -c "
        SELECT
          p.proname,
          pg_get_function_identity_arguments(p.oid) AS args,
          p.prosecdef,
          p.provolatile,
          p.proconfig
        FROM pg_proc p
        JOIN pg_namespace n ON n.oid = p.pronamespace
        WHERE n.nspname = 'public'
          AND p.proname IN (
            'is_current_user_admin',
            'increment_vote_count',
            'update_profile_after_auth',
            'handle_new_user',
            'validate_vote_choice',
            'profile_self_update_allowed'
          )
        ORDER BY p.proname, p.oid;
      " | tee .planning/phases/14-security-definer-search-path-migration/evidence/catalog-assertion-production.txt

    Verify every row has `prosecdef=t`, `proconfig` containing `search_path=`, and the function name appears exactly ONCE (no accidental overloads). `is_current_user_admin` must have `provolatile=s`.

    CHECK 4 — Supabase Dashboard advisor screenshot:
    Open Supabase Dashboard → Database → Advisors → Security. Confirm no `0011_function_search_path_mutable` entries for the 6 (or 7) target functions. Screenshot the advisor screen showing zero or only the system-owned rls_auto_enable WARN. Save to `evidence/advisor-zero-warns.png`.

    CHECK 5 — Production submit-vote smoke (D-01):
    Per decision D-01, this is a real cast-a-vote on production:
    1. Sign in via Discord on polls.wtcsmapban.com (private browser window is fine).
    2. Find an open, non-prod-impact test poll (or ask the admin to ensure one exists).
    3. Cast a vote.
    4. Confirm the UI reflects the vote (count incremented, "you've already responded" state activates).
    5. In Supabase Studio, run:
         SELECT * FROM public.votes WHERE user_id = '<your uid>' ORDER BY created_at DESC LIMIT 1;
         SELECT * FROM public.vote_counts WHERE poll_id = '<poll uid>';
    Confirm the vote row exists in `votes` and the `vote_counts.count` incremented.
    Screenshot the Studio query output. Save to `evidence/smoke-vote-studio-queries.png`.

    After all checks pass, paste the TEST-11 exit code 0 line (from `evidence/test-11-pre-deploy.txt`), the is_current_user_admin regression exit code 0 line (from `evidence/is_current_user_admin_regression.txt`), the functiondef diff result, the catalog assertion result, and note the evidence filenames in SUMMARY.md per D-03.
  </what-built>
  <how-to-verify>
    1. `post-deploy-lint.txt` shows zero 0011 WARNs (R1) or one WARN for `rls_auto_enable` only (R2).
    2. `functiondef-diff-production.txt` shows no semantic body change vs pre-deploy snapshot after `SET search_path` normalization.
    3. `catalog-assertion-production.txt` shows correct prosecdef/provolatile/proconfig for every function; no overloads.
    4. Dashboard advisor screenshot matches lint output.
    5. Studio query output confirms vote row in `public.votes` and incremented count in `public.vote_counts`.
    6. All evidence files exist in `.planning/phases/14-security-definer-search-path-migration/evidence/`.
  </how-to-verify>
  <resume-signal>Type "VERIFIED: lint = [0 WARNs | 1 WARN rls_auto_enable only per W0 carve-out], functiondef diff = [SEMANTIC IDENTICAL | DRIFT — investigate], catalog = [CORRECT prosecdef/provolatile/proconfig, no overloads], smoke vote = OK, evidence captured" to complete the phase and write SUMMARY.md.</resume-signal>
  <acceptance_criteria>
    - `evidence/post-deploy-lint.txt` exists; `grep -c '0011_function_search_path_mutable' evidence/post-deploy-lint.txt` == 0 (R1) OR == 1 with the single line referencing `rls_auto_enable` ONLY (R2, with DBHY-02 amendment commit in git history per W0)
    - `evidence/post-deploy-functiondef-snapshot.sql` exists and contains `CREATE OR REPLACE FUNCTION public.` for every target function (6 or 7 entries)
    - `evidence/functiondef-diff-production.txt` exists; the diff shows ONLY the `SET search_path` line change (after normalization, no semantic body change — formatting-only differences from pg_get_functiondef canonicalization are acceptable)
    - `evidence/catalog-assertion-production.txt` exists; each target function appears exactly once with `prosecdef=t`, `proconfig` containing `search_path=`; `is_current_user_admin` has `provolatile=s`
    - `evidence/advisor-zero-warns.png` screenshot exists
    - `evidence/smoke-vote-studio-queries.png` screenshot exists showing vote row in public.votes and incremented public.vote_counts row
    - TEST-11 exit code 0, is_current_user_admin regression exit code 0, and functiondef diff result referenced in SUMMARY.md (per D-03)
    - SUMMARY.md created at `.planning/phases/14-security-definer-search-path-migration/14-01-SUMMARY.md` following the standard template
  </acceptance_criteria>
  <done>Zero 0011 WARNs for user-owned functions (R1 = literal zero; R2 = one rls_auto_enable WARN per amended DBHY-02 carve-out); machine-enforced production functiondef diff confirms body-identical post-deploy; production catalog assertion confirms prosecdef/provolatile/proconfig correct with no overloads; submit-vote round-trip confirmed; TEST-11 and is_current_user_admin regression pass outputs captured; evidence folder contains all artifacts; SUMMARY.md written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres function body → schema resolution | SECURITY DEFINER functions execute with the function owner's role; under mutable `search_path`, an object with the same name in a user-controlled schema can shadow a builtin or trusted object |
| Migration file → production database | `supabase db push` carries service-role credentials; an error in the migration SQL applies atomically per function but could break a hot path |
| pg_get_functiondef diff → is_current_user_admin semantics | If the body drifts from the migration 9 form, admin RLS policies across 7+ tables silently change behavior (grant or deny access incorrectly) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | `public.is_current_user_admin()` — body drift breaks admin RLS on 7+ tables | mitigate | Body-identical rewrite verified via `pg_get_functiondef` diff against migration 9 baseline (Task 02-05). TEST-11 12-cell RLS matrix re-run provides runtime defense-in-depth (Task 07). |
| T-14-02 | Tampering | `public.increment_vote_count()` — 42P01 error ("relation does not exist") under `search_path = ''` if any bare table reference slips through | mitigate | RESEARCH.md confirmed body already uses `INSERT INTO public.vote_counts`. Task 01 acceptance criteria: `grep -n 'INSERT INTO' ...` returns only fully-qualified lines. Task 02-05: local `supabase db reset` and `supabase db lint --local` confirm zero errors before push. |
| T-14-03 | Tampering | SECURITY DEFINER functions owned by wrong role after CREATE OR REPLACE — if the executor accidentally uses `CREATE FUNCTION` instead of `CREATE OR REPLACE FUNCTION`, a new function entry is created with different ownership | mitigate | All blocks use `CREATE OR REPLACE FUNCTION` (locked decision per D-Phase-14). Task 01 acceptance: `grep -c 'CREATE OR REPLACE FUNCTION'` count verified. OID preserved; trigger `on_vote_inserted` continues to reference `increment_vote_count` by the original OID (A1 assumption — standard Postgres behavior). |
| T-14-09 | Tampering | `update_profile_after_auth` overload not fully hardened — `CREATE OR REPLACE FUNCTION` does NOT replace across signatures. If a stale 3-param SECURITY DEFINER overload remains in production from migration 2, Migration 14 hardens only the 4-param signature and the 3-param overload remains with mutable `search_path` → residual `0011_function_search_path_mutable` WARN → DBHY-02 not met | mitigate | W0 Check 1B enumerates ALL `update_profile_after_auth` overloads via `pg_get_function_identity_arguments`. Outcome U1 (only 4-param) ⇒ no extra action. Outcome U2 (both overloads) ⇒ Task 01 adds `DROP FUNCTION IF EXISTS public.update_profile_after_auth(BOOLEAN, TEXT, TEXT);` OR a second `CREATE OR REPLACE FUNCTION` block hardening the 3-param signature. Post-deploy catalog assertion (Task 08b CHECK 3) confirms every retained overload has `proconfig` containing `search_path=`. Addresses Codex HIGH H4-cycle2. |
| T-14-04 | Tampering | Production push fails partway — migration history in mixed state | mitigate | Each `CREATE OR REPLACE FUNCTION` is an idempotent DDL statement. If the push fails mid-migration, re-running `supabase db push` re-applies the migration without side effects. Rollback path (D-08): paste the prior function body from the source migration into Supabase Studio SQL editor — no paired down-migration needed. |
| T-14-05 | Denial of Service (admin lockout) | Broken `is_current_user_admin()` locks out admin Studio path, preventing fix-forward via the admin role | mitigate | W0 Check 3 pre-stages `evidence/pre-deploy-functiondef-snapshot.sql` with the current production `pg_get_functiondef` for every target function BEFORE `supabase db push`. If admin Studio path is locked out post-deploy, the restore SQL is executable via the service-role key (which bypasses RLS) using the Supabase CLI or a direct service-role psql session. Addresses Gemini + Codex MEDIUM concern on rollback path viability. |
| T-14-06 | Tampering | `is_current_user_admin()` body drift undetected — TEST-11 alone tests `vote_counts` policy which has no admin OR-branch, so it does not strongly prove is_current_user_admin behavior on other admin-gated tables; AND naive direct-psql regression fixtures can pass for the wrong reason (privileged session bypasses RLS) | mitigate | Task 07b authors `tests/sql/is_current_user_admin_regression.sql` exercising 4 identity branches (admin/non-admin/mfa_false/guild_false) AND admin-gated RLS on `audit_log`. Cycle-2 fixture hardenings: (a) seeds `auth.users` FK before profiles, (b) uses correct `(before, after)` columns, (c) no top-level `PERFORM` (PL/pgSQL-only), (d) wraps every RLS-asserting SELECT in `SET LOCAL ROLE authenticated; ... RESET ROLE;` so RLS is actually exercised (a privileged psql session would bypass RLS, making the non-admin assertion pass for the wrong reason). Task 02-05 catalog assertion further confirms `provolatile=s` (STABLE preserved). Addresses Codex HIGH H1+H3 and Gemini MEDIUM M1. |
| T-14-07 | Tampering | Production body drift between local and production after deploy — local lint clean but production runtime broken | mitigate | Task 08b runs post-deploy `pg_get_functiondef` capture and `diff -u` against the W0 pre-deploy snapshot, with `SET search_path` line normalization. Any body diff beyond the expected single-line change blocks phase closure. Addresses Codex MEDIUM "post-deploy functiondef diff" concern. |
| T-14-08 | Tampering | Extension-provided function (`gen_random_uuid`, `uuid_generate_v4`, `citext`) called bare under `search_path = ''` — 42883 "function does not exist" at runtime | mitigate | W0 Check 2 greps all source migration bodies for bare extension function calls and bare `auth.uid()` calls. If any matches found, Task 01 must use the `extensions.<fn>` (or `auth.<fn>`) qualifier. Addresses Gemini MEDIUM concern on extension function qualification. |
| T-14-SC | Tampering | npm/npx package installs (supabase CLI) | accept | No new packages installed in this phase. Supabase CLI is invoked via `npx supabase` using the project's existing npm toolchain. No new package-manager install tasks. |
</threat_model>

<verification>
Phase gate (all must pass before marking Phase 14 complete):

1. `supabase/migrations/00000000000014_harden_security_definer_search_path.sql` exists in the repository; `git log --oneline supabase/migrations/00000000000014_*` shows the `feat(db)` commit.
2. `npx supabase db reset --local` exits 0 (all 14 migrations apply cleanly on local stack).
3. `npx supabase db lint --linked 2>&1 | grep -c '0011_function_search_path_mutable'` == 0 against production for W0 OUTCOME R1; == 1 with the single line referencing `rls_auto_enable` ONLY for W0 OUTCOME R2 (and the DBHY-02 amendment commit exists in git history).
4. TEST-11 (`e2e/integration/vote-counts-rls.test.ts`) 12-cell matrix: `npx vitest run e2e/integration/vote-counts-rls.test.ts` exits 0 (exit-code based, not PASS/pass string grep).
5. `tests/sql/is_current_user_admin_regression.sql` exists, is executable (FK-correct: seeds `auth.users` first; schema-correct: uses `(actor_id, action, target_type, target_id, before, after)`; SQL-correct: no top-level `PERFORM`; RLS-correct: every RLS-asserting SELECT is wrapped in `SET LOCAL ROLE authenticated; ... RESET ROLE;`), and `psql -v ON_ERROR_STOP=1 -f` exits 0 with 6 PASS notices; addresses cycle-2 HIGH concerns H1 + H3 + M1 + M2 that TEST-11 alone does not prove `is_current_user_admin()` behavior on admin-gated tables.
6. Production `submit-vote` smoke: vote row in `public.votes`, `public.vote_counts.count` incremented — confirmed via Studio query; screenshot captured.
7. `grep -c 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` == 0.
8. Machine-enforced post-deploy `pg_get_functiondef` diff between `evidence/pre-deploy-functiondef-snapshot.sql` and `evidence/post-deploy-functiondef-snapshot.sql` shows ONLY the `SET search_path` line change (after normalization) for every target function — no body semantic drift.
9. Post-deploy catalog assertion (`evidence/catalog-assertion-production.txt`) shows every target function with `prosecdef=t`, `proconfig` containing `search_path=`, and the expected overload count per name: every function has exactly ONE entry (no accidental overloads) — `update_profile_after_auth` has exactly ONE row if W0 selected U1 or U2-DROP, OR exactly TWO rows (3-param and 4-param, both with `proconfig` containing `search_path=`) if W0 selected U2-harden-both; `is_current_user_admin` has `provolatile=s`. Any stale unhardened overload (e.g., a 3-param `update_profile_after_auth` without `search_path=` in `proconfig`) is a blocker.
10. Emergency restore artifact `evidence/pre-deploy-functiondef-snapshot.sql` exists and was committed BEFORE `supabase db push`.
11. Evidence folder at `.planning/phases/14-security-definer-search-path-migration/evidence/` contains: `pre-deploy-lint.txt`, `post-deploy-lint.txt`, `test-11-pre-deploy.txt`, `is_current_user_admin_regression.txt`, `pre-deploy-functiondef-snapshot.sql`, `post-deploy-functiondef-snapshot.sql`, `functiondef-diff-local.txt`, `functiondef-diff-production.txt`, `catalog-assertion-local.txt`, `catalog-assertion-production.txt`, `local-lint.txt`, plus at minimum one screenshot file for advisor and one for Studio queries (per D-03).
12. SUMMARY.md exists at `.planning/phases/14-security-definer-search-path-migration/14-01-SUMMARY.md` with TEST-11 exit-code line, is_current_user_admin regression exit-code line, functiondef diff result, catalog assertion result, and evidence filenames referenced.
</verification>

<success_criteria>
Phase 14 is complete when:
- Supabase advisor (`supabase db lint --linked`) shows zero `0011_function_search_path_mutable` WARNs for all user-owned pre-Phase-11 SECURITY DEFINER functions (R1 = literal zero; R2 = one carve-out WARN for `rls_auto_enable` only, with the DBHY-02 amendment commit recorded)
- A production `submit-vote` round-trip succeeds end-to-end post-deploy (vote inserted, trigger fires, vote_counts incremented)
- TEST-11 12-cell RLS matrix passes with zero regressions (exit code 0)
- Direct `is_current_user_admin()` regression fixture passes against local stack — 4 identity branches and at least one admin-gated RLS table beyond `vote_counts` (audit_log) exercised
- Machine-enforced post-deploy `pg_get_functiondef` diff confirms every target function body is semantically identical to pre-deploy (only `SET search_path` value changed)
- Post-deploy catalog assertion confirms `prosecdef`, `provolatile` (STABLE on is_current_user_admin), `proconfig`, and `proowner` correct; no UNHARDENED overloads (every `update_profile_after_auth` row present in production has `proconfig` containing `search_path=` per W0 U-outcome decision)
- Emergency restore SQL pre-staged before deploy (rollback path usable via service-role even if admin Studio path is locked out)
- Extension-function and `auth.uid()` audits clean (per W0 Check 2)
- Migration 14 SQL file exists in the repository and local `supabase db reset` applies all 14 migrations cleanly
- `11-PATTERNS.md` line 83 drift removed; REVIEW-FIX-H3 named as the shipped vote_counts policy form
</success_criteria>

<output>
Create `.planning/phases/14-security-definer-search-path-migration/14-01-SUMMARY.md` when done.
Include:
- requirements-completed (DBHY-01, DBHY-02, DBHY-03, DBHY-04)
- W0 outcomes:
  - rls_auto_enable: R1 user-owned / R2 system-owned with DBHY-02 amendment commit hash / R3 escalated
  - update_profile_after_auth overload: U1 only 4-param / U2 both overloads with DROP-or-harden decision / U3 only 3-param (stop)
- TEST-11 exit code line pasted verbatim
- is_current_user_admin regression exit code line + 6 PASS notice lines pasted verbatim
- Machine-enforced functiondef diff result (semantic identical vs body drift)
- Catalog assertion result (prosecdef/provolatile/proconfig per function; no overloads)
- Extension/auth.uid audit result from W0 (clean or matches handled)
- Evidence file references (pre/post-deploy lint, pre/post-deploy functiondef snapshots, functiondef diffs, catalog assertions, regression output, advisor screenshot, Studio screenshot)
- rls_auto_enable resolution (system-owned or user-owned)
- Function count (6 or 7)
- Review concerns addressed:
  - Cycle 1 — Codex HIGH (TEST-11 inadequacy → Task 07b regression; rls_auto_enable/DBHY-02 ambiguity → W0 Check 1), Gemini/Codex MEDIUM (rollback robustness → W0 Check 3 pre-deploy snapshot; functiondef diff machine-enforced → Task 02-05 + Task 08b; catalog assertion → Task 02-05 + Task 08b; extension function audit → W0 Check 2), Codex LOW (exit-code based TEST-11 → Task 07a)
  - Cycle 2 — Codex HIGH H1 (Task 07b fixture FK + columns + PERFORM fixed), Codex HIGH H3 + Gemini MEDIUM M1 (SET LOCAL ROLE authenticated wraps RLS-asserting SELECTs), Codex HIGH H4 (W0 Check 1B `update_profile_after_auth` overload enumeration U1/U2/U3 + Task 01 explicit DROP-or-harden), Codex MEDIUM M3 (Query A includes `pg_get_functiondef`), Codex LOW L1 (R2/R3 overlap tightened — extension-owned ⇒ R2 unambiguously)
</output>
