---
phase: 14-security-definer-search-path-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00000000000014_harden_security_definer_search_path.sql
  - .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md
  - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md
  - .planning/phases/14-security-definer-search-path-migration/evidence/
autonomous: false
requirements:
  - DBHY-01
  - DBHY-02
  - DBHY-03
  - DBHY-04

must_haves:
  truths:
    - "`supabase db lint --linked` against production shows zero `0011_function_search_path_mutable` WARNs for all target functions"
    - "One production `submit-vote` round-trip succeeds — vote row inserted, `increment_vote_count` trigger fires, `vote_counts` row incremented"
    - "TEST-11 12-cell RLS matrix (`e2e/integration/vote-counts-rls.test.ts`) passes with zero regressions post-migration"
    - "Migration 14 SQL file exists with `CREATE OR REPLACE FUNCTION … SECURITY DEFINER … SET search_path = ''` for every target function and `supabase db reset --local` applies all migrations cleanly"
    - "`11-PATTERNS.md` line 83 no longer implies the admin-OR bypass form may be current; REVIEW-FIX-H3 is named as the decision"
  artifacts:
    - path: "supabase/migrations/00000000000014_harden_security_definer_search_path.sql"
      provides: "search_path hardening for 6 (or 7) pre-Phase-11 SECURITY DEFINER functions"
      contains: "CREATE OR REPLACE FUNCTION"
    - path: ".planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md"
      provides: "corrected vote_counts policy prose (DBHY-04)"
      contains: "REVIEW-FIX-H3"
    - path: ".planning/phases/14-security-definer-search-path-migration/evidence/"
      provides: "lint output + Studio screenshot confirming vote row + TEST-11 pass output"
  key_links:
    - from: "supabase/migrations/00000000000014_harden_security_definer_search_path.sql"
      to: "public.is_current_user_admin()"
      via: "CREATE OR REPLACE FUNCTION — only SET search_path value changes"
      pattern: "SET search_path = ''"
    - from: "supabase/migrations/00000000000014_harden_security_definer_search_path.sql"
      to: "public.increment_vote_count()"
      via: "CREATE OR REPLACE FUNCTION — body verbatim from migration 2; no body change"
      pattern: "INSERT INTO public.vote_counts"
    - from: "e2e/integration/vote-counts-rls.test.ts"
      to: "public.is_current_user_admin()"
      via: "Postgres RLS policy evaluated by supabase-js SELECT"
      pattern: "vote-counts-rls"

user_setup:
  - service: supabase-production
    why: "Production push (supabase db push) and post-deploy lint require authenticated CLI session"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"
    dashboard_config:
      - task: "Verify rls_auto_enable ownership (Wave 0)"
        location: "Supabase Dashboard → Database → Functions → filter by rls_auto_enable"
      - task: "Post-deploy evidence screenshot"
        location: "Supabase Dashboard → Database → Advisors → Security (confirm zero 0011 WARNs)"
      - task: "Post-deploy smoke vote evidence"
        location: "Supabase Dashboard → Table Editor → public.votes and public.vote_counts"
---

<objective>
Harden all pre-Phase-11 SECURITY DEFINER Postgres functions by adding `SET search_path = ''` and confirming body references are fully schema-qualified. Clear all `0011_function_search_path_mutable` advisor WARNs from production. Fix one prose drift in `11-PATTERNS.md` (DBHY-04).

Purpose: The Supabase security advisor flags 7 functions created before the project standardised on `SET search_path = ''`. An attacker or misconfigured session with write access to `public` schema could shadow a builtin and redirect these functions' behavior. Clearing the WARNs also removes noise from the advisor dashboard and demonstrates security hygiene for v1.3.

Output: `supabase/migrations/00000000000014_harden_security_definer_search_path.sql` (6 or 7 functions), corrected `11-PATTERNS.md` prose at line 83, evidence folder with lint output + Studio screenshot + TEST-11 pass output.
</objective>

<execution_context>
@/Users/khaiphan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khaiphan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-security-definer-search-path-migration/14-CONTEXT.md
@.planning/phases/14-security-definer-search-path-migration/14-RESEARCH.md
@.planning/phases/14-security-definer-search-path-migration/14-PATTERNS.md
@.planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md

<interfaces>
<!-- Key migration bodies the executor needs. Extracted from prior research. No codebase exploration needed. -->

From supabase/migrations/00000000000009_admin_integrity_rls.sql (authoritative is_current_user_admin):
```sql
CREATE OR REPLACE FUNCTION public.is_current_user_admin()
RETURNS BOOLEAN
LANGUAGE sql
SECURITY DEFINER
STABLE
SET search_path = public   -- Migration 14: change ONLY this value to ''
AS $$
  SELECT COALESCE(
    (SELECT is_admin AND mfa_verified AND guild_member
     FROM public.profiles WHERE id = auth.uid()),
    false
  );
$$;
```

From supabase/migrations/00000000000002_triggers.sql lines 172-188 (authoritative increment_vote_count):
-- CRITICAL: INSERT INTO public.vote_counts is ALREADY fully qualified.
-- CONTEXT.md claim of bare reference is INCORRECT per RESEARCH.md.
-- Migration 14 adds ONLY SET search_path = ''. No body change.
```sql
CREATE OR REPLACE FUNCTION public.increment_vote_count()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.vote_counts (poll_id, choice_id, count)
  VALUES (NEW.poll_id, NEW.choice_id, 1)
  ON CONFLICT (poll_id, choice_id) DO UPDATE
  SET count = public.vote_counts.count + 1;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

From supabase/migrations/00000000000003_guild_membership.sql lines 41-62 (authoritative update_profile_after_auth — 4-param signature):
-- CRITICAL: Use 4-param signature. 3-param would create a new overload and clear zero WARNs.
```sql
CREATE OR REPLACE FUNCTION public.update_profile_after_auth(
  p_mfa_verified BOOLEAN,
  p_discord_username TEXT,
  p_avatar_url TEXT,
  p_guild_member BOOLEAN DEFAULT FALSE
)
RETURNS void AS $$
BEGIN
  UPDATE public.profiles
  SET
    mfa_verified = p_mfa_verified,
    discord_username = p_discord_username,
    avatar_url = p_avatar_url,
    guild_member = p_guild_member,
    updated_at = NOW()
  WHERE id = auth.uid();
  IF NOT FOUND THEN
    RAISE EXCEPTION 'Profile not found for current user';
  END IF;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

From 14-PATTERNS.md — 11-PATTERNS.md DBHY-04 drift location:
Line 83 current (drift): "...and the v1.0.5 admin-bypass policy in `migrations/00000000000005_admin_phase4.sql:75-88` (whichever is current)..."
Line 83 replacement: "...and the v1.0.5 admin-bypass policy at `migrations/00000000000005_admin_phase4.sql:75-88` (both superseded). The shipped policy (migration 10, REVIEW-FIX-H3) has NO `is_current_user_admin()` OR-bypass..."
Lines 85-101 SQL block: CORRECT as-is — DO NOT MODIFY.
</interfaces>
</context>

<tasks>

<!-- ================================================================== -->
<!-- WAVE 0: Pre-flight — rls_auto_enable ownership + local stack check  -->
<!-- ================================================================== -->

<task type="checkpoint:human-verify" id="14-01-W0" gate="blocking">
  <name>Task W0: Verify rls_auto_enable ownership and local stack reachability</name>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-RESEARCH.md (Open Questions section — rls_auto_enable)
    - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md (Wave 0 Requirements)
    - supabase/config.toml (to confirm linked project ref and local stack config)
  </read_first>
  <what-built>
    Two pre-flight verifications that gate Migration 14 scope:

    1. **`rls_auto_enable` ownership check** — determines whether Migration 14 targets 6 or 7 functions.
       Run this query in Supabase Studio SQL editor (linked project) or via CLI:
         SELECT pg_get_functiondef(oid), proowner::regrole
         FROM pg_proc
         WHERE proname = 'rls_auto_enable'
           AND pronamespace = 'public'::regnamespace;
       Alternative CLI (if Studio not available):
         npx supabase db dump --linked --schema=public | grep -A 10 rls_auto_enable

    2. **Local stack reachability check** — confirm `supabase db reset --local` applies migrations 0–13 cleanly before Migration 14 is written:
         npx supabase db reset --local
       (Exit 0 = local stack healthy. If this fails, fix the stack before Wave 1.)

    3. **Smoke fixture check** — confirm whether `tests/sql/increment_vote_count_smoke.sql` already exists:
         ls tests/sql/increment_vote_count_smoke.sql 2>/dev/null && echo EXISTS || echo MISSING
       If MISSING, create it with: INSERT INTO public.votes (poll_id, choice_id, user_id) VALUES ('<test-poll-id>', '<test-choice-id>', auth.uid()); SELECT count FROM public.vote_counts WHERE poll_id = '<test-poll-id>' AND choice_id = '<test-choice-id>';
       Note: The smoke fixture is a reference SQL script for manual inspection during Wave 1; it does not need to be runnable end-to-end in local Postgres without seeded data.
  </what-built>
  <how-to-verify>
    1. Run the `pg_get_functiondef` query above in Supabase Studio.
    2. Record the outcome: (A) function found in public schema with user-writable body → Migration 14 targets 7 functions, include rls_auto_enable as Function 7; (B) function not found in public schema → Migration 14 targets 6 functions, the advisor WARN for rls_auto_enable may not be clearable by user migration.
    3. Run `npx supabase db reset --local` and confirm exit 0.
    4. Check for `tests/sql/increment_vote_count_smoke.sql`.
  </how-to-verify>
  <resume-signal>
    Type "W0: rls_auto_enable = [SYSTEM|USER body: paste here], local stack = [OK|BROKEN], smoke fixture = [EXISTS|CREATED]" to proceed to Wave 1. The function count (6 or 7) locked at this step drives Task 01 scope.
  </resume-signal>
  <acceptance_criteria>
    - rls_auto_enable ownership documented (system-owned → 6 functions; user-owned → 7 functions)
    - `npx supabase db reset --local` exits 0 with migrations 0–13 applied
    - tests/sql/ directory exists (create it if not: `mkdir -p tests/sql`)
  </acceptance_criteria>
  <done>rls_auto_enable ownership confirmed, function count locked, local stack healthy, smoke fixture present or noted as test-data-dependent.</done>
</task>

<!-- ================================================================== -->
<!-- WAVE 1: Author Migration 14 + local apply + local lint              -->
<!-- ================================================================== -->

<task type="auto" id="14-01-01" tdd="false">
  <name>Task 01: Author Migration 14 — CREATE OR REPLACE FUNCTION for 6 (or 7) target functions</name>
  <files>supabase/migrations/00000000000014_harden_security_definer_search_path.sql</files>
  <read_first>
    - supabase/migrations/00000000000002_triggers.sql (authoritative bodies for handle_new_user, validate_vote_choice, increment_vote_count; lines 86-188)
    - supabase/migrations/00000000000003_guild_membership.sql (authoritative 4-param update_profile_after_auth; lines 41-62)
    - supabase/migrations/00000000000004_fix_trigger_rpc_context.sql (authoritative profile_self_update_allowed; lines 12-45)
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (authoritative is_current_user_admin with SET search_path = public; lines 11-26)
    - supabase/migrations/00000000000013_demote_admin_guarded_advisory_lock.sql (file-header comment style template; lines 1-36)
    - supabase/migrations/00000000000007_fix_pr_review.sql (section-divider style; lines 16-18)
    - .planning/phases/14-security-definer-search-path-migration/14-PATTERNS.md (per-function templates, ordering guidance, REVOKE guidance)
  </read_first>
  <action>
    Create `supabase/migrations/00000000000014_harden_security_definer_search_path.sql`.

    FILE-LEVEL HEADER (per migration 13 style, no phase/plan rot-tags in source):
    A multi-line `--` block describing: adds `SET search_path = ''` to pre-Phase-11 SECURITY DEFINER functions flagged by advisor `0011_function_search_path_mutable`; uses `CREATE OR REPLACE FUNCTION` to preserve OIDs (trigger references); lists the 6 (or 7) cleared functions by name; notes local verification via `supabase db reset` + `supabase db lint` per D-04.

    SECTION ORDERING (lowest-risk first per RESEARCH.md — enables bisection during local testing):
    1. validate_vote_choice
    2. handle_new_user
    3. profile_self_update_allowed
    4. update_profile_after_auth
    5. is_current_user_admin
    6. increment_vote_count
    7. rls_auto_enable (ONLY if W0 confirmed user-owned in public schema)

    EACH FUNCTION BLOCK must:
    - Open with a `-- =====================================================================` section divider: `-- FUNCTION N: <name>`
    - Use `CREATE OR REPLACE FUNCTION public.<name>(...)` with the EXACT signature from the source migration listed in read_first (matching the latest migration that defines that function)
    - Include `SECURITY DEFINER` — never omit; its absence silently converts to SECURITY INVOKER
    - Include `SET search_path = ''` in the pre-AS options block (not post-`$$`)
    - Use the pre-AS options form: `LANGUAGE plpgsql / SECURITY DEFINER / SET search_path = ''` then `AS $$ ... $$;`
    - Copy the function body VERBATIM from the authoritative source migration — the only change is adding `SET search_path = ''`

    SPECIFIC FUNCTION NOTES:
    - `is_current_user_admin`: copy body verbatim from migration 9 (lines 11-26). Change ONLY `SET search_path = public` → `SET search_path = ''`. Retain `STABLE` keyword — omitting it degrades to VOLATILE and disables per-statement RLS caching.
    - `increment_vote_count`: body copied verbatim from migration 2 (lines 172-188). The `INSERT INTO public.vote_counts` and `public.vote_counts.count` are ALREADY fully qualified — no body change. Per RESEARCH.md CRITICAL FINDING, CONTEXT.md's bare-reference claim is incorrect. Only add `SET search_path = ''`.
    - `update_profile_after_auth`: use the 4-param signature from migration 3 `(p_mfa_verified BOOLEAN, p_discord_username TEXT, p_avatar_url TEXT, p_guild_member BOOLEAN DEFAULT FALSE)`. Using the 3-param signature creates a separate OID overload and clears zero WARNs for the live function.
    - `profile_self_update_allowed`: body from migration 4 (lines 12-45). The `current_user = session_user` comparison uses Postgres `pg_catalog` session variables — they resolve correctly under `search_path = ''` without schema qualification.
    - `validate_vote_choice` and `handle_new_user`: bodies from migration 2 (lines 86-159). All DML targets already use `public.<table>` — no body changes needed.
    - `rls_auto_enable` (conditional): include ONLY if W0 confirmed user-owned in public schema. Copy body from the source verified in W0. If system-owned, omit entirely — do not add a placeholder block.

    REVOKE: Do not add new REVOKE statements. Migration 14's sole purpose is `search_path` hardening. If any prior migration applied `REVOKE EXECUTE` for a function in this list, that grant persists after `CREATE OR REPLACE` — no re-application needed unless the prior migration explicitly used `GRANT EXECUTE` after the `REVOKE`.

    After writing the file, run these verification greps (expected values depend on W0 outcome):
    - `grep -c 'CREATE OR REPLACE FUNCTION' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` → must equal 6 or 7
    - `grep -c 'SECURITY DEFINER' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` → must equal 6 or 7 (same count)
    - `grep -c "SET search_path = ''" supabase/migrations/00000000000014_harden_security_definer_search_path.sql` → must equal 6 or 7
  </action>
  <verify>
    <automated>
      grep -c 'CREATE OR REPLACE FUNCTION' supabase/migrations/00000000000014_harden_security_definer_search_path.sql &&
      grep -c 'SECURITY DEFINER' supabase/migrations/00000000000014_harden_security_definer_search_path.sql &&
      grep -c "SET search_path = ''" supabase/migrations/00000000000014_harden_security_definer_search_path.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/00000000000014_harden_security_definer_search_path.sql` exists
    - `grep -c 'CREATE OR REPLACE FUNCTION' ... == 6` (or 7 if rls_auto_enable confirmed user-owned in W0)
    - `grep -c 'SECURITY DEFINER' ...` equals the same count as CREATE OR REPLACE FUNCTION (every function block has it)
    - `grep -c "SET search_path = ''" ...` equals the same count (every function block has it)
    - `grep -c 'INSERT INTO public.vote_counts' ...` == 1 (increment_vote_count body uses already-qualified reference; no bare `INSERT INTO vote_counts` appears anywhere in the file)
    - `grep -n 'STABLE' ...` shows STABLE on the is_current_user_admin block (not stripped)
    - `grep -n 'p_guild_member' ...` shows the 4th parameter of update_profile_after_auth is present (confirms 4-param signature)
    - File contains no fenced code block markers (```); it is valid SQL only
    - File contains no plan/phase rot-tag comments (per CLAUDE.md: WHY comments only, no round/phase IDs in source)
  </acceptance_criteria>
  <done>Migration 14 SQL file authored with correct per-function CREATE OR REPLACE blocks; grep counts match the W0-determined function count; SECURITY DEFINER and SET search_path = '' appear in every block.</done>
</task>

<task type="auto" id="14-01-02-05">
  <name>Task 02–05: Local apply + local lint + is_current_user_admin body-identical check + update_profile_after_auth signature check</name>
  <files>supabase/migrations/00000000000014_harden_security_definer_search_path.sql</files>
  <read_first>
    - supabase/migrations/00000000000014_harden_security_definer_search_path.sql (just authored in Task 01 — do NOT re-read the source migrations; use this file as the canonical state)
    - supabase/migrations/00000000000009_admin_integrity_rls.sql (lines 11-26 — is_current_user_admin authoritative body for diff comparison)
  </read_first>
  <action>
    Run the full local validation sequence in order. Fix any error in Migration 14 before proceeding to the next step.

    STEP 1 — Apply locally (DBHY-01 task 14-01-02):
      npx supabase db reset --local
    Expected: exit 0, all 14 migrations apply without error. If Migration 14 fails, read the error carefully — common causes: (a) wrong function signature creates a new overload rather than replacing the existing one, (b) missing `public.` prefix on a table reference under `search_path = ''`, (c) `STABLE` omitted on is_current_user_admin. Fix Migration 14 and re-run.

    STEP 2 — Verify is_current_user_admin body-identical (DBHY-01 task 14-01-02):
      psql "$LOCAL_DB_URL" -c "SELECT pg_get_functiondef('public.is_current_user_admin()'::regprocedure)"
    Compare the returned body to migration 9's source (read_first above). The body SQL between `AS $$` and `$$` must be byte-identical. Only the `search_path` attribute in the function options (`SET search_path = ''` instead of `SET search_path = public`) may differ. If there is any other diff, the executor introduced a body change — revert it.

    STEP 3 — Verify update_profile_after_auth signature (DBHY-01 task 14-01-04):
      psql "$LOCAL_DB_URL" -c "\df+ public.update_profile_after_auth"
    Confirm: exactly ONE function entry exists with 4 parameters (`p_mfa_verified bool, p_discord_username text, p_avatar_url text, p_guild_member bool`). If TWO entries appear (4-param and 3-param overload), Migration 14 used the wrong signature — the function with `DEFAULT FALSE` on the 4th param should replace only the 4-param form. Check Migration 14's signature block and fix if needed.

    STEP 4 — Local lint (DBHY-01 task 14-01-05):
      npx supabase db lint --local 2>&1 | grep -c '0011_function_search_path_mutable'
    Expected: 0. If any 0011 WARNs remain, they either point to a target function that was skipped in Migration 14, or to `rls_auto_enable` (which may be a system function — acceptable per W0 finding). Review the lint output to identify which function is still flagged and fix accordingly.

    STEP 5 — increment_vote_count trigger smoke (DBHY-01 task 14-01-03 — reference check only):
    Read `tests/sql/increment_vote_count_smoke.sql` and confirm the INSERT target is `public.vote_counts`. This is a reference assertion, not a runnable psql invocation (requires seeded poll/choice data). The assertion is: the migration 14 block for increment_vote_count uses `INSERT INTO public.vote_counts` (already present from the verbatim body copy).
      grep -n 'INSERT INTO' supabase/migrations/00000000000014_harden_security_definer_search_path.sql
    Expected: one line containing `INSERT INTO public.vote_counts`; no line containing bare `INSERT INTO vote_counts`.
  </action>
  <verify>
    <automated>
      npx supabase db reset --local &&
      npx supabase db lint --local 2>&1 | grep -c '0011_function_search_path_mutable' | grep -x '0' &&
      grep -n 'INSERT INTO public.vote_counts' supabase/migrations/00000000000014_harden_security_definer_search_path.sql
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db reset --local` exits 0 (all 14 migrations applied cleanly; task ID 14-01-02 pass)
    - `pg_get_functiondef('public.is_current_user_admin()'::regprocedure)` body matches migration 9 body verbatim — only `SET search_path` value differs (task ID 14-01-02 pass)
    - `\df+ public.update_profile_after_auth` returns exactly one entry with 4 parameters including `p_guild_member` (task ID 14-01-04 pass)
    - `npx supabase db lint --local 2>&1 | grep -c '0011_function_search_path_mutable'` returns 0 (task ID 14-01-05 pass) — or returns 1 only for `rls_auto_enable` if W0 confirmed it is system-owned and therefore not clearable by user migration
    - `grep -n 'INSERT INTO' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` returns only `INSERT INTO public.vote_counts`; no bare `INSERT INTO vote_counts` line (task ID 14-01-03 pass)
    - `grep -n 'STABLE' supabase/migrations/00000000000014_harden_security_definer_search_path.sql` returns the STABLE line for is_current_user_admin (not stripped)
  </acceptance_criteria>
  <done>Local stack applies Migration 14 cleanly; zero 0011 WARNs on local lint (excluding rls_auto_enable if system-owned); is_current_user_admin body-identical confirmed; update_profile_after_auth 4-param signature confirmed; increment_vote_count INSERT target is public.vote_counts.</done>
</task>

<!-- ================================================================== -->
<!-- WAVE 2: DBHY-04 doc fix + pre-deploy lint + TEST-11 matrix re-run  -->
<!-- ================================================================== -->

<task type="auto" id="14-01-06">
  <name>Task 06: DBHY-04 — Fix prose drift in 11-PATTERNS.md (line 83)</name>
  <files>.planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md</files>
  <read_first>
    - .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md (entire file — read to confirm current state of line 83 and the SQL block at lines 85-101 before editing)
    - .planning/phases/14-security-definer-search-path-migration/14-PATTERNS.md (DBHY-04 section — exact source and replacement prose, changelog note format, placement)
  </read_first>
  <action>
    Make two edits to `11-PATTERNS.md`. The SQL code block at lines 85-101 is already correct and must not be touched.

    EDIT 1 — Add changelog note (D-06 canonical form):
    Insert the following as the SECOND LINE of the file (immediately after the `# Phase 11:…` H1 heading, before the `**Mapped:**` metadata line), with a blank line before and after:
      Updated 2026-05-16 (Phase 14, DBHY-04) — original form had admin-OR drift; see REVIEW-FIX-H3.

    EDIT 2 — Replace the drift phrase at line 83 (after the changelog insertion, line numbers shift; search by content not line number):
    Current phrase to locate: "and the v1.0.5 admin-bypass policy in `migrations/00000000000005_admin_phase4.sql:75-88` (whichever is current)."
    The replacement substitutes "(whichever is current)" with "(both superseded)" and adds "The shipped policy (migration 10, REVIEW-FIX-H3) has NO `is_current_user_admin()` OR-bypass —" before the existing "admin reads go through..." clause.

    FULL REPLACEMENT LINE (replace the entire sentence that contains "whichever is current"):
    Old: "...and the v1.0.5 admin-bypass policy in `migrations/00000000000005_admin_phase4.sql:75-88` (whichever is current). The shipped policy has NO `is_current_user_admin()` OR-bypass..."
    New: "...and the v1.0.5 admin-bypass policy at `migrations/00000000000005_admin_phase4.sql:75-88` (both superseded). The shipped policy (migration 10, REVIEW-FIX-H3) has NO `is_current_user_admin()` OR-bypass..."

    The key changes are:
    - "in" → "at" (minor style fix)
    - "(whichever is current)" → "(both superseded)"
    - Insert "(migration 10, REVIEW-FIX-H3)" after "The shipped policy"

    After editing, verify:
      grep -n 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md
    Expected: no output (the drift phrase is gone).
      grep -n 'REVIEW-FIX-H3' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md
    Expected: at least 2 matches (changelog note + the prose fix; the original reference in the SQL block comment may add more).
  </action>
  <verify>
    <automated>
      grep -n 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md | wc -l | grep -x '       0' || grep -c 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md | grep -x '0'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` == 0 (drift phrase removed; task ID 14-01-06 pass)
    - `grep -n 'Updated 2026-05-16 (Phase 14, DBHY-04)' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns a match in the first 5 lines of the file (changelog note present at top)
    - `grep -n 'both superseded' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns a match (replacement phrase present)
    - `grep -n 'migration 10, REVIEW-FIX-H3' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns a match (policy source cited)
    - Lines 85-101 SQL block is unchanged — `grep -c 'is_current_user_admin' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` returns 0 inside the SQL block section (the SQL block has no admin OR-bypass; if this grep returns > 0, investigate whether the SQL was accidentally modified)
  </acceptance_criteria>
  <done>11-PATTERNS.md line 83 drift phrase removed; changelog note added at top; SQL block at lines 85-101 untouched; REVIEW-FIX-H3 named as the shipped form.</done>
</task>

<task type="auto" id="14-01-07">
  <name>Task 07: Pre-deploy lint against linked project + TEST-11 12-cell RLS matrix re-run</name>
  <files>.planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt</files>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-RESEARCH.md (Environment Availability section — TEST-11 note on ES256 bug; Validation Architecture section)
    - e2e/integration/vote-counts-rls.test.ts (read first 30 lines to confirm TEST-11 header and describe block structure; do not re-read the full test file)
    - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md (Per-Task Verification Map row 14-01-07)
  </read_first>
  <action>
    Run two pre-deploy checks. Capture output to evidence folder.

    STEP 1 — Create evidence directory:
      mkdir -p .planning/phases/14-security-definer-search-path-migration/evidence

    STEP 2 — Pre-deploy lint against linked project (D-04, first of two lint runs):
    This runs `supabase db lint` against the linked PRODUCTION project (before Migration 14 is pushed). It shows the current pre-migration WARN count as a baseline, and also validates that the CLI is authenticated and the linked project is reachable.
      npx supabase db lint --linked 2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt
    Save this output — it is the baseline WARN count before Migration 14 deploys. Expected: 7 (or 6) entries for `0011_function_search_path_mutable` for the target functions.

    STEP 3 — TEST-11 12-cell RLS matrix re-run (DBHY-03, automated portion):
    TEST-11 uses `supabase.from()` (direct Postgres RLS, not Edge Functions) and is unaffected by the local ES256 supabase-edge-runtime bug.
      npm run test:integration 2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt
    Expected: all 12 cells pass (zero failures, zero skips in the vote-counts-rls.test.ts describe block). This is a pre-deploy run against the local stack after `supabase db reset --local` (Migration 14 applied). If any cell fails, investigate before proceeding to Wave 3 — a failure here means Migration 14 introduced a body drift in `is_current_user_admin`.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt &&
      test -f .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt &&
      grep -c 'PASS\|pass' .planning/phases/14-security-definer-search-path-migration/evidence/test-11-pre-deploy.txt
    </automated>
  </verify>
  <acceptance_criteria>
    - `evidence/pre-deploy-lint.txt` exists and contains output from `npx supabase db lint --linked` (task ID 14-01-07 pass)
    - `evidence/test-11-pre-deploy.txt` exists and contains TEST-11 run output
    - `grep -c 'failed\|FAIL' evidence/test-11-pre-deploy.txt` == 0 (no failing cells in TEST-11 matrix)
    - `grep -i 'vote-counts-rls' evidence/test-11-pre-deploy.txt` returns a match confirming the correct test file ran
    - If any 0011 WARNs in `pre-deploy-lint.txt` are for functions OTHER than `rls_auto_enable` (when W0 confirmed it as system-owned), flag as a blocker before Wave 3
  </acceptance_criteria>
  <done>Pre-deploy lint output captured; TEST-11 matrix passes with zero failures against local stack with Migration 14 applied; evidence folder populated.</done>
</task>

<!-- ================================================================== -->
<!-- WAVE 3: [BLOCKING] Production push + post-deploy verification       -->
<!-- ================================================================== -->

<task type="checkpoint:human-action" id="14-01-08-push" gate="blocking">
  <name>Task 08a [BLOCKING]: Push Migration 14 to production</name>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-CONTEXT.md (D-09 local replay requirement, D-10 deploy timing, D-08 rollback strategy)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt (confirm pre-deploy baseline captured before pushing)
  </read_first>
  <what-built>
    Migration 14 has been authored (Task 01), applied locally (Tasks 02–05), DBHY-04 doc fix is in (Task 06), and pre-deploy evidence is captured (Task 07). The schema push to production is required before post-deploy verification can proceed.

    Two pre-push commits are required per D-07:
    1. `feat(db): migration 14 — security definer search_path lockdown` — commits `supabase/migrations/00000000000014_harden_security_definer_search_path.sql`
    2. `docs(11-patterns): align vote_counts skeleton with shipped REVIEW-FIX-H3 form` — commits `.planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md`

    Then push to production:
      SUPABASE_ACCESS_TOKEN=<your-token> npx supabase db push
    Or if authenticated via `npx supabase login`:
      npx supabase db push

    Note: `supabase db push` is atomic per migration. If it fails partway, Migration 14 uses `CREATE OR REPLACE FUNCTION` statements which are idempotent — safe to re-run. No paired rollback migration is needed (D-08: fix-forward via Supabase Studio if a function breaks).

    If the push requires interactive confirmation that cannot be suppressed, type `y` and confirm.
  </what-built>
  <how-to-verify>
    1. Confirm `npx supabase db push` exits 0 and shows Migration 14 applied.
    2. In Supabase Dashboard, go to Database → Migrations and confirm `00000000000014_harden_security_definer_search_path` appears in the applied list.
  </how-to-verify>
  <resume-signal>Type "PUSHED: migration 14 applied to production" to proceed to post-deploy verification (Task 08b).</resume-signal>
  <acceptance_criteria>
    - Migration 14 appears in the Supabase Dashboard → Database → Migrations list with status applied
    - Two git commits made: feat(db) commit for the SQL file, docs(11-patterns) commit for the PATTERNS.md fix
    - `npx supabase db push` exit 0
  </acceptance_criteria>
  <done>Migration 14 applied to production via supabase db push; two commits in git history per D-07.</done>
</task>

<task type="checkpoint:human-verify" id="14-01-08" gate="blocking">
  <name>Task 08b: Post-deploy verification — lint + smoke vote + evidence capture</name>
  <read_first>
    - .planning/phases/14-security-definer-search-path-migration/14-CONTEXT.md (D-01 smoke vote instructions, D-03 evidence format, D-04 post-deploy lint)
    - .planning/phases/14-security-definer-search-path-migration/14-VALIDATION.md (Manual-Only Verifications section)
    - .planning/phases/14-security-definer-search-path-migration/evidence/pre-deploy-lint.txt (for before/after comparison)
  </read_first>
  <what-built>
    Post-deploy verification is the phase gate. Three checks must pass:

    CHECK 1 — Post-deploy lint (D-04, second of two lint runs):
      npx supabase db lint --linked 2>&1 | tee .planning/phases/14-security-definer-search-path-migration/evidence/post-deploy-lint.txt
    Expected: zero `0011_function_search_path_mutable` WARNs for all user-owned target functions. If rls_auto_enable was confirmed as system-owned in W0, one WARN for it may remain — this is acceptable and should be noted in the evidence file.

    CHECK 2 — Supabase Dashboard advisor screenshot:
    Open Supabase Dashboard → Database → Advisors → Security. Confirm no `0011_function_search_path_mutable` entries for the 6 (or 7) target functions. Screenshot the advisor screen showing zero or only the system-owned rls_auto_enable WARN. Save to `evidence/advisor-zero-warns.png`.

    CHECK 3 — Production submit-vote smoke (D-01):
    Per decision D-01, this is a real cast-a-vote on production:
    1. Sign in via Discord on polls.wtcsmapban.com (private browser window is fine).
    2. Find an open, non-prod-impact test poll (or ask the admin to ensure one exists).
    3. Cast a vote.
    4. Confirm the UI reflects the vote (count incremented, "you've already responded" state activates).
    5. In Supabase Studio, run:
         SELECT * FROM public.votes WHERE user_id = '<your uid>' ORDER BY created_at DESC LIMIT 1;
         SELECT * FROM public.vote_counts WHERE poll_id = '<poll uid>';
    Confirm the vote row exists in `votes` and the `vote_counts.count` incremented.
    Screenshot the Studio query output. Save to `evidence/smoke-vote-studio-queries.png`.

    After both screenshots are captured, paste the TEST-11 pass output (from `evidence/test-11-pre-deploy.txt`) and note the evidence filenames in SUMMARY.md per D-03.
  </what-built>
  <how-to-verify>
    1. `post-deploy-lint.txt` shows zero 0011 WARNs for user-owned functions.
    2. Dashboard advisor screenshot shows the same.
    3. Studio query output confirms vote row in `public.votes` and incremented count in `public.vote_counts`.
    4. All three evidence files exist in `.planning/phases/14-security-definer-search-path-migration/evidence/`.
  </how-to-verify>
  <resume-signal>Type "VERIFIED: lint clean [N warns remain for rls_auto_enable: YES/NO], smoke vote OK, evidence captured" to complete the phase and write SUMMARY.md.</resume-signal>
  <acceptance_criteria>
    - `npx supabase db lint --linked 2>&1 | grep -c '0011_function_search_path_mutable'` == 0 (or == 1 only if W0 confirmed rls_auto_enable as system-owned; task ID 14-01-08 pass)
    - `evidence/post-deploy-lint.txt` exists and contains post-deploy output
    - `evidence/advisor-zero-warns.png` screenshot exists (or documented alternative evidence format)
    - `evidence/smoke-vote-studio-queries.png` screenshot exists showing vote row in public.votes and incremented public.vote_counts row
    - TEST-11 pass output referenced in SUMMARY.md (per D-03)
    - SUMMARY.md created at `.planning/phases/14-security-definer-search-path-migration/14-01-SUMMARY.md` following the standard template
  </acceptance_criteria>
  <done>Zero 0011 WARNs for user-owned functions; production submit-vote round-trip confirmed; TEST-11 matrix pass output captured; evidence folder contains lint output + advisor screenshot + Studio query screenshot; SUMMARY.md written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres function body → schema resolution | SECURITY DEFINER functions execute with the function owner's role; under mutable `search_path`, an object with the same name in a user-controlled schema can shadow a builtin or trusted object |
| Migration file → production database | `supabase db push` carries service-role credentials; an error in the migration SQL applies atomically per function but could break a hot path |
| pg_get_functiondef diff → is_current_user_admin semantics | If the body drifts from the migration 9 form, admin RLS policies across 7+ tables silently change behavior (grant or deny access incorrectly) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | `public.is_current_user_admin()` — body drift breaks admin RLS on 7+ tables | mitigate | Body-identical rewrite verified via `pg_get_functiondef` diff against migration 9 baseline (Task 02-05). TEST-11 12-cell RLS matrix re-run provides runtime defense-in-depth (Task 07). |
| T-14-02 | Tampering | `public.increment_vote_count()` — 42P01 error ("relation does not exist") under `search_path = ''` if any bare table reference slips through | mitigate | RESEARCH.md confirmed body already uses `INSERT INTO public.vote_counts`. Task 01 acceptance criteria: `grep -n 'INSERT INTO' ...` returns only fully-qualified lines. Task 02-05: local `supabase db reset` and `supabase db lint --local` confirm zero errors before push. |
| T-14-03 | Tampering | SECURITY DEFINER functions owned by wrong role after CREATE OR REPLACE — if the executor accidentally uses `CREATE FUNCTION` instead of `CREATE OR REPLACE FUNCTION`, a new function entry is created with different ownership | mitigate | All blocks use `CREATE OR REPLACE FUNCTION` (locked decision per D-Phase-14). Task 01 acceptance: `grep -c 'CREATE OR REPLACE FUNCTION'` count verified. OID preserved; trigger `on_vote_inserted` continues to reference `increment_vote_count` by the original OID (A1 assumption — standard Postgres behavior). |
| T-14-04 | Tampering | Production push fails partway — migration history in mixed state | mitigate | Each `CREATE OR REPLACE FUNCTION` is an idempotent DDL statement. If the push fails mid-migration, re-running `supabase db push` re-applies the migration without side effects. Rollback path (D-08): paste the prior function body from the source migration into Supabase Studio SQL editor — no paired down-migration needed. |
| T-14-SC | Tampering | npm/npx package installs (supabase CLI) | accept | No new packages installed in this phase. Supabase CLI is invoked via `npx supabase` using the project's existing npm toolchain. No new package-manager install tasks. |
</threat_model>

<verification>
Phase gate (all must pass before marking Phase 14 complete):

1. `supabase/migrations/00000000000014_harden_security_definer_search_path.sql` exists in the repository; `git log --oneline supabase/migrations/00000000000014_*` shows the `feat(db)` commit.
2. `npx supabase db reset --local` exits 0 (all 14 migrations apply cleanly on local stack).
3. `npx supabase db lint --linked 2>&1 | grep -c '0011_function_search_path_mutable'` == 0 against production (or == 1 only for confirmed-system-owned `rls_auto_enable`).
4. TEST-11 (`e2e/integration/vote-counts-rls.test.ts`) 12-cell matrix: all cells pass (`npm run test:integration` exit 0 for this test file).
5. Production `submit-vote` smoke: vote row in `public.votes`, `public.vote_counts.count` incremented — confirmed via Studio query; screenshot captured.
6. `grep -c 'whichever is current' .planning/milestones/v1.2-phases/11-schema-rls-ef-foundations/11-PATTERNS.md` == 0.
7. Evidence folder at `.planning/phases/14-security-definer-search-path-migration/evidence/` contains: `pre-deploy-lint.txt`, `post-deploy-lint.txt`, `test-11-pre-deploy.txt`, plus at minimum one screenshot file for advisor and one for Studio queries (per D-03).
8. SUMMARY.md exists at `.planning/phases/14-security-definer-search-path-migration/14-01-SUMMARY.md` with TEST-11 pass output pasted and evidence filenames referenced.
</verification>

<success_criteria>
Phase 14 is complete when:
- Supabase advisor (`supabase db lint --linked`) shows zero `0011_function_search_path_mutable` WARNs for all user-owned pre-Phase-11 SECURITY DEFINER functions
- A production `submit-vote` round-trip succeeds end-to-end post-deploy (vote inserted, trigger fires, vote_counts incremented)
- TEST-11 12-cell RLS matrix passes with zero regressions, proving `is_current_user_admin()` body-identical claim
- Migration 14 SQL file exists in the repository and local `supabase db reset` applies all 14 migrations cleanly
- `11-PATTERNS.md` line 83 drift removed; REVIEW-FIX-H3 named as the shipped vote_counts policy form
</success_criteria>

<output>
Create `.planning/phases/14-security-definer-search-path-migration/14-01-SUMMARY.md` when done.
Include: requirements-completed (DBHY-01, DBHY-02, DBHY-03, DBHY-04), TEST-11 pass output pasted verbatim, evidence file references (pre-deploy-lint.txt, post-deploy-lint.txt, advisor screenshot, Studio screenshot), rls_auto_enable resolution (system-owned or user-owned), function count (6 or 7).
</output>
