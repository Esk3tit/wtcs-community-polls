---
phase: 05-launch-hardening
plan: 10
type: execute
wave: 4
depends_on: [08, 09]
files_modified:
  - README.md
autonomous: true
requirements: []
threat_model_refs: []
must_haves:
  truths:
    - "README.md is a wholesale rewrite — zero Vite scaffold cruft remains"
    - "All 13 D-15 sections present in the specified order"
    - "docs/screenshots/*.png files are referenced via Markdown image tags"
    - "shields.io badges render on GitHub (build status, license, hosting, supabase)"
    - "Env-var reference table mirrors D-12 layout exactly, including SUPABASE_ANON_KEY under GH Actions (HIGH #3) and WITHOUT LOCAL_* keys (M5 — derived at runtime)"
    - "Tech stack table lists every Phase 5 addition (Sentry, PostHog, Playwright, Dependabot, GH Actions)"
    - "Upgrade ritual section (§12) documents how to safely bump pinned deps + flags esm.sh as a trust anchor"
    - "'Opinions not votes' framing present in § What it is + why it exists"
  artifacts:
    - path: "README.md"
      provides: "Public-product-grade README with 13 D-15 sections"
      contains: "WTCS Community Suggestions"
  key_links:
    - from: "README.md §3"
      to: "docs/screenshots/*.png"
      via: "Markdown image references"
      pattern: "docs/screenshots/[a-z-]+\\.png"
    - from: "README.md §9"
      to: ".env.example"
      via: "env-var table matches D-12 split"
      pattern: "VITE_SENTRY_DSN"
---

<objective>
Full README rewrite per D-15 — replaces the current Vite scaffold default with
a public-product-grade README organized into the 13 prescribed sections.
Audience: anyone landing on the GitHub repo (community admins, potential
contributors, WTCS members, future Khai) — NOT just engineers.

Required sections in order (from 05-CONTEXT.md D-15):
1. Hero — centered WTCS logo + one-line tagline
2. Shields.io badges — build status, license, hosting, Supabase
3. Live screenshots / GIFs — topics list, suggestion with results, admin,
   mobile
4. What it is + why it exists ("opinions not votes" framing)
5. Tech stack summary
6. Local development setup
7. Running tests
8. Deploying (push-to-main, EF deploy, env-var setup)
9. Env-var reference table (mirrors D-12 split — HIGH #3 adds SUPABASE_ANON_KEY; M5 removes LOCAL_*)
10. Project structure overview
11. Contributing notes (issues, PRs, scope of v1 vs out-of-scope)
12. Upgrade ritual for pinned deps (npm outdated, Dependabot review, esm.sh
    trust-anchor note)
13. License (Apache 2.0)

Aesthetic rules from CONTEXT: clean Markdown, table-formatted env vars +
tech stack, real committed screenshots (not external hosts), no emoji
clutter, no Vite scaffold cruft.

Purpose: closes D-15 — launch-grade public README.

Output: Single `README.md` at repo root, replacing the Vite scaffold default.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-launch-hardening/05-CONTEXT.md
@.planning/phases/05-launch-hardening/05-RESEARCH.md
@.planning/phases/05-launch-hardening/05-UI-SPEC.md
@.env.example
@LICENSE
@package.json

<interfaces>
<!-- 05-CONTEXT.md § D-15 — full 13-section spec + aesthetic rules -->
<!-- 05-UI-SPEC.md § Cross-Reference: README Visual Treatment -->
<!-- PROJECT.md — "opinions not votes" framing, terminology map, core value, -->
<!--   constraints, out-of-scope items for §11 -->
<!-- .env.example — canonical D-12 env-var list to mirror in §9 -->

<!-- Screenshot paths (from Plan 05-09, must exist): -->
<!--   docs/screenshots/topics-list.png -->
<!--   docs/screenshots/suggestion-with-results.png -->
<!--   docs/screenshots/admin-shell.png -->
<!--   docs/screenshots/mobile-view.png -->

<!-- WTCS logo path (existing, confirmed in 04-UAT test 2 pass): -->
<!--   public/wtcs-logo.png -->

<!-- Badge URLs (use shields.io static badges for things without live data, -->
<!-- dynamic badges where available; all badges MUST render without auth): -->
<!--   Build: https://img.shields.io/github/actions/workflow/status/<owner>/<repo>/ci.yml?branch=main -->
<!--   License: https://img.shields.io/github/license/<owner>/<repo> -->
<!--   Hosting: https://img.shields.io/badge/hosted%20on-Netlify-00C7B7?logo=netlify -->
<!--   Supabase: https://img.shields.io/badge/backend-Supabase-3ECF8E?logo=supabase -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with all 13 D-15 sections</name>
  <read_first>
    - README.md (current — Vite scaffold; confirm it needs wholesale replace)
    - .planning/PROJECT.md (opinions-not-votes framing, constraints, out-of-scope for §11)
    - .planning/phases/05-launch-hardening/05-CONTEXT.md § D-15 (13 sections + aesthetic)
    - .env.example (D-12 env-var contract to mirror in §9)
    - package.json (tech stack versions for §5)
    - LICENSE (confirm Apache 2.0 for §13)
    - public/wtcs-logo.png (confirm exists for §1 hero image)
    - docs/screenshots/ directory listing (confirm all 4 PNGs exist — blocks build if missing)
  </read_first>
  <files>README.md</files>
  <action>
    Wholesale-replace `README.md` with the 13-section structure below.
    Executor should substitute `<OWNER>` and `<REPO>` with the actual GitHub
    owner/repo (probably `khaiphan41/wtcs-community-polls` or similar — check
    `git remote get-url origin` or ask the user).

    ```markdown
    <p align="center">
      <img src="public/wtcs-logo.png" alt="WTCS" width="120" />
    </p>

    <h1 align="center">WTCS Community Suggestions</h1>

    <p align="center">
      Share opinions on competitive scene proposals with verified Discord identity.
    </p>

    <p align="center">
      <a href="https://github.com/<OWNER>/<REPO>/actions/workflows/ci.yml"><img src="https://img.shields.io/github/actions/workflow/status/<OWNER>/<REPO>/ci.yml?branch=main" alt="Build status" /></a>
      <a href="LICENSE"><img src="https://img.shields.io/github/license/<OWNER>/<REPO>" alt="License" /></a>
      <a href="https://polls.wtcsmapvote.com"><img src="https://img.shields.io/badge/hosted%20on-Netlify-00C7B7?logo=netlify" alt="Hosted on Netlify" /></a>
      <img src="https://img.shields.io/badge/backend-Supabase-3ECF8E?logo=supabase" alt="Supabase backend" />
    </p>

    ---

    ## Screenshots

    | Topics list | Suggestion with results |
    |-------------|-------------------------|
    | ![Topics list](docs/screenshots/topics-list.png) | ![Suggestion with results](docs/screenshots/suggestion-with-results.png) |

    | Admin shell | Mobile view |
    |-------------|-------------|
    | ![Admin shell](docs/screenshots/admin-shell.png) | ![Mobile view](docs/screenshots/mobile-view.png) |

    ---

    ## What it is

    WTCS Community Suggestions is a Discord-authenticated platform where the
    War Thunder Competitive Scene community shares opinions on proposals — for
    example, "Remove MiG-29 12-3 from this lineup" or "Change respawn point
    positions for El Alamein."

    This platform gathers community **opinions**, not binding votes. WTCS has
    no direct authority over War Thunder's development — only Gaijin
    Entertainment can implement game changes. What this tool does is collect
    and present organized community feedback that WTCS admins can relay to
    Gaijin.

    **Core value:** one verified Discord account, one response, no
    manipulation.

    Hosted at [polls.wtcsmapvote.com](https://polls.wtcsmapvote.com) as a
    sibling to the main WTCS Map Vote/Ban app.

    ---

    ## Tech stack

    | Layer | Tool | Version |
    |-------|------|---------|
    | Language | TypeScript | 6.0.2 |
    | UI | React | 19.2.4 |
    | Router | TanStack Router | 1.168.10 |
    | Build | Vite | 8.0.4 |
    | Design | shadcn/ui + Tailwind CSS v4 | Maia / Neutral |
    | Backend | Supabase (Postgres + Auth + Edge Functions + Storage) | — |
    | Hosting | Netlify (legacy free tier) | — |
    | Rate limiting | Upstash Redis | 1.34.x |
    | Errors | Sentry | 10.49.0 |
    | Analytics | PostHog | 1.369.3 |
    | E2E | Playwright | 1.59.1 |
    | CI/CD | GitHub Actions | — |
    | Dependency hygiene | Dependabot + `npm ci` + pinned esm.sh | — |

    All dependencies are pinned to exact versions (no `^`/`~` ranges) per the
    upgrade-ritual section below.

    ---

    ## Local development

    ```bash
    # 1. Install the Supabase CLI if you don't have it yet.
    # https://supabase.com/docs/guides/cli/getting-started

    # 2. Spin up the local Supabase stack (Postgres + Auth + Functions + Studio).
    supabase start

    # 3. Install deps (lockfile-strict).
    npm ci

    # 4. Copy `.env.example` to `.env` and fill in the values printed by `supabase start`
    # (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY). Leave VITE_SENTRY_DSN and
    # VITE_POSTHOG_KEY blank for local dev — Sentry/PostHog init is a no-op without keys.
    cp .env.example .env

    # 5. Run the dev server.
    npm run dev
    ```

    ---

    ## Running tests

    ```bash
    # Unit tests (Vitest + RTL)
    npm test

    # Type check + lint
    npm run lint

    # End-to-end smoke tests (Playwright against the preview build).
    # Requires `supabase start` to be running; supabase start auto-applies
    # supabase/seed.sql, then apply the additive E2E fixture seed:
    # psql "$(supabase status --output json | jq -r '.DB.URL')" -f e2e/fixtures/seed.sql
    npx playwright test --grep @smoke
    ```

    CI runs the full suite on every PR. See `.github/workflows/ci.yml`.

    ---

    ## Deploying

    The repo is wired for zero-friction continuous deployment:

    - **Netlify (web app):** every push to `main` triggers a Netlify build +
      deploy to `polls.wtcsmapvote.com`. Pull requests get a unique preview URL.
    - **Supabase Edge Functions:** every push to `main` that touches
      `supabase/functions/**` triggers `.github/workflows/deploy-edge-functions.yml`,
      which runs `supabase functions deploy --use-api` for all 15 functions.
    - **Cron sweep:** `.github/workflows/cron-sweep.yml` fires daily at
      `03:00 UTC`, invoking `close-expired-polls` via a secret-gated POST
      (with an `Authorization: Bearer ${SUPABASE_ANON_KEY}` header to satisfy
      the Supabase Edge Function gateway's default `verify_jwt` check).
      This keeps the Supabase free tier alive AND auto-closes expired
      suggestions.

    First-time setup for a new environment: fill in every key listed in the
    env-var table below, then trigger a deploy.

    ---

    ## Environment variables

    Three scopes, three distinct stores — never mix them up.

    ### Netlify (client build)

    These are baked into the shipped JS bundle. `VITE_*` prefix is required.

    | Key | Purpose |
    |-----|---------|
    | `VITE_SUPABASE_URL` | Public Supabase project URL |
    | `VITE_SUPABASE_ANON_KEY` | Public anon key for browser Supabase client |
    | `VITE_SENTRY_DSN` | Public Sentry ingest URL |
    | `VITE_POSTHOG_KEY` | Public PostHog project API key |
    | `VITE_COMMIT_SHA` | (optional) Netlify provides `COMMIT_REF` automatically |

    ### Netlify (build-time only, never shipped to browser)

    NO `VITE_` prefix — keeps these out of the client bundle.

    | Key | Purpose |
    |-----|---------|
    | `SENTRY_ORG` | Sentry organization slug (for sourcemap upload) |
    | `SENTRY_PROJECT` | Sentry project slug |
    | `SENTRY_AUTH_TOKEN` | Sentry auth token (scope: `project:releases` + `project:write`) |

    ### Supabase Edge Function runtime

    Set via `supabase secrets set KEY=value --project-ref <ref>`.

    | Key | Purpose |
    |-----|---------|
    | `CLOSE_SWEEPER_SECRET` | Gate for the `close-expired-polls` EF — only the GH Actions cron knows it |
    | `UPSTASH_REDIS_URL` | Upstash REST URL for rate limiting |
    | `UPSTASH_REDIS_TOKEN` | Upstash REST auth token |
    | `DISCORD_GUILD_ID` | WT esports Discord server ID (guild membership check) |

    ### GitHub Actions repo secrets

    Set via GitHub repo Settings → Secrets and variables → Actions.

    | Key | Purpose |
    |-----|---------|
    | `SUPABASE_ACCESS_TOKEN` | For `supabase functions deploy` |
    | `SUPABASE_PROJECT_REF` | Project ref for the CLI |
    | `SUPABASE_URL` | Full `https://<ref>.supabase.co` URL (used by cron workflow) |
    | `SUPABASE_ANON_KEY` | Same public key as `VITE_SUPABASE_ANON_KEY` — required by cron-sweep.yml to satisfy the EF gateway's default `verify_jwt` check |
    | `CLOSE_SWEEPER_SECRET` | Same value as the Supabase secret above |

    Local-development Supabase keys are NOT stored as GH secrets — CI derives
    them at runtime from `supabase status --output json` (they're
    deterministic fixed defaults).

    See `.env.example` in the repo root for the same contract in one place.

    ---

    ## Project structure

    ```
    src/
      components/        # React components (shadcn/ui + Tailwind)
        auth/            # AuthGuard, AuthErrorPage
        layout/          # Navbar, MobileNav
        suggestions/     # SuggestionCard, SuggestionList, SuggestionForm, SuggestionSkeleton
        admin/           # Admin shell (Tabs, CategoriesList, AdminsList, AdminSuggestionsTab)
        ui/              # shadcn primitives (Button, Dialog, Input, etc.)
        AppErrorFallback.tsx
        ConsentChip.tsx
      contexts/          # AuthContext (session + PostHog identify/reset)
      hooks/             # useAuth, useSuggestions
      lib/               # supabase client, posthog init, sentry replay loader, utils
      routes/            # TanStack Router file-based routes
      __tests__/         # Vitest + RTL

    supabase/
      functions/         # 15 Deno Edge Functions (submit-vote, create-poll, close-expired-polls, etc.)
      migrations/        # SQL schema + RLS policies
      seed.sql           # Admin seed + categories

    e2e/
      playwright.config.ts
      helpers/auth.ts    # Session injection helper (loginAs uses signInWithPassword)
      fixtures/          # test-users.ts + seed.sql (bcrypt-hashed local fixture password)
      tests/             # Four @smoke specs

    .github/
      workflows/         # ci.yml, deploy-edge-functions.yml, cron-sweep.yml
      dependabot.yml     # weekly grouped PRs

    docs/
      screenshots/       # README images
    ```

    ---

    ## Contributing

    This is a v1 community project. Scope is intentionally tight — see
    `.planning/REQUIREMENTS.md` for the full requirement list and the
    out-of-scope table.

    **In-scope for v1:** Discord OAuth with 2FA + guild membership, admin
    suggestion lifecycle, one-response-per-user with respondent-gated results,
    admin-managed categories, pin/archive, mobile-first responsive UI,
    observability + supply-chain hardening.

    **Out of scope (permanently or v2):** anonymous responses, comments,
    email notifications, ranked-choice responses, user-created suggestions,
    cross-app admin sync with the Map Vote app.

    Issues and PRs welcome at <https://github.com/<OWNER>/<REPO>/issues>.

    ---

    ## Upgrade ritual for pinned dependencies

    Every dep is pinned to an exact version. Two paths to upgrade:

    ### Automated (Dependabot)

    Dependabot opens grouped weekly PRs every Monday. Review cadence:

    1. Open the Dependabot PR.
    2. Let CI run — if it passes, merge.
    3. If CI fails, check the `npm audit` and test output for breaking
       changes. Pin to the failing version if the upgrade is risky; otherwise
       fix forward.

    Dependabot's weekly commits also reset GitHub's 60-day inactivity clock,
    which would otherwise auto-disable the scheduled cron workflow. This is
    not coincidental — it's a load-bearing architectural property.

    ### Manual (ad-hoc bumps)

    ```bash
    npm outdated                                    # see what's stale
    npm install <package>@<version> --save-exact    # no `^` in the lockfile
    npm run lint && npm test -- --run               # local gate
    git commit -am "chore(deps): bump <package> to <version>"
    ```

    ### esm.sh imports are a separate trust anchor

    Edge Functions import from `https://esm.sh/<package>@<exact-version>`.
    Dependabot does NOT scan these. To upgrade:

    1. Browse `supabase/functions/**/index.ts` — find the current pinned
       version (e.g. `@supabase/supabase-js@2.101.1`).
    2. Check the target version on npm: `npm view @supabase/supabase-js
       version`.
    3. Update every occurrence in every EF (16 files total — see
       `supabase/functions/_shared/admin-auth.ts` plus 15 function dirs).
    4. Run `supabase start` + `e2e` to verify nothing broke.
    5. Merge, let `deploy-edge-functions.yml` fire on push-to-main.

    esm.sh has no lockfile equivalent; exact-version pinning shrinks the
    window for supply-chain tampering but doesn't eliminate it. Consider
    migrating to `npm:` specifiers + a per-function `deno.json` importmap if
    the attack surface ever grows.

    ---

    ## License

    [Apache License 2.0](LICENSE).
    ```

    Substitute `<OWNER>/<REPO>` with the actual GitHub path. Use `git remote
    get-url origin` to derive it; if ambiguous, pause and ask the user.

    No emojis. No exclamation marks outside the standard Markdown syntax.
    Tone matches DESIGN-SYSTEM.md ("direct, no hype").
  </action>
  <acceptance_criteria>
    - `test -f README.md` succeeds
    - README is NOT the Vite scaffold: `grep -q 'Vite + React' README.md` returns empty (no scaffold cruft)
    - `grep -q 'WTCS Community Suggestions' README.md` succeeds (§1 hero)
    - `grep -q 'img.shields.io' README.md` succeeds (§2 badges)
    - `grep -q 'docs/screenshots/topics-list.png' README.md` succeeds (§3)
    - `grep -q 'docs/screenshots/suggestion-with-results.png' README.md` succeeds (§3)
    - `grep -q 'docs/screenshots/admin-shell.png' README.md` succeeds (§3)
    - `grep -q 'docs/screenshots/mobile-view.png' README.md` succeeds (§3)
    - `grep -q 'opinions' README.md` succeeds (§4 framing — must cite opinions-not-votes)
    - `grep -q 'Tech stack' README.md` succeeds (§5)
    - `grep -q 'Local development' README.md` succeeds (§6)
    - `grep -q 'supabase start' README.md` succeeds (§6)
    - `grep -q 'Running tests' README.md` succeeds (§7)
    - `grep -q 'playwright' README.md` succeeds (§7)
    - `grep -q 'Deploying' README.md` succeeds (§8)
    - `grep -q 'Environment variables' README.md` succeeds (§9)
    - `grep -q 'VITE_SENTRY_DSN' README.md` succeeds (§9 env table)
    - `grep -q 'CLOSE_SWEEPER_SECRET' README.md` succeeds (§9 env table)
    - `grep -q 'SUPABASE_ANON_KEY' README.md` succeeds (§9 env table — HIGH #3)
    - Tripwire (M5): `grep -q 'LOCAL_ANON_KEY' README.md` returns empty (no local-only keys in GH secrets table)
    - Tripwire (M5): `grep -q 'LOCAL_SERVICE_ROLE_KEY' README.md` returns empty
    - `grep -q 'Project structure' README.md` succeeds (§10)
    - `grep -q 'Contributing' README.md` succeeds (§11)
    - `grep -q 'Upgrade ritual' README.md` succeeds (§12)
    - `grep -q 'esm.sh' README.md` succeeds (§12 supply-chain trust-anchor note)
    - `grep -q 'Dependabot' README.md` succeeds (§12)
    - `grep -q 'License' README.md` succeeds (§13)
    - `grep -q 'Apache' README.md` succeeds (§13 — matches LICENSE file)
    - Tripwire (no emoji): `grep -cE '[😀-🙏🌀-🗿✀-➿]' README.md` returns `0` (no emoji clutter per CONTEXT aesthetic)
    - Tripwire (no Vite scaffold): `grep -q 'Currently, two official plugins' README.md` returns empty
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f README.md && test -z "$(grep \"Currently, two official plugins\" README.md)" && for k in "WTCS Community Suggestions" "img.shields.io" "docs/screenshots/topics-list.png" "opinions" "Tech stack" "supabase start" "playwright" "Environment variables" "VITE_SENTRY_DSN" "CLOSE_SWEEPER_SECRET" "SUPABASE_ANON_KEY" "Upgrade ritual" "esm.sh" "Dependabot" "Apache"; do grep -q "$k" README.md || { echo "MISSING: $k"; exit 1; }; done && test -z "$(grep LOCAL_ANON_KEY README.md)" && test -z "$(grep LOCAL_SERVICE_ROLE_KEY README.md)" && echo OK'</automated>
  </verify>
  <done>README.md replaces the Vite scaffold with 13 D-15 sections; all 4 screenshot paths referenced; env-var tables mirror D-12 split (incl. SUPABASE_ANON_KEY — HIGH #3 — and WITHOUT LOCAL_* keys — M5); upgrade ritual documents esm.sh as a trust anchor; no emoji clutter.</done>
</task>

</tasks>

<security>
## Threats Mitigated
None directly — README is documentation. But §9 env-var table and §12 upgrade-ritual section are supporting materials for every threat mitigated elsewhere.

## Residual Risks
None.
</security>

<threat_model>
## Trust Boundaries
None (documentation only).

## STRIDE Threat Register
None applicable.
</threat_model>

<verification>
- 13 sections all present (grep-driven acceptance criteria covers each)
- Screenshot paths match Plan 05-09 commits
- Env-var tables match D-12 / `.env.example` (incl. HIGH #3 addition + M5 removal)
- Upgrade ritual names Dependabot + esm.sh correctly
</verification>

<success_criteria>
- D-15 closed: public-product-grade README committed
- Zero Vite scaffold cruft remaining
- Matches CONTEXT aesthetic rules
- HIGH #3 consistency: SUPABASE_ANON_KEY documented in README's GH Actions table
- M5 consistency: no LOCAL_* keys documented (CI derives them at runtime)
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-hardening/05-10-SUMMARY.md` per template.
</output>
