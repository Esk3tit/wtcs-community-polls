---
phase: 05-launch-hardening
plan: 09
type: execute
wave: 4
depends_on: [08]
files_modified:
  - docs/screenshots/topics-list.png
  - docs/screenshots/suggestion-with-results.png
  - docs/screenshots/admin-shell.png
  - docs/screenshots/mobile-view.png
autonomous: false
requirements: []
threat_model_refs: []
must_haves:
  truths:
    - "docs/screenshots/ directory exists with at least 4 committed PNG files"
    - "Screenshots are real production captures (not Figma mocks or placeholder images)"
    - "File paths match the exact names expected by README (topics-list.png, suggestion-with-results.png, admin-shell.png, mobile-view.png)"
  artifacts:
    - path: "docs/screenshots/topics-list.png"
      provides: "Hero screenshot — public topics list on prod"
    - path: "docs/screenshots/suggestion-with-results.png"
      provides: "Results-revealed view after response submission"
    - path: "docs/screenshots/admin-shell.png"
      provides: "Admin panel view"
    - path: "docs/screenshots/mobile-view.png"
      provides: "Mobile-responsive view (UIDN-02 proof)"
  key_links:
    - from: "docs/screenshots/*.png"
      to: "README.md (Plan 05-10)"
      via: "Markdown image references in README §3"
      pattern: "!\\[.*\\]\\(docs/screenshots/.*\\.png\\)"
---

<objective>
Capture and commit the four README screenshots per D-15 §3. Must be real
production captures from `polls.wtcsmapban.com` (Plan 05-08 deployed it),
not mocks, not wireframes, not external image hosts (CONTEXT: "committed to
`docs/screenshots/` (not external image hosts)").

Screenshots split into mobile + desktop sets so the README can demonstrate
the mobile-first UIDN-02 claim with evidence.

Purpose: Wave 4 prerequisite for Plan 05-10 README rewrite — the README
references these exact paths.

Output: Four PNGs committed to `docs/screenshots/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-launch-hardening/05-CONTEXT.md
@.planning/DESIGN-SYSTEM.md

<interfaces>
<!-- 05-CONTEXT.md § D-15 §3 — "Live screenshots / GIFs — topics list, a -->
<!--   suggestion with results revealed, admin shell, mobile view" -->
<!-- 05-UI-SPEC.md § README Visual Treatment — "Committed screenshots in -->
<!--   docs/screenshots/ (no external image hosts)" -->

<!-- File name contract — Plan 05-10 README references these exact paths: -->
<!--   docs/screenshots/topics-list.png -->
<!--   docs/screenshots/suggestion-with-results.png -->
<!--   docs/screenshots/admin-shell.png -->
<!--   docs/screenshots/mobile-view.png -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture four screenshots from polls.wtcsmapban.com and commit to docs/screenshots/</name>
  <what-built>Production is live (Plan 05-08). Screenshots can only be captured by a human operating a browser at the prod URL — Claude has no headless-browser-with-auth capability in this environment.</what-built>
  <how-to-verify>
    **Prep:**
    - Sign in with Discord on `https://polls.wtcsmapban.com`
    - Ensure at least 3 fixture/real suggestions exist (browse `/topics`)
    - If not: log in as admin, create 3 sample suggestions via `/admin`
    - Make sure at least one suggestion has ≥2 responses so the results view
      shows real percentages

    **Create directory:**
    ```
    mkdir -p docs/screenshots
    ```

    **Capture settings (both desktop + mobile):**
    - Browser: Chrome or Safari (system default)
    - Zoom: 100%
    - Light mode (matches README aesthetic)
    - Window clean: no Chrome DevTools open, no bookmark bar visible
    - macOS: `Cmd-Shift-4` → Space → click window (or use Firefox/Chrome
      built-in screenshot tool)

    **1. `docs/screenshots/topics-list.png`** (desktop, ~1440×900)
    - Navigate to `/topics` while signed in
    - Full viewport capture — capture the WHOLE landing page including
      navbar + suggestion list
    - Should show at least 3 suggestion cards, category tabs, search box

    **2. `docs/screenshots/suggestion-with-results.png`** (desktop, ~1440×900)
    - Click a suggestion card
    - Submit a response (or view as a user who already responded)
    - Capture the expanded card showing result bars, percentages, raw counts
    - Ideally crop to just the card + surrounding context (not full page)

    **3. `docs/screenshots/admin-shell.png`** (desktop, ~1440×900)
    - Navigate to `/admin` (must be signed in as admin)
    - Capture the Suggestions tab with ≥1 row in the list
    - Should show the three tabs (Suggestions / Categories / Admins) + kebab
      menu hint

    **4. `docs/screenshots/mobile-view.png`** (mobile, ~390×844 — iPhone 14 Pro)
    - In Chrome/Safari DevTools, toggle device emulation to iPhone 14 Pro
      (or native device if available)
    - Navigate to `/topics`
    - Capture the full mobile viewport showing suggestion cards stacked
      single-column

    **Optimization (recommended but optional):**
    - Run each through `pngquant --quality=80-95 --ext=.png --force *.png`
      if pngquant available — keeps PNGs small enough for repo (target
      < 500 KB each)
    - OR use macOS Preview → Export → JPEG at 90% if PNG size exceeds 1 MB
      (UI-SPEC § README Visual Treatment allows PNG or WebP — executor's
      discretion)

    **Commit:**
    ```
    git add docs/screenshots/
    git commit -m "docs(05): add README screenshots"
    ```
  </how-to-verify>
  <resume-signal>Type "screenshots-committed" after all four files exist in `docs/screenshots/` and are committed to git. Or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify screenshot files exist and are reasonable sizes</name>
  <read_first>docs/screenshots/ directory listing (after Task 1)</read_first>
  <files></files>
  <action>
    Sanity-check the four files:
    - Each file exists
    - Each is a valid PNG (identify via file header magic bytes — `file
      docs/screenshots/*.png` should report "PNG image data")
    - No file is suspiciously small (< 5KB suggests a placeholder or empty)
    - No file is massive (> 2MB suggests unoptimized screenshot)

    If any file is missing, too small, or not a PNG, loop back to Task 1 with
    a specific note.
  </action>
  <acceptance_criteria>
    - `test -f docs/screenshots/topics-list.png` succeeds
    - `test -f docs/screenshots/suggestion-with-results.png` succeeds
    - `test -f docs/screenshots/admin-shell.png` succeeds
    - `test -f docs/screenshots/mobile-view.png` succeeds
    - Each file is a valid PNG: `file docs/screenshots/*.png | grep -c 'PNG image data'` returns `4`
    - Each file is > 5KB: `find docs/screenshots/ -name '*.png' -size -5k | wc -l` returns `0`
    - No file > 2MB: `find docs/screenshots/ -name '*.png' -size +2M | wc -l` returns `0` (if over, ask human to optimize — non-blocking warning is acceptable)
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'for f in topics-list suggestion-with-results admin-shell mobile-view; do test -f docs/screenshots/$f.png || { echo "MISSING: $f.png"; exit 1; }; done && [ $(file docs/screenshots/*.png | grep -c "PNG image data") = 4 ] && [ $(find docs/screenshots/ -name "*.png" -size -5k | wc -l | tr -d " ") = 0 ] && echo OK'</automated>
  </verify>
  <done>All 4 screenshot files exist, are valid PNGs, and are within reasonable size bounds. Ready for Plan 05-10 README references.</done>
</task>

</tasks>

<security>
## Threats Mitigated
None — screenshots are public-facing assets.

## Residual Risks
- Screenshots may contain real usernames/avatars from Discord. Acceptable per D-15 — the content is a WTCS community platform; captures are equivalent to visiting the live URL.
- If a screenshot includes an admin view with other admins' Discord names visible, that's already public information (admins are identified on the site). No additional masking required.
</security>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod site → screenshot file → README → GitHub | Committing to repo makes it public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-18 | Information Disclosure | screenshot content | accept | Content is inherently public (visible on live site); no PII masking required beyond what's already public |
</threat_model>

<verification>
- 4 PNG files in docs/screenshots/
- Valid PNG magic bytes
- Reasonable size bounds
</verification>

<success_criteria>
- All four screenshots captured from the live prod site
- Files committed at the exact paths Plan 05-10 will reference
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-hardening/05-09-SUMMARY.md` per template.
</output>
