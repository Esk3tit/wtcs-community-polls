---
phase: 07-observability-hardening
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - .planning/phases/07-observability-hardening/07-VERIFICATION.md
  - .planning/closure/OBSV-02-bundle-delta.md
  - .planning/phases/07-observability-hardening/artifacts/.gitkeep
autonomous: false
requirements: [OBSV-01, OBSV-02]

must_haves:
  truths:
    - "07-VERIFICATION.md exists and follows the 06-VERIFICATION.md 12-section structure scaled to Phase 7's 5 ROADMAP success criteria + 7 required artifacts (D-08, RESEARCH structural analog)"
    - "07-VERIFICATION.md ## Human Verification Required carries the four D-08 evidence rows: Sentry componentStack screenshot, un-mangled stack screenshot, permalink + release SHA, .map names[] excerpt + grep '__name(' output"
    - "Sentry event mechanism.type for the smoke event is one of {auto.function.react.error_handler, auto.function.react.error_boundary, generic} (NEVER auto.browser.global_handlers.onerror) — captured as evidence row inline (CONTEXT <specifics>, RESEARCH Pitfall 1)"
    - "Sentry event tags include boundary='app-root' (proves OBSV-01 ErrorBoundary onError belt fired — D-08 evidence #1)"
    - "Top stack frames in the Sentry event show un-mangled identifiers like RootLayout, RenderThrowSmoke, SmokePage, AppErrorFallback, or route component names (proves OBSV-02 keepNames symbolication — D-08 evidence #2)"
    - "OBSV-02-bundle-delta.md exists under .planning/closure/ with frontmatter (requirement, measured, target_pct, actual_pct, status), Method paragraph, Total gzip table, Per-chunk gzip table, Target check rationale, and Raw build output blocks (D-11, D-12, D-13, D-14)"
    - "OBSV-02-bundle-delta.md measurement granularity is total gzip delta + per-chunk gzip table comparing main baseline vs phase-7 build (D-12 — per-chunk view catches surprise hot spots even if total is fine)"
    - "Bundle-size measurement tool is Vite/Rolldown's built-in build output table (`vite build` printed per-chunk gzipped column) pasted verbatim into the closure doc — no new tooling (D-13)"
    - "Both build measurements (main baseline without keepNames, phase-7 branch with keepNames) were taken in the same session using git worktree to eliminate baseline drift (RESEARCH Pitfall 6)"
    - "Phase 7 PR's Netlify deploy preview was the verification venue (NOT vite dev, NOT Vitest — ROADMAP SC #5, CONTEXT D-07, RESEARCH Pitfall 1)"
    - "07-VERIFICATION.md sign-off line records solo verifier (Khai) — D-09: OBSV-01/02 are config + wiring not security/auth gates, and the Sentry event is independently re-openable via the permalink, so the second-human session pool stays reserved for Phase 03 UAT carry-forward in Phase 8"
  artifacts:
    - path: ".planning/phases/07-observability-hardening/07-VERIFICATION.md"
      provides: "Phase 7 closure record — 5 ROADMAP SC × status + 4 D-08 evidence rows + Sentry permalink + release SHA + .map excerpt"
      contains: "## Human Verification Required"
    - path: ".planning/closure/OBSV-02-bundle-delta.md"
      provides: "OBSV-02 bundle-size closure evidence — total + per-chunk gzip table comparing main vs phase-7 keepNames build"
      contains: "## Per-chunk gzip table"
    - path: ".planning/phases/07-observability-hardening/artifacts/"
      provides: "Directory for binary screenshot artifacts (Sentry event screenshots, optional sourcemap diff visualisations) per D-10"
      contains: ".gitkeep"
  key_links:
    - from: ".planning/phases/07-observability-hardening/07-VERIFICATION.md ## Required Artifacts"
      to: ".planning/closure/OBSV-02-bundle-delta.md"
      via: "cross-reference path in the OBSV-02 row"
      pattern: "OBSV-02-bundle-delta\\.md"
    - from: "07-VERIFICATION.md ## Human Verification Required row 4"
      to: "dist/assets/*.js.map names[] excerpt + grep '__name(' output"
      via: "verbatim paste of grep output + 5-10 lines of names[] from the deploy-preview build's sourcemap"
      pattern: "__name\\("
    - from: "07-VERIFICATION.md ## Goal Achievement table"
      to: "ROADMAP.md § Phase 7 Success Criteria 1-5"
      via: "one row per criterion citing the inline evidence row"
      pattern: "Success Criterion"
---

<objective>
Capture the four pieces of D-08 manual evidence on the Phase 7 PR's Netlify deploy preview and write them up against the 06-VERIFICATION.md structural template, then produce the OBSV-02 bundle-size closure doc by running the keepNames-vs-baseline build comparison in a single session. This is the final "is the capture path actually right end-to-end" gate — Plans 01 and 02 wired the mechanics; Plan 03 proves they work against a real Sentry event on a real deploy preview.

Purpose: ROADMAP SC #5 is explicit that dev / Vitest are insufficient. CONTEXT D-07/D-08 demand four specific evidence artifacts before the phase is called done. CONTEXT D-11..D-14 demand a dedicated OBSV-02-bundle-delta.md doc with per-chunk granularity. This plan turns those mandates into committed, audit-clean files.

Output: `.planning/phases/07-observability-hardening/07-VERIFICATION.md` (NEW), `.planning/closure/OBSV-02-bundle-delta.md` (NEW — first instance under `.planning/closure/`), `.planning/phases/07-observability-hardening/artifacts/.gitkeep` (NEW — directory shell for D-10 screenshot storage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-observability-hardening/07-CONTEXT.md
@.planning/phases/07-observability-hardening/07-RESEARCH.md
@.planning/phases/07-observability-hardening/07-PATTERNS.md
@.planning/phases/07-observability-hardening/07-VALIDATION.md
@.planning/phases/07-observability-hardening/07-01-SUMMARY.md
@.planning/phases/07-observability-hardening/07-02-SUMMARY.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VERIFICATION.md

<interfaces>
<!-- 06-VERIFICATION.md is the structural analog. Every Phase 7 verification doc convention below comes from it (frontmatter, 12 top-level sections, table column shapes). -->
<!-- Excerpt: 06-VERIFICATION.md frontmatter (lines 1-7) — NOTE: the existing doc uses ___ (underscores) as delimiters, but Phase 7 MUST use --- (hyphens) to match standard YAML frontmatter convention. -->

```yaml
---
phase: 06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden
verified: 2026-04-26T08:56:00Z
status: passed
score: 7/7 roadmap success criteria verified (plus all 7 plan must-have sets)
overrides_applied: 0
---
```

<!-- Phase 7 frontmatter target shape — adjust score gate to 5/5 ROADMAP SC, no plan must-have sets clause: -->

```yaml
---
phase: 07-observability-hardening
verified: <ISO-timestamp>
status: passed | partial
score: 5/5 roadmap success criteria verified
overrides_applied: 0
---
```

<!-- 12 top-level sections (PATTERNS file lines 261-275): -->
<!-- 1. # Phase 7: Observability Hardening — Verification Report
     2. **Phase Goal** prose paragraph
     3. **Verified:** / **Status:** / **Re-verification:** triplet
     4. ## Goal Achievement — Roadmap Success Criteria (table: # | Success Criterion | Status | Evidence)
     5. ## Required Artifacts (table: Artifact | Expected | Status | Details)
     6. ## Key Link Verification (table: From | To | Via | Status | Details)
     7. ## Behavioral Spot-Checks (table: Behavior | Command | Result | Status)
     8. ## Anti-Patterns Found (expected empty for Phase 7)
     9. ## Requirements Coverage (OBSV-01, OBSV-02 status flips with inline evidence)
    10. ## Human Verification Required (the four D-08 evidence rows live HERE)
    11. ## Gaps Summary (closes "Disposition: PASSED")
    12. Footer trailer (italicised _Verified: <ts>_ + _Verifier: Khai_) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger /__smoke?render=1 on Phase 7 PR's Netlify deploy preview and capture D-08 evidence (manual — Sentry UI inspection)</name>
  <read_first>
    - .planning/phases/07-observability-hardening/07-CONTEXT.md (D-07: manual one-shot on Phase 7 PR's deploy preview; D-08: four required evidence items; D-09: solo sign-off; D-10: artifact directory + commit-vs-gitignore policy)
    - .planning/phases/07-observability-hardening/07-RESEARCH.md (Pitfall 1 lines 471-479: dev / StrictMode masks the test → MUST verify on Netlify deploy preview production build; Pitfall 5 lines 515-527: Dedupe behavior; <specifics>: mechanism.type acceptable values)
    - .planning/phases/07-observability-hardening/07-VALIDATION.md (Manual-Only Verifications section — defines exactly what each screenshot must show)
    - ROADMAP.md § Phase 7 Success Criteria #5 (verbatim: "Verification is performed on a Netlify deploy preview — dev server / Vitest are explicitly insufficient")
  </read_first>
  <what-built>
    Plans 01 and 02 are merged into the Phase 7 PR. Netlify auto-builds a deploy preview when the PR is pushed. The preview URL has the format `https://deploy-preview-<PR#>--<site-slug>.netlify.app`. On that preview, `/__smoke?render=1` should:
    1. NOT 404 (because `VITE_NETLIFY_CONTEXT === 'deploy-preview'`, not `'production'`).
    2. Render the lazy-loaded `RenderThrowSmoke` component, which throws from its render body.
    3. Be caught by `Sentry.ErrorBoundary` (which Plan 01 wired with `beforeCapture` + `onError` and which sits inside `<StrictMode>`).
    4. Trigger `onCaughtError` (Plan 01's React 19 hook), which routes through `Sentry.reactErrorHandler()`.
    5. Send an event to the project's Sentry org with `mechanism.type` in the approved allowlist (see step C below), populated `componentStack`, `tags.boundary === 'app-root'`, and un-mangled top-frame identifiers (`RootLayout`, `RenderThrowSmoke`, `SmokePage`, route component names).
  </what-built>
  <how-to-verify>
    Push the Phase 7 branch to GitHub and open the PR. Wait for Netlify's deploy preview to finish (status check in the PR shows the preview URL). Then walk this sequence and produce all four D-08 artifacts.

    **A — Trigger the smoke (browser):**
    1. Open `https://deploy-preview-<PR#>--<site-slug>.netlify.app/__smoke?render=1` in a browser (any browser; signed-out is fine — the route does not require auth).
    2. Confirm: page renders the AppErrorFallback fallback UI (NOT a 404, NOT a blank page). If you see TanStack's default 404, the env gate is wrong — STOP and check `import.meta.env.VITE_NETLIFY_CONTEXT` was actually set in the build (`netlify.toml` Plan 01 Task 2 must have shipped).

    **B — Find the Sentry event:**
    3. Open the project's Sentry org → Issues → search for `RenderThrowSmoke: deliberate render-phase throw for Sentry verification` (the deterministic message string from `src/components/debug/RenderThrowSmoke.tsx`). The fresh event from step 1 should be the most recent.
    4. Open that event. **Before proceeding, record: (a) the event's Sentry timestamp (UTC ISO string from the event header), and (b) confirm `environment === 'production'` is NOT shown — the event environment tag should be `deploy-preview` or similar (not `production`).** If the event shows `environment: production`, you may have hit a cached or wrong event — re-trigger with a hard refresh and re-search.

    **C — Capture EVIDENCE #1 (componentStack + boundary tag — proves OBSV-01 capture path):**
    5. In the event detail view, screenshot the `Tags` panel showing `boundary: app-root` AND screenshot the `Component Stack` / `React Component Stack` panel showing the populated stack frames.
    6. Save the screenshot(s) under `.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png` (or `.jpg` — pick one, document it).
    7. Confirm `mechanism.type` (open the event's "JSON" tab → `exception.values[0].mechanism.type`). Record the actual string value.

       **APPROVED mechanism.type values (any of these is a PASS):**
       - `auto.function.react.error_handler` — emitted by `reactErrorHandler` wired to the createRoot hooks (Plan 01 Task 1 primary path)
       - `auto.function.react.error_boundary` — emitted by `Sentry.ErrorBoundary` SDK integration
       - `generic` — manual `captureException` call (the onError belt in Plan 01 Task 1)

       **FAILURE mechanism.type value:**
       - `auto.browser.global_handlers.onerror` (or any `auto.browser.*` variant) — this means the error fell through to `window.onerror` / `globalHandlersIntegration` and the React 19 hooks did NOT catch it. STOP — file Phase 7 as not-yet-done; investigate Plan 01 Task 1 wiring (createRoot options object shape). RESEARCH Pitfall 1 explicit failure mode.

    **D — Capture EVIDENCE #2 (un-mangled stack frames — proves OBSV-02 symbolication):**
    8. In the same event, expand the top exception frame. Screenshot showing the function/component identifiers as `RenderThrowSmoke`, `SmokePage`, `RootLayout`, `AppErrorFallback`, or other project component names — NOT `xR`, `$M`, or other 1-2 letter mangled identifiers.
    9. Save under `.planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png`.
       - **FAILURE MODE:** if frames show mangled names like `xR` or `$M`, the keepNames flag did not take effect or sourcemap upload failed. STOP — verify Plan 01 Task 2 (`grep -c 'keepNames: true' vite.config.ts` returns 1; the inspection build `SENTRY_AUTH_TOKEN= npx vite build` must produce maps with `grep -c '"RenderThrowSmoke"\|"RootLayout"' dist/assets/*.js.map` >= 1).

    **E — Capture EVIDENCE #3 (permalink + release SHA — long-lived event reference):**
    10. Copy the event's permalink from Sentry's URL bar (typically `https://<org>.sentry.io/issues/<id>/events/<event-id>/`).
    11. Find the release SHA on the event (Tags or Release section — should match `VITE_COMMIT_SHA` from the deploy-preview build).
    12. Record both as plain text — paste these directly into `07-VERIFICATION.md ## Human Verification Required` (Task 2 below).

    **F — Capture EVIDENCE #4 (.map names[] excerpt + __name grep — proves keepNames mechanically took effect for THIS deploy):**

    IMPORTANT: `sentryVitePlugin` deletes `.js.map` files after uploading them to Sentry as part of `npm run build`. Do NOT run `npm run build` and then try to grep the maps — they will be gone. Instead, run a separate inspection build that skips Sentry upload and skips deletion:

    13. Locally, check out the same SHA the deploy preview was built from (`git checkout <release-sha>`).
    14. Run the inspection build: `SENTRY_AUTH_TOKEN= npx vite build 2>/dev/null` (empty token → plugin skips upload and skips `filesToDeleteAfterUpload`). This produces `dist/` with `.js.map` files intact.
    15. Run: `grep -o '__name(' dist/assets/*.js | head -20` and capture the output.
    16. Run: `grep -o '"RenderThrowSmoke"\|"RootLayout"\|"AppErrorFallback"\|"SmokePage"' dist/assets/*.js.map | sort -u | head -20` to capture a names[] excerpt. These are the real project identifiers that must be present; the generic "App" string does NOT exist as a component name in this codebase — do NOT search for it.
    17. Save the captured text inline in `07-VERIFICATION.md ## Human Verification Required` row 4 (Task 2 below). DO NOT commit the raw `.map` files; they are post-build artifacts. The text excerpt is sufficient.

    **G — Optional housekeeping:**
    18. Decide on the screenshot commit policy per D-10: if the PNG is < 1MB, commit it; if > 1MB, add to `.gitignore` and reference the artifact path with a "(local-only, see Sentry permalink)" note. Permalink + text excerpts MUST be committed regardless.
    19. If `mechanism.type` was `generic` rather than `auto.function.react.*`, that is acceptable per the approved allowlist above. Record which one was observed; all three approved values pass.
  </how-to-verify>
  <resume-signal>
    Type "evidence captured" once all four artifacts (one screenshot + one screenshot + permalink-and-sha-text + names-and-grep-text) are saved/recorded, the event timestamp and environment tag are confirmed (environment must NOT be 'production'), and the mechanism.type is in the approved allowlist. Or type "FAILED: <reason>" if mechanism.type was `auto.browser.global_handlers.onerror` or stack frames were mangled.
  </resume-signal>
  <files>.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png, .planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png</files>
  <action>See &lt;how-to-verify&gt; above — this checkpoint is a manual evidence-capture step on the Phase 7 PR's Netlify deploy preview. Capture the four D-08 artifacts (componentStack screenshot, un-mangled stack screenshot, Sentry permalink + release SHA text, sourcemap names[] + __name grep text). Run the sourcemap inspection BEFORE the full build (or use the SENTRY_AUTH_TOKEN= inspection build) since sentryVitePlugin deletes maps after upload. Confirm event environment tag is NOT 'production' and record event timestamp.</action>
  <verify>
    <automated>MISSING — manual checkpoint per CONTEXT D-07/D-08; verification is solo human inspection of Sentry event mechanism.type, componentStack, and frame names. The closure receipt is recorded in Task 2's 07-VERIFICATION.md.</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png` exists.
    - File `.planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png` exists.
    - File `.planning/phases/07-observability-hardening/artifacts/sentry-mechanism-type.png` exists (per the JSON-tab screenshot in 07-VALIDATION.md Manual-Only table — captures `exception.values[0].mechanism.type` field).
    - File `.planning/phases/07-observability-hardening/artifacts/sourcemap-names-excerpt.txt` exists with `RenderThrowSmoke` substring (run: `grep -c 'RenderThrowSmoke' artifacts/sourcemap-names-excerpt.txt` returns >= 1). Also accepts `RootLayout` or `AppErrorFallback` as proof of real identifier preservation — NOT the generic "App" string.
    - File `.planning/phases/07-observability-hardening/artifacts/__name-grep.txt` exists and is non-empty (`test -s artifacts/__name-grep.txt`).
    - User-captured `mechanism.type` value MUST be `auto.function.react.error_handler`, `auto.function.react.error_boundary`, OR `generic` (NOT any `auto.browser.*` variant) — this is the GO/NO-GO sentinel for OBSV-01. Recorded inline in 07-VERIFICATION.md `## Human Verification Required` row 3.
    - Event timestamp and environment tag confirmed: environment is NOT 'production' (the event came from the deploy-preview environment, not a cached production event). Record the ISO timestamp in 07-VERIFICATION.md.
  </acceptance_criteria>
  <done>All four D-08 evidence artifacts captured: (1) screenshot of componentStack + boundary='app-root' tag, (2) screenshot of un-mangled top frames showing real project identifiers (RenderThrowSmoke, SmokePage, RootLayout — not mangled 1-2 char names), (3) Sentry permalink + release SHA copied for paste into 07-VERIFICATION.md, (4) sourcemap names[] excerpt + grep '__name(' output captured using the inspection build (SENTRY_AUTH_TOKEN= npx vite build). Mechanism.type confirmed as one of the three approved values (auto.function.react.error_handler, auto.function.react.error_boundary, generic). Event environment confirmed as 'deploy-preview' not 'production'. Event timestamp recorded.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 07-VERIFICATION.md against the 06-VERIFICATION.md template, paste D-08 evidence inline</name>
  <files>.planning/phases/07-observability-hardening/07-VERIFICATION.md, .planning/phases/07-observability-hardening/artifacts/.gitkeep</files>
  <read_first>
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VERIFICATION.md (FULL READ — structural template; Phase 7 follows the 12-section shape exactly; NOTE: that file uses ___ as frontmatter delimiters — Phase 7 MUST use --- hyphens instead)
    - .planning/phases/07-observability-hardening/07-PATTERNS.md (`07-VERIFICATION.md` NEW section — divergences from the Phase 6 template: 5/5 SC instead of 7/7, 7 artifacts instead of 8, Anti-Patterns Found expected empty)
    - .planning/phases/07-observability-hardening/07-CONTEXT.md (D-08 four evidence items, D-09 solo sign-off, D-10 artifact directory)
    - .planning/phases/07-observability-hardening/07-RESEARCH.md (Validation Architecture rows for OBSV-01/OBSV-02; <specifics> mechanism.type values)
    - .planning/ROADMAP.md § Phase 7 Success Criteria 1-5 (verbatim — these are the rows in `## Goal Achievement`)
    - The four D-08 evidence artifacts captured in Task 1
  </read_first>
  <action>
    **Step A — Create directory shell.** Create the file `.planning/phases/07-observability-hardening/artifacts/.gitkeep` (empty file). This makes the artifact directory commit-trackable per D-10. If Task 1 committed PNG screenshots in this directory, leave them; if Task 1 chose to gitignore them, add an entry like `*.png` under `.planning/phases/07-observability-hardening/artifacts/` to the existing repo `.gitignore` — but only if necessary (do NOT add a sweeping `*.png` ignore globally).

    **Step B — Create `.planning/phases/07-observability-hardening/07-VERIFICATION.md`** with the EXACT structure below. All frontmatter delimiters MUST be `---` (three hyphens), NOT `___` (three underscores). Sections 1-3 are prose; sections 4-7 are tables; sections 8-11 are tables/prose; section 12 is a footer trailer.

    ```markdown
    ---
    phase: 07-observability-hardening
    verified: <ISO-8601 timestamp at moment of writing — UTC>
    status: passed
    score: 5/5 roadmap success criteria verified
    overrides_applied: 0
    ---

    # Phase 7: Observability Hardening — Verification Report

    Production Sentry receives every render-phase React error with un-mangled, human-readable stack frames so launch-time error triage is reliable. This phase wired Sentry.reactErrorHandler() into React 19's createRoot error hooks (OBSV-01), set Rolldown's keepNames flag to preserve original function/component names through Oxc minify (OBSV-02), and shipped a permanent /__smoke render-phase throw canary so future regressions can be re-detected with one deploy-preview click.

    **Verified:** <ISO timestamp> on Phase 7 PR Netlify deploy preview at `<deploy-preview-url>` (release SHA `<short-sha>`). Sentry event timestamp: `<event-ISO-timestamp-UTC>`. Event environment: `<deploy-preview or branch-deploy — must NOT be 'production'>`.
    **Status:** passed
    **Re-verification:** `/__smoke?render=1` on any future deploy preview re-exercises the entire chain.

    ## Goal Achievement — Roadmap Success Criteria

    | # | Success Criterion | Status | Evidence |
    |---|-------------------|--------|----------|
    | 1 | Render-phase throw on Netlify deploy preview produces a Sentry event with populated React `componentStack` AND `error.value` present (OBSV-01 capture path) | ✅ pass | Event `<short event-id>`; `componentStack` populated; `tags.boundary === 'app-root'` — see `## Human Verification Required` row 1. |
    | 2 | Same event's top stack frames show original function/component names (`RootLayout`, `RenderThrowSmoke`, `SmokePage`, route components) — not `xR`/`$M` (OBSV-02 symbolication) | ✅ pass | Top frame: `RenderThrowSmoke` (un-mangled). See screenshot at `artifacts/sentry-unmangled-frames.png` and `## Human Verification Required` row 2. |
    | 3 | Built `dist/assets/*.js.map` `names[]` contains entries for kept identifiers AND chunks contain `__name(...)` calls | ✅ pass | `grep -c '__name(' dist/assets/*.js` returns `<n>`; `names[]` contains `RenderThrowSmoke`, `RootLayout`, or `AppErrorFallback`. See `## Human Verification Required` row 4. |
    | 4 | Bundle-size delta from `keepNames` documented (≤1.5% gzip target) | ✅ pass / over-target-accepted | Captured at `.planning/closure/OBSV-02-bundle-delta.md`: actual `<x.x>%` gzip delta. <"within target" or "over target by Y pp; observability gain accepted per D-14"> |
    | 5 | Verification performed on Netlify deploy preview (NOT dev / NOT Vitest — StrictMode masks render-phase capture) | ✅ pass | Deploy preview URL pinned above; mechanism.type = `<one of: auto.function.react.error_handler, auto.function.react.error_boundary, generic>` (NOT any `auto.browser.*` variant). |

    ## Required Artifacts

    | Artifact | Expected | Status | Details |
    |----------|----------|--------|---------|
    | `src/main.tsx` | createRoot called with three Sentry.reactErrorHandler-wrapped React 19 hooks; ErrorBoundary beforeCapture (scope.setTag boundary='app-root') + onError belt with tags.boundary='app-root' | ✅ exists | Plan 01 Task 1; verified by grep counts in 07-01-SUMMARY.md acceptance criteria. |
    | `vite.config.ts` | rolldownOptions.output.keepNames: true under existing build block (sourcemap: 'hidden' preserved; sentryVitePlugin untouched) | ✅ exists | Plan 01 Task 2; verified by `grep -c 'keepNames: true' vite.config.ts` returns 1. |
    | `netlify.toml` | [build].command shell-exports VITE_NETLIFY_CONTEXT=$CONTEXT (D-05) | ✅ exists | Plan 01 Task 2; verified by `grep -c 'VITE_NETLIFY_CONTEXT=\$CONTEXT' netlify.toml` returns 1. |
    | `src/components/debug/RenderThrowSmoke.tsx` | Named-export render-phase throw with deterministic message (D-01) | ✅ exists | Plan 02 Task 1. |
    | `src/routes/[__smoke].tsx` | Bracket-escaped TanStack route file with createFileRoute('/__smoke'), validateSearch, beforeLoad env-gate, lazy-loaded throw component (D-02..D-06) | ✅ exists | Plan 02 Task 2. |
    | `src/routeTree.gen.ts` | Auto-regenerated to register the __smoke route (fullPath: '/__smoke') | ✅ updated | Plan 02 Task 2 ran `npm run build` which re-ran tsr generate. |
    | `.planning/closure/OBSV-02-bundle-delta.md` | Total + per-chunk gzip table comparing main vs phase-7 builds, with target check rationale (D-11..D-14) | ✅ created | This plan, Task 3. |

    ## Key Link Verification

    | From | To | Via | Status | Details |
    |------|----|----|--------|---------|
    | `src/main.tsx` createRoot | `@sentry/react` reactErrorHandler | options object: `{ onUncaughtError, onCaughtError, onRecoverableError }` | ✅ wired | All three hooks present (Plan 01 Task 1). |
    | `src/main.tsx` Sentry.ErrorBoundary | `@sentry/react` beforeCapture + captureException | `beforeCapture={(scope) => scope.setTag('boundary','app-root')}` + `onError` belt | ✅ wired | `tags.boundary: 'app-root'` guaranteed on SDK event via beforeCapture; belt as fallback (Plan 01 Task 1). |
    | `src/routes/[__smoke].tsx` | `src/components/debug/RenderThrowSmoke` | `lazy(() => import(...).then(m => ({ default: m.RenderThrowSmoke })))` | ✅ wired | RenderThrowSmoke chunk present in `dist/assets/` (Plan 02 Task 2). |
    | `vite.config.ts` build | Rolldown `__name(fn,'orig')` helper injection | `rolldownOptions.output.keepNames: true` | ✅ wired | `grep -l '__name(' dist/assets/*.js` non-empty (Plan 01 Task 2 build). |
    | `netlify.toml` [build].command | `import.meta.env.VITE_NETLIFY_CONTEXT` in route | shell substitution `VITE_NETLIFY_CONTEXT=$CONTEXT` | ✅ wired | Deploy preview behaved correctly (`/__smoke?render=1` reachable; live prod behavior to be re-confirmed post-merge). |

    ## Behavioral Spot-Checks

    | Behavior | Command | Result | Status |
    |----------|---------|--------|--------|
    | Typecheck passes after createRoot rewrite | `npx tsc -b --noEmit` | exit 0 | ✅ pass |
    | Production build succeeds | `npm run build` | exit 0 | ✅ pass |
    | keepNames helper present in built chunks | `grep -l '__name(' dist/assets/*.js \| wc -l` | `<>= 1>` | ✅ pass |
    | Real project identifier in sourcemap names[] | `grep -c '"RenderThrowSmoke"\|"RootLayout"' dist/assets/*.js.map` (inspection build — SENTRY_AUTH_TOKEN= npx vite build) | `<>= 1>` | ✅ pass |
    | RenderThrowSmoke chunk lazy-split | `grep -l 'RenderThrowSmoke' dist/assets/*.js \| wc -l` | `<>= 1>` | ✅ pass |
    | Smoke route reaches Sentry on deploy preview | manual: open `/__smoke?render=1` | event landed | ✅ pass |
    | Sentry mechanism.type | manual: event JSON tab | `<auto.function.react.error_handler or auto.function.react.error_boundary or generic>` | ✅ pass |

    ## Anti-Patterns Found

    None. Phase 7 is config + wiring + permanent debug-route work — no business-logic surface to over-engineer.

    ## Requirements Coverage

    | REQ-ID | Pre-Phase-7 | Post-Phase-7 | Gate | Status |
    |--------|-------------|--------------|------|--------|
    | OBSV-01 | Render-phase errors swallowed by React (only setTimeout/onerror events landed at Sentry per Phase 6 baseline) | createRoot hooks + ErrorBoundary beforeCapture + belt route every render-phase error to Sentry; mechanism.type ∈ {auto.function.react.error_handler, auto.function.react.error_boundary, generic}; componentStack populated; boundary='app-root' tag set | Sentry event from `/__smoke?render=1` on deploy preview shows correct mechanism + tags + stack | ✅ pass |
    | OBSV-02 | Top stack frames mangled as `xR`/`$M`-style identifiers; symbolication useless for triage | keepNames: true injects __name(fn,'orig') helpers; Function.prototype.name preserved through Oxc; Sentry surfaces original `RootLayout`/`RenderThrowSmoke`/`SmokePage`/route names | Same Sentry event's top frames show un-mangled names AND `dist/assets/*.js.map` `names[]` contains those identifiers AND chunks carry `__name(` calls | ✅ pass |

    ## Human Verification Required

    The four D-08 evidence items (CONTEXT.md):

    | # | Evidence | Method | Location | Status |
    |---|----------|--------|----------|--------|
    | 1 | Sentry event screenshot showing populated `componentStack` + `tags.boundary === 'app-root'` (proves OBSV-01 capture path) | Open Sentry event → Tags panel + React Component Stack panel → screenshot | `.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png` | ✅ captured |
    | 2 | Sentry event screenshot showing top stack frames with un-mangled names (`RootLayout`, `RenderThrowSmoke`, `SmokePage` — NOT `xR`/`$M`) (proves OBSV-02 symbolication) | Same event → expand top frame → screenshot | `.planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png` | ✅ captured |
    | 3 | Sentry event permalink + release SHA + event timestamp + environment tag pinned in this doc | Copy URL from Sentry event view; copy release SHA from event tags; record event ISO timestamp and environment | Permalink: `<paste full URL here>` · Release SHA: `<paste short SHA here>` · Event timestamp: `<paste ISO timestamp here>` · Environment: `<paste environment tag here — must NOT be 'production'>` | ✅ pinned |
    | 4 | Built `dist/assets/*.js.map` `names[]` excerpt + `grep '__name(' dist/assets/*.js \| head` output (mechanical proof keepNames took effect for THIS release — run using SENTRY_AUTH_TOKEN= inspection build so maps are not deleted) | `SENTRY_AUTH_TOKEN= npx vite build` at release SHA → grep maps → copy ~10 lines from `names` array | See fenced blocks below | ✅ captured |

    **Mechanism type sanity check (CONTEXT `<specifics>`):** observed value was `<one of: auto.function.react.error_handler, auto.function.react.error_boundary, generic>`. Approved allowlist: `auto.function.react.error_handler` (reactErrorHandler hook path), `auto.function.react.error_boundary` (Sentry ErrorBoundary SDK), `generic` (manual captureException belt). Any `auto.browser.*` value would have been a Phase 7 failure (Pitfall 1).

    ### Evidence #4 — sourcemap names[] excerpt

    ```json
    // From dist/assets/<chunk>-<hash>.js.map (post inspection build `SENTRY_AUTH_TOKEN= npx vite build` at release SHA <short-sha>):
    "names": [
      "RenderThrowSmoke",
      "RootLayout",
      "<paste 5-10 more real project identifiers here from one of the chunks>",
      ...
    ]
    ```

    ### Evidence #4 — `__name(` helper presence

    ```text
    $ grep -o '__name(' dist/assets/*.js | head -10
    <paste actual output here — should show many lines>
    ```

    Empty output here would be a Phase 7 failure mode — keepNames did not take effect.

    ## Gaps Summary

    None. All five ROADMAP success criteria pass; both REQ rows flip from Pending → ✅ pass; no anti-patterns introduced. The `.planning/closure/OBSV-02-bundle-delta.md` doc carries the full bundle-size delta with target check rationale; if the actual delta exceeded 1.5% gzip, the doc applies the D-14 overage policy (document + ship).

    Disposition: PASSED.

    ---
    _Verified: <ISO timestamp>_
    _Verifier: Khai (solo sign-off per D-09 — OBSV-01/02 are config + wiring, not security/auth gates; the Sentry event is independently re-openable via the permalink)_
    ```

    Replace every `<placeholder>` with the actual value captured in Task 1. Do NOT leave any `<...>` token or `{{...}}` placeholder in the final committed file.

    Decision IDs implemented: D-07 (manual one-shot), D-08 (all four evidence rows), D-09 (solo sign-off recorded), D-10 (artifacts directory referenced).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-observability-hardening/07-VERIFICATION.md; test -f .planning/phases/07-observability-hardening/artifacts/.gitkeep; grep -c '## Human Verification Required' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c '## Goal Achievement' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'OBSV-01' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'OBSV-02' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'auto\.function\.react\|generic' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c '<paste\|{{' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'TODO\|XXX\|<replace_me>' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c '<deploy-preview-url>\|<short-sha>\|<ISO timestamp>' .planning/phases/07-observability-hardening/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-observability-hardening/07-VERIFICATION.md` exists.
    - File `.planning/phases/07-observability-hardening/artifacts/.gitkeep` exists.
    - `grep -c '## Goal Achievement' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Required Artifacts' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Key Link Verification' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Behavioral Spot-Checks' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Anti-Patterns Found' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Requirements Coverage' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Human Verification Required' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Gaps Summary' 07-VERIFICATION.md` returns 1.
    - `grep -c 'OBSV-01' 07-VERIFICATION.md` returns >= 2 (Goal Achievement row + Requirements Coverage row).
    - `grep -c 'OBSV-02' 07-VERIFICATION.md` returns >= 2.
    - `grep -c 'boundary.*app-root\|app-root' 07-VERIFICATION.md` returns >= 2 (component stack row + key link row).
    - `grep -c 'auto\.function\.react\|generic' 07-VERIFICATION.md` returns >= 1 (mechanism type sanity check sentence — approved allowlist values).
    - `grep -c 'auto\.browser\.' 07-VERIFICATION.md` returns 0 (the failure-mode mechanism type must NOT appear as a passing value).
    - `grep -c 'permalink\|Permalink' 07-VERIFICATION.md` returns >= 1.
    - `grep -c '__name(' 07-VERIFICATION.md` returns >= 1 (Evidence #4 grep block).
    - Placeholder check — ALL of these must return 0: `grep -c '<paste' 07-VERIFICATION.md`; `grep -c '{{' 07-VERIFICATION.md`; `grep -c 'TODO' 07-VERIFICATION.md`; `grep -c 'XXX' 07-VERIFICATION.md`; `grep -c '<replace_me>' 07-VERIFICATION.md`; `grep -c '<deploy-preview-url>' 07-VERIFICATION.md`; `grep -c '<short-sha>' 07-VERIFICATION.md`.
    - Frontmatter uses `---` delimiters (hyphens) NOT `___` (underscores): `head -1 07-VERIFICATION.md | grep -c '^---$'` returns 1.
    - Frontmatter `phase: 07-observability-hardening`; `score: 5/5 roadmap success criteria verified`; `status: passed`.
    - Event timestamp and environment tag recorded: `grep -c 'Event timestamp\|event.*timestamp\|environment.*deploy-preview' 07-VERIFICATION.md` returns >= 1.
  </acceptance_criteria>
  <done>
    `07-VERIFICATION.md` exists with the 12-section structure from `06-VERIFICATION.md`, `---` frontmatter delimiters (not `___`), frontmatter scoring 5/5 ROADMAP success criteria, all four D-08 evidence rows populated with real Task 1 captures (no placeholder tokens remain), event timestamp and environment tag recorded, mechanism.type from the approved allowlist, and the artifacts/ directory exists as a tracked location for the screenshot PNGs.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run baseline-vs-keepNames build comparison in a single session and write OBSV-02-bundle-delta.md (D-11..D-14)</name>
  <files>.planning/closure/OBSV-02-bundle-delta.md</files>
  <read_first>
    - .planning/phases/07-observability-hardening/07-CONTEXT.md (D-11: dedicated `.planning/closure/OBSV-02-bundle-delta.md`; D-12: total + per-chunk; D-13: Vite/Rolldown built-in build output; D-14: overage policy)
    - .planning/phases/07-observability-hardening/07-PATTERNS.md (`.planning/closure/OBSV-02-bundle-delta.md` NEW section — recommended structure: frontmatter + Method + Total table + Per-chunk table + Target check + Raw build output)
    - .planning/phases/07-observability-hardening/07-RESEARCH.md (Pitfall 6 lines 529-537: baseline drift mitigation — same session for both measurements)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VERIFICATION.md (`## Behavioral Spot-Checks` and `## Requirements Coverage` table styles — closest in-repo "before/after measurement" analogs)
  </read_first>
  <action>
    **Step A — Create the closure directory if it does not exist.** Run `mkdir -p .planning/closure` (this is the first instance under `.planning/closure/` per RESEARCH lines 219-220 + PATTERNS "No Analog Found" entry).

    **Step B — Run the same-session build comparison using git worktree.** RESEARCH Pitfall 6 mitigation: both measurements MUST come from the same shell session to eliminate baseline drift. Use `git worktree` to build the main baseline WITHOUT mutating the current branch — this avoids `git checkout main` branch switching which would destroy in-progress Phase 7 work.

    1. Note the current Phase 7 branch name (e.g., `gsd/phase-07-observability-hardening`).
    2. Capture the exact main-branch base commit SHA: `BASE_SHA=$(git merge-base HEAD origin/main)` (this is the SHA main was at when Phase 7 branched; it will be recorded in the closure doc for audit reproducibility).
    3. Create a temporary worktree for the baseline: `git worktree add /tmp/p07-baseline $BASE_SHA`. This checks out the baseline commit into `/tmp/p07-baseline` without touching the current working tree.
    4. In the baseline worktree: `cd /tmp/p07-baseline && rm -rf dist node_modules/.vite && npm ci && npm run build 2>&1 | tee /tmp/p07-baseline-build.log`. Capture full stdout. Vite/Rolldown prints a per-chunk table near the end including a `gzip:` column.
    5. Record the total `dist/` gzip size by summing the gzip column OR `find /tmp/p07-baseline/dist -name '*.js' -o -name '*.css' -o -name '*.html' | xargs -I{} gzip -c {} | wc -c` (alternative; record which method was used).
    6. Remove the worktree: `git worktree remove /tmp/p07-baseline --force`.
    7. Back in the Phase 7 working tree: `rm -rf dist node_modules/.vite && npm ci && npm run build 2>&1 | tee /tmp/p07-keepnames-build.log`. Capture full stdout.
    8. Sum total gzip the same way you did in step 5.

    **Step C — Write `.planning/closure/OBSV-02-bundle-delta.md`** with this EXACT structure. All frontmatter delimiters MUST be `---` (three hyphens), NOT `___` (three underscores).

    ```markdown
    ---
    requirement: OBSV-02
    measured: <ISO-8601 timestamp at moment of measurement — UTC>
    target_pct: 1.5
    actual_pct: <computed delta as a number, e.g. 0.92>
    status: <within-target | over-target-accepted>
    measured_by: Khai
    base_sha: <the BASE_SHA from git merge-base HEAD origin/main — for audit reproducibility>
    method: same-session-git-worktree (RESEARCH Pitfall 6 mitigation — worktree avoids branch mutation)
    ---

    # OBSV-02: keepNames bundle-size delta

    Compares total + per-chunk gzipped output of `dist/` between the main-branch base commit (no `keepNames`) and the Phase 7 branch (with `rolldownOptions.output.keepNames: true`). Both builds were run in the same session. The main baseline was built in a `git worktree` at the merge-base SHA so no branch switching occurred (RESEARCH Pitfall 6 mitigation — `git checkout main` would mutate branch state and depend on network; worktree is isolated and reproducible).

    ## Method

    1. Single shell session, same hardware, Node 22 (per `netlify.toml` `[build.environment]` `NODE_VERSION = "22"`).
    2. Baseline: `git worktree add /tmp/p07-baseline <BASE_SHA>` → `npm ci && npm run build`. Worktree removed after measurement.
    3. Phase-7: `rm -rf dist node_modules/.vite && npm ci && npm run build`. Both builds run with the same `npm run build` script: `tsr generate && tsc -b && vite build`.
    4. Vite/Rolldown's printed per-chunk table is captured verbatim (D-13 — no new tooling).
    5. Total gzip size: `<state which method — sum-of-vite-table-column or find+gzip+wc-c>`.
    6. Base SHA for audit: `<BASE_SHA>` (from `git merge-base HEAD origin/main` at measurement time).
    7. Per-chunk: row-by-row from the Vite/Rolldown output table; chunks present only on phase-7 (e.g. the lazy-split `RenderThrowSmoke` chunk) are annotated `(new)`.

    ## Total gzip delta

    | Build | Commit SHA | Total gzip (bytes) | Delta vs main | Delta % |
    |-------|------------|--------------------|---------------|---------|
    | main (base) | `<BASE_SHA short>` | `<bytes>` | — | — |
    | phase-7 (`gsd/phase-07-observability-hardening`) | `<short-sha>` | `<bytes>` | `+<bytes>` | `+<x.xx>%` |

    ## Per-chunk gzip table

    | Chunk | main (gzip B) | phase-7 (gzip B) | Delta B | Delta % |
    |-------|---------------|-------------------|---------|---------|
    | `<index-hash>.js` | `<n>` | `<n>` | `+<n>` | `+<x.x>%` |
    | `<vendor-or-react-hash>.js` | `<n>` | `<n>` | `+<n>` | `+<x.x>%` |
    | `<route-chunks each on its own row>` | `<n>` | `<n>` | `+<n>` | `+<x.x>%` |
    | `<RenderThrowSmoke chunk>.js` | — (n/a) | `<n>` | `+<n>` | (new) |
    | `<index-hash>.css` | `<n>` | `<n>` | `<delta>` | `<x.x>%` |

    ## Target check

    Target: ≤ 1.5% total gzip delta (D-14).
    Actual: `<x.x>%`.

    `<one of:>`
    - "`<actual>%` — within ≤1.5% target. Phase 7 acceptable as designed."
    - "`<actual>%` — over target by `<delta-pp>` pp; observability gain accepted per CONTEXT D-14, revisit if Netlify caching costs become a concern."

    ## Raw build output

    ### main baseline (worktree at base SHA <BASE_SHA>)

    ```text
    <paste the FULL Vite/Rolldown output table from /tmp/p07-baseline-build.log here — typically 30-60 lines including the per-chunk table>
    ```

    ### phase-7 with keepNames

    ```text
    <paste the FULL Vite/Rolldown output table from /tmp/p07-keepnames-build.log here>
    ```

    ---
    _Measured: <ISO timestamp>_
    _Measurer: Khai_
    _Build commands: `rm -rf dist node_modules/.vite && npm ci && npm run build` × 2 (same session, see Method)_
    _Pitfall 6 mitigation: baseline built in git worktree at `<BASE_SHA>` (from `git merge-base HEAD origin/main`); no branch switching occurred_
    ```

    Replace every `<placeholder>` with actual data from your captured logs. NO `<...>` placeholder tokens or `{{...}}` tokens may remain in the committed file. After writing the file, run the placeholder-detection check:
    ```bash
    grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' .planning/closure/OBSV-02-bundle-delta.md
    ```
    This must return no output (exit code 1). If any placeholder tokens remain, fix them before proceeding.

    Decision IDs implemented: D-11 (path under `.planning/closure/`), D-12 (total + per-chunk granularity), D-13 (Vite/Rolldown built-in tool), D-14 (overage policy applied — within-target OR over-target-accepted with rationale).
  </action>
  <verify>
    <automated>test -f .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Total gzip delta' .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Per-chunk gzip table' .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Target check' .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Raw build output' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^requirement: OBSV-02$' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^base_sha:' .planning/closure/OBSV-02-bundle-delta.md; grep -E '^status: (within-target|over-target-accepted)$' .planning/closure/OBSV-02-bundle-delta.md; grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' .planning/closure/OBSV-02-bundle-delta.md; head -1 .planning/closure/OBSV-02-bundle-delta.md | grep -c '^---$'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/closure/OBSV-02-bundle-delta.md` exists (and the `.planning/closure/` directory was created).
    - Frontmatter uses `---` delimiters (hyphens), NOT `___` (underscores): `head -1 OBSV-02-bundle-delta.md | grep -c '^---$'` returns 1.
    - Frontmatter contains `requirement: OBSV-02`, `target_pct: 1.5`, a numeric `actual_pct`, `status: within-target` OR `status: over-target-accepted`, and `base_sha:` (the merge-base commit SHA for audit reproducibility).
    - All five sections present: `## Method`, `## Total gzip delta`, `## Per-chunk gzip table`, `## Target check`, `## Raw build output`.
    - Total gzip table has exactly 2 data rows (main, phase-7).
    - Per-chunk gzip table has at least 3 data rows including at least one chunk annotated `(new)` if the lazy `RenderThrowSmoke` split produced a fresh chunk.
    - Method section references `git worktree` (not `git checkout main`) for the baseline isolation.
    - Target check section ends with one of the two D-14 sentences (within-target or over-target-accepted with revisit rationale).
    - Raw build output section contains two fenced text blocks, each at least 10 lines (the actual `npm run build` per-chunk tables).
    - Placeholder-detection: `grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' OBSV-02-bundle-delta.md` returns no output (exit 1 = no matches). `grep -c '<bytes>' OBSV-02-bundle-delta.md` returns 0. `grep -c '<x\.x>' OBSV-02-bundle-delta.md` returns 0.
    - Footer trailer present: `_Measured:`, `_Measurer:`, `_Build commands:`, `_Pitfall 6 mitigation:`.
  </acceptance_criteria>
  <done>
    `.planning/closure/OBSV-02-bundle-delta.md` exists with `---` frontmatter delimiters, a `base_sha` field for audit reproducibility, a Method paragraph documenting same-session `git worktree` measurement (no branch switching), a 2-row total gzip table, a per-chunk table with one row per `dist/` chunk, a Target check section applying the D-14 policy, raw build-output blocks pasted verbatim from `npm run build` for both branches, no placeholder tokens remaining, and a footer pinning the measurer + Pitfall 6 mitigation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → public repo | Sentry permalink + release SHA committed to `.planning/phases/07-observability-hardening/07-VERIFICATION.md`. Permalink resolves only inside the project's Sentry org (auth-gated by Sentry side). |
| documentation → public repo | `.planning/closure/OBSV-02-bundle-delta.md` exposes per-chunk byte sizes — public information already inferable from production `dist/` over HTTPS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | I (Information disclosure) | Committed Sentry permalink could be probed by scrapers (e.g. github-search-leak bots) | accept | Permalink itself is not a secret — it requires Sentry org auth to view the event. The deterministic message string (`'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'`) is benign and will appear in repo regardless. |
| T-07-11 | I (Information disclosure) | Committed bundle-size table reveals chunk sizes at a specific commit | accept | Same trust level as the deployed bundle itself — anyone can fetch the prod URL and measure. No new disclosure. |
| T-07-12 | S (Spoofing) | Manual evidence capture relies on solo sign-off (D-09); no second-human verifier | accept | CONTEXT D-09 explicit decision — OBSV-01/02 are config + wiring, not security/auth gates, and the Sentry event itself is independently re-openable via the permalink (T-07-10). The second-human session pool is reserved for Phase 03 UAT 2+3 carry-forward in Phase 8. Solo sign-off is acceptable here. |
| T-07-13 | T (Tampering) | A future maintainer could edit 07-VERIFICATION.md to claim a passing status without re-running the smoke | mitigate | The Sentry permalink + release SHA pinned in the doc are independently verifiable: anyone can open the permalink and confirm mechanism.type, componentStack, and frame names against what the doc claims. Re-verification is single-click on any future deploy preview. |
</threat_model>

<verification>
- All three artifact files exist (07-VERIFICATION.md, OBSV-02-bundle-delta.md, artifacts/.gitkeep).
- Both files use `---` frontmatter delimiters (not `___`).
- 07-VERIFICATION.md frontmatter scores 5/5 ROADMAP success criteria; status = passed.
- 07-VERIFICATION.md ## Human Verification Required has all four D-08 evidence rows populated with real captures (no `<...>` or `{{...}}` placeholder tokens).
- Event timestamp and environment tag recorded; environment confirms 'deploy-preview' not 'production'.
- OBSV-02-bundle-delta.md frontmatter has a numeric actual_pct, a `base_sha` field, and a within-target / over-target-accepted status.
- OBSV-02-bundle-delta.md per-chunk table contains at least 3 rows, one annotated `(new)` for the lazy RenderThrowSmoke chunk if it appeared.
- OBSV-02-bundle-delta.md Method section references `git worktree` (not `git checkout main`).
- Placeholder-detection: `grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' 07-VERIFICATION.md OBSV-02-bundle-delta.md` returns no output.
- Sentry event mechanism.type recorded as one of the three approved values (`auto.function.react.error_handler`, `auto.function.react.error_boundary`, `generic`) — NEVER `auto.browser.*`.
- Both ROADMAP REQ-IDs (OBSV-01, OBSV-02) flip from Pending → ✅ pass with inline evidence.
</verification>

<success_criteria>
- All four D-08 evidence artifacts captured against the Phase 7 PR's Netlify deploy preview (NOT dev, NOT Vitest — ROADMAP SC #5).
- `.planning/phases/07-observability-hardening/07-VERIFICATION.md` exists with the 12-section template scaled to Phase 7 (5 SC, 7 artifacts, no plan-must-have-set count), `---` delimiters, event timestamp + environment tag recorded, and mechanism.type from the three-value approved allowlist.
- `.planning/closure/OBSV-02-bundle-delta.md` exists with `git worktree`-based same-session baseline-vs-keepNames measurement, a `base_sha` field for audit reproducibility, total + per-chunk granularity, `---` delimiters, explicit D-14 policy application, and zero remaining placeholder tokens.
- `artifacts/.gitkeep` makes the screenshot directory a tracked path; PNGs are committed if < 1MB and gitignored otherwise per D-10.
- Sentry event mechanism.type is one of `{auto.function.react.error_handler, auto.function.react.error_boundary, generic}` — anything else is a Phase 7 failure mode (Pitfall 1).
- Top stack frames show un-mangled `RootLayout` / `RenderThrowSmoke` / `SmokePage` / route component identifiers — anything else means keepNames did not take effect or sourcemap upload failed.
- Both REQ-IDs (OBSV-01, OBSV-02) flip Pending → ✅ pass in `## Requirements Coverage`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-hardening/07-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Frontmatter must include `requirements-completed: [OBSV-01, OBSV-02]` (final evidence captured against deploy preview; both REQs flip from Pending → ✅ pass — this is the plan that marks requirements as fully completed, not merely addressed).
</output>
