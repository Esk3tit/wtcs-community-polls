---
phase: 07-observability-hardening
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
status: revised-with-reviews
files_modified:
  - .planning/phases/07-observability-hardening/07-VERIFICATION.md
  - .planning/closure/OBSV-02-bundle-delta.md
  - .planning/phases/07-observability-hardening/artifacts/.gitkeep
autonomous: false
requirements: [OBSV-01, OBSV-02]

must_haves:
  truths:
    - "07-VERIFICATION.md exists and follows the 06-VERIFICATION.md 12-section structure scaled to Phase 7's 5 ROADMAP success criteria + 7 required artifacts (D-08, RESEARCH structural analog)"
    - "07-VERIFICATION.md ## Human Verification Required carries the four D-08 evidence rows: Sentry componentStack screenshot, un-mangled stack screenshot, permalink + release SHA + event timestamp + environment, .map names[] excerpt + grep '__name(' output"
    - "PRIMARY pass criterion (Round-2 MEDIUM-1): at least one Sentry event MUST have mechanism.type === 'auto.function.react.error_handler' OR 'auto.function.react.error_boundary' (proves the React 19 root hooks or the Sentry ErrorBoundary SDK caught the render-phase throw). Additional 'generic' events from the manual onError belt are EXPECTED companions, not standalone passes. Solo 'generic' (no react.* companion) is PARTIAL — sign-off blocked pending investigation. CONTEXT <specifics>, RESEARCH Pitfall 1. Round-3 LOW-5 escalation rule: if observed strings differ from the literal allowlist BUT clearly indicate React-hook capture or React-boundary capture (e.g., a future Sentry rename), escalate to CONTEXT owner (Khai) for one-off sign-off override; record the actual string AND the override decision in 07-VERIFICATION.md."
    - "Sentry event tags include boundary='app-root' (proves OBSV-01 ErrorBoundary onError belt fired AND/OR Plan 01 Task 1 taggedHandler factory tagged the hook event — Round-2 MEDIUM-5 dedupe-resilient — D-08 evidence #1)"
    - "Sentry event environment field is 'deploy-preview' (or 'branch-deploy'), NOT 'production' — proven by the Plan 01 Task 1 Round-2 HIGH-1 fix that coalesces VITE_NETLIFY_CONTEXT into Sentry.init"
    - "Sentry event exception.values[0].value matches the deterministic smoke message 'RenderThrowSmoke: deliberate render-phase throw for Sentry verification' (Round-2 LOW-2 fix — explicitly verified)"
    - "Top stack frames in the Sentry event show un-mangled identifiers like RootLayout, RenderThrowSmoke, SmokePage, AppErrorFallback, or route component names (proves OBSV-02 keepNames symbolication — D-08 evidence #2)"
    - "OBSV-02-bundle-delta.md exists under .planning/closure/ with frontmatter (requirement, measured, target_pct, actual_pct, status), Method paragraph, Total gzip table, Per-chunk gzip table, Target check rationale, and Raw build output blocks (D-11, D-12, D-13, D-14)"
    - "OBSV-02-bundle-delta.md captures TWO deltas (Round-2 MEDIUM-3 fix): (a) keepNames-only delta = phase-7-with-keepNames minus phase-7-without-keepNames (clean per-flag attribution — the OBSV-02 commitment surface); (b) total Phase 7 delta = phase-7-with-keepNames minus main (audit-relevant shipping cost). D-14 policy applied to the keepNames-only delta."
    - "Bundle-size measurement source-of-truth (Round-3 LOW-4 fix): Vite/Rolldown's printed per-chunk gzip column is the SINGLE source of truth for total + per-chunk gzip values. The alternative `find dist/assets -name '*.js' | xargs gzip -c | wc -c` method is REMOVED from the plan — D-13 specifies 'Vite/Rolldown built-in tool' and the two methods can produce different numbers (cross-file vs per-file gzip). All gzip cells in the closure doc come from parsing Vite's printed table."
    - "All three build measurements (main baseline, phase-7-without-keepNames, phase-7-with-keepNames) were taken in the same session using git worktree to eliminate baseline drift (RESEARCH Pitfall 6). For Plan 03 Task 1 step F's local sourcemap re-inspection at the deploy-preview release SHA, a temporary worktree (`git worktree add $TMPDIR <release-sha>`) is used INSTEAD of `git checkout <release-sha>` to avoid colliding with the developer's dirty working tree on the Phase 7 branch (Round-3 LOW-2 fix)."
    - "Phase 7 PR's Netlify deploy preview was the verification venue (NOT vite dev, NOT Vitest — ROADMAP SC #5, CONTEXT D-07, RESEARCH Pitfall 1)"
    - "Sourcemap names[] inspection uses `jq -r '.names[]' dist/assets/*.js.map | grep -E '^(RenderThrowSmoke|RootLayout|AppErrorFallback)$'` (Round-2 MEDIUM-4 fix — JSON-aware extraction targets the names field, not raw grep over the whole file which would false-positive on sourcesContent). Round-3 LOW-1 fix: the Node fallback uses `JSON.parse(fs.readFileSync(f, 'utf8'))` (since Node treats `.map` files as JS, not JSON, for `require()`); the multi-file form is documented inline."
    - "07-VERIFICATION.md sign-off line records solo verifier (Khai) — D-09: OBSV-01/02 are config + wiring not security/auth gates, and the Sentry event is independently re-openable via the permalink, so the second-human session pool stays reserved for Phase 03 UAT carry-forward in Phase 8"
  artifacts:
    - path: ".planning/phases/07-observability-hardening/07-VERIFICATION.md"
      provides: "Phase 7 closure record — 5 ROADMAP SC × status + 4 D-08 evidence rows + Sentry permalink + release SHA + event timestamp + environment + .map excerpt"
      contains: "## Human Verification Required"
    - path: ".planning/closure/OBSV-02-bundle-delta.md"
      provides: "OBSV-02 bundle-size closure evidence — total + per-chunk gzip table comparing main vs phase-7-without-keepNames vs phase-7-with-keepNames builds (3-way comparison per Round-2 MEDIUM-3); gzip values sourced from Vite's printed per-chunk table per Round-3 LOW-4"
      contains: "## Per-chunk gzip table"
    - path: ".planning/phases/07-observability-hardening/artifacts/"
      provides: "Directory for binary screenshot artifacts (Sentry event screenshots, optional sourcemap diff visualisations) per D-10"
      contains: ".gitkeep"
  key_links:
    - from: ".planning/phases/07-observability-hardening/07-VERIFICATION.md ## Required Artifacts"
      to: ".planning/closure/OBSV-02-bundle-delta.md"
      via: "cross-reference path in the OBSV-02 row"
      pattern: "OBSV-02-bundle-delta\\.md"
    - from: "07-VERIFICATION.md ## Human Verification Required row 4"
      to: "dist/assets/*.js.map names[] excerpt (via jq) + grep '__name(' output"
      via: "verbatim paste of jq output + 5-10 lines of names[] from the deploy-preview build's sourcemap"
      pattern: "__name\\("
    - from: "07-VERIFICATION.md ## Goal Achievement table"
      to: "ROADMAP.md § Phase 7 Success Criteria 1-5"
      via: "one row per criterion citing the inline evidence row"
      pattern: "Success Criterion"
---

## Round-2 + Round-3 Review Revisions

This plan was patched after Round-2 cross-AI review (`07-REVIEWS-v2.md`) and again after Round-3 cross-AI review (`07-REVIEWS.md`). Surgical edits only — wave structure, plan count, locked decisions, and unchallenged tasks preserved.

### Round-2 (verified RESOLVED in Round-3 by all three reviewers)

| Concern | Severity | Source | Resolution | Where applied |
|---------|----------|--------|------------|---------------|
| HIGH-2 | HIGH | Codex | Inspection build switched from `SENTRY_AUTH_TOKEN= npx vite build` (untested assumption) to `npx vite build --mode development` (deterministic — vite.config.ts:30 `disable: mode !== 'production'` short-circuits the plugin, including its delete hook). Updated steps 13-16 of Task 1 + Task 2 verification template + Task 1 acceptance criteria. | Task 1 step F preamble + steps 13-16; Task 2 Behavioral Spot-Checks row + Evidence #4 fenced block |
| MEDIUM-1 | MEDIUM | Codex + Cursor | Two-tier mechanism-type pass criteria. PRIMARY: at least one event with `auto.function.react.error_handler` OR `auto.function.react.error_boundary`. Additional `generic` events are EXPECTED companions (the belt fires intentionally). Solo `generic` (no react.* companion) is PARTIAL — sign-off blocked pending investigation of Plan 01 Task 1 hook wiring. | Task 1 step C; Task 1 acceptance_criteria; Task 2 ## Human Verification Required mechanism row; Task 2 success_criteria |
| MEDIUM-2 | MEDIUM | Codex | Removed the contradictory `grep -c 'auto\.browser\.' 07-VERIFICATION.md returns 0` rule. The positive check (`grep -c 'auto\.function\.react\|generic' >= 1`) is sufficient; absence of `auto.browser.*` is implied by the per-event acceptance in Task 1. | Task 2 verify block + acceptance_criteria (removed the auto\.browser\. == 0 check) |
| MEDIUM-3 | MEDIUM | Codex | Bundle-delta now captures THREE worktrees in the same session: main baseline, phase-7-without-keepNames, phase-7-with-keepNames. Closure doc reports BOTH the keepNames-isolated delta (clean per-flag — the OBSV-02 commitment surface) AND the total Phase 7 delta (audit shipping cost). D-14 policy applied to the keepNames-only delta. | Task 3 action steps 1-9 (third worktree added); Task 3 closure-doc structure (3 rows in Total table; explicit "keepNames isolated delta" row in Target check) |
| MEDIUM-4 | MEDIUM | Codex | Sourcemap names[] inspection switched from raw grep (false-positives on sourcesContent) to JSON-aware extraction via `jq -r '.names[]' dist/assets/*.js.map \| grep -E '^(RenderThrowSmoke\|RootLayout\|AppErrorFallback)$'`. Node fallback documented for environments without jq. | Task 1 step F-16 + acceptance_criteria; sourcemap-names-excerpt.txt now stores jq output |
| LOW-1 | LOW | Codex | Informational — per-task atomic commit gate enforces non-zero exit blocks the task. 1-line note added in verify block. | Task 2 verify block (1-line note) |
| LOW-2 | LOW | Codex | Explicit acceptance check on `exception.values[0].value` matching the deterministic smoke message string. | Task 1 step C sub-bullet; Task 2 ## Human Verification Required new row (or extension of row 1) |
| Cursor Suggestion (rollback path) | suggestion | Cursor | New "If frames are mangled" failure-mode block in Task 1 step E. First check sourcemap upload via Sentry release artifacts API (`curl /api/0/projects/$ORG/$PROJ/releases/$RELEASE/files/`). Don't conflate keepNames-broken vs upload-pipeline-broken. | Task 1 step E "If frames are mangled" block |
| Gemini Suggestion (smoke event tag) | suggestion | Gemini | DEFERRED — env-gate already prevents prod exposure; tag adds touchpoints without proportional value at v1.1. Recorded in CONTEXT.md `## Deferred Ideas` if not already there (no plan edit). | n/a (deferred) |
| Cursor Suggestion (trim non-critical greps) | suggestion | Cursor | Dropped redundant heading-presence greps (`grep -c '## Goal Achievement'`, `grep -c '## Required Artifacts'`, `grep -c '## Key Link Verification'`) from Task 2 verify block. Kept all content greps (OBSV-01, OBSV-02, boundary, mechanism, permalink, frontmatter delimiter check) and ALL placeholder-leak detection greps. | Task 2 verify block + acceptance_criteria |

### Round-3 fixes

| Concern | Severity | Source | Resolution | Where applied |
|---------|----------|--------|------------|---------------|
| LOW-1 (Node fallback `require()` on `.map` is broken) | LOW | Codex | The Round-2 Node fallback used `require('./$f')` on `.js.map` files. Node's CommonJS loader treats `.map` as JavaScript, not JSON, so `require()` fails with a syntax error. Switched to `JSON.parse(fs.readFileSync(f, 'utf8'))`. Documented the multi-file form per Codex's snippet so the developer can grep all maps in one command. | Task 1 step G-17 Node fallback snippet rewritten |
| LOW-2 (`git checkout <release-sha>` collides with dirty tree) | LOW | Codex | Round-2 wording said "Locally, check out the same SHA the deploy preview was built from (`git checkout <release-sha>`)". Phase 7 has uncommitted/dirty files in the working tree (smoke route + RenderThrowSmoke + RouteTree.gen.ts) when the developer hits Plan 03. `git checkout` would fail or be destructive. Switched to a temporary worktree (`git worktree add $TMPDIR <release-sha>`) — same convention as Plan 03 Task 3's existing worktree machinery. | Task 1 step F-14 rewritten (worktree pattern); cleanup step added |
| LOW-4 (mixed gzip methods) | LOW | Cursor | The Round-2 plan accepted two gzip-total methods: (a) Vite's printed per-chunk gzip column summed, and (b) `find dist/assets -name '*.js' \| xargs gzip -c \| wc -c`. These can produce different numbers (cross-file vs per-file gzip). D-13 specifies "Vite/Rolldown built-in tool" — picked Vite's column as the single source of truth and removed the alternative method from the plan. | Task 3 action step B-5; Task 3 method documentation; closure-doc Method paragraph |
| LOW-5 (PRIMARY mechanism string exactness) | LOW | Cursor + Codex | If Sentry renames `auto.function.react.error_handler` / `auto.function.react.error_boundary` in a patch release, sign-off could false-fail. Added an escalation rule to Task 1 acceptance + Task 2 verification template: "If observed mechanism.type strings differ from the literal allowlist BUT clearly indicate React-hook capture or React-boundary capture (e.g., `react.errorboundary` or any future Sentry rename that semantically maps to one of the two PRIMARY paths), escalate to CONTEXT owner (Khai) for a one-off sign-off override. Record the actual string observed AND the override decision in 07-VERIFICATION.md `## Human Verification Required`." | Task 1 step C escalation paragraph; Task 2 mechanism-row template note; Task 2 acceptance_criteria escalation note |
| Round-3 consensus HIGH on Plan 02 Task 3 (curl vacuous for SPA gates) | HIGH (consensus) | Gemini + Codex + Cursor | Resolved in Plan 02 — does not affect Plan 03. Plan 03's deploy-preview verification still has primacy for the Sentry capture-path proof, as documented in Plan 02's Round-3 audit-trail row. | n/a — Plan 02 only |
| Round-3 single-reviewer MEDIUM on reactErrorHandler overload | MEDIUM (single-reviewer) | Cursor | Resolved in Plan 01 by source inspection — confirmed ADDITIVE per `node_modules/@sentry/react/build/esm/error.js:90-105` + `error.d.ts:22-27` JSDoc. No code change required. Plan 03 mechanism.type assertion (`auto.function.react.error_handler`) is unaffected — the implementation always sets that mechanism string at error.js:99 regardless of whether a callback is passed. | n/a — Plan 01 only; Plan 03 mechanism assertion unchanged |
| Round-3 consensus MEDIUM on 07-VALIDATION.md drift | MEDIUM (consensus) | Codex + Cursor | Addressed by refreshing `07-VALIDATION.md` (sibling doc, not a PLAN.md) to match the revised plans — replace `SENTRY_AUTH_TOKEN= npx vite build` with `npx vite build --mode development`, update mechanism allowlist to two-tier criterion, update Sentry.reactErrorHandler invocation-count assertions for the taggedHandler factory pattern. Original sign-off preserved with a Round-3 alignment note. | Refresh applied to 07-VALIDATION.md (separate file). |

---

<objective>
Capture the four pieces of D-08 manual evidence on the Phase 7 PR's Netlify deploy preview and write them up against the 06-VERIFICATION.md structural template, then produce the OBSV-02 bundle-size closure doc by running a 3-way same-session build comparison (main / phase-7-without-keepNames / phase-7-with-keepNames) per Round-2 MEDIUM-3 to isolate the keepNames-only delta from the total Phase 7 shipping cost. Round-3 LOW fixes tighten the surrounding mechanics: Node fallback for jq uses `JSON.parse(fs.readFileSync)` (Round-3 LOW-1), local sourcemap re-inspection uses `git worktree` not `git checkout` (Round-3 LOW-2), gzip totals come from Vite's printed column only per D-13 (Round-3 LOW-4), and a Sentry-string-rename escalation rule covers future patch-release renames (Round-3 LOW-5). This is the final "is the capture path actually right end-to-end" gate — Plans 01 and 02 wired the mechanics; Plan 03 proves they work against a real Sentry event on a real deploy preview.

Purpose: ROADMAP SC #5 is explicit that dev / Vitest are insufficient. CONTEXT D-07/D-08 demand four specific evidence artifacts before the phase is called done. CONTEXT D-11..D-14 demand a dedicated OBSV-02-bundle-delta.md doc with per-chunk granularity. Round-2 review fixes tighten mechanism evidence (require ≥1 `auto.function.react.*` event), use plugin-disabled inspection builds for sourcemap reads, parse map JSON for names[] verification, and capture clean keepNames-only attribution. Round-3 review fixes patch the surrounding mechanics so the executor isn't blocked by broken tooling or ambiguous methodology. This plan turns those mandates into committed, audit-clean files.

Output: `.planning/phases/07-observability-hardening/07-VERIFICATION.md` (NEW), `.planning/closure/OBSV-02-bundle-delta.md` (NEW — first instance under `.planning/closure/`), `.planning/phases/07-observability-hardening/artifacts/.gitkeep` (NEW — directory shell for D-10 screenshot storage).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-observability-hardening/07-CONTEXT.md
@.planning/phases/07-observability-hardening/07-RESEARCH.md
@.planning/phases/07-observability-hardening/07-PATTERNS.md
@.planning/phases/07-observability-hardening/07-VALIDATION.md
@.planning/phases/07-observability-hardening/07-REVIEWS.md
@.planning/phases/07-observability-hardening/07-01-SUMMARY.md
@.planning/phases/07-observability-hardening/07-02-SUMMARY.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VERIFICATION.md

<interfaces>
<!-- 06-VERIFICATION.md is the structural analog. Every Phase 7 verification doc convention below comes from it (frontmatter, 12 top-level sections, table column shapes). -->
<!-- Excerpt: 06-VERIFICATION.md frontmatter (lines 1-7) — NOTE: the existing doc uses ___ (underscores) as delimiters, but Phase 7 MUST use --- (hyphens) to match standard YAML frontmatter convention. -->

**06-VERIFICATION.md frontmatter shape (structural analog)** — keys: `phase`, `verified` (ISO timestamp), `status` (passed | partial), `score` (e.g. "7/7 roadmap success criteria verified (plus all 7 plan must-have sets)"), `overrides_applied` (numeric, typically 0). Example values from Phase 6: `phase: 06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden`, `verified: 2026-04-26T08:56:00Z`, `status: passed`. Phase 6 doc uses `___` as fence delimiters; Phase 7 MUST use `---`.

**Phase 7 frontmatter target shape** — same five keys as above, scaled to Phase 7's 5 ROADMAP success criteria, no plan-must-have-set count clause. Specifically: `phase: 07-observability-hardening`, `verified: <ISO-timestamp>`, `status: passed` (or `partial`), `score: 5/5 roadmap success criteria verified`, `overrides_applied: 0`. Frontmatter delimiters MUST be `---` (three hyphens) — NOT `___` (three underscores).

<!-- 12 top-level sections (PATTERNS file lines 261-275): -->
<!-- 1. # Phase 7: Observability Hardening — Verification Report
     2. **Phase Goal** prose paragraph
     3. **Verified:** / **Status:** / **Re-verification:** triplet
     4. ## Goal Achievement — Roadmap Success Criteria (table: # | Success Criterion | Status | Evidence)
     5. ## Required Artifacts (table: Artifact | Expected | Status | Details)
     6. ## Key Link Verification (table: From | To | Via | Status | Details)
     7. ## Behavioral Spot-Checks (table: Behavior | Command | Result | Status)
     8. ## Anti-Patterns Found (expected empty for Phase 7)
     9. ## Requirements Coverage (OBSV-01, OBSV-02 status flips with inline evidence)
    10. ## Human Verification Required (the four D-08 evidence rows live HERE)
    11. ## Gaps Summary (closes "Disposition: PASSED")
    12. Footer trailer (italicised _Verified: <ts>_ + _Verifier: Khai_) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger /__smoke?render=1 on Phase 7 PR's Netlify deploy preview and capture D-08 evidence (manual — Sentry UI inspection)</name>
  <read_first>
    - .planning/phases/07-observability-hardening/07-CONTEXT.md (D-07: manual one-shot on Phase 7 PR's deploy preview; D-08: four required evidence items; D-09: solo sign-off; D-10: artifact directory + commit-vs-gitignore policy)
    - .planning/phases/07-observability-hardening/07-RESEARCH.md (Pitfall 1 lines 471-479: dev / StrictMode masks the test → MUST verify on Netlify deploy preview production build; Pitfall 5 lines 515-527: Dedupe behavior; <specifics>: mechanism.type acceptable values)
    - .planning/phases/07-observability-hardening/07-VALIDATION.md (Manual-Only Verifications section — defines exactly what each screenshot must show; refreshed in Round-3 to match Plan 03 mechanism-allowlist + plugin-disabled inspection build)
    - .planning/phases/07-observability-hardening/07-REVIEWS.md (HIGH-2 inspection-build switch; MEDIUM-1 two-tier mechanism criteria; MEDIUM-4 jq-based names[] extraction; LOW-2 error.value verification; Cursor rollback path; Round-3 LOW-1 Node fallback fix; Round-3 LOW-2 worktree pattern; Round-3 LOW-5 mechanism-rename escalation rule)
    - ROADMAP.md § Phase 7 Success Criteria #5 (verbatim: "Verification is performed on a Netlify deploy preview — dev server / Vitest are explicitly insufficient")
  </read_first>
  <what-built>
    Plans 01 and 02 are merged into the Phase 7 PR. Netlify auto-builds a deploy preview when the PR is pushed. The preview URL has the format `https://deploy-preview-<PR#>--<site-slug>.netlify.app`. On that preview, `/__smoke?render=1` should:
    1. NOT 404 (because `VITE_NETLIFY_CONTEXT === 'deploy-preview'`, not `'production'`).
    2. Render the lazy-loaded `RenderThrowSmoke` component, which throws from its render body.
    3. Be caught by `Sentry.ErrorBoundary` (which Plan 01 wired with `beforeCapture` + `onError` and which sits inside `<StrictMode>`).
    4. Trigger `onCaughtError` (Plan 01's React 19 hook, wrapped in the taggedHandler factory which sets `boundary='app-root'` on the active scope before delegating), which routes through `Sentry.reactErrorHandler()`.
    5. Send at least one event to the project's Sentry org with `mechanism.type === 'auto.function.react.error_handler'` OR `'auto.function.react.error_boundary'` (PRIMARY pass — Round-2 MEDIUM-1) — possibly accompanied by additional `'generic'` events from the manual onError belt (those are EXPECTED companions, not standalone passes). The event(s) must have populated `componentStack`, `tags.boundary === 'app-root'`, un-mangled top-frame identifiers (`RootLayout`, `RenderThrowSmoke`, `SmokePage`, route component names), `exception.values[0].value` matching the deterministic smoke message, and `environment === 'deploy-preview'` (Round-2 HIGH-1 fix).
  </what-built>
  <how-to-verify>
    Push the Phase 7 branch to GitHub and open the PR. Wait for Netlify's deploy preview to finish (status check in the PR shows the preview URL). Then walk this sequence and produce all four D-08 artifacts.

    **A — Trigger the smoke (browser):**
    1. Open `https://deploy-preview-<PR#>--<site-slug>.netlify.app/__smoke?render=1` in a browser (any browser; signed-out is fine — the route does not require auth).
    2. Confirm: page renders the AppErrorFallback fallback UI (NOT a 404, NOT a blank page). If you see TanStack's default 404, the env gate is wrong — STOP and check `import.meta.env.VITE_NETLIFY_CONTEXT` was actually set in the build (`netlify.toml` Plan 01 Task 2 must have shipped).

    **B — Find the Sentry event:**
    3. Open the project's Sentry org → Issues → search for `RenderThrowSmoke: deliberate render-phase throw for Sentry verification` (the deterministic message string from `src/components/debug/RenderThrowSmoke.tsx`). The fresh event(s) from step 1 should be the most recent — there may be multiple events (one from the createRoot hook path, one from the ErrorBoundary onError belt; Sentry.Dedupe may collapse them, but inspect ALL events with this message string from the last 5 minutes).
    4. For EACH event, open it. **Before proceeding, record: (a) the event's Sentry timestamp (UTC ISO string from the event header), and (b) confirm `environment === 'deploy-preview'` (NOT `'production'`)** — Plan 01 Task 1 Round-2 HIGH-1 fix coalesces VITE_NETLIFY_CONTEXT into Sentry.init's environment field, so this MUST report `'deploy-preview'` (or `'branch-deploy'` if you triggered from a non-PR branch deploy). If the event shows `environment: production`, STOP — the HIGH-1 fix did not ship correctly.

    **C — Capture EVIDENCE #1 (componentStack + boundary tag — proves OBSV-01 capture path):**
    5. In the event detail view, screenshot the `Tags` panel showing `boundary: app-root` AND screenshot the `Component Stack` / `React Component Stack` panel showing the populated stack frames.
    6. Save the screenshot(s) under `.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png` (or `.jpg` — pick one, document it).
    7. **Round-2 MEDIUM-1 — two-tier mechanism criteria.** Confirm `mechanism.type` for EACH event with the smoke message string (open the event's "JSON" tab → `exception.values[0].mechanism.type`). Record the actual string value(s) observed across all matching events.

       **PRIMARY pass — at least one event must have `mechanism.type === 'auto.function.react.error_handler'` OR `mechanism.type === 'auto.function.react.error_boundary'`.** This proves the React 19 root hooks (or the Sentry ErrorBoundary SDK) caught the render-phase throw — the OBSV-01 primary capture path.

       **SECONDARY observation — additional events with `mechanism.type === 'generic'` are EXPECTED.** The manual `onError` belt in Plan 01 Task 1 fires `Sentry.captureException` directly, which produces `mechanism.type === 'generic'`. This is intentional defense-in-depth; the belt fires alongside the SDK paths.

       **PARTIAL pass — solo `'generic'` (no `auto.function.react.*` companion):** if you observe ONLY `'generic'` events with NO `auto.function.react.*` companion, this is a PARTIAL pass — it proves the belt fired but does NOT prove the primary capture path. STOP and investigate Plan 01 Task 1 hook wiring (the createRoot options object and the taggedHandler factory) before sign-off.

       **FAILURE — any `auto.browser.*` mechanism.type** (e.g. `auto.browser.global_handlers.onerror`) — this means the error fell through to `window.onerror` / `globalHandlersIntegration` and the React 19 hooks did NOT catch it. STOP — file Phase 7 as not-yet-done; investigate Plan 01 Task 1 wiring (createRoot options object shape + taggedHandler factory). RESEARCH Pitfall 1 explicit failure mode.

       **Round-3 LOW-5 — Sentry-string-rename escalation rule.** If observed mechanism.type strings DIFFER from the literal allowlist `{auto.function.react.error_handler, auto.function.react.error_boundary}` BUT clearly indicate React-hook capture or React-boundary capture (e.g., `react.errorboundary`, `react.error_handler`, or any future Sentry patch-release rename that semantically maps to one of the two PRIMARY paths), DO NOT auto-fail. Escalate to the CONTEXT owner (Khai) for a one-off sign-off override. In `07-VERIFICATION.md ## Human Verification Required` row 3 (mechanism row), record BOTH the actual observed string AND the override decision verbatim ("Override granted: observed `<actual>`, semantically maps to PRIMARY path because `<reason>`. Khai, <date>."). The literal-string assertions in this plan's grep checks would otherwise false-fail on a Sentry rename — this escalation rule is the single sanctioned bypass.

    7b. **Round-2 LOW-2 — error.value verification.** While inspecting the JSON tab, confirm `exception.values[0].value` matches the deterministic smoke message string from RenderThrowSmoke EXACTLY: `'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'`. Record the observed value. If the string does not match, EITHER the smoke component message was edited (check Plan 02 Task 1) OR Sentry truncated the value field. Note the discrepancy in 07-VERIFICATION.md if observed.

    **D — Capture EVIDENCE #2 (un-mangled stack frames — proves OBSV-02 symbolication):**
    8. In the same event, expand the top exception frame. Screenshot showing the function/component identifiers as `RenderThrowSmoke`, `SmokePage`, `RootLayout`, `AppErrorFallback`, or other project component names — NOT `xR`, `$M`, or other 1-2 letter mangled identifiers.
    9. Save under `.planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png`.

    **E — If frames are mangled (Round-2 Cursor rollback path):**

    If the screenshot at step 9 shows mangled names like `xR` / `$M`, do NOT immediately assume `keepNames` is broken. Two distinct failure modes look identical at this layer:
       (a) keepNames flag did not take effect (Plan 01 Task 2 build wrote `dist/` without `__name(` calls)
       (b) sourcemap upload pipeline failed (sentryVitePlugin couldn't authenticate or didn't run)

    Disambiguate BEFORE editing code:
    1. Get the deploy-preview release SHA from the Sentry event's Tags / Release section. Call it `$RELEASE`.
    2. Get your Sentry org slug + project slug from the Sentry URL bar. Call them `$ORG` and `$PROJ`. Set `$SENTRY_AUTH_TOKEN` to a Sentry user-token with project read scope (separate from the build-time token; do NOT commit this).
    3. Query the release artifacts API:
       ```bash
       curl -s -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" \
         "https://sentry.io/api/0/projects/$ORG/$PROJ/releases/$RELEASE/files/" \
         | jq -r '.[].name' | grep '\.map$' | head -10
       ```
    4. If the response lists `.map` files for this release → upload pipeline is healthy; the keepNames flag is the suspect. Re-verify step F locally below to confirm `__name(` calls and `names[]` content.
    5. If the response is empty / errors / no `.map` files appear → upload pipeline is broken (sentryVitePlugin authToken or filesToDeleteAfterUpload misfired). Investigate `vite.config.ts` `sentryVitePlugin({...})` and the build-time `SENTRY_AUTH_TOKEN` — NOT keepNames.

    Don't conflate the two failure modes when filing the regression — they have different fixes.

    **F — Capture EVIDENCE #3 (permalink + release SHA + event timestamp + environment — long-lived event reference):**
    10. Copy the event's permalink from Sentry's URL bar (typically `https://<org>.sentry.io/issues/<id>/events/<event-id>/`).
    11. Find the release SHA on the event (Tags or Release section — should match `VITE_COMMIT_SHA` from the deploy-preview build).
    12. Record event timestamp (UTC ISO from the event header) AND environment tag value (must be `'deploy-preview'` or `'branch-deploy'`, NEVER `'production'` per Round-2 HIGH-1).
    13. Record all four as plain text — paste these directly into `07-VERIFICATION.md ## Human Verification Required` (Task 2 below).

    **G — Capture EVIDENCE #4 (Round-2 HIGH-2 + MEDIUM-4 — .map names[] excerpt via plugin-disabled build + jq-based JSON inspection + __name grep — proves keepNames mechanically took effect for THIS deploy; Round-3 LOW-1 + LOW-2 fixes applied):**

    IMPORTANT: `sentryVitePlugin` deletes `.js.map` files after uploading them to Sentry as part of `npm run build`. Do NOT run `npm run build` and then try to grep the maps — they will be gone. The Round-2 HIGH-2 fix is to use `--mode development` to deterministically disable the plugin (vite.config.ts:30 `disable: mode !== 'production'` short-circuits the plugin including its delete hook) — much more reliable than the empty-token assumption used in Round-1.

    14. **Round-3 LOW-2 — temporary worktree (NOT `git checkout`).** Locally, instead of `git checkout <release-sha>` in the main working tree (which collides with the Phase 7 branch's uncommitted/dirty files), use a temporary worktree at the deploy-preview release SHA. This is the same convention as Task 3's bundle-delta machinery — reuse it here:
        ```bash
        TMPDIR=$(mktemp -d -t phase7-inspect-XXXXXX)
        git worktree add "$TMPDIR" <release-sha>
        cd "$TMPDIR"
        npm ci  # (or npm install if you prefer; ci is faster with the lockfile)
        ```
    15. Run the inspection build (Round-2 HIGH-2 — switched from `SENTRY_AUTH_TOKEN= npx vite build` to `npx vite build --mode development`):
        ```bash
        npm run generate && npx tsc -b --noEmit && npx vite build --mode development 2>/dev/null
        ```
        Mode `'development'` triggers the plugin's `disable: mode !== 'production'` guard at vite.config.ts:30, deterministically skipping upload AND `filesToDeleteAfterUpload`. This produces `dist/` with `.js.map` files intact.
    16. Run: `grep -o '__name(' dist/assets/*.js | head -20` and capture the output. Save back to the main worktree's artifacts directory:
        ```bash
        grep -o '__name(' dist/assets/*.js | head -20 > /Users/<you>/code/wtcs-community-polls/.planning/phases/07-observability-hardening/artifacts/__name-grep.txt
        ```
        (Adjust the absolute path to your project root. The temporary worktree is ephemeral; artifacts must be written back to the main worktree's tracked path.)
    17. **Round-2 MEDIUM-4 + Round-3 LOW-1 — JSON-aware names[] extraction.** Raw grep over `.js.map` false-positives on the `sourcesContent` array (which contains the original TypeScript source, where `RenderThrowSmoke` appears as the export name). Use jq to target the `names` field directly:
        ```bash
        for f in dist/assets/*.js.map; do
          jq -r '.names[]' "$f" 2>/dev/null \
            | grep -E '^(RenderThrowSmoke|RootLayout|AppErrorFallback|SmokePage)$' \
            && echo "  ↳ in $f"
        done | tee /Users/<you>/code/wtcs-community-polls/.planning/phases/07-observability-hardening/artifacts/sourcemap-names-excerpt.txt
        ```
        jq is BSD-standard on macOS. **Round-3 LOW-1 — fixed Node fallback.** If running on a minimal Linux without jq, use the Node script below (the Round-2 fallback used `require('./$f')` on `.js.map` files; Node's CommonJS loader treats `.map` as JavaScript, not JSON, so `require()` fails with a syntax error — switched to `JSON.parse(fs.readFileSync(...))`):
        ```bash
        node -e "const fs=require('fs'); for (const f of process.argv.slice(1)) { const m=JSON.parse(fs.readFileSync(f,'utf8')); for (const n of m.names || []) if (/^(RenderThrowSmoke|RootLayout|AppErrorFallback|SmokePage)$/.test(n)) console.log(n + '  ↳ in ' + f) }" dist/assets/*.js.map \
          | tee /Users/<you>/code/wtcs-community-polls/.planning/phases/07-observability-hardening/artifacts/sourcemap-names-excerpt.txt
        ```
        The single Node invocation iterates ALL `.map` files in one process; no shell `for` loop required.
    18. Save the captured text inline in `07-VERIFICATION.md ## Human Verification Required` row 4 (Task 2 below). DO NOT commit the raw `.map` files; they are post-build artifacts. The text excerpts (jq output + grep output) are sufficient.
    19. **Round-3 LOW-2 — cleanup.** Return to the main worktree and remove the temporary one:
        ```bash
        cd /Users/<you>/code/wtcs-community-polls
        git worktree remove "$TMPDIR" --force
        rm -rf "$TMPDIR" 2>/dev/null  # belt-and-suspenders; worktree remove typically handles it
        ```
        Confirm `git worktree list` no longer shows the temporary worktree.

    **H — Optional housekeeping:**
    20. Decide on the screenshot commit policy per D-10: if the PNG is < 1MB, commit it; if > 1MB, add to `.gitignore` and reference the artifact path with a "(local-only, see Sentry permalink)" note. Permalink + text excerpts MUST be committed regardless.
    21. **Round-2 MEDIUM-1 final check before sign-off:** if `mechanism.type` was `generic` rather than `auto.function.react.*` AND no `auto.function.react.*` companion event was observed, this is a PARTIAL pass — DO NOT type "evidence captured". Type "FAILED: solo generic, no react.* companion — Plan 01 Task 1 hook wiring suspect" and investigate before re-running. (Round-3 LOW-5 escalation applies only to Sentry-rename ambiguity, NOT to solo-generic.)
  </how-to-verify>
  <resume-signal>
    Type "evidence captured" once all four artifacts (componentStack screenshot + un-mangled frames screenshot + permalink-and-sha-and-timestamp-and-environment-text + jq-based names-excerpt-and-grep-text) are saved/recorded, the event timestamp and environment tag are confirmed (environment must be `'deploy-preview'` or `'branch-deploy'`, NEVER `'production'`), `exception.values[0].value` matches the deterministic smoke message (LOW-2), AND at least one event has mechanism.type in {auto.function.react.error_handler, auto.function.react.error_boundary} — additional `generic` events are expected companions (Round-2 MEDIUM-1). If observed mechanism string differs from the literal allowlist due to a Sentry patch-release rename but clearly maps to a PRIMARY path, type "evidence captured (Round-3 LOW-5 override: observed `<actual>`, mapped to PRIMARY)" with override recorded in 07-VERIFICATION.md. Or type "FAILED: <reason>" if mechanism.type was `auto.browser.global_handlers.onerror`, OR if frames were mangled (run step E rollback path before filing), OR if only `generic` events were observed without any `auto.function.react.*` companion (PARTIAL — investigate Plan 01 Task 1 hook wiring).
  </resume-signal>
  <files>.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png, .planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png, .planning/phases/07-observability-hardening/artifacts/sentry-mechanism-type.png, .planning/phases/07-observability-hardening/artifacts/sourcemap-names-excerpt.txt, .planning/phases/07-observability-hardening/artifacts/__name-grep.txt</files>
  <action>See &lt;how-to-verify&gt; above — this checkpoint is a manual evidence-capture step on the Phase 7 PR's Netlify deploy preview. Capture the four D-08 artifacts (componentStack screenshot, un-mangled stack screenshot, Sentry permalink + release SHA + event timestamp + environment text, sourcemap names[] via jq + __name grep text via Round-2 HIGH-2 plugin-disabled inspection build run inside a Round-3 LOW-2 temporary worktree). Apply Round-2 MEDIUM-1 two-tier mechanism criteria (PRIMARY: ≥1 auto.function.react.* event; SECONDARY: generic events expected; PARTIAL: solo generic blocked) plus Round-3 LOW-5 escalation rule for Sentry-string-rename ambiguity. Verify exception.values[0].value matches deterministic smoke message (LOW-2). Verify environment is 'deploy-preview' not 'production' (HIGH-1 sanity check). If frames mangled, run step E rollback path BEFORE assuming keepNames is broken (Cursor rollback suggestion). Use Round-3 LOW-1 corrected Node fallback (`JSON.parse(fs.readFileSync)`) if jq is unavailable.</action>
  <verify>
    <automated>MISSING — manual checkpoint per CONTEXT D-07/D-08; verification is solo human inspection of Sentry event mechanism.type (Round-2 MEDIUM-1 two-tier + Round-3 LOW-5 rename-escalation), componentStack, frame names, exception.values[0].value (Round-2 LOW-2), and environment tag (Round-2 HIGH-1). The closure receipt is recorded in Task 2's 07-VERIFICATION.md.</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png` exists.
    - File `.planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png` exists.
    - File `.planning/phases/07-observability-hardening/artifacts/sentry-mechanism-type.png` exists (per the JSON-tab screenshot in 07-VALIDATION.md Manual-Only table — captures `exception.values[0].mechanism.type` field for at least one event).
    - File `.planning/phases/07-observability-hardening/artifacts/sourcemap-names-excerpt.txt` exists (Round-2 MEDIUM-4 + Round-3 LOW-1 — produced via `jq -r '.names[]' dist/assets/*.js.map | grep -E '^(RenderThrowSmoke|RootLayout|AppErrorFallback|SmokePage)$'` OR the Round-3 LOW-1 corrected Node fallback using `JSON.parse(fs.readFileSync)`) with a `RenderThrowSmoke` substring (run: `grep -c 'RenderThrowSmoke' artifacts/sourcemap-names-excerpt.txt` returns >= 1). Also accepts `RootLayout`, `AppErrorFallback`, or `SmokePage` as proof of real identifier preservation in the `names[]` array — NOT raw grep over the whole `.js.map` file (which would false-positive on `sourcesContent`).
    - `jq -r '.names[]' dist/assets/*.js.map | grep -cE '^(RenderThrowSmoke|RootLayout|AppErrorFallback|SmokePage)$'` returns >= 1 (the inspection-build sourcemaps contain real project identifiers in the `names` array — Round-2 MEDIUM-4 JSON-aware verification). Run inside the Round-3 LOW-2 temporary worktree at the deploy-preview release SHA.
    - File `.planning/phases/07-observability-hardening/artifacts/__name-grep.txt` exists and is non-empty (`test -s artifacts/__name-grep.txt`).
    - **Round-2 MEDIUM-1 — PRIMARY pass criterion:** User-captured `mechanism.type` PRIMARY value MUST be `'auto.function.react.error_handler'` OR `'auto.function.react.error_boundary'` (NOT any `auto.browser.*` variant). Additional `'generic'` events are observed-and-expected (belt path). Solo `'generic'` (no `react.*` companion) is PARTIAL — sign-off blocked pending investigation. Recorded inline in 07-VERIFICATION.md `## Human Verification Required` row 3.
    - **Round-3 LOW-5 — Sentry-string-rename escalation:** if observed mechanism.type strings differ from the literal allowlist `{auto.function.react.error_handler, auto.function.react.error_boundary}` BUT clearly indicate React-hook capture or React-boundary capture (future Sentry rename), the CONTEXT owner (Khai) may grant a one-off override. Both the actual observed string AND the override decision MUST be recorded inline in 07-VERIFICATION.md `## Human Verification Required` row 3 (e.g., "Override granted: observed `<actual>`, semantically maps to PRIMARY path because `<reason>`. Khai, <date>."). Without explicit recorded override, the literal-allowlist criterion still applies.
    - **Round-2 LOW-2 — exception value verification:** User confirmed `exception.values[0].value` matches the deterministic smoke message string `'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'`. Recorded inline in 07-VERIFICATION.md.
    - **Round-2 HIGH-1 — environment tag verification:** Event environment confirmed as `'deploy-preview'` (or `'branch-deploy'`), NOT `'production'`. Recorded inline in 07-VERIFICATION.md. Event timestamp also recorded.
    - **Round-2 HIGH-2 — inspection build verification:** The sourcemap `.map` files exist in `dist/assets/` after running `npx vite build --mode development` inside the Round-3 LOW-2 temporary worktree (test inside the temp worktree: `ls dist/assets/*.js.map | wc -l` returns >= 1; the plugin-disabled build leaves maps on disk).
    - **Round-3 LOW-2 — temporary worktree cleanup:** after artifacts are written back to the main worktree, the temporary worktree is removed (`git worktree list` no longer shows the temp path).
  </acceptance_criteria>
  <done>All four D-08 evidence artifacts captured: (1) screenshot of componentStack + boundary='app-root' tag, (2) screenshot of un-mangled top frames showing real project identifiers (RenderThrowSmoke, SmokePage, RootLayout — not mangled 1-2 char names), (3) Sentry permalink + release SHA + event timestamp + environment tag copied for paste into 07-VERIFICATION.md, (4) sourcemap names[] excerpt (via Round-2 MEDIUM-4 jq-based extraction with Round-3 LOW-1 corrected Node fallback) + grep '__name(' output captured using the Round-2 HIGH-2 plugin-disabled inspection build (`npx vite build --mode development`) run inside a Round-3 LOW-2 temporary worktree (NOT `git checkout <release-sha>` in the dirty Phase 7 working tree). Round-2 MEDIUM-1 PRIMARY pass criterion met: at least one event has mechanism.type ∈ {auto.function.react.error_handler, auto.function.react.error_boundary}; any additional `generic` events are documented as expected belt companions. Round-3 LOW-5 escalation rule available for future Sentry-rename ambiguity (with explicit override recorded). Round-2 LOW-2: exception.values[0].value matches the deterministic smoke message. Round-2 HIGH-1: event environment confirmed as 'deploy-preview' not 'production'. Event timestamp recorded. If frames were mangled, Round-2 Cursor rollback path was applied first to disambiguate keepNames-broken vs upload-pipeline-broken before signing off as failure. Temporary worktree cleaned up.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 07-VERIFICATION.md against the 06-VERIFICATION.md template, paste D-08 evidence inline (Round-2 MEDIUM-1 two-tier mechanism + MEDIUM-2 grep contradiction fix + LOW-2 error.value row + Cursor trim non-critical greps + Round-3 LOW-5 mechanism-rename escalation)</name>
  <files>.planning/phases/07-observability-hardening/07-VERIFICATION.md, .planning/phases/07-observability-hardening/artifacts/.gitkeep</files>
  <read_first>
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VERIFICATION.md (FULL READ — structural template; Phase 7 follows the 12-section shape exactly; NOTE: that file uses ___ as frontmatter delimiters — Phase 7 MUST use --- hyphens instead)
    - .planning/phases/07-observability-hardening/07-PATTERNS.md (`07-VERIFICATION.md` NEW section — divergences from the Phase 6 template: 5/5 SC instead of 7/7, 7 artifacts instead of 8, Anti-Patterns Found expected empty)
    - .planning/phases/07-observability-hardening/07-CONTEXT.md (D-08 four evidence items, D-09 solo sign-off, D-10 artifact directory)
    - .planning/phases/07-observability-hardening/07-RESEARCH.md (Validation Architecture rows for OBSV-01/OBSV-02; <specifics> mechanism.type values)
    - .planning/phases/07-observability-hardening/07-REVIEWS.md (MEDIUM-1 two-tier mechanism criteria; MEDIUM-2 drop the contradictory auto.browser. == 0 grep; LOW-2 error.value row; Cursor trim non-critical greps; Round-3 LOW-5 escalation rule)
    - .planning/ROADMAP.md § Phase 7 Success Criteria 1-5 (verbatim — these are the rows in `## Goal Achievement`)
    - The four D-08 evidence artifacts captured in Task 1
  </read_first>
  <action>
    **Step A — Create directory shell.** Create the file `.planning/phases/07-observability-hardening/artifacts/.gitkeep` (empty file). This makes the artifact directory commit-trackable per D-10. If Task 1 committed PNG screenshots in this directory, leave them; if Task 1 chose to gitignore them, add an entry like `*.png` under `.planning/phases/07-observability-hardening/artifacts/` to the existing repo `.gitignore` — but only if necessary (do NOT add a sweeping `*.png` ignore globally).

    **Step B — Create `.planning/phases/07-observability-hardening/07-VERIFICATION.md`** with the EXACT structure below. All frontmatter delimiters MUST be `---` (three hyphens), NOT `___` (three underscores). Sections 1-3 are prose; sections 4-7 are tables; sections 8-11 are tables/prose; section 12 is a footer trailer.

    ```markdown
    ---
    phase: 07-observability-hardening
    verified: <ISO-8601 timestamp at moment of writing — UTC>
    status: passed
    score: 5/5 roadmap success criteria verified
    overrides_applied: 0
    ---

    # Phase 7: Observability Hardening — Verification Report

    Production Sentry receives every render-phase React error with un-mangled, human-readable stack frames so launch-time error triage is reliable. This phase wired Sentry.reactErrorHandler() into React 19's createRoot error hooks (OBSV-01, with dedupe-resilient boundary tagging via the taggedHandler factory per Round-2 MEDIUM-5), set Rolldown's keepNames flag to preserve original function/component names through Oxc minify (OBSV-02), coalesced VITE_NETLIFY_CONTEXT into Sentry.init's environment field so deploy-preview events report the correct environment (Round-2 HIGH-1), and shipped a permanent /__smoke render-phase throw canary so future regressions can be re-detected with one deploy-preview click.

    **Verified:** <ISO timestamp> on Phase 7 PR Netlify deploy preview at `<deploy-preview-url>` (release SHA `<short-sha>`). Sentry event timestamp: `<event-ISO-timestamp-UTC>`. Event environment: `<deploy-preview or branch-deploy — must NOT be 'production'>` (Round-2 HIGH-1: VITE_NETLIFY_CONTEXT coalesced into Sentry.init).
    **Status:** passed
    **Re-verification:** `/__smoke?render=1` on any future deploy preview re-exercises the entire chain.

    ## Goal Achievement — Roadmap Success Criteria

    | # | Success Criterion | Status | Evidence |
    |---|-------------------|--------|----------|
    | 1 | Render-phase throw on Netlify deploy preview produces a Sentry event with populated React `componentStack` AND `error.value` present (OBSV-01 capture path) | ✅ pass | Event `<short event-id>`; `componentStack` populated; `tags.boundary === 'app-root'`; `exception.values[0].value === 'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'` (Round-2 LOW-2 verified). See `## Human Verification Required` rows 1 + 5. |
    | 2 | Same event's top stack frames show original function/component names (`RootLayout`, `RenderThrowSmoke`, `SmokePage`, route components) — not `xR`/`$M` (OBSV-02 symbolication) | ✅ pass | Top frame: `RenderThrowSmoke` (un-mangled). See screenshot at `artifacts/sentry-unmangled-frames.png` and `## Human Verification Required` row 2. |
    | 3 | Built `dist/assets/*.js.map` `names[]` contains entries for kept identifiers AND chunks contain `__name(...)` calls | ✅ pass | `grep -c '__name(' dist/assets/*.js` returns `<n>`; `jq -r '.names[]' dist/assets/*.js.map \| grep -E '^(RenderThrowSmoke\|RootLayout\|AppErrorFallback\|SmokePage)$'` returns `<n>` matches (Round-2 MEDIUM-4 JSON-aware extraction; inspection build via Round-2 HIGH-2 `npx vite build --mode development`; Round-3 LOW-2 ran inside a temporary git worktree at the release SHA). See `## Human Verification Required` row 4. |
    | 4 | Bundle-size delta from `keepNames` documented (≤1.5% gzip target) | ✅ pass / over-target-accepted | Captured at `.planning/closure/OBSV-02-bundle-delta.md`: keepNames-isolated delta `<x.xx>%` gzip (Round-2 MEDIUM-3 — clean per-flag attribution from 3-way comparison). Round-3 LOW-4: gzip values sourced from Vite's printed per-chunk gzip column. <"within target" or "over target by Y pp; observability gain accepted per D-14"> |
    | 5 | Verification performed on Netlify deploy preview (NOT dev / NOT Vitest — StrictMode masks render-phase capture) | ✅ pass | Deploy preview URL pinned above. **Round-2 MEDIUM-1 PRIMARY pass:** at least one event with mechanism.type `<one of: auto.function.react.error_handler, auto.function.react.error_boundary>` observed. Additional `generic` events from the manual onError belt are expected companions, not standalone passes. |

    ## Required Artifacts

    | Artifact | Expected | Status | Details |
    |----------|----------|--------|---------|
    | `src/main.tsx` | createRoot called with three taggedHandler factories (each calling Sentry.withScope + scope.setTag('boundary','app-root') + Sentry.reactErrorHandler — Round-2 MEDIUM-5; Round-3 confirmed reactErrorHandler is ADDITIVE per `node_modules/@sentry/react/build/esm/error.js:90-105`); ErrorBoundary beforeCapture (scope.setTag boundary='app-root') + onError belt; Sentry.init `environment: VITE_NETLIFY_CONTEXT ?? MODE` (Round-2 HIGH-1) | ✅ exists | Plan 01 Task 1; verified by grep counts in 07-01-SUMMARY.md acceptance criteria including `scope.setTag('boundary', 'app-root')` count >= 2. |
    | `vite.config.ts` | rolldownOptions.output.keepNames: true under existing build block (sourcemap: 'hidden' preserved; sentryVitePlugin untouched; `disable: mode !== 'production'` unchanged for Round-2 HIGH-2 inspection build) | ✅ exists | Plan 01 Task 2; verified by `grep -c 'keepNames: true' vite.config.ts` returns 1. |
    | `netlify.toml` | [build].command shell-exports VITE_NETLIFY_CONTEXT=$CONTEXT (D-05; also feeds Sentry.init environment per Round-2 HIGH-1) | ✅ exists | Plan 01 Task 2; verified by `grep -c 'VITE_NETLIFY_CONTEXT=\$CONTEXT' netlify.toml` returns 1. |
    | `src/components/debug/RenderThrowSmoke.tsx` | Named-export render-phase throw with deterministic message (D-01) | ✅ exists | Plan 02 Task 1. |
    | `src/routes/[__smoke].tsx` | Bracket-escaped TanStack route file with createFileRoute('/__smoke'), validateSearch, beforeLoad env-gate, lazy-loaded throw component (D-02..D-06) | ✅ exists | Plan 02 Task 2. |
    | `src/routeTree.gen.ts` | Auto-regenerated to register the __smoke route (fullPath: '/__smoke'); committed alongside the route file (Round-2 Gemini suggestion) | ✅ updated | Plan 02 Task 2 ran `npm run build` which re-ran tsr generate. |
    | `.planning/closure/OBSV-02-bundle-delta.md` | Total + per-chunk gzip table comparing main vs phase-7-without-keepNames vs phase-7-with-keepNames builds (Round-2 MEDIUM-3 3-way comparison), with target check rationale (D-11..D-14); gzip values sourced from Vite's printed per-chunk column per Round-3 LOW-4 single-source-of-truth | ✅ created | This plan, Task 3. |

    ## Key Link Verification

    | From | To | Via | Status | Details |
    |------|----|----|--------|---------|
    | `src/main.tsx` createRoot | `@sentry/react` reactErrorHandler | options object: `{ onUncaughtError, onCaughtError, onRecoverableError }` each through taggedHandler factory | ✅ wired | All three hooks present, each setting `boundary='app-root'` on the active scope before delegating (Plan 01 Task 1 Round-2 MEDIUM-5 fix). Round-3 confirmed reactErrorHandler is ADDITIVE — inner callback runs after default capture (error.js:90-105 + JSDoc). |
    | `src/main.tsx` Sentry.ErrorBoundary | `@sentry/react` beforeCapture + captureException | `beforeCapture={(scope) => scope.setTag('boundary','app-root')}` + `onError` belt | ✅ wired | `tags.boundary: 'app-root'` guaranteed on SDK event via beforeCapture; belt as fallback (Plan 01 Task 1). |
    | `src/main.tsx` Sentry.init `environment` | Netlify `$CONTEXT` (via VITE_NETLIFY_CONTEXT) | `environment: import.meta.env.VITE_NETLIFY_CONTEXT ?? import.meta.env.MODE` | ✅ wired | Sentry event environment field reports `'<deploy-preview or branch-deploy>'` per Round-2 HIGH-1. |
    | `src/routes/[__smoke].tsx` | `src/components/debug/RenderThrowSmoke` | `lazy(() => import(...).then(m => ({ default: m.RenderThrowSmoke })))` | ✅ wired | RenderThrowSmoke chunk present in `dist/assets/` (Plan 02 Task 2). |
    | `vite.config.ts` build | Rolldown `__name(fn,'orig')` helper injection | `rolldownOptions.output.keepNames: true` | ✅ wired | `grep -l '__name(' dist/assets/*.js` non-empty (Plan 01 Task 2 build). |
    | `netlify.toml` [build].command | `import.meta.env.VITE_NETLIFY_CONTEXT` in route AND in Sentry.init | shell substitution `VITE_NETLIFY_CONTEXT=$CONTEXT` | ✅ wired | Deploy preview behaved correctly (`/__smoke?render=1` reachable; Sentry event environment is 'deploy-preview'; live prod behavior to be re-confirmed post-merge). |

    ## Behavioral Spot-Checks

    | Behavior | Command | Result | Status |
    |----------|---------|--------|--------|
    | Typecheck passes after createRoot rewrite | `npx tsc -b --noEmit` | exit 0 | ✅ pass |
    | Production build succeeds | `npm run build` | exit 0 | ✅ pass |
    | keepNames helper present in built chunks | `grep -l '__name(' dist/assets/*.js \| wc -l` | `<>= 1>` | ✅ pass |
    | Real project identifier in sourcemap names[] (Round-2 MEDIUM-4 — JSON-aware) | `jq -r '.names[]' dist/assets/*.js.map \| grep -cE '^(RenderThrowSmoke\|RootLayout\|AppErrorFallback\|SmokePage)$'` (inspection build via Round-2 HIGH-2: `npx vite build --mode development`; run inside Round-3 LOW-2 temporary worktree at release SHA) | `<>= 1>` | ✅ pass |
    | RenderThrowSmoke chunk lazy-split | `grep -l 'RenderThrowSmoke' dist/assets/*.js \| wc -l` | `<>= 1>` | ✅ pass |
    | Smoke route reaches Sentry on deploy preview | manual: open `/__smoke?render=1` | event landed | ✅ pass |
    | Sentry mechanism.type primary path (Round-2 MEDIUM-1) | manual: event JSON tab — at least one event must have `auto.function.react.*` | `<auto.function.react.error_handler or auto.function.react.error_boundary, OR Round-3 LOW-5 override-recorded value>` observed | ✅ pass |
    | Sentry environment tag (Round-2 HIGH-1) | manual: event JSON tab → `environment` field | `<deploy-preview or branch-deploy>` | ✅ pass |
    | exception.values[0].value matches deterministic smoke message (Round-2 LOW-2) | manual: event JSON tab → `exception.values[0].value` | `'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'` | ✅ pass |

    ## Anti-Patterns Found

    None. Phase 7 is config + wiring + permanent debug-route work — no business-logic surface to over-engineer.

    ## Requirements Coverage

    | REQ-ID | Pre-Phase-7 | Post-Phase-7 | Gate | Status |
    |--------|-------------|--------------|------|--------|
    | OBSV-01 | Render-phase errors swallowed by React (only setTimeout/onerror events landed at Sentry per Phase 6 baseline) | createRoot taggedHandler factories + ErrorBoundary beforeCapture + belt route every render-phase error to Sentry; PRIMARY mechanism.type ∈ {auto.function.react.error_handler, auto.function.react.error_boundary} (Round-2 MEDIUM-1; Round-3 LOW-5 rename escalation available); generic belt events expected; componentStack populated; boundary='app-root' tag set on whichever event survives dedupe (Round-2 MEDIUM-5); exception.values[0].value matches smoke message (Round-2 LOW-2); environment tag is deploy-preview not production (Round-2 HIGH-1) | Sentry event from `/__smoke?render=1` on deploy preview shows correct mechanism + tags + stack + environment + value | ✅ pass |
    | OBSV-02 | Top stack frames mangled as `xR`/`$M`-style identifiers; symbolication useless for triage | keepNames: true injects __name(fn,'orig') helpers; Function.prototype.name preserved through Oxc; Sentry surfaces original `RootLayout`/`RenderThrowSmoke`/`SmokePage`/route names | Same Sentry event's top frames show un-mangled names AND `dist/assets/*.js.map` `names[]` (via jq, Round-2 MEDIUM-4; Round-3 LOW-1 corrected Node fallback) contains those identifiers AND chunks carry `__name(` calls. Round-2 MEDIUM-3: keepNames-isolated bundle delta documented in `.planning/closure/OBSV-02-bundle-delta.md` (Round-3 LOW-4: gzip values from Vite's printed column only). | ✅ pass |

    ## Human Verification Required

    The four D-08 evidence items (CONTEXT.md), with Round-2 review fixes for two-tier mechanism criteria + error.value verification + environment tag verification, plus Round-3 LOW-5 escalation rule for Sentry-string-rename ambiguity:

    | # | Evidence | Method | Location | Status |
    |---|----------|--------|----------|--------|
    | 1 | Sentry event screenshot showing populated `componentStack` + `tags.boundary === 'app-root'` (proves OBSV-01 capture path) | Open Sentry event → Tags panel + React Component Stack panel → screenshot | `.planning/phases/07-observability-hardening/artifacts/sentry-componentstack.png` | ✅ captured |
    | 2 | Sentry event screenshot showing top stack frames with un-mangled names (`RootLayout`, `RenderThrowSmoke`, `SmokePage` — NOT `xR`/`$M`) (proves OBSV-02 symbolication) | Same event → expand top frame → screenshot | `.planning/phases/07-observability-hardening/artifacts/sentry-unmangled-frames.png` | ✅ captured |
    | 3 | Sentry event permalink + release SHA + event timestamp + environment tag pinned in this doc | Copy URL from Sentry event view; copy release SHA from event tags; record event ISO timestamp and environment | Permalink: `<paste full URL here>` · Release SHA: `<paste short SHA here>` · Event timestamp: `<paste ISO timestamp here>` · Environment: `<paste environment tag here — must be 'deploy-preview' or 'branch-deploy', NOT 'production' per Round-2 HIGH-1>` | ✅ pinned |
    | 4 | Built `dist/assets/*.js.map` `names[]` excerpt (Round-2 MEDIUM-4: via jq, NOT raw grep; Round-3 LOW-1: Node fallback uses `JSON.parse(fs.readFileSync)` not `require()`) + `grep '__name(' dist/assets/*.js \| head` output (mechanical proof keepNames took effect for THIS release — Round-2 HIGH-2: run via `npx vite build --mode development` plugin-disabled inspection build; Round-3 LOW-2: inside a temporary git worktree at release SHA, NOT `git checkout` in main worktree) | At release SHA → temporary worktree (`git worktree add $TMPDIR <release-sha>`) → `npm ci && npx vite build --mode development` → `jq -r '.names[]' dist/assets/*.js.map \| grep -E '^(RenderThrowSmoke\|RootLayout\|AppErrorFallback\|SmokePage)$'` → grep `__name(` → copy outputs → cleanup `git worktree remove` | See fenced blocks below | ✅ captured |
    | 5 | exception.values[0].value matches deterministic smoke message (Round-2 LOW-2 explicit verification — ROADMAP SC #1 "error.value present") | Event JSON tab → `exception.values[0].value` | Observed value: `<paste exact value here — must equal 'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'>` | ✅ verified |

    **Round-2 MEDIUM-1 + Round-3 LOW-5 — Mechanism type two-tier sanity check + rename-escalation (CONTEXT `<specifics>`):**

    PRIMARY pass — observed value(s) on at least one event: `<one of: auto.function.react.error_handler, auto.function.react.error_boundary>`. This proves the React 19 root hooks (taggedHandler factory in Plan 01 Task 1) or the Sentry ErrorBoundary SDK caught the render-phase throw.

    SECONDARY observation — additional events with `mechanism.type === 'generic'` are EXPECTED (the manual `onError` belt in Plan 01 Task 1 fires `Sentry.captureException` directly, producing `mechanism.type === 'generic'`). This is intentional defense-in-depth.

    Solo `'generic'` (no `auto.function.react.*` companion) would have been a PARTIAL pass — sign-off would have been blocked pending investigation of Plan 01 Task 1 hook wiring. Confirmed: this scenario was NOT observed.

    Any browser-global handler mechanism would have been a Phase 7 failure (RESEARCH Pitfall 1). Confirmed: this scenario was NOT observed.

    **Round-3 LOW-5 — Sentry-string-rename escalation:** if observed mechanism strings differed from the literal allowlist due to a Sentry patch-release rename but clearly mapped to a PRIMARY path, the override decision would be recorded HERE in the form: "Override granted: observed `<actual>`, semantically maps to PRIMARY path because `<reason>`. Khai, `<date>`." Confirmed: `<no override applied | override applied with text above>`.

    ### Evidence #4 — sourcemap names[] excerpt (Round-2 MEDIUM-4 jq extraction; Round-3 LOW-1 Node fallback corrected; Round-3 LOW-2 temporary worktree pattern)

    ```text
    # From dist/assets/<chunk>-<hash>.js.map (post inspection build `npx vite build --mode development` at release SHA <short-sha>):
    # Generated via: jq -r '.names[]' dist/assets/*.js.map | grep -E '^(RenderThrowSmoke|RootLayout|AppErrorFallback|SmokePage)$'
    # (Round-3 LOW-1 Node fallback if jq unavailable: `node -e "const fs=require('fs'); for (const f of process.argv.slice(1)) { const m=JSON.parse(fs.readFileSync(f,'utf8')); for (const n of m.names || []) if (/^(RenderThrowSmoke|RootLayout|AppErrorFallback|SmokePage)$/.test(n)) console.log(n + '  ↳ in ' + f) }" dist/assets/*.js.map`)
    # (Round-3 LOW-2 ran inside temporary worktree: `git worktree add $TMPDIR <release-sha>` then `npm ci && npx vite build --mode development`; cleanup with `git worktree remove $TMPDIR --force` afterward)
    <paste actual jq output here — should show identifier names matching the regex>
    ```

    ### Evidence #4 — `__name(` helper presence

    ```text
    $ grep -o '__name(' dist/assets/*.js | head -10
    <paste actual output here — should show many lines>
    ```

    Empty output here would be a Phase 7 failure mode — keepNames did not take effect. If observed in conjunction with mangled stack frames in the Sentry event, run the Round-2 Cursor rollback path (Task 1 step E) to disambiguate keepNames-broken vs upload-pipeline-broken before filing.

    ## Gaps Summary

    None. All five ROADMAP success criteria pass; both REQ rows flip from Pending → ✅ pass; no anti-patterns introduced. The `.planning/closure/OBSV-02-bundle-delta.md` doc carries the full bundle-size delta with Round-2 MEDIUM-3 3-way comparison (main / phase-7-without-keepNames / phase-7-with-keepNames) and target check rationale; if the keepNames-only delta exceeded 1.5% gzip, the doc applies the D-14 overage policy (document + ship). Round-3 LOW-4: gzip values are sourced from Vite's printed per-chunk column only (single source of truth per D-13).

    Disposition: PASSED.

    ---
    _Verified: <ISO timestamp>_
    _Verifier: Khai (solo sign-off per D-09 — OBSV-01/02 are config + wiring, not security/auth gates; the Sentry event is independently re-openable via the permalink)_
    _Round-2 review concerns addressed: HIGH-1 (Sentry environment tag), HIGH-2 (plugin-disabled inspection build), MEDIUM-1 (two-tier mechanism criteria), MEDIUM-2 (dropped contradictory grep), MEDIUM-3 (3-way bundle comparison), MEDIUM-4 (jq-based names[] extraction), MEDIUM-5 (dedupe-resilient boundary tagging), LOW-1 (informational), LOW-2 (error.value verified)_
    _Round-3 review concerns addressed: consensus HIGH (Plan 02 Task 3 curl→manual), consensus MEDIUM (07-VALIDATION.md drift refreshed), single-reviewer MEDIUM (reactErrorHandler ADDITIVE confirmed by source), LOW-1 (Node fallback `JSON.parse` fix), LOW-2 (`git worktree` not `git checkout`), LOW-3 (ErrorInfo prose corrected), LOW-4 (Vite gzip column single-source-of-truth), LOW-5 (Sentry-rename escalation rule)_
    ```

    Replace every `<placeholder>` with the actual value captured in Task 1. Do NOT leave any `<...>` token or `{{...}}` placeholder in the final committed file.

    Decision IDs implemented: D-07 (manual one-shot), D-08 (all four evidence rows + Round-2 LOW-2 fifth row for error.value), D-09 (solo sign-off recorded), D-10 (artifacts directory referenced). Round-2 review fixes: HIGH-1 (environment tag), HIGH-2 (inspection build), MEDIUM-1 (two-tier mechanism), MEDIUM-2 (dropped contradictory grep), MEDIUM-4 (jq names[] extraction), LOW-2 (error.value). Round-3 review fixes: LOW-1 (Node fallback), LOW-2 (worktree), LOW-4 (Vite gzip column), LOW-5 (rename escalation).

    Note re: LOW-1 (Codex Round-2 review): each command in the verify-block list runs as a separate exit-code check; per-task atomic commit gate enforces non-zero exit blocks the task. Informational only — no plan edit beyond this note.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-observability-hardening/07-VERIFICATION.md; test -f .planning/phases/07-observability-hardening/artifacts/.gitkeep; grep -c '## Human Verification Required' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'OBSV-01' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'OBSV-02' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'auto\.function\.react\|generic' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c '<paste\|{{' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'TODO\|XXX\|<replace_me>' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c '<deploy-preview-url>\|<short-sha>\|<ISO timestamp>' .planning/phases/07-observability-hardening/07-VERIFICATION.md; head -1 .planning/phases/07-observability-hardening/07-VERIFICATION.md | grep -c '^---$'; grep -c 'permalink\|Permalink' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'deploy-preview\|branch-deploy' .planning/phases/07-observability-hardening/07-VERIFICATION.md; grep -c 'deliberate render-phase throw for Sentry verification' .planning/phases/07-observability-hardening/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-observability-hardening/07-VERIFICATION.md` exists.
    - File `.planning/phases/07-observability-hardening/artifacts/.gitkeep` exists.
    - `grep -c '## Human Verification Required' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Requirements Coverage' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Anti-Patterns Found' 07-VERIFICATION.md` returns 1.
    - `grep -c '## Gaps Summary' 07-VERIFICATION.md` returns 1.
    - **Cursor "trim non-critical greps" applied:** the redundant heading-presence greps `grep -c '## Goal Achievement'`, `grep -c '## Required Artifacts'`, `grep -c '## Key Link Verification'`, `grep -c '## Behavioral Spot-Checks'` are NOT in this acceptance list (preserved content greps below carry the verification weight; sections are visually inspected, not gated). The grep-count assertion that the structure exists comes from the OBSV-* and content greps.
    - `grep -c 'OBSV-01' 07-VERIFICATION.md` returns >= 2 (Goal Achievement row + Requirements Coverage row).
    - `grep -c 'OBSV-02' 07-VERIFICATION.md` returns >= 2.
    - `grep -c 'boundary.*app-root\|app-root' 07-VERIFICATION.md` returns >= 2 (component stack row + key link row).
    - **Round-2 MEDIUM-1 — mechanism criteria recorded:** `grep -c 'auto\.function\.react\|generic' 07-VERIFICATION.md` returns >= 1 (mechanism type sanity check sentence — approved allowlist values present).
    - **Round-2 MEDIUM-2 — contradictory grep DROPPED:** the prior assertion `grep -c 'auto\.browser\.' 07-VERIFICATION.md returns 0` is REMOVED. The doc may legitimately contain `auto.browser.*` in failure-mode prose; the per-event acceptance in Task 1 enforces no such mechanism was the OBSERVED value.
    - **Round-2 LOW-2 — error.value verification recorded:** `grep -c 'deliberate render-phase throw for Sentry verification' 07-VERIFICATION.md` returns >= 1 (the deterministic smoke message string is pasted into row 5 / SC #1 evidence).
    - **Round-2 HIGH-1 — environment tag verification recorded:** `grep -c 'deploy-preview\|branch-deploy' 07-VERIFICATION.md` returns >= 2 (mentioned in Verified header + row 3 + key link table; environment is NOT 'production').
    - `grep -c 'permalink\|Permalink' 07-VERIFICATION.md` returns >= 1.
    - `grep -c '__name(' 07-VERIFICATION.md` returns >= 1 (Evidence #4 grep block).
    - **Round-2 MEDIUM-4 — jq-based extraction documented:** `grep -c "jq -r '\.names\[\]'" 07-VERIFICATION.md` returns >= 1 (the JSON-aware extraction command appears in the doc, replacing raw grep over .map files).
    - **Round-2 HIGH-2 — plugin-disabled inspection build referenced:** `grep -c 'mode development\|--mode development' 07-VERIFICATION.md` returns >= 1.
    - **Round-3 LOW-2 — temporary worktree referenced:** `grep -c 'git worktree' 07-VERIFICATION.md` returns >= 1 (Evidence #4 mechanism documented).
    - **Round-3 LOW-5 — Sentry-rename escalation rule documented:** `grep -c 'Override granted\|rename escalation\|LOW-5' 07-VERIFICATION.md` returns >= 1 (the escalation paragraph in `## Human Verification Required` either records "no override applied" or the actual override).
    - Placeholder check — ALL of these must return 0: `grep -c '<paste' 07-VERIFICATION.md`; `grep -c '{{' 07-VERIFICATION.md`; `grep -c 'TODO' 07-VERIFICATION.md`; `grep -c 'XXX' 07-VERIFICATION.md`; `grep -c '<replace_me>' 07-VERIFICATION.md`; `grep -c '<deploy-preview-url>' 07-VERIFICATION.md`; `grep -c '<short-sha>' 07-VERIFICATION.md`; `grep -c '<ISO timestamp>' 07-VERIFICATION.md`.
    - Frontmatter uses `---` delimiters (hyphens) NOT `___` (underscores): `head -1 07-VERIFICATION.md | grep -c '^---$'` returns 1.
    - Frontmatter `phase: 07-observability-hardening`; `score: 5/5 roadmap success criteria verified`; `status: passed`.
    - Event timestamp and environment tag recorded in Verified header AND row 3.
  </acceptance_criteria>
  <done>
    `07-VERIFICATION.md` exists with the 12-section structure from `06-VERIFICATION.md`, `---` frontmatter delimiters (not `___`), frontmatter scoring 5/5 ROADMAP success criteria, all four D-08 evidence rows populated with real Task 1 captures (no placeholder tokens remain) plus a Round-2 LOW-2 fifth row for `exception.values[0].value` verification, event timestamp and environment tag (`deploy-preview` or `branch-deploy`, NEVER `production` per Round-2 HIGH-1) recorded, Round-2 MEDIUM-1 two-tier mechanism criteria documented (PRIMARY: auto.function.react.*; SECONDARY: generic expected; PARTIAL: solo generic blocked) augmented with Round-3 LOW-5 escalation rule (rename → CONTEXT-owner override recorded inline), Round-2 MEDIUM-2 contradictory grep dropped from acceptance criteria, Round-2 MEDIUM-4 jq-based names[] extraction referenced (with Round-3 LOW-1 corrected Node fallback documented inline), Round-2 HIGH-2 plugin-disabled inspection build referenced (with Round-3 LOW-2 temporary worktree pattern documented inline), and the artifacts/ directory exists as a tracked location for the screenshot PNGs.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run 3-way same-session build comparison (main / phase-7-without-keepNames / phase-7-with-keepNames) and write OBSV-02-bundle-delta.md (Round-2 MEDIUM-3 — keepNames-only attribution; Round-3 LOW-4 — Vite gzip column single-source-of-truth)</name>
  <files>.planning/closure/OBSV-02-bundle-delta.md</files>
  <read_first>
    - .planning/phases/07-observability-hardening/07-CONTEXT.md (D-11: dedicated `.planning/closure/OBSV-02-bundle-delta.md`; D-12: total + per-chunk; D-13: Vite/Rolldown built-in build output; D-14: overage policy)
    - .planning/phases/07-observability-hardening/07-PATTERNS.md (`.planning/closure/OBSV-02-bundle-delta.md` NEW section — recommended structure: frontmatter + Method + Total table + Per-chunk table + Target check + Raw build output)
    - .planning/phases/07-observability-hardening/07-RESEARCH.md (Pitfall 6 lines 529-537: baseline drift mitigation — same session for all measurements)
    - .planning/phases/07-observability-hardening/07-REVIEWS.md (MEDIUM-3 — 3-way comparison: main + phase-7-without-keepNames + phase-7-with-keepNames; D-14 policy applied to keepNames-isolated delta, not to total Phase 7 delta; Round-3 LOW-4 — Vite printed per-chunk gzip column is the single source of truth, drop the alternative `find | xargs gzip` method)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VERIFICATION.md (`## Behavioral Spot-Checks` and `## Requirements Coverage` table styles — closest in-repo "before/after measurement" analogs)
  </read_first>
  <action>
    **Step A — Create the closure directory if it does not exist.** Run `mkdir -p .planning/closure` (this is the first instance under `.planning/closure/` per RESEARCH lines 219-220 + PATTERNS "No Analog Found" entry).

    **Step B — Run a 3-way same-session build comparison using git worktree (Round-2 MEDIUM-3).** RESEARCH Pitfall 6 mitigation: all measurements MUST come from the same shell session to eliminate baseline drift. Round-2 MEDIUM-3 isolates the keepNames flag's contribution from the rest of Phase 7's shipping cost — without this 3-way comparison, the total `main → phase-7` delta conflates `keepNames: true` with the new `RenderThrowSmoke` chunk and the `__smoke` route, and we cannot honestly claim "the keepNames bundle delta is X%".

    **Round-3 LOW-4 — single source of truth for gzip values.** All gzip cells in this closure doc come from Vite/Rolldown's printed per-chunk gzip column (the `gzip:` column in the build output table). D-13 specifies "Vite/Rolldown built-in tool"; the alternative `find dist/assets -name '*.js' | xargs gzip -c | wc -c` method (cross-file gzip) can produce different numbers from per-file gzip then summed. Picking Vite's column eliminates audit ambiguity. Total = sum of per-chunk gzip values from the printed table.

    1. Note the current Phase 7 branch name (e.g., `gsd/phase-07-observability-hardening`).
    2. Capture the exact main-branch base commit SHA: `BASE_SHA=$(git merge-base HEAD origin/main)` (this is the SHA main was at when Phase 7 branched; it will be recorded in the closure doc for audit reproducibility).
    3. Capture the current Phase 7 branch HEAD SHA: `PHASE7_SHA=$(git rev-parse HEAD)`.
    4. **Worktree #1 — main baseline.** Create the baseline worktree: `git worktree add /tmp/p07-baseline $BASE_SHA`. Then: `cd /tmp/p07-baseline && rm -rf dist node_modules/.vite && npm ci && npm run build 2>&1 | tee /tmp/p07-baseline-build.log`. Capture full stdout. Vite/Rolldown prints a per-chunk table near the end including a `gzip:` column.
    5. **Round-3 LOW-4 — total gzip from Vite's printed column ONLY.** Sum the per-chunk gzip values from the Vite output table (parse `/tmp/p07-baseline-build.log`). Record the total AND the parsing method (e.g., "summed all numeric values from the `gzip:` column of Vite's printed table"). DO NOT use `find ... | xargs gzip -c | wc -c` — that method has been removed from this plan per Round-3 LOW-4.
    6. Remove worktree #1: `git worktree remove /tmp/p07-baseline --force`.
    7. **Worktree #2 — phase-7 WITHOUT keepNames** (Round-2 MEDIUM-3 — clean per-flag attribution). Create a worktree at the Phase 7 HEAD: `git worktree add /tmp/p07-no-keepnames $PHASE7_SHA`. Then revert ONLY the keepNames addition in `vite.config.ts`:
       ```bash
       cd /tmp/p07-no-keepnames
       # Surgical revert — remove only the rolldownOptions.output.keepNames block, keep everything else (smoke route, RenderThrowSmoke component, all of Plan 01 Task 1, netlify.toml).
       # Use a temp script or hand-edit so vite.config.ts becomes:
       #   build: { sourcemap: 'hidden' },
       # i.e. revert to the line shape from before Plan 01 Task 2's edit, but leave EVERY OTHER Phase 7 file intact.
       # Verify the surgical revert: grep -c 'keepNames' vite.config.ts must return 0; the rest of Phase 7 is intact.
       ```
       Then: `rm -rf dist node_modules/.vite && npm ci && npm run build 2>&1 | tee /tmp/p07-no-keepnames-build.log`. Capture stdout.
    8. Sum total gzip from Vite's printed column in `/tmp/p07-no-keepnames-build.log` the same way (Round-3 LOW-4 — same single source of truth). Record value.
    9. Remove worktree #2: `git worktree remove /tmp/p07-no-keepnames --force`.
    10. **Worktree #3 — phase-7 WITH keepNames (the actual Phase 7 build).** Back in the Phase 7 working tree (no worktree needed; this is the current tree): `rm -rf dist node_modules/.vite && npm ci && npm run build 2>&1 | tee /tmp/p07-keepnames-build.log`. Capture full stdout.
    11. Sum total gzip from Vite's printed column in `/tmp/p07-keepnames-build.log` the same way.
    12. Compute the two deltas:
        - **keepNames-isolated delta (Round-2 MEDIUM-3 — clean per-flag attribution; THE OBSV-02 commitment surface):** `(worktree #3 total) − (worktree #2 total)`. This is the apples-to-apples cost of just toggling `keepNames: true` on the Phase 7 codebase.
        - **Total Phase 7 delta (audit shipping cost):** `(worktree #3 total) − (worktree #1 total)`. This is what visitors of the deployed site actually download more.
        - The keepNames-isolated delta is what D-14's 1.5% target applies to (it's the OBSV-02 commitment); the total Phase 7 delta is informational.

    **Step C — Write `.planning/closure/OBSV-02-bundle-delta.md`** with this EXACT structure. All frontmatter delimiters MUST be `---` (three hyphens), NOT `___` (three underscores).

    ```markdown
    ---
    requirement: OBSV-02
    measured: <ISO-8601 timestamp at moment of measurement — UTC>
    target_pct: 1.5
    keepnames_isolated_delta_pct: <computed delta as a number, e.g. 0.92 — the OBSV-02 commitment surface, Round-2 MEDIUM-3>
    total_phase7_delta_pct: <computed total Phase 7 shipping delta as a number, e.g. 1.87>
    status: <within-target | over-target-accepted>
    measured_by: Khai
    base_sha: <the BASE_SHA from git merge-base HEAD origin/main — for audit reproducibility>
    phase7_sha: <the PHASE7_SHA from git rev-parse HEAD>
    method: same-session-3-way-git-worktree (RESEARCH Pitfall 6 + Round-2 MEDIUM-3 — main / phase-7-without-keepNames / phase-7-with-keepNames; worktree avoids branch mutation; 3-way isolates keepNames flag from rest of Phase 7 surface area)
    gzip_source: vite-printed-gzip-column (Round-3 LOW-4 — single source of truth per D-13)
    ---

    # OBSV-02: keepNames bundle-size delta (Round-2 MEDIUM-3 — 3-way attribution; Round-3 LOW-4 — Vite-column source-of-truth)

    Compares total + per-chunk gzipped output of `dist/` across THREE builds run in the same shell session: (a) main-branch base commit (no `keepNames`, no Phase 7 surface); (b) Phase 7 branch with `keepNames` REMOVED via surgical revert (isolates keepNames effect); (c) Phase 7 branch with `keepNames` enabled (the actual ship). The keepNames-only delta is `(c) − (b)`; the total Phase 7 shipping delta is `(c) − (a)`. The OBSV-02 ≤1.5% target applies to the **keepNames-only delta** — the rest of Phase 7's surface area (the new `RenderThrowSmoke` chunk + `__smoke` route) is unrelated to OBSV-02 and would ship even without the keepNames flag.

    Round-2 MEDIUM-3 (Codex review) flagged that comparing main directly to phase-7 conflates keepNames with the new chunk; this 3-way comparison resolves that. Round-3 LOW-4 alignment: this doc uses Vite's printed per-chunk gzip column as the single source of truth per D-13 (cross-file gzip alternatives can produce different numbers from per-file gzip summed).

    ## Method

    1. Single shell session, same hardware, Node 22 (per `netlify.toml` `[build.environment]` `NODE_VERSION = "22"`).
    2. **Build (a) — main baseline:** `git worktree add /tmp/p07-baseline <BASE_SHA>` → `npm ci && npm run build`. Worktree removed after measurement.
    3. **Build (b) — phase-7 WITHOUT keepNames:** `git worktree add /tmp/p07-no-keepnames <PHASE7_SHA>` → surgical revert of `vite.config.ts` rolldownOptions.output.keepNames addition (every other Phase 7 file intact) → `npm ci && npm run build`. Worktree removed after measurement.
    4. **Build (c) — phase-7 WITH keepNames:** Phase 7 working tree, `rm -rf dist node_modules/.vite && npm ci && npm run build`.
    5. All builds run with the same `npm run build` script: `tsr generate && tsc -b && vite build`.
    6. Vite/Rolldown's printed per-chunk table is captured verbatim (D-13 — no new tooling).
    7. **Round-3 LOW-4 — total gzip method:** sum of Vite's printed `gzip:` column for all chunks in each build. This is the single source of truth per D-13 (cross-file gzip-stream methods are not used because they can produce different numbers from per-file gzip summed).
    8. Base SHA for audit: `<BASE_SHA>` (from `git merge-base HEAD origin/main` at measurement time). Phase 7 SHA: `<PHASE7_SHA>`.
    9. Per-chunk: row-by-row from each Vite/Rolldown output table; chunks present only in (b) or (c) (e.g. the lazy-split `RenderThrowSmoke` chunk) are annotated `(new in phase-7)`.

    ## Total gzip delta

    | Build | Commit SHA | Total gzip (bytes — sum of Vite gzip column) | Delta vs main | Delta % |
    |-------|------------|----------------------------------------------|---------------|---------|
    | (a) main (base) | `<BASE_SHA short>` | `<bytes>` | — | — |
    | (b) phase-7 WITHOUT keepNames | `<PHASE7_SHA short> + revert` | `<bytes>` | `+<bytes>` | `+<x.xx>%` |
    | (c) phase-7 WITH keepNames | `<PHASE7_SHA short>` | `<bytes>` | `+<bytes>` | `+<x.xx>%` |

    **keepNames-isolated delta (Round-2 MEDIUM-3 — the OBSV-02 commitment):** `(c) − (b) = +<bytes>` = `+<x.xx>%`.

    **Total Phase 7 shipping delta (informational):** `(c) − (a) = +<bytes>` = `+<x.xx>%`.

    ## Per-chunk gzip table

    Source: Vite's printed per-chunk gzip column for each build (Round-3 LOW-4).

    | Chunk | (a) main (gzip B) | (b) p7-no-keepNames (gzip B) | (c) p7-keepNames (gzip B) | keepNames-only delta B | keepNames-only delta % |
    |-------|-------------------|------------------------------|---------------------------|-----------------------|------------------------|
    | `<index-hash>.js` | `<n>` | `<n>` | `<n>` | `+<n>` | `+<x.x>%` |
    | `<vendor-or-react-hash>.js` | `<n>` | `<n>` | `<n>` | `+<n>` | `+<x.x>%` |
    | `<route-chunks each on its own row>` | `<n>` | `<n>` | `<n>` | `+<n>` | `+<x.x>%` |
    | `<RenderThrowSmoke chunk>.js` | — | `<n>` | `<n>` | `+<n>` | `+<x.x>%` (new in phase-7; keepNames effect: column delta) |
    | `<index-hash>.css` | `<n>` | `<n>` | `<n>` | `<delta>` | `<x.x>%` |

    ## Target check

    Target: ≤ 1.5% **keepNames-isolated** total gzip delta (D-14 + Round-2 MEDIUM-3 — applied to the per-flag delta, not the total Phase 7 delta).

    keepNames-isolated delta: `<x.xx>%`. Total Phase 7 shipping delta: `<x.xx>%` (informational only — includes the new `__smoke` route + `RenderThrowSmoke` chunk + any minor codegen output drift).

    `<one of:>`
    - "keepNames-isolated delta `<actual>%` — within ≤1.5% target. Phase 7 acceptable as designed."
    - "keepNames-isolated delta `<actual>%` — over target by `<delta-pp>` pp; observability gain accepted per CONTEXT D-14, revisit if Netlify caching costs become a concern."

    ## Raw build output

    ### (a) main baseline (worktree at base SHA <BASE_SHA>)

    ```text
    <paste the FULL Vite/Rolldown output table from /tmp/p07-baseline-build.log here — typically 30-60 lines including the per-chunk table>
    ```

    ### (b) phase-7 WITHOUT keepNames (worktree at <PHASE7_SHA> with surgical revert)

    ```text
    <paste the FULL Vite/Rolldown output table from /tmp/p07-no-keepnames-build.log here>
    ```

    ### (c) phase-7 WITH keepNames

    ```text
    <paste the FULL Vite/Rolldown output table from /tmp/p07-keepnames-build.log here>
    ```

    ---
    _Measured: <ISO timestamp>_
    _Measurer: Khai_
    _Build commands: `rm -rf dist node_modules/.vite && npm ci && npm run build` × 3 (same session, see Method)_
    _Pitfall 6 mitigation: all builds run in same session via git worktree at `<BASE_SHA>` and `<PHASE7_SHA>`; no branch switching occurred_
    _Round-2 MEDIUM-3 mitigation: 3-way comparison isolates keepNames-only delta from total Phase 7 shipping cost; D-14 target applied to keepNames-isolated delta only_
    _Round-3 LOW-4 alignment: gzip values sourced from Vite's printed per-chunk gzip column only (single source of truth per D-13)._
    ```

    Replace every `<placeholder>` with actual data from your captured logs. NO `<...>` placeholder tokens or `{{...}}` tokens may remain in the committed file. After writing the file, run the placeholder-detection check:
    ```bash
    grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' .planning/closure/OBSV-02-bundle-delta.md
    ```
    This must return no output (exit code 1). If any placeholder tokens remain, fix them before proceeding.

    Decision IDs implemented: D-11 (path under `.planning/closure/`), D-12 (total + per-chunk granularity, now 3-way per Round-2 MEDIUM-3), D-13 (Vite/Rolldown built-in tool — Round-3 LOW-4 picks the printed gzip column as single source of truth), D-14 (overage policy applied — within-target OR over-target-accepted with rationale; applied to keepNames-isolated delta per Round-2 MEDIUM-3).
  </action>
  <verify>
    <automated>test -f .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Total gzip delta' .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Per-chunk gzip table' .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Target check' .planning/closure/OBSV-02-bundle-delta.md; grep -c '## Raw build output' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^requirement: OBSV-02$' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^base_sha:' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^phase7_sha:' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^keepnames_isolated_delta_pct:' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^total_phase7_delta_pct:' .planning/closure/OBSV-02-bundle-delta.md; grep -c '^gzip_source: vite-printed-gzip-column' .planning/closure/OBSV-02-bundle-delta.md; grep -E '^status: (within-target|over-target-accepted)$' .planning/closure/OBSV-02-bundle-delta.md; grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' .planning/closure/OBSV-02-bundle-delta.md; head -1 .planning/closure/OBSV-02-bundle-delta.md | grep -c '^---$'; grep -c 'p7-no-keepnames\|phase-7 WITHOUT keepNames' .planning/closure/OBSV-02-bundle-delta.md; grep -c "find.*xargs.*gzip\|xargs -I{} gzip" .planning/closure/OBSV-02-bundle-delta.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/closure/OBSV-02-bundle-delta.md` exists (and the `.planning/closure/` directory was created).
    - Frontmatter uses `---` delimiters (hyphens), NOT `___` (underscores): `head -1 OBSV-02-bundle-delta.md | grep -c '^---$'` returns 1.
    - Frontmatter contains `requirement: OBSV-02`, `target_pct: 1.5`, `keepnames_isolated_delta_pct:` (numeric — Round-2 MEDIUM-3 the OBSV-02 commitment surface), `total_phase7_delta_pct:` (numeric — informational shipping cost), `status: within-target` OR `status: over-target-accepted`, `base_sha:` (the merge-base commit SHA for audit reproducibility), `phase7_sha:` (Phase 7 HEAD SHA), AND **Round-3 LOW-4: `gzip_source: vite-printed-gzip-column`** (records the single source of truth for gzip values).
    - All five sections present: `## Method`, `## Total gzip delta`, `## Per-chunk gzip table`, `## Target check`, `## Raw build output`.
    - **Round-2 MEDIUM-3 — Total gzip table has exactly 3 data rows:** main, phase-7-without-keepNames, phase-7-with-keepNames.
    - Per-chunk gzip table has at least 3 data rows including at least one chunk annotated `(new in phase-7)` if the lazy `RenderThrowSmoke` split produced a fresh chunk in Phase 7.
    - The Total gzip delta section explicitly cites BOTH a "keepNames-isolated delta" line and a "Total Phase 7 shipping delta" line (Round-2 MEDIUM-3).
    - Method section references `git worktree` (not `git checkout main`) for all three build isolation steps; section explicitly mentions the surgical-revert step for build (b).
    - **Round-3 LOW-4 — single gzip method:** Method paragraph step 7 explicitly states gzip values come from Vite's printed per-chunk gzip column ONLY; the `find ... | xargs gzip -c | wc -c` method is NOT documented as an accepted alternative. `grep -c "find.*xargs.*gzip\|xargs -I{} gzip" .planning/closure/OBSV-02-bundle-delta.md` returns 0 (the alternative method must NOT appear as a measurement option in the doc).
    - Target check section ends with one of the two D-14 sentences (within-target or over-target-accepted with revisit rationale), AND explicitly states the target applies to the keepNames-isolated delta (not the total Phase 7 delta).
    - Raw build output section contains THREE fenced text blocks (one per worktree), each at least 10 lines (the actual `npm run build` per-chunk tables).
    - Placeholder-detection: `grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' OBSV-02-bundle-delta.md` returns no output (exit 1 = no matches). `grep -c '<bytes>' OBSV-02-bundle-delta.md` returns 0. `grep -c '<x\.x>' OBSV-02-bundle-delta.md` returns 0.
    - Footer trailer present: `_Measured:`, `_Measurer:`, `_Build commands:`, `_Pitfall 6 mitigation:`, `_Round-2 MEDIUM-3 mitigation:`, `_Round-3 LOW-4 alignment:`.
  </acceptance_criteria>
  <done>
    `.planning/closure/OBSV-02-bundle-delta.md` exists with `---` frontmatter delimiters, `base_sha` + `phase7_sha` fields for audit reproducibility, separate `keepnames_isolated_delta_pct` and `total_phase7_delta_pct` frontmatter fields per Round-2 MEDIUM-3, `gzip_source: vite-printed-gzip-column` frontmatter field per Round-3 LOW-4, a Method paragraph documenting same-session 3-way `git worktree` measurement (no branch switching) including the surgical keepNames-revert step for build (b) and explicitly stating Vite's printed gzip column as the single source of truth (Round-3 LOW-4), a 3-row total gzip table, a per-chunk table with one row per `dist/` chunk across all three builds, a Target check section applying the D-14 policy to the keepNames-isolated delta (not the total Phase 7 delta), three raw build-output blocks pasted verbatim from `npm run build` for the three worktrees, no placeholder tokens remaining, and a footer pinning the measurer + Pitfall 6 mitigation + Round-2 MEDIUM-3 mitigation + Round-3 LOW-4 alignment.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → public repo | Sentry permalink + release SHA committed to `.planning/phases/07-observability-hardening/07-VERIFICATION.md`. Permalink resolves only inside the project's Sentry org (auth-gated by Sentry side). |
| documentation → public repo | `.planning/closure/OBSV-02-bundle-delta.md` exposes per-chunk byte sizes — public information already inferable from production `dist/` over HTTPS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | I (Information disclosure) | Committed Sentry permalink could be probed by scrapers (e.g. github-search-leak bots) | accept | Permalink itself is not a secret — it requires Sentry org auth to view the event. The deterministic message string (`'RenderThrowSmoke: deliberate render-phase throw for Sentry verification'`) is benign and will appear in repo regardless. |
| T-07-11 | I (Information disclosure) | Committed bundle-size table reveals chunk sizes at a specific commit | accept | Same trust level as the deployed bundle itself — anyone can fetch the prod URL and measure. No new disclosure. |
| T-07-12 | S (Spoofing) | Manual evidence capture relies on solo sign-off (D-09); no second-human verifier | accept | CONTEXT D-09 explicit decision — OBSV-01/02 are config + wiring, not security/auth gates, and the Sentry event itself is independently re-openable via the permalink (T-07-10). The second-human session pool is reserved for Phase 03 UAT 2+3 carry-forward in Phase 8. Solo sign-off is acceptable here. |
| T-07-13 | T (Tampering) | A future maintainer could edit 07-VERIFICATION.md to claim a passing status without re-running the smoke | mitigate | The Sentry permalink + release SHA + event timestamp + environment tag pinned in the doc are independently verifiable: anyone can open the permalink and confirm mechanism.type, componentStack, frame names, exception value, and environment against what the doc claims. Re-verification is single-click on any future deploy preview. Round-2 LOW-2 + HIGH-1 fixes increase the granularity of what can be re-verified. |
</threat_model>

<verification>
- All three artifact files exist (07-VERIFICATION.md, OBSV-02-bundle-delta.md, artifacts/.gitkeep).
- Both files use `---` frontmatter delimiters (not `___`).
- 07-VERIFICATION.md frontmatter scores 5/5 ROADMAP success criteria; status = passed.
- 07-VERIFICATION.md ## Human Verification Required has all four D-08 evidence rows populated with real captures (no `<...>` or `{{...}}` placeholder tokens) PLUS the Round-2 LOW-2 fifth row for `exception.values[0].value`.
- Event timestamp and environment tag recorded; environment confirms 'deploy-preview' or 'branch-deploy', NOT 'production' (Round-2 HIGH-1 sanity check passes).
- OBSV-02-bundle-delta.md frontmatter has `keepnames_isolated_delta_pct` AND `total_phase7_delta_pct` (Round-2 MEDIUM-3), a `base_sha` field, a `phase7_sha` field, a `gzip_source: vite-printed-gzip-column` field (Round-3 LOW-4), and a within-target / over-target-accepted status.
- OBSV-02-bundle-delta.md per-chunk table contains rows for all three builds (main / phase-7-without-keepNames / phase-7-with-keepNames); at least one row annotated `(new in phase-7)` for the lazy RenderThrowSmoke chunk if it appeared.
- OBSV-02-bundle-delta.md Method section references `git worktree` (not `git checkout main`) and explicitly mentions the surgical keepNames-revert step for build (b).
- OBSV-02-bundle-delta.md Total gzip delta section reports BOTH a keepNames-isolated delta AND a total Phase 7 shipping delta (Round-2 MEDIUM-3).
- OBSV-02-bundle-delta.md does NOT document the `find ... | xargs gzip -c | wc -c` alternative gzip method (Round-3 LOW-4 — single source of truth).
- Placeholder-detection: `grep -E '\{\{.*\}\}|TODO|XXX|<replace_me>|<paste' 07-VERIFICATION.md OBSV-02-bundle-delta.md` returns no output.
- Sentry event mechanism.type recorded with at least one event in {auto.function.react.error_handler, auto.function.react.error_boundary} (Round-2 MEDIUM-1 PRIMARY pass) — additional `generic` events documented as expected belt companions, not as standalone passes. Round-3 LOW-5 escalation rule applied if (and only if) a Sentry patch-release rename was observed; override decision recorded inline.
- `exception.values[0].value` matches deterministic smoke message (Round-2 LOW-2).
- Both ROADMAP REQ-IDs (OBSV-01, OBSV-02) flip from Pending → ✅ pass with inline evidence.
</verification>

<success_criteria>
- All four D-08 evidence artifacts captured against the Phase 7 PR's Netlify deploy preview (NOT dev, NOT Vitest — ROADMAP SC #5).
- `.planning/phases/07-observability-hardening/07-VERIFICATION.md` exists with the 12-section template scaled to Phase 7 (5 SC, 7 artifacts, no plan-must-have-set count), `---` delimiters, event timestamp + environment tag (`deploy-preview` or `branch-deploy` per Round-2 HIGH-1) recorded, mechanism.type two-tier criteria documented per Round-2 MEDIUM-1 (PRIMARY: ≥1 auto.function.react.* event; SECONDARY: generic expected; PARTIAL: solo generic blocked) augmented with Round-3 LOW-5 rename-escalation rule, `exception.values[0].value` verified per Round-2 LOW-2, Round-3 LOW-1 corrected Node fallback documented, Round-3 LOW-2 temporary worktree pattern documented.
- `.planning/closure/OBSV-02-bundle-delta.md` exists with `git worktree`-based same-session 3-way baseline-vs-no-keepNames-vs-with-keepNames measurement (Round-2 MEDIUM-3), `base_sha` and `phase7_sha` fields for audit reproducibility, separate `keepnames_isolated_delta_pct` and `total_phase7_delta_pct` frontmatter fields, `gzip_source: vite-printed-gzip-column` frontmatter field (Round-3 LOW-4), total + per-chunk granularity, `---` delimiters, explicit D-14 policy application to the keepNames-isolated delta, single-source-of-truth gzip method (Round-3 LOW-4 — `find | xargs gzip` removed), and zero remaining placeholder tokens.
- `artifacts/.gitkeep` makes the screenshot directory a tracked path; PNGs are committed if < 1MB and gitignored otherwise per D-10.
- **Round-2 MEDIUM-1 — PRIMARY pass:** Sentry event mechanism.type includes at least one of `{auto.function.react.error_handler, auto.function.react.error_boundary}` — anything else is a Phase 7 failure mode (Pitfall 1) OR (if only `generic` observed) a PARTIAL pass blocking sign-off. **Round-3 LOW-5:** if observed string differs from the literal allowlist due to a Sentry patch-release rename but clearly maps to a PRIMARY path, CONTEXT-owner override recorded inline.
- Top stack frames show un-mangled `RootLayout` / `RenderThrowSmoke` / `SmokePage` / route component identifiers — anything else means keepNames did not take effect or sourcemap upload failed (run Round-2 Cursor rollback path before assuming keepNames-broken).
- **Round-2 LOW-2:** `exception.values[0].value` matches deterministic smoke message string verbatim.
- Both REQ-IDs (OBSV-01, OBSV-02) flip Pending → ✅ pass in `## Requirements Coverage`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-observability-hardening/07-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Frontmatter must include `requirements-completed: [OBSV-01, OBSV-02]` (final evidence captured against deploy preview; both REQs flip from Pending → ✅ pass — this is the plan that marks requirements as fully completed, not merely addressed).
</output>
