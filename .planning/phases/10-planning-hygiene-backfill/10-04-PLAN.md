---
phase: 10-planning-hygiene-backfill
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-admin-panel-suggestion-management/04-UAT.md
  - .planning/phases/10-planning-hygiene-backfill/10-04-SUMMARY.md
autonomous: true
requirements: [DOCS-04]

must_haves:
  truths:
    - "04-UAT.md contains a new top-level '## Off-Record Verification' section"
    - "The section has a sub-block for UAT 6a with fields: Test, Executor, Verified at, Result, Notes"
    - "The Executor field cites MapCommittee (Discord ID 290377966251409410)"
    - "The Result field is PASS"
    - "The Notes field describes the demote click flow and points to 13 unit tests"
    - "The original deferred 6a row (result: deferred) remains untouched"
  artifacts:
    - path: ".planning/phases/04-admin-panel-suggestion-management/04-UAT.md"
      provides: "Phase 04 UAT with Off-Record Verification evidence for test 6a"
      contains: "Off-Record Verification"
  key_links:
    - from: "D-10 (section heading)"
      to: "04-UAT.md new section"
      via: "append — do not modify original 6a row"
      pattern: "Off-Record Verification"
    - from: "D-11 (field schema)"
      to: "Off-Record Verification sub-block"
      via: "Test / Executor / Verified at / Result / Notes fields"
      pattern: "MapCommittee"
    - from: "Phase 8 D-12 pattern"
      to: "section structure"
      via: "mirrors 03-UAT.md Second-Human Verification shape"
      pattern: "executor:"
---

<objective>
Append an "Off-Record Verification" section to `.planning/phases/04-admin-panel-suggestion-management/04-UAT.md` recording Phase 04 UAT test 6a (demote admin click flow) as passed, citing the off-record execution on the second admin (Discord ID `290377966251409410` / MapCommittee) during the v1.0 → v1.1 transition.

Purpose: Test 6a was deferred because MapCommittee had never signed in. The demote click flow was subsequently verified off-record when MapCommittee signed in during the v1.0 → v1.1 transition. This append records that pass evidence without modifying the original deferred record (historical preservation per D-10).

Output: Append-only edit to 04-UAT.md, closing DOCS-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-planning-hygiene-backfill/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Off-Record Verification section to 04-UAT.md</name>
  <files>
    .planning/phases/04-admin-panel-suggestion-management/04-UAT.md
  </files>

  <read_first>
    - .planning/phases/04-admin-panel-suggestion-management/04-UAT.md (file being modified — read ENTIRE file; locate the end of the file and the original test 6 / 6a section; verify the deferred 6a row is still present and will NOT be touched)
    - .planning/phases/08-e2e-test-hygiene/08-CONTEXT.md § D-12 (Second-Human Verification section pattern — field schema to mirror)
    - .planning/phases/08-e2e-test-hygiene/08-UAT-10-SCRIPT.md (evidence-append template analogue — structure for sub-block fields)
    - .planning/phases/03-response-integrity/03-UAT.md § Second-Human Verification (the exact shape to mirror for Off-Record Verification)
  </read_first>

  <action>
Append a new top-level `## Off-Record Verification` section at the END of `04-UAT.md`. Do NOT modify any existing content — pure append after the last line of the file.

Per D-10: Use heading "## Off-Record Verification" (consistent with Phase 8 D-12 Second-Human Verification pattern but adapted for an in-house second admin, not a second external human). The original deferred 6a row (`result: deferred`) remains untouched.

Per D-11: Field schema for the sub-block:

```markdown
## Off-Record Verification

> Phase 10 / DOCS-04 closure. UAT test 6a (Live demote-admin click flow) was deferred
> at v1.0 ship time because the second seeded admin (Discord ID 290377966251409410 /
> MapCommittee) had never signed in. MapCommittee signed in during the v1.0 → v1.1
> transition, enabling the demote click flow to be verified off-record. This section
> records that pass evidence per CONTEXT.md D-10/D-11.
>
> The original `result: deferred` record above is preserved — it documents the historical
> reason for deferral. This section records the subsequent pass.

### UAT 6a — Live Demote-Admin Click Flow

- Test: UAT 6a — Live demote-admin click flow
- Executor: MapCommittee (Discord ID `290377966251409410`)
- Verified at: during v1.0 → v1.1 transition (2026-04-28 → 2026-05-07); no precise UTC timestamp available from git/audit records
- Result: PASS
- Notes: |
    MapCommittee signed in during the v1.0 → v1.1 transition, appearing in the Admins
    list. The admin who ran the v1.0 UAT re-run (YES KiNG / Discord ID 267747104607305738)
    was able to see the Demote button on the MapCommittee row (D-06 self-demote guard
    correctly hides the button on the executor's own row). The demote click flow opened
    a confirmation dialog; confirming removed MapCommittee from the admin list with a
    success toast — matching the expected behavior from UAT test 6 specification.
    Demote logic source-side coverage: 13 unit tests in `src/__tests__/admin/demote-admin.test.ts`.
```

Exact append instructions:
1. Read the full file to find the last line
2. Append the section above starting with a blank line separator, then the `## Off-Record Verification` heading
3. Do NOT change any byte of the existing file content above the append point
  </action>

  <verify>
    <automated>grep -q "## Off-Record Verification" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "SECTION EXISTS" || echo "SECTION MISSING"</automated>
    <automated>grep -q "MapCommittee" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "EXECUTOR HANDLE OK" || echo "EXECUTOR HANDLE MISSING"</automated>
    <automated>grep -q "290377966251409410" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "DISCORD ID OK" || echo "DISCORD ID MISSING"</automated>
    <automated>grep -qE "Result: PASS|result: PASS|result: pass" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "RESULT OK" || echo "RESULT MISSING"</automated>
    <automated>grep -q "result: deferred" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "ORIGINAL DEFERRED PRESERVED" || echo "ORIGINAL DEFERRED MISSING (BAD)"</automated>
    <automated>grep -qE "13 unit tests|demote-admin.test.ts" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "NOTES OK" || echo "NOTES MISSING"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "## Off-Record Verification" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md` exits 0
    - `grep -q "MapCommittee\|290377966251409410" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md` exits 0 (D-11 Executor field)
    - `grep -q "result: deferred" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md` exits 0 (original 6a row preserved)
    - `grep -q "13 unit tests\|demote-admin.test.ts" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md` exits 0 (D-11 Notes field)
    - File still starts with the YAML frontmatter block (no corruption at top)
    - Original test 6 section text unchanged (verify by reading the test 6 section before and after)
  </acceptance_criteria>

  <done>04-UAT.md has a new ## Off-Record Verification section at the end with UAT 6a evidence citing MapCommittee / Discord ID 290377966251409410, Result: PASS, and a pointer to 13 unit tests. Original deferred 6a row preserved. DOCS-04 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ edits | Documentation-only; not deployed, not user-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | Historical accuracy of 04-UAT.md | mitigate | Append-only operation — the read_first instruction requires the executor to read the full file before editing, and to verify the original deferred 6a row is untouched. Git diff provides audit trail. The Notes field must cite the 13 unit tests as corroborating source-side evidence for the off-record pass claim. |

attack_surface: "none — documentation-only append to a .planning/ artifact; not deployed, not user-facing"
</threat_model>

<verification>
```bash
grep -q "## Off-Record Verification" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "SECTION OK"
grep -q "MapCommittee\|290377966251409410" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "EXECUTOR OK"
grep -q "result: deferred" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "ORIGINAL PRESERVED"
grep -q "demote-admin.test.ts\|13 unit" .planning/phases/04-admin-panel-suggestion-management/04-UAT.md && echo "NOTES OK"
```
</verification>

<success_criteria>
- 04-UAT.md has a new `## Off-Record Verification` section at end of file (D-10)
- Section has UAT 6a sub-block with Test / Executor / Verified at / Result / Notes fields (D-11)
- Executor field cites MapCommittee (Discord ID 290377966251409410)
- Result is PASS
- Notes describes demote click flow and cites 13 unit tests
- Original deferred 6a row is untouched (historical preservation per D-10)
- REQUIREMENTS.md DOCS-04 checkbox remains [ ] — flipped to [x] by Plan 05 (closure)
</success_criteria>

<output>
After completion, create `.planning/phases/10-planning-hygiene-backfill/10-04-SUMMARY.md`

Include in frontmatter:
```yaml
requirements-completed: [DOCS-04]
```
</output>
