---
phase: 10-planning-hygiene-backfill
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md
  - .planning/phases/01-foundation-authentication/01-04-SUMMARY.md
  - .planning/phases/02-browsing-responding/02-01-SUMMARY.md
  - .planning/phases/02-browsing-responding/02-02-SUMMARY.md
  - .planning/phases/02-browsing-responding/02-03-SUMMARY.md
  - .planning/phases/02-browsing-responding/02-04-SUMMARY.md
  - .planning/phases/03-response-integrity/03-02-SUMMARY.md
  - .planning/phases/04-admin-panel-suggestion-management/04-02-SUMMARY.md
  - .planning/phases/04-admin-panel-suggestion-management/04-04-SUMMARY.md
  - .planning/phases/04-admin-panel-suggestion-management/04-05-SUMMARY.md
  - .planning/phases/10-planning-hygiene-backfill/10-03-SUMMARY.md
autonomous: false  # CONTEXT.md D-08 says single-source REQ-ID candidates "are flagged
                   # as discrepancies in 10-RESEARCH.md for resolution by the user
                   # BEFORE write — do NOT silently include or exclude them."
                   # Cross-AI review (HIGH #2) flagged that autonomous: true contradicts
                   # this. Task 1 produces the discrepancy file; Task 2 may not run
                   # until the user has reviewed any discrepancies and recorded a
                   # decision in 10-DOCS-03-DISCREPANCIES.md. autonomous: false makes
                   # the human-gate explicit at execute-phase time.
requirements: [DOCS-03]

must_haves:
  truths:
    - "Task 1 produces 10-DOCS-03-DISCREPANCIES.md with cross-check results for all 9 SUMMARY files BEFORE any SUMMARY file is touched (D-08; review HIGH #2)"
    - "Task 2 may NOT modify any SUMMARY file until the discrepancy file contains either a 'No discrepancies found' marker OR a user-populated 'Resolved-by: <handle> on <date>' sign-off line"
    - "All 9 SUMMARY files that are missing requirements-completed frontmatter have the field added"
    - "REQ-IDs declared in each file are grounded in ≥2 of 3 cross-check sources (D-07/D-08)"
    - "Single-source-only candidates are documented in a discrepancy record AND surfaced for explicit user resolution before any writes"
    - "No REQ-ID is fabricated — every declared ID is traceable to REQUIREMENTS.md, a VERIFICATION.md traceability row, or the milestone audit"
    - "Existing frontmatter content in each SUMMARY file is preserved; only requirements-completed is added"
  artifacts:
    - path: ".planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md"
      provides: "Discrepancy table for single-source candidates (produced before writes per D-08)"
      contains: "discrepancy"
    - path: ".planning/phases/01-foundation-authentication/01-04-SUMMARY.md"
      provides: "requirements-completed frontmatter with AUTH-04, AUTH-05, TEST-02"
      contains: "requirements-completed:"
    - path: ".planning/phases/02-browsing-responding/02-01-SUMMARY.md"
      provides: "requirements-completed frontmatter"
      contains: "requirements-completed:"
    - path: ".planning/phases/03-response-integrity/03-02-SUMMARY.md"
      provides: "requirements-completed: [VOTE-04]"
      contains: "requirements-completed:"
  key_links:
    - from: "D-07 (3 sources)"
      to: "REQ-ID declarations"
      via: "cross-check: VERIFICATION.md traceability + REQUIREMENTS.md + milestone audit"
      pattern: "requirements-completed:"
    - from: "D-08 (≥2 of 3)"
      to: "discrepancy surfacing"
      via: "single-source candidates flagged before write"
      pattern: "discrepancy"
    - from: "D-09 (file enumeration)"
      to: "9 SUMMARY files"
      via: "find .planning/phases/0[1-4]-*/*-SUMMARY.md + audit cross-reference"
      pattern: "SUMMARY.md"
---

<objective>
Add `requirements-completed:` frontmatter arrays to the 9 SUMMARY files in phases 01-04 that are currently missing them. REQ-IDs are sourced by cross-checking 3 references and declared only when ≥2 sources agree (D-07/D-08). Single-source-only candidates surface in a discrepancy file before any writes.

Purpose: Bring all phase 01-04 SUMMARY files into traceability compliance. The milestone audit flagged these 9 files as missing declarations; the underlying requirements ARE satisfied, but the SUMMARY frontmatter doesn't declare them.

**Count reconciliation (17 vs 9):** ROADMAP § Phase 10 success criterion #3 and 10-CONTEXT.md reference "17 SUMMARY files" — that figure counts 17 *REQ-ID gap citations* across the milestone audit's `tech_debt § phase_01..04` rows, not 17 distinct files. Cross-referencing the audit rows with `find .planning/phases/0[1-4]-*/*-SUMMARY.md` resolves to **9 distinct SUMMARY files**, several of which are missing multiple REQ-IDs (e.g., a single SUMMARY may need 3 REQ-IDs declared, counting as 3 audit citations against 1 file). The 9-file work-list below is the canonical scope for this plan. DOCS-03 is closed when all 9 files have `requirements-completed:` declared per the D-08 ≥2-of-3 cross-check.

Output: 9 updated SUMMARY files + a discrepancy record (10-DOCS-03-DISCREPANCIES.md), closing DOCS-03.

Wave 2 dependency: Depends on Plan 02 (10-02) because 03-VERIFICATION.md produced there is one of the three cross-check sources for Phase 03 SUMMARY files. Without it, the VERIFICATION source is "silent" for Phase 03 — which forces single-source-only candidates for 03-02-SUMMARY REQ-IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-planning-hygiene-backfill/10-CONTEXT.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-check REQ-IDs and produce discrepancy record</name>
  <files>
    .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md
  </files>

  <read_first>
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md § requirements_integration_map (primary work-list for which REQ-IDs belong to which SUMMARY; the audit table has per-file rows with "OK with summary debt" verdicts)
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md § tech_debt (phase_01..04 rows enumerate the files and the REQ-ID groups per file)
    - .planning/REQUIREMENTS.md (source 2: inline evidence cites per REQ-ID — look for phase 01-04 inline evidence)
    - .planning/phases/01-foundation-authentication/01-VERIFICATION.md § Requirements Coverage (source 1: traceability table for phase 01 REQ-IDs)
    - .planning/phases/02-browsing-responding/02-VERIFICATION.md § Requirements Coverage (source 1 for phase 02 REQ-IDs)
    - .planning/phases/03-response-integrity/03-VERIFICATION.md (source 1 for phase 03 REQ-IDs — created by Plan 02)
    - .planning/phases/04-admin-panel-suggestion-management/04-VERIFICATION.md § Requirements Coverage (source 1 for phase 04 REQ-IDs)
    - .planning/phases/01-foundation-authentication/01-04-SUMMARY.md (current frontmatter — what's already there)
    - .planning/phases/02-browsing-responding/02-01-SUMMARY.md through 02-04-SUMMARY.md (what each plan provides/requires)
    - .planning/phases/03-response-integrity/03-02-SUMMARY.md
    - .planning/phases/04-admin-panel-suggestion-management/04-02-SUMMARY.md
    - .planning/phases/04-admin-panel-suggestion-management/04-04-SUMMARY.md
    - .planning/phases/04-admin-panel-suggestion-management/04-05-SUMMARY.md
  </read_first>

  <action>
Perform the D-07/D-08 cross-check for each of the 9 SUMMARY files and produce `10-DOCS-03-DISCREPANCIES.md`.

**The 9 SUMMARY files in scope** (from `find .planning/phases/0[1-4]-*/*-SUMMARY.md` cross-referenced with audit):

1. `01-04-SUMMARY.md` — audit: "AUTH-04, AUTH-05, TEST-02 covered there per VERIFICATION but not declared"
2. `02-01-SUMMARY.md` — audit: "VOTE-*, RSLT-*, CATG-* covered per VERIFICATION but not declared"
3. `02-02-SUMMARY.md` — same group
4. `02-03-SUMMARY.md` — same group
5. `02-04-SUMMARY.md` — same group
6. `03-02-SUMMARY.md` — audit: "VOTE-04 covered per REQUIREMENTS.md inline evidence but not declared"
7. `04-02-SUMMARY.md` — audit: "POLL-02/POLL-03/POLL-04/VOTE-*/TEST-05 covered per VERIFICATION"
8. `04-04-SUMMARY.md` — same group
9. `04-05-SUMMARY.md` — the gap-closure plan (admin nav link); check what REQs it satisfies

**Cross-check methodology per D-07 (3 sources):**
- Source 1: `<phase>-VERIFICATION.md` Requirements Coverage traceability table (shows which plan each REQ was satisfied in)
- Source 2: `REQUIREMENTS.md` inline evidence citations (text after `[x] **REQ-ID**:`)
- Source 3: `v1.0-MILESTONE-AUDIT.md § requirements_integration_map` rows (the "SUMMARY frontmatter" column notes which SUMMARYs have declarations)

**Per D-08: a REQ-ID is declared when ≥2 of 3 sources agree it belongs to that plan's SUMMARY.**

**Pre-computed cross-check results** (use these to drive the discrepancy analysis):

For `01-04-SUMMARY.md`:
- AUTH-04: Source 1 (01-VERIFICATION.md row "01-03, 01-04"), Source 2 (REQUIREMENTS.md mentions Phase 1), Source 3 (audit row "01-03: declared" — but 01-04 also covers auth tests) → 2+ agree → DECLARE
- AUTH-05: Source 1 (01-VERIFICATION.md row "01-03, 01-04"), Source 2, Source 3 → 2+ agree → DECLARE
- TEST-02: Source 1 (01-VERIFICATION.md "TEST-02: SATISFIED | 01-04 SUMMARY lacks line"), Source 3 ("01-04 SUMMARY lacks line" explicitly named) → 2+ agree → DECLARE

For `02-01-SUMMARY.md` through `02-04-SUMMARY.md`:
- The audit says "02-* SUMMARYs lack line" for VOTE-01..03, RSLT-01..05, CATG-02..04, INFR-04.
- Read each plan's `provides:` section to determine which REQs map to which plan. The cross-check assigns REQ-IDs to the plan whose `provides:` includes the feature, corroborated by VERIFICATION.md traceability.
- Read 02-01 through 02-04 SUMMARY frontmatter `provides:` blocks to determine the assignment. Then cross-check against 02-VERIFICATION.md traceability.
- Expected assignments (verify against actual SUMMARY `provides:` fields when reading):
  - 02-01: VOTE-01, VOTE-02 (browse + submit-vote EF)
  - 02-02: INFR-04 (EF deployment scaffold), RSLT-01 (RLS on vote_counts)
  - 02-03: TEST-03 (Phase 2 tests)
  - 02-04: VOTE-03, RSLT-02, RSLT-03, RSLT-04, RSLT-05, CATG-02, CATG-03, CATG-04 (results/categories UI)
  
  However: Read each file's actual `provides:` to determine the correct assignment. Do NOT guess — read the files.

For `03-02-SUMMARY.md`:
- VOTE-04: Source 1 (03-VERIFICATION.md row "VOTE-04 | 03-02"), Source 2 (REQUIREMENTS.md VOTE-04 inline evidence cites to 03-02-SUMMARY), Source 3 (audit "03-02 SUMMARY lacks line") → 2+ agree → DECLARE

For `04-02-SUMMARY.md`:
- POLL-02: Source 1 (04-VERIFICATION.md "POLL-02: 04-02/04-04 missing"), Source 3 (audit "POLL-02 | OK with summary debt" and provides list in 04-02-SUMMARY) → check `provides:` in the file to confirm
- TEST-05: Source 1 (04-VERIFICATION.md "TEST-05: 04-* SUMMARYs lack line"), Source 3 → assign to first plan that ships admin tests (04-02 ships source-analysis tests) → check `provides:`

For `04-04-SUMMARY.md`:
- POLL-03, POLL-04: Source 1 (04-VERIFICATION.md), Source 3 (audit) → verify against `provides:`

For `04-05-SUMMARY.md`:
- This is the gap-closure plan for admin nav link. No v1.0 REQ-ID explicitly assigned to it in the audit (the audit predates 04-05). Read its `provides:` to determine if any REQ-ID covers it. If no REQ-ID clearly maps to this plan from ≥2 sources, it is a discrepancy candidate — surface it.

**Output file**: Create `10-DOCS-03-DISCREPANCIES.md` with:
1. A table of all 9 files with their planned `requirements-completed` declarations
2. Any single-source-only candidates flagged as discrepancies
3. Recommendation for each discrepancy (include with note / exclude with note)

Format:
```markdown
# DOCS-03 Cross-Check Results

Generated: 2026-05-07
Rule: REQ-ID declared only when ≥2 of 3 sources agree (D-08)
Sources: (1) phase VERIFICATION.md | (2) REQUIREMENTS.md inline | (3) v1.0 milestone audit

## Planned Declarations

| File | planned requirements-completed | Source 1 (VERIFICATION) | Source 2 (REQUIREMENTS.md) | Source 3 (Audit) | Agree count | Decision |
|------|-------------------------------|------------------------|---------------------------|-----------------|-------------|----------|
| 01-04-SUMMARY.md | [AUTH-04, AUTH-05, TEST-02] | AUTH-04 ✓, AUTH-05 ✓, TEST-02 ✓ | ✓ | ✓ | 3 | DECLARE |
| 02-01-SUMMARY.md | [...] | [...] | [...] | [...] | ... | DECLARE/FLAG |
...

## Discrepancies (Single-Source Candidates)

| File | REQ-ID | Only Source | Recommendation |
|------|--------|-------------|----------------|
| ...  | ...    | ...         | ...            |

## Resolution

[State how each discrepancy was resolved — INCLUDE or EXCLUDE — with rationale]
```

If no discrepancies are found (all candidates have ≥2 sources), the file MUST contain the canonical marker line `Discrepancies: none` (its own line, no surrounding noise) AT THE TOP of the `## Resolution` section. The file must still exist. (For backward compat with prior wording, "No discrepancies found" / "No single-source candidates" also pass — but `Discrepancies: none` is the canonical form going forward.)

If discrepancies ARE found, the file MUST present each one with these columns:
`File | REQ-ID | Only Source | Recommendation | User decision`. The "User decision"
column is left BLANK by Task 1 — the user fills it in (INCLUDE / EXCLUDE / DEFER) at
review time. Task 1 then appends a templated sign-off line at the bottom of the file:
`Resolved-by: <fill-in-handle> on <YYYY-MM-DD>` left blank for the user to populate.

**Important (review HIGH #2/#4 cycle-2 residual):** Task 2's precondition gate uses
the STRICT regex `^Resolved-by: [^<[:space:]][^<]+ on 20[0-9]{2}-[0-9]{2}-[0-9]{2}$`,
which rejects the placeholder template Task 1 writes. The user MUST replace the
`<fill-in-handle>` with their real Discord handle (or similar) AND replace
`<YYYY-MM-DD>` with a real ISO date in 20xx-mm-dd form before Task 2 will run.
This prevents the template line itself from satisfying the gate.

In addition, every `User decision` cell in the discrepancy table MUST be non-empty
when discrepancies exist — Task 2 verifies this independently of the `Resolved-by:`
line. The ONLY path that bypasses both checks is the `Discrepancies: none` marker
(used when Task 1's cross-check finds no discrepancies at all).

Task 2 will not run until the user has either signed off with the strict regex pattern
above AND filled in every `User decision` cell, OR the canonical `Discrepancies: none`
marker is present. This is the human-resolution gate per CONTEXT.md D-08 and review
HIGH #2.
  </action>

  <verify>
    <automated>test -f .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md && echo "DISCREPANCY FILE EXISTS" || echo "DISCREPANCY FILE MISSING"</automated>
    <automated>grep -q "discrepanc\|Discrepanc\|DECLARE\|No discrepancies" .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md && echo "CONTENT OK" || echo "CONTENT MISSING"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md` exits 0
    - File contains either a discrepancy table OR the canonical empty-discrepancy marker — verify with: `grep -qE "^Discrepancies: none$|No discrepancies found|No single-source candidates" 10-DOCS-03-DISCREPANCIES.md` exits 0 (canonical: `Discrepancies: none`; legacy phrasings accepted for backward compat)
    - If discrepancies exist, the table contains a "User decision" column with empty cells (user fills these in) AND a `Resolved-by: <fill-in-handle> on <YYYY-MM-DD>` template line for user sign-off (review HIGH #2)
    - The empty-discrepancy marker (`Discrepancies: none`) is the ONLY path that bypasses the Task 2 sign-off gate; if any discrepancy row is present, the user MUST fill in every `User decision` cell AND replace the `Resolved-by:` template placeholders before Task 2 will run
    - File contains a "Planned Declarations" section enumerating all 9 SUMMARY files
    - File is readable by the executor of Task 2 (drives the write decisions)
    - No SUMMARY file has been modified yet — this task is analysis-only, producing the discrepancy record
  </acceptance_criteria>

  <done>10-DOCS-03-DISCREPANCIES.md produced with cross-check results for all 9 SUMMARY files. Discrepancy table complete. Ready for Task 2 to execute writes.</done>
</task>

<task type="auto">
  <name>Task 2: Write requirements-completed frontmatter to all 9 SUMMARY files (user-gated)</name>

  <preconditions>
    Per CONTEXT.md D-08 and review HIGH #2: Task 2 may NOT modify any SUMMARY file
    until the user has reviewed `10-DOCS-03-DISCREPANCIES.md` and recorded an explicit
    resolution for every single-source candidate.

    The discrepancy file MUST contain ONE of the following user-resolution markers
    before Task 2 may proceed:

    1. **No discrepancies marker** — the line `## Resolution\n\nNo discrepancies found.`
       (or wording equivalent: "No single-source candidates"). This is acceptable when
       Task 1's cross-check finds every REQ-ID in ≥2 sources. Task 2 may proceed.

    2. **User sign-off marker** — the discrepancy table contains a "User decision"
       column populated for every flagged row (no empty cells, no "<...>" placeholder
       text), AND the file contains a non-placeholder sign-off line matching the
       strict regex `^Resolved-by: [^<[:space:]][^<]+ on 20[0-9]{2}-[0-9]{2}-[0-9]{2}$`
       — i.e. a real handle (not starting with `<` or whitespace, no `<...>` placeholder
       characters anywhere) and a real ISO date in 20xx-mm-dd form. Task 2 may proceed.

       **Why the strict regex (review HIGH #2/#4 cycle-2 residual):** Task 1 prescribes
       writing a TEMPLATE line `Resolved-by: <fill-in-handle> on <YYYY-MM-DD>` for the
       user to populate. A loose `^Resolved-by:` grep would match that placeholder
       verbatim and let Task 2 run before the user has actually signed off. The
       strict regex rejects any `<...>` placeholder syntax and requires a real ISO
       date, so the gate cannot be bypassed by the template line itself.

    If neither marker is present, Task 2 MUST stop and surface the discrepancy file
    to the user — do NOT modify any SUMMARY file. The plan is `autonomous: false`
    specifically so execute-phase pauses here for the user to resolve discrepancies.
  </preconditions>
  <files>
    .planning/phases/01-foundation-authentication/01-04-SUMMARY.md
    .planning/phases/02-browsing-responding/02-01-SUMMARY.md
    .planning/phases/02-browsing-responding/02-02-SUMMARY.md
    .planning/phases/02-browsing-responding/02-03-SUMMARY.md
    .planning/phases/02-browsing-responding/02-04-SUMMARY.md
    .planning/phases/03-response-integrity/03-02-SUMMARY.md
    .planning/phases/04-admin-panel-suggestion-management/04-02-SUMMARY.md
    .planning/phases/04-admin-panel-suggestion-management/04-04-SUMMARY.md
    .planning/phases/04-admin-panel-suggestion-management/04-05-SUMMARY.md
  </files>

  <read_first>
    - .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md (MANDATORY — Task 1 output drives all write decisions; use the "Decision: DECLARE" rows only)
    - Each of the 9 SUMMARY files being modified (read current frontmatter before editing to ensure correct insertion point)
  </read_first>

  <action>
Using the declarations table from `10-DOCS-03-DISCREPANCIES.md` (Task 1 output), add `requirements-completed:` to the frontmatter of each SUMMARY file where the decision is DECLARE.

**Rules:**
1. Add `requirements-completed: [ID1, ID2, ...]` as a new frontmatter key, immediately after the last existing frontmatter key (before the closing `---`)
2. Preserve ALL existing frontmatter keys verbatim — only ADD, never remove or modify
3. Preserve ALL body content verbatim — only the frontmatter block changes
4. For discrepancy-flagged REQ-IDs with recommendation EXCLUDE, do NOT add them — the discrepancy file explains why
5. For 04-05-SUMMARY.md: if cross-check finds no REQ-ID with ≥2 sources, add `requirements-completed: []` (empty array) with a comment line `# No v1.0 REQ-ID maps to this gap-closure plan (admin nav link) per D-08 cross-check; see 10-DOCS-03-DISCREPANCIES.md`

**Concrete write for `01-04-SUMMARY.md`** (from pre-computed cross-check):
Add to frontmatter:
```yaml
requirements-completed: [AUTH-04, AUTH-05, TEST-02]
```

**Concrete write for `03-02-SUMMARY.md`** (from pre-computed cross-check):
Add to frontmatter:
```yaml
requirements-completed: [VOTE-04]
```

**For all 02-* and 04-02/04-04/04-05 files**: use the declarations from `10-DOCS-03-DISCREPANCIES.md`. The plan body (`provides:` fields) and VERIFICATION.md traceability determine the exact REQ-ID list per file.

After all 9 files are edited, run the verification grep gate below.
  </action>

  <verify>
    <automated>for f in \
  .planning/phases/01-foundation-authentication/01-04-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-01-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-02-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-03-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-04-SUMMARY.md \
  .planning/phases/03-response-integrity/03-02-SUMMARY.md \
  .planning/phases/04-admin-panel-suggestion-management/04-02-SUMMARY.md \
  .planning/phases/04-admin-panel-suggestion-management/04-04-SUMMARY.md \
  .planning/phases/04-admin-panel-suggestion-management/04-05-SUMMARY.md; do \
  grep -q "requirements-completed:" "$f" && echo "OK: $f" || echo "MISSING: $f"; \
done</automated>
    <automated>grep -q "requirements-completed:.*AUTH-04.*AUTH-05.*TEST-02\|requirements-completed:.*AUTH-05.*AUTH-04.*TEST-02" .planning/phases/01-foundation-authentication/01-04-SUMMARY.md && echo "01-04 IDS OK" || echo "01-04 IDS FAIL"</automated>
    <automated>grep -q "requirements-completed:.*VOTE-04" .planning/phases/03-response-integrity/03-02-SUMMARY.md && echo "03-02 VOTE-04 OK" || echo "03-02 VOTE-04 FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - **PRECONDITION (review HIGH #2 / D-08, cycle-2 residual fix):** Either `grep -q "^Discrepancies: none$\|No discrepancies found\|No single-source candidates" .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md` exits 0 (empty-discrepancy bypass — `Discrepancies: none` is the canonical marker; the legacy phrasings remain accepted for backward compat with the Task 1 template), OR `grep -qE '^Resolved-by: [^<[:space:]][^<]+ on 20[0-9]{2}-[0-9]{2}-[0-9]{2}$' .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md` exits 0 (strict non-placeholder regex — rejects the `Resolved-by: <fill-in-handle> on <YYYY-MM-DD>` template Task 1 writes; requires a real handle and ISO date). If neither exits 0, Task 2 MUST stop without modifying any SUMMARY file.
    - **PRECONDITION 2 (review HIGH #2 cycle-2 residual fix — non-empty User decision cells):** When the discrepancy table contains rows (i.e. at least one row beyond the header/separator), every `User decision` cell MUST be non-empty AND must NOT match the placeholder pattern. Verify with: `awk -F'|' '/^\| /{n++} END{exit (n>2)?0:1}' 10-DOCS-03-DISCREPANCIES.md` (rows present) AND `! grep -E '^\|.*\|[[:space:]]*\|[[:space:]]*$' .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md` (no row ends with an empty trailing cell). The empty-discrepancy bypass (`Discrepancies: none`) is the ONLY path that skips this check.
    - All 9 SUMMARY files have `requirements-completed:` in frontmatter — the grep gate above prints "OK" for every file
    - `grep -q "requirements-completed:.*AUTH-04" .planning/phases/01-foundation-authentication/01-04-SUMMARY.md` exits 0
    - `grep -q "requirements-completed:.*AUTH-05" .planning/phases/01-foundation-authentication/01-04-SUMMARY.md` exits 0
    - `grep -q "requirements-completed:.*TEST-02" .planning/phases/01-foundation-authentication/01-04-SUMMARY.md` exits 0
    - `grep -q "requirements-completed:.*VOTE-04" .planning/phases/03-response-integrity/03-02-SUMMARY.md` exits 0
    - Per-ID separate checks (review LOW #2 — replace alternation `(A|B)` with explicit per-ID grep): each declared ID is checked individually in the file's frontmatter; alternation patterns that pass with only one ID present are NOT used
    - Body content (everything after the closing `---`) of all 9 files is unchanged
    - No REQ-ID that was in a single-source-only discrepancy (recommendation EXCLUDE) appears in any `requirements-completed:` array
  </acceptance_criteria>

  <done>All 9 SUMMARY files have requirements-completed frontmatter declarations grounded in ≥2-of-3 source agreement. Discrepancy record exists. DOCS-03 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ edits | Documentation-only; not deployed, not user-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | requirements-completed REQ-ID accuracy | mitigate | D-08 cross-check rule requires ≥2 of 3 sources to agree before any declaration. Single-source candidates are flagged in 10-DOCS-03-DISCREPANCIES.md and not written until reviewed. Prevents fabricated REQ-ID claims. |
| T-10-04 | Information Disclosure | SUMMARY frontmatter body corruption | accept | Executor reads the full file before editing; only the frontmatter insertion point changes; body preserved verbatim. Git diff provides post-write audit trail. |

attack_surface: "none — documentation-only edits to .planning/ artifacts; not deployed, not user-facing"
</threat_model>

<verification>
```bash
# All 9 files have requirements-completed:
for f in \
  .planning/phases/01-foundation-authentication/01-04-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-01-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-02-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-03-SUMMARY.md \
  .planning/phases/02-browsing-responding/02-04-SUMMARY.md \
  .planning/phases/03-response-integrity/03-02-SUMMARY.md \
  .planning/phases/04-admin-panel-suggestion-management/04-02-SUMMARY.md \
  .planning/phases/04-admin-panel-suggestion-management/04-04-SUMMARY.md \
  .planning/phases/04-admin-panel-suggestion-management/04-05-SUMMARY.md; do
  grep -q "requirements-completed:" "$f" && echo "OK: $f" || echo "MISSING: $f"
done

# Spot-check key declarations
grep -q "AUTH-04" .planning/phases/01-foundation-authentication/01-04-SUMMARY.md && echo "01-04 AUTH-04 OK"
grep -q "VOTE-04" .planning/phases/03-response-integrity/03-02-SUMMARY.md && echo "03-02 VOTE-04 OK"

# Discrepancy record exists
test -f .planning/phases/10-planning-hygiene-backfill/10-DOCS-03-DISCREPANCIES.md && echo "DISCREPANCY FILE OK"
```
</verification>

<success_criteria>
- All 9 SUMMARY files in phases 01-04 that were missing `requirements-completed:` now have it
- REQ-ID declarations are grounded in ≥2 of 3 cross-check sources per D-07/D-08
- `10-DOCS-03-DISCREPANCIES.md` documents the cross-check results and any discrepancy resolutions
- Body content of all 9 files unchanged
- REQUIREMENTS.md DOCS-03 checkbox remains [ ] — flipped to [x] by Plan 05 (closure)
</success_criteria>

<output>
After completion, create `.planning/phases/10-planning-hygiene-backfill/10-03-SUMMARY.md`

Include in frontmatter:
```yaml
requirements-completed: [DOCS-03]
```
</output>
