---
phase: 06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/favicon.svg
  - public/favicon.ico
  - public/apple-touch-icon.png
  - public/favicon-32.png
  - index.html
autonomous: false
requirements: []
tags: [D-07, D-10]

must_haves:
  truths:
    - "Default Vite scaffold favicon (the React-leaf SVG currently at public/favicon.svg) is replaced with a WTCS-branded favicon set generated from src/assets/wtcs-logo.png (D-07)"
    - "Favicon set includes: favicon.svg (scalable), favicon.ico (legacy multi-res), apple-touch-icon.png (180x180), and a 32x32 PNG (D-07)"
    - "Browser tab title reads 'WTCS Community Suggestions' on every route (NOT the lowercase slug 'wtcs-community-polls') (D-10)"
    - "<meta name='description'> is present in index.html with the locked 153-char copy from UI-SPEC Surface 6 (D-10)"
    - "Favicon renders cleanly on light AND dark Chrome/Safari/Firefox tab bar at 16x16 (visual verification, UI-SPEC Surface 5)"
    - "Terminology compliance: title and description use 'suggestions/opinions/responses' user-facing vocabulary, NOT 'polls/votes' (PROJECT.md)"
  artifacts:
    - path: "public/favicon.svg"
      provides: "Replaces Vite scaffold SVG with WTCS-branded scalable mark (D-07)"
    - path: "public/favicon.ico"
      provides: "Legacy multi-res ICO fallback for browser address bar (D-07)"
    - path: "public/apple-touch-icon.png"
      provides: "180x180 PNG for iOS home-screen / Safari pinned tab (D-07)"
    - path: "public/favicon-32.png"
      provides: "32x32 PNG for browsers that prefer raster favicons (D-07)"
    - path: "index.html"
      provides: "Updated <title>, <meta description>, expanded <link rel='icon'> block (D-07, D-10)"
      contains: "WTCS Community Suggestions"
  key_links:
    - from: "index.html"
      to: "public/favicon.svg"
      via: "<link rel='icon' type='image/svg+xml' href='/favicon.svg'>"
      pattern: "rel=\"icon\".*favicon\\.svg"
    - from: "index.html"
      to: "public/apple-touch-icon.png"
      via: "<link rel='apple-touch-icon' sizes='180x180' href='/apple-touch-icon.png'>"
      pattern: "apple-touch-icon"
---

<objective>
Replace the Vite scaffold favicon (currently a generic React-leaf SVG at `public/favicon.svg`) with a WTCS-branded favicon set generated from `src/assets/wtcs-logo.png`. Update `index.html` to lock the new title `WTCS Community Suggestions` and a 153-char meta description.

Purpose: Phase 6 launch polish (D-07, D-10). The current title is the lowercase scaffold slug `wtcs-community-polls`; the favicon is generic React. Both signal "unfinished" to first-time visitors.

Output: WTCS-branded multi-format favicon set in `public/`, a polished `index.html` head block, zero new build dependencies, zero `netlify.toml` changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-CONTEXT.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-RESEARCH.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-PATTERNS.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-UI-SPEC.md
@index.html

<interfaces>
<!-- Existing index.html head (line-by-line) — executor must preserve everything outside the surfaces below. -->

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>wtcs-community-polls</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Source asset: `src/assets/wtcs-logo.png` (226x200 RGBA PNG, already in repo, used by Navbar component).

Locked output paths (UI-SPEC Surface 5):
- public/favicon.svg (replaces existing 9.5 KB Vite scaffold)
- public/favicon.ico (NEW; multi-res 16/32/48)
- public/apple-touch-icon.png (NEW; 180x180)
- public/favicon-32.png (NEW; matches `<link sizes="32x32">` reference in UI-SPEC)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Generate WTCS-branded favicon set via realfavicongenerator.net (D-07)</name>
  <read_first>
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-UI-SPEC.md (Surface 5 — locked output set + tooling decision)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-RESEARCH.md (Pattern 6 — embedded-CSS dark-mode SVG; Pitfall 8 — overwriting existing favicon.svg is intentional)
    - src/assets/wtcs-logo.png (source asset, 226x200)
  </read_first>
  <files>(see <how-to-verify> below — this is a checkpoint task; no source files modified at this step)</files>
  <action>(see <how-to-verify> below — this is a checkpoint task; the action is human-driven verification, not autonomous code change)</action>
  <verify>(see <acceptance_criteria> below)</verify>
  <done>(see <acceptance_criteria> below)</done>
  <what-built>
    Nothing yet — favicon generation requires uploading the source PNG to realfavicongenerator.net and downloading the output set. This is the only task in Phase 6 that legitimately requires a human (file upload to a third-party web service).
  </what-built>
  <how-to-verify>
    1. Open https://realfavicongenerator.net in a browser.
    2. Upload `src/assets/wtcs-logo.png` (226x200 RGBA).
    3. Configure (within the generator UI):
       - iOS: use the source image, no padding override, transparent background.
       - Android Chrome: theme color `#0a0a0a` (matches shadcn Neutral preset; UI-SPEC Color section).
       - Windows Metro: skip (out of scope for v1.0 — UI-SPEC explicitly does NOT include `mstile`).
       - macOS Safari: skip pinned-tab SVG (we'll use the multi-format SVG for the modern path).
       - Path: `/` (root, served by Netlify from `public/`).
       - Compression: default (lossless).
    4. Click "Generate your Favicons and HTML code". Download the ZIP.
    5. From the ZIP, extract these EXACT files into `public/`:
       - `favicon.svg` (overwrites existing scaffold; Pitfall 8 is INTENTIONAL)
       - `favicon.ico`
       - `apple-touch-icon.png` (rename from `apple-touch-icon-180x180.png` if needed)
       - `favicon-32x32.png` → rename to `favicon-32.png` (matches UI-SPEC `<link sizes="32x32" href="/favicon-32.png">`)
    6. DELETE all other files from the ZIP (mstile-*, browserconfig.xml, site.webmanifest, android-chrome-*, etc.) — they are out of scope and would clutter `public/`.
    7. **Visual check at 16x16:** Open `public/favicon.svg` in a browser tab; zoom out to 100%. The WTCS mark must be recognizable. If the 226x200 source loses recognizability at 16x16:
       - Open the downloaded `favicon.svg` in a text editor.
       - Either (a) accept the auto-generated SVG if visually OK, OR (b) hand-simplify by removing micro-detail paths and re-saving. Do NOT introduce new colors — keep within the source palette.
    8. **Dark-mode handling:** Per UI-SPEC + RESEARCH.md Pattern 6, the preferred path is a single SVG with embedded `<style>` block containing `@media (prefers-color-scheme: dark)`. realfavicongenerator does NOT emit this by default. After step 5, IF the favicon contrast on dark browser chrome is insufficient (visually verify in Chrome with the OS in dark mode):
       - Open `public/favicon.svg` in a text editor.
       - Add a `<style>` block immediately after `<svg ...>` with these exact rules (adjust path classes to match the generator's output):
         ```xml
         <style>
           .icon-fg { fill: #0a0a0a; }
           @media (prefers-color-scheme: dark) {
             .icon-fg { fill: #fafafa; }
           }
         </style>
         ```
       - Add `class="icon-fg"` to the foreground path(s) that need to flip color.
       - Save. This avoids needing a second `favicon-dark.svg` file (UI-SPEC marks dark variant as "ONLY if needed").
    9. Stage the four files: `git add public/favicon.svg public/favicon.ico public/apple-touch-icon.png public/favicon-32.png`. Do NOT commit yet — Task 2 commits the index.html change atomically with the asset replacement.
  </how-to-verify>
  <acceptance_criteria>
    - File `public/favicon.svg` exists and `wc -c < public/favicon.svg` returns a value > 0 AND the byte content differs from the prior 9.5 KB Vite scaffold (verifiable by `git diff --stat public/favicon.svg` showing change).
    - File `public/favicon.ico` exists; `file public/favicon.ico` reports `MS Windows icon resource` (or similar legacy ICO signature).
    - File `public/apple-touch-icon.png` exists; `file public/apple-touch-icon.png` reports `PNG image data, 180 x 180` (or specifies 180x180 dimensions).
    - File `public/favicon-32.png` exists; `file public/favicon-32.png` reports `PNG image data, 32 x 32`.
    - No additional ZIP-derived files committed (no `mstile*`, no `browserconfig.xml`, no `site.webmanifest`, no `android-chrome-*`, no `apple-touch-icon-precomposed.png` etc.):
      `ls public/ | grep -E "mstile|browserconfig|webmanifest|android-chrome|precomposed"` returns NOTHING.
    - All four files staged in git index (`git diff --cached --name-only` includes them).
  </acceptance_criteria>
  <resume-signal>Type "favicons generated" or paste `ls -la public/*.svg public/*.ico public/*.png` output to close this task.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update index.html — title, meta description, expanded link icon block (D-07, D-10)</name>
  <files>index.html</files>
  <read_first>
    - index.html (target file — full read; only the `<head>` block changes)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-UI-SPEC.md (Surface 5 + Surface 6 — locked output)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-PATTERNS.md (Bucket C — index.html divergence specifics)
  </read_first>
  <action>
    Update `index.html`. The `<head>` block becomes EXACTLY this (locked, copy verbatim from UI-SPEC Surface 5 and Surface 6):

    ```html
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="UTF-8" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
        <link rel="icon" type="image/x-icon" href="/favicon.ico" />
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="description" content="Share opinions on War Thunder Competitive Scene proposals. Verified Discord identity, one response per account, transparent results." />
        <title>WTCS Community Suggestions</title>
      </head>
    ```

    Body block stays untouched:

    ```html
      <body>
        <div id="root"></div>
        <script type="module" src="/src/main.tsx"></script>
      </body>
    </html>
    ```

    Notes for the executor:
    - The title is EXACTLY `WTCS Community Suggestions` (26 chars; UI-SPEC Surface 6 lock). Replaces the prior `wtcs-community-polls` lowercase slug.
    - The meta description is EXACTLY `Share opinions on War Thunder Competitive Scene proposals. Verified Discord identity, one response per account, transparent results.` (153 chars; UI-SPEC Surface 6 lock). Uses user-facing terminology (`opinions`, `response`) per PROJECT.md.
    - Four `<link rel="icon">` tags in this exact order: SVG, 32x32 PNG, ICO, apple-touch-icon. UI-SPEC Surface 5 locks the order.
    - DO NOT add Open Graph or Twitter card meta tags — UI-SPEC Surface 6 explicitly puts them out of scope.
    - DO NOT add a `<link rel="icon" media="(prefers-color-scheme: dark)" ...>` line; the dark-mode handling lives inside the SVG via embedded CSS (RESEARCH.md Pattern 6 + Task 1 step 8). If the favicon-dark.svg fallback was created in Task 1 step 8, also add this line BEFORE the `<meta name="viewport">`:
      ```html
      <link rel="icon" type="image/svg+xml" href="/favicon-dark.svg" media="(prefers-color-scheme: dark)" />
      ```
      But only if Task 1 produced that file.
    - DO NOT modify `<html lang="en">`, `<meta charset>`, `<meta viewport>`, or anything in `<body>`.

    After saving:
    - Run `npm run build`. Vite emits `dist/index.html` with the new head block AND copies the four favicon files to `dist/`.
    - Verify the dist output: `grep -c "WTCS Community Suggestions" dist/index.html` returns 1 AND `ls dist/favicon.svg dist/favicon.ico dist/apple-touch-icon.png dist/favicon-32.png` lists all four.
    - Run `npm run lint`. (Lint may not parse index.html but the command must still exit 0 over the JS/TS surface.)
    - Commit all five changed files in ONE atomic commit with message: `docs(06-03): WTCS-branded favicon set + polished title/meta`. The commit body should reference D-07 and D-10.
  </action>
  <verify>
    <automated>npm run lint && npm run build && grep -c "WTCS Community Suggestions" index.html && grep -c "WTCS Community Suggestions" dist/index.html && grep -c "Share opinions on War Thunder Competitive Scene" index.html && grep -c "rel=\"apple-touch-icon\"" index.html && test -f dist/favicon.svg && test -f dist/favicon.ico && test -f dist/apple-touch-icon.png && test -f dist/favicon-32.png</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WTCS Community Suggestions" index.html` returns 1.
    - `grep -c "wtcs-community-polls" index.html` returns 0 (lowercase slug fully removed from title).
    - `grep -c "Share opinions on War Thunder Competitive Scene proposals\\. Verified Discord identity, one response per account, transparent results\\." index.html` returns 1 (exact 153-char meta).
    - `grep -E "name=\"description\"" index.html | wc -l` returns 1.
    - `grep -c "rel=\"icon\".*favicon\\.svg" index.html` returns 1.
    - `grep -c "rel=\"icon\".*favicon-32\\.png" index.html` returns 1.
    - `grep -c "rel=\"icon\".*favicon\\.ico" index.html` returns 1.
    - `grep -c "rel=\"apple-touch-icon\".*sizes=\"180x180\"" index.html` returns 1.
    - `grep -E "(og:|twitter:)" index.html` returns NOTHING (Open Graph / Twitter cards explicitly out of scope).
    - `grep -E "vote|poll" index.html` returns NOTHING (user-facing terminology compliance — PROJECT.md lock).
    - `npm run build` exits 0 and produces `dist/index.html` containing the new title.
    - `dist/favicon.svg`, `dist/favicon.ico`, `dist/apple-touch-icon.png`, `dist/favicon-32.png` all exist.
  </acceptance_criteria>
  <done>index.html locked to verbatim UI-SPEC Surface 5 + Surface 6 content; four favicon files ship in dist/; build green; commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — favicon renders cleanly on light + dark browser chrome (UI-SPEC Surface 5)</name>
  <read_first>
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-UI-SPEC.md (Surface 5 visual constraint: must be readable at 16x16; dark-mode contrast acceptable on `prefers-color-scheme: dark`)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-VALIDATION.md ("Manual-Only Verifications" row for D-07)
  </read_first>
  <files>(see <how-to-verify> below — this is a checkpoint task; no source files modified at this step)</files>
  <action>(see <how-to-verify> below — this is a checkpoint task; the action is human-driven verification, not autonomous code change)</action>
  <verify>(see <acceptance_criteria> below)</verify>
  <done>(see <acceptance_criteria> below)</done>
  <what-built>
    Tasks 1-2 generated and wired the favicon set. This task verifies it renders cleanly across browsers (out-of-repo visual test).
  </what-built>
  <how-to-verify>
    1. Wait for the merge to main and the Netlify production deploy to land (typically <2 min). Confirm via `gh run watch` or by checking https://polls.wtcsmapban.com directly.
    2. Force a hard refresh on the production URL (Cmd+Shift+R / Ctrl+Shift+R) to bypass cached assets.
    3. Open the production URL in:
       - Chrome (light OS theme) — inspect the tab favicon at native size; zoom out to 100% so the favicon shows at ~16x16.
       - Chrome (dark OS theme) — same inspection.
       - Safari (light + dark) — same.
       - Firefox (light + dark) — same.
    4. For each browser/theme combo, the WTCS mark must be:
       - Recognizable as a WTCS logo (NOT a blob or pixel mush).
       - Readable contrast against the browser's tab-bar background.
    5. Open https://polls.wtcsmapban.com on iOS Safari (or simulator). "Add to Home Screen". The home-screen icon should use `apple-touch-icon.png` (180x180) and render with no Apple-imposed white border.
    6. View source on https://polls.wtcsmapban.com. Confirm:
       - `<title>WTCS Community Suggestions</title>` is present.
       - `<meta name="description" content="Share opinions...">` is present with the 153-char copy.
    7. (Optional but recommended) Paste the URL into Google's Rich Results Test or Open Graph debugger to see how SERP previews render the new title + description.
    8. If ANY browser/theme combo fails the contrast or recognizability check:
       - Document which combo failed in `06-03-SUMMARY.md`.
       - If fixable in <30min: regenerate the SVG with corrected dark-mode CSS embedded, re-deploy, re-verify.
       - If not fixable in v1.0: open a follow-up issue, accept current state, document trade-off.
    9. Append a "## Visual verification" section to `06-03-SUMMARY.md` with one row per browser/theme combo: PASS/FAIL plus a one-line note.
  </how-to-verify>
  <acceptance_criteria>
    - polls.wtcsmapban.com loads with `<title>WTCS Community Suggestions</title>` (verifiable via `curl -s https://polls.wtcsmapban.com | grep -c "WTCS Community Suggestions"` returns ≥ 1).
    - Production URL serves `/favicon.svg`, `/favicon.ico`, `/apple-touch-icon.png`, `/favicon-32.png` with HTTP 200 (verifiable via `for f in favicon.svg favicon.ico apple-touch-icon.png favicon-32.png; do curl -sI "https://polls.wtcsmapban.com/$f" | head -1; done` shows `HTTP/2 200` for each).
    - Visual verification table in `06-03-SUMMARY.md` covers Chrome light, Chrome dark, Safari light, Safari dark, Firefox light, Firefox dark, and iOS home-screen (7 rows minimum).
    - At least Chrome (light + dark) PASS rows are recorded; if Safari or Firefox fail, document the failure mode but do NOT block the merge — the SVG fallback still works on every modern browser.
  </acceptance_criteria>
  <resume-signal>Type "visuals green" or paste a screenshot/PASS-table summary to close this task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Netlify CDN | Static asset delivery for favicon files; cache-busting via Netlify's HTML-revalidate default headers (RESEARCH.md Runtime State Inventory) |
| Build pipeline → public/ | Files in `public/` ship verbatim into `dist/`; no build-time transformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | favicon contains sensitive metadata (EXIF, generator string) | accept | realfavicongenerator outputs scrub EXIF by default; ICO/PNG generator string is harmless; no PII embedded |
| T-06-12 | Tampering | Stale cached favicon survives across deploy | mitigate | Netlify default cache headers for unhashed `public/` assets are HTML-class (max-age=0, must-revalidate) per RESEARCH.md Runtime State Inventory; hard refresh is sufficient (Task 3 step 2). No `netlify.toml` change required. |
| T-06-13 | Denial of Service | Favicon file too large slows initial render | mitigate | realfavicongenerator outputs are typically <10 KB total; `wc -c` audit in Task 2 verify ensures none exceeds reasonable bound |
</threat_model>

<verification>
- public/favicon.svg replaced (NOT the original Vite scaffold)
- public/favicon.ico exists with multi-res content
- public/apple-touch-icon.png exists at 180x180
- public/favicon-32.png exists at 32x32
- index.html `<title>` reads exactly `WTCS Community Suggestions`
- index.html meta description is the locked 153-char string
- Four `<link rel="icon">` tags in UI-SPEC Surface 5 order
- No Open Graph or Twitter meta tags introduced
- User-facing copy uses `opinions/responses`, NOT `polls/votes`
- npm run build succeeds; dist/ contains all four favicon files
- Production deploy at polls.wtcsmapban.com serves the new title + favicons
- Visual check at 16x16 passes on Chrome light + dark (minimum)
</verification>

<success_criteria>
- D-07 favicon set generated from src/assets/wtcs-logo.png and committed to public/
- D-10 title + meta description locked to UI-SPEC Surface 6 verbatim
- No new build dependencies installed
- No netlify.toml changes
- Husky pre-commit + lint-staged green
- Visual verification PASS on at least Chrome light + dark
- Original Vite scaffold favicon (React-leaf SVG) fully removed
- Lowercase slug `wtcs-community-polls` no longer appears in user-facing surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-03-SUMMARY.md`.
</output>
