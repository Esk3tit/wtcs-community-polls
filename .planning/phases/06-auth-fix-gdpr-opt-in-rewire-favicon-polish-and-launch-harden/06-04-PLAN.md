---
phase: 06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-launch-hardening/05-VERIFICATION.md
autonomous: false
requirements: []
tags: [D-08, D-09]

must_haves:
  truths:
    - "A real production frontend error is triggered, the resulting Sentry issue shows un-minified function names + source-map-resolved line numbers, and the deliberate-error trigger is rolled back in the SAME commit that closes the verification entry (D-08)"
    - "05-VERIFICATION.md `human_verification:` row for `Sentry sourcemap upload on first main-branch build` flips from `resolved` (indirect bundle inspection) to `resolved` with direct symbolicated-stack-trace evidence — including a Sentry issue link or screenshot path"
    - "REQUIREMENTS.md status column is audited row-by-row against actual phase artifacts (UAT.md / VERIFICATION.md / merged test files); each row newly marked Complete cites the specific evidence file (D-09)"
    - "REQUIREMENTS.md Traceability table at the bottom is kept in sync with the v1 Requirements checkbox states above"
    - "The 9th Phase 4 UAT cross-account test stays Pending — explicitly out of scope for Phase 6 (CONTEXT.md domain section)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status column audit per D-09; one atomic commit"
      contains: "Status"
    - path: ".planning/phases/05-launch-hardening/05-VERIFICATION.md"
      provides: "Sentry symbolicated-stack-trace evidence appended; sourcemap human-verification row closed with direct evidence (D-08)"
      contains: "symbolicated"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/{phase}/{phase}-UAT.md or {phase}-VERIFICATION.md"
      via: "Each newly-Complete row cites a specific evidence file"
      pattern: "Complete.*\\d{2}-(UAT|VERIFICATION)"
---

<objective>
Close two residual launch-hardening cleanup items left over from Phase 5:

1. **D-08:** Trigger a real production frontend error against the deployed Sentry-instrumented bundle, confirm in the Sentry UI that the resulting issue shows a fully-symbolicated stack trace (un-minified function names + source-map-resolved line numbers), then ROLL BACK the deliberate-error path in the SAME commit. Update `05-VERIFICATION.md` with direct evidence.

2. **D-09:** Audit `REQUIREMENTS.md` and update the Status column for v1 requirements that actually shipped in Phases 1-5 but were never marked Complete. Each newly-Complete row must cite the specific phase artifact (UAT entry, VERIFICATION entry, or merged-test file) that proves it shipped. Don't mark Complete on assumption.

Purpose: 05-VERIFICATION currently has the Sentry sourcemap row marked `resolved` based on INDIRECT bundle inspection (the source-map upload-and-strip pattern was inferred from the absence of `sourceMappingURL` comments in the bundle). The user explicitly wants direct symbolication evidence to close the launch hardening loop. REQUIREMENTS.md drift is a documentation hygiene problem flagged in the same Phase 5 verifier output.

Output: Sentry sourcemap human-verification entry closed with direct evidence; REQUIREMENTS.md status column synced to actual phase artifacts; zero new product code in main after Task 1's deliberate-error path is rolled back.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-CONTEXT.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-RESEARCH.md
@.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-PATTERNS.md
@.planning/phases/05-launch-hardening/05-VERIFICATION.md
@.planning/phases/04-admin-panel-suggestion-management/04-UAT.md

<interfaces>
<!-- Existing REQUIREMENTS.md candidate-list per CONTEXT.md D-09 (verbatim from RESEARCH.md phase_requirements section) -->

Likely-Complete candidates (must be evidence-checked, not auto-flipped):
- AUTH-01, AUTH-02, AUTH-04, AUTH-05  (Phase 1 closure + Phase 4 UAT 2026-04-25 prod verification)
- ADMN-01  (Phase 1 D-10 seed migration)
- VOTE-01, VOTE-02, VOTE-03  (Phase 2 + Phase 4 UAT)
- RSLT-01..RSLT-05  (Phase 2)
- CATG-02, CATG-03, CATG-04  (Phase 2 + Phase 4)
- UIDN-01, UIDN-02, UIDN-03  (Phase 1)
- INFR-03, INFR-04  (Phase 1 + Phase 5 verification)
- TEST-01, TEST-02, TEST-03, TEST-04  (Phase 1-3 closure)
- AUTH-03  (Phase 3 — verify guild-membership variant on AuthErrorPage shipped)
- VOTE-04  (Phase 3 — verify Upstash rate-limiting)

Out-of-scope (DO NOT mark Complete):
- 9th Phase 4 UAT cross-account test — requires second human (CONTEXT.md domain section)

Relevant evidence files:
- .planning/phases/01-foundation-authentication/01-04-SUMMARY.md (auth tests)
- .planning/phases/02-browsing-responding/02-*-SUMMARY.md (vote/results)
- .planning/phases/03-response-integrity/03-*-SUMMARY.md (guild + Upstash)
- .planning/phases/04-admin-panel-suggestion-management/04-UAT.md (re-run on prod 2026-04-25 — 8 of 9 blocked tests now pass)
- .planning/phases/05-launch-hardening/05-VERIFICATION.md (TEST-06 + INFR-02 already Complete)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Sentry symbolicated stack-trace verification (D-08) — trigger, capture, roll back atomically</name>
  <read_first>
    - .planning/phases/05-launch-hardening/05-VERIFICATION.md (current `human_verification:` row for sourcemap; the `resolved_evidence` field needs direct symbolication evidence appended)
    - .planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-CONTEXT.md (D-08 — Claude's discretion on trigger mechanism: admin-only debug route OR deliberate EF malformed payload OR one-time CI smoke)
    - src/components/AppErrorFallback.tsx (the existing error-boundary fallback that will catch the deliberate throw)
    - src/lib/sentry.ts (Sentry.init shape — confirms `release: import.meta.env.VITE_COMMIT_SHA` is set, which is the Sentry-side mapping key for sourcemap matching)
  </read_first>
  <files>(see <how-to-verify> below — this is a checkpoint task; no source files modified at this step)</files>
  <action>(see <how-to-verify> below — this is a checkpoint task; the action is human-driven verification, not autonomous code change)</action>
  <verify>(see <acceptance_criteria> below)</verify>
  <done>(see <acceptance_criteria> below)</done>
  <what-built>
    Nothing persistent. This task ships a SHORT-LIVED deliberate-error path, captures evidence, then rolls it back in the SAME commit that closes the verification entry.
  </what-built>
  <how-to-verify>
    Use Claude's discretion to pick ONE of three mechanisms (RESEARCH.md notes the choice). Recommended: **admin-only debug throw via query param**, lowest blast radius.

    Mechanism A — admin-only debug throw (RECOMMENDED):
    1. On a feature branch, add a tiny debug throw to an admin route. Pick `src/components/admin/AdminShell.tsx` (or whichever existing admin component is rendered after admin auth-gate clears). Add at the top of the component body (before any other state):
       ```typescript
       if (typeof window !== 'undefined' && new URLSearchParams(window.location.search).get('sentry-smoke') === '1') {
         throw new Error('Sentry sourcemap smoke — Phase 6 D-08')
       }
       ```
    2. Commit with message `chore(06-04): TEMP — Sentry smoke trigger (rolled back in next commit)`.
    3. Push the branch. Open PR. Merge to main. Wait for Netlify deploy + sourcemap upload (Phase 5 D-13 wires `sentryVitePlugin` to upload on main builds).
    4. Visit https://polls.wtcsmapban.com/admin?sentry-smoke=1 in the user's main browser (admin account). The error should fire, the AppErrorFallback should render, and Sentry should receive the issue.
    5. Open the Sentry project dashboard at https://o4510971582349312.ingest.us.sentry.io (or via the Sentry UI). Find the new `Sentry sourcemap smoke — Phase 6 D-08` issue.
    6. Click into the issue. Inspect the stack trace. Confirm:
       - Function names are NOT minified (e.g., shows `AdminShell` not `e` or `t`).
       - Line numbers point at SOURCE files (e.g., `src/components/admin/AdminShell.tsx:N`) NOT the compiled bundle (e.g., `assets/index-rZTs8ZMe.js:1:42312`).
       - Frames include surrounding source context (the `+1`/`-1` line preview Sentry shows when the sourcemap is good).
    7. Take a screenshot of the Sentry issue stack trace. Save to `docs/screenshots/sentry-symbolication-evidence-2026-04-25.png` (or similar dated filename — keep `docs/screenshots/` per Phase 5 D-15).
    8. Open a SECOND PR that ROLLS BACK the deliberate throw. Same file, remove the 3 lines added in step 1. Same atomic commit also updates `05-VERIFICATION.md` (Task 2 below). Commit message: `chore(05-VERIFICATION,06-04): close D-08 sourcemap symbolication; remove smoke trigger`.
    9. Merge the rollback PR. Verify on main that `grep -r "sentry-smoke" src/` returns NOTHING.

    Mechanism B (alternative — deliberate Edge Function malformed payload): if the user prefers EF-side, instead of step 1, push a transient EF that throws a known string. Tear down in the same atomic commit. Less blast-radius-favorable than Mechanism A.

    Mechanism C (alternative — one-time CI smoke): add a Playwright @manual test that hits `?sentry-smoke=1` as part of Phase 6 CI, then deletes itself. Most ephemeral but harder to inspect human-readable Sentry evidence in CI without screenshot infra.

    Pick ONE. Document the choice in 06-04-SUMMARY.md.

    Constraint reminder: the deliberate-error code MUST be rolled back in the SAME commit that closes 05-VERIFICATION.md. CONTEXT.md D-08 is explicit: "Roll back the deliberate-error path immediately after verification — no test artifacts left in main." That means the second PR's diff against main must show:
    - The 3 lines added in Task 1 step 1 are removed.
    - 05-VERIFICATION.md gets a `resolved_evidence` update.
    - Optionally: the screenshot file path is added.
    Nothing else.
  </how-to-verify>
  <acceptance_criteria>
    - Sentry issue exists with title `Sentry sourcemap smoke — Phase 6 D-08` (or similar; recorded in 06-04-SUMMARY.md).
    - Screenshot file exists at the documented path under `docs/screenshots/`.
    - The deliberate throw is fully removed from main: `grep -r "sentry-smoke\\|Phase 6 D-08" src/` returns NOTHING after rollback.
    - The rollback commit shows EXACTLY this delta vs. the pre-rollback main: removal of the 3 lines + update to 05-VERIFICATION.md + (optionally) addition of the screenshot.
    - The Sentry stack trace screenshot shows symbolicated frames (function names NOT minified single letters; line numbers pointing at source files under `src/`).
  </acceptance_criteria>
  <resume-signal>Type "sentry symbolicated at <issue-id>" or paste the screenshot path to close this task.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update 05-VERIFICATION.md with direct symbolication evidence (D-08)</name>
  <files>.planning/phases/05-launch-hardening/05-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/05-launch-hardening/05-VERIFICATION.md (target file — full read; the `human_verification:` block at lines 21-32 contains the row to update)
  </read_first>
  <action>
    The current `05-VERIFICATION.md` frontmatter has a `re_verification.closed` entry for "Sentry sourcemap upload on first main-branch build" with `evidence:` describing INDIRECT bundle inspection (the absence of `sourceMappingURL` comments). It also has a `human_verification:` row with the same name, status `resolved`, and `resolved_evidence` mentioning the same indirect proof and noting "A symbolicated stack-trace test would require triggering a real frontend error against Sentry's dashboard, which is a separate human action."

    Phase 6 Task 1 produced that direct evidence. Update the file by APPENDING (not replacing — preserve the indirect inspection note as historical record) a new line to BOTH the `re_verification.closed[]` evidence string and the `human_verification[]` row's `resolved_evidence` string for "Sentry sourcemap upload on first main-branch build".

    For the `re_verification.closed[]` block, change:
    ```yaml
        - test: "Sentry sourcemap upload on first main-branch build"
          evidence: "Live JS bundle at https://polls.wtcsmapban.com/assets/index-rZTs8ZMe.js (470 KB) inspected on 2026-04-25. ... [existing text preserved verbatim] ..."
    ```
    to:
    ```yaml
        - test: "Sentry sourcemap upload on first main-branch build"
          evidence: "Live JS bundle at https://polls.wtcsmapban.com/assets/index-rZTs8ZMe.js (470 KB) inspected on 2026-04-25. ... [existing text preserved verbatim] ... [Phase 6 D-08 update, <YYYY-MM-DD>]: Direct symbolication confirmed via deliberate ?sentry-smoke=1 throw on admin route — Sentry issue <ISSUE-ID> shows un-minified function names (AdminShell, RootLayout) and source-resolved line numbers (src/components/admin/AdminShell.tsx:N). Screenshot at docs/screenshots/sentry-symbolication-evidence-<YYYY-MM-DD>.png. Smoke trigger rolled back in same commit (grep on src/ returns zero matches for 'sentry-smoke')."
    ```
    Replace `<YYYY-MM-DD>` with today's date (2026-04-25 or later when this task runs), `<ISSUE-ID>` with the actual Sentry issue identifier from Task 1 step 5.

    For the `human_verification[]` row "Sentry sourcemap upload on first main-branch build", apply the SAME append to its `resolved_evidence:` field. Status remains `resolved` (it was already marked resolved on indirect grounds; this just adds direct grounds).

    Add at the bottom of the file (BEFORE the `*Verified:* / *Verifier:*` footer block) a new section:

    ```markdown
    ## Phase 6 Update — Direct Sourcemap Symbolication (2026-04-25)

    Phase 6 D-08 closed the indirect-only evidence gap on the Sentry sourcemap row by triggering a deliberate frontend error and capturing direct symbolicated stack-trace evidence.

    | Property | Value |
    |----------|-------|
    | Mechanism | Admin-only `?sentry-smoke=1` query param throw, rolled back in same commit |
    | Sentry issue | `<ISSUE-URL>` |
    | Screenshot | `docs/screenshots/sentry-symbolication-evidence-<YYYY-MM-DD>.png` |
    | Rollback verified | `grep -r "sentry-smoke" src/` returns 0 matches on main as of <YYYY-MM-DD> |

    No other Phase 5 verification items affected.
    ```

    Do NOT modify the `re_verification.remaining[]` row for "9 blocked Phase 4 UAT items" — that row stays as-is (out of scope per CONTEXT.md).

    Commit this change atomically with the smoke-rollback diff per Task 1 step 8.
  </action>
  <verify>
    <automated>grep -c "Phase 6 D-08" .planning/phases/05-launch-hardening/05-VERIFICATION.md && grep -c "sentry-symbolication-evidence" .planning/phases/05-launch-hardening/05-VERIFICATION.md && grep -c "Direct Sourcemap Symbolication" .planning/phases/05-launch-hardening/05-VERIFICATION.md && bash -c '! grep -r "sentry-smoke" src/'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 6 D-08" .planning/phases/05-launch-hardening/05-VERIFICATION.md` returns ≥ 2 (one in `re_verification.closed[].evidence`, one in `human_verification[].resolved_evidence`).
    - `grep -c "sentry-symbolication-evidence" .planning/phases/05-launch-hardening/05-VERIFICATION.md` returns ≥ 1.
    - `grep -c "## Phase 6 Update" .planning/phases/05-launch-hardening/05-VERIFICATION.md` returns 1.
    - The original `evidence:` text for "Sentry sourcemap upload on first main-branch build" (mentioning the 470 KB live bundle inspection) is PRESERVED — verifiable via `grep -c "470 KB" .planning/phases/05-launch-hardening/05-VERIFICATION.md` returns ≥ 1.
    - `grep -r "sentry-smoke" src/` returns NOTHING on main after the rollback.
    - `grep -E "## (re_verification|remaining)" .planning/phases/05-launch-hardening/05-VERIFICATION.md` does NOT show changes to the "9 blocked Phase 4 UAT items" row content.
  </acceptance_criteria>
  <done>05-VERIFICATION.md gains direct symbolication evidence (preserves prior indirect note); rollback commit verified; no test artifacts left in main.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: REQUIREMENTS.md status sync — evidence-driven audit (D-09)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (target file — full read)
    - .planning/phases/04-admin-panel-suggestion-management/04-UAT.md (re-run 2026-04-25 — primary evidence for AUTH/VOTE/RSLT/CATG)
    - .planning/phases/05-launch-hardening/05-VERIFICATION.md (already-Complete TEST-06 + INFR-02; add INFR-03/04 evidence if it covers them)
    - .planning/phases/03-response-integrity/03-*-SUMMARY.md (AUTH-03, VOTE-04 evidence)
    - .planning/phases/01-foundation-authentication/01-04-SUMMARY.md (TEST-01, TEST-02 evidence)
    - .planning/phases/02-browsing-responding/02-*-SUMMARY.md (TEST-03 evidence)
  </read_first>
  <action>
    Audit REQUIREMENTS.md row by row. For each candidate REQ-ID listed in CONTEXT.md D-09 (and the Interfaces block above), read the cited evidence file. Flip the checkbox AND the Traceability table row Status to `Complete` ONLY when the evidence file contains direct proof. Add an inline evidence comment (markdown HTML comment OR inline link) on each newly-flipped row pointing at the proving file.

    Evidence-driven flip table (executor MUST verify each before flipping):

    | REQ | Evidence file | What to grep for |
    |-----|---------------|------------------|
    | AUTH-01 | `04-UAT.md` (2026-04-25 re-run) | "Discord OAuth E2E" or "test 1 (login)" PASS |
    | AUTH-02 | `04-UAT.md` or `01-04-SUMMARY.md` | "2FA" rejection variant; AuthErrorPage `2fa-required` reason in tests |
    | AUTH-03 | `03-01-SUMMARY.md` | "guild membership" + AuthErrorPage `not-in-server` variant |
    | AUTH-04 | `01-04-SUMMARY.md` | session-persistence test |
    | AUTH-05 | `04-UAT.md` | logout test PASS |
    | ADMN-01 | `01-02-SUMMARY.md` | seed admins migration |
    | VOTE-01 | `02-*-SUMMARY.md` or `04-UAT.md` | UNIQUE constraint test or one-vote-per-user E2E |
    | VOTE-02 | `02-*-SUMMARY.md` | submit-vote Edge Function tests |
    | VOTE-03 | `02-*-SUMMARY.md` | RLS UPDATE/DELETE-deny tests |
    | VOTE-04 | `03-02-SUMMARY.md` | Upstash rate limiting E2E |
    | RSLT-01..05 | `02-*-SUMMARY.md` + `04-UAT.md` | results visibility tests passing |
    | CATG-02..04 | `02-*-SUMMARY.md` + `04-UAT.md` | filter/search tests passing |
    | UIDN-01..03 | `01-04-SUMMARY.md` + `04-UAT.md` | mobile-first + theme-toggle tests; UI-checker reports |
    | INFR-03 | `01-*-SUMMARY.md` + `05-VERIFICATION.md` | direct supabase-js reads + RLS verification |
    | INFR-04 | `02-*-SUMMARY.md` + `05-VERIFICATION.md` | EF for vote-write verified live |
    | TEST-01 | `01-01-SUMMARY.md` | Vitest + RTL configured |
    | TEST-02 | `01-04-SUMMARY.md` | auth tests passing |
    | TEST-03 | `02-*-SUMMARY.md` | response/results tests merged |
    | TEST-04 | `03-*-SUMMARY.md` | guild + rate-limit tests merged |

    For each REQ where evidence is found:
    1. Flip the checkbox in the v1 Requirements section: `- [ ] **REQ-XX**: ...` → `- [x] **REQ-XX**: ...`.
    2. Add an inline evidence comment AFTER the description: `- [x] **REQ-XX**: <description> <!-- Evidence: .planning/phases/{phase}/{file} -->`. Keep it terse — one comment per row.
    3. Flip the Traceability table row Status: `| REQ-XX | Phase N | Pending |` → `| REQ-XX | Phase N | Complete |`.

    For each REQ where evidence is INSUFFICIENT or AMBIGUOUS:
    1. Leave the checkbox unchecked.
    2. Add a short comment explaining why: `<!-- Phase 6 D-09 audit: evidence not found in {expected file}; status remains Pending -->`.
    3. List these in 06-04-SUMMARY.md "## REQ Audit Gaps".

    DO NOT flip:
    - The 9th Phase 4 UAT cross-account item — explicitly out of scope.
    - Any REQ for which evidence cannot be located.

    Update the document footer "Last updated:" line to today's date with a one-line summary: `*Last updated: <YYYY-MM-DD> — Phase 6 D-09 status sync: <N> requirements flipped to Complete (each with cited evidence); <M> remain Pending pending evidence*`.

    Update the Coverage block at the bottom:
    ```
    **Coverage:**
    - v1 requirements: 45 total
    - Mapped to phases: 45
    - Unmapped: 0
    - Complete (post-Phase-6 D-09 audit): <N>
    - Pending: <45 - N>
    ```

    Commit message: `docs(REQUIREMENTS,06-04): Phase 6 D-09 status sync — flip <N> v1 requirements to Complete with evidence citations`.
  </action>
  <verify>
    <automated>bash -c 'pre=$(grep -c "^- \\[ \\]" .planning/REQUIREMENTS.md); post=$(grep -c "^- \\[x\\]" .planning/REQUIREMENTS.md); echo "Unchecked: $pre, Checked: $post"; test "$post" -gt 17' && grep -c "Phase 6 D-09" .planning/REQUIREMENTS.md && grep -c "Evidence: .planning/phases/" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - At least 18 v1 requirement checkboxes are now `[x]` (the 6 already-Complete admin/categories/lifecycle requirements + at least 12 newly-flipped ones from the D-09 candidate list).
    - At least 12 inline evidence comments of the form `<!-- Evidence: .planning/phases/{phase}/{file} -->` are present.
    - The Traceability table at the bottom of REQUIREMENTS.md is in sync with the checkbox states above (no row marked Pending where the checkbox is `[x]`, no row marked Complete where the checkbox is `[ ]`):
      ```bash
      # Cross-check helper
      checked_count=$(grep -c "^- \\[x\\] \\*\\*" .planning/REQUIREMENTS.md)
      complete_count=$(awk '/^\\| Coverage:|^---/{exit} /Complete/{print}' .planning/REQUIREMENTS.md | grep -c "Complete")
      ```
      Expectation: `checked_count` and `complete_count` are equal (or differ by exactly 0 — the audit produces a coherent picture).
    - The 9th Phase 4 UAT row is NOT marked Complete anywhere.
    - `grep -c "Phase 6 D-09" .planning/REQUIREMENTS.md` returns ≥ 1 (footer "Last updated" line references the D-09 audit).
    - `grep -c "Coverage:" .planning/REQUIREMENTS.md` returns 1 and the block has updated counts.
    - One atomic commit per file rule (D-09 explicit): `git log --oneline -1` shows the latest commit touches ONLY `.planning/REQUIREMENTS.md` (or REQUIREMENTS.md + 06-04-SUMMARY.md if you record gaps in summary; do NOT mix with the Sentry rollback commit from Task 2).
  </acceptance_criteria>
  <done>REQUIREMENTS.md status column synced to actual phase artifacts with inline evidence citations; Traceability table coherent with checkbox states; out-of-scope items left Pending; one atomic commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Sentry SaaS | Smoke-test issue contains synthetic stack trace; no PII because the throw site has no user data in scope |
| Repo main branch | Deliberate-error code MUST NOT survive past Task 1 rollback commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Information Disclosure | Smoke-test issue exposes prod admin route URL pattern in Sentry public links | accept | Sentry issue access requires logged-in project member; admin route URL is already publicly known (returns 404/redirect for non-admins per Phase 4 routing) |
| T-06-15 | Tampering | Deliberate throw forgotten in main breaks production for admins | mitigate | Two-layer defense: (a) gated by query param `?sentry-smoke=1` (no accidental triggers); (b) Task 1 step 9 grep gate verifies removal in same commit; acceptance_criteria for Task 2 enforces grep returning zero |
| T-06-16 | Repudiation | REQUIREMENTS row flipped to Complete without evidence | mitigate | Task 3 mandates inline evidence comment per flipped row; acceptance_criteria asserts ≥12 such comments exist; ambiguous cases stay Pending and are listed in 06-04-SUMMARY.md gaps section |
</threat_model>

<verification>
- Sentry issue from deliberate throw shows symbolicated stack trace
- Screenshot saved to docs/screenshots/
- Smoke trigger rolled back atomically; grep on src/ confirms zero residue
- 05-VERIFICATION.md preserves prior indirect evidence AND adds direct symbolication evidence
- 05-VERIFICATION.md "## Phase 6 Update" section appended
- REQUIREMENTS.md ≥18 checkboxes are [x] (6 prior-Complete + ≥12 newly-flipped)
- ≥12 inline evidence comments cite specific phase artifacts
- Traceability table in sync with checkboxes
- 9th Phase 4 UAT cross-account row stays Pending
- One atomic commit per file (D-09 rule); separate commit from Task 2 rollback
- Coverage block at bottom of REQUIREMENTS.md shows updated Complete/Pending counts
</verification>

<success_criteria>
- D-08 closed with direct symbolication evidence; deliberate-error path rolled back in same commit
- D-09 status audit landed; each newly-Complete row cites evidence; Traceability table coherent
- No test artifacts left in main (`grep -r sentry-smoke src/` returns zero)
- 05-VERIFICATION.md preserves historical indirect-evidence note (forensic continuity)
- Husky pre-commit + lint-staged green
- 9th Phase 4 UAT cross-account test stays Pending (out of scope)
</success_criteria>

<output>
After completion, create `.planning/phases/06-auth-fix-gdpr-opt-in-rewire-favicon-polish-and-launch-harden/06-04-SUMMARY.md`. Include a "## REQ Audit Gaps" section listing any REQ-IDs left Pending for lack of evidence.
</output>
