---
phase: 16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
requirements: [PERF-01]
autonomous: false  # slopcheck checkpoint:human-verify before install
must_haves:
  truths:
    - "`npm run build:analyze` produces `dist/stats.html` (treemap) under PERF-01's env-gated visualizer plugin"
    - "Production builds without `ANALYZE=true` still chain `sentryVitePlugin` last and upload sourcemaps (OBSV-04 evidence chain preserved)"
    - "Any invocation with `ANALYZE=true` in a Netlify production deploy (`CONTEXT=production`, with legacy `NETLIFY_CONTEXT=production` also guarded) aborts at vite config-load time with an OBSV-04-naming error message"
    - "Phase 15's 7-name keepNames allowlist still passes against a vanilla `npm run build`"
  artifacts:
    - path: "package.json"
      provides: "`build:analyze` script + `rollup-plugin-visualizer@7.0.1` devDep"
      contains: "build:analyze"
    - path: "vite.config.ts"
      provides: "env-gated visualizer-vs-sentry mutex + D-09 config-load throw"
      contains: "rollup-plugin-visualizer"
    - path: "dist/stats.html"
      provides: "Treemap emitted only when ANALYZE=true (regenerable; not committed)"
  key_links:
    - from: "vite.config.ts"
      to: "rollup-plugin-visualizer"
      via: "import + conditional plugins.push under ANALYZE env-gate"
      pattern: "ANALYZE.*visualizer|visualizer.*ANALYZE"
    - from: "vite.config.ts module scope"
      to: "process.env.ANALYZE && (process.env.CONTEXT || process.env.NETLIFY_CONTEXT)"
      via: "throw new Error before defineConfig"
      pattern: "OBSV-04"
---

<objective>
Add `rollup-plugin-visualizer@7.0.1` as a dev-dependency, integrate it into `vite.config.ts` behind a mutually-exclusive `ANALYZE=true` env-gate against `sentryVitePlugin`, add a `build:analyze` npm script, and throw at config-load when `ANALYZE=true && NETLIFY_CONTEXT=production` to protect the Phase 15 OBSV-04 sourcemap-upload chain (D-09). Wave 1 only — PERF-02 baseline (plan 02) depends on this landing first.

Purpose: Bundle analysis is a precondition for measuring PERF-02's pre-change baseline and verifying PERF-04's named chunks. The mutex + D-09 throw prevents an accidental `ANALYZE=true` in Netlify CI from silently displacing `sentryVitePlugin` from its required last position and skipping sourcemap upload.

Output: `vite.config.ts` carries the env-gated plugin block + the config-load throw; `package.json` carries the new script + devDep; `npm run build` (without `ANALYZE`) behaves byte-identically to today.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-CONTEXT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-RESEARCH.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-PATTERNS.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-UI-SPEC.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-VALIDATION.md

@vite.config.ts
@package.json
@scripts/verify-sourcemap-names.mjs

<interfaces>
<!-- Existing vite.config.ts shape Phase 16 must extend (NOT replace). -->
<!-- Source: vite.config.ts lines 1-57 (verified 2026-05-27). -->

Current plugin order (line 14-28): `tanstackRouter() → react() → tailwindcss() → sentryVitePlugin({...disable: mode !== 'production'})`.
Current build.rolldownOptions.output (line 33-35): `{ keepNames: true }` — must be preserved verbatim (Phase 15 invariant).
Existing module-level comment (line 8): `// Sentry plugin must be LAST — tree-shaking landmine.` — load-bearing context for D-09; preserve.

Current package.json scripts (line 7-15):
  "dev": "vite",
  "build": "tsr generate && tsc -b && vite build",
  "lint": "eslint .",
  "preview": "vite preview",
  "test": "vitest"
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Slopcheck — verify `rollup-plugin-visualizer@7.0.1` legitimacy before install (PERF-01, T-16-SC)</name>
  <what-built>
    Per RESEARCH.md § Package Legitimacy Audit, `rollup-plugin-visualizer@7.0.1` is `[ASSUMED]` (slopcheck unavailable at research time). The Package Legitimacy Gate mandates a blocking-human checkpoint before any `npm install --save-dev` for `[ASSUMED]`/`[SUS]` packages. This task gates the install at Task 2 behind explicit operator verification.
  </what-built>
  <how-to-verify>
    1. Run `npm view rollup-plugin-visualizer@7.0.1 version` — must echo `7.0.1`.
    2. Run `npm view rollup-plugin-visualizer@7.0.1 repository.url` — must point at `github.com/btd/rollup-plugin-visualizer` (https or git+ form).
    3. Open `https://www.npmjs.com/package/rollup-plugin-visualizer/v/7.0.1` in a browser and confirm the publisher is `btd` (a long-standing maintainer; package first published 2017).
    4. Confirm the README / source repo at `github.com/btd/rollup-plugin-visualizer` matches the npm metadata (no typosquat, no abandoned fork).
    5. If anything looks off (different repo URL, suspended publisher, freshly-published package, repo doesn't exist), STOP and report — do NOT proceed to Task 2.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the `npm install --save-dev rollup-plugin-visualizer@7.0.1` step. Otherwise describe what looked off and the plan halts.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install `rollup-plugin-visualizer@7.0.1` as devDependency and add `build:analyze` npm script</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npm install --save-dev --save-exact rollup-plugin-visualizer@7.0.1` from the project root. The `--save-exact` flag is MANDATORY: a plain `npm install --save-dev` writes `"^7.0.1"` (caret range) unless the project has `save-exact=true` in `.npmrc`, which this project does not. The Package Legitimacy Audit requires an exact pin, so `--save-exact` forces the literal `"7.0.1"`. This adds the exact-pinned devDep into `devDependencies` (alphabetically sorted between `globals` and `husky` per existing ordering) and updates `package-lock.json`. Then edit `package.json` `"scripts"` block to add a new entry `"build:analyze": "ANALYZE=true npm run build"` placed immediately after the existing `"build"` script (line 11 area). CRITICAL: the script MUST be `ANALYZE=true npm run build` (NOT `ANALYZE=true tsr generate && tsc -b && vite build`). The inline form `ANALYZE=true tsr generate && tsc -b && vite build` scopes the `ANALYZE` env var only to the first command (`tsr generate`); `tsc -b` and `vite build` run WITHOUT `ANALYZE`, so `vite build` never sees the flag and `dist/stats.html` is never emitted. Wrapping the whole chain in `npm run build` re-exports `ANALYZE=true` to the entire child process, so `vite build` sees it. After install + edit, verify the script runs end-to-end (not just that the string is present). After verifying the package landed, confirm the literal version string `"7.0.1"` (no `^`/`~`) via the automated check below.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); if (p.devDependencies['rollup-plugin-visualizer'] !== '7.0.1') process.exit(1); if (p.scripts['build:analyze'] !== 'ANALYZE=true npm run build') process.exit(2); console.log('OK')" && bash -o pipefail -c 'ANALYZE=true npm run build:analyze 2>&1 | tail -20' && test -f dist/stats.html && rm -rf dist</automated>
  </verify>
  <done>`package.json` contains exactly `"rollup-plugin-visualizer": "7.0.1"` (no `^`/`~` prefix — confirms `--save-exact` worked) in `devDependencies` and `"build:analyze": "ANALYZE=true npm run build"`; running `npm run build:analyze` emits `dist/stats.html` (proves the env var reaches `vite build`); `package-lock.json` reflects the install; `node_modules/rollup-plugin-visualizer` exists; `npm run lint` and `npm run test` still pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Edit `vite.config.ts` — D-09 throw + env-gated visualizer/sentry mutex (PERF-01)</name>
  <files>vite.config.ts</files>
  <behavior>
    - Module-load with empty env: plugins array ends with `sentryVitePlugin({...})` exactly as today; build emits sourcemaps + upload (mode-gated) preserving Phase 15 OBSV-04 contract.
    - Module-load with `ANALYZE=true` only: plugins array ends with `visualizer({...})` instead of `sentryVitePlugin`; build emits `dist/stats.html`; sourcemap upload is intentionally skipped (analyze is local-only).
    - Module-load with `ANALYZE=true` in a Netlify production deploy (where Netlify sets `CONTEXT=production`): `defineConfig` is never invoked because module-scope `throw` fires first. The legacy `NETLIFY_CONTEXT=production` is ALSO guarded for defense-in-depth, but `CONTEXT` is the var Netlify natively sets (verified against `netlify.toml:9` — `command = "npm ci && VITE_COMMIT_SHA=$COMMIT_REF VITE_NETLIFY_CONTEXT=$CONTEXT npm run build"`, which re-exports `$CONTEXT` as `VITE_NETLIFY_CONTEXT` for the browser bundle but never sets a bare `NETLIFY_CONTEXT` in the build process). Error message includes the literal token `OBSV-04` and references the Phase 15 sourcemap-upload chain (UI-SPEC § Copywriting Contract).
    - `rolldownOptions.output.keepNames: true` stays present and unchanged (Phase 15 invariant — RESEARCH § Common Pitfalls > Pitfall 8 forbids creating a parallel `rollupOptions`).
  </behavior>
  <action>
    Edit `vite.config.ts` (current state: 57 lines, verified 2026-05-27). All changes target this single file.

    (a) Add a new import line after the existing `sentryVitePlugin` import (line 6): `import { visualizer } from 'rollup-plugin-visualizer'`. Place alphabetically OK — order is conventional only here.

    (b) Insert a module-scope `if`/`throw` block BEFORE `export default defineConfig(...)` (current line 13). The block fires at config-load time on any invocation. Condition: `if (process.env.ANALYZE === 'true' && (process.env.CONTEXT === 'production' || process.env.NETLIFY_CONTEXT === 'production'))`. CRITICAL: `CONTEXT` (not `NETLIFY_CONTEXT`) is the env var Netlify natively sets to `production` in a production deploy — verified against `netlify.toml:9`. Keying only on `NETLIFY_CONTEXT` (the original plan) would make the trap INERT in real Netlify CI because Netlify never sets a bare `NETLIFY_CONTEXT`; it sets `CONTEXT` and the toml re-exports it as `VITE_NETLIFY_CONTEXT`. Guard BOTH `CONTEXT` and `NETLIFY_CONTEXT` so the trap fires in real Netlify CI (`CONTEXT`) AND under any local/manual env that sets the legacy name. The thrown Error message must (per UI-SPEC line 116-123): name the OBSV-04 evidence chain explicitly by token, reference Phase 15's sourcemap upload, state the rollup-plugin-visualizer + sentryVitePlugin last-position mutex as the root cause, and tell the developer what to do (unset `ANALYZE` or run analyze locally only). Follow the suggested wording from UI-SPEC line 121 — refine for terseness if helpful but keep the `OBSV-04` token and the Phase 15 reference (UI-SPEC contract requires both). Free-standing module-scope comment above the block (WHY-only, no plan/phase IDs in source): explain that this guards the Phase 15 sourcemap-upload chain against accidental ANALYZE leakage in Netlify CI, and note that `CONTEXT` is Netlify's native deploy-context var (the legacy `NETLIFY_CONTEXT` is guarded too for defense-in-depth).

    (c) Refactor the current `plugins:` literal (lines 14-28) into a `const plugins = [...]` built BEFORE the `return` of `defineConfig`. Inside the `defineConfig(({ mode }) => { ... })` factory body: build `const plugins = [tanstackRouter({ target: 'react', autoCodeSplitting: true }), react(), tailwindcss()]`; then `if (process.env.ANALYZE === 'true') { plugins.push(visualizer({ filename: 'dist/stats.html', template: 'treemap', gzipSize: true, brotliSize: true, open: false })) } else { plugins.push(sentryVitePlugin({ org: process.env.SENTRY_ORG, project: process.env.SENTRY_PROJECT, authToken: process.env.SENTRY_AUTH_TOKEN, sourcemaps: { filesToDeleteAfterUpload: './dist/**/*.map' }, disable: mode !== 'production' })) }`; then `return { plugins, build: { ... }, resolve: { ... }, test: { ... } }`. Convert the factory from the current implicit-return arrow `({ mode }) => ({ ... })` to a braced arrow with explicit `return`. The `sentryVitePlugin` invocation must be byte-identical to today's lines 21-27 (org/project/authToken/sourcemaps/disable preserved). The existing line-8 comment `// Sentry plugin must be LAST — tree-shaking landmine.` stays — extend it with a one-line WHY note that visualizer is the mutually-exclusive analyze-only alternative (no plan/phase IDs in the comment; rationale only).

    (d) `build.rolldownOptions.output.keepNames: true` stays exactly as-is (line 33-35). Phase 16 will EXTEND that `output` object with `manualChunks` later in plan 04 — do NOT touch it in this plan.

    (e) All other vite.config.ts blocks (`build.sourcemap: 'hidden'`, `resolve.alias`, `test: {...}`) preserve byte-for-byte.

    Project conventions: TypeScript strict; ESM only; no plan/round/phase IDs in comments (this is `src/`-adjacent — WHY-only). Use the `@/` import alias would not apply here since this file is not under `src/` — vite.config.ts uses relative imports for its tooling.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'npm run build 2>&1 | tail -30' && ls dist/assets/ | head -5 && node scripts/verify-sourcemap-names.mjs</automated>
  </verify>
  <done>
    1. `npm run build` exits 0 (run under `bash -o pipefail -c` so a build failure is NOT masked by `tail` exiting 0); `dist/` is populated; no `dist/stats.html` emitted (sentryVitePlugin path active).
    2. `node scripts/verify-sourcemap-names.mjs` exits 0 with "7/7 allowlisted names found" (Phase 15 keepNames invariant holds).
    3. `ANALYZE=true npm run build` exits 0; `dist/stats.html` is emitted; treemap opens locally and shows the pre-PERF-03 chunk graph with `posthog-js` in the main entry chunk.
    4. `CONTEXT=production ANALYZE=true npm run build` exits NON-ZERO; stderr/stdout contains the literal token `OBSV-04` (this is the env var Netlify actually sets — see netlify.toml:9). `NETLIFY_CONTEXT=production ANALYZE=true npm run build` ALSO exits NON-ZERO (legacy guard).
    5. `vite.config.ts` shows `rolldownOptions.output.keepNames: true` unchanged.
    6. No `rollupOptions` key was introduced anywhere in the file (Pitfall 8 anti-regression).
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify D-09 trap fires + sourcemap-name allowlist still green</name>
  <files>(none — verification only)</files>
  <action>
    Run the verification invocations end-to-end and capture exit codes + key output lines into the task summary. (1) `npm run build` — must exit 0 and emit dist/assets/*.js without `dist/stats.html`. Confirm via `ls dist/stats.html 2>&1 | grep -c 'No such file'` returns `1`. (2) `node scripts/verify-sourcemap-names.mjs` — must exit 0 and print `7/7 allowlisted names found — keepNames contract holds.` (string from `scripts/verify-sourcemap-names.mjs:76`). (3) `ANALYZE=true npm run build` — must exit 0 and emit `dist/stats.html`. Confirm via `test -f dist/stats.html`. (4a) `CONTEXT=production ANALYZE=true npm run build` — must exit NON-ZERO (this is the var Netlify natively sets — netlify.toml:9). Capture stderr; confirm it contains the literal token `OBSV-04` via `grep -c 'OBSV-04'` ≥ 1. (4b) `NETLIFY_CONTEXT=production ANALYZE=true npm run build` — must ALSO exit NON-ZERO with `OBSV-04` (legacy defense-in-depth guard). (5) `git diff --stat vite.config.ts package.json` — confirm only these two files (plus package-lock.json) changed. All build invocations that capture output run under `bash -o pipefail -c` so a failing build cannot be masked by a downstream pipe stage exiting 0.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c 'rm -rf dist && npm run build && test ! -f dist/stats.html && node scripts/verify-sourcemap-names.mjs && rm -rf dist && ANALYZE=true npm run build && test -f dist/stats.html && rm -rf dist && (CONTEXT=production ANALYZE=true npm run build 2>&1 | grep -q "OBSV-04" && echo D09_TRAP_CONTEXT_OK) && (NETLIFY_CONTEXT=production ANALYZE=true npm run build 2>&1 | grep -q "OBSV-04" && echo D09_TRAP_LEGACY_OK)' || (echo D09_TRAP_DID_NOT_FIRE >&2 && exit 1)</automated>
  </verify>
  <done>All invocations behave per the action's expected exit codes; D-09 error message names `OBSV-04`; the trap fires on BOTH `CONTEXT=production` (Netlify-native) and `NETLIFY_CONTEXT=production` (legacy); Phase 15 sourcemap-names allowlist passes against the vanilla build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine / Netlify CI → `vite.config.ts` module-load | Build environment can set `ANALYZE` and `NETLIFY_CONTEXT` env vars; misconfiguration here breaks the OBSV-04 sourcemap-upload chain shipped in Phase 15. |
| npm registry → developer/CI install | New dev-dependency `rollup-plugin-visualizer@7.0.1` enters the build chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-SC | Tampering (supply chain) | `rollup-plugin-visualizer@7.0.1` (`[ASSUMED]` per RESEARCH.md § Package Legitimacy Audit) | mitigate | Blocking `checkpoint:human-verify` (Task 1) before `npm install --save-dev`. Operator runs `npm view ...@7.0.1` and visually confirms the `github.com/btd/rollup-plugin-visualizer` source repo. Never auto-approvable (workflow.auto_advance ignored for this checkpoint). |
| T-16-01 | Tampering (build supply chain) | `vite.config.ts` plugin order — visualizer and sentryVitePlugin both require last position; if both active, sourcemap upload silently skipped | mitigate | Env-gated mutex (Task 3): plugins array contains visualizer OR sentryVitePlugin, never both. D-09 throw at config-load aborts the build if `ANALYZE=true && (CONTEXT=production || NETLIFY_CONTEXT=production)`, preventing accidental Netlify production builds without sourcemap upload. CRITICAL: the guard keys on `CONTEXT` (Netlify-native, verified netlify.toml:9) AND legacy `NETLIFY_CONTEXT` — keying only on `NETLIFY_CONTEXT` would make the trap inert in real Netlify CI. Error message names `OBSV-04` explicitly so a future maintainer sees the trap, not just the symptom. |
| T-16-02 | Information disclosure | `dist/stats.html` (treemap) leaks bundle composition publicly if deployed | accept | Treemap is only emitted under `ANALYZE=true` (developer-local). The D-09 throw blocks production builds with `ANALYZE=true`. The `build:analyze` script does not run in Netlify CI by default. No-op risk if invariants hold. |
| T-16-03 | Repudiation | Sourcemap upload skipped silently → Sentry stack frames are mangled → on-call cannot attribute errors | mitigate | Same control as T-16-01 (D-09 throw). Phase 15's `scripts/verify-sourcemap-names.mjs` also runs in CI lint-and-unit; allowlist regression would fail the job. |

No new HIGH-severity threats introduced by this plan — it is a hardening pass that protects an existing security boundary (OBSV-04 sourcemap-upload integrity).
</threat_model>

<verification>
End-to-end checks for this plan (all automated; build invocations that capture output run under `bash -o pipefail -c` so a failing build is never masked by a downstream pipe stage):

1. `npm run build` (no env vars) → exit 0; no `dist/stats.html`; `dist/assets/*.js` present.
2. `node scripts/verify-sourcemap-names.mjs` against that build → exit 0; "7/7 allowlisted names found" — Phase 15 contract holds.
3. `ANALYZE=true npm run build` → exit 0; `dist/stats.html` emitted; opens to a treemap.
3b. `npm run build:analyze` → exit 0; `dist/stats.html` emitted (proves the `ANALYZE=true npm run build` script form propagates the env var to `vite build`, unlike the broken inline `ANALYZE=true tsr generate && tsc -b && vite build` form).
4. `CONTEXT=production ANALYZE=true npm run build` → exit ≠ 0; stderr contains `OBSV-04` (Netlify-native var). `NETLIFY_CONTEXT=production ANALYZE=true npm run build` → exit ≠ 0; stderr contains `OBSV-04` (legacy guard).
5. `npm run lint && npm run test` → exit 0 (no regressions from the import addition).
6. `git diff --stat` shows only `package.json`, `package-lock.json`, `vite.config.ts` changed.
</verification>

<success_criteria>
- `package.json` carries `"rollup-plugin-visualizer": "7.0.1"` in `devDependencies` (exact-pinned via `--save-exact`, no `^`/`~`) and `"build:analyze": "ANALYZE=true npm run build"` in `scripts` (the `npm run build` wrapper form — NOT the broken inline `ANALYZE=true tsr generate && tsc -b && vite build` which never propagates `ANALYZE` to `vite build`).
- `vite.config.ts` carries: (a) the new `visualizer` import; (b) a module-scope `throw` block that fires when `ANALYZE=true && (CONTEXT=production || NETLIFY_CONTEXT=production)` and whose error message contains the literal token `OBSV-04` (the `CONTEXT` guard is the load-bearing one — it is the var Netlify natively sets); (c) an env-gated `if/else` that pushes `visualizer({...})` OR `sentryVitePlugin({...})` to the plugins array — never both; (d) unchanged `build.rolldownOptions.output.keepNames: true`.
- `ANALYZE=true npm run build` produces a working treemap at `dist/stats.html`.
- A vanilla `npm run build` produces sourcemap-uploadable artifacts and passes the Phase 15 sourcemap-names guard.
- The D-09 production-trap aborts before any artifact is written when both env vars are set.
- `npm run lint && npm run test` exit 0; no regressions in the existing test suite.
</success_criteria>

<output>
Create `.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-01-SUMMARY.md` documenting: which scripts/devDeps landed; the exact wording of the D-09 throw message; the four verification invocations and their actual exit codes; any deviations from the planner instructions; whether the sourcemap-name allowlist still passes; the file-size of `dist/stats.html` from the analyze run. List `requirements-completed: [PERF-01]` in frontmatter.
</output>
</content>
</invoke>