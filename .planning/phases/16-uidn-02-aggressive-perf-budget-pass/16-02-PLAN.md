---
phase: 16
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .planning/closure/v1.3-bundle-audit-pre.html
requirements: [PERF-02]
autonomous: true
must_haves:
  truths:
    - "Pre-change bundle audit baseline is committed under `.planning/closure/` and shows `posthog-js` in the main critical-path chunk"
    - "The baseline is captured BEFORE PERF-03's PostHog lazy-load lands, so the PERF-03/PERF-04 delta is measurable against this artifact"
  artifacts:
    - path: ".planning/closure/v1.3-bundle-audit-pre.html"
      provides: "Pre-change treemap; reproducible from `git log` via the PERF-01 build:analyze script"
  key_links:
    - from: ".planning/closure/v1.3-bundle-audit-pre.html"
      to: ".planning/closure/OBSV-02-bundle-delta.md"
      via: "sibling closure-evidence convention"
      pattern: "v1\\.3-bundle-audit-pre\\.html"
---

<objective>
Capture the pre-change bundle baseline by running `ANALYZE=true npm run build` against the pre-PERF-03 tree, copy `dist/stats.html` to `.planning/closure/v1.3-bundle-audit-pre.html`, and commit it as evidence. This baseline is the pre-image PERF-03/PERF-04 deltas are measured against — it MUST land BEFORE PERF-03's lazy-load conversion (per RESEARCH § Cross-requirement sequencing: PERF-02 must capture the baseline before PERF-03 lands).

Purpose: PERF-03's headline claim is "~180-200 KB removed from the main critical-path chunk." That delta is only verifiable if the pre-image is captured at this exact point in history.

Output: A single committed HTML evidence file under `.planning/closure/` showing the pre-PERF-03 treemap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-CONTEXT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-RESEARCH.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-PATTERNS.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-VALIDATION.md

@.planning/closure/OBSV-02-bundle-delta.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run analyze build against pre-PERF-03 tree and capture baseline (PERF-02)</name>
  <files>.planning/closure/v1.3-bundle-audit-pre.html</files>
  <action>
    Confirm the working tree is in its post-PERF-01 / pre-PERF-03 state — i.e., the visualizer plugin is wired into `vite.config.ts` (plan 01 has shipped), but `src/main.tsx` still statically imports `PostHogProvider` from `posthog-js/react` at line 5, the `<PostHogProvider client={posthog}>` wrap at lines 95-99 is still present, and `src/components/PostHogGate.tsx` does NOT yet exist. Verify with: `grep -n "PostHogProvider" src/main.tsx | head -3` (must show line 5 and line 95 references); `test ! -e src/components/PostHogGate.tsx`. If either check fails, STOP — the working tree has drifted ahead and the baseline would no longer be pre-PERF-03.

    Then run: `rm -rf dist && ANALYZE=true npm run build`. Expect exit 0; expect `dist/stats.html` to be emitted (size typically 500 KB - 2 MB per RESEARCH § Patterns > Gotcha). Then copy: `cp dist/stats.html .planning/closure/v1.3-bundle-audit-pre.html`.

    **Treemap visual inspection is the binding done criterion** (NOT a `grep` against the compressed HTML — visualizer HTML is gzip-base64 embedded data and `grep` against it is unreliable). Open `.planning/closure/v1.3-bundle-audit-pre.html` in a browser locally and visually confirm:
    - `posthog-js/dist/module.full.js` (or an equivalent posthog chunk-content label) sits inside the MAIN entry chunk, NOT in a separate split chunk.
    - approximate unminified size of the posthog tile is captured for the SUMMARY note.
    - presence/absence of supabase-js / sentry-replay as auto-split chunks is captured (RESEARCH § D-04 notes these are expected to be auto-split today).

    Capture a one-paragraph SUMMARY note describing the visual observation — this is the canonical evidence record. The `.html` file itself is the reproducible artifact under git; the visual observation locks in what the treemap shows at this commit.

    Project conventions: this is a `.planning/closure/` evidence artifact — committed to history per the `OBSV-02-bundle-delta.md` placement analog. Large HTML files in `.planning/closure/` are precedented (see existing artifacts under that directory).
  </action>
  <verify>
    <automated>set -e; grep -q "PostHogProvider" src/main.tsx && test ! -e src/components/PostHogGate.tsx && rm -rf dist && ANALYZE=true npm run build && test -f dist/stats.html && cp dist/stats.html .planning/closure/v1.3-bundle-audit-pre.html && test -f .planning/closure/v1.3-bundle-audit-pre.html && echo BASELINE_CAPTURED_OK</automated>
  </verify>
  <done>
    1. `.planning/closure/v1.3-bundle-audit-pre.html` exists (file-existence assertion in `<verify>`).
    2. Visual treemap inspection (out-of-band) has been performed AND the one-paragraph observation is recorded in SUMMARY.md — naming which chunk holds posthog-js, the approximate unminified bytes, and which auto-split chunks (supabase-js / sentry-replay) are present today.
    3. The pre-state checks (PostHogProvider still statically imported; PostHogGate file absent) both pass before the analyze build runs.
    4. The build exits 0 with no errors.
    5. `git status` shows the new file under `.planning/closure/` ready to commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build environment → committed evidence file | The committed HTML is reproducible from `git log` + `npm run build:analyze`; integrity comes from git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information disclosure | `.planning/closure/v1.3-bundle-audit-pre.html` reveals bundle composition publicly via the repo | accept | Repository is already public on GitHub; bundle composition is observable from any deployed `.js` artifact regardless. No new information surface. |
| T-16-05 | Repudiation | Future PERF-03 verifier disputes the size delta claim | mitigate | Committed evidence under `.planning/closure/` is reproducible from `git log` — the v1.3-bundle-audit-pre.html is the canonical pre-image. Plan 03 captures the post-image during its verification. |

Plan 02 is a read-only / evidence-capture plan. No new code or runtime surface. Inherits T-16-01..T-16-03 controls from plan 01 (already shipped).
</threat_model>

<verification>
1. `.planning/closure/v1.3-bundle-audit-pre.html` exists (binary file-existence check in `<verify>`).
2. Treemap visual inspection (out-of-band, recorded in SUMMARY): opens in a browser to a navigable rollup-plugin-visualizer treemap; `posthog-js` is visible in the main entry chunk (NOT split into a separate chunk).
3. The build that produced it ran against the post-plan-01 / pre-plan-03 tree (verified by the action's pre-checks).
4. `git diff --stat` shows exactly one new file under `.planning/closure/`.
5. No edits made to any source file or `vite.config.ts`.
</verification>

<success_criteria>
- A committed HTML treemap exists at `.planning/closure/v1.3-bundle-audit-pre.html`.
- Visual inspection of the treemap (recorded in SUMMARY) confirms `posthog-js` resides in the main critical-path chunk (the pre-image for PERF-03's delta claim).
- No source code mutated by this plan.
</success_criteria>

<output>
Create `.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-02-SUMMARY.md` documenting: the SHA of HEAD when the baseline was captured (verifying pre-PERF-03 state); the byte size of `dist/stats.html` and of the copied `.planning/closure/v1.3-bundle-audit-pre.html`; the one-paragraph treemap visual-observation (which chunk holds posthog-js + its approximate unminified bytes + which auto-split chunks are present today — supabase-js, sentry-replay per D-04). List `requirements-completed: [PERF-02]` in frontmatter.
</output>
