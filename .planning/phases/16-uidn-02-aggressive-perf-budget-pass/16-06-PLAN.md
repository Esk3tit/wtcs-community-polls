---
phase: 16
plan: 06
type: execute
wave: 5
depends_on: ["16-03", "16-05"]
files_modified:
  - src/main.tsx
  - src/components/layout/Navbar.tsx
  - src/components/layout/MobileNav.tsx
requirements: [PERF-06]
autonomous: false  # Task 3 runs the full Playwright e2e suite (needs build+preview server) AND requires an operator hover-smoke confirmation; not safe to auto-complete
must_haves:
  truths:
    - "`createRouter({ routeTree, defaultPreload: 'intent' })` is wired in `src/main.tsx` — hover/focus on any `<Link>` begins preloading the destination route chunk"
    - "EVERY layout `/admin` `<Link>` carries `preload={false}` — both the desktop `Navbar.tsx` link AND the `MobileNav.tsx` link — so AdminGuard's `beforeLoad` no longer fires on hover/touch on either surface (V4 Access Control boundary preserved per Pitfall 6). The mobile link is in scope because `defaultPreload: 'intent'` is app-wide; leaving it un-opted-out would reintroduce the hover-redirect leak on the mobile path."
    - "Existing explicit `preload=\"intent\"` on Logo / Topics / Archive links is preserved (defensive belt-and-braces per UI-SPEC § Route Lazy-Load Behavior) — these are now redundant with the router-level default but kept intentionally; the tiny markup-debt is accepted (LOW review finding) in exchange for self-documenting per-link intent and resilience if the router default is ever changed"
    - "Phase 15 invariants (Sentry boundary chain, keepNames allowlist) untouched"
  artifacts:
    - path: "src/main.tsx"
      provides: "Router instantiated with `defaultPreload: 'intent'` — applies to all `<Link>` navigation app-wide"
      contains: "defaultPreload: 'intent'"
    - path: "src/components/layout/Navbar.tsx"
      provides: "Desktop Admin `<Link>` explicitly opts OUT of preload via `preload={false}`; comment WHY-only"
      contains: "preload={false}"
    - path: "src/components/layout/MobileNav.tsx"
      provides: "Mobile Admin `<Link to=\"/admin\">` explicitly opts OUT of preload via `preload={false}` (second `/admin` link; inherits the app-wide `defaultPreload: 'intent'` without this opt-out); comment WHY-only"
      contains: "preload={false}"
  key_links:
    - from: "src/main.tsx createRouter"
      to: "TanStack Router preload engine"
      via: "defaultPreload: 'intent' option"
      pattern: "defaultPreload"
    - from: "Navbar Admin <Link>"
      to: "AdminGuard beforeLoad (admin route)"
      via: "explicit preload={false} prevents hover-triggered guard fire (Pitfall 6)"
      pattern: "preload={false}"
---

<objective>
Add `defaultPreload: 'intent'` to the `createRouter` call in `src/main.tsx` so all `<Link>` navigation app-wide begins preloading its destination chunk on hover/focus. Companion change: add `preload={false}` to the Admin `<Link>` in `src/components/layout/Navbar.tsx` so hover does not trigger AdminGuard's `beforeLoad` redirect for non-admin users — preserving the V4 Access Control boundary that existed pre-Phase-16 via the absence-of-preload (Pitfall 6).

Purpose: One-line config change for app-wide hover-preloading + one-line per-Link explicit opt-out to preserve the existing admin-route safety. Net UX: faster route navigation; no security regression.

Output: Two files edited, each with a single-line addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-CONTEXT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-RESEARCH.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-PATTERNS.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-UI-SPEC.md

@src/main.tsx
@src/components/layout/Navbar.tsx

<interfaces>
<!-- After plan 03 lands, src/main.tsx line 42 still reads `const router = createRouter({ routeTree })` -->
<!-- (plan 03 intentionally LEFT createRouter unchanged so PERF-06 lives atomically here per Cross-requirement sequencing). -->

main.tsx line 42 (current): `const router = createRouter({ routeTree })`
main.tsx line 42 (post-Phase-16): `const router = createRouter({ routeTree, defaultPreload: 'intent' })`

Navbar.tsx lines 58-67 (current Admin Link block — Admin Link has NO preload attribute today;
the absence is what currently opts it out under the default `preload: 'off'`):
  ```
  {isAdmin && (
    // No preload — AdminGuard beforeLoad would redirect non-admins on hover. Per-link opt-in only.
    <Link
      to="/admin"
      className="text-sm text-muted-foreground hover:text-foreground transition-colors"
      activeProps={{ className: 'text-foreground' }}
    >
      Admin
    </Link>
  )}
  ```
After PERF-06: add `preload={false}` to the Admin <Link> so the explicit opt-out preserves the current behavior under the new router-level `defaultPreload: 'intent'`. The existing inline comment stays but is extended one phrase to mention the explicit `preload={false}` override.

Other Navbar <Link>s (Logo line 26-37; Topics line 42-49; Archive line 50-57) already have explicit `preload="intent"` — keep them per UI-SPEC § Route Lazy-Load Behavior (defensive belt-and-braces, no harm).

MobileNav.tsx lines 54-65 (SECOND Admin Link block — the mobile/sheet nav; Admin Link has NO preload
attribute today, same as Navbar, so it currently opts out under the default `preload: 'off'`):
  ```
  {isAdmin && (
    // No preload — AdminGuard beforeLoad would redirect non-admins on hover. Per-link opt-in only.
    <SheetClose asChild>
      <Link
        to="/admin"
        className="py-3 text-sm text-foreground hover:text-foreground/80 transition-colors"
        activeProps={{ className: 'font-medium' }}
      >
        Admin
      </Link>
    </SheetClose>
  )}
  ```
This is a SECOND `/admin` `<Link>` (distinct from Navbar's). Because `defaultPreload: 'intent'` (Task 1)
is router-level and app-wide, this mobile link ALSO inherits `'intent'` after Phase 16 — so it needs the
SAME explicit `preload={false}` opt-out as the desktop link, or the Pitfall-6 hover/touch-redirect leak
returns on the mobile path. MobileNav's Topics (lines 35-42) and Archive (lines 45-52) links already
carry `preload="intent"` — leave them as-is (belt-and-braces, same as Navbar).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `defaultPreload: 'intent'` to createRouter in `src/main.tsx` (PERF-06 router-level config)</name>
  <files>src/main.tsx</files>
  <action>
    Edit `src/main.tsx`. ONE-line change at line 42: replace `const router = createRouter({ routeTree })` with `const router = createRouter({ routeTree, defaultPreload: 'intent' })`. This is the sole edit in this task.

    Verify post-plan-03 state still holds before editing: line 42 should currently read `const router = createRouter({ routeTree })`; the `<ConsentProvider><PostHogGate><RouterProvider router={router} /></PostHogGate></ConsentProvider>` block should be intact (Phase 16 plan 03 lands first). If the state has drifted (e.g. the createRouter line is already changed), STOP — something else has moved.

    DO NOT touch: Sentry.init (lines 22-38), `taggedHandler` (lines 58-69), `createRoot` handlers (lines 71-75), `<Sentry.ErrorBoundary>` (lines 77-93). These are Phase 15 invariants.

    Project conventions: TypeScript strict; ESM; `@/` alias; WHY-only comments; no plan/phase IDs in source. No new comment needed for this one-line change — `defaultPreload: 'intent'` is a self-explanatory TanStack Router config option.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c "grep -q \"defaultPreload: 'intent'\" src/main.tsx && grep -q \"createRouter({ routeTree, defaultPreload: 'intent' })\" src/main.tsx && npm run lint && npm run build 2>&1 | tail -5"</automated>
  </verify>
  <done>
    1. `src/main.tsx` line 42 (or current equivalent post-plan-03) reads `const router = createRouter({ routeTree, defaultPreload: 'intent' })`.
    2. No other lines in `main.tsx` were touched — Sentry / boundary / ConsentProvider+PostHogGate wiring intact.
    3. `npm run lint && npm run build` exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `preload={false}` to EVERY layout Admin `<Link>` — desktop Navbar AND MobileNav (PERF-06 Pitfall 6 mitigation)</name>
  <files>src/components/layout/Navbar.tsx, src/components/layout/MobileNav.tsx</files>
  <action>
    Edit `src/components/layout/Navbar.tsx`. Locate the Admin `<Link to="/admin">` block at lines 58-67 (the conditional `{isAdmin && (...)}` block — confirmed present 2026-05-27). Add a new prop `preload={false}` to the `<Link>` element. Place it on its own JSX line for clarity:
    ```
    <Link
      to="/admin"
      preload={false}
      className="text-sm text-muted-foreground hover:text-foreground transition-colors"
      activeProps={{ className: 'text-foreground' }}
    >
      Admin
    </Link>
    ```
    The existing inline comment at line 59 (`// No preload — AdminGuard beforeLoad would redirect non-admins on hover. Per-link opt-in only.`) is the load-bearing WHY context — KEEP and tighten it to mention that `preload={false}` is now the explicit override (since the router-level default flipped from off → intent). REQUIRED updated wording: `// Explicit preload={false} — router default is now 'intent', but AdminGuard beforeLoad would redirect non-admins on hover. Hover-redirect leaks the admin route's existence = security leak.` This makes the security rationale unmistakable so a future maintainer doing a preload-cleanup pass does NOT remove `preload={false}`. CRITICAL project rule (CLAUDE.md + MEMORY): the comment must be WHY-only and MUST NOT cite any plan/research/phase ID — do NOT write "Pitfall 6" or any "PERF-0x"/"Phase N" token in the source comment. The security WHY ("hover-redirect leaks the admin route = security leak") is the durable rationale; the "Pitfall 6" cross-ref belongs in the commit message / SUMMARY, not in `src/`.

    DO NOT touch: the outer Logo `<Link>` (lines 26-37, has `preload="intent"`), Topics `<Link>` (lines 42-49, has `preload="intent"`), Archive `<Link>` (lines 50-57, has `preload="intent"`). All three keep their existing explicit `preload="intent"` — defensive belt-and-braces per UI-SPEC § Route Lazy-Load Behavior (`Existing explicit preload="intent" on individual <Link> ... NOT removed`). The avatar `<img>` at lines 103-107 is unrelated.

    If plan 05 has already landed and added the `<picture>` wrap, leave that wrap alone — this task only touches the Admin `<Link>` block.

    **Then edit `src/components/layout/MobileNav.tsx` — the SECOND `/admin` `<Link>`.** Locate the Admin `<Link to="/admin">` inside the `{isAdmin && (...)}` block at lines 54-65 (wrapped in `<SheetClose asChild>`; confirmed present 2026-05-27, with NO `preload` attribute today). Add `preload={false}` to that `<Link>` on its own JSX line:
    ```
    <SheetClose asChild>
      <Link
        to="/admin"
        preload={false}
        className="py-3 text-sm text-foreground hover:text-foreground/80 transition-colors"
        activeProps={{ className: 'font-medium' }}
      >
        Admin
      </Link>
    </SheetClose>
    ```
    Tighten the existing line-55 comment (`// No preload — AdminGuard beforeLoad would redirect non-admins on hover. Per-link opt-in only.`) to the SAME security-WHY wording used for the desktop link: `// Explicit preload={false} — router default is now 'intent', but AdminGuard beforeLoad would redirect non-admins on hover. Hover-redirect leaks the admin route's existence = security leak.` This is REQUIRED because `defaultPreload: 'intent'` (Task 1) is app-wide and this mobile link is a SECOND `/admin` link — without the explicit opt-out it inherits `'intent'` and reintroduces the Pitfall-6 hover/touch-redirect leak on the mobile path. WHY-only, NO plan/Pitfall/phase IDs in the comment (CLAUDE.md + MEMORY rule).
    DO NOT touch MobileNav's Topics `<Link>` (lines 35-42, `preload="intent"`) or Archive `<Link>` (lines 45-52, `preload="intent"`) — leave their explicit `preload="intent"` as-is (belt-and-braces, same as Navbar).

    Project conventions: PascalCase `.tsx`; named exports; `@/` alias; WHY-only comments; no plan/phase IDs in any new or updated comment.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c 'grep -q "preload={false}" src/components/layout/Navbar.tsx && grep -q "preload={false}" src/components/layout/MobileNav.tsx && grep -rlE "to=\"/admin\"" src/components/layout/ | while read f; do grep -A3 "to=\"/admin\"" "$f" | grep -q "preload={false}" || { echo "MISSING preload={false} near /admin Link in $f"; exit 1; }; done && npm run lint && npm run test -- Navbar 2>&1 | tail -5'</automated>
  </verify>
  <done>
    1. `src/components/layout/Navbar.tsx` Admin `<Link to="/admin">` carries `preload={false}`.
    2. `src/components/layout/MobileNav.tsx` Admin `<Link to="/admin">` ALSO carries `preload={false}` — every layout `/admin` link is opted out (the automated verify loops over all layout files containing `to="/admin"` and fails if any lacks `preload={false}`).
    3. Both Admin links' inline comments carry the WHY-only security rationale (router default flipped to 'intent'; hover-redirect leaks admin route existence), with NO plan/Pitfall/phase IDs.
    4. Other Navbar AND MobileNav `<Link>` elements (Logo / Topics / Archive) still have their existing `preload="intent"` — unchanged.
    5. `npm run lint && npm run test` exit 0; Navbar snapshot (if present) updated with minimal diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full e2e suite (enforced, not echoed) + operator hover-no-preload smoke on Admin link</name>
  <files>(none — verification only)</files>
  <action>
    Run the existing Playwright suite for real: `npm run e2e`. This is now ENFORCED in the automated verify below (the original plan only echoed a reminder, which the cross-AI review flagged — a `autonomous: true` plan that depends on an un-run e2e check is a false-positive risk). All pre-existing specs must pass — `defaultPreload: 'intent'` is a behavior-additive change (hover triggers earlier preload) and should not break any navigation assertion. If a spec fails because it relied on absent preload (unlikely), document the failure mode in SUMMARY and triage before proceeding. The Playwright `webServer` config (`e2e/playwright.config.ts:34-43`) builds + serves the preview automatically when run locally (not CI), so `npm run e2e` is self-contained.

    Operator hover-smoke (this plan is `autonomous: false` — the operator performs this and confirms): `npm run preview` against the production build, then **sign in with a Discord account that has the admin role** — the Admin `<Link to="/admin">` is gated behind `{isAdmin && (...)}` in `Navbar.tsx`, so it is NOT rendered for anonymous or non-admin users and there would be nothing to hover. (If you cannot sign in as an admin in the preview environment, the hover-smoke cannot be performed against the live link — fall back to confirming the static `preload={false}` grep from Task 2 and note in SUMMARY that the runtime hover-smoke was skipped for lack of an admin session.) Once the Admin link is visible, open `/` in a browser with DevTools Network tab + filter set to `admin`. Hover the Admin link in the desktop nav for ~2 seconds. Expected: ZERO network requests fire matching `admin` — the `preload={false}` explicitly opts out of hover-triggered preload. As a positive control, hover the Topics or Archive link in the same tab — those should trigger a route-chunk preload (one network request per hover). Record the observation in SUMMARY (including whether you were signed in as admin).

    Run `node scripts/verify-sourcemap-names.mjs` against a fresh build to confirm Phase 15 invariants hold.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c 'rm -rf dist && npm run build && node scripts/verify-sourcemap-names.mjs && npm run lint && npm run test 2>&1 | tail -5 && npm run e2e 2>&1 | tail -20'</automated>
  </verify>
  <done>
    1. `npm run e2e` exits 0 (RUN as part of the automated verify, not merely noted) — all pre-existing Playwright specs pass under the new `defaultPreload: 'intent'` config.
    2. Operator hover-smoke confirmed (operator signed in as admin so the `isAdmin`-gated Admin link renders): hovering Admin link in production preview triggers ZERO admin-chunk network requests; hovering Topics/Archive triggers a chunk preload (positive control). Recorded in SUMMARY (including admin-session status; if no admin session was available, SUMMARY notes the runtime hover-smoke was skipped and the static `preload={false}` grep stands in).
    3. `npm run build && node scripts/verify-sourcemap-names.mjs` exits 0; Phase 15 invariants intact.
    4. SUMMARY captures the e2e exit code + operator hover-smoke observations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User hover on Admin link → AdminGuard `beforeLoad` (V4 Access Control) | `defaultPreload: 'intent'` flips the implicit `<Link>` preload default from off → intent. Without the per-link `preload={false}` opt-out, hovering Admin would trigger the AdminGuard's beforeLoad redirect for non-admin users — a hover-redirect security leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-17 | Information disclosure / Elevation of Privilege | Admin Link hover-redirect leak — non-admin users see brief redirect flash when hovering an Admin link (Pitfall 6) | mitigate | `preload={false}` on BOTH layout Admin Links (desktop `Navbar.tsx` AND `MobileNav.tsx`) explicitly opts out of hover/touch-preload. Task 2 verify loops over EVERY layout file containing `to="/admin"` and fails if any lacks `preload={false}` — so a missed surface (the original gap on MobileNav) is caught automatically. Task 3 manual smoke confirms zero admin-chunk network requests on Admin hover. UI-SPEC § Route Lazy-Load Behavior mandates this explicit opt-out. |
| T-16-18 | Tampering | A future PR removes the `preload={false}` from an Admin Link → hover-redirect leak returns | accept (residual) | The inline comment on BOTH links documents the security WHY; PR review is the primary control. The Task 2 all-layout-links loop also catches a regression on either surface. A lint rule could enforce per-link preload audit, but is out of scope for v1.3. |
| T-16-19 | DoS (perceived) | `defaultPreload: 'intent'` causes excessive network requests on UI hover-heavy interactions | accept | TanStack Router's preload engine deduplicates and respects in-flight requests; the per-chunk cost is paid once per route. Phase 16's PERF-04 manualChunks ensures vendor chunks are cache-stable. Net effect is positive — preload is a perf feature. |

Inherits all prior-plan controls (D-09 throw, GDPR lazy gate, sourcemap upload integrity, keepNames).
</threat_model>

<verification>
1. `src/main.tsx` `createRouter({ routeTree, defaultPreload: 'intent' })` — grep-verified.
2. EVERY layout `/admin` `<Link>` carries `preload={false}` — both `src/components/layout/Navbar.tsx` AND `src/components/layout/MobileNav.tsx` — verified by the Task 2 loop over all layout files containing `to="/admin"`.
3. `npm run lint && npm run test && npm run build` exit 0.
4. Phase 15 sourcemap-names allowlist passes.
5. `npm run e2e` (Playwright) exits 0.
6. Manual smoke (operator signed in as admin, since the Admin link is `isAdmin`-gated) confirms hovering the desktop Admin link triggers zero admin-route network requests in production preview. The mobile Admin link (in the `<Sheet>` nav) carries the same `preload={false}`; the static grep loop (Task 2) is the load-bearing proof for the mobile surface since hover-preload is desktop-pointer-driven.
</verification>

<success_criteria>
- Router config carries `defaultPreload: 'intent'`.
- BOTH layout Admin Links (desktop Navbar + MobileNav) explicitly opt out via `preload={false}` — Pitfall 6 (V4 Access Control hover-redirect leak) mitigated on every nav surface, not just desktop.
- All existing e2e specs pass under the new router config.
- Phase 15 + plan 01 D-09 + plan 03 GDPR controls all preserved.
</success_criteria>

<output>
Create `.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-06-SUMMARY.md` documenting: the exact one-line change in `main.tsx`; the exact one-line+comment change in `Navbar.tsx` AND in `MobileNav.tsx` (both layout `/admin` links now carry `preload={false}`; confirm both comments are WHY-only with no plan/Pitfall/phase IDs); confirmation that the Task 2 all-layout-`/admin`-links loop passed (no surface left un-opted-out); the `npm run e2e` exit code (RUN as part of the enforced verify, not deferred); operator hover-smoke observation (zero admin-chunk requests on Admin hover; positive control preload fires on Topics/Archive); a note that the redundant Logo/Topics/Archive `preload="intent"` attributes were intentionally retained (accepted markup-debt). List `requirements-completed: [PERF-06]` in frontmatter.
</output>
</content>
</invoke>