---
phase: 16
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - src/lib/posthog-facade.ts
  - src/components/PostHogProviderInner.tsx
  - src/components/PostHogGate.tsx
  - src/main.tsx
  - src/contexts/AuthContext.tsx
  - src/contexts/ConsentContext.tsx
  - src/__tests__/contexts/AuthContext.test.tsx
  - src/__tests__/contexts/ConsentContext.test.tsx
  - src/__tests__/lib/posthog-facade.test.ts
  - src/__tests__/components/PostHogGate.test.tsx
  - e2e/tests/posthog-consent-gate.spec.ts
requirements: [PERF-03]
autonomous: false  # MANDATORY Playwright runtime gate (Task 6) requires a built+preview server; operator confirms the e2e gate result
must_haves:
  truths:
    - "D-01: lazy PostHog initialization fires only when `ConsentContext.state === 'allow'`; `<PostHogGate>` ALWAYS renders `{children}` directly (outside any Suspense boundary) and mounts the lazy loader as a sibling — declining/undecided users never download `posthog-js` (Tasks 3+4)"
    - "D-02: thin `src/lib/posthog-facade.ts` namespace-object exposes a synchronous queue-and-replay `identify`/`reset` API, statically imported by AuthContext (Tasks 1+2+5)"
    - "D-03: `initPostHog()` retains `opt_out_capturing_by_default: true` / `opt_out_persistence_by_default: true`; GDPR consent semantic unchanged — plan 03 does NOT touch `src/lib/posthog.ts`"
    - "MANDATORY runtime gate: a Playwright check against `npm run preview` (production build) proves the app shell loads with ZERO `posthog-js` network requests before the user clicks Allow — this empirical check is a required PERF-03 closure gate, not optional (it is the only proof the bundler actually keeps posthog-js out of the initial network payload; unit/component tests prove source intent only)"
    - "Children NEVER blank or remount during the lazy-import window: `{children}` (the router) is NOT a descendant of the suspending lazy component — the lazy PostHog loader is a side-effect sibling that renders `null`, so `<Suspense fallback={null}>` replacing it with `null` has zero visible effect on the app"
    - "When consent state flips to `'allow'`, the lazy PostHog module loads, calls `initPostHog()` + `posthog.setClient(client)`, and the facade queue drains so prior `identify`/`reset`/`opt_in_capturing`/`opt_out_capturing` calls reach the real client"
    - "AuthContext and ConsentContext continue to call `posthog.identify`/`reset`/`opt_in_capturing`/`opt_out_capturing` exactly as today — only the import path changes"
    - "Phase 6 GDPR opt-IN semantics preserved: `opt_out_capturing_by_default: true` remains in `initPostHog()`; declining users never download `posthog-js`"
    - "Phase 15 invariants preserved: `boundary: app-root` tag chain in `main.tsx`, ErrorBoundary placement, and the 7-name keepNames allowlist all unchanged"
  artifacts:
    - path: "src/lib/posthog-facade.ts"
      provides: "Statically-importable namespace-object surface (identify, reset, opt_in_capturing, opt_out_capturing, setClient) with queue-then-flush semantics; uses `import type` only so the facade contributes ZERO bytes of posthog-js to the critical-path chunk"
      contains: "export const posthog"
    - path: "src/components/PostHogProviderInner.tsx"
      provides: "Dynamically-imported side-effect loader that statically imports `initPostHog` from `@/lib/posthog` (the SINGLE runtime import site for posthog-js, transitively), calls `initPostHog()` once at module scope, calls `posthog.setClient(client)` from the facade, and renders `null`. It does NOT wrap children — no component in this app consumes `usePostHog()`, so the React context provider is unnecessary; mounting it as a child-wrapping Suspense boundary is what caused the HIGH review finding (children blanking during import). Renders `null` so Suspense fallback has no visible effect."
      contains: "initPostHog"
    - path: "src/components/PostHogGate.tsx"
      provides: "Consent gate that ALWAYS renders `{children}` directly, and — only when state === 'allow' — ALSO renders `<Suspense fallback={null}><LazyPostHogLoader /></Suspense>` as a SIBLING (children are NOT nested inside the suspending component). Structure: `return (<>{children}{state === 'allow' && <Suspense fallback={null}><LazyPostHogLoader /></Suspense>}</>)`. Because children live outside the Suspense boundary, the lazy import never blanks or remounts the router."
      contains: "PostHogGate"
    - path: "src/__tests__/lib/posthog-facade.test.ts"
      provides: "Wave 0 unit test for queue-before-load + forward-after-load semantics via the namespace-object surface"
    - path: "src/__tests__/components/PostHogGate.test.tsx"
      provides: "Wave 0 component test for always-render-children + conditional sibling-loader-mount under mocked useConsent; includes the synchronous-children regression guard for the HIGH blanking finding"
    - path: "e2e/tests/posthog-consent-gate.spec.ts"
      provides: "MANDATORY runtime gate (Playwright against npm run preview): asserts ZERO network requests matching /posthog/ before clicking Allow, and that a posthog request DOES fire after Allow. This is the only empirical proof the production bundle keeps posthog-js out of the initial network payload — required for PERF-03 closure (not optional)."
      contains: "posthog"
  key_links:
    - from: "src/main.tsx"
      to: "src/components/PostHogGate.tsx"
      via: "<ConsentProvider><PostHogGate>{children}</PostHogGate></ConsentProvider> at the provider-tree root"
      pattern: "<PostHogGate>"
    - from: "src/components/PostHogGate.tsx"
      to: "src/components/PostHogProviderInner.tsx"
      via: "React.lazy dynamic import with .then(m => ({ default: m.PostHogProviderInner })) destructure; rendered as a sibling of {children}, NOT a wrapper"
      pattern: "lazy.*PostHogProviderInner"
    - from: "src/components/PostHogProviderInner.tsx"
      to: "src/lib/posthog-facade.ts"
      via: "module-scope posthog.setClient(client) call after initPostHog() returns"
      pattern: "posthog.setClient"
    - from: "src/contexts/AuthContext.tsx + src/contexts/ConsentContext.tsx"
      to: "src/lib/posthog-facade.ts"
      via: "named-import of the namespace object: `import { posthog } from '@/lib/posthog-facade'`"
      pattern: "from '@/lib/posthog-facade'"
---

<objective>
Convert PostHog from a synchronous critical-path-static-import to a consent-gated lazy-loaded side-effect loader, splitting the work into a thin static facade (`posthog-facade.ts`) and a dynamically-imported loader (`PostHogProviderInner.tsx`) mounted by `<PostHogGate>`. `<PostHogGate>` ALWAYS renders `{children}` directly; only on consent `'allow'` does it ALSO mount the lazy loader as a SIBLING (inside `<Suspense fallback={null}>`), which fetches the posthog-js chunk, calls `initPostHog()`, and bridges the client into the facade. Because `{children}` (the router) is NOT a descendant of the suspending component, the lazy import never blanks or remounts the app — a defect the original child-wrapping design would have introduced. Add the two Wave 0 unit/component tests that lock the queue-then-flush + always-render-children-and-conditionally-mount-loader semantics, plus the MANDATORY Playwright runtime gate proving zero posthog-js network requests before Allow.

Purpose: PERF-03's headline win — ~180-200 KB of `posthog-js` shifts off the main critical-path chunk; users who decline (or never decide) never download posthog-js. The facade is a namespace-object so AuthContext + ConsentContext call sites stay byte-identical (only the import path changes from `@/lib/posthog` to `@/lib/posthog-facade`).

Output: 3 new source files + 2 new test files (unit + component) + 1 new Playwright e2e spec + 3 edited source files. Phase 6 GDPR opt-IN semantics preserved verbatim; Phase 15 boundary-tag + keepNames invariants preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-CONTEXT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-RESEARCH.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-PATTERNS.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-UI-SPEC.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-VALIDATION.md

@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-02-SUMMARY.md

@src/main.tsx
@src/lib/posthog.ts
@src/contexts/AuthContext.tsx
@src/contexts/ConsentContext.tsx
@src/hooks/useConsent.ts
@src/routes/__root.tsx
@src/routes/[__smoke].tsx
@src/__tests__/setup/smoke.test.ts
@src/__tests__/contexts/ConsentContext.test.tsx
@src/__tests__/layout/Navbar.test.tsx

<interfaces>
<!-- Existing call-site shapes the facade must absorb without behavior change. -->
<!-- Source: AuthContext.tsx:6, 168, 181; ConsentContext.tsx:4, 55, 58 (verified 2026-05-27). -->

AuthContext.tsx:6      `import { posthog } from '@/lib/posthog'`
AuthContext.tsx:168    `posthog.identify(providerId)`        // inside useEffect, consent='allow' gated
AuthContext.tsx:181    `posthog.reset()`                      // inside signOut, fired before API call

ConsentContext.tsx:4   `import { posthog } from '@/lib/posthog'`
ConsentContext.tsx:55  `posthog.opt_in_capturing()`           // inside useEffect on state==='allow'
ConsentContext.tsx:58  `posthog.opt_out_capturing()`          // inside useEffect on state==='decline'

src/lib/posthog.ts:1   `import posthog from 'posthog-js'`     // default import of posthog-js (an object with .identify / .reset / ...)
src/lib/posthog.ts:40  `export { posthog }`                   // re-exports the posthog-js default object as a named binding

The fact that today `import { posthog } from '@/lib/posthog'` resolves to a namespace-shaped object (posthog-js's default export) is the reason the facade must mirror that exact surface — a namespace-object export — so the call-site syntax `posthog.identify(...)` stays byte-identical after the import-path swap.

src/main.tsx:5         `import { PostHogProvider } from 'posthog-js/react'`  // REMOVE; moves into Inner
src/main.tsx:6         `import { initPostHog } from '@/lib/posthog'`         // REMOVE; moves into Inner
src/main.tsx:40        `const posthog = initPostHog()`                       // REMOVE; moves into Inner
src/main.tsx:95-99     `<PostHogProvider client={posthog}>`                  // REPLACE with PostHogGate
                       `  <ConsentProvider>`                                  // INVERT: ConsentProvider moves OUTER
                       `    <RouterProvider router={router} />`               // stays innermost
                       `  </ConsentProvider>`
                       `</PostHogProvider>`

Phase 15 invariants in main.tsx that MUST NOT be touched: lines 22-38 (`Sentry.init`), lines 58-69 (`taggedHandler` definition + scope.setTag('boundary','app-root')), lines 71-75 (`createRoot` `onUncaughtError`/`onCaughtError`/`onRecoverableError` taggedHandler wiring), lines 77-93 (`<Sentry.ErrorBoundary>` with `beforeCapture` setting `boundary: app-root`, `onError` capturing with `tags: { boundary: 'app-root' }`).

The `createRouter` `defaultPreload: 'intent'` edit on line 42 belongs EXCLUSIVELY to plan 16-06 (PERF-06). Plan 03 must NOT touch line 42.

src/routes/[__smoke].tsx:13-17 — exact named-export lazy destructure analog to mirror in PostHogGate:
```
const RenderThrowSmoke = lazy(() =>
  import('@/components/debug/RenderThrowSmoke').then(m => ({
    default: m.RenderThrowSmoke,
  }))
)
```

Namespace-object facade shape — the exact public surface (per RESOLVED Q3 in RESEARCH § Open Questions):
```
// src/lib/posthog-facade.ts (target shape — implementation in Task 2)
export const posthog = {
  identify(id: string): void { /* if client: client.identify(id); else queue */ },
  reset(): void                { /* same pattern */ },
  opt_in_capturing(): void     { /* same pattern */ },
  opt_out_capturing(): void    { /* same pattern */ },
  setClient(c): void           { /* assign module-scope client; drain queue once */ },
}
```

src/lib/posthog.ts — existing initPostHog() ships `opt_out_capturing_by_default: true` and `opt_out_persistence_by_default: true`. D-03 mandates this stays verbatim — Phase 16 does not touch posthog.ts. NOTE: `src/lib/posthog.ts:1` statically imports `posthog-js` and `src/lib/posthog.ts:40` re-exports it. This means the runtime-import invariant is NOT "no module imports `posthog-js`" (posthog.ts always will) — it is "no module in the critical-path graph imports `@/lib/posthog` or `posthog-js` at runtime; the ONLY runtime importer is the lazily-loaded `PostHogProviderInner.tsx`." The facade (`@/lib/posthog-facade`) imports `posthog-js` via `import type` ONLY, so it carries zero runtime bytes.

PostHog React-consumer audit (verified 2026-05-27): the ANCHORED audit `grep -rnE "usePostHog\(|from 'posthog-js/react'|<PostHogProvider\b" src/` returns ONLY `src/main.tsx:5` (the `posthog-js/react` import) and `src/main.tsx:95` (the `<PostHogProvider client={posthog}>` open tag) — the references being removed/relocated by this plan (the closing `</PostHogProvider>` at `main.tsx:99` is not a consumer and is not matched by `<PostHogProvider\b`). NO component calls `usePostHog()`. The patterns are anchored on purpose — `usePostHog\(`, `from 'posthog-js/react'`, `<PostHogProvider\b` — so they do NOT substring-match the new `PostHogProviderInner` component (Task 4): `<PostHogProvider\b` does not match `<PostHogProviderInner` (`Inner` follows with no word boundary), and PostHogProviderInner's `import`/lazy-destructure lines contain no `from 'posthog-js/react'`. This is WHY `PostHogProviderInner` does not need to render `<PostHogProvider client={...}>{children}</PostHogProvider>`: there are no React-context consumers to provide for. Task 5 re-runs this anchored grep as a precheck and HALTS if any unexpected `usePostHog()` consumer has appeared since (which would require wrapping that consumer's subtree, reintroducing the blanking risk).

PostHogProviderInner target shape (side-effect loader, NOT a child wrapper):
```
// src/components/PostHogProviderInner.tsx (target shape — implementation in Task 4)
import { initPostHog } from '@/lib/posthog'
import { posthog } from '@/lib/posthog-facade'
// Module scope: runs exactly once when the lazy chunk first resolves.
const client = initPostHog()
posthog.setClient(client)        // bridge → drains the facade queue into the real client
export function PostHogProviderInner() { return null }  // side-effect only; renders nothing
```

PostHogGate target shape (children always rendered; loader is a sibling):
```
// src/components/PostHogGate.tsx (target shape — implementation in Task 4)
const LazyPostHogLoader = lazy(() =>
  import('@/components/PostHogProviderInner').then(m => ({ default: m.PostHogProviderInner }))
)
export function PostHogGate({ children }: { children: ReactNode }) {
  const { state } = useConsent()
  return (
    <>
      {children}
      {state === 'allow' && (
        <Suspense fallback={null}>
          <LazyPostHogLoader />
        </Suspense>
      )}
    </>
  )
}
```
The CRITICAL correctness property: `{children}` is a sibling of the `<Suspense>` boundary, never a descendant. When the lazy import is in-flight, Suspense replaces `<LazyPostHogLoader />` (which renders `null` anyway) with the `null` fallback — `{children}` (the router) is completely unaffected. This is the fix for the verified HIGH review finding that the original `<Suspense fallback={null}><LazyPostHogProviderInner>{children}</LazyPostHogProviderInner></Suspense>` form would blank/remount the entire router during the consent='allow' import window.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — write `posthog-facade.test.ts` (RED) before implementation</name>
  <files>src/__tests__/lib/posthog-facade.test.ts</files>
  <behavior>
    - Test 1 (queue-then-flush): With a freshly re-imported module (no `setClient` called), invoke `posthog.identify('user-1')` and `posthog.reset()` via the namespace object. The internal queue must hold both calls; the spy client must have received nothing yet. Then call `posthog.setClient(spy)`. After the call, `spy.identify` must have been called exactly once with `'user-1'`, `spy.reset` exactly once with no args, and the internal queue must be empty.
    - Test 2 (forward-after-load): With a fresh module re-import, call `posthog.setClient(spy)` immediately; then call `posthog.identify('user-2')`. `spy.identify` must have been called exactly once with `'user-2'`. The queue must remain empty (the call forwarded synchronously without queuing).
    - Test 3 (opt_in / opt_out routing): Mirror Test 2 for `posthog.opt_in_capturing()` and `posthog.opt_out_capturing()` — both must forward to the spy client.
    - Test 4 (per-test isolation): `vi.resetModules()` in `beforeEach`, then dynamic `await import('@/lib/posthog-facade')` per test (the facade's module-scope state must reset between tests).
    - Test 5 (queue depth cap — defensive): Before `setClient`, call `posthog.identify(...)` more than the queue cap (use a small documented cap, e.g. 50) times. After exceeding the cap, the queue must stop growing (drop newest, keep oldest, or vice versa — assert a fixed maximum `length`). This guards the Gemini-flagged "queue grows indefinitely if the lazy chunk fetch never resolves (transient CDN failure)" scenario. The exact drop policy is implementer's choice; the binding assertion is that the queue length never exceeds the documented cap.
  </behavior>
  <action>
    Create `src/__tests__/lib/posthog-facade.test.ts` mirroring the vitest skeleton from `src/__tests__/setup/smoke.test.ts:1-15` and the mock+reset pattern from `src/__tests__/contexts/ConsentContext.test.tsx:9-25`. Imports: `import { describe, it, expect, vi, beforeEach } from 'vitest'`. No need to `vi.mock('posthog-js')` — the test exercises the facade with a hand-built stub client passed to `setClient`, never invoking the real posthog-js. Build a `function makeStub()` returning an object with `identify: vi.fn()`, `reset: vi.fn()`, `opt_in_capturing: vi.fn()`, `opt_out_capturing: vi.fn()` (these match the four methods the facade forwards to). `beforeEach`: `vi.resetModules()` + `vi.clearAllMocks()` so the module-scope queue/client reset between tests.

    Inside each test, consume the facade through its namespace-object public surface: `const { posthog } = await import('@/lib/posthog-facade')`, then assert behavior on `posthog.identify(...)`, `posthog.reset()`, `posthog.opt_in_capturing()`, `posthog.opt_out_capturing()`, `posthog.setClient(stub)` — exactly as RESOLVED Q3 in RESEARCH.md prescribes.

    Run the file with `npm run test -- posthog-facade`. Tests MUST FAIL (RED) because `src/lib/posthog-facade.ts` does not yet exist — confirm the failure is "Cannot find module '@/lib/posthog-facade'" (or equivalent), not a logic miscompare. Commit the RED state before Task 2 lands.

    Project conventions: ESM only; `import type` for `posthog-js` types if used (verbatimModuleSyntax); no plan/phase IDs in test names; WHY-only comments. Use the `@/` alias for the dynamic import. File path: `src/__tests__/lib/posthog-facade.test.ts` (mirrors source path `src/lib/posthog-facade.ts`).
  </action>
  <verify>
    <automated>npm run test -- posthog-facade 2>&1 | grep -E '(FAIL|Cannot find module|posthog-facade)' | head -5</automated>
  </verify>
  <done>Test file exists at `src/__tests__/lib/posthog-facade.test.ts` with 3-4 tests covering queue-before-load, forward-after-load, opt-in/out routing, and per-test module reset — all consuming the facade as `const { posthog } = await import('@/lib/posthog-facade')`. Tests fail RED with module-not-found errors (or equivalent) — proving the test exercises the future implementation rather than a stale fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `src/lib/posthog-facade.ts` (GREEN) — namespace-object export</name>
  <files>src/lib/posthog-facade.ts</files>
  <behavior>
    - Module exports a single namespace-object binding `posthog` with five methods matching today's call sites in AuthContext + ConsentContext: `identify(id: string): void`, `reset(): void`, `opt_in_capturing(): void`, `opt_out_capturing(): void`, `setClient(c): void`.
    - When the module-scope `client` is null, each forwarder method pushes a thunk `(c) => c.method(...)` onto a module-scope FIFO queue.
    - When `client` is set, each forwarder method invokes it directly.
    - `setClient(c)` assigns the module-scope `client` reference and synchronously drains the queue: `while (queue.length) queue.shift()!(c)` — replays exactly once.
    - DEFENSIVE: the queue is bounded by a small documented cap (e.g. `const QUEUE_CAP = 50`). When `client` is null and the queue is already at `QUEUE_CAP`, the forwarder drops the call rather than pushing (prevents unbounded growth if the lazy chunk fetch never resolves — the Gemini-flagged transient-CDN-failure scenario). The cap is generous relative to the single-digit number of identify/reset/opt_* calls a real session makes, so it never trips in normal operation; it is purely a safety valve.
    - The module statically imports `posthog-js` types ONLY via `import type`; it contains ZERO runtime imports of `posthog-js`. This is the load-bearing constraint for Pitfall 4 (single static-import site for `posthog-js` lives ONLY in PostHogProviderInner.tsx).
  </behavior>
  <action>
    Create `src/lib/posthog-facade.ts`. Use the type-only import from `src/lib/posthog.ts:1` as the analog: `import type posthog from 'posthog-js'` — under `verbatimModuleSyntax: true` this is erased at build time, contributing nothing to the bundle. Define a `type Client = typeof posthog` for readability.

    Module-scope state (NOT exported): `let client: Client | null = null`, `const queue: Array<(c: Client) => void> = []`, and `const QUEUE_CAP = 50` (documented safety valve). Add a private helper `function enqueue(fn: (c: Client) => void): void { if (client) { fn(client); return } if (queue.length >= QUEUE_CAP) return; queue.push(fn) }` so the cap logic lives in one place and every forwarder routes through it.

    Export ONE binding — the namespace object — exactly as RESOLVED Q3 in RESEARCH § Open Questions prescribes, routing all four forwarders through `enqueue` (which forwards immediately if `client` is set, else queues up to the cap):
    ```
    export const posthog = {
      identify(id: string): void { enqueue((c) => c.identify(id)) },
      reset(): void              { enqueue((c) => c.reset()) },
      opt_in_capturing(): void   { enqueue((c) => c.opt_in_capturing()) },
      opt_out_capturing(): void  { enqueue((c) => c.opt_out_capturing()) },
      setClient(c: Client): void {
        client = c
        while (queue.length) queue.shift()!(c)
      },
    }
    ```

    Do NOT export four separate named functions. Do NOT use `export default`. The namespace-object shape is non-negotiable — it is what makes the AuthContext + ConsentContext import-path swap a one-line, byte-identical-call-site change.

    Add a single module-top WHY-only comment block: explain that the facade is statically importable so AuthContext / ConsentContext stay synchronous, and that the queue absorbs calls during the lazy-import window. DO NOT cite any plan or decision IDs in the source file (per CLAUDE.md "no review-round/phase-ID archaeology in src/").

    Re-run `npm run test -- posthog-facade`. Tests MUST PASS (GREEN). Tighten naming if needed but keep the five-method namespace-object surface exact.

    Project conventions: TypeScript strict (`noUnusedLocals`, `noUnusedParameters`); `import type` mandatory for `posthog-js`; ESM; named export only (no default); camelCase `.ts` filename; WHY-only comments.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c 'npm run test -- posthog-facade 2>&1 | tail -10; grep -q "export const posthog" src/lib/posthog-facade.ts; grep -q "import type" src/lib/posthog-facade.ts; ! grep -qE "^import .* from .posthog-js.$" src/lib/posthog-facade.ts'</automated>
  </verify>
  <done>
    1. `src/lib/posthog-facade.ts` exists with the single namespace-object `export const posthog = { identify, reset, opt_in_capturing, opt_out_capturing, setClient }`.
    2. `import type posthog from 'posthog-js'` is the ONLY mention of `posthog-js` (no runtime import).
    3. `npm run test -- posthog-facade` reports all tests passing (GREEN).
    4. `npm run lint` exits 0 against the new file.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 — write `PostHogGate.test.tsx` (RED) before implementation</name>
  <files>src/__tests__/components/PostHogGate.test.tsx</files>
  <behavior>
    The gate's contract is: ALWAYS render `{children}`; mount the lazy loader as a sibling ONLY when state === 'allow'. The loader is a side-effect component that renders `null`, so the test asserts the loader's SIDE EFFECT (the mocked module was imported/invoked) rather than visible DOM from the loader.
    - Test 1 (state='undecided'): Mock `useConsent` to return `{ state: 'undecided' }`. Render `<PostHogGate><div data-testid="children">CHILD</div></PostHogGate>`. `screen.getByTestId('children')` is present (children ALWAYS render). The mocked `PostHogProviderInner` side-effect spy was NOT invoked (loader not mounted).
    - Test 2 (state='decline'): Same as Test 1 but with `state: 'decline'`. Same outcome — children present, loader spy NOT invoked.
    - Test 3 (state='allow'): Mock `useConsent` to return `{ state: 'allow' }`. Render the same tree. `screen.getByTestId('children')` is present IMMEDIATELY and synchronously (children are NOT behind Suspense — this is the binding assertion that proves children never blank). Then `await` the loader side-effect spy being called (the lazy import resolves async even when mocked) — i.e. `await vi.waitFor(() => expect(loaderSpy).toHaveBeenCalled())`. The loader mock renders `null`, so there is NO `lazy-inner-mount` DOM node; the side-effect spy is the proof of mount.
    - Test 4 (children stay mounted across the import window — regression guard for the HIGH finding): With `state: 'allow'`, assert `screen.getByTestId('children')` is in the document on the FIRST synchronous render (before the loader spy resolves). This is the direct regression test for the verified HIGH finding (original child-wrapping design blanked children during import). Because children are a sibling of `<Suspense>`, not a descendant, they must be present synchronously.
  </behavior>
  <action>
    Create `src/__tests__/components/PostHogGate.test.tsx`. Imports: `import { describe, it, expect, vi } from 'vitest'`, `import { render, screen, waitFor } from '@testing-library/react'`. Mock `useConsent` using the pattern from `src/__tests__/layout/Navbar.test.tsx:4-7`:
    `const mockUseConsent = vi.fn()` then `vi.mock('@/hooks/useConsent', () => ({ useConsent: () => mockUseConsent() }))`. Mock `PostHogProviderInner` as a SIDE-EFFECT spy that renders null: `const loaderSpy = vi.fn()` then `vi.mock('@/components/PostHogProviderInner', () => ({ PostHogProviderInner: () => { loaderSpy(); return null } }))`. Per-test: set `mockUseConsent.mockReturnValue({ state: '...' })` before `render(<PostHogGate><div data-testid="children">CHILD</div></PostHogGate>)`. For the `state: 'allow'` cases, assert `screen.getByTestId('children')` synchronously first (regression guard), then `await waitFor(() => expect(loaderSpy).toHaveBeenCalled())` for the lazy-import resolution. `beforeEach`: `loaderSpy.mockClear()`.

    Run `npm run test -- PostHogGate`. Tests MUST FAIL (RED) — `src/components/PostHogGate.tsx` does not yet exist. Confirm the failure is module-not-found, not a logic miscompare.

    No `@tanstack/react-router` mock needed — PostHogGate does NOT use `<Link>` (per RESEARCH.md PATTERNS § PostHogGate Gotchas).

    Project conventions: PascalCase `.tsx` test file mirrors source path under `src/__tests__/components/PostHogGate.test.tsx`; `@/` alias; WHY-only comments; no plan IDs.
  </action>
  <verify>
    <automated>npm run test -- PostHogGate 2>&1 | grep -E '(FAIL|Cannot find module|PostHogGate)' | head -5</automated>
  </verify>
  <done>Test file exists at `src/__tests__/components/PostHogGate.test.tsx` with 4 tests: undecided/decline render children + loader-spy NOT called; allow renders children synchronously (regression guard for the HIGH blanking finding) + loader-spy eventually called. RED state confirmed via module-not-found error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement `PostHogProviderInner.tsx` (side-effect loader) + `PostHogGate.tsx` (children-always + sibling loader) (GREEN)</name>
  <files>src/components/PostHogProviderInner.tsx, src/components/PostHogGate.tsx</files>
  <behavior>
    - `PostHogProviderInner` is a side-effect loader whose module-load side effects (statically imports `initPostHog` from `@/lib/posthog` — which transitively pulls in `posthog-js` — calls `initPostHog()` at module scope, calls `posthog.setClient(client)` from the facade) run EXACTLY ONCE per page-load when the lazy chunk first resolves. The component body renders `null`. It does NOT statically import `posthog-js/react` and does NOT wrap children — no component in this app calls `usePostHog()`, so the React-context provider is unnecessary, and wrapping children is what introduced the verified HIGH blanking finding.
    - `PostHogGate` reads `useConsent()`. It ALWAYS renders `{children}` directly. When `state === 'allow'`, it ALSO renders `<Suspense fallback={null}><LazyPostHogLoader /></Suspense>` as a SIBLING of children. Structure: `return (<>{children}{state === 'allow' && (<Suspense fallback={null}><LazyPostHogLoader /></Suspense>)}</>)`.
    - `LazyPostHogLoader` is a module-scope `const` (not declared inside the component body — declaring it inside would recreate the lazy reference every render and defeat dedup). It uses `lazy(() => import('@/components/PostHogProviderInner').then(m => ({ default: m.PostHogProviderInner })))` — the named-export destructure form from `src/routes/[__smoke].tsx:13-17`.
  </behavior>
  <action>
    Create `src/components/PostHogProviderInner.tsx`. Imports (named exports only, per CLAUDE.md project rule): `import { initPostHog } from '@/lib/posthog'`, `import { posthog } from '@/lib/posthog-facade'`. Do NOT import `PostHogProvider` from `posthog-js/react` (no React-context consumers exist; importing it would needlessly add to the lazy chunk). Module-scope (NOT inside the component function): `const client = initPostHog()` then `posthog.setClient(client)` — these run exactly once per page-load when the lazy chunk first resolves. The facade `posthog` is the namespace object; calling `posthog.setClient(client)` is the bridge that drains the queue into the real posthog-js instance. Component: `export function PostHogProviderInner() { return null }` — a pure side-effect loader; it renders nothing. WHY-only comment at the top: this module is the SINGLE runtime-import site for `posthog-js` (via `@/lib/posthog`); it renders null because nothing consumes `usePostHog()`, and rendering null keeps it safe to mount inside a Suspense boundary without affecting visible UI. No plan/phase IDs.

    Create `src/components/PostHogGate.tsx`. Imports: `import { lazy, Suspense, type ReactNode } from 'react'`, `import { useConsent } from '@/hooks/useConsent'`. Module-scope `const LazyPostHogLoader = lazy(() => import('@/components/PostHogProviderInner').then(m => ({ default: m.PostHogProviderInner })))` — the named-export destructure mirrors `src/routes/[__smoke].tsx:13-17` exactly. Component:
    ```
    export function PostHogGate({ children }: { children: ReactNode }) {
      const { state } = useConsent()
      return (
        <>
          {children}
          {state === 'allow' && (
            <Suspense fallback={null}>
              <LazyPostHogLoader />
            </Suspense>
          )}
        </>
      )
    }
    ```
    CRITICAL: `{children}` MUST be a sibling of the `<Suspense>` boundary, never a descendant. This is the fix for the verified HIGH finding — the original `<Suspense fallback={null}><LazyPostHogProviderInner>{children}</LazyPostHogProviderInner></Suspense>` nested children inside the suspending component, so Suspense replaced the entire router subtree with `null` during the import window, blanking/remounting the app. With children as a sibling, the only thing Suspense can replace is `<LazyPostHogLoader />` (which renders `null` anyway), so the import window is visually invisible. WHY-only comment explaining the consent gate AND the sibling-not-descendant requirement (no plan IDs).

    Re-run `npm run test -- PostHogGate` AND `npm run test -- posthog-facade`. Both MUST PASS (GREEN).

    Project conventions: PascalCase `.tsx`; named exports for both components (no default export — the lazy consumer uses `.then(m => ({ default: m.PostHogProviderInner }))` to satisfy React.lazy's default-export requirement while preserving the no-default-export project rule); `@/` alias; WHY-only comments.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'npm run test -- PostHogGate posthog-facade 2>&1 | tail -15'</automated>
  </verify>
  <done>
    1. `src/components/PostHogProviderInner.tsx` exports `PostHogProviderInner` (named), statically imports `initPostHog` from `@/lib/posthog` (NOT `posthog-js/react`), calls `initPostHog()` then `posthog.setClient(client)` at module scope, and the component body returns `null`.
    2. `src/components/PostHogGate.tsx` exports `PostHogGate` (named), reads `useConsent()`, ALWAYS renders `{children}`, and renders `<Suspense fallback={null}><LazyPostHogLoader /></Suspense>` as a SIBLING only when `state === 'allow'`. `{children}` is NOT nested inside `<Suspense>`.
    3. Both files use named exports only; no `export default`.
    4. The lazy destructure form `.then(m => ({ default: m.PostHogProviderInner }))` is present in PostHogGate.tsx.
    5. `npm run test -- PostHogGate posthog-facade` → GREEN for both suites (including the children-render-synchronously regression guard and the queue-cap test).
    6. `npm run lint` exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Wire PostHogGate into main.tsx + swap import paths in AuthContext + ConsentContext (+ their tests)</name>
  <files>src/main.tsx, src/contexts/AuthContext.tsx, src/contexts/ConsentContext.tsx, src/__tests__/contexts/AuthContext.test.tsx, src/__tests__/contexts/ConsentContext.test.tsx</files>
  <action>
    (PRECHECK — MANDATORY before any edit, addresses the MEDIUM "existing PostHog React consumers" finding) Run the ANCHORED consumer audit `grep -rnE "usePostHog\(|from 'posthog-js/react'|<PostHogProvider\b" src/`. The patterns are deliberately anchored — `usePostHog\(` (a hook call, not a substring), `from 'posthog-js/react'` (the import specifier), and `<PostHogProvider\b` (the JSX open-tag with a trailing word boundary) — so they match GENUINE React-context consumers but do NOT substring-match the new `PostHogProviderInner` component created by Task 4 (`<PostHogProvider\b` does not match `<PostHogProviderInner` because `Inner` follows without a word boundary; the `import {...} from '@/components/PostHogProviderInner'` lines contain no `from 'posthog-js/react'`). The ONLY expected matches are `src/main.tsx:5` (`import { PostHogProvider } from 'posthog-js/react'`) and `src/main.tsx:95` (`<PostHogProvider client={posthog}>`) — the references this task removes/relocates. (The closing `</PostHogProvider>` at `src/main.tsx:99` is intentionally NOT matched by `<PostHogProvider\b` — it is the close of the removed block, not a consumer.) If the grep returns ANY OTHER match — e.g. a component calling `usePostHog()` or importing `posthog-js/react` — STOP and report: that consumer needs the React-context provider, which means `PostHogProviderInner` would have to wrap that consumer's subtree (reintroducing the blanking risk) or the consumer must be refactored to the facade. Do NOT proceed with the null-rendering loader design until the audit is clean. Record the grep output in SUMMARY.md.

    Edit `src/main.tsx` (existing 102 lines, verified 2026-05-27):
    (a) DELETE line 5 `import { PostHogProvider } from 'posthog-js/react'`.
    (b) DELETE line 6 `import { initPostHog } from '@/lib/posthog'`.
    (c) ADD a new import line in the React/router import cluster (near current line 7): `import { PostHogGate } from '@/components/PostHogGate'`. Keep `import { ConsentProvider } from '@/contexts/ConsentContext'` as-is.
    (d) DELETE line 40 `const posthog = initPostHog()` — that call relocates into `PostHogProviderInner.tsx` module scope.
    (e) REPLACE lines 95-99 (the `<PostHogProvider client={posthog}><ConsentProvider><RouterProvider router={router} /></ConsentProvider></PostHogProvider>` block) with the INVERTED `<ConsentProvider><PostHogGate><RouterProvider router={router} /></PostHogGate></ConsentProvider>` form. ConsentProvider becomes the OUTER wrapper (so PostHogGate's `useConsent()` call finds context). PostHogGate receives `<RouterProvider router={router} />` as its `children` and — per its sibling-not-descendant design (Task 4) — renders that router unconditionally while mounting the lazy posthog loader as a sibling on consent='allow'. The router NEVER unmounts or blanks during the lazy-import window because it is `children` (a sibling of the `<Suspense>` boundary), not a descendant of the suspending loader (this is the fix for the verified HIGH finding — RESEARCH § Pitfall 3 originally assumed Suspense kept children rendered, which is false). The surrounding `<StrictMode><Sentry.ErrorBoundary>...</Sentry.ErrorBoundary></StrictMode>` block (lines 76-100) stays exactly as-is — Phase 15 invariant.
    (f) Phase 15 invariants — DO NOT TOUCH: `Sentry.init` block (lines 22-38), `taggedHandler` definition (lines 58-69), `createRoot` `onUncaughtError`/`onCaughtError`/`onRecoverableError` (lines 71-75), `<Sentry.ErrorBoundary>` placement + `beforeCapture` + `onError` (lines 77-93). Any drift here breaks the `boundary: app-root` tag invariant.
    (g) DO NOT TOUCH line 42 `const router = createRouter({ routeTree })`. The `defaultPreload: 'intent'` edit belongs EXCLUSIVELY to plan 16-06 (PERF-06). Cross-requirement sequencing in RESEARCH § 473-474 keeps each PERF requirement atomic per plan.

    Edit `src/contexts/AuthContext.tsx`:
    (h) Line 6: change `import { posthog } from '@/lib/posthog'` to `import { posthog } from '@/lib/posthog-facade'`. ONLY the source path changes; the named binding stays `posthog`. The call sites at line 168 (`posthog.identify(providerId)`) and line 181 (`posthog.reset()`) are byte-identical — the namespace-object shape of the facade (RESOLVED Q3) makes this swap a one-token edit per import. No other edits to this file.

    Edit `src/contexts/ConsentContext.tsx`:
    (i) Line 4: change `import { posthog } from '@/lib/posthog'` to `import { posthog } from '@/lib/posthog-facade'`. Existing call sites at line 55 (`posthog.opt_in_capturing()`) and line 58 (`posthog.opt_out_capturing()`) stay byte-identical. The `loadSentryReplayIfConsented()` call on line 56 stays as-is — Phase 16 does NOT touch Sentry Replay lazy-loading. No other edits to this file.

    **MANDATORY — re-point the existing PostHog test mocks to the facade (otherwise `npm run test` at the verify below FAILS despite correct source).** After steps (h)/(i) the production code calls `@/lib/posthog-facade`, so a `vi.mock('@/lib/posthog')` in the dependent context tests no longer intercepts the call path — vitest mocks by exact module specifier. Re-point both tests to the swapped specifier:
    (j) Edit `src/__tests__/contexts/AuthContext.test.tsx`: change the `vi.mock('@/lib/posthog', ...)` at line 14 to `vi.mock('@/lib/posthog-facade', ...)`. This is the ONLY occurrence of `'@/lib/posthog'` in that file (the test has no top-level `import { posthog } from '@/lib/posthog'` — it references the mocked surface via the `vi.hoisted` spies). Keep the mock factory body byte-identical (it returns `{ posthog: { opt_in_capturing, opt_out_capturing, identify: mockIdentify, reset }, initPostHog: vi.fn() }`); the extra `initPostHog` key is a harmless superset of the facade's exports and the test only asserts on `posthog.*`. No other edits to this file.
    (k) Edit `src/__tests__/contexts/ConsentContext.test.tsx`: re-point BOTH occurrences of `'@/lib/posthog'` to `'@/lib/posthog-facade'` — the top-level `import { posthog } from '@/lib/posthog'` at line 6 AND the `vi.mock('@/lib/posthog', ...)` at line 9. The import must point at the SAME specifier the mock targets so the `posthog` binding the test asserts on (`expect(posthog.opt_in_capturing).toHaveBeenCalled()` / `opt_out_capturing` at lines 50-111) is the mocked facade surface that ConsentContext now calls. Keep the mock factory body byte-identical. The `@/lib/sentry` mock at line 18 stays as-is. No other edits to this file.
    Both swaps are pure module-specifier re-points — no assertion or factory-shape change — so the existing opt-in/opt-out/identify/reset call-count assertions continue to hold against the facade-routed production path.

    Project conventions: ESM; `@/` alias; named exports; WHY-only comments; no plan IDs in any new or edited comment.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c 'npm run lint; npm run test 2>&1 | tail -10; grep -q "import { posthog } from .@/lib/posthog-facade." src/contexts/AuthContext.tsx; grep -q "import { posthog } from .@/lib/posthog-facade." src/contexts/ConsentContext.tsx; grep -q "<PostHogGate>" src/main.tsx; ! grep -q "PostHogProvider client={posthog}" src/main.tsx; ! grep -qE "@/lib/posthog'\''" src/__tests__/contexts/AuthContext.test.tsx; grep -qE "@/lib/posthog-facade'\''" src/__tests__/contexts/AuthContext.test.tsx; ! grep -qE "@/lib/posthog'\''" src/__tests__/contexts/ConsentContext.test.tsx; grep -qE "@/lib/posthog-facade'\''" src/__tests__/contexts/ConsentContext.test.tsx; echo WIRING_OK'</automated>
  </verify>
  <done>
    1. `src/main.tsx` no longer statically imports `PostHogProvider` from `posthog-js/react`, no longer imports `initPostHog`, no longer calls `initPostHog()` at top level. It DOES import `PostHogGate` from `@/components/PostHogGate`. The provider tree is `<ConsentProvider><PostHogGate><RouterProvider /></PostHogGate></ConsentProvider>` (ConsentProvider outer). Line 42 `createRouter({ routeTree })` is UNCHANGED.
    2. `src/contexts/AuthContext.tsx` line 6 reads `import { posthog } from '@/lib/posthog-facade'`. Call sites at lines 168 + 181 are unchanged.
    3. `src/contexts/ConsentContext.tsx` line 4 reads `import { posthog } from '@/lib/posthog-facade'`. Call sites at lines 55 + 58 are unchanged.
    4. Phase 15 invariants in main.tsx (Sentry.init, taggedHandler, createRoot handlers, Sentry.ErrorBoundary with `boundary: app-root`) are byte-identical to today.
    5. `npm run lint && npm run test` exit 0.
  </done>
</task>

<task type="auto" gate="blocking">
  <name>Task 6: MANDATORY runtime GDPR gate — Playwright e2e asserting zero posthog-js network before Allow</name>
  <files>e2e/tests/posthog-consent-gate.spec.ts</files>
  <behavior>
    - Against the production build served by `npm run preview` (Playwright `webServer` runs `npm run build && npm run preview` at `http://localhost:4173` per `e2e/playwright.config.ts:34-43`), an UNAUTHENTICATED first visit to `/` records ZERO network requests whose URL matches `/posthog/i` (covers both the `posthog-js` chunk fetch AND any `*.posthog.com` ingest request) up to and including the moment the consent banner is visible and BEFORE the Allow button is clicked.
    - After clicking the `Allow` button in the consent banner (`aria-label="Anonymous usage analytics consent"`, button text `Allow` per `src/components/ConsentBanner.tsx:34,47`), at least one network request matching `/posthog/i` DOES fire (the lazy chunk and/or ingest) — proving the gate opens on consent and the lazy load is correctly wired.
    - The app shell (e.g. the Navbar / a known landing element) is visible throughout — the page never blanks (the HIGH-finding regression, observable at runtime).
  </behavior>
  <action>
    Create `e2e/tests/posthog-consent-gate.spec.ts`. Mirror the import + structure of an existing spec (`e2e/tests/browse-respond.spec.ts` / `filter-search.spec.ts`): `import { test, expect } from '@playwright/test'`. No `loginAs` needed — the consent gate must hold for unauthenticated visitors too (and avoiding auth keeps the spec deterministic).

    Pattern:
    ```
    test('no posthog network before consent Allow; fires after', async ({ page }) => {
      const posthogRequests: string[] = []
      page.on('request', (req) => {
        if (/posthog/i.test(req.url())) posthogRequests.push(req.url())
      })
      await page.goto('/')
      // Wait for the consent banner to be visible — proves the shell rendered.
      const banner = page.getByRole('region', { name: 'Anonymous usage analytics consent' })
      await expect(banner).toBeVisible()
      // INVARIANT: nothing posthog-shaped requested before the user opts in.
      expect(posthogRequests, `posthog requests before Allow: ${posthogRequests.join(', ')}`).toHaveLength(0)
      // Opt in.
      await banner.getByRole('button', { name: 'Allow' }).click()
      // After consent, the lazy chunk and/or ingest must fire.
      await expect.poll(() => posthogRequests.length, { timeout: 10_000 }).toBeGreaterThan(0)
    })
    ```
    Adjust the banner locator to match the actual `ConsentBanner.tsx` DOM (it uses `aria-label="Anonymous usage analytics consent"` on its container at line 34 and an `Allow` `<Button>` at line 47 — confirm the container's implicit role; if it is not `region`, fall back to `page.getByLabel('Anonymous usage analytics consent')`). If `VITE_POSTHOG_KEY` is unset in the preview build, `posthog.init` no-ops and NO ingest request fires even after Allow — in that case the post-Allow assertion should check for the lazy CHUNK fetch (a request to a `posthog`-named JS asset under `/assets/`) rather than an ingest request; document which signal was used in SUMMARY.md. The pre-Allow zero-request assertion is the load-bearing one and holds regardless of the key.

    Run `npm run e2e -- posthog-consent-gate`. The spec MUST PASS. This is a BLOCKING gate for PERF-03 closure — do NOT mark the plan complete if this spec fails or is skipped.

    Project conventions: spec under `e2e/tests/`; chromium-only (existing config); WHY-only comments; no plan IDs.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'npm run e2e -- posthog-consent-gate 2>&1 | tail -20'</automated>
  </verify>
  <done>
    1. `e2e/tests/posthog-consent-gate.spec.ts` exists and PASSES under `npm run e2e`.
    2. The pre-Allow assertion confirms ZERO `/posthog/i` network requests before the user clicks Allow (the load-bearing GDPR + perf invariant — empirically proven against the production bundle, not just source intent).
    3. The post-Allow assertion confirms posthog activity (lazy chunk and/or ingest) DOES fire after consent.
    4. SUMMARY.md records the spec result and which post-Allow signal was used (ingest request vs lazy chunk fetch, depending on whether VITE_POSTHOG_KEY is set in the preview env).
  </done>
</task>

<task type="auto">
  <name>Task 7: Verify Phase 15 keepNames allowlist + full suite green + runtime-import invariant</name>
  <files>(none — verification only)</files>
  <action>
    (a) Run `rm -rf dist && npm run build` (under `bash -o pipefail -c` when piping). Expect exit 0. Then run `node scripts/verify-sourcemap-names.mjs`. Expect exit 0 with the literal output `7/7 allowlisted names found — keepNames contract holds.` (string from scripts/verify-sourcemap-names.mjs:76). The new `PostHogGate` / `PostHogProviderInner` names are NOT in the 7-name allowlist and do not need to be — only the original Phase 15 names are contract-binding.

    (b) **Runtime-import invariant (CORRECTED from the original plan's false grep expectation).** The original done-criterion claimed `grep -rE "from 'posthog-js'" src/` should show the single static-import site as `PostHogProviderInner.tsx` — this is WRONG because `src/lib/posthog.ts:1` also statically imports `posthog-js` (and always has). The real invariant: NO module in the critical-path graph imports `@/lib/posthog` or `posthog-js` at runtime; the ONLY runtime importer of `@/lib/posthog` is the lazily-loaded `PostHogProviderInner.tsx`. Verify with: (1) `grep -rln "from '@/lib/posthog'" src/` returns EXACTLY ONE file path — and that path is `src/components/PostHogProviderInner.tsx`. The matcher is ANCHORED with the closing single-quote (`...posthog'`) so it does NOT match `from '@/lib/posthog-facade'` — the trailing quote is what distinguishes the runtime module from the facade. A bare `grep -v posthog-facade` filter is BANNED here: `grep -rl` emits FILENAMES, so the filter would drop a whole file even though the matched line was the facade import, and an unanchored pattern (`from .@/lib/posthog.`) would let the regex `.` match the `-` in `-facade` — that exact pair of defects is what produced a false-negative `3` against the asserted `1` in the prior plan. The single-string-equality assertion (`test "$(grep -rln ...)" = "src/components/PostHogProviderInner.tsx"`) simultaneously proves both "exactly one file matches" and "that file is the loader" — any extra runtime importer yields a multi-line string that cannot equal the single path. (2) `grep -n "import type" src/lib/posthog-facade.ts` confirms the facade's posthog-js import is type-only. (3) the ANCHORED consumer scan `grep -rnE "usePostHog\(|from 'posthog-js/react'|<PostHogProvider\b" src/` returns NOTHING (the React-context provider was removed entirely; no consumers exist). The anchors are required so the scan does NOT substring-match the retained `PostHogProviderInner` loader: `<PostHogProvider\b` does not match `<PostHogProviderInner` (no word boundary before `Inner`), and `PostHogProviderInner.tsx` imports neither `posthog-js/react` nor calls `usePostHog()`. A bare-substring `PostHogProvider` scan here would FALSELY fail against the correct `PostHogProviderInner` files — that is the defect this anchored form fixes. (4) **Direct-`posthog-js` source guard (broadens the invariant beyond `@/lib/posthog`):** the SOLE file with a direct *value* import of bare `posthog-js` must be `src/lib/posthog.ts` (the lazy module, retained verbatim per D-03). A future new file doing `import x from 'posthog-js'` would be a runtime-graph regression the `@/lib/posthog` check in (1) cannot catch (it imports the package directly, not via the facade or `@/lib/posthog`). Assert with a LINE-level (not filename-filter) pipeline that is portable POSIX ERE (NO `grep -P`): `test "$(grep -rnE "from 'posthog-js'$" src/ | grep -v "import type" | cut -d: -f1 | sort -u)" = "src/lib/posthog.ts"`. Three properties make this robust and trace-verified against the live tree (returns exactly `src/lib/posthog.ts` today): the closing single-quote anchor (`posthog-js'$`) excludes `posthog-js/react` (its line ends `'posthog-js/react'`); `grep -v "import type"` drops the facade's type-only import LINE (it operates on `file:line:content` records from `grep -rn`, so it removes only the matching line — NOT a whole file, avoiding the banned `grep -rl | grep -v` filename-filter defect that produced prior false negatives); `cut -d: -f1 | sort -u` then collapses to unique filenames, and the single-string equality proves both "exactly one file" and "that file is `posthog.ts`". This catches any binding name (including one starting with `t`, which a `^import [^t]` form would falsely miss). Record all four grep results in SUMMARY.md.

    (c) Run `npm run test` for the full suite. All pre-existing tests must pass; the two new vitest suites pass; total green.

    (d) Run `npm run lint` for the full suite. Exit 0.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c 'rm -rf dist && npm run build && node scripts/verify-sourcemap-names.mjs && test "$(grep -rln "from '\''@/lib/posthog'\''" src/)" = "src/components/PostHogProviderInner.tsx" && test "$(grep -rnE "from '\''posthog-js'\''$" src/ | grep -v "import type" | cut -d: -f1 | sort -u)" = "src/lib/posthog.ts" && ! grep -rqnE "usePostHog\(|from '\''posthog-js/react'\''|<PostHogProvider\b" src/ && grep -q "import type" src/lib/posthog-facade.ts && npm run lint && npm run test 2>&1 | tail -10'</automated>
  </verify>
  <done>
    1. `npm run build` exit 0; sourcemap-names allowlist still passes (Phase 15 invariant preserved).
    2. The runtime-import invariant holds: exactly one module (`PostHogProviderInner.tsx`) runtime-imports `@/lib/posthog`; the SOLE direct value-importer of bare `posthog-js` is `src/lib/posthog.ts` (D-03 lazy module, retained); the facade uses `import type` only; the anchored scan `grep -rnE "usePostHog\(|from 'posthog-js/react'|<PostHogProvider\b" src/` returns NOTHING — no React-context consumer references remain in `src/` (anchored so the retained `PostHogProviderInner` loader is NOT substring-matched).
    3. `npm run lint` and `npm run test` exit 0 with no regressions.
    4. All Phase 16 wave-3 invariants documented in SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User's browser → product (consent banner) | GDPR opt-IN: zero analytics traffic until user clicks Allow. PostHog network requests crossing this boundary before consent='allow' would violate Phase 6's opt-IN rewire (V8 Data Protection). |
| Lazy chunk fetch boundary | The dynamic `import('@/components/PostHogProviderInner')` is the SOLE path that brings `posthog-js` into the network graph. Any other static import of `posthog-js` anywhere in the tree would defeat the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Information disclosure (privacy) | `posthog-js` loads before consent='allow' — GDPR opt-IN violation | mitigate | PostHogGate renders `{children}` unconditionally and only mounts the lazy loader sibling when `state === 'allow'`. PostHogProviderInner is the SINGLE runtime-import site for `@/lib/posthog` (→ posthog-js); the facade uses `import type` only. Initial bundle never references the posthog-js runtime. PostHogGate component test (Task 3) asserts the gate semantics under mocked useConsent state. **PRIMARY CONTROL (now MANDATORY): the Task 6 Playwright e2e gate empirically proves ZERO `/posthog/i` network requests before Allow against the production preview build — this is the only check that proves bundler/runtime behavior, not just source intent. It is a BLOCKING gate for PERF-03 closure, no longer optional.** |
| T-16-07 | Tampering | Future code change reintroduces a static `import` of `posthog-js`/`@/lib/posthog` outside PostHogProviderInner → bundle regresses | mitigate | Detection is via Task 7's runtime-import invariant check (exactly one module runtime-imports `@/lib/posthog`), the post-PERF-04 treemap inspection (plan 04 verifies `vendor-posthog` is reachable ONLY from the lazy chunk), AND the mandatory Task 6 Playwright network gate which would fail if a static import leaked posthog-js into the initial payload. Long-term: a lint rule could enforce the single-importer invariant, but is out of scope for this phase. |
| T-16-08 | Denial of service | Lazy chunk fetch fails (CDN error) → loader never mounts → identify/reset calls queue indefinitely → memory leak | mitigate | The facade queue is now bounded by a documented `QUEUE_CAP = 50` (Task 2): when the client is unset and the queue is at cap, further calls are dropped rather than pushed. The cap is far above the single-digit number of identify/reset/opt_* calls a real session makes, so it never trips in normal operation; it is a safety valve against the transient-CDN-failure scenario Gemini flagged. Task 1 Test 5 asserts the queue length never exceeds the cap. |
| T-16-09 | Tampering | StrictMode double-invoke double-flushes the queue | mitigate | `posthog.setClient` is called at MODULE SCOPE of PostHogProviderInner.tsx (not inside the component body). Module evaluation runs exactly once per page-load regardless of StrictMode. RESEARCH § PostHogProviderInner Gotcha is explicit. |
| T-16-10 | V8 Data Protection (privacy invariant preservation) | initPostHog's `opt_out_capturing_by_default: true` accidentally removed during this refactor | mitigate | Phase 16 does NOT touch `src/lib/posthog.ts` (per D-03). The existing init shape is preserved verbatim. The facade's `opt_in_capturing` / `opt_out_capturing` forwarders match today's call sites exactly so the ConsentContext effect's opt-in flip on `state === 'allow'` continues to work. |

This plan is the security-most-relevant plan in the phase. The GDPR consent-gate timing invariant (Phase 6) is preserved by architecture (single runtime-import site for `@/lib/posthog`/posthog-js inside the lazy chunk; facade uses type-only imports) and verified by TWO layers: the PostHogGate component test (source-intent control) AND the MANDATORY Task 6 Playwright network gate (empirical bundler/runtime control). The empirical gate is the load-bearing one — it is the only check that proves what the production bundle actually ships, which is precisely the gap the cross-AI review flagged as a verified HIGH concern.
</threat_model>

<verification>
1. `npm run test -- posthog-facade` → all tests pass (GREEN), including the queue-cap test.
2. `npm run test -- PostHogGate` → all 4 tests pass (GREEN), including the synchronous-children regression guard.
3. `npm run test` (full suite) → exit 0; pre-existing tests continue to pass; new suites green.
4. `npm run lint` → exit 0.
5. `npm run build` → exit 0.
6. `node scripts/verify-sourcemap-names.mjs` → "7/7 allowlisted names found — keepNames contract holds." (Phase 15 invariant preserved).
7. **MANDATORY (blocking):** `npm run e2e -- posthog-consent-gate` → PASS. Zero `/posthog/i` network requests before Allow; posthog activity fires after Allow. This is the empirical GDPR + perf gate against the production preview build — the load-bearing proof the bundle keeps posthog-js out of the initial payload.
8. Runtime-import invariant (CORRECTED — anchored matcher): `grep -rln "from '@/lib/posthog'" src/` returns EXACTLY ONE file path and it is `src/components/PostHogProviderInner.tsx` — the sole runtime importer of `@/lib/posthog`. The closing-quote anchor (`posthog'`) means `@/lib/posthog-facade` is NOT matched, so AuthContext/ConsentContext (which import the facade only) do not falsely count. `src/lib/posthog-facade.ts` uses `import type` only. The ANCHORED consumer scan `grep -rnE "usePostHog\(|from 'posthog-js/react'|<PostHogProvider\b" src/` returns NOTHING — anchored on `usePostHog\(`, `from 'posthog-js/react'`, `<PostHogProvider\b` so it matches genuine consumers but does NOT substring-match the retained `PostHogProviderInner` loader (a bare `PostHogProvider` substring scan would falsely fail against the correct new files — that is the verify defect this anchored form fixes). (The original plan's claim that `from 'posthog-js'` should appear once in PostHogProviderInner was wrong — `src/lib/posthog.ts:1` also imports posthog-js and always will; and the prior cycle's `grep -v posthog-facade` filter was filename-level not line-level, plus its unanchored `.` matched `-facade`, yielding a false-negative `3`.)
9. main.tsx `<Sentry.ErrorBoundary>` block (`boundary: app-root` tag chain) is byte-identical to pre-Phase-16 state. Line 42 `createRouter({ routeTree })` is byte-identical to today (plan 06 owns the `defaultPreload: 'intent'` edit).
</verification>

<success_criteria>
- 3 new source files (`posthog-facade.ts`, `PostHogProviderInner.tsx`, `PostHogGate.tsx`), 2 new vitest files, and 1 new Playwright e2e spec (`e2e/tests/posthog-consent-gate.spec.ts`) committed; named-export convention preserved.
- 3 edited source files (`main.tsx`, `AuthContext.tsx`, `ConsentContext.tsx`) — only the targeted lines changed (no drift in Phase 15 invariants; no drift into PERF-06 territory on line 42).
- Facade tests (queue-then-flush + forward-after-load + opt_in/out routing + queue-cap) green, all consuming the namespace-object surface.
- PostHogGate component tests (undecided/decline/allow + synchronous-children regression guard) green.
- **MANDATORY: `npm run e2e -- posthog-consent-gate` passes — zero posthog network before Allow against the production preview build (empirical GDPR + perf gate).**
- `{children}` (the router) is a sibling of the `<Suspense>` boundary, never a descendant — the app never blanks during the lazy-import window.
- Runtime-import invariant: exactly one module runtime-imports `@/lib/posthog`; the anchored scan `grep -rnE "usePostHog\(|from 'posthog-js/react'|<PostHogProvider\b" src/` returns NOTHING — no React-context consumer references remain in `src/` (anchored so the retained `PostHogProviderInner` loader is not substring-matched).
- Phase 15 sourcemap-names allowlist passes against the post-Phase-16 build.
- `npm run lint && npm run test && npm run build` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-03-SUMMARY.md` documenting: which files were created and which edited; the PostHog React-consumer audit grep output (Task 5 precheck); the exact lazy destructure form used (Option B per CLAUDE.md no-default-export rule); the RED → GREEN cycle for both Wave 0 vitest tests; the MANDATORY Playwright gate result (`npm run e2e -- posthog-consent-gate`) and which post-Allow signal was used (ingest request vs lazy chunk fetch, depending on whether VITE_POSTHOG_KEY is set in the preview env); confirmation that children render synchronously (no blanking — the HIGH-finding regression guard); the runtime-import invariant grep results (Task 7); whether Phase 15 keepNames allowlist still passes; whether `<Sentry.ErrorBoundary>` placement was preserved byte-for-byte; explicit confirmation that line 42 of main.tsx (`createRouter`) was NOT touched (plan 06 owns that edit); confirmation that the dependent context tests (`AuthContext.test.tsx`, `ConsentContext.test.tsx`) had their `vi.mock(...)` / `import` re-pointed from `@/lib/posthog` to `@/lib/posthog-facade` so the mocks still intercept the swapped production path. List `requirements-completed: [PERF-03]` in frontmatter.
</output>
</content>
