---
phase: 16
plan: 04
type: execute
wave: 4
depends_on: ["16-01", "16-03"]
files_modified:
  - vite.config.ts
requirements: [PERF-04]
autonomous: true
must_haves:
  truths:
    - "D-04: only two explicitly-named chunks — `vendor-react` (react + react-dom only) and `vendor-posthog` (posthog-js + posthog-js/react); supabase-js + sentry-replay stay Rolldown auto-split (Task 1)"
    - "Production builds emit `vendor-react-*.js` and `vendor-posthog-*.js` named chunks under `dist/assets/`"
    - "`vendor-react` contains ONLY `react` + `react-dom` (NOT TanStack Router, NOT @radix-ui/react-*, NOT @sentry/react, NOT react-error-boundary, NOT @testing-library/react)"
    - "`vendor-posthog` is reachable ONLY via the lazy chunk fetched from `<PostHogGate>` — no static-import path pulls it into the main entry chunk"
    - "Phase 15 keepNames + 7-name allowlist + D-09 throw all continue to hold"
  artifacts:
    - path: "vite.config.ts"
      provides: "Extended `build.rolldownOptions.output` with a `manualChunks` matcher that pins react+react-dom → vendor-react and posthog-js → vendor-posthog"
      contains: "manualChunks"
  key_links:
    - from: "vite.config.ts rolldownOptions.output"
      to: "Rolldown chunk graph"
      via: "manualChunks function or object with package-boundary-anchored matchers"
      pattern: "vendor-(react|posthog)"
---

<objective>
Extend `vite.config.ts` `build.rolldownOptions.output` to add a `manualChunks` matcher that splits two named vendor chunks: `vendor-react` (react + react-dom only) and `vendor-posthog` (posthog-js + posthog-js/react). Per D-04, no other chunks are explicitly named — Rolldown's existing auto-split for supabase-js and sentry-replay is left alone.

Purpose: Cache stability. React + react-dom rarely change; PostHog rarely changes; isolating them into stable named chunks means app-only changes don't bust the user's cache for these large libraries. Verification of the PERF-03 architecture: `vendor-posthog` reaching the network graph ONLY via the lazy chunk proves the gate works at the chunk-graph level.

Output: One file edited; two named chunks visible in `dist/assets/` post-build; treemap inspection confirms no kitchen-sink contamination in `vendor-react`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-CONTEXT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-RESEARCH.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-PATTERNS.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-VALIDATION.md

@vite.config.ts
@scripts/verify-sourcemap-names.mjs
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-03-SUMMARY.md

<interfaces>
<!-- Current vite.config.ts after plan 03 lands (verified 2026-05-27 plus plan 01 plugin-mutex extensions). -->
<!-- The output object at lines ~33-35 today: rolldownOptions: { output: { keepNames: true } } -->
<!-- Phase 16 plan 04 EXTENDS this object — keepNames stays, manualChunks is added alongside. -->
<!-- Pitfall 8 explicit: do NOT introduce `rollupOptions` — use `rolldownOptions` (Vite 8/Rolldown canonical). -->

Existing/expected packages whose paths are NOT to be matched by `vendor-react`:
  - @tanstack/react-router (substring match would catch this — explicitly excluded per D-04)
  - @radix-ui/react-* (radix primitives)
  - @sentry/react
  - react-error-boundary (transitively)
  - @testing-library/react (test-only — should not be in prod bundle anyway)

Existing chunks left alone (auto-split by Rolldown today per D-04):
  - @supabase/supabase-js (~213 KB)
  - @sentry/replay (~140 KB; lazy-loaded already via Phase 6 consent rewire)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `manualChunks` matcher to `vite.config.ts` rolldownOptions.output (PERF-04)</name>
  <files>vite.config.ts</files>
  <action>
    Edit `vite.config.ts` to extend the existing `build.rolldownOptions.output` object. The object currently contains `keepNames: true` only — Phase 16 plan 04 adds `manualChunks` alongside it (do NOT replace; do NOT relocate to a `rollupOptions` key — Pitfall 8 is explicit). The recommended matcher form per RESEARCH § PERF-04 Planner Instruction is the OBJECT form (Rolldown matches on resolved package, eliminating Pitfall 2's path-substring risk):

    `manualChunks: { 'vendor-react': ['react', 'react-dom'], 'vendor-posthog': ['posthog-js', 'posthog-js/react'] }`

    If the object form does not produce the expected chunks in the analyze treemap (e.g. Rolldown's object-keyed manualChunks resolves the package strings to import-spec syntax rather than node_modules-directory match), fall back to the FUNCTION form with explicit `/node_modules/<pkg>/` boundary matchers per RESEARCH § PERF-04 fallback: `manualChunks: (id) => { if (/[\\/]node_modules[\\/](react|react-dom)[\\/]/.test(id)) return 'vendor-react'; if (/[\\/]node_modules[\\/]posthog-js[\\/]/.test(id)) return 'vendor-posthog'; }`. The function form's regex anchors on `/node_modules/<pkg>/` boundaries — `id.includes('react')` is FORBIDDEN per Pitfall 2 (it catches @tanstack/react-router, @radix-ui/react-*, @sentry/react, react-error-boundary, @testing-library/react). The `posthog-js` package directory contains both `posthog-js` and `posthog-js/react` exports — a single matcher `/node_modules/posthog-js/` covers both.

    Try the object form first. If `npm run build` exits 0 AND `ls dist/assets/ | grep -E '^vendor-(react|posthog)-'` shows TWO matches AND `ANALYZE=true npm run build` treemap confirms `vendor-react` contains ONLY react + react-dom, the object form is the chosen shape. Otherwise, swap to the function form and re-verify.

    `keepNames: true` stays in the same `output` object — Phase 15 invariant. No other vite.config.ts blocks touched. The Phase 16 plan-01 D-09 throw + env-gated plugin mutex stays intact.

    Project conventions: TypeScript strict; no `rollupOptions` (Vite 8 canonical is `rolldownOptions`); WHY-only comment explaining the matcher chose `vendor-react` (cache stability for react + react-dom which rarely change) and `vendor-posthog` (cache stability + isolation as the lazy-loaded analytics module) — no plan/phase IDs in the source comment.
  </action>
  <verify>
    <automated>set -e; rm -rf dist && npm run build && ls dist/assets/ | grep -E '^vendor-react-' | head -1 && ls dist/assets/ | grep -E '^vendor-posthog-' | head -1 && node scripts/verify-sourcemap-names.mjs && echo MANUALCHUNKS_OK</automated>
  </verify>
  <done>
    1. `dist/assets/` contains at least one file matching `^vendor-react-.*\.js$` and at least one matching `^vendor-posthog-.*\.js$` after `npm run build`.
    2. `node scripts/verify-sourcemap-names.mjs` exits 0 (Phase 15 keepNames invariant preserved).
    3. `ANALYZE=true npm run build` followed by visual inspection of `dist/stats.html` confirms `vendor-react` contains ONLY `react` + `react-dom` (NOT @tanstack/react-router, NOT @radix-ui/*, NOT @sentry/react).
    4. `vendor-posthog` chunk is reachable in the treemap ONLY via the lazy import path (descends from the PostHogGate lazy chunk, not from the main entry chunk). Capture a one-paragraph treemap observation into the SUMMARY.
    5. `npm run lint && npm run test` exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify treemap shape + capture post-change bundle observation</name>
  <files>(none — verification + SUMMARY-content capture)</files>
  <action>
    Run `ANALYZE=true npm run build` against the post-Task-1 tree. Open the resulting `dist/stats.html` in a browser and inspect:
    (a) Does `vendor-react-*.js` exist as a named chunk? Note its unminified + gzip size.
    (b) Does `vendor-react` contain ONLY `react` and `react-dom`? Visually scan the chunk's contents — confirm absence of TanStack Router, Radix, Sentry, Testing Library, react-error-boundary.
    (c) Does `vendor-posthog-*.js` exist as a named chunk? Note its size.
    (d) From which top-level chunk does the import graph reach `vendor-posthog`? Confirm it is the lazy chunk descending from `<PostHogGate>` (not the main entry chunk).
    (e) What is the new main entry chunk's unminified size vs the PERF-02 baseline (`.planning/closure/v1.3-bundle-audit-pre.html`)? Compute the delta — RESEARCH targets ~180-200 KB reduction.

    Capture (a)-(e) as a structured paragraph in the plan SUMMARY. This paragraph is the verification evidence for both PERF-03's "~180-200 KB removed from critical-path" claim AND PERF-04's chunk-shape claim.

    Re-run the sourcemap-name guard against the analyze build to confirm Phase 15 contract still holds across both build modes: `node scripts/verify-sourcemap-names.mjs` — exit 0.
  </action>
  <verify>
    <automated>set -e; rm -rf dist && ANALYZE=true npm run build && test -f dist/stats.html && node scripts/verify-sourcemap-names.mjs && ls -la dist/assets/ | grep -E '(vendor-react|vendor-posthog)' && echo TREEMAP_VERIFIED_OK</automated>
  </verify>
  <done>
    1. `dist/stats.html` produced; treemap opens to a navigable bundle map.
    2. `vendor-react` named chunk present and contains ONLY react + react-dom.
    3. `vendor-posthog` named chunk present and reachable ONLY via the lazy chunk from `<PostHogGate>` (not the main entry chunk).
    4. Main entry chunk size delta vs PERF-02 baseline documented in SUMMARY.
    5. Phase 15 keepNames allowlist passes against the analyze build.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output (`dist/assets/`) → browser cache | Cache-stability gains from named chunks reduce surface for stale-bundle bugs but introduce no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Tampering (build correctness) | `manualChunks` matcher catches the wrong packages → `vendor-react` becomes kitchen-sink (Pitfall 2) | mitigate | Use OBJECT form (package-name keyed) as primary; FUNCTION form with `/node_modules/<pkg>/` boundary-anchored regex as fallback. Treemap inspection in Task 2 verifies `vendor-react` contains ONLY react + react-dom. |
| T-16-12 | Tampering | `manualChunks` field on wrong key (`rollupOptions` vs `rolldownOptions`) — silently ignored (Pitfall 8) | mitigate | Plan action is explicit: extend the existing `rolldownOptions.output` object (line 33-35 area); do NOT introduce `rollupOptions`. Task 1 verify command checks for the named chunks in `dist/assets/` — absence proves the field is on the wrong key. |
| T-16-13 | Information disclosure | `vendor-posthog` chunk pulled into the main entry chunk by a stray static import → GDPR opt-IN bundle violation (regression of T-16-06 from plan 03) | mitigate | Treemap inspection in Task 2 explicitly verifies `vendor-posthog` is reachable ONLY via the lazy chunk. If it's reachable from the main entry chunk, plan 03's gate is broken and must be fixed before plan 04 ships. |

Inherits T-16-01 / T-16-02 / T-16-03 controls from plan 01 (D-09 throw still active).
</threat_model>

<verification>
1. `npm run build` exits 0; `ls dist/assets/ | grep -E '^vendor-(react|posthog)-'` returns two matches.
2. `ANALYZE=true npm run build` + treemap inspection confirms `vendor-react` is react + react-dom only and `vendor-posthog` is reachable only via the lazy chunk.
3. `node scripts/verify-sourcemap-names.mjs` exits 0 against both vanilla and analyze builds.
4. `npm run lint && npm run test` exit 0.
5. Plan 01's D-09 throw still fires when `ANALYZE=true && NETLIFY_CONTEXT=production` (no regression).
</verification>

<success_criteria>
- `vite.config.ts` `build.rolldownOptions.output` carries both `keepNames: true` (Phase 15) and `manualChunks: {...}` (Phase 16).
- Production builds emit `vendor-react-*.js` and `vendor-posthog-*.js` named chunks.
- `vendor-react` contains ONLY react + react-dom; treemap confirms.
- `vendor-posthog` is reachable ONLY via the lazy chunk; treemap confirms.
- Main entry chunk shrinks measurably vs the PERF-02 baseline (delta documented in SUMMARY).
- Phase 15 invariants and plan 01 D-09 throw all preserved.
</success_criteria>

<output>
Create `.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-04-SUMMARY.md` with: the chosen matcher form (object or function fallback); the unminified + gzip sizes of the two new vendor chunks; the main entry chunk delta vs PERF-02 baseline; treemap-derived verification that `vendor-react` is kitchen-sink-free and `vendor-posthog` is lazy-only-reachable. List `requirements-completed: [PERF-04]` in frontmatter.
</output>
</content>
