---
phase: 16
slug: uidn-02-aggressive-perf-budget-pass
status: draft
shadcn_initialized: true
preset: new-york / neutral / lucide / inter (shipped at v1.0)
created: 2026-05-27
---

# Phase 16 — UI Design Contract

> Visual and interaction contract for Phase 16 (UIDN-02 Aggressive Perf-Budget Pass). Generated by gsd-ui-researcher.
>
> **Phase character:** Performance/infrastructure phase. The visible UI surface is intentionally minimal — Phase 16 must NOT introduce any visible visual change. The only DOM-level edit (Navbar `<img>` → `<picture><source><img>`) is required to be visually indistinguishable from the v1.2 baseline. The "contract" this UI-SPEC encodes is therefore primarily one of **preservation**: existing tokens, existing layout, existing copy stay frozen, plus measurable perf budgets that downstream agents must honor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized at v1.0) |
| Preset | new-york style + Neutral baseColor + lucide icons (from `components.json`) |
| Component library | shadcn/ui (vendored under `src/components/ui/`) + radix-ui primitives |
| Icon library | lucide-react 1.7.0 |
| Font | Inter (system-font stack — no `@font-face` per v1.3 Out-of-Scope anti-feature) |

Source: `/Users/khaiphan/code/wtcs-community-polls/components.json` (confirmed 2026-05-27).

Phase 16 does NOT add any new shadcn primitive — every needed primitive (Button, DropdownMenu, etc.) already exists in `src/components/ui/`. The PostHogGate wrapper is a plain React component (no UI surface), not a shadcn block.

---

## Phase Scope: Visible UI Surfaces

| Surface | Change | Visual delta required |
|---------|--------|----------------------|
| `Navbar.tsx` logo | `<img>` → `<picture><source type="image/webp"><img></picture>` | **None.** Same pixel dimensions, same `alt`, same `className`, same `width`/`height`. Browser swaps source format only. |
| `main.tsx` PostHog wrapper | Replace `<PostHogProvider>` with lazy `<PostHogGate>` | **None.** Children render identically; only the bundle-load timing changes. |
| `vite.config.ts` plugin order | Env-gated visualizer vs sentryVitePlugin | **None.** No runtime UI impact. |
| `wtcs-logo.webp` asset | New file added under `src/assets/` | **None.** Logo renders identically; smaller bytes only. |
| `createRouter` config | `defaultPreload: 'intent'` | **None.** Behavior change is faster route navigation on hover — no new UI element. |

**Non-surfaces (intentional negative space):**

- No new pages, routes, modals, toasts, or notifications.
- No copy changes anywhere in the product.
- No skeleton screens, loading spinners, or suspense fallbacks introduced (PostHog lazy-load happens silently; it has no user-facing trigger).
- No empty-state, error-state, or destructive-confirmation surfaces (no CRUD work in this phase).

---

## Spacing Scale

Phase 16 inherits the shipped v1.0 scale unchanged. No new spacing tokens declared.

| Token | Value | Usage in this phase |
|-------|-------|----------------------|
| sm | 8px (`gap-2`) | Navbar right-cluster spacing (already present) |
| md | 16px (`px-4`) | Navbar horizontal padding mobile (already present) |
| lg | 24px (`px-6`) | Navbar horizontal padding desktop (already present) |

Exceptions: 44px minimum touch target on Navbar logo `<Link>` (`min-h-[44px]`, already shipped) — preserved when wrapping `<img>` in `<picture>`.

**Constraint to executor:** Do NOT introduce new Tailwind spacing utilities in this phase. Any new utility in the diff is grounds for review pushback.

---

## Typography

Phase 16 inherits the shipped v1.0 stack unchanged. No type changes.

| Role | Size | Weight | Line Height | Source |
|------|------|--------|-------------|--------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 (Tailwind default) | Tailwind defaults + Inter system stack |
| Label | 14px (`text-sm`) | 500 (medium) | 1.5 | Used on Navbar profile dropdown username row (already shipped) |
| Heading (page) | 24px (`text-2xl`) | 600 (semibold) | 1.2 | Existing routes (not touched by Phase 16) |

**Constraint to executor:** Do NOT introduce new font sizes, weights, or font-family declarations in this phase. Inter remains a system-font-only stack — explicitly anti-feature to add `@font-face` per v1.3 Out-of-Scope.

---

## Color

Phase 16 inherits the shipped v1.0 palette unchanged. shadcn new-york + Neutral baseColor with CSS-variable theming via `next-themes`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `bg-background` (HSL var) | Page surfaces — unchanged |
| Secondary (30%) | `bg-card` / `bg-muted` (HSL vars) | Navbar, cards, sidebar — unchanged |
| Accent (10%) | `bg-primary` (HSL var) | Reserved for: primary CTAs (Sign in, Submit response, Save admin form, primary nav active state). Phase 16 does NOT introduce any new accent-use site. |
| Destructive | `bg-destructive` (HSL var) | Reserved for: Sign out menu item + admin Delete buttons. Phase 16 does NOT introduce any new destructive surface. |

Accent reserved for (verbatim, no additions): primary buttons, focus-ring indicator, active route link state, response submission CTA, admin save CTA.

**Constraint to executor:** Do NOT introduce new color tokens, new HSL variables, or new utility-class color references in this phase. If a diff touches a `bg-*` / `text-*` / `border-*` class anywhere outside Navbar's `<picture>` wrap, that is grounds for review pushback.

---

## Copywriting Contract

Phase 16 has **no new user-facing copy**. All existing copy is frozen verbatim. No new toasts, no new error messages, no new confirmation dialogs.

| Element | Copy | Status |
|---------|------|--------|
| Primary CTA | n/a — no new CTA in this phase | Frozen |
| Empty state heading | n/a — no new empty surface | Frozen |
| Empty state body | n/a — no new empty surface | Frozen |
| Error state | n/a — no new error surface | Frozen |
| Destructive confirmation | n/a — no new destructive surface | Frozen |
| Navbar logo `alt` | "WTCS Community Suggestions" | **Preserve verbatim** when wrapping in `<picture>` (currently on `<img>` at Navbar.tsx:34). |
| Navbar logo `aria-label` | "WTCS Community Suggestions" | **Preserve verbatim** (currently on `<Link>` at Navbar.tsx:30). |

**PostHog lazy-load is silent.** No user-facing copy or visual indicator signals that posthog is or isn't loaded. The Consent banner copy (shipped in Phase 6) is unchanged.

**Vite throw-on-misconfigure message (D-09) — developer-facing only, NOT user-facing:**

The thrown Error message at `vite.config.ts` config-load time when `ANALYZE=true && NETLIFY_CONTEXT=production` is set must:
- Reference OBSV-04 + Phase 15 sourcemap-upload chain explicitly by name
- Be terse + actionable (one-sentence diagnosis + one-sentence remediation)
- Suggested form (planner may refine wording): `Refusing production build with ANALYZE=true: rollup-plugin-visualizer and sentryVitePlugin are mutually exclusive (both require last position), so ANALYZE=true on a production build skips sourcemap upload and silently breaks the OBSV-04 evidence chain shipped in Phase 15. Unset ANALYZE or run analyze locally only.`

This is the only new "copy" Phase 16 introduces and it surfaces exclusively in build-time CI logs / developer terminals — never to a product user.

---

## Performance Budget as UI Contract

Phase 16's distinguishing feature: the design contract is largely a set of **measurable budgets** that the executor must honor and the auditor must verify.

### Bundle budgets

| Budget | Pre-change baseline (PERF-02) | Post-change target | Verified via |
|--------|--------------------------------|--------------------|--------------|
| Main critical-path entry chunk | Captured in `.planning/closure/v1.3-bundle-audit-pre.html` (treemap on `main` before any Phase 16 edit) | Pre-baseline minus ~180–200 KB unminified (PostHog removed) | Re-run `ANALYZE=true npm run build`; compare treemaps |
| `vendor-react` named chunk | Does not exist | Contains `react` + `react-dom` **only** (NOT TanStack Router) | Bundle treemap shows the named chunk |
| `vendor-posthog` named chunk | Does not exist | Contains `posthog-js` + `posthog-js/react` only | Bundle treemap shows the named chunk |
| `posthog-js` network request before user clicks Allow | n/a — always loads at boot today | **Zero requests** before consent state transitions to `'allow'` | DevTools Network tab on Netlify preview |

### Lighthouse mobile budget (PERF-07)

| Budget | Value | Outcome path |
|--------|-------|--------------|
| Performance score, all 5 routes (D-05) | ≥ 90 | PASS: PROJECT.md `Mobile-first responsive design` row flips ⚠️ → ✓; UIDN-02 closes |
| Performance score, any route (D-05) | < 90 | DEFER: row stays ⚠️; D-12 follow-up trigger remains; UIDN-02 stays open |
| Audit target (D-06) | Production (`https://polls.wtcsmapban.com`) | Post-merge from `main` once Netlify production deploy lands |
| Audit cadence (D-13) | Single run per milestone | No repeated runs; outcome on the first run is the recorded outcome |

### Layout-shift budget (CLS) on Navbar `<picture>` swap

| Budget | Value | Verified via |
|--------|-------|--------------|
| Cumulative layout shift introduced by `<picture>` wrap | **Zero** | Explicit `width`/`height` on inner `<img>` (preserved from existing markup); Lighthouse CLS sub-metric on the home route |

### A11y / Best Practices / SEO Lighthouse categories

**Out of scope.** Phase 16 explicitly does not target a threshold for A11y, Best Practices, or SEO Lighthouse categories. Performance score is the only PASS/DEFER lever per CONTEXT D-05 and the discussion log.

---

## Loading & Suspense States

PostHog is loaded via `React.lazy(() => import(...))` inside `<PostHogGate>` (D-01). The SIBLING-loader semantic is the contract — `{children}` (the router) ALWAYS renders directly and unconditionally; the lazy PostHog loader is mounted as a SEPARATE sibling inside `<Suspense fallback={null}>` only when consent resolves to `'allow'`:

| Consent state | Tree shape | User-visible loading state |
|---------------|------------|-----------------------------|
| `'undecided'` | `{children}` rendered directly; PostHog loader absent from tree | **None** — no spinner, no skeleton, no Suspense fallback |
| `'decline'` | `{children}` rendered directly; PostHog loader absent from tree | **None** — `posthog-js` never enters the network graph |
| `'allow'` | `{children}` rendered directly (sibling); `<Suspense fallback={null}><LazyPostHogLoader /></Suspense>` mounted ALONGSIDE it | **`fallback={null}`** — `{children}` are siblings of the Suspense boundary, not inside it, so they stay rendered untouched during the dynamic import. No visible loading indicator. |

**Why a sibling loader and not child-wrapping:** the loader (`PostHogProviderInner`) renders `null` — it has no rendered surface and wraps no children. Wrapping `{children}` inside the suspending component (the deprecated `<LazyPostHogProvider>{children}</LazyPostHogProvider>` shape) was WRONG: Suspense replaces the entire suspending subtree with the `null` fallback, which would blank/remount the router during the dynamic-import window. Keeping the router as a sibling avoids that defect. While the lazy import resolves, `{children}` render normally with `posthog-facade` queuing identify/reset calls. The user sees no difference between "posthog loaded" and "posthog loading."

**Executor constraint:** Do NOT introduce a Suspense fallback with a spinner, skeleton, or loading toast for the PostHog lazy-load. The contract is that this lazy-load is silent.

---

## Image Loading Contract (PERF-05)

| Element | Required markup |
|---------|-----------------|
| Navbar logo wrapper | `<picture>` with one `<source type="image/webp" srcSet={webpLogo}>` and one fallback `<img src={pngLogo} alt="WTCS Community Suggestions" width={...} height={...} className="h-8 w-auto md:h-9">` |
| WebP asset | `src/assets/wtcs-logo.webp` — manual conversion, committed; PNG fallback retained at `src/assets/wtcs-logo.png` |
| Explicit `width` / `height` | **Required on `<img>`** to preserve zero-CLS (matches existing Tailwind sizing `h-8 w-auto md:h-9`). Planner picks numeric values from current rendered dimensions. |
| `loading` attribute | Not required — Navbar logo is above-the-fold; default eager load is correct |
| `decoding` attribute | Not required for this phase |

**Out of scope per ROADMAP:** `vite-imagetools`, `sharp`, or any automated build-time conversion. The conversion is a one-time manual step landed in the commit.

---

## Route Lazy-Load Behavior (PERF-06)

`createRouter({ defaultPreload: 'intent' })` in `src/main.tsx` applies app-wide:

| Behavior | Contract |
|----------|----------|
| Hover/focus on any `<Link>` | TanStack Router begins preloading the destination route's code chunk |
| Tap/click after hover preload completed | Navigation feels instant (chunk already in cache) |
| Tap/click before hover preload completed | Falls back to normal lazy navigation (cold) |
| Existing explicit `preload="intent"` on individual `<Link>` (Navbar logo, Topics, Archive) | Redundant after this change but **not removed** — defensive belt-and-braces; no harm |
| `<Link to="/admin">` in Navbar (lines 58-66) | Explicit comment notes "No preload — AdminGuard beforeLoad would redirect non-admins on hover." This per-link opt-OUT must be preserved. The `defaultPreload: 'intent'` change applies app-wide, so the admin Link must continue to explicitly suppress preload (set `preload={false}` if needed) to retain its current behavior. |

**Executor constraint:** Verify that turning on `defaultPreload: 'intent'` does NOT trigger preload on the admin Link. If TanStack Router's `defaultPreload` opts every Link in by default, the admin Link must receive `preload={false}` to preserve the existing security behavior (no redirect-on-hover side effects).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none new in this phase — all needed primitives already vendored | not required |
| Third-party registries | **none declared** | not applicable |

Phase 16 is a perf/infrastructure phase. No new shadcn blocks, no third-party registry blocks, no new vendored primitives. Existing `src/components/ui/` inventory (17 primitives: alert, badge, button, card, checkbox, collapsible, dialog, dropdown-menu, input, label, progress, select, sheet, sonner, switch, tabs, textarea) is sufficient and unchanged.

`rollup-plugin-visualizer@7.0.1` is a Vite **build-time** plugin (devDependency, dev-only), NOT a shadcn registry component. The registry safety gate does not apply to it.

---

## Cross-Cutting Constraints

These constraints bind the executor across all of Phase 16's edits:

1. **Visual no-op invariant.** Production screenshots taken before and after Phase 16's diff must be visually indistinguishable on the home, topics, archive, and admin index routes at both mobile and desktop breakpoints. If a screenshot diff shows any pixel change beyond pure logo-format swap, that is a phase defect.
2. **shadcn discipline.** No new shadcn components added, no existing shadcn component className-overridden, no shadcn primitive removed. Diff scope is config + lazy wrapper + Navbar `<picture>` only.
3. **Token discipline.** No new Tailwind utility classes appear in the diff outside of (a) `<picture>` wrapping, (b) one-line config additions. Color tokens, spacing tokens, type tokens, radius tokens are all frozen.
4. **Copy freeze.** No user-facing string is changed. Only the developer-facing `vite.config.ts` throw message is new copy.
5. **Phase 15 invariant preservation.** The `boundary: app-root` Sentry tag and the `keepNames: true` sourcemap-name allowlist must continue to pass. PostHog lazy-load + manualChunks change must not displace `sentryVitePlugin` from last position in non-ANALYZE builds, and must not break the 7-name allowlist verified by `scripts/verify-sourcemap-names.mjs`.
6. **Accessibility preservation.** Navbar logo `alt` text, `aria-label`, and `min-h-[44px]` touch target all preserved verbatim. The `<picture>` element does not interfere with screen readers (it is transparent to assistive tech; the inner `<img>` `alt` is what's announced).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (no new copy; existing copy frozen verbatim; developer-facing throw message clearly scoped)
- [ ] Dimension 2 Visuals: PASS (visual no-op invariant declared; only DOM addition is `<picture>` wrap with zero-CLS guarantee)
- [ ] Dimension 3 Color: PASS (no color changes; 60/30/10 contract inherited from v1.0 shipped state)
- [ ] Dimension 4 Typography: PASS (no type changes; Inter system stack preserved; no font additions per Out-of-Scope)
- [ ] Dimension 5 Spacing: PASS (no new spacing tokens; existing scale preserved including 44px touch target)
- [ ] Dimension 6 Registry Safety: PASS (no third-party registry; no new shadcn blocks; rollup-plugin-visualizer is a build plugin, not a UI primitive)

**Approval:** pending
