---
phase: 16
plan: 07
type: execute
wave: 6
depends_on: ["16-01", "16-02", "16-03", "16-04", "16-05", "16-06"]
files_modified:
  - .planning/closure/UIDN-02-mobile-evidence.md
  - .planning/closure/UIDN-02-audit-mobile-stdout.txt  # captured stdout — .txt (NOT .log: .gitignore:3 ignores *.log) and OUTSIDE artifacts/lighthouse/ (audit-mobile.sh:28 rm -rf's that dir); this path is git-trackable
  - .planning/PROJECT.md  # conditionally edited on PASS path only — canonical Key Decision table lives here (there is NO root-level PROJECT.md)
requirements: [PERF-07]
autonomous: false  # operator deploys to prod, runs audit-mobile.sh, branches PASS/DEFER
must_haves:
  truths:
    - "D-06: audit target is production (`https://polls.wtcsmapban.com`), NOT the Netlify deploy preview, per D-13 single-run policy (Task 1)"
    - "D-07: on DEFER, append `## v1.3 Rerun` with `**v1.3 outcome: DEFER**`; UIDN-02 issue stays open; `.planning/PROJECT.md` row stays ⚠️ (decision branch — Task 3 defer → Task 4, skip Task 5)"
    - "D-08: on PASS, `**v1.3 outcome: PASS**`; `.planning/PROJECT.md` row flips ⚠️ → ✓; UIDN-02 closes via PR body `Closes #18` (decision branch — Task 3 pass → Tasks 4+5)"
    - "A new `## v1.3 Rerun (YYYY-MM-DD)` section is appended to `.planning/closure/UIDN-02-mobile-evidence.md` with per-route Lighthouse Performance scores for all 5 audited routes"
    - "On PASS (5/5 routes Perf ≥ 90): the `Mobile-first responsive design` Key Decision row at `.planning/PROJECT.md:263` flips ⚠️ → ✓, UIDN-02 issue #18 closed via PR body `Closes #18`"
    - "On DEFER (any route Perf < 90): row stays ⚠️ Revisit; UIDN-02 issue stays open; D-12 follow-up trigger remains the resolution path"
    - "Either PASS or DEFER is an acceptable outcome per D-12 + D-13 + D-05 — no Lighthouse re-runs"
  artifacts:
    - path: ".planning/closure/UIDN-02-mobile-evidence.md"
      provides: "Append new `## v1.3 Rerun (YYYY-MM-DD)` section with per-route table + verdict line"
      contains: "v1.3 Rerun"
    - path: ".planning/closure/UIDN-02-audit-mobile-stdout.txt"
      provides: "Captured stdout from `bash .planning/closure/audit-mobile.sh` including the `=== Summary ===` block + EXIT_CODE line (Phase 13 D-27 mktemp+bash-c+cp pattern). Lives directly under `.planning/closure/` (NOT inside `artifacts/lighthouse/`, which audit-mobile.sh:28 `rm -rf`'s) and uses a `.txt` extension (NOT `.log`, which `.gitignore:3` would ignore) so it is committable."
      contains: "=== Summary ==="
    - path: ".planning/PROJECT.md"
      provides: "Key Decision row `Mobile-first responsive design` (line 263) status flips ⚠️ → ✓ on PASS path only; untouched on DEFER path. NOTE: there is NO root-level PROJECT.md — the canonical Key Decisions table lives at .planning/PROJECT.md."
      contains: "Mobile-first responsive design"
  key_links:
    - from: ".planning/closure/UIDN-02-mobile-evidence.md § v1.3 Rerun"
      to: ".planning/PROJECT.md Key Decision row"
      via: "PASS verdict triggers row flip + Closes #18; DEFER verdict leaves row unchanged"
      pattern: "v1\\.3 outcome"
---

<objective>
After plans 01-06 merge and Netlify ships the production deploy, the operator runs `bash .planning/closure/audit-mobile.sh` against `https://polls.wtcsmapban.com`, captures the stdout + exit code to `.planning/closure/UIDN-02-audit-mobile-stdout.txt` (via the D-27 mktemp+cp pattern — see Task 2), appends a `## v1.3 Rerun (YYYY-MM-DD)` section to `.planning/closure/UIDN-02-mobile-evidence.md` with per-route scores, and branches on outcome:

- **PASS** (5/5 routes Performance ≥ 90 per D-05): flip the `Mobile-first responsive design` Key Decision row in `.planning/PROJECT.md` (line 263 — there is NO root-level PROJECT.md) from ⚠️ to ✓; close GitHub issue #18 via PR body `Closes #18` (D-08).
- **DEFER** (any route Performance < 90): row stays ⚠️ Revisit; UIDN-02 issue stays open; D-12 follow-up trigger remains the resolution path (D-07).

Both outcomes are acceptable per D-12 + D-13. There are no re-runs (D-13 single-run policy).

Purpose: Close UIDN-02 (or extend it to the next perf-budget change per D-12) with a single Lighthouse rerun on the production deploy. This is the final phase gate.

Output: One appended evidence section + one conditional `.planning/PROJECT.md` row flip + one captured stdout evidence file (`.planning/closure/UIDN-02-audit-mobile-stdout.txt`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-CONTEXT.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-RESEARCH.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-PATTERNS.md
@.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-UI-SPEC.md

@.planning/closure/audit-mobile.sh
@.planning/closure/UIDN-02-mobile-evidence.md

<interfaces>
<!-- Existing audit harness pinned at the .planning/closure/audit-mobile.sh head (verified 2026-05-27). -->
BASE_URL: https://polls.wtcsmapban.com
ROUTES: ["/", "/topics", "/archive", "/auth/error", "/admin"]
ROUTE_NAMES: ["home", "topics", "archive", "auth-error", "admin"]
THRESHOLDS: Perf=90, A11y=95, BP=95, SEO=90
Output dir: .planning/closure/artifacts/lighthouse/
Per-route artifacts: lh-mobile-{home,topics,archive,auth-error,admin}.report.{html,json} (gitignored; sha256-pinned in MANIFEST.json)
Script prints `=== Summary ===` block + accumulates failures across routes (no fail-fast); exits non-zero if any threshold missed.

D-05 PASS criterion: ALL 5 routes Performance ≥ 90. Anything less = DEFER (matches Phase 13 strict criterion).
D-06 audit target: production (https://polls.wtcsmapban.com), NOT a Netlify deploy preview.
D-13: single run per milestone — no retries.

Existing v1.2 Rerun section structure in UIDN-02-mobile-evidence.md (to mirror for v1.3 Rerun):
  ## v1.2 Rerun (2026-05-13)
  ### Per-route Lighthouse mobile scores
  | Route | Perf | A11y | BP | SEO |
  (5 rows)
  ### Verdict
  **v1.2 outcome: DEFER** (4/5 routes < 90)
  ### Cross-references
  - Phase 13 SHA + ROADMAP § Phase 13 + GitHub Issue #18

Phase 13 D-27 stdout-capture pattern (referenced from 13-evidence around line 122 — operator should reuse that mktemp+bash-c+cp form to capture audit-mobile.sh stdout exactly).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm Phase 16 PR is merged AND Netlify production deploy has shipped</name>
  <what-built>
    Plans 01-06 must be merged to `main` AND Netlify's production deploy must have rolled out to `https://polls.wtcsmapban.com` before the Lighthouse rerun. Per D-06 + D-13, the audit MUST hit the post-merge production URL — running against a deploy preview or against pre-merge `main` invalidates the measurement.
  </what-built>
  <how-to-verify>
    1. Confirm the Phase 16 PR is merged: `gh pr view <PR-NUMBER> --json state,mergeCommit | jq` — `state` must be `MERGED` and a `mergeCommit.oid` must exist.
    2. Confirm Netlify production deploy succeeded for that merge: visit Netlify dashboard → site → Production → confirm the deploy referencing the merge commit SHA is in `Published` state. (Or run `gh api repos/Esk3tit/wtcs-community-polls/deployments` and confirm the most recent production deployment is `success`.) **Fork note:** the canonical repo for this project is `Esk3tit/wtcs-community-polls` (verified against `git remote get-url origin` = `https://github.com/Esk3tit/wtcs-community-polls.git`). If the operator is running this command from a fork, `git remote get-url origin` will report a different owner/repo — substitute the canonical `Esk3tit/wtcs-community-polls` string in the `gh api` invocation before running (do NOT derive owner/repo dynamically; the canonical reference is the source of truth).
    3. Confirm production serves the post-Phase-16 bundle WITH the correct chunk-reachability invariant. CRITICAL (corrected from the original plan, which wrongly expected `vendor-posthog` in the initial HTML): the served `index.html` MUST reference `vendor-react` but MUST NOT reference `vendor-posthog`. `vendor-posthog` is consent-gated lazy (PERF-03/PERF-04) — its presence in the initial HTML would be a privacy/perf REGRESSION, not proof of deploy. Run:
       - `curl -sI https://polls.wtcsmapban.com/` returns 200.
       - `curl -s https://polls.wtcsmapban.com/ | grep -q 'vendor-react'` → MUST succeed (the React vendor chunk IS in the initial payload).
       - `curl -s https://polls.wtcsmapban.com/ | grep -q 'vendor-posthog'` → MUST FAIL (exit non-zero) — `vendor-posthog` is ABSENT from the initial HTML. If it IS present, STOP: the lazy gate regressed and the deploy is NOT correct; do not run the audit.
       Run the two greps under `bash -c` so exit codes are unambiguous: `bash -c 'curl -s https://polls.wtcsmapban.com/ | grep -q vendor-react && ! (curl -s https://polls.wtcsmapban.com/ | grep -q vendor-posthog) && echo DEPLOY_CHUNK_INVARIANT_OK'`.
    4. If any of (1)-(3) is not yet true, STOP — wait for the merge + deploy (or fix the lazy-gate regression if step 3's posthog assertion fails). Re-running the audit before production reflects the change would yield meaningless v1.3 scores.
  </how-to-verify>
  <resume-signal>Type "approved" once PR is merged, Netlify production deploy is `Published`, the served `index.html` references `vendor-react`, AND `vendor-posthog` is confirmed ABSENT from the initial HTML (lazy gate intact).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run `audit-mobile.sh` against production + capture stdout + interpret outcome</name>
  <files>.planning/closure/UIDN-02-audit-mobile-stdout.txt</files>
  <action>
    From the project root, run the existing Lighthouse mobile harness against production. Use the Phase 13 D-27 stdout-capture pattern: **tee to a `mktemp` file OUTSIDE the artifacts directory, then copy the complete log into place AFTER `audit-mobile.sh` finishes.** This is MANDATORY for two independent reasons:
    1. **`audit-mobile.sh:28` runs `rm -rf "$ARTIFACTS_DIR"` (where `$ARTIFACTS_DIR=.planning/closure/artifacts/lighthouse`) as its FIRST action.** Teeing directly into that directory races the cleanup: `tee` opens the log, the script unlinks that inode mid-run, the script writes its `=== Summary ===` into the freshly-recreated directory, and only a post-script append survives — destroying the per-route scores. The `mktemp` lives outside that directory, so it survives the `rm -rf`.
    2. **`${PIPESTATUS[0]}` is BASH-only.** The operator's interactive shell is zsh (the project's documented shell), where `$PIPESTATUS` is a 1-indexed array (`pipestatus[1]`) and `${PIPESTATUS[0]}` expands empty. Wrapping the pipeline in an explicit `bash -c` forces bash semantics regardless of the login shell.

    Invocation (shell-safe; do NOT substitute the BANNED naive `tee … | EXIT_CODE` form that races the `rm -rf` and is zsh-incompatible):
    ```
    tmp_log="$(mktemp)"
    bash -c 'bash .planning/closure/audit-mobile.sh 2>&1 | tee "$1"; exit "${PIPESTATUS[0]}"' _ "$tmp_log"
    audit_status=$?
    printf 'EXIT_CODE=%s\n' "$audit_status" >> "$tmp_log"
    cp "$tmp_log" .planning/closure/UIDN-02-audit-mobile-stdout.txt
    ```
    Notes on this exact form:
    - `mktemp` writes OUTSIDE the cleaned `$ARTIFACTS_DIR`, so the captured stdout survives `audit-mobile.sh:28`'s `rm -rf`.
    - `bash -c '... | tee "$1"; exit "${PIPESTATUS[0]}"' _ "$tmp_log"` runs the pipeline under bash — `${PIPESTATUS[0]}` is the bash-indexed exit code of the audit script (not `tee`). The `_` is `$0`; `"$tmp_log"` is `$1`.
    - `audit_status=$?` captures the wrapper's true exit (= the script's exit). Do NOT name this variable `status` — `status` is a read-only special variable in zsh and the assignment would error if this block ever runs under zsh.
    - The final `cp` lands the COMPLETE log (Summary block + all per-route lines + the appended `EXIT_CODE=` line) at `.planning/closure/UIDN-02-audit-mobile-stdout.txt` — a `.txt` (NOT `.log`, which `.gitignore:3` ignores) directly under `.planning/closure/` (NOT inside `artifacts/lighthouse/`, which `.gitignore:49` ignores AND the script deletes). This path IS git-trackable; commit it normally (no `git add -f` needed — verified with `git check-ignore`).
    The script audits 5 routes (home, topics, archive, auth-error, admin), prints a `=== Summary ===` block with `PASS/FAIL <route>: P=NN A=NN BP=NN SEO=NN` lines, and exits 0 on all-routes-pass, non-zero on any threshold miss. Do NOT re-run if a route misses — per D-13 single-run policy, the first outcome is the recorded outcome.

    Parse the `=== Summary ===` block to determine the outcome (see Task 3 for the branch decision). Capture per-route Performance scores in a structured table for the evidence file. Note: A11y / BP / SEO are reported by the script but are EXPLICITLY out of scope for the PASS/DEFER decision per CONTEXT D-05 and UI-SPEC § Performance Budget — only Perf ≥ 90 across all 5 routes matters for the verdict.

    Per-route Lighthouse artifacts (HTML + JSON under `.planning/closure/artifacts/lighthouse/lh-mobile-*.report.{html,json}`) are gitignored per the existing audit-mobile.sh convention (and are deleted at the next run by `audit-mobile.sh:28`); only the captured `.planning/closure/UIDN-02-audit-mobile-stdout.txt` is committed.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c "F=.planning/closure/UIDN-02-audit-mobile-stdout.txt; test -f \"\$F\" && grep -q '=== Summary ===' \"\$F\" && [ \$(grep -cE '^(PASS|FAIL) (home|topics|archive|auth-error|admin):' \"\$F\") -eq 5 ] && grep -qE '^EXIT_CODE=[0-9]+\$' \"\$F\" && ! git check-ignore -q \"\$F\""</automated>
  </verify>
  <done>
    1. `.planning/closure/UIDN-02-audit-mobile-stdout.txt` exists, is NOT gitignored (`git check-ignore` returns nothing), and contains the `=== Summary ===` block with 5 per-route lines — i.e. the full audit output survived `audit-mobile.sh:28`'s `rm -rf` because it was teed to a `mktemp` outside that directory.
    2. The captured `EXIT_CODE=<n>` line is present (matches `^EXIT_CODE=[0-9]+$`) and reflects the script's exit code (captured via the bash-c `${PIPESTATUS[0]}` wrapper, so it is correct even when the operator shell is zsh).
    3. Operator has decided PASS (5/5 Perf ≥ 90) or DEFER (any route Perf < 90) based on the per-route Performance numbers — recorded in the next task.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Branch on PASS vs DEFER outcome</name>
  <decision>
    Based on the per-route Performance scores captured in Task 2's stdout log, choose between:
    - **PASS path (D-08):** ALL 5 routes' Performance ≥ 90. Plan continues to Task 4 (append evidence as PASS) + Task 5 (flip the `.planning/PROJECT.md` row + close issue #18 in the PR).
    - **DEFER path (D-07):** ANY route's Performance < 90 (even a single route at 89 = DEFER per D-05 strict criterion). Plan continues to Task 4 (append evidence as DEFER) + SKIP Task 5 (`.planning/PROJECT.md` untouched).
  </decision>
  <context>
    Per D-12, DEFER is an acceptable outcome — not a failure. The strict 5/5 PASS criterion (matching Phase 13) preserves the original UIDN-02 closure intent; DEFER keeps the row at ⚠️ Revisit until the next perf-budget change triggers another rerun. No retries per D-13.
  </context>
  <options>
    <option id="pass">
      <name>PASS — all 5 routes Performance ≥ 90</name>
      <pros>UIDN-02 closes; `.planning/PROJECT.md` Key Decision row (line 263) flips ⚠️ → ✓; GitHub issue #18 closes via PR body keyword.</pros>
      <cons>None — this is the success path.</cons>
    </option>
    <option id="defer">
      <name>DEFER — any route Performance &lt; 90</name>
      <pros>Honest accounting per D-12; future perf-budget phase triggers next rerun.</pros>
      <cons>UIDN-02 stays open; ⚠️ Revisit remains in `.planning/PROJECT.md`.</cons>
    </option>
  </options>
  <resume-signal>Select: `pass` or `defer` (matching the per-route scores in `.planning/closure/UIDN-02-audit-mobile-stdout.txt`).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Append `## v1.3 Rerun (YYYY-MM-DD)` section to UIDN-02-mobile-evidence.md (both PASS + DEFER branches)</name>
  <files>.planning/closure/UIDN-02-mobile-evidence.md</files>
  <action>
    Append a new section to `.planning/closure/UIDN-02-mobile-evidence.md`. Place it AFTER the existing `## v1.2 Rerun (2026-05-13)` section (whichever section is currently last in the file). The new section's structure mirrors v1.2 verbatim:

    Heading: `## v1.3 Rerun (YYYY-MM-DD)` — replace YYYY-MM-DD with the actual date the audit was run.

    ### Per-route Lighthouse mobile scores
    A 5-row table with columns `Route | Perf | A11y | BP | SEO`:
    | Route | Perf | A11y | BP | SEO |
    |-------|------|------|----|-----|
    | home | NN | NN | NN | NN |
    | topics | NN | NN | NN | NN |
    | archive | NN | NN | NN | NN |
    | auth-error | NN | NN | NN | NN |
    | admin | NN | NN | NN | NN |
    Values pulled from `.planning/closure/UIDN-02-audit-mobile-stdout.txt`'s `=== Summary ===` block.

    ### Breakpoint matrix
    Single-line note: "Screenshot matrix unchanged from v1.2 — this phase reruns Lighthouse only per D-13 (single Lighthouse rerun per milestone). No Playwright screenshot capture in Phase 16."

    ### Verdict
    For PASS: `**v1.3 outcome: PASS**` on its own line (bold).
    For DEFER: `**v1.3 outcome: DEFER**` on its own line (bold), followed by a one-paragraph rationale naming which routes missed the 90 threshold and by how much. Per D-07, this defers the row update; D-12 follow-up trigger remains.

    ### Cross-references
    Bullet list with:
    - Phase 16 merge commit SHA (from Task 1 step 1's `gh pr view` output)
    - `.planning/ROADMAP.md § Phase 16: UIDN-02 Aggressive Perf-Budget Pass`
    - GitHub Issue #18 (state = closed on PASS; state = open on DEFER)

    The file's frontmatter at the top is UNCHANGED. Only this new section is appended.
  </action>
  <verify>
    <automated>bash -o pipefail -e -c "grep -q '## v1.3 Rerun' .planning/closure/UIDN-02-mobile-evidence.md && grep -E '\*\*v1\.3 outcome: (PASS|DEFER)\*\*' .planning/closure/UIDN-02-mobile-evidence.md | head -1 && tail -40 .planning/closure/UIDN-02-mobile-evidence.md"</automated>
  </verify>
  <done>
    1. `.planning/closure/UIDN-02-mobile-evidence.md` carries a new `## v1.3 Rerun (YYYY-MM-DD)` section appended after the existing v1.2 section.
    2. The section contains per-route scores + the breakpoint-matrix note + the bold verdict line (PASS or DEFER per Task 3's selection) + cross-references.
    3. The file's frontmatter is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 5: ON PASS PATH ONLY — flip `.planning/PROJECT.md` row ⚠️ → ✓ + close issue #18 in PR body</name>
  <files>.planning/PROJECT.md</files>
  <action>
    **EXECUTE THIS TASK ONLY IF Task 3 SELECTED `pass`.** On DEFER path, SKIP this task entirely — `.planning/PROJECT.md` stays untouched.

    CRITICAL PATH NOTE (corrected from the original plan): the Key Decisions table lives at `.planning/PROJECT.md` (line 263) — there is NO root-level `PROJECT.md` (`ls PROJECT.md` → No such file). Editing a bare `PROJECT.md` would create a stray root file while the real ⚠️ row never flips, silently failing the phase's headline deliverable. ALL edits in this task target `.planning/PROJECT.md`.

    PASS path actions:
    (a) Edit `.planning/PROJECT.md`. Locate the Key Decisions table row for `Mobile-first responsive design` at line 263. The status cell currently reads `⚠️ Revisit (v1.2 rerun — 4/5 routes under threshold; follow-up tied to next perf-budget change per D-12; see .planning/closure/UIDN-02-mobile-evidence.md § v1.2 Rerun)`. On PASS, change the leading status glyph from `⚠️ Revisit` to `✓` and update the parenthetical to reference the v1.3 rerun PASS (e.g. `✓ (v1.3 rerun — 5/5 routes Perf ≥ 90; see .planning/closure/UIDN-02-mobile-evidence.md § v1.3 Rerun)`). No other table cells touched.
    (b) The closure PR body for this phase MUST include the keyword `Closes #18` to auto-close GitHub Issue #18 (the UIDN-02 tracking issue) on merge. If this plan is being run as a follow-up PR after the main Phase 16 PR already merged, the operator opens a tiny closure PR with just the `.planning/PROJECT.md` + evidence-file changes and includes `Closes #18` in that PR's body.
    (c) On DEFER path (skipped here): UIDN-02 issue #18 stays open; the `D-12` follow-up trigger (next perf-budget change re-runs the Lighthouse audit) remains the resolution path; the `.planning/PROJECT.md` row stays ⚠️ Revisit.
  </action>
  <verify>
    <automated>bash -o pipefail -c 'if grep -q "\*\*v1\.3 outcome: PASS\*\*" .planning/closure/UIDN-02-mobile-evidence.md; then grep -E "Mobile-first responsive design.*✓" .planning/PROJECT.md && echo PROJECT_ROW_FLIPPED_OK; else echo DEFER_PATH_PROJECT_UNCHANGED && grep -E "Mobile-first responsive design.*⚠️" .planning/PROJECT.md; fi'</automated>
  </verify>
  <done>
    1. If Task 3 selected `pass`: `.planning/PROJECT.md` `Mobile-first responsive design` row (line 263) status is `✓`; NO stray root-level `PROJECT.md` was created (`ls PROJECT.md` still errors); the closure PR body includes `Closes #18`; GitHub issue #18 is `state: closed` after PR merge (verified via `gh issue view 18`).
    2. If Task 3 selected `defer`: `.planning/PROJECT.md` is UNCHANGED; GitHub issue #18 remains `state: open`; the SUMMARY documents the routes that missed the threshold + their actual Perf scores; D-12 follow-up trigger explicitly named.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production deploy (Netlify) → Lighthouse measurement | The audit runs against the live production URL. Network conditions, CDN cache state, and Netlify edge POP all influence Lighthouse scores — these are uncontrollable variables. D-13 single-run policy explicitly accepts this volatility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-20 | Repudiation | Future maintainer disputes the v1.3 Lighthouse outcome — was it PASS or DEFER? | mitigate | Captured `.planning/closure/UIDN-02-audit-mobile-stdout.txt` (committed — a git-trackable path outside the gitignored/`rm -rf`-ed `artifacts/lighthouse/` tree) is the canonical evidence. The `=== Summary ===` block + `EXIT_CODE=` line are byte-traceable. |
| T-16-21 | Tampering | Operator runs the audit against the deploy preview (not production) by accident → invalidates the v1.3 measurement | mitigate | Task 1 verifies `curl -sI https://polls.wtcsmapban.com/` returns 200 and serves the post-Phase-16 bundle (vendor-react present, vendor-posthog ABSENT) BEFORE the audit runs. The audit-mobile.sh script's BASE_URL is pinned to production. Task 1 is a blocking checkpoint. |
| T-16-23 | Tampering (silent deliverable failure) | The Key Decision row edit targets a non-existent root `PROJECT.md` instead of `.planning/PROJECT.md` → a stray root file is created, the real ⚠️ row never flips, and the phase's headline deliverable silently fails | mitigate | ALL PROJECT.md references in this plan are now `.planning/PROJECT.md` (line 263 is the canonical row). Task 5 done-criterion explicitly asserts no stray root `PROJECT.md` was created AND the `.planning/PROJECT.md` row shows ✓. The path was verified against the live repo (`ls PROJECT.md` → No such file; `.planning/PROJECT.md:263` holds the row). |
| T-16-22 | Information disclosure | Lighthouse audit artifacts (per-route HTML/JSON) committed and pushed → may surface internal route shape to public repo | accept | Per-route HTML+JSON are gitignored per the existing audit-mobile.sh convention. Only `.planning/closure/UIDN-02-audit-mobile-stdout.txt` is committed, which contains only score numbers and route names (all already public). |

No new auth/access-control surface touched by this plan. Inherits all prior-plan controls.
</threat_model>

<verification>
1. Task 1 confirmed: Phase 16 PR is merged; Netlify production deploy is `Published`; production `index.html` references `vendor-react` AND does NOT reference `vendor-posthog` (lazy gate intact — the corrected chunk-reachability invariant).
2. `bash .planning/closure/audit-mobile.sh` ran exactly ONCE against production (captured via the D-27 mktemp + `bash -c` + `cp` pattern: teed to a `mktemp` outside `artifacts/lighthouse/` so it survives `audit-mobile.sh:28`'s `rm -rf`, exit code captured via the bash-c `${PIPESTATUS[0]}` wrapper so it is correct under zsh, then copied to the trackable `.planning/closure/UIDN-02-audit-mobile-stdout.txt`, which is committed — `git check-ignore` confirms it is NOT ignored).
3. `.planning/closure/UIDN-02-mobile-evidence.md` carries a new `## v1.3 Rerun (YYYY-MM-DD)` section with the 5-row score table + verdict line + cross-references.
4. On PASS path: `.planning/PROJECT.md` Key Decision row `Mobile-first responsive design` (line 263) shows `✓`; no stray root `PROJECT.md` exists; PR body `Closes #18`; `gh issue view 18` reports `state: closed`.
5. On DEFER path: `.planning/PROJECT.md` unchanged; `gh issue view 18` reports `state: open`; SUMMARY names which routes missed Perf ≥ 90 and by how much.
6. No re-runs attempted (D-13 single-run policy).
</verification>

<success_criteria>
- Audit ran against production after the Phase 16 PR merged + Netlify deployed (verified vendor-react present + vendor-posthog absent from initial HTML).
- Stdout captured (D-27 mktemp + `bash -c` + `cp`: survives `audit-mobile.sh:28` `rm -rf`, shell-agnostic exit-code capture) + committed as the git-trackable `.planning/closure/UIDN-02-audit-mobile-stdout.txt`.
- `UIDN-02-mobile-evidence.md` v1.3 Rerun section appended with the verdict (PASS or DEFER) explicitly stated.
- On PASS: `.planning/PROJECT.md` row (line 263) flipped to ✓ + #18 closed via PR keyword; no stray root PROJECT.md created.
- On DEFER: `.planning/PROJECT.md` unchanged + #18 stays open + D-12 follow-up trigger documented.
- Both outcomes are acceptable per D-12; the phase is ship-able under either branch.
</success_criteria>

<output>
Create `.planning/phases/16-uidn-02-aggressive-perf-budget-pass/16-07-SUMMARY.md` documenting: the merge commit SHA + Netlify deploy ID; the Task-1 deploy chunk-invariant result (vendor-react present, vendor-posthog absent from initial HTML); the exact per-route scores from the captured stdout log; the chosen branch (PASS or DEFER) and rationale (which routes missed if any); whether `.planning/PROJECT.md` (line 263) was flipped to ✓ or left ⚠️ (and confirm no stray root PROJECT.md was created); the `gh issue view 18` state after PR merge; the audit run date. List `requirements-completed: [PERF-07]` in frontmatter. The SUMMARY closes Phase 16.
</output>
</content>
