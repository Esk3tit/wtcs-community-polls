---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - eslint.config.js
  - components.json
  - src/index.css
  - src/lib/utils.ts
  - .env.example
  - .gitignore
  - public/_redirects
  - src/test/setup.ts
autonomous: true
requirements:
  - UIDN-03
  - INFR-01
  - TEST-01

must_haves:
  truths:
    - "npm run dev starts without errors"
    - "npx vitest run executes successfully (0 tests, no config errors)"
    - "shadcn/ui components can be added via npx shadcn@latest add"
    - "Tailwind CSS v4 classes are applied in the browser"
    - "Path alias @ resolves to src/ directory"
    - "TanStack Router generates routeTree.gen.ts from src/routes/ files"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vite config with TanStack Router, React, Tailwind v4 plugins + Vitest test block"
      contains: "tanstackRouter"
    - path: "components.json"
      provides: "shadcn/ui configuration for Maia preset"
      contains: "aliases"
    - path: "src/index.css"
      provides: "Tailwind v4 import + shadcn Maia/Neutral CSS variables for light and dark modes"
      contains: "@import \"tailwindcss\""
    - path: "src/lib/utils.ts"
      provides: "cn() utility combining clsx + tailwind-merge"
      exports: ["cn"]
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "VITE_SUPABASE_URL"
    - path: "public/_redirects"
      provides: "Netlify SPA routing catch-all"
      contains: "/* /index.html 200"
    - path: "src/test/setup.ts"
      provides: "Vitest setup file with RTL matchers"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "vite.config.ts"
      to: "src/routes/"
      via: "TanStack Router plugin generates routeTree.gen.ts"
      pattern: "tanstackRouter"
    - from: "vite.config.ts"
      to: "src/index.css"
      via: "Tailwind CSS v4 Vite plugin processes CSS"
      pattern: "tailwindcss"
    - from: "tsconfig.app.json"
      to: "src/"
      via: "path alias @/* resolves to src/*"
      pattern: "paths.*@/"

user_setup:
  - service: supabase
    why: "Database, auth, and RLS policies"
    env_vars:
      - name: VITE_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: VITE_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon/public key"
    dashboard_config:
      - task: "Create a new Supabase project (or use existing)"
        location: "Supabase Dashboard -> New Project"
      - task: "Enable Discord OAuth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Discord"
      - task: "Set Discord Client ID and Client Secret"
        location: "Supabase Dashboard -> Authentication -> Providers -> Discord (get from Discord Developer Portal -> Applications -> OAuth2)"
      - task: "Set redirect URL to http://localhost:5173/auth/callback for local dev"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs"
---

<objective>
Set up the complete project infrastructure: install all dependencies, configure Vite with TanStack Router + Tailwind CSS v4 + Vitest, initialize shadcn/ui with the Maia/Neutral preset, configure path aliases, ESLint, environment template, and Netlify SPA routing.

Purpose: Every subsequent plan depends on having packages installed, build tooling configured, and the design system initialized. This plan creates the foundation that all other plans build on.
Output: A fully configured project that runs `npm run dev` and `npx vitest run` without errors, with shadcn/ui ready to add components.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN-SYSTEM.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Current state of files this plan modifies -->

From vite.config.ts (current):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
})
```

From package.json (current):
```json
{
  "name": "wtcs-community-polls",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
```

From tsconfig.app.json (current):
```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "esnext",
    "types": ["vite/client"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
```

From eslint.config.js (current):
```javascript
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      ecmaVersion: 2020,
      globals: globals.browser,
    },
  },
])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure build tooling</name>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, tsconfig.app.json, eslint.config.js, .env.example, .gitignore, public/_redirects</files>
  <read_first>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, eslint.config.js, .gitignore</read_first>
  <action>
1. Install all production dependencies:
```bash
npm install @supabase/supabase-js @tanstack/react-router lucide-react clsx tailwind-merge class-variance-authority sonner
```

2. Install Tailwind CSS v4 with Vite plugin:
```bash
npm install tailwindcss @tailwindcss/vite
```

3. Install all dev dependencies:
```bash
npm install -D @tanstack/router-plugin @tanstack/react-router-devtools vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom supabase
```

4. Rewrite `vite.config.ts` to include all three plugins in the correct order (TanStack Router BEFORE React, per Pitfall 4), plus Vitest test config, plus path alias:
```typescript
import path from 'path'
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { TanStackRouterVite } from '@tanstack/router-plugin/vite'

export default defineConfig({
  plugins: [
    TanStackRouterVite({
      target: 'react',
      autoCodeSplitting: true,
    }),
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/setup.ts',
    css: true,
  },
})
```

5. Update `tsconfig.app.json` to add path alias `@/*` -> `src/*`:
Add to compilerOptions:
```json
"baseUrl": ".",
"paths": {
  "@/*": ["./src/*"]
}
```

6. Update `tsconfig.json` to add `"references"` if not present, ensuring both app and node configs are referenced. Keep existing structure but verify references array includes both tsconfig.app.json and tsconfig.node.json.

7. Update `eslint.config.js` to add `routeTree.gen.ts` to global ignores:
Change `globalIgnores(['dist'])` to `globalIgnores(['dist', 'src/routeTree.gen.ts'])`.

8. Create `.env.example`:
```
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

9. Update `.gitignore` to add `.env.local` and `.env` (ensure env files with secrets are ignored). Also add `src/routeTree.gen.ts` to gitignore since it is auto-generated.

10. Create `public/_redirects` with Netlify SPA catch-all:
```
/*    /index.html   200
```

11. Add test scripts to `package.json`:
- `"test": "vitest run"`
- `"test:watch": "vitest"`
- `"test:coverage": "vitest run --coverage"`

12. Create an empty `src/routes/` directory with a placeholder `.gitkeep` file so TanStack Router plugin has its routes directory.
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -20 && echo "---" && node -e "const pkg = require('./package.json'); console.log('supabase-js:', !!pkg.dependencies['@supabase/supabase-js']); console.log('tanstack-router:', !!pkg.dependencies['@tanstack/react-router']); console.log('tailwindcss:', !!pkg.dependencies['tailwindcss']); console.log('vitest:', !!pkg.devDependencies['vitest']);"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include `@supabase/supabase-js`, `@tanstack/react-router`, `tailwindcss`, `@tailwindcss/vite`, `lucide-react`, `clsx`, `tailwind-merge`, `class-variance-authority`, `sonner`
    - package.json devDependencies include `@tanstack/router-plugin`, `@tanstack/react-router-devtools`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `jsdom`, `supabase`
    - package.json scripts include `"test": "vitest run"` and `"test:watch": "vitest"`
    - vite.config.ts contains `TanStackRouterVite` BEFORE `react()` in plugins array
    - vite.config.ts contains `tailwindcss()` in plugins array
    - vite.config.ts contains `test:` block with `environment: 'jsdom'` and `setupFiles: './src/test/setup.ts'`
    - vite.config.ts contains `'@': path.resolve(__dirname, './src')`
    - tsconfig.app.json contains `"@/*": ["./src/*"]` in paths
    - eslint.config.js contains `routeTree.gen.ts` in ignores
    - .env.example contains `VITE_SUPABASE_URL=` and `VITE_SUPABASE_ANON_KEY=`
    - .gitignore contains `.env.local`
    - public/_redirects contains `/* /index.html 200`
    - src/routes/ directory exists
  </acceptance_criteria>
  <done>All dependencies installed, Vite configured with TanStack Router + Tailwind v4 + Vitest, path aliases working, ESLint ignoring generated files, env template and Netlify routing in place.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui with Maia preset and configure test setup</name>
  <files>components.json, src/index.css, src/lib/utils.ts, src/test/setup.ts</files>
  <read_first>vite.config.ts, tsconfig.app.json, src/index.css, .planning/phases/01-foundation-authentication/01-UI-SPEC.md, .planning/DESIGN-SYSTEM.md</read_first>
  <action>
1. Initialize shadcn/ui. Run:
```bash
npx shadcn@latest init
```
When prompted, select:
- Style: New York (then apply Maia preset CSS)
- Base color: Neutral
- CSS variables: Yes
- Use `src/lib/utils.ts` path

If the CLI does not support interactive mode (non-TTY), create the files manually:

2. Create `components.json` (shadcn configuration):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/index.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

3. Create `src/lib/utils.ts`:
```typescript
import { clsx, type ClassValue } from 'clsx'
import { twMerge } from 'tailwind-merge'

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

4. Replace `src/index.css` entirely with Tailwind v4 import + shadcn Maia/Neutral preset CSS variables. The file must start with `@import "tailwindcss";` (Tailwind v4 syntax, NOT the v3 `@tailwind base/components/utilities` directives). Then include the full shadcn CSS custom properties for BOTH `:root` (light mode) and `.dark` (dark mode) using the Neutral base color from the Maia preset (preset ID: bbVJxbc).

The CSS structure must be:
```css
@import "tailwindcss";

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 0 0% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 0 0% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 0 0% 3.9%;
    --primary: 0 0% 9%;
    --primary-foreground: 0 0% 98%;
    --secondary: 0 0% 96.1%;
    --secondary-foreground: 0 0% 9%;
    --muted: 0 0% 96.1%;
    --muted-foreground: 0 0% 45.1%;
    --accent: 0 0% 96.1%;
    --accent-foreground: 0 0% 9%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 89.8%;
    --input: 0 0% 89.8%;
    --ring: 0 0% 3.9%;
    --chart-1: 12 76% 61%;
    --chart-2: 173 58% 39%;
    --chart-3: 197 37% 24%;
    --chart-4: 43 74% 66%;
    --chart-5: 27 87% 67%;
    --radius: 0.5rem;
    --sidebar-background: 0 0% 98%;
    --sidebar-foreground: 240 5.3% 26.1%;
    --sidebar-primary: 240 5.9% 10%;
    --sidebar-primary-foreground: 0 0% 98%;
    --sidebar-accent: 240 4.8% 95.9%;
    --sidebar-accent-foreground: 240 5.9% 10%;
    --sidebar-border: 220 13% 91%;
    --sidebar-ring: 217.2 91.2% 59.8%;
  }

  .dark {
    --background: 0 0% 3.9%;
    --foreground: 0 0% 98%;
    --card: 0 0% 3.9%;
    --card-foreground: 0 0% 98%;
    --popover: 0 0% 3.9%;
    --popover-foreground: 0 0% 98%;
    --primary: 0 0% 98%;
    --primary-foreground: 0 0% 9%;
    --secondary: 0 0% 14.9%;
    --secondary-foreground: 0 0% 98%;
    --muted: 0 0% 14.9%;
    --muted-foreground: 0 0% 63.9%;
    --accent: 0 0% 14.9%;
    --accent-foreground: 0 0% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 0 0% 98%;
    --border: 0 0% 14.9%;
    --input: 0 0% 14.9%;
    --ring: 0 0% 83.1%;
    --chart-1: 220 70% 50%;
    --chart-2: 160 60% 45%;
    --chart-3: 30 80% 55%;
    --chart-4: 280 65% 60%;
    --chart-5: 340 75% 55%;
    --sidebar-background: 240 5.9% 10%;
    --sidebar-foreground: 240 4.8% 95.9%;
    --sidebar-primary: 224.3 76.3% 48%;
    --sidebar-primary-foreground: 0 0% 100%;
    --sidebar-accent: 240 3.7% 15.9%;
    --sidebar-accent-foreground: 240 4.8% 95.9%;
    --sidebar-border: 240 3.7% 15.9%;
    --sidebar-ring: 217.2 91.2% 59.8%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
    font-family: Inter, ui-sans-serif, system-ui, sans-serif;
  }
}
```

Note: The exact CSS variable values should come from the shadcn init or the Maia/Neutral preset. If `npx shadcn@latest init` generates these automatically, use its output. The above are the standard Neutral preset values -- the init command may generate slightly different values for the Maia style. Use whatever the CLI generates, or if manual, use the values above which are the official Neutral preset.

5. Create `src/test/setup.ts`:
```typescript
import '@testing-library/jest-dom/vitest'
```

6. Delete the old CSS files that will be replaced:
- Delete `src/App.css` (replaced by Tailwind utility classes)
- Delete the contents of `src/App.tsx` temporarily (replace with a minimal component that just renders a div so the build does not break):
```tsx
function App() {
  return <div>WTCS Community Polls</div>
}

export default App
```

7. Remove old asset imports from `src/main.tsx` if they reference deleted CSS. Keep it minimal:
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

Note: main.tsx will be rewritten in Plan 03 to use RouterProvider. For now, keep it working with the basic App component.

8. Add the four Phase 1 shadcn components:
```bash
npx shadcn@latest add button card dropdown-menu sheet sonner
```
This will create files in `src/components/ui/` for each component and install any missing Radix UI dependencies automatically.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5 && echo "---" && test -f src/lib/utils.ts && echo "utils.ts exists" && test -f components.json && echo "components.json exists" && test -f src/test/setup.ts && echo "setup.ts exists" && grep -c "@import" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - src/index.css starts with `@import "tailwindcss";` (NOT `@tailwind base`)
    - src/index.css contains `:root {` with `--background:` and `--foreground:` CSS variables
    - src/index.css contains `.dark {` with dark mode CSS variables
    - src/index.css body rule includes `font-family: Inter`
    - src/lib/utils.ts exports `cn` function using `clsx` and `twMerge`
    - components.json contains `"style": "new-york"` and `"baseColor": "neutral"`
    - components.json aliases include `"ui": "@/components/ui"` and `"utils": "@/lib/utils"`
    - src/components/ui/button.tsx exists (shadcn Button component)
    - src/components/ui/card.tsx exists (shadcn Card component)
    - src/components/ui/dropdown-menu.tsx exists (shadcn DropdownMenu component)
    - src/components/ui/sheet.tsx exists (shadcn Sheet component)
    - src/test/setup.ts contains `@testing-library/jest-dom/vitest`
    - `npx vitest run` exits without configuration errors
    - src/App.css is deleted
  </acceptance_criteria>
  <done>shadcn/ui initialized with Maia/Neutral preset, all Phase 1 shadcn components added, Tailwind v4 CSS configured with light/dark theme variables, Vitest test setup file created, old CSS removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local | Contains Supabase anon key -- public but should not be committed |
| public/_redirects | Netlify routing config -- wrong config could expose raw files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-00a | Information Disclosure | .env.local | mitigate | .gitignore includes `.env.local` and `.env` to prevent secret commit |
| T-1-00b | Tampering | public/_redirects | accept | Netlify _redirects is a standard SPA pattern; no sensitive routing at this stage |
</threat_model>

<verification>
1. `npm run dev` starts Vite dev server without errors
2. `npx vitest run` completes without configuration errors (0 tests OK)
3. `npm run build` succeeds (tsc + vite build)
4. `src/components/ui/button.tsx` exists (shadcn installed)
5. TanStack Router plugin generates `src/routeTree.gen.ts` when route files exist in `src/routes/`
</verification>

<success_criteria>
- All 15+ npm packages installed and lockfile committed
- Vite config has TanStack Router, React, and Tailwind v4 plugins in correct order
- shadcn/ui initialized with Neutral preset, 4 UI components added
- Tailwind v4 CSS with light/dark theme variables in src/index.css
- Vitest configured with jsdom and RTL setup file
- Path alias @ -> src/ working in TypeScript
- .env.example documents required Supabase environment variables
- Netlify _redirects handles SPA routing
- ESLint ignores auto-generated routeTree.gen.ts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
