---
phase: 18-test-environment-repair
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - TEST-17

must_haves:
  truths:
    - "Both `supabase/setup-cli@v2` steps in ci.yml use the same exact pinned version"
    - "The pinned CLI version bundles edge-runtime >= v1.74.0"
    - "The CI `test-integration` job passes with 0 failures and 0 skips after the bump"
    - "No `401 Unauthorized` errors appear in integration test output on CI"
    - "`[functions.*] verify_jwt = false` entries in config.toml remain intact (not touched by this plan)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pinned Supabase CLI version for both CI jobs"
      contains: "supabase/setup-cli@v2"
  key_links:
    - from: ".github/workflows/ci.yml supabase/setup-cli@v2 version"
      to: "supabase/edge-runtime Docker image bundled per CLI release"
      via: "CLI Dockerfile template"
      pattern: "version: [0-9]+\\.[0-9]+\\.[0-9]+"
---

<objective>
Bump the Supabase CLI version pin in `.github/workflows/ci.yml` from `2.92.1` to the latest stable release that bundles `supabase/edge-runtime:v1.74.0`, fixing the ES256 JWT-verification bug in the local gateway layer.

Purpose: CLI v2.92.1 bundles `supabase/edge-runtime:v1.73.3` which has the ES256 fallback-to-HS256 bug described in `supabase/config.toml` lines 24–30. The edge-runtime v1.74.0 (released 2026-04-30) fixes this. Since the edge-runtime image version is bundled per CLI release (no independent pin mechanism), the fix is to advance the CLI version. The executor MUST resolve the exact latest-stable CLI semver at execution time from `https://github.com/supabase/cli/releases` — do not use a guessed literal.

Output: Both `supabase/setup-cli@v2` steps in `ci.yml` updated to the same exact resolved version. Full integration suite green in CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-test-environment-repair/18-CONTEXT.md
@.planning/phases/18-test-environment-repair/18-RESEARCH.md
@.planning/phases/18-test-environment-repair/18-PATTERNS.md
@.planning/phases/18-test-environment-repair/18-VALIDATION.md
@.planning/phases/18-test-environment-repair/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve latest stable CLI version bundling edge-runtime v1.74.0</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — locate BOTH `supabase/setup-cli@v2` steps. Per 18-PATTERNS.md lines 71–85: the first is at lines 60–62 (test-integration job, preceded by an anchor comment at line 59 "Same pinned Supabase CLI version as the e2e job"); the second is at lines 128–130 (e2e job, with a comment about D-16 §1). Both must be updated to the same version string.
    - `18-RESEARCH.md` § Q1 and Assumptions A1 — confirms the specific CLI semver is ASSUMED and must be verified at execution time. Also records that CLI v2.99.0 was latest as of research date (2026-05-20); verify the current latest stable release is >= v2.99.0 and bundles edge-runtime v1.74.0.
    - `18-PATTERNS.md` "Exact CLI version pin pattern" section — the exact YAML structure to match; both step occurrences get the same `version: X.Y.Z` value, never a range, never `latest`.
  </read_first>
  <action>
    Step 1 — Resolve the CLI version. Fetch `https://github.com/supabase/cli/releases` (or use `gh release list --repo supabase/cli --limit 10`) to identify the latest stable release after v2.92.1 that bundles `supabase/edge-runtime:v1.74.0` or later. Research identified v2.99.0 as a candidate as of 2026-05-20; check if a newer stable release exists as of execution date. Record the resolved version.

    Step 2 — Update both `supabase/setup-cli@v2` steps in `.github/workflows/ci.yml` to the resolved version. The two locations are: (a) the `test-integration` job step at lines ~60–62, and (b) the `e2e` job step at lines ~128–130. Replace only the `version:` value on those lines. Do not change the step structure, the surrounding `env:` blocks, the `PGOPTIONS: -c app.e2e_seed_allowed=true` guard, or any other CI steps.

    Step 3 — Verify via `git diff .github/workflows/ci.yml` that exactly two `version:` lines changed and nothing else.

    Optional but recommended: also update `deploy-edge-functions.yml` if it contains a `supabase/setup-cli@v2` step pinned to the old version, for consistency.
  </action>
  <verify>
    <automated>npm run test:integration</automated>
  </verify>
  <acceptance_criteria>
    - Both `supabase/setup-cli@v2 with: version:` entries in `ci.yml` contain the same resolved version string (not `2.92.1`)
    - The resolved version is >= the version known to bundle `edge-runtime:v1.74.0`
    - `npm run test:integration` exits 0 locally (full suite, 0 failures, 0 skips) — confirms the same CLI version works locally when applied
    - `git diff .github/workflows/ci.yml` shows exactly the two version line changes and no other diff
    - CI `test-integration` job passes on push with 0 skip / 0 fail (manual CI check after PR push — see VALIDATION.md manual-only verification row)
  </acceptance_criteria>
  <done>Both `setup-cli@v2` version pins in ci.yml updated to the same resolved stable version bundling edge-runtime >= v1.74.0; full integration suite exits 0 locally</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → GitHub Actions runners | CLI version bump changes which Docker image runs in CI; no new network surface opened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | Supabase CLI version bump introduces breaking changes to EF APIs | accept | Research verified (18-RESEARCH.md § Q1): edge-runtime v1.73.x → v1.74.0 release notes show only bug fixes (tokio-eld, HTTP client timeouts, URL handling) — no breaking changes to EF APIs or behavior. The full integration suite running green after the bump confirms no regressions. |
| T-18-04 | Tampering | CI workflow modified beyond version pin | mitigate | `git diff .github/workflows/ci.yml` confirms only the two `version:` lines changed. No other CI step structure, `env:` blocks, or `PGOPTIONS` guard is altered. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Supabase CLI is a GitHub Actions action, not an npm package. |
</threat_model>

<verification>
After the task completes:
1. `git diff .github/workflows/ci.yml` shows exactly two changed lines (both `version: 2.92.1` → the new pin)
2. `npm run test:integration` exits 0 with full suite green locally (0 failures, 0 skips)
3. After pushing the branch: CI `test-integration` job passes on GitHub Actions (manual confirmation per VALIDATION.md)
</verification>

<success_criteria>
- Both `supabase/setup-cli@v2` steps in `.github/workflows/ci.yml` pinned to the same stable version bundling edge-runtime >= v1.74.0
- `npm run test:integration` exits 0 with 0 failures, 0 skips on local stack
- CI `test-integration` job passes on the new CLI version (confirmed after push)
</success_criteria>

<output>
Create `.planning/phases/18-test-environment-repair/18-02-SUMMARY.md` when done
</output>
