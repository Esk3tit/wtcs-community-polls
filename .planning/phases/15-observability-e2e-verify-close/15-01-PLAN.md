---
phase: 15-observability-e2e-verify-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/[__smoke].tsx
autonomous: true
requirements: [OBSV-03, OBSV-05]
tags: [observability, sentry, smoke-harness, react-19]

must_haves:
  truths:
    - "Visiting /__smoke?fire=render in a non-production build throws a render-phase Error with message literal 'OBSV-03 render'"
    - "Visiting /__smoke?fire=dedupe in a non-production build throws a render-phase Error with message literal 'OBSV-05 dedupe'"
    - "Production build still returns 404 for /__smoke (existing VITE_NETLIFY_CONTEXT prod-gate untouched)"
    - "After either throw is caught by Sentry.ErrorBoundary, Sentry.lastEventId() is surfaced both via console.log AND on document.body.dataset.sentryEventId"
    - "The two scenarios fire DISTINCT error messages so Sentry.dedupeIntegration() does not collapse the second event"
  artifacts:
    - path: "src/routes/[__smoke].tsx"
      provides: "Extended SmokeSearch validator + branching render that dispatches to render-throw or dedupe-check sub-smoke"
      contains: "fire?: 'render' | 'dedupe'"
  key_links:
    - from: "src/routes/[__smoke].tsx"
      to: "src/components/debug/RenderThrowSmoke.tsx"
      via: "lazy import (existing pattern, extended for second message)"
      pattern: "lazy\\(\\(\\) => import\\('@/components/debug/"
    - from: "src/routes/[__smoke].tsx"
      to: "document.body.dataset.sentryEventId"
      via: "useEffect after ErrorBoundary catches"
      pattern: "dataset\\.sentryEventId"
---

<objective>
Extend the existing prod-gated `/__smoke` route with two distinct render-phase throw triggers (`?fire=render` and `?fire=dedupe`) so OBSV-05's Dedupe smoke can fire DISTINCT error messages per scenario, and surface `Sentry.lastEventId()` via both `console.log` (human verifier) and `document.body.dataset.sentryEventId` (future automation).

Purpose: Provide the smoke verifier the two triggers it needs to (a) capture an OBSV-03 render-phase event with `boundary: app-root` tag, and (b) confirm that `Sentry.dedupeIntegration()` does NOT collapse a second event when the message differs.

Output: Modified `src/routes/[__smoke].tsx` with extended search validator + branching render. Existing `src/components/debug/RenderThrowSmoke.tsx` either extended to accept a `message` prop OR a sibling `DedupeCheckSmoke` component added — executor decides per file-count minimization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-observability-e2e-verify-close/15-CONTEXT.md
@.planning/phases/15-observability-e2e-verify-close/15-RESEARCH.md
@.planning/phases/15-observability-e2e-verify-close/15-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Existing route shape (from 15-PATTERNS.md analog block). Extend in place. -->
<!-- Current SmokeSearch interface: { render?: '1' } — extend to add `fire?: 'render' | 'dedupe'`. -->
<!-- Existing throw component signature (from src/components/debug/RenderThrowSmoke.tsx): -->
<!--   export function RenderThrowSmoke(): never -->
<!-- It throws `new Error('RenderThrowSmoke: deliberate render-phase throw for Sentry verification')`. -->
<!-- Two viable executor choices for the two distinct messages: -->
<!--   (a) Extend signature to `export function RenderThrowSmoke({ message }: { message: string }): never` -->
<!--   (b) Add sibling component `export function DedupeCheckSmoke(): never` with fixed `'OBSV-05 dedupe'` message -->
<!-- Either is acceptable; pick whichever keeps the component count smallest and the `import type` boilerplate smallest. -->
<!-- Sentry surface (from src/main.tsx): Sentry.lastEventId() returns string | undefined. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend SmokeSearch validator + branch render to two distinct throw messages</name>
  <files>src/routes/[__smoke].tsx, src/components/debug/RenderThrowSmoke.tsx</files>
  <read_first>
    - src/routes/[__smoke].tsx (current SmokeSearch validator + beforeLoad prod-gate + SmokePage component — extend in place; do NOT replace the file)
    - src/components/debug/RenderThrowSmoke.tsx (existing render-phase throw component — see WHY-only comment header that the executor must match in style)
    - src/main.tsx (Sentry init site — reference only; do NOT modify in this task; understand where `boundary: app-root` tag is set and where `Sentry.dedupeIntegration()` is pinned)
    - .planning/phases/15-observability-e2e-verify-close/15-PATTERNS.md `src/routes/[__smoke].tsx (MOD ...)` section (full divergence guide; executor follows the "two-button shape, single route" pattern under D-02)
    - .planning/phases/15-observability-e2e-verify-close/15-RESEARCH.md ## Architecture Patterns Pattern 1 (sub-route vs query-param vs in-page button tradeoff)
    - ./CLAUDE.md ## Conventions § Comments (WHY-only in src/; no plan/phase IDs)
  </read_first>
  <action>
    Extend the existing prod-gated `/__smoke` route to add a second render-phase throw trigger discriminated by a query parameter. Required concrete changes per D-01/D-02/D-04:

    Add a `fire?: 'render' | 'dedupe'` field to the `SmokeSearch` interface. Extend the existing `validateSearch` arrow to accept and validate `fire` alongside the existing `render` field; reject unknown values by returning the empty `SmokeSearch`. Preserve the existing `validateSearch` shape for the `render` field unchanged.

    Preserve the existing `beforeLoad` prod-gate (`VITE_NETLIFY_CONTEXT === 'production'` → `throw notFound()`) verbatim. Do NOT introduce any new gate, admin guard, or auth check.

    In `SmokePage`, read both `render` and `fire` via `Route.useSearch()`. Branch on `fire`:
    - `fire === 'render'`: render a component that throws `new Error('OBSV-03 render')`
    - `fire === 'dedupe'`: render a component that throws `new Error('OBSV-05 dedupe')`
    - else (neither `fire` nor `render=1` set): keep the existing instructional `<p>` message; extend its copy to mention `?fire=render` and `?fire=dedupe` alongside `?render=1`.

    Preserve the existing `?render=1` branch (lazy-loaded `RenderThrowSmoke` with its default message) untouched so the existing canary still works.

    For the new branches, the executor picks ONE of two shapes (both research-supported in 15-PATTERNS.md Divergence block):
    - Shape A: extend `RenderThrowSmoke` to `export function RenderThrowSmoke({ message }: { message: string }): never` and pass distinct messages from each branch. Update existing `?render=1` call site to pass its current literal message string explicitly.
    - Shape B: add a sibling file `src/components/debug/DedupeCheckSmoke.tsx` exporting `function DedupeCheckSmoke(): never` that throws `new Error('OBSV-05 dedupe')`, and add a second tiny component (e.g., inline factory or a second sibling file `OBSV03RenderSmoke.tsx`) for the `'OBSV-03 render'` message. Leave existing `RenderThrowSmoke.tsx` untouched.

    Whichever shape is picked, both new render-paths MUST go through `lazy(() => import('...'))` + `Suspense` to mirror the existing pattern (lines 9-13, 71-75 of the current file).

    Comments must be WHY-only and must NOT cite "Phase 15", "OBSV-03", "OBSV-05", "D-01", "D-02", "D-03", or any plan/phase/decision identifier in `src/`. Permissible WHY explanations: "two distinct messages so Dedupe doesn't collapse the second event"; "second trigger lives at a query-param-discriminated URL so screenshot evidence captures which scenario fired".

    The route uses `verbatimModuleSyntax: true` and `erasableSyntaxOnly: true` per tsconfig.app.json. Any type-only imports MUST use `import type`.

    Do NOT touch `src/main.tsx`. Do NOT touch `vite.config.ts`. Do NOT introduce any new dependency. Do NOT add a `?debug=sentry-test` query-param gate (D-01 explicitly reframes onto the existing route).
  </action>
  <acceptance_criteria>
    - `src/routes/[__smoke].tsx` `SmokeSearch` interface contains a literal `fire?: 'render' | 'dedupe'` field declaration (grep: `grep -n "fire\\?:" src/routes/\\[__smoke\\].tsx` returns at least one match)
    - `src/routes/[__smoke].tsx` validateSearch accepts both `'render'` and `'dedupe'` as valid `fire` values
    - Production-gate `if (import.meta.env.VITE_NETLIFY_CONTEXT === 'production')` block is present and unchanged from current code
    - Two distinct literal error message strings appear under `src/components/debug/` and/or `src/routes/[__smoke].tsx`: `'OBSV-03 render'` AND `'OBSV-05 dedupe'`. Grep: `grep -rn "OBSV-03 render\\|OBSV-05 dedupe" src/` returns at least 2 matches across the two strings
    - `npm run lint` exits 0 — no new ESLint violations
    - `tsr generate` produces a `src/routeTree.gen.ts` that compiles under `tsc -b` (i.e., `npm run build` exits 0)
    - `grep -nE "Phase 15|OBSV-0[345]|D-0[1-9]|D-1[0-6]" src/routes/\\[__smoke\\].tsx src/components/debug/*.tsx` returns ZERO matches (no plan/phase/decision-ID archaeology in src/)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; ! grep -rnE "Phase 15|OBSV-0[345]|D-0[1-9]|D-1[0-6]" src/routes/ src/components/debug/</automated>
  </verify>
  <done>
    Route module declares `fire?: 'render' | 'dedupe'` in SmokeSearch; validateSearch admits both literal values; SmokePage renders the appropriate throw component based on `fire`; both `'OBSV-03 render'` and `'OBSV-05 dedupe'` literal strings exist in source under `src/`; existing `?render=1` canary path still works; production-gate untouched; src/ comments contain no plan/phase/decision-ID references.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface Sentry.lastEventId() via console.log + document.body.dataset.sentryEventId</name>
  <files>src/routes/[__smoke].tsx</files>
  <read_first>
    - src/routes/[__smoke].tsx (now in its Task-1-extended form — the `SmokePage` function and the new branches landed in Task 1)
    - src/main.tsx (Sentry init — confirm `Sentry.lastEventId()` is exported and that `onError` inside `Sentry.ErrorBoundary` already receives an `eventId` third arg; this task does NOT modify main.tsx but must be informed by its current shape)
    - src/components/AppErrorFallback.tsx (existing fallback rendered after the throw is caught — relevant context for WHERE the dataset write becomes observable in the DOM)
    - .planning/phases/15-observability-e2e-verify-close/15-PATTERNS.md `src/routes/[__smoke].tsx` § Divergence "Surface `Sentry.lastEventId()` two ways (D-03)"
    - .planning/phases/15-observability-e2e-verify-close/15-RESEARCH.md ## Architecture Patterns Pattern 2 (DOM dataset surface)
    - ./CLAUDE.md ## Code Style § Logging (no committed `console.log` left in app code — Task requires intentional exception with eslint-disable)
  </read_first>
  <action>
    Inside `SmokePage` (NOT inside the throw components — those never return), add a `useEffect` that, when `fire` is `'render'` or `'dedupe'`, reads `Sentry.lastEventId()` after the throw is caught by `Sentry.ErrorBoundary` and surfaces it both:
    - via `console.log(...)` — intentional human-verifier surface per D-03; wrap with an inline `// eslint-disable-next-line no-console` directive and a WHY comment ("intentional eventId surface for smoke verifier")
    - via `document.body.dataset.sentryEventId = <id>` — writes the literal lastEventId string to the DOM so a future Playwright spec can read it; if `Sentry.lastEventId()` returns `undefined`, write the empty string (NOT `'undefined'`)

    The effect must run AFTER the boundary catches and re-renders the fallback. Two viable shapes per 15-PATTERNS.md:
    - Shape A: effect runs in `SmokePage` itself on mount; since the throw happens during render of the inner component and is caught by the surrounding ErrorBoundary (defined in `src/main.tsx`), `SmokePage` re-renders with the fallback. The effect ID-write fires on that re-render. Use `requestAnimationFrame` or `queueMicrotask` to defer the read to the next tick so the boundary's `onError` callback (which calls `Sentry.captureException`) has run.
    - Shape B: write the dataset attribute from inside the ErrorBoundary's `onError` callback in `src/main.tsx`. This is more surgical (one write site) but spreads smoke-test concerns into the production error path. Rejected by 15-PATTERNS.md unless executor has strong reason — prefer Shape A.

    Use Shape A. Do NOT modify `src/main.tsx`.

    The Sentry import goes at the top of `src/routes/[__smoke].tsx`: `import * as Sentry from '@sentry/react'` (mirrors `src/main.tsx` line 2 conventions).

    Comments must be WHY-only and must NOT cite Phase 15, OBSV-NN, or D-NN.

    Do NOT alter the existing throw-component lazy imports from Task 1. Do NOT introduce a new dependency.
  </action>
  <acceptance_criteria>
    - `src/routes/[__smoke].tsx` imports `Sentry` from `@sentry/react` (grep: `grep -n "from '@sentry/react'" src/routes/\\[__smoke\\].tsx` returns one match)
    - The file contains both literal patterns: `Sentry.lastEventId()` AND `document.body.dataset.sentryEventId` (grep: `grep -c "Sentry.lastEventId\\|dataset.sentryEventId" src/routes/\\[__smoke\\].tsx` returns at least 2)
    - At least one `// eslint-disable-next-line no-console` directive appears in `src/routes/[__smoke].tsx` with an adjacent WHY comment (the executor's WHY phrasing — but a comment line touching the disable directive must exist)
    - `npm run lint` exits 0 — the disable directive scope is correct
    - `tsc -b` exits 0 — no type errors from the Sentry import or the `useEffect` body
    - `grep -nE "Phase 15|OBSV-0[345]|D-0[1-9]|D-1[0-6]" src/routes/\\[__smoke\\].tsx` returns ZERO matches
    - When the executor runs `npm run dev` and visits `http://localhost:5173/__smoke?fire=render` in a browser, opening DevTools shows a `console.log` line printing a Sentry event ID string (validator visual check during execution; automated verification is the next task in Plan 04)
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build &amp;&amp; grep -c "dataset.sentryEventId" src/routes/\[__smoke\].tsx</automated>
  </verify>
  <done>
    `SmokePage` has a `useEffect` that, when `fire` is set, writes the Sentry lastEventId both to the console (with eslint-disable + WHY comment) and to `document.body.dataset.sentryEventId`. No modifications to `src/main.tsx`. No plan/phase/decision-ID archaeology. Build and lint both green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `/__smoke` route | Already enforced via `beforeLoad` prod-gate (`VITE_NETLIFY_CONTEXT === 'production'` → `throw notFound()`). Phase 15 does NOT introduce a new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | `/__smoke` smoke route exposing render-throw triggers | mitigate | Existing `beforeLoad` returns 404 in production builds (line 23-27 of current file). Plan 01 preserves this gate verbatim — does NOT add the `?fire` parameter to a production-accessible surface. |
| T-15-02 | Tampering | DOM `data-sentry-event-id` attribute readable to any script on the page | accept | The route is prod-gated; in dev/preview contexts, no third-party scripts run that we don't ship; in production the route returns 404 so no DOM attribute is written. The attribute carries a Sentry event ID (not PII, not a session token). |

No new package installs in this plan; package-supply-chain threat (T-15-SC) does not apply.
</threat_model>

<verification>
- `npm run lint` exits 0
- `npm run build` exits 0 (tsr generate + tsc -b + vite build all clean)
- `grep -rnE "Phase 15|OBSV-0[345]|D-0[1-9]|D-1[0-6]" src/routes/ src/components/debug/` returns ZERO matches
- Manual: `npm run dev`, visit `/__smoke?fire=render`, see the `RenderThrowSmoke` boundary fallback + DevTools `console.log` line carrying an event ID + `document.body` has `data-sentry-event-id="..."` attribute set
- Manual: `npm run dev`, visit `/__smoke?fire=dedupe`, same surface with a DIFFERENT event ID
</verification>

<success_criteria>
1. Two distinct render-phase throw triggers exist on the `/__smoke` route, discriminated by `?fire=render` and `?fire=dedupe`
2. Each trigger fires a render-phase `Error` with a literal, distinct message string (`'OBSV-03 render'` vs `'OBSV-05 dedupe'`)
3. After the throw is caught, `Sentry.lastEventId()` is surfaced both via `console.log` and via `document.body.dataset.sentryEventId`
4. Existing `VITE_NETLIFY_CONTEXT` production gate is unchanged; production builds still 404 on `/__smoke`
5. Existing `?render=1` legacy canary path still works
6. `src/` source contains no Phase/OBSV/D-NN identifiers (WHY-only comments)
</success_criteria>

<output>
Create `.planning/phases/15-observability-e2e-verify-close/15-01-SUMMARY.md` when done. List the two literal error-message strings landed, the two surfaces of `lastEventId`, and confirm the prod-gate is unchanged.
</output>
