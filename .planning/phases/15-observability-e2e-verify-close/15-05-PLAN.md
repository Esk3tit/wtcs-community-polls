---
phase: 15-observability-e2e-verify-close
plan: 05
type: execute
wave: 4
depends_on: ['15-04']
files_modified:
  - .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OBSV-03, OBSV-04, OBSV-05, TEST-14, TEST-15, TEST-16]
tags: [evidence-finalize, issue-closure, github, post-merge]

must_haves:
  truths:
    - "`.planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md` exists with `status: closed` in frontmatter and `ci_run_url` pointing to the POST-MERGE CI run on `main`"
    - "The Phase 15 PR has been merged into `main`"
    - "GitHub issues #11, #12, #13, #17, #19 are all in `closed` state"
    - "Each of the five closed issues has a closure comment linking to `15-EVIDENCE.md` at the specific section anchor for that requirement"
    - "The seven artifacts/*.png screenshots are committed (not just staged) on the post-merge `main` (4 Sentry + 3 CI — includes the new sentry-obsv-05-counts.png per-message-count screenshot from Plan 04)"
    - "STATE.md and ROADMAP.md reflect Phase 15 shipped (5/5 plans complete)"
  artifacts:
    - path: ".planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md"
      provides: "Finalized phase evidence with post-merge CI run URL and `status: closed`"
    - path: ".planning/phases/15-observability-e2e-verify-close/artifacts/"
      provides: "Seven committed PNG screenshots (4 Sentry + 3 CI) on the merged main branch"
  key_links:
    - from: "15-EVIDENCE.md"
      to: "GitHub issues #11, #12, #13, #17, #19 closure comments"
      via: "operator-run `gh issue comment <N>`"
      pattern: "gh issue (comment|close)"
    - from: "Phase 15 PR merge"
      to: "Issue closure"
      via: "GitHub auto-close keywords in PR body"
      pattern: "Closes #"
---

<objective>
Finalize the Phase 15 evidence trail and close the five GitHub issues:

1. Merge the Phase 15 PR. The PR body already carries `Closes #11, Closes #12, Closes #13, Closes #17, Closes #19` auto-close keywords (set up in Plan 04 Task 1) — merging auto-closes all five issues.
2. After merge, re-capture the post-merge CI run URL on `main` per D-12 (the pre-merge URL captured in Plan 04 is replaced).
3. Rename `15-EVIDENCE-DRAFT.md` → `15-EVIDENCE.md`, update frontmatter `status: closed` + `ci_run_url: <post-merge URL>`, and commit alongside the 6 PNG artifacts.
4. Operator runs `gh issue comment <N>` on each of the five issues, linking to the specific section anchor in the finalized `15-EVIDENCE.md` on `main`. (Auto-close already happened; the comment is the durable evidence pointer per D-16.)
5. Update STATE.md and ROADMAP.md to reflect Phase 15 shipped.

Purpose: Lock the verify-and-close phase. Once Plan 05 completes, every Phase 15 success criterion is satisfied AND every referenced GitHub issue has a permanent evidence anchor.

Output: Finalized `15-EVIDENCE.md`, six committed PNGs on `main`, five `closed` GitHub issues with closure comments, STATE/ROADMAP updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15-observability-e2e-verify-close/15-CONTEXT.md
@.planning/phases/15-observability-e2e-verify-close/15-PATTERNS.md
@.planning/phases/15-observability-e2e-verify-close/15-VALIDATION.md
@./CLAUDE.md
@.planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md

<interfaces>
<!-- D-12 freshness rule: the cited CI run URL MUST be the POST-merge run on main, NOT the pre-merge PR run. -->
<!-- D-16 closure mechanism: -->
<!--   (i) PR body auto-close keywords transition issues to closed on merge -->
<!--   (ii) per-issue closure comments add a durable evidence pointer (operator-run gh issue comment) -->
<!-- 15-PATTERNS.md "Per-issue closure comment after merge" — no analog helper script; manual gh issue comment per issue. -->
<!-- The post-merge CI run completes within ~5-10 minutes of merge; do NOT proceed to Task 3 until it is green. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator commits draft EVIDENCE + artifacts to the PR branch, requests review, and merges</name>
  <what-built>
    Plan 04 produced six PNG artifacts and a draft EVIDENCE file. The PR already carries auto-close keywords from Plan 04 Task 1. Plan 05 Task 1 commits the evidence to the PR branch and proceeds with the merge.
  </what-built>
  <how-to-verify>
    Operator runs the following steps.

    Step 1 — Commit evidence to the PR branch:
    (a) From the repo root: `git add .planning/phases/15-observability-e2e-verify-close/artifacts/ .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md`
    (b) Verify the staged set: `git status`. The six PNGs + the draft EVIDENCE file should be staged. No other files should be staged in this commit.
    (c) Commit: `git commit -m "docs(15): capture phase evidence — Sentry events + CI pass lines + sentry-cli output"`
    (d) Push: `git push`.

    Step 2 — Verify the PR is ready to merge:
    (a) Run `gh pr view --json mergeable,statusCheckRollup,reviewDecision`. Confirm: `mergeable: MERGEABLE`, latest CI run conclusion = success on all jobs (`lint-and-unit` AND `e2e`), and review (if required) is approved or branch protection rules permit merge.
    (b) If the PR is still in draft state, mark it ready for review: `gh pr ready`.
    (c) Confirm the PR body still includes the five auto-close keywords (`gh pr view --json body --jq '.body' | grep -cE "Closes #(11|12|13|17|19)"` should return 5).

    Step 3 — Merge:
    (a) Run `gh pr merge --merge` (matches the repo convention from recent PR #30). If the repo uses squash or rebase merges, use the matching flag.
    (b) Verify merge succeeded: `gh pr view --json mergedAt --jq '.mergedAt'` returns a non-null timestamp.
    (c) Verify the five GitHub issues are now `closed`:
       `for N in 11 12 13 17 19; do echo "Issue #$N: $(gh issue view $N --json state --jq '.state')"; done`
       Expected: five lines each ending in `CLOSED`.

    If auto-close DID NOT fire (any issue still `OPEN`):
    - Check the PR body for the exact `Closes #N` keyword grammar (no typos).
    - Fallback: manually close the still-open issue with `gh issue close <N>`.
  </how-to-verify>
  <resume-signal>
    Type "merged" with the merge commit SHA and confirm all five issues show closed. If any issue remained open after merge, type "merged-with-fallback: #<N>" and proceed (Task 3 still runs).
  </resume-signal>
  <files>(none directly — operator commits Plan 04 artifacts via gh CLI; no planner-tracked file changes in this checkpoint)</files>
  <action>OPERATOR CHECKPOINT — see how-to-verify above for the full step list. Operator stages the six PNGs + 15-EVIDENCE-DRAFT.md, commits, pushes, marks the PR ready for review, then runs `gh pr merge`. After merge, verifies all five GitHub issues transitioned to closed state via the auto-close keywords already in the PR body from Plan 04 Task 1.</action>
  <verify>
    <human-check>Operator confirms merge commit SHA + all five issues report state CLOSED (no longer OPEN)</human-check>
  </verify>
  <done>Phase 15 PR merged into main. All five GitHub issues (#11, #12, #13, #17, #19) are in closed state (either via auto-close keywords or via fallback gh issue close).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture post-merge CI run URL on main and rename DRAFT → final EVIDENCE</name>
  <what-built>
    After PR merge to main, GitHub Actions automatically runs the CI workflow on the new main head. This post-merge CI run is the canonical TEST-14/15/16 evidence per D-12. The pre-merge URL in 15-EVIDENCE-DRAFT.md is replaced with the post-merge URL in 15-EVIDENCE.md.
  </what-built>
  <how-to-verify>
    Operator runs the following.

    Step 1 — Wait for the post-merge CI run on main to complete:
    (a) `gh run watch` (auto-watches the latest run on the default branch) OR `gh run list --branch main --limit 3 --json status,conclusion,databaseId,url,headSha,workflowName` and poll until the top entry has `status: completed`.
    (b) Confirm `conclusion: success`. If `failure`, STOP and investigate.
    (c) Capture the post-merge run URL — emit in chat. This becomes the new ci_run_url.
    (d) Capture the post-merge headSha — confirm it matches the merge commit from Task 1 Step 3.

    Step 2 — Pull main, rename and finalize EVIDENCE.md:
    (a) `git checkout main && git pull`
    (b) `git mv .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE-DRAFT.md .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md`
    (c) Edit 15-EVIDENCE.md:
       - Frontmatter: change `status: draft-pending-merge` → `status: closed`
       - Frontmatter: change `ci_run_url:` value to the post-merge URL from Step 1
       - Frontmatter: add `merged_at: <ISO timestamp>` and `merge_commit: <SHA>`
       - Body sections TEST-14/15/16: replace the "PRE-MERGE — Plan 05 replaces with POST-MERGE URL" flag text with the post-merge URL
       - Body: add a "Final state" closing section noting all five issues are closed (capture each issue's closedAt timestamp via `gh issue view <N> --json closedAt --jq '.closedAt'`)
    (d) Commit on main: `git add .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md && git commit -m "docs(15): finalize phase evidence with post-merge CI run URL" && git push`
       (If repo branch protection forbids direct push to main, open a small follow-up PR titled "docs(15): finalize Phase 15 evidence" — auto-close keywords NOT needed since all issues are already closed.)

    Step 3 — Sanity check:
    (a) `grep -E "status: closed|ci_run_url:" .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md` returns both lines, with the URL pointing to the POST-merge run.
    (b) `! grep -F "draft-pending-merge" .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md` succeeds (no draft markers remaining).
  </how-to-verify>
  <resume-signal>
    Type "finalized" with: (a) post-merge CI run URL, (b) merge commit SHA, (c) confirmation that 15-EVIDENCE.md exists on main with status: closed and the new URL. If branch protection blocked direct push, type "finalized-via-pr: <PR URL>".
  </resume-signal>
  <files>.planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md</files>
  <action>OPERATOR CHECKPOINT — see how-to-verify above for the full step list. Operator waits for the post-merge CI run on main to complete green, captures the new run URL, then renames 15-EVIDENCE-DRAFT.md to 15-EVIDENCE.md (via git mv), updates frontmatter (status: closed, ci_run_url to post-merge URL, merged_at, merge_commit), replaces pre-merge URL references in body sections, and commits on main (or via small follow-up PR if branch protection forbids direct push).</action>
  <verify>
    <automated>test -f .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md &amp;&amp; grep -q 'status: closed' .planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md</automated>
    <human-check>Operator confirms post-merge CI run URL is in EVIDENCE.md and no draft markers remain</human-check>
  </verify>
  <done>15-EVIDENCE.md exists on main with status: closed and post-merge ci_run_url. 15-EVIDENCE-DRAFT.md no longer exists. Frontmatter contains merged_at and merge_commit fields.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator posts per-issue closure comments linking to evidence anchors</name>
  <what-built>
    All five issues are already in closed state (auto-closed on PR merge). Per D-16, each closed issue gets ONE follow-up comment that links to the specific section anchor in 15-EVIDENCE.md on main. This is the durable evidence pointer for future readers.
  </what-built>
  <how-to-verify>
    Operator runs five `gh issue comment` invocations, one per closed issue.

    The base URL for evidence anchors is the GitHub blob URL for the file on main:
    `https://github.com/Esk3tit/wtcs-community-polls/blob/main/.planning/phases/15-observability-e2e-verify-close/15-EVIDENCE.md`

    Section anchors are GitHub's deterministic markdown slugs derived from headings. Verify the exact slug GitHub renders by opening the EVIDENCE file URL and clicking each heading — the URL bar shows the canonical anchor (lowercase, spaces → -, punctuation stripped except -, collapsed multiple -).

    Expected approximate anchors (verify before pasting):
    - `#obsv-03--sentry-react-19-errorboundary-render-phase-capture`
    - `#obsv-04--viterolldown-sourcemap-function-name-preservation`
    - `#obsv-05--dedupe-integration-triple-handler-collapse-with-distinct-messages`
    - `#test-14--admin-createspects-passes-in-ci-on-main`
    - `#test-15--browse-respondspects-passes-in-ci-on-main`
    - `#test-16--filter-searchspects-passes-in-ci-on-main`

    Step 1 — Issue #17 (OBSV-03 + OBSV-05):
    `gh issue comment 17 --body "Closed by Phase 15 PR — see evidence: <base-url>#<obsv-03-anchor> (OBSV-03) and <base-url>#<obsv-05-anchor> (OBSV-05)."`

    Step 2 — Issue #19 (OBSV-04):
    `gh issue comment 19 --body "Closed by Phase 15 PR — see evidence: <base-url>#<obsv-04-anchor>"`

    Step 3 — Issue #11 (TEST-14):
    `gh issue comment 11 --body "Closed by Phase 15 PR — see evidence: <base-url>#<test-14-anchor>"`

    Step 4 — Issue #12 (TEST-15):
    `gh issue comment 12 --body "Closed by Phase 15 PR — see evidence: <base-url>#<test-15-anchor>"`

    Step 5 — Issue #13 (TEST-16):
    `gh issue comment 13 --body "Closed by Phase 15 PR — see evidence: <base-url>#<test-16-anchor>"`

    Step 6 — Sanity check:
    (a) For each of the five issues: `gh issue view <N> --json comments --jq '.comments[-1].body'` returns the closure-comment body just posted.
    (b) Click each link in the comment to confirm it resolves to the correct section on main. If a slug is wrong, edit the comment via `gh issue comment <N> --edit-last --body "..."`.

    NOTE: If any issue remained open after PR merge (Task 1 Step 3 fallback), use `gh issue close <N> --comment "..."` for that issue instead of `gh issue comment <N>`. This both closes the issue and posts the comment in one call.
  </how-to-verify>
  <resume-signal>
    Type "closure-comments-posted" with confirmation that all five issues now have a closure comment with a working anchor link. If any anchor was wrong and you edited it, note the issue numbers that were edited.
  </resume-signal>
  <files>(none — gh CLI invocations only; comments live on GitHub, not in the repo)</files>
  <action>OPERATOR CHECKPOINT — see how-to-verify above for the full step list. Operator runs five gh issue comment invocations (one per closed issue: #11, #12, #13, #17, #19), each linking to the specific section anchor in 15-EVIDENCE.md on main. Operator clicks each link to verify it resolves to the correct section.</action>
  <verify>
    <human-check>Operator confirms all five closure comments exist with working anchor links resolving to the correct sections of 15-EVIDENCE.md</human-check>
  </verify>
  <done>Five GitHub closure comments posted on issues #11, #12, #13, #17, #19. Each comment's anchor link resolves to the correct section of 15-EVIDENCE.md on main.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update STATE.md to mark Phase 15 complete and update ROADMAP.md progress table</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (Current Position section — needs to flip Phase 14 → 15 of 17 next; mark Phase 15 complete; increment progress counters)
    - .planning/ROADMAP.md (§ Phase 15 entry; § Progress table at bottom; § milestone summary table at bottom)
    - All Plan files for Phase 15 (15-01 through 15-05) — confirm the count is 5
    - .planning/REQUIREMENTS.md (Traceability table at bottom — six rows to flip from Pending → Complete)
  </read_first>
  <action>
    Update `.planning/STATE.md`:
    - Change `Phase: 14 of 17 COMPLETE → 15 of 17 next` to `Phase: 15 of 17 COMPLETE → 16 of 17 next`
    - Add a "Phase 15 complete" entry under Decisions describing: 5 plans shipped, 5 issues closed (#11, #12, #13, #17, #19), evidence in 15-EVIDENCE.md
    - **`progress.completed_phases` increment must be read-then-write (cross-AI replan, CR Finding 11):** do NOT hardcode `from 1 → 2`. Read the CURRENT integer value from `.planning/STATE.md` frontmatter, validate it's a non-negative integer (`Number.isInteger(n) && n >= 0`), increment by 1, and write the new value. Concrete shell form:
      ```bash
      CURRENT=$(grep -E "^\s*completed_phases:" .planning/STATE.md | head -1 | awk '{print $2}')
      [[ "$CURRENT" =~ ^[0-9]+$ ]] || { echo "ERROR: completed_phases is not a non-negative integer: '$CURRENT'"; exit 1; }
      NEW=$((CURRENT + 1))
      # Then edit the frontmatter line in-place to set completed_phases to $NEW.
      ```
      This guards against drift where another phase (e.g., Phase 14) already incremented the counter and the hardcoded `1 → 2` would silently overwrite Phase 14's increment with a stale value.
    - Update frontmatter `progress.total_plans` and `progress.completed_plans` to reflect 5 new plans (total 6, completed 6)
    - Update `progress.percent` to 50 (2/4 v1.3 phases complete)
    - Update `last_updated` and `last_activity` lines with current date
    - Update `stopped_at` to "Phase 15 shipped"
    - Add a "By Phase" row for Phase 15 in the Performance Metrics table

    Update `.planning/ROADMAP.md`:
    - Phase 15 entry: prepend `[x]` to the `- [ ] **Phase 15: ...**` list item under the v1.3 section header
    - Phase 15 entry under "## Phase Details": below the `**Plans**: 5 plans` line + plan list, add a `**COMPLETED <ISO date>** — ...` summary line mirroring Phase 14's style (one paragraph summarizing: smoke harness landed at /__smoke with two distinct triggers, verify-sourcemap-names.mjs script + CI lint-and-unit step landed, 6 PNGs + 15-EVIDENCE.md committed, all 5 GitHub issues #11/#12/#13/#17/#19 closed with per-issue closure comments)
    - Mark all 5 plan checkboxes as `[x]` in the Plans list
    - Update the Progress table row: `| 15. Observability + E2E Verify & Close | 5/5 | Shipped | <date> |`
    - Update the milestone summary table at the bottom: v1.3 row Plans count incremented; status string stays "🔄 In progress" until all 4 phases ship

    Update `.planning/REQUIREMENTS.md` Traceability table:
    - Flip 6 rows from `Pending` → `Complete`: OBSV-03, OBSV-04, OBSV-05, TEST-14, TEST-15, TEST-16

    Commit (on main, or via follow-up PR if branch protection):
    `git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md && git commit -m "docs(15): close phase 15 — update state, roadmap, requirements traceability"`
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter `progress.completed_phases` is incremented by exactly 1 from its prior value (read-then-write, NOT hardcoded — cross-AI replan, CR Finding 11). Verify: `git show HEAD~1:.planning/STATE.md | grep -E "^\s*completed_phases:" | awk '{print $2}'` returns N, and `grep -E "^\s*completed_phases:" .planning/STATE.md | awk '{print $2}'` returns N+1.
    - `.planning/STATE.md` `## Current Position` section shows `Phase: 15 of 17 COMPLETE` (or equivalent text indicating Phase 15 is done)
    - `.planning/ROADMAP.md` Phase 15 list item is checked: `- [x] **Phase 15:`
    - `.planning/ROADMAP.md` Phase 15 detail section has a `**COMPLETED ...** —` summary line
    - `.planning/ROADMAP.md` Progress table shows Phase 15 as `5/5` and `Shipped`
    - `.planning/REQUIREMENTS.md` Traceability table shows OBSV-03/04/05 + TEST-14/15/16 all as `Complete`
    - The commit message follows the existing convention (`docs(15):` prefix matches Phase 14's `docs(14):` pattern)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Phase 15.*Shipped\\|Phase 15.*5/5" .planning/ROADMAP.md &amp;&amp; grep -c "OBSV-03.*Complete\\|OBSV-04.*Complete\\|OBSV-05.*Complete\\|TEST-14.*Complete\\|TEST-15.*Complete\\|TEST-16.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>STATE.md reflects Phase 15 complete; ROADMAP.md Phase 15 entry shows 5/5 plans shipped with completion date; Progress table updated; REQUIREMENTS.md Traceability table shows 6 rows flipped Pending → Complete. Commit landed on main.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's GitHub credentials → gh CLI commands (`gh pr merge`, `gh issue comment`, `gh issue close`) | Operator-owned credentials; no shared service tokens. |
| Direct commit to `main` (Task 2 + Task 4) | If branch protection forbids direct pushes, fallback to a small follow-up PR. Either path lands the same content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | Wrong issue closed via gh CLI typo | accept | Operator visually inspects `gh issue view <N>` output before closing; closure is reversible via `gh issue reopen <N>`. |
| T-15-11 | Repudiation | Phase 15 PR merged without all five auto-close keywords firing | mitigate | Task 1 Step 3 explicitly verifies each of the five issues is `closed` after merge; the fallback `gh issue close <N>` path closes any that didn't auto-close. |
| T-15-12 | Information Disclosure | A closure comment links to an EVIDENCE.md anchor that doesn't exist (slug mismatch) | mitigate | Task 3 Step 6(b) requires the operator to click each link and verify it resolves; corrects via `gh issue comment <N> --edit-last`. |

No new package installs in this plan; package-supply-chain threat T-15-SC does not apply.
</threat_model>

<verification>
- Phase 15 PR is merged into `main`
- `15-EVIDENCE.md` exists on `main` with `status: closed` and the post-merge CI run URL
- All five GitHub issues are `closed` with a closure comment containing a working evidence-anchor URL
- STATE.md and ROADMAP.md reflect Phase 15 complete
- Six PNG artifacts are committed (not just staged) on `main`
- REQUIREMENTS.md Traceability table flipped 6 rows Pending → Complete
</verification>

<success_criteria>
1. Phase 15 PR merged; auto-close fired for all five issues
2. Post-merge CI run URL captured and recorded in `15-EVIDENCE.md`
3. EVIDENCE file renamed from DRAFT, frontmatter updated to `status: closed`
4. Five closure comments posted (one per closed issue) with working evidence-anchor links
5. STATE.md and ROADMAP.md updated to reflect Phase 15 shipped
6. REQUIREMENTS.md Traceability table flipped Pending → Complete for OBSV-03/04/05 + TEST-14/15/16
</success_criteria>

<output>
Create `.planning/phases/15-observability-e2e-verify-close/15-05-SUMMARY.md` when done. Include: merge commit SHA, post-merge CI run URL, the five closed-issue links, and a one-line "all done" confirmation.
</output>
